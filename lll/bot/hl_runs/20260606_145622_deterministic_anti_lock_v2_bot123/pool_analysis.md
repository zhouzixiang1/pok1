# 更新对手池 70 手牌评测分析

## 评测口径

- 当前 bot：`bots\experiments\anti_lock_v2.py`
- 对手数量：`3`
- 每个对手：`10 场`，由 `5 组`换牌换庄镜像组成。
- 每场：`70 手牌`。
- 所有牌堆和本地随机种子固定，可复现。

## 总体结果

| 对手 | 镜像组胜-负-平 | 实际场胜-负-平 | 总筹码差 | 场均筹码差 | 中位数 | 波动标准差 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `bot1` | 4-1-0 | 8-2-0 | 7466.0 | 746.6 | 523.5 | 9417.5 |
| `bot2` | 4-1-0 | 8-2-0 | 30283.0 | 3028.3 | 1751.5 | 7963.8 |
| `bot3` | 5-0-0 | 8-2-0 | 38159.0 | 3815.9 | 1531.5 | 11946.3 |

## 合并统计

- 镜像组总成绩：`13-2-0`；实际场总成绩：`24-6-0`。
- 30 场总筹码差：`75908.0`；场均：`2530.3`；单场中位数：`748.5`；标准差：`9656.6`。
- 15 组镜像净值中位数：`3539.0`；当前 bot 异常动作 `0` 次，对手异常动作 `0` 次。
- 实验结论：`GREEN`。未发现崩溃或非法动作，收益指标非负。

## 稳定性和合法性

- `bot1`：当前 bot 异常动作 `0` 次，对手异常动作 `0` 次；单场最差 `-15098.0`，最好 `18632.0`。
- `bot2`：当前 bot 异常动作 `0` 次，对手异常动作 `0` 次；单场最差 `-11435.0`，最好 `20531.0`。
- `bot3`：当前 bot 异常动作 `0` 次，对手异常动作 `0` 次；单场最差 `-14680.0`，最好 `19194.0`。

## 可提升之处

- 样本共提取 `2100` 手牌结果，其中单手亏损达到 1000 或以上的样本 `14` 个。
- 大额亏损中，终局动作为全下或带 anti-lock 标记的样本 `4` 个；这部分应优先区分正常价值打光和反锁定例外。
- 大额亏损中，带连续 check 探测信号或 probe 分支的样本 `0` 个；只统计聚类，不据此直接削弱 `100` 小探测机制。
- anti-lock 终局线共 `27` 次，`21 胜 5 负`，净筹码 `78636.0`；整体收益为正，但仍需关注少数大额亏损。
- anti-lock 中估算胜率低于 40% 的 `7` 次净筹码 `-10986.0`；胜率不低于 40% 的 `20` 次净筹码 `89622.0`。优先继续验证低权益时的触发门槛和下注尺度，而不是删除 anti-lock 例外。
- `probe_raise` 终局线 `144` 次，净筹码 `40565.0`；`value_or_pressure_raise` 终局线 `158` 次，净筹码 `62663.0`。两条主进攻线整体为正，暂不应因少数失败样本整体收紧。
- 河牌非 anti-lock 大额亏损共 `4` 手，最差为对 `bot1` 的 `-15100.0`；应结合对手跟注范围和下注尺寸逐手复盘，不据单手改策略。
- 按总筹码差，当前压力最大的对手依次为：`bot1`（7466.0）、`bot2`（30283.0）、`bot3`（38159.0）。

## 高损失样本

- `bot1` 第 1 场/第 15 手：`-20000.0`，转牌，终局动作 `-2`（全下），分支 `anti_lock_pressure`，胜率 `0.5861`，成牌强度 `0.2304`，价值档 `strong`，底池 `25808`，需跟 `14190`，anti-lock `True`。
- `bot3` 第 2 场/第 9 手：`-20000.0`，转牌，终局动作 `-2`（全下），分支 `anti_lock_pressure`，胜率 `0.5255`，成牌强度 `0.2295`，价值档 `strong`，底池 `29232`，需跟 `10766`，anti-lock `True`。
- `bot3` 第 5 场/第 24 手：`-15743.0`，河牌，终局动作 `10020`（下注/加注），分支 `anti_lock_pressure`，胜率 `0.6082`，成牌强度 `0.4095`，价值档 `strong`，底池 `11446`，需跟 `0`，anti-lock `True`。
- `bot1` 第 3 场/第 10 手：`-15100.0`，河牌，终局动作 `11228`（下注/加注），分支 `value_or_pressure_raise`，胜率 `0.7699`，成牌强度 `0.5854`，价值档 `nut`，底池 `7744`，需跟 `0`，anti-lock `False`。
- `bot2` 第 6 场/第 33 手：`-11641.0`，河牌，终局动作 `0`（过牌/跟注），分支 `anti_lock_pressure`，胜率 `0.0399`，成牌强度 `0.0906`，价值档 `none`，底池 `14626`，需跟 `8656`，anti-lock `True`。
- `bot2` 第 9 场/第 11 手：`-1804.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.5218`，成牌强度 `0.2286`，价值档 `none`，底池 `6538`，需跟 `2930`，anti-lock `False`。
- `bot3` 第 10 场/第 21 手：`-1709.0`，河牌，终局动作 `0`（过牌/跟注），分支 `call_defense`，胜率 `0.5586`，成牌强度 `0.694`，价值档 `strong`，底池 `2164`，需跟 `1254`，anti-lock `False`。
- `bot1` 第 5 场/第 20 手：`-1415.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.525`，成牌强度 `0.2306`，价值档 `none`，底池 `4321`，需跟 `1491`，anti-lock `False`。
- `bot1` 第 6 场/第 20 手：`-1342.0`，翻牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.6576`，成牌强度 `0.2301`，价值档 `thin`，底池 `4344`，需跟 `1660`，anti-lock `False`。
- `bot3` 第 5 场/第 10 手：`-1318.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.6005`，成牌强度 `0.2288`，价值档 `none`，底池 `5622`，需跟 `2986`，anti-lock `False`。
- `bot2` 第 3 场/第 7 手：`-1290.0`，河牌，终局动作 `0`（过牌/跟注），分支 `check_or_pot_control`，胜率 `0.0897`，成牌强度 `0.0914`，价值档 `none`，底池 `2580`，需跟 `0`，anti-lock `False`。
- `bot3` 第 1 场/第 16 手：`-1210.0`，河牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.2969`，成牌强度 `0.23`，价值档 `none`，底池 `5929`，需跟 `3509`，anti-lock `False`。
- `bot2` 第 1 场/第 20 手：`-1193.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.5569`，成牌强度 `0.2291`，价值档 `none`，底池 `4900`，需跟 `2514`，anti-lock `False`。
- `bot1` 第 2 场/第 34 手：`-1051.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.2583`，成牌强度 `0.2259`，价值档 `none`，底池 `4388`，需跟 `2286`，anti-lock `False`。
- `bot1` 第 5 场/第 22 手：`-906.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.4653`，成牌强度 `0.4044`，价值档 `thin`，底池 `4122`，需跟 `2310`，anti-lock `False`。
- `bot3` 第 1 场/第 15 手：`-877.0`，翻牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.2815`，成牌强度 `0.0904`，价值档 `none`，底池 `3404`，需跟 `1650`，anti-lock `False`。
- `bot3` 第 10 场/第 5 手：`-869.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.2324`，成牌强度 `0.0904`，价值档 `none`，底池 `4021`，需跟 `2283`，anti-lock `False`。
- `bot1` 第 2 场/第 3 手：`-844.0`，转牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.5213`，成牌强度 `0.2285`，价值档 `none`，底池 `3994`，需跟 `2306`，anti-lock `False`。
- `bot2` 第 5 场/第 28 手：`-844.0`，翻牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.2957`，成牌强度 `0.0912`，价值档 `none`，底池 `2836`，需跟 `1148`，anti-lock `False`。
- `bot2` 第 10 场/第 1 手：`-821.0`，翻牌，终局动作 `-1`（弃牌），分支 `fold_defense`，胜率 `0.1701`，成牌强度 `0.0887`，价值档 `none`，底池 `3131`，需跟 `1489`，anti-lock `False`。

## 统计边界

- 每个对手只有 5 组镜像样本，足以发现明显崩溃、非法动作和重复失败模式，但不足以确认小幅策略优劣。
- 单场接近正负 20000 的结果会显著拉动均值，因此同时保留中位数、标准差和镜像组净值。
- 弃牌终局天然记录为负筹码，不能直接把 `fold_defense` 的累计负值解释为错误弃牌；需要结合底池赔率和对手范围逐手复盘。
- 本报告只提出观察方向，不修改任何 bot。

## 复现实验

```powershell
python .\德扑平台\pool_eval.py .\bots\experiments\anti_lock_v2.py .\bots\bot1.py .\bots\bot2.py .\bots\bot3.py --groups 5 --hands 70 --seed 2026360601
```
