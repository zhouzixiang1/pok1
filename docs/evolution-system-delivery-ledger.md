# National TCP Evolution Delivery Ledger

> Operator implementation record only. This file is not strategy evidence,
> rating evidence, a lesson store, an experience store, or a prompt input.
> Active code must never inject this text into Master, Worker, Reviewer,
> Critic, source selection, H2H, replay spotlight, or any evaluation cycle.

## Recording contract

Every implementation batch records its baseline, findings, changed files,
tests, Git identity, runtime transition, rollback boundary, and current
consecutive-generation observation count. Entries describe work performed;
they never authorize a bot, checkpoint, certificate, rating row, or recovery.

The only completion evidence remains the active contracts in `AGENTS.md`:
content-bound strict artifacts, current-epoch checkpoints, immutable native
evaluation cycles, signed official certificates, annotated completion tags,
and runtime health evidence.

## 2026-07-15 — alignment programme opened

### Objective

Close the national TCP contract across rules, server/runtime state, official
wire evidence, quality and publication gates, rendered LLM prompts, frozen
evidence, backend projections, frontend rendering, and regression tests. Then
restore the strict epoch, publish v143 and v144, establish the immutable rating
cycle, and observe ten consecutive generations without a repair, restart, or
authority drift.

### Authoritative baseline

- operator `origin/main`: `8df50853bd3d57ff07e6e768f7cae1b8ae12eedb`;
- runtime checkout HEAD before infrastructure work: the same commit;
- epoch: `national_tcp_policy_v1`;
- checkpoint: v143 from numeric high-water v142, `direction_audited`,
  `generation:143:workflow-v18`;
- published strict bots: none;
- stability observation: `0/10`;
- web/orchestrator/rating daemon: stopped;
- Arena worktree: explicitly retained.

### Repository hygiene performed

- fetched tags and pruned stale remote-tracking references;
- removed 17 clean worktrees whose HEAD was already contained by
  `origin/main`;
- pruned two missing worktree registrations;
- removed nine merged local branches with `git branch -d`;
- retained the dirty operator checkout, `.evolution_pok`, `pok-arena`, all
  unmerged research branches, and `codex/neural-v146-livepool` with its
  untracked experiment data;
- did not delete any remote branch;
- did not use `git clean`, reset, checkout, `branch -D`, force worktree
  removal, or inspect root `archive/` content;
- two already-unregistered worktree directories retain partial ignored/cache
  files after ordinary removal was refused by filesystem permissions. They are
  not Git worktrees and remain pending conservative cleanup.

Subsequent operator coordination completed branch governance outside this
worktree. The retained local/remote set is `main`, `pok-arena`,
`codex/national-protocol-evolution-alignment`, and
`codex/three-bot-consolidation`. This alignment work did not modify or clean
Arena or the consolidation branch. Its remote ref was created at the baseline
commit and will move only after the tested alignment commit is created.

### Development checkout

- branch: `codex/national-protocol-evolution-alignment`;
- worktree:
  `/home/zzx/project/pok/.codex_worktrees/national-protocol-evolution-alignment`;
- base: current `origin/main` at the commit above.

### Initial evidence

Before implementation, focused protocol, prompt-boundary, strict-artifact,
rating, evidence-snapshot, and publication tests produced 177 passing tests.
That result is only a regression baseline. It does not prove the broader
cross-layer objective, because the initial audit found uncovered paths in
official replay state, hand-70 completion, rendered bootstrap prompts,
unfiltered Git history, evaluation-contract scope, and Master stream recovery.

### Active work batches

| Batch | Scope | State | Completion evidence |
|---|---|---|---|
| P1 | official replay, board/showdown evidence, hand-70/full-v5 | implemented; focused green | protocol/harness/certification positive and negative suites |
| P2 | rendered prompts, history injection, role read/write/tool/model scope | implemented; strict invocation/gate-descriptor repair and frozen-tree suites green, merge pending | 19-role semantic-producer, provenance, poison/render/path/symlink/TOCTOU/hook/budget and accepted-effect/log tests |
| P3 | Master stream/effect recovery and bounded retries | implemented; phase-revision/per-slot-context/control-plane classification and frozen-tree suites green, merge pending | timeout/lease/replay/provider-termination/partial-packet tests |
| P4 | quality gates, capability probes, durable post-publication handoff, backend/frontend and N/10 projection | implemented; backend/frontend and final frozen-tree suites green, merge pending | journal/effect reproof, AST/runtime causality/API/TS/component tests |
| P5 | full verification and merge to `origin/main` | complete through stream-ownership commit `0a26795aa71fa92049acebef94968a0c9f7553d7`; the strict-authority recovery repair repeats the same merge gate | full suites, Git and remote identities |
| P6 | runtime recovery, v143/v144 publication and ten generations | reconciliation/identity rotation complete; stale workflow-v20 is stopped at v143 direction-audited and must be canonically abandoned after merge, never retried | tags, certificates, immutable cycles and live observation ledger |

### v18 failure evidence and repaired invariant

The stopped v143 workflow was not a strategy rejection. Its first proposal
attempt ran for 79.23 seconds, emitted 6,182 characters and then failed strict
projection. The schema-repair stream continued to emit bookkeeping-only SDK
`SystemMessage` events at 120.17 seconds (3,491/3,505) and 240.18 seconds
(7,641/7,655), emitted only 133 text characters, and did not finish shutdown
until 313.35 seconds. Durable effects were left `running` under dead PIDs.

The repaired contract treats SDK bookkeeping as non-productive, bounds first,
idle, stall and total time across schema retries/backoff, makes the terminal
result authoritative, reclaims dead effect owners, and permits one durable
schema repair before failing closed. A provider stream that resists
cancellation is an infrastructure failure and must be terminated through its
owned transport before any retry may start.

### Legacy evidence-envelope branch decision

Reviewed `codex/evidence-envelope-handoff` commit
`fc7d62d30783d2ae8710dc8f331d717f3d902e36` without cherry-picking it.
Decision: **semantically superseded; retain only as isolated Git history**.

That commit was written for the retired mixed prompt/experience architecture:
it adds `prompt_evidence.py` alongside now-removed experience-pool, guardian,
calibration and global spotlight consumers, and its fixtures use the obsolete
v155 migration shape. Blindly applying it would reintroduce active dependencies
on facilities that the strict epoch explicitly retired. The current alignment
implements the still-valid intent through checkpoint-bound bootstrap receipts,
`master_context_contract`, generation evidence snapshots, deterministic
bootstrap direction/audit receipts, strict role effects, a system-supplied
Critic code envelope, outer-orchestrator bootstrap sanitization, a no-memory
cycle archivist, and resolved-path role allowlists. No source byte from that
legacy commit is transplanted.

### Advisory Critic and canonical-route closure

The first advisory conversion changed `run_critic` output but left historical
`critic_repair` synthesis, in-place Worker events, direct
`critic_checked -> workers_done` transitions, and broad HEAD-drift tool sets in
the active path. That made the prose advisory while candidate mutation remained
reachable. The route projection also merged recovery-compatible tools into the
ordinary stage allowlist, so a completed quality/review stage could expose
`execute_workers` before a new hard-gate failure existed.

`pipeline_state.py`, `tool_planning.py`, `agent_workers.py`,
`evolution_infra.py`, and the Orchestrator prompt now separate these
authorities. A schema-valid Critic result proceeds to native precommit
regardless of score; an incomplete receipt retries only Critic; a legacy
Critic-owned repair contract fails closed into controlled abandon/re-prepare;
and only a measured precommit regression can route the unchanged
`critic_checked` candidate to Workers. Normal route allowlists expose only the
canonical next tool, while HEAD-drift recovery policy remains a second guard
rather than an alternate pipeline.

Focused evidence at this point:

```text
python -m pytest web/tests/test_pipeline_state_machine.py \
  web/tests/test_runtime_governance_refactor.py -q
161 passed, 1 warning
```

This focused result does not close P5; the full suite is still required after
the concurrent backend/frontend official-state work is stable.

### Backend authority and dashboard closure

The dashboard previously had to infer or omit several real runtime facts:
canonical pipeline routing was not returned by `/health`; v143 exposed only a
static operator pause; published certificates dropped their validated profile;
daemon liveness trusted a reusable PID and treated missing/invalid heartbeats as
healthy; and every status poll synchronously reopened remote refs. Configuration
updates could race a live orchestrator, silently clamp values, swallow disk/reset
failures, block the event loop while stopping a daemon, and leave memory, disk,
daemon, and the N/10 identity describing different configurations.

`control.py`, `state.py`, `stability_observation.py`, `daemon_management.py`,
`certification.py`, and `official_certification.py` now close those boundaries:

- route policy is derived from the same re-read, epoch-validated checkpoint;
- v143 jobs refine a digest-bound four-state operator transition, while only the
  operator finalize CLI can publish;
- normal and bootstrap jobs expose exact 5/3×70, opponent authority and zero
  weights, and published profiles are derived only from the signed certificate;
- stability binds the effective daemon configuration and uses one coalesced
  background verification with pending/fresh/stale/failed TTL semantics;
- daemon health binds PID start ticks and classifies missing, invalid,
  non-finite, future and stale heartbeats, including disabled-but-live state;
- running config/session mutation is 409, configuration is range-strict and
  atomically fsynced, and reset/daemon failures roll back through an isolated
  blocking boundary.

Focused evidence after this closure:

```text
python -m pytest web/tests/test_routes_certification_epoch.py \
  web/tests/test_official_certification.py web/tests/test_epoch_authority.py \
  web/tests/test_routes_control.py web/tests/test_stability_observation.py \
  web/tests/test_rc3_daemon_grace.py -q
180 passed, 1 warning

python -m pytest web/tests/test_frontend_contract_closure.py \
  web/tests/test_official_pipeline_state.py \
  web/tests/test_runtime_governance_refactor.py \
  web/tests/test_policy_prompt_ownership.py \
  web/tests/test_system_control_contract.py \
  web/tests/test_epoch_launch_guard.py \
  web/tests/test_active_national_only_architecture.py \
  web/tests/test_candidate_pipeline_schema.py -q
191 passed, 1 warning
```

These focused results close P4 implementation evidence but do not close P5 or
the live P6 acceptance sequence.

### Matrix scope expansion and pinned-oracle verification

The first matrix draft left three active contracts implicit inside broader
rows: platform server/client ownership and port 10001, bounded system-owned
precompute facts, and the annotated high-water/schema-2 reset authority.  It
also described publication mostly through the gate row instead of naming the
working-byte/index/tag/remote-tree transaction.  They are now independent
matrix rows with their own producer, consumer, dynamic gate, prompt wording,
positive/negative regression and fail-closed result.

Focused evidence for those rows:

```text
python -m pytest web/tests/test_active_documentation_contract.py \
  web/tests/test_policy_epoch_reset.py \
  web/tests/test_strict_precompute_anytime.py \
  web/tests/test_managed_bot_socket.py \
  web/tests/test_national_epoch_registry.py \
  web/tests/test_publication_transaction.py \
  web/tests/test_official_publication_identity.py -q
88 passed, 1 warning

python -m pytest sever/tests -q
31 passed
```

The immutable oracle bytes were re-hashed without editing them:

- raise-boundary oracle:
  `a83a1ec2680577d71ddb985ddba00c5bcda40817ef2fb92c0c41938dccef3756`;
- terminal-settlement oracle:
  `ad96bc4fbe7939597b7a86ff6f9193ed2e50891be9b6b9c074883f5750c23bd9`.

Both equal `OFFICIAL_ORACLE_DOC_DIGESTS` in
`web/core/runtime_architecture_policy.py`. This is an input-integrity check,
not a rerun of the official EXE and not certification evidence.

### Checked-byte to executed-byte closure

The artifact hash previously excluded Python cache products while the central
executor mounted the complete host bot directory at `/bot`. Python `-B` stops
cache writes but does not disable reads: an unchecked-hash
`__pycache__/policy.<tag>.pyc` or `precompute.<tag>.pyc` can override the
content-bound source. This created both an unbound-byte path and a
check-to-launch race across native evaluation, ratings, precommit, Arena and
formal certification.

The repaired boundary has one causal chain. Strict active layout rejects cache
directories and top-level bytecode; `.task_context` is permitted only by an
explicit Worker-phase call. Before any endpoint is consumed, the central
executor reads the exact top-level source files through no-follow descriptors,
reconstructs and verifies the expected artifact hash and required five-file
set, seals each captured payload in a write/grow/shrink-sealed memfd, and gives
the child a new `/bot` containing only those read-only files. The host bot
directory is never mounted. Python still starts with `-I -B` as defense in
depth, but neither flag is treated as the content-binding mechanism.

The same projection is now consumed by direct native matches (and therefore
rating/precommit), official certification, diagnostic Arena, the official wire
probe, national decision fixtures, and the runtime probe. The managed execution
identity advances to `pok-managed-executor-identity-v2`; the tracked formal
profile advances to
`official-exe-2021-wine9-managed-executor-v6` and binds the sealed-source
contract and source hash.

Dynamic regression evidence includes a fixture that first proves both
unchecked-hash pycs win under ordinary `-I -B` source imports, then proves the
strict launch rejects them before consuming the endpoint. A second fixture
injects both pycs and `sitecustomize.py` after the source snapshot; the managed
process still sees exactly five files, imports the benign captured values,
reports isolated mode and bytecode-write suppression, and observes no startup
marker.

A follow-up active-path scan found that `scripts/official_wire_probe.py` still
constructed `sys.executable <bot>/national_bot.py` and launched it with the
host harness `_popen`. Although that receipt has zero certification and rating
authority, it executed candidate bytes outside every candidate isolation rule.
The probe now accepts unpublished or published strict five-file directories
without requiring publication eligibility, rejects arbitrary files, directory
symlinks, cache/work-control/extra entries, forged identity documents and
candidate-owned runtime bytes, seals their content, validates the formal
profile, leases the exact proxy connection, and calls the central official
sandbox. Its receipt binds the canonical candidate-validation digest and issue
list. The system-owned scripted client remains a parser fixture, not an
arbitrary bot-path escape hatch.

```text
python -m pytest web/tests/test_managed_bot_executor.py \
  web/tests/test_strict_artifact_source_projection.py -q
12 passed, 1 skipped, 1 warning

python -m pytest web/tests/test_official_bot_sandbox.py \
  web/tests/test_national_runtime_probe.py \
  web/tests/test_national_decision_tester.py -q
49 passed, 1 warning

python -m pytest \
  web/tests/test_bot_artifact_integrity.py \
  web/tests/test_strict_artifact_scope_chain.py \
  web/tests/test_strict_artifact_source_projection.py \
  web/tests/test_strict_bot_namespace.py \
  web/tests/test_worker_boundary.py \
  web/tests/test_parallel_worker_boundary.py \
  web/tests/test_national_capability_hardening.py \
  web/tests/test_national_runtime_probe.py \
  web/tests/test_national_native_strict_artifacts.py \
  web/tests/test_national_native_template_connection.py \
  web/tests/test_managed_bot_executor.py \
  web/tests/test_national_decision_tester.py \
  web/tests/test_context_precommit_injection.py \
  web/tests/test_gate_execution.py \
  web/tests/test_precommit_attempt_checkpoint.py \
  web/tests/test_precommit_eval_contract.py \
  web/tests/test_precommit_eval_fixes.py \
  web/tests/test_precommit_repair_targets.py \
  web/tests/test_official_wire_probe_cli_boundary.py \
  web/tests/test_official_wire_probe.py \
  web/tests/test_official_bot_sandbox.py \
  web/tests/test_official_platform_harness.py \
  web/tests/test_official_platform_resource.py \
  web/tests/test_official_certification.py \
  web/tests/test_official_certification_job.py \
  web/tests/test_official_certificate_signing.py \
  web/tests/test_official_certify_cli.py \
  web/tests/test_official_commit_gate.py \
  web/tests/test_official_publication_identity.py \
  web/tests/test_publication_transaction.py \
  web/tests/test_official_bootstrap.py \
  web/tests/test_policy_epoch_bootstrap.py \
  web/tests/test_system_control_contract.py \
  web/tests/test_strict_precompute_anytime.py \
  web/tests/test_official_eligibility.py -q
489 passed, 1 skipped, 1 warning

validate_execution_profile(..., probe_sandbox=True)
ok=true, issues=[], managed_probe_ok=true,
profile_id=official-exe-2021-wine9-managed-executor-v6

python -m pytest web/tests/test_official_wire_probe_cli_boundary.py \
  web/tests/test_official_wire_probe.py \
  web/tests/test_official_bot_sandbox.py \
  web/tests/test_official_platform_harness.py -q
90 passed, 1 warning
```

These results close the source-projection defect only. They do not substitute
for the full P5 suite, a signed official certificate, publication, or the live
P6 consecutive-generation observation.

The standalone wire probe remains diagnostic-only and now rejects target 70.
It defaults to one complete paired wire settlement and accepts at most 69;
natural hand 70 has no wire settlement in the pinned EXE and can be proved only
by the formal `official-full-v5` wire/THP/footer cross-binding.  Its receipt
therefore states zero certification and rating weight explicitly.

A second active-entry scan retired `scripts/research_eval/` into
`archive/cleanup_20260715/legacy_research_eval/`.  That unused historical
harness accepted arbitrary script paths, launched them directly on the host,
and referenced detached research worktrees plus external neural assets.  The
active absolute-path test exemption was removed and a filesystem regression
now rejects those arbitrary-entry markers anywhere in active `scripts/`.
Generated bytecode left beside the retired sources was removed, not archived
as executable evidence.  The official harness also removed its unused
`resolve_bot_entry` / `build_bot_command` raw-command API and configurable
Python executable; its only bot launch boundary is now the sealed artifact,
endpoint lease and managed sandbox path.  The low-authority acceptance CLI no
longer advertises script inputs: before its production runner starts, both
inputs must be non-symlink strict five-file directories directly below the
active `bots/` namespace with valid identity documents and system runtime.

The active restart observer was also narrowed to its name: restart and observe.
Its retired `--clear-checkpoint backup-and-clear` path deleted the checkpoint
and moved guessed “unfinished” candidates by comparing directory numbers with
the highest tag, bypassing canonical abandon/reconciliation.  Its provider
session keep/clear switch likewise described a history authority that no
longer exists.  Both options and all manual state-move/delete code are removed;
passing either old option is a usage error.  The helper now states that
checkpoint/candidate cleanup must use the governed transaction.

### Orchestrator provider-history boundary

The role registry previously claimed checkpoint-bound history while
`orchestrator_session.json` persisted only an opaque provider `session_id`.
Changing epoch, workflow, checkpoint revision, prompt bytes or MCP projection
could therefore resume invisible server-side conversation history that none of
the local receipts could verify.  Cross-process provider resume is now
forbidden: a legacy sidecar is deleted on sight, successful Result messages do
not persist their session ID, normal/retry SDK options carry `resume=None`, and
recovery reconstructs a fresh provider stream from the validated checkpoint,
sealed prompt and typed MCP projection.  Watchdog liveness uses an in-process
owned-stream flag, not provider history.  Dynamic regression injects a stale
session loader and observes that the dispatched SDK options still contain no
resume capability.

```text
python -m pytest web/tests/test_orchestrator_stream_cleanup.py \
  web/tests/test_llm_role_contract_registry.py -q
29 passed, 1 warning

python -m pytest web/tests/test_official_platform_harness.py \
  web/tests/test_official_wire_probe_cli_boundary.py \
  web/tests/test_official_wire_probe.py -q
85 passed, 1 warning

python -m pytest \
  web/tests/test_runtime_governance_refactor.py::test_active_scripts_have_no_retired_arbitrary_bot_entry_harness \
  web/tests/test_runtime_governance_refactor.py::test_runtime_code_has_no_local_absolute_project_paths -q
2 passed, 1 warning

bash -n scripts/pok_restart_observe.sh
python -m pytest web/tests/test_restart_observe_script.py \
  web/tests/test_active_documentation_contract.py::test_operator_helper_help_preserves_epoch_and_evidence_boundaries -q
3 passed, 1 warning
```

### Adversarial control, certification and dashboard follow-up

The first focused-green dashboard batch still had cross-layer gaps that its
static tests did not exercise.  An invalid bootstrap job could disappear into
"no job"; a normal job could claim `strict_published_pool` from a self-contained
receipt; terminal status was not fully bound to request/selection/envelope;
the v143 certificate retained checkout-local absolute paths; frontend code
discarded a valid jobless failure transition; control mutations could overlap
across blocking awaits; status polling performed Git/certificate work on the
event loop; daemon cleanup trusted too little process identity; and the SSE
contract had only source-string assertions.

The follow-up closes those causes rather than changing UI labels:

- bootstrap discovery preserves malformed/ambiguous attempts as a digest-bound
  `bootstrap_failed` transition and exposes only the explicit `--force` retry;
- normal opponent authority reopens the annotated tag, exact artifact,
  certificate and latest verdict ledger; terminal status binds the original
  request identity, selection and job envelope;
- portable v143 verification allows checkout-path relocation only after the
  signed original digests and current candidate/control/policy bytes match;
- normal jobs remain observable through certifying, failed, inconclusive and
  publishing stages without letting a running/failed state inject a pass;
- start/stop/config share a cancellation-draining lifecycle lock,
  task/shutdown ownership is token-fenced, and disk fsync no longer holds the
  in-memory status lock; the provider-session DELETE route is retired with 410
  and GET exposes only the fresh-stream/checkpoint recovery policy;
- status, health and certification GET projections execute through the bounded
  blocking isolation boundary; paired status/health responses must carry the
  same stability digest and full checkpoint revision identity;
- daemon cleanup binds start ticks, a per-launch owner-token digest, exact
  command and process-group leader, proves TERM/KILL exit before replacement,
  and prevents an old process from overwriting the replacement heartbeat;
  health uses that same classifier, so a forged fresh heartbeat cannot make an
  owner/command/group mismatch appear alive;
- DataProvider and evolution SSE now use the same injectable production
  controller exercised by fake-transport tests for malformed data, liveness,
  single reconnect, epoch blocking, stale-source fencing and cleanup; an
  unfinished backend task keeps every mutation control disabled throughout
  shutdown rather than exposing buttons that can only receive 409.

Focused evidence at this stage:

```text
certification identity/profile/route compatibility: 191 passed, 1 warning
certification blocking-boundary follow-up:             143 passed, 1 warning
control lifecycle/API/frontend contract:               228 passed, 1 warning
daemon/stability/orchestrator regression shards:        126 passed, 6 skipped, 1 warning
repository HEAD/branch stability shard:                  24 passed, 1 warning
frontend dynamic SSE/control ownership:                  12 passed
npm run lint:                                           passed
npm run build:                                          passed (163 modules)
```

These results remain focused evidence.  They do not close P5 until the final
combined/full commands run after every concurrent repair has stopped changing
the tree.

### Paired namespace and stopped-runtime reconciliation closure

The initial reset/recovery path treated any annotated completion or high-water
tag as numeric authority, and production, reset and reconciliation each parsed
the refs independently. A completion-only or high-water-only ref could
therefore advance one consumer but not another. The checkpoint envelope also
did not bind the live abandon-chain head, while generic cleanup cleared the
checkpoint before deleting the candidate. That ordering had a real crash
window: an orphan candidate could remain after the only routable control state
was gone.

The repaired implementation has one canonical paired-tag namespace resolver.
A version is numeric authority only when both exact annotated refs peel to the
same commit. Epoch initialization separately proves that the highest v143+
namespace version is the highest eligible five-file/tag-tree/signed-certificate
publication. Schema-2 checkpoints bind published high-water, abandon floor,
unique chain-head digest and allocation floor. A repeated terminal append is
idempotent only while its exact receipt remains the sole chain head; replay of
an older matching checkpoint fails closed.

Generic abandonment now shares the publication mutex and first persists both a
transaction claim and the typed live launch barrier. The claim binds the full
checkpoint CAS identity, reason, Git HEAD and exact candidate manifest before
the receipt or candidate path can change. Receipt creation/replay proves the
ledger inode and parent durable; completion/high-water refs, `.completed`, Git
tracking and the manifest are rechecked immediately before an atomic move into
a content-addressed transaction quarantine. Both rename parents are synced
before the full-identity checkpoint CAS, and a terminal transaction receipt is
durable before the live claim is cleared. The quarantine remains zero-authority
preimage evidence rather than being deleted or adopted.

Claim, receipt, Git-predicate, source/quarantine XOR, manifest, rename, fsync or
CAS failure fails closed with the recovery barrier intact. Retries re-fsync
already-published ledger and directory effects instead of assuming a raised
operation did not land. The stopped-runtime CLI consumes the same schema-2
transaction kernel and separately retains a narrow schema-1 compatibility path
for the historical clear-before-delete window; drift is preserved. Preparation
likewise never removes or adopts an existing target: only the exact live bytes
revalidated by the checkpoint-owned prepared-artifact contract may resume.

Namespace reads are also fail-closed across time. If the paired-tag authority
snapshot is unavailable or no longer matches the high-water read, initialization
is false and the published/active Bot projection is emptied, so scheduler,
prompts, API and dashboard cannot temporarily consume scanned artifacts as
authority.

Legacy reconciliation claims now validate every nested candidate, checkpoint,
ledger, cost-scope and runtime-control field. Runtime-control names are a
unique allowlist, not paths. Archive roots are canonical direct children of the
runtime reconciliation root, and every existing/created archive ancestor and
destination directory must be a real non-symlink directory before a claim or
move. Legacy rows remain raw quarantined bytes with authority weight zero.

Focused version/publication/allocation/recovery evidence:

```text
python -m pytest web/tests/test_strict_bot_namespace.py \
  web/tests/test_policy_epoch_reset.py \
  web/tests/test_national_epoch_registry.py \
  web/tests/test_publication_transaction.py \
  web/tests/test_official_publication_identity.py \
  web/tests/test_epoch_authority.py \
  web/tests/test_checkpoint_epoch_recovery.py \
  web/tests/test_abandon_helper.py \
  web/tests/test_policy_epoch_reconciliation.py \
  web/tests/test_generation_scheduler.py -q
177 passed, 1 warning
```

Additional crash/adversarial closure after that batch:

```text
abandon transaction (claim/ledger/XOR/drift/CAS): 18 passed, 1 warning
reconciliation including schema-2 dry/execute resume: 20 passed, 1 warning
epoch authority including transient namespace failure: 40 passed, 1 warning
prepare preimage and exact prepared-contract retry: 5 passed, 1 warning
control route/daemon/frontend contract shard: 112 passed, 6 skipped, 1 warning
frontend dynamic contract: 12 passed; production build passed (163 modules)
```

### Schema-2 abandon-claim authenticity closure

The first schema-2 transaction draft treated a canonical self-digest as if it
were live authority. A locally re-signed claim could change its transaction id
or nested fields, and recovery did not consistently reopen the Git HEAD,
tracked-tree state, publication refs, transaction copy, candidate preimage,
source/quarantine phase and ledger prefix. There was also an ordering window in
which both claim files were durable but a subsequent HEAD or candidate drift
could be detected only after the irreversible abandon receipt had already
reserved the label.

The final schema-2 validator accepts an exact-key envelope only. Its transaction
id binds the checkpoint identity, reason, bounded candidate manifest, fixed
quarantine contract, ledger prefix/exact six-field successor and captured Git
state. Active recovery reopens both durable claim copies through no-follow
directory/file descriptors; verifies before/after/live inode, link, size,
mtime and ctime identity; and proves current HEAD, clean tracked tree, untracked
candidate, both absent publication refs, exact ledger prefix and
source/quarantine XOR. After the transaction and live claim are durable, the
same complete validation runs again before any ledger append. A live claim of
any validity blocks epoch initialization and empties the active-Bot projection.

Terminal receipts are validated historically rather than required to remain
today's chain head: a completed transaction stays valid after later legitimate
commits and abandon rows because its original prefix, exact successor row,
claim and final receipt still bind. It never authorizes a same-version source
directory that reappears after checkpoint clear.

Focused and independent evidence for this frozen block:

```text
schema-2 abandon/epoch/reconcile authenticity matrix: 149 passed
independent adversarial abandon+epoch+reconcile replay: 89 passed, 1 warning
active-file py_compile:                               passed
git diff --check:                                    passed
```

Negatives include a self-resigned transaction id, extra/missing fields,
candidate and quarantine traversal, symlink/hardlink entries, an old live HEAD,
same-length in-place JSON rewriting, candidate/HEAD drift after live-claim
publication but before ledger append, and source-label reappearance after
checkpoint clear. The two pre-append fault injections both leave zero new
ledger rows, zero checkpoint clears, and the candidate/checkpoint preserved.

Before merge, a read-only compatibility simulation opened the real stopped
`.evolution_pok` inputs with the current alignment implementation. A fresh
`python -B` process imported the alignment script/module directly and rebound
only its root/path constants to `.evolution_pok`; it did not copy or install
alignment files into the runtime and did not invoke `--execute`. This is not a
post-merge cold-entry run of the script from the runtime checkout. Watched
ledger, checkpoint, candidate, claim, receipt and archive paths were hashed
before and after and were identical:

- mode `dry_run`, `mutates=false`;
- checkpoint
  `3f19e3be2122cddf8fefeb41dd6269b57a71243ceb3c3daff75d0561cbdb65e3`;
- legacy ledger
  `86bfb8a21cfc756c17c62b9ca655e5ee8ae3bdf99ec193f657bfd3b936dba907`;
- candidate manifest
  `2375b4b805a55223e03c0e5b657e36018112430082bc5191e9c3a6eda09c86bf`;
- independent five-entry candidate-tree snapshot before/after
  `13ee28965ba4762f76cac772f8b27087dba43596d633a442deadac45ea3cbf49`;
- exact successor and receipt eligibility `true` for
  `generation:143:workflow-v18`.

No live reconciliation, quarantine, receipt append, checkpoint clear or
candidate move has been executed. After merge and runtime fast-forward, the
native script must still be run from `.evolution_pok` first in dry-run mode.
Execution remains a separate stopped-runtime operator action under the runbook.

### Active LLM role authority closure

The first role registry recorded role names, tools and renderer labels, but the
provider boundary still trusted caller-assembled prompt strings. Several
wrappers merely concatenated `ordered_sections`, so a caller could submit an
arbitrary complete prompt and self-compute its hash. Evidence provenance was
also independent of some provider-visible material: a Combined view could
differ from its frozen bundle; audit/Critic H2H inputs and Worker output
metadata were outside their required receipts. Mutable tool/path dictionaries
and `strict_authority` survived a quota await; model was unregistered; template
reads could follow module globals/import caches; guard errors could fail open;
and prompt budgeting could trim already-receipted renderer bytes.

The repaired producer-to-consumer chain is:

```text
checkpoint / immutable artifact / frozen evaluation inputs
  -> one of 19 exact semantic renderer input contracts
  -> repository-fixed producer template and fixed assembly
  -> typed renderer + producer-derived evidence + real MCP receipts
  -> pre-await frozen model/tool/read/write/evidence capability
  -> private strict-effect descriptor copied from the frozen JSON receipt
  -> mandatory fail-closed hooks and untrimmed final contract suffix
  -> Claude SDK / Orchestrator MCP provider
  -> schema validator and fenced effect consumer
```

Every provider-visible dynamic input is now either derived inside the producer
from the admitted frozen object (the Combined rendered view is inside its
content-bound bundle) or represented by a required producer-derived digest.
The registry fixes `sonnet` through dispatch receipt, frozen capability, final
suffix and SDK options. Producers reopen exact `Path(__file__)` template paths;
the Orchestrator has no import-time prompt cache. Caller scopes normalize once,
and strict schema/bind/dispatch/replay/complete/fail consume only the private
descriptor. Result state is copied back only after it cannot affect authority.

Independent negatives cover all 19 roles: correct-looking caller sections with
arbitrary content and correct hashes, forged text/provenance, a poisoned view
beside a valid bundle, alternate/post-render-drifted templates, unknown
role/tool/model, outside/symlink paths, quota-wait mutation, unavailable or
throwing read/write/evidence/read-only/exact-Bash hooks, and an oversized sealed
prompt all fail before provider execution. The recursive AST expectation is
hardcoded independently of the registry and covers aliases, attribute calls,
subpackages, scripts and both direct SDK entry points. A second independent
call-site scan fixes the active count at 19 and rejects caller-owned
`sections`, `ordered_sections`, or provider-ready prompt fields. The durable
Worker producer passes no external context files, matching the role registry's
no-context-file ABI. This ledger and the alignment matrix remain operator
records and are never renderer inputs.

Focused evidence:

```text
python -m pytest web/tests/test_llm_role_contract_registry.py -q
22 passed, 1 warning

python -m pytest web/tests/test_strict_prompt_evidence_boundary.py \
  web/tests/test_master_proposal_ensemble.py \
  web/tests/test_master_success_return.py \
  web/tests/test_critic_code_evidence.py \
  web/tests/test_cycle_archivist.py \
  web/tests/test_official_llm_analysis.py \
  web/tests/test_operator_sdk_probe.py \
  web/tests/test_crossover_synthesis.py \
  web/tests/test_crossover_compat_recovery.py \
  web/tests/test_llm_role_contract_registry.py \
  web/tests/test_llm_zero_tools.py \
  web/tests/test_llm_role_observability.py \
  web/tests/test_llm_availability_store.py \
  web/tests/test_master_evidence_guard.py \
  web/tests/test_llm_read_scope_guard.py \
  web/tests/test_strict_authority_workflow.py -q
231 passed, 1 warning

python -m pytest web/tests/test_evidence_snapshot.py \
  web/tests/test_literature_probe_timeout.py \
  web/tests/test_policy_prompt_ownership.py \
  web/tests/test_national_prompt_rules.py \
  web/tests/test_hidden_fixes.py -q
72 passed, 1 warning
```

The Master infrastructure-overlay test initially failed before recording its
first retry because its hand-built v143→v144 checkpoint did not install the
new live allocation-authority fixture. The test now injects the same explicit
synthetic successor authority used by other stage/CAS tests; it retains the
production expectation of two `retry_same_tool` responses followed by one
centralized `abandon_generation`. The full module passes 4 tests.

### Rating-history identity closure

The history rebuild path previously admitted any row that self-asserted a
complete 70-hand sample.  Missing/old evaluation identities and even
official/Arena-shaped rows could therefore rebuild H2H and chip metrics; the
daemon could then freeze those derived values into a new immutable cycle even
though the later evidence snapshot filtered its raw history index.

Match admission, H2H reconstruction and secondary chip aggregation now require
the exact current 64-hex evaluation identity, epoch
`national_tcp_policy_v1`, and execution mode `native_tcp`.  A caller without
that authority can retain an already committed H2H projection but receives no
history rebuild or chip signal.  Every daemon consumer supplies the current
identity, rating-history trend rows carry the same epoch/mode/digest, and cycle
publication validates every match/rating append row before signing the
manifest.  `get_match_history` reads only the current immutable bundle.

The staged-match commit path also had a latent exact-projection defect: its
saved summary contained `evaluation_epoch` and `execution_mode`, but the
reconstructed comparison omitted both keys, so a real staged native match
could never commit.  Both keys are now bound alongside bots, outcome, identity
and strength fields before any rating mutation.  Re-signed legacy-epoch,
`official_exe`, and `national_arena` payloads fail before H2H/history/replay
state changes.

```text
python -m pytest web/tests/test_rating_snapshot.py \
  web/tests/test_strength_order.py \
  web/tests/test_evaluation_bundle.py \
  web/tests/test_evidence_snapshot.py -q
62 passed, 1 warning
```

### Durable post-publication and cross-surface authority closure

Publication previously left stability accounting, daemon signals, rotation,
log cleanup, pool reaping and Cycle Archivist work as loosely ordered cleanup.
That allowed the publishing checkpoint to disappear before a recoverable
obligation existed, and a syntactically re-signed receipt could claim an effect
whose durable bytes were missing or different. Concurrent status/SSE reads
could also combine one epoch revision with a later handoff or stability state.

The repaired path creates a schema-2 post-publication handoff and archive base
snapshot under the publication lock before checkpoint CAS clear. First creation
of each authority directory fsyncs both the directory and parent entry and
rechecks their inode identities. The active pointer selects one exact record;
completed historical records are not scanned as live authority. One fenced
owner executes the following ordered steps:

`stability_observation → reap_signal → priority_eval → archive_rotation →
log_cleanup → pool_reap → cycle_annotation → housekeeping`.

Every step now validates an exact plan key set and exact output key set, the
publication id, plan digest and step-specific identity before execution and
again during discovery/finalization. This rejects forged-skip outputs, partial
receipt sets, vacuous empty archive lists and target recomputation. Claim
release always drops the process-local ownership token even when the durable
release write fails, so the same process can safely recover the still-running
record rather than deadlocking itself.

Receipt authenticity is not treated as effect existence:

- stability completion reopens the operator state and finds the exact
  publication row with the same continuity/count projection;
- the exact frozen `.reap_signal` and `priority_eval.json` payloads are
  idempotently republished and read back; producer and daemon consumer use the
  same stable sidecar EX lock, including exact-inode read/unlink;
- archive rotation freezes every managed source in one high-level plan before
  a low-level archive can exist, copies only the planned cold prefix, retains
  the live append-only source, and validates plan/watermark/archive chains;
- strict v143+ logs are archived with normalized immutable tar and manifest
  receipts while preserving the live log tree, quarantine path and all sibling
  files; the retired destructive cleanup API fails closed;
- pool reaping binds a schema-2 active-pool digest, rating-selection snapshot,
  policy, limit and complete deterministic target sequence. Multi-reap and
  zero-reap plans are both frozen; completion re-proves tombstones, registry,
  remote refs when required, and absent `.completed` capabilities;
- final housekeeping reopens rotation/log/reap/annotation effects and proves
  the exact publication HEAD and clean worktree without a housekeeping commit.

The backend now brackets handoff reads between matching canonical epoch
snapshots and binds control status/health/SSE to the same epoch, handoff
identity/revision and cached stability projection. Handoff overlap or failed
stability verification degrades health. Frontend validators and controllers
accept only that typed identity, discard stale/out-of-order events, clear state
after stream loss or epoch change, and render the real
`pending`/`running`/`blocked` launch barrier rather than an inferred idle state.

The first stopped-runtime v143 launch then exposed two recovery-only defects
that the source-only suites had not exercised together. A fresh `--one-gen`
provider session repeatedly handed off the same pre-existing `selected`
checkpoint before it could call `prepare_next_gen`; the stream now owns its
startup checkpoint identity and hands off only after that session observes a
different revision/stage. Separately, a detached stability verifier could
strand the single-flight flag when a `BaseException` escaped its thread; all
thread exits now publish a failed refresh and unconditionally release the slot
so invalidation/retry can progress. Both paths have dedicated regressions.

After commit `291f2d40c0301c4262c1848fe549e8999ac83174` reached
`origin/main` and the stopped runtime, canonical one-generation recovery
successfully prepared the exact five-file v143 artifact and advanced workflow-v19
to `direction_audited`. The first live Master call then exposed a third
recovery-only defect: while its nested proposal roles were still producing
current-generation progress, the independent five-minute actionable-stage
poller treated the unchanged startup `direction_audited` stage as abandoned and
cancelled `run_master`. The stream poller now fences the workflow/stage/version
plus authoritative next-tool/intent identity owned by the in-flight tool,
ignoring same-route revision metadata; post-message handoff still uses the full
revision/stage identity, while a real stage change or a different authoritative
same-stage route still enables deterministic recovery. Dedicated
regressions hold a delayed Master result past repeated actionable polls and
exercise the same-stage route transition while preserving the existing
stage-transition timeout test.

That repair was committed as
`0a26795aa71fa92049acebef94968a0c9f7553d7`, fast-forwarded to
`origin/main`, and synchronized into the stopped runtime. The critical-input
HEAD drift was correctly refused at workflow-v19 `direction_audited`; the
canonical abandon transaction quarantined its exact five-file candidate under
receipt `e19b0aafe79bdae5b6ed902d985efb6c67588f60a195215a806c90893a49b6ad`.
Because the strict pool is still empty, the retryable-first-strict rule kept
the allocation successor at v143 and created workflow-v20 rather than skipping
to v144. Preparation and direction audit then completed on the new baseline.

The live workflow-v20 Master proved the stream-ownership repair by continuing
past five minutes without an actionable-stage cancellation. It then exposed a
fourth recovery-only producer/consumer mismatch. Its revision-4 authority
journal durably accepted the mechanism and counterfactual proposals and
recorded one compute-memory schema rejection; the exact packet therefore
failed closed at two proposals. Persisting the bounded Master failure advanced
the checkpoint to revision 5. The next call looked up those same accepted
slots using revision 5 instead of their authority-phase revision 4, raised
`strict_authority_recovery_checkpoint_revision_mismatch`, and incorrectly
projected that control-plane mismatch as a retryable `master_llm` overlay at
revision 6. No new provider effect, plan, Worker edit, or candidate mutation
occurred on that second call.

The repaired strict authority derives one frozen revision from the first
durable effect in each phase. It verifies the complete effect input and
generation/stage/role binding plus one frozen context per slot before reusing
the anchor, replays accepted slots, lets only missing slots consume their
original schema budget, and rejects mixed revisions, rollback, or same-slot
context drift. Final validation scans rejected and otherwise unaccepted effects
as well as accepted receipts, so a failed call cannot hide a second phase
revision. A real two-invocation ensemble test
proves that a partial packet followed by checkpoint metadata advancement makes
no second provider call for the two accepted proposals, calls only the missing
proposal, completes both ballots, and validates every receipt at the original
revision. Runtime remains stopped at workflow-v20 revision 6 with
`master_plan=null`; after this repair reaches `origin/main`, canonical abandon
must quarantine workflow-v20 before a fresh v143 workflow is prepared.

Cross-review then found two adjacent evidence boundaries that the phase anchor
alone did not close. Proposal/ballot packet evidence and Reviewer/Critic gate
evidence are now bound to the accepted provider effect's final prompt digest,
terminal output, result/usage identity, deterministic role result, and exact
append-only log. Accepted-effect replay does not append a second prompt/output.
If a process stops between acceptance and evidence binding, recovery may append
one matching trailer to an existing non-empty regular log or reuse exactly one
matching trailer; missing, empty, symlink/non-regular, duplicate, mismatched, or
later changed logs fail closed. The packet and system receipt re-open those
bindings instead of trusting a caller-created evidence object.

Reviewer and Critic strict call descriptors now own every semantic renderer
input and bind the real checked-in producer/template identity. Provider
rendering consumes only those descriptor bytes, and the Critic read scope is
frozen beside them. The first-strict Critic deliberately receives an explicit
no-strength contract and `None` evidence-snapshot scope; it never calls the
normal H2H snapshot producer. The previous-Critic value is reconstructed
identically before dispatch and after gate projection. Any journal, renderer,
context, prompt, log, or invocation-evidence violation found before dispatch,
during provider execution, or during receipt construction is classified as a
control-plane failure and uses the canonical abandon transaction with zero LLM
infrastructure retry debt. Final Master needs no additional mutable-log
dependency: its provider prompt/output/session/result are already bound by the
completed effect, accepted receipt, authority summary, and deterministic final
projection.

A final backend/frontend/actual-transition audit found three presentation
differences that existing green tests had not exercised. A schema-valid Critic
always advances with `approved=true` because its verdict is advisory, but the UI
was incorrectly using that execution bit as the recommendation; it now renders
the real `advisory_approved` value and shows an unavailable verdict when the
field is absent. The independent pipeline-checkpoint response now passes a
schema-2/positive-revision runtime parser before React state and its revision is
part of the full active-generation identity fence, so a same-stage older
response is withheld. The evolution initial-state projection also carries that
revision without inventing an SSE event. Finally, explicit `--no-daemon` mode
now treats a missing daemon PID as `not_applicable`, while enabled-but-missing
and disabled-but-live cases remain degraded. Dynamic negative-Critic,
old-revision, and both daemon-configuration regressions cover the real consumer
paths.

The final combined rerun also exposed an older managed-launch ownership race.
The host test inspected Bubblewrap's unique process-owner environment
immediately after `Popen`, while Bubblewrap could already be replacing that
environment for the sandbox. Under concurrent load `/proc/<pid>/environ`
occasionally appeared empty and made the quality gate nondeterministic. Owner-
marked launches now insert a one-shot Bubblewrap `--block-fd` pipe, verify the
exact single owner marker while execution is fenced, then write one byte to
release. A short bounded poll accepts only the directly observed transient empty
setup window; any non-empty mismatch fails immediately. Verification or release
failure terminates, waits, then kills/reaps if necessary, and every barrier,
seccomp, inherited, and callback-owned descriptor is closed. No-owner launches
keep their original argv, environment, and FD set. Unit tests cover command/FD
binding, mismatch cleanup, and the no-owner path; the original dynamic race ran
20/20 under concurrent load.

Final frozen-tree combined verification after the implementation and stale
fixture migrations was:

```text
cd web && python -m pytest tests -q
2554 passed, 20 skipped

python -m pytest sever/tests -q
31 passed

cd web/frontend && npm test
15 passed

cd web/frontend && npm run lint
passed

cd web/frontend && npm run build
passed
```

The current strict-authority recovery repair tree is green: all suites, active-source
`py_compile`, and `git diff --check` passed after the implementation stopped
changing. Its merged parent stream-ownership commit is
`0a26795aa71fa92049acebef94968a0c9f7553d7`. P5 baseline commit
`94714124711e51044f5a91411e47297f340fb265` was pushed and fast-forwarded to
`origin/main`, then synchronized into the stopped runtime. The schema-1
workflow-v18 checkpoint/candidate/ledger were durably quarantined through the
canonical reconciliation transaction, the evaluator identity was explicitly
rotated without carrying strength rows, and official doctor passed. The first
live workflow-v19 launch exposed and then recovered from the one-gen and
single-flight defects above. Workflow-v20 then supplied the stream and strict-
authority evidence recorded above. No complete three-proposal packet, ballots,
selected mechanism, compiled Worker plan, or `master_planned` stage was
accepted. No strict v143/v144 publication, immutable rating cycle, or
post-restart 10-generation observation has yet been executed.
The `fc7d62d30783d2ae8710dc8f331d717f3d902e36` verdict remains unchanged:
semantically superseded, history-only, and not cherry-picked.

### Rollback boundary

Infrastructure is synchronized to `.evolution_pok` only after focused/full
tests pass, the exact task commit reaches `origin/main`, and the runtime is at
a stopped safe point. A later contract-changing repair must repeat that gate;
the current v143 checkpoint is resumed or terminally abandoned only through
canonical recovery logic, never by deleting files.
