{
  "summary": "Deep multi-agent investigation of evolution pipeline logic bugs, stuck states, and daemon crashes",
  "agentCount": 9,
  "logs": [
    "Code audit complete: 4 audit reports",
    "Stuck state analysis complete",
    "Daemon crash analysis complete",
    "Critic loop analysis complete",
    "Crossover audit complete"
  ],
  "result": "# 演化系统综合修复计划\n\n基于对所有相关模块的完整代码审计，以下按严重程度和类别组织的详细修复方案。\n\n---\n\n## 1. 确认的 Bug（已确认缺陷）\n\n### Bug C1: `timed_out` 检查点导致流水线死锁（严重）\n\n**严重程度:** 严重\n**文件:** `/home/zzx/project/pok/web/core/tool_gates.py` 第203-205行\n**关联文件:** `/home/zzx/project/pok/web/core/orchestrator_session.py` 第86-132行, `/home/zzx/project/pok/web/core/orchestrator_context.py` 第104-116行, `/home/zzx/project/pok/web/core/evolution_infra.py` 第75-86行\n\n**问题描述:**\n当演化周期超时（`orchestrator.py` 第211行）时，`pipeline_state.json` 被写入 `stage: \"timed_out\"`。之后有两种恢复路径，均存在问题：\n\n1. `_startup_recovery()` 不处理 `timed_out` 阶段：它不在快速清除列表（`\"archived\"`, `\"prepared\"` 无 master_plan）中，也不在 `recoverable_stages` 中。如果检查点 < 30分钟，会走到通用 `\"resume\"` 路径。\n2. `prepare_next_gen()` 的门卫（`tool_gates.py` 第203-205行）拒绝覆盖非 `None`/`\"prepared\"` 的阶段：\n```python\nif _ckpt and _ckpt.get(\"stage\") not in (None, \"prepared\"):\n    return _json_tool_result({\"error\": f\"Pipeline for v{next_v} already at stage '{_ckpt['stage']}'. ...\"})\n```\n`\"timed_out\"` 不在允许列表中，所以 `prepare_next_gen` 工具会被阻塞。\n\n3. `orchestrator_context.py` 的 `stage_hints` 字典没有 `\"timed_out\"` 条目，LLM 只收到 `\"call get_status to assess\"` 的模糊提示。\n\n**复现路径:**\n1. 演化周期运行时间超过 `CYCLE_TIMEOUT`（3600秒）\n2. 超时处理器写入 `stage: \"timed_out\"` 到 `pipeline_state.json`\n3. 下一个周期尝试恢复，但无法推进或清除 `timed_out` 状态\n4. 如果 LLM 恰好调用 `prepare_next_gen`，会被拒绝并报错\n5. 系统陷入死锁，直到30分钟过期后的强制清除\n\n**建议修复方案（四处联动）:**\n\n修复1 -- `tool_gates.py` 第203-205行，允许 `timed_out` 被覆盖：\n```python\n# 修改前\nif _ckpt and _ckpt.get(\"stage\") not in (None, \"prepared\"):\n# 修改后\nif _ckpt and _ckpt.get(\"stage\") not in (None, \"prepared\", \"timed_out\"):\n```\n\n修复2 -- `orchestrator_session.py` 第88-97行，`_startup_recovery` 显式处理 `timed_out`：\n```python\n# 在第90行之后添加\nif stage == \"timed_out\":\n    # 超时的生成已废弃，清除检查点并从头开始\n    _LOG.info(\"Recovery: clearing timed_out checkpoint, starting fresh\")\n    clear_pipeline_checkpoint()\n    _clear_orchestrator_session()\n    return {\"action\": \"fresh_start\"}\n```\n\n修复3 -- `orchestrator_context.py` 第104-114行，添加 `timed_out` 提示：\n```python\nstage_hints = {\n    # ... 现有条目 ...\n    \"timed_out\": \"上一生成周期超时被废弃。调用 prepare_next_gen 从头开始新一轮演化。不要尝试恢复超时的工作。\",\n}\n```\n\n修复4 -- `evolution_infra.py` 第75行，将 `timed_out` 加入 `STAGE_ORDER`（仅作文档用途）：\n```python\nSTAGE_ORDER = [\"prepared\", \"direction_audited\", \"master_planned\", \"workers_done\",\n               \"quality_passed\", \"reviewed\", \"critic_checked\", \"verified\", \"archived\",\n               \"timed_out\"]\n```\n\n---\n\n### Bug C2: `.completed` 哨兵文件无 `git tag` 导致系统从未提交的代码演化（严重）\n\n**严重程度:** 严重\n**文件:** `/home/zzx/project/pok/web/core/evolution_infra.py` 第324-354行\n**关联文件:** `/home/zzx/project/pok/web/core/generation_scheduler.py` 第46-50行, `/home/zzx/project/pok/web/core/tool_gates.py` 第195-196行\n\n**问题描述:**\n`find_current_v()` 将 `.completed` 哨兵文件等同于 `git tag`（Source 2，第340-346行）。当前 `v17` 有 `.completed` 但无 `bot-v17` tag。系统认为 `v17` 是最新的活跃机器人，并尝试从其演化 `v18`。\n\n`prepare_next_gen` 工具（`tool_gates.py` 第195-196行）只检查 `.completed` 存在性，不验证 `git_has_tag`：\n```python\nif not source_dir.exists() or not (source_dir / \".completed\").exists():\n    return _json_tool_result({\"error\": f\"Source bot {source_dir.name} not found or incomplete\"})\n```\n\n**复现路径:**\n1. 周期在 `commit_bot` 之前崩溃或超时（但 `.completed` 已由某些中间步骤写入）\n2. `find_current_v()` 返回未提交的版本号\n3. 下一轮 `prepare_generation()` 使用该版本作为演化源\n4. 从可能损坏/不完整的代码演化\n\n**建议修复方案:**\n\n修复1 -- `evolution_infra.py` 第340-346行，`find_current_v()` 优先使用 `git tag`：\n```python\ndef find_current_v():\n    versions = set()\n    tag_versions = set()\n\n    # Source 1: git tags（权威来源）\n    tags = _git(\"tag\", \"-l\", \"bot-v*\", check=False).strip().splitlines()\n    for tag in tags:\n        try:\n            v = int(tag.replace(\"bot-v\", \"\"))\n            versions.add(v)\n            tag_versions.add(v)\n        except ValueError:\n            pass\n\n    # Source 2: .completed 哨兵文件（仅当有对应 tag 时才使用）\n    if BOTS_DIR.exists():\n        for d in os.listdir(BOTS_DIR):\n            if d.startswith(\"claude_v\") and os.path.isdir(BOTS_DIR / d):\n                if (BOTS_DIR / d / \".completed\").exists():\n                    try:\n                        v = int(d.split(\"_v\")[1])\n                        # 只接受有 tag 背书的版本\n                        if v in tag_versions:\n                            versions.add(v)\n                    except ValueError:\n                        pass\n\n    # Source 3: 保持原样（用于检测目录残留）\n    if BOTS_DIR.exists():\n        for d in os.listdir(BOTS_DIR):\n            if d.startswith(\"claude_v\") and os.path.isdir(BOTS_DIR / d):\n                try:\n                    v = int(d.split(\"_v\")[1])\n                    versions.add(v)  # 仅用于 max() 计算版本号\n                except ValueError:\n                    pass\n\n    return max(versions) if versions else 0\n```\n\n注意：Source 3 保留以确保 `next_v` 单调递增，但 Source 2 需要 `tag` 背书。\n\n修复2 -- `tool_gates.py` 第195-196行，添加 `git tag` 校验：\n```python\nif not source_dir.exists() or not (source_dir / \".completed\").exists():\n    return _json_tool_result({\"error\": f\"Source bot {source_dir.name} not found or incomplete\"})\n# 新增: 验证 git tag 存在\nif not git_has_tag(source_v):\n    return _json_tool_result({\n        \"error\": f\"Source bot v{source_v} has .completed but no git tag 'bot-v{source_v}'. \"\n                 f\"Cannot evolve from uncommitted code. Try a different source.\"\n    })\n```\n\n修复3 -- `generation_scheduler.py` 第46-50行，添加防御性校验：\n```python\ncurrent_v = find_current_v()\nactive_v = find_latest_active_v()\n\n# 防御: 确保源 bot 已提交\nif active_v > 0 and not git_has_tag(active_v):\n    _LOG.warning(f\"Latest active bot v{active_v} has no git tag, falling back\")\n    # 回退到最高有 tag 的版本\n    tag_v = find_current_v()  # 修复后的版本已优先 tag\n    if tag_v != active_v:\n        _LOG.warning(f\"Falling back from v{active_v} to v{tag_v}\")\n        active_v = tag_v\n```\n\n---\n\n### Bug C3: 529 重试路径无超时保护（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/orchestrator.py` 第248-264行\n\n**问题描述:**\n当第一次 `_stream_response` 因 `529` 限流失败时，重试代码在 `line 261` 直接调用 `await _stream_response(retry_opts)` 而没有 `asyncio.wait_for` 包装。如果重试也挂起，将无限阻塞。\n\n```python\n# 第184-189行: 第一次调用有 timeout 保护\nasync with asyncio.timeout(CYCLE_TIMEOUT):\n    full_output, total_cost, cycle_completed, query_gen, auth_error = (\n        await _stream_response(options)\n    )\n\n# 第261行: 重试调用无 timeout 保护！\nfull_output, retry_cost, cycle_completed, query_gen, auth_error = (\n    await _stream_response(retry_opts)  # 无 asyncio.wait_for!\n)\n```\n\n**复现路径:**\n1. 第一次 LLM 调用返回 `529` 限流\n2. 系统创建新会话重试\n3. 第二次调用也挂起（网络问题或 `API` 问题）\n4. 整个编排器线程永久阻塞\n\n**建议修复方案:**\n```python\n# 将第258-264行替换为:\nfor retry_attempt in range(3):  # 最多3次529重试\n    try:\n        full_output, retry_cost, cycle_completed, query_gen, auth_error = (\n            await asyncio.wait_for(\n                _stream_response(retry_opts),\n                timeout=CYCLE_TIMEOUT,\n            )\n        )\n        if not _is_rate_limited(full_output):\n            total_cost += retry_cost\n            break\n        # 仍然是529，准备下一次重试\n        _clear_orchestrator_session()\n        _LOG.warning(f\"529 rate limit on retry attempt {retry_attempt + 1}\")\n        await asyncio.sleep(min(30 * (2 ** retry_attempt), 120))  # 指数退避\n        retry_opts = ClaudeAgentOptions(...)\n    except asyncio.TimeoutError:\n        _LOG.error(f\"Timeout on 529 retry attempt {retry_attempt + 1}\")\n        if retry_attempt == 2:\n            raise  # 重试耗尽，向上抛出\n```\n\n---\n\n### Bug C4: `tool_eval.py` 中 `RESULTS_DIR` 未定义导致 `inline eval` 崩溃（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/tool_eval.py` 第340行\n\n**问题描述:**\n`run_inline_eval` 函数引用了 `RESULTS_DIR`，但该变量未在 `import` 列表中（第17-19行）：\n```python\nfrom evolution_core import (\n    get_bot_dir, get_active_bots, load_ratings, CORE_DIR,\n)\n# RESULTS_DIR 未导入!\n```\n\n第340行使用：\n```python\nhistory_file = RESULTS_DIR / \"rating_history.jsonl\"\n```\n\n运行时将触发 `NameError`。\n\n**复现路径:**\n1. 编排器 LLM 调用 `run_inline_eval` 工具\n2. 工具执行到第340行时崩溃\n3. 返回错误结果给 LLM\n\n**建议修复方案:**\n```python\n# tool_eval.py 第17行，添加 RESULTS_DIR 到导入\nfrom evolution_core import (\n    get_bot_dir, get_active_bots, load_ratings, CORE_DIR, RESULTS_DIR,\n)\n```\n\n---\n\n### Bug C5: Worker 重试声称已从源重置代码，但实际未重置（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/tool_planning.py` 第338-345行\n\n**问题描述:**\n当 `execute_workers` 因 `reviewer_feedback` 被重新调用时（`critic` 拒绝后的重试），代码附加了一条说明：\n```python\nif reviewer_feedback and ckpt.get(\"stage\") == \"workers_done\":\n    reviewer_feedback += (\n        f\"\\n\\nNOTE: This is a retry. The code in bots/claude_v{next_v}/ has been reset \"\n        f\"from source bots/claude_v{source_v}/. Any modifications described in the feedback \"\n        f\"above no longer exist in the code — you must re-implement them from scratch.\"\n    )\n```\n\n但 `next_dir` 中的代码从未被重置！Worker 在上一次尝试的修改基础上继续编辑，导致累积性、不可预测的复合变更。\n\n**复现路径:**\n1. `critic` 拒绝 `v17`（分数=5.0）\n2. 编排器调用 `execute_workers` 重试\n3. Worker 收到 \"代码已重置\" 的错误信息\n4. Worker 在已修改的代码上叠加新修改\n5. 产出不可预测的复合代码\n\n**建议修复方案 -- `tool_planning.py` 第338行之后添加实际重置逻辑:**\n```python\nif reviewer_feedback and ckpt.get(\"stage\") == \"workers_done\":\n    # 实际执行代码重置：从源 bot 复制干净副本\n    source_dir = get_bot_dir(source_v)\n    next_dir = get_bot_dir(next_v)\n    if source_dir.exists() and next_dir.exists():\n        import shutil\n        _LOG.info(f\"Resetting v{next_v} code from source v{source_v} before retry\")\n        # 保留 .completed 不要删除\n        for item in next_dir.iterdir():\n            if item.name != \".completed\":\n                if item.is_dir():\n                    shutil.rmtree(item)\n                else:\n                    item.unlink()\n        # 从源复制\n        for item in source_dir.iterdir():\n            if item.name != \".completed\":\n                if item.is_dir():\n                    shutil.copytree(item, next_dir / item.name)\n                else:\n                    shutil.copy2(item, next_dir / item.name)\n    \n    reviewer_feedback += (\n        f\"\\n\\nNOTE: This is a retry. The code in bots/claude_v{next_v}/ has been reset \"\n        f\"from source bots/claude_v{source_v}/. Any modifications described in the feedback \"\n        f\"above no longer exist in the code — you must re-implement them from scratch.\"\n    )\n```\n\n---\n\n### Bug C6: `Critic` 重试使用原始 `Master Plan`，导致重复失败（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/tool_planning.py` 第314-315行\n**关联文件:** `/home/zzx/project/pok/web/core/agent_review.py` 第21-90行\n\n**问题描述:**\n当 `critic` 拒绝后重试 `workers` 时，`execute_workers` 使用检查点中的原始 `master_plan tasks`：\n```python\ntasks = ckpt[\"master_plan\"].get(\"tasks\", [])\n```\n\n`Critic` 的反馈作为 `reviewer_feedback` 上下文传入，但 `master plan` 本身未重新生成。这导致 `workers` 被要求执行原始任务（例如调整后手弃牌阈值），而 `critic` 要求完全不同的变更（恢复翻前缺口处理器）。`v17` 的三次 `critic` 拒绝都源于此：每次重试都产生相同类型的变更。\n\n**复现路径:**\n1. `Master` 规划任务 A（调参）和任务 B（调参）\n2. `Workers` 执行任务，`critic` 拒绝（要求结构性变更）\n3. 编排器重试 `workers`，使用相同的任务 A/B\n4. `Workers` 再次调参，`critic` 再次拒绝\n5. 循环重复直到耗尽重试预算\n\n**建议修复方案:**\n\n方案 A（推荐）-- 在 `critic` 拒绝后重新调用 `Master`：\n在 `orchestrator.md` 提示中添加明确指引：\n```\n## Critic Rejection Protocol\n\nWhen the critic rejects with score < 6:\n1. If this is the FIRST rejection (generation_attempt < 2):\n   - Call execute_workers with reviewer_feedback to retry with same plan\n2. If this is the SECOND rejection (generation_attempt >= 2):\n   - Do NOT retry workers with the same plan\n   - Instead, call run_master with the critic's feedback as context to generate a NEW plan\n   - Then call execute_workers with the new plan\n3. If generation_attempt >= 3: force_advance=true or abandon the generation\n```\n\n方案 B（代码强制）-- 在 `tool_planning.py` 的 `execute_workers` 中添加逻辑：\n```python\n# 在第315行之后\ngeneration_attempt = ckpt.get(\"generation_attempt\", 0)\nif reviewer_feedback and generation_attempt >= 2:\n    # 强制要求新 plan\n    return _json_tool_result({\n        \"error\": f\"generation_attempt={generation_attempt}. Critic rejected twice with the same plan. \"\n                 f\"Call run_master first to generate a new plan incorporating critic feedback.\",\n        \"require_new_plan\": True,\n    })\n```\n\n---\n\n### Bug C7: Daemon 恢复代码的异常传播漏洞（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/elo_daemon.py` 第683-701行\n\n**问题描述:**\n`BrokenProcessPool` 恢复处理器在 `except` 块内创建新的 `ProcessPoolExecutor` 并调用 `executor.submit()`（第700行），但此代码不在 `try` 块内。如果 `forkserver` 进程已死，`submit()` 抛出 `ConnectionRefusedError`，该异常不被捕获，导致 `daemon` 致命退出（`rc=1`）。\n\n```python\n# 第683行: except 块开始\nexcept (BrokenProcessPool, BrokenPipeError, ConnectionResetError) as exc:\n    # ... drain futures (line 688-692) ...\n    executor = ProcessPoolExecutor(max_workers=num_workers, ...)  # line 693\n    # ...\n    executor.submit(...)  # line 700 -- 可能抛出 ConnectionRefusedError!\n# 第701行: except 块结束\n# 第703行: 外层 except Exception 捕获，但写入 crash log 并 re-raise\n```\n\n**复现路径:**\n1. `Worker` 进程被 `SIGKILL`（`OOM` 或手动）\n2. `BrokenProcessPool` 触发恢复处理器\n3. `Forkserver` 已死（同进程组，一起被杀）\n4. 新 `executor.submit()` 抛出 `ConnectionRefusedError`\n5. 异常传播到外层 `handler`，`daemon` 退出\n\n**建议修复方案 -- 用 `spawn` 替代 `forkserver` 并添加嵌套 `try/except`:**\n```python\nexcept (BrokenProcessPool, BrokenPipeError, ConnectionResetError) as exc:\n    _LOG.warning(f\"Process pool broken: {exc}, recovering...\")\n    # ... drain futures ...\n    try:\n        # 使用 spawn 上下文避免 forkserver 单点故障\n        import multiprocessing\n        mp_ctx = multiprocessing.get_context('spawn')\n        executor = ProcessPoolExecutor(\n            max_workers=num_workers,\n            mp_context=mp_ctx,\n        )\n        # 重新提交任务...\n        for _ in range(num_workers):\n            fut = executor.submit(_battle_worker, ...)\n            futures.add(fut)\n    except (ConnectionRefusedError, OSError) as recover_exc:\n        _LOG.error(f\"Failed to create new process pool: {recover_exc}\")\n        _LOG.error(\"Will retry on next save cycle\")\n        # 不 re-raise，让 daemon 继续运行，在下一个 save cycle 重试\n```\n\n---\n\n### Bug C8: Daemon 监控线程的崩溃计数器永不累积（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/daemon_management.py` 第186-225行\n\n**问题描述:**\n重启计数器 `restart_count` 在每次检查周期中被重置为 `0`。当 `daemon` 崩溃后，监控线程启动新 `daemon`（替换 `daemon_proc`）。在下一个3秒检查周期中，`old proc.poll()` 返回非 `None`（已死），但 `current_proc` 是新 `daemon`（存活且 `!= old proc`），所以触发了 \"intentionally replaced\" 分支，`restart_count` 被重置为 `0`。\n\n日志证据：所有重启事件都显示 `\"attempt 1\"`，从未出现 `attempt 2-5`。这意味着5次重启安全限制永远不会触发，`daemon` 会无限重启。\n\n**复现路径:**\n1. `Daemon` 崩溃（`rc=1`）\n2. 监控线程启动新 `daemon`，`restart_count = 1`\n3. 3秒后检查：`old proc` 已死，`new proc` 存活且不同 -> `restart_count = 0`\n4. `Daemon` 再次崩溃 -> 重复步骤 2-3\n5. 永远循环\n\n**建议修复方案:**\n```python\n# daemon_management.py，修改监控循环逻辑\nrestart_count = 0\nlast_crash_time = 0\n\nwhile not stop_event.is_set():\n    stop_event.wait(3.0)\n    if stop_event.is_set():\n        break\n\n    with _daemon_lock:\n        proc = daemon_proc\n\n    if proc is not None and proc.poll() is not None:\n        rc = proc.returncode\n        is_crash = rc != 0  # 非零退出码 = 崩溃\n\n        with _daemon_lock:\n            current_proc = daemon_proc\n\n        if current_proc is not None and current_proc is not proc and current_proc.poll() is None:\n            if is_crash:\n                # 崩溃导致的新进程不算\"有意图的替换\"\n                restart_count += 1\n            else:\n                # 正常退出（如 stop_daemon 调用）才是有意图的\n                restart_count = 0\n        else:\n            restart_count += 1\n\n        if restart_count > 5:\n            _LOG.error(f\"Daemon crashed {restart_count} times, giving up\")\n            break\n\n        backoff = min(3 * (2 ** min(restart_count - 1, 5)), 120)\n        _LOG.warning(f\"Daemon crashed (rc={rc}), restarting (attempt {restart_count}, backoff={backoff}s)\")\n        time.sleep(backoff)\n\n        try:\n            start_daemon()\n        except Exception as e:\n            _LOG.error(f\"Failed to restart daemon: {e}\")\n```\n\n---\n\n### Bug C9: `precommit_eval` 超时错误消息硬编码 \"900s\" 与实际不符（低）\n\n**严重程度:** 低\n**文件:** `/home/zzx/project/pok/web/core/tool_eval.py` 第151-155行\n\n**问题描述:**\n实际超时为 `max(300, n_games * 120)`（第115行），最大值为 `600s`（`n_games <= 5`），但错误消息硬编码为 `\"900s limit\"`。\n\n**建议修复方案:**\n```python\n# 第151-155行\nexcept _asyncio.TimeoutError:\n    matchup[\"error\"] = f\"Mirror battle timed out ({per_game_timeout}s limit)\"\n    blockers.append({\n        \"reason\": \"match_timeout\",\n        \"opponent\": opponent,\n        \"details\": f\"Mirror battle against {opponent} exceeded {per_game_timeout}s timeout\",\n    })\n```\n\n---\n\n### Bug C10: `_decide_strategy` 停滞检测覆盖 `recommended_source`（中）\n\n**严重程度:** 中\n**文件:** `/home/zzx/project/pok/web/core/generation_scheduler.py` 第149-197行\n\n**问题描述:**\n`_decide_strategy()` 按顺序评估条件。如果停滞被检测到（第161行），立即返回 `\"crossover\"`，永远不会到达 `recommended_source` 检查（第183行）。即使组合分析师明确建议从特定祖先演化（如 \"v13最强，从v13演化\"），停滞检测也会强制交叉。\n\n**建议修复方案:**\n```python\ndef _decide_strategy(current_v, active_v, analysis, gen_count):\n    stag = analysis.get(\"stagnation_info\", \"\")\n    pv = analysis.get(\"performance_verification\", \"\")\n    rec_source = analysis.get(\"recommended_source\")\n\n    # 优先使用分析师推荐的源（如果有明确推荐）\n    if rec_source and rec_source != current_v:\n        _LOG.info(f\"Strategy: using recommended source v{rec_source}\")\n        return \"master\", rec_source\n\n    # 然后检查停滞\n    if \"STAGNATION_DETECTED\" in stag:\n        # ... 现有停滞逻辑 ...\n```\n\n---\n\n### Bug C11: `run_crossover` 不验证父代的 `.completed` 和 `git tag`（高）\n\n**严重程度:** 高\n**文件:** `/home/zzx/project/pok/web/core/tool_commit.py` 第374-411行\n\n**问题描述:**\n`run_crossover` 只检查目标目录是否有 `.completed`（第396行），但不验证父代 `A/B` 的完整性：\n```python\n# 只检查目标，不检查父代\nif target_dir.exists() and (target_dir / \".completed\").exists():\n    return _json_tool_result({\"error\": \"Target already completed\"})\n```\n\n`MIN_CROSSOVER_DECISION_RATE = 0.6` 在 `evolution_infra.py` 第63行定义，但从未在任何地方使用。\n\n**建议修复方案:**\n```python\n# tool_commit.py，run_crossover 函数中添加验证\nasync def run_crossover(args):\n    parent_a = args.get(\"parent_a\")\n    parent_b = args.get(\"parent_b\")\n    target_v = args.get(\"target_v\")\n\n    # ... 现有解析逻辑 ...\n\n    # 新增: 验证父代完整性\n    for pv in (parent_a, parent_b):\n        p_dir = get_bot_dir(pv)\n        if not p_dir.exists():\n            return _json_tool_result({\"error\": f\"Parent v{pv} directory not found\"})\n        if not (p_dir / \".completed\").exists():\n            return _json_tool_result({\"error\": f\"Parent v{pv} is incomplete (no .completed)\"})\n        if not git_has_tag(pv):\n            return _json_tool_result({\"error\": f\"Parent v{pv} has no git tag 'bot-v{pv}'\"})\n\n    # 新增: 验证决策测试通过率\n    from decision_tester import run_decision_tests\n    for pv in (parent_a, parent_b):\n        results = run_decision_tests(get_bot_dir(pv))\n        if results and results.get(\"pass_rate\", 1.0) < MIN_CROSSOVER_DECISION_RATE:\n            return _json_tool_result({\n                \"error\": f\"Parent v{pv} decision test pass rate {results['pass_rate']:.0%} \"\n                         f\"below minimum {MIN_CROSSOVER_DECISION_RATE:.0%}\"\n            })\n\n    # 新增: 防止自交\n    if parent_a == parent_b:\n        return _json_tool_result({\"error\": \"Cannot crossover with self (parent_a == parent_b)\"})\n\n    # ... 继续现有逻辑 ...\n```\n\n---\n\n### Bug C12: `wait_for_daemon_eval` 的崩溃检测仅在 `games == 0` 时触发（中）\n\n**严重程度:** 中\n**文件:** `/home/zzx/project/pok/web/core/evolution_infra.py` 第469-476行\n\n**问题描述:**\n`daemon` 崩溃检测嵌套在 `if games == 0` 块内。如果 `daemon` 在产出部分游戏后崩溃（如15/100场），崩溃不会被检测到，函数会等到完整 `timeout`（600秒）过期。且 `proc` 引用在 `daemon` 自动重启后变得过时。\n\n**建议修复方案:**\n```python\n# 将 daemon 存活检查移到 games == 0 块外部\nwhile time.time() - start < timeout:\n    # ... 现有 ratings 加载逻辑 ...\n    games = stats.get(bot_name, {}).get(\"games\", 0)\n\n    # Daemon 存活检查（移出 if games == 0 块）\n    if daemon_proc is not None and daemon_proc.poll() is not None:\n        _LOG.warning(f\"Daemon process died (rc={daemon_proc.returncode}) during eval wait\")\n        if games >= min_games:\n            _LOG.info(f\"Have {games} games (>= {min_games}), proceeding anyway\")\n            break\n        else:\n            _LOG.warning(f\"Only {games} games, needed {min_games}. Waiting for daemon restart...\")\n            # 等待 daemon 重启\n            time.sleep(10)\n            # 重新获取 proc 引用\n            with _daemon_lock:\n                daemon_proc = daemon_proc  # 从全局获取新引用\n            continue\n\n    if games >= min_games:\n        break\n    if games == 0:\n        # ... 现有的长时间等待逻辑 ...\n```\n\n---\n\n## 2. 潜在风险（潜在风险）\n\n### R1: 非 `atomic save` 导致数据截断风险\n\n**文件:** `/home/zzx/project/pok/web/core/elo_daemon.py` -- `save_h2h`, `save_bot_stats`, `save_stats`\n**风险:** 这些函数使用 `locked_file(\"w\")` 的 `seek(0) + truncate()` 模式，而非 `tmp + fsync + os.replace` 的 `atomic` 模式。在 `truncate()` 之后、`json.dump()` 完成之前如果收到 `SIGKILL`，文件将被截断为空或不完整。\n**概率:** 低（窗口约10-50毫秒）\n**影响:** `h2h` 矩阵或 `bot_stats` 数据丢失，影响配对选择和统计\n\n**建议改进:**\n```python\ndef _atomic_json_write(filepath, data):\n    \"\"\"Atomic write using tmp + fsync + rename pattern.\"\"\"\n    tmp = filepath.with_suffix(\".tmp\")\n    with open(tmp, \"w\") as f:\n        json.dump(data, f, indent=2)\n        f.flush()\n        os.fsync(f.fileno())\n    os.replace(str(tmp), str(filepath))\n```\n将所有 `save_*` 函数改用此模式。\n\n### R2: `Crossover` 中 `copytree` 和 `.completed` 删除之间的崩溃\n\n**文件:** `/home/zzx/project/pok/web/core/agent_review.py` 第262-265行\n**风险:** 如果在 `shutil.copytree(parent_a_dir, target_dir)`（第264行）和 `.completed.unlink()`（第265行）之间崩溃，目标目录将包含父代 `A` 的代码和父代 `A` 的 `.completed` 哨兵。系统会认为这个版本已完成，阻止后续操作。\n**概率:** 极低（两行代码之间崩溃的窗口极小）\n**影响:** 中（需要手动删除 `.completed` 才能继续）\n\n**建议改进:**\n```python\n# 在 copytree 之前先删除目标（如果存在）\nif target_dir.exists():\n    shutil.rmtree(target_dir)\n# 先复制，再删除 .completed\nshutil.copytree(parent_a_dir, target_dir)\n(target_dir / \".completed\").unlink(missing_ok=True)\n```\n\n### R3: `_cleanup_incomplete` 读取检查点无文件锁\n\n**文件:** `/home/zzx/project/pok/web/core/generation_scheduler.py` 第283-293行\n**风险:** `checkpoint_file.read_text()` 无 `fcntl` 锁。如果编排器同时通过 `os.replace` 写入检查点，读取可能失败（被 `except Exception` 捕获），导致错误删除进行中的 `bot` 目录。\n**概率:** 极低（窗口极小，且 `os.replace` 是 `atomic` 的）\n**影响:** 中（删除正在工作的 `bot` 代码）\n\n**建议改进:**\n```python\n# 改用带锁的读取\nfrom evolution_infra import read_pipeline_checkpoint\nckpt = read_pipeline_checkpoint()\nif ckpt and ckpt.get(\"next_v\") == v and ckpt.get(\"stage\") not in (None, \"archived\", \"timed_out\"):\n    continue\n```\n\n### R4: `Critic` `force_advance` 无限使用风险\n\n**文件:** `/home/zzx/project/pok/web/core/tool_gates.py` 第388行, `tool_commit.py` 第88-89行\n**风险:** `force_advance=true` 完全绕过 `critic` 质量门，且无跨代限制。编排器 `LLM` 可以无限使用此标志跳过所有质量检查。\n**概率:** 低（取决于 `LLM` 行为）\n**影响:** 高（允许低质量代码提交）\n\n**建议改进:** 在 `tool_gates.py` 中添加跨代计数器：\n```python\n# 在 run_critic 中\nforce_advance_count = _ckpt.get(\"force_advance_count\", 0) if _ckpt else 0\nif force_advance and force_advance_count >= 3:\n    return _json_tool_result({\n        \"error\": f\"force_advance used {force_advance_count} times. \"\n                 f\"Maximum allowed is 3 consecutive force-advances.\"\n    })\n```\n\n### R5: `Orchestrator` `LLM` 可绕过 `_pick_crossover_parents` 选择任意父代\n\n**文件:** `/home/zzx/project/pok/web/core/tool_commit.py` 第374-411行\n**风险:** 编排器 `LLM` 可以直接调用 `run_crossover(parent_a=X, parent_b=Y)` 绕过调度器的 `_pick_crossover_parents()` 逻辑。`v15 x v11` 的配对就是 `LLM` 自主选择的，导致了近亲繁殖问题（`v11` 后代已被交叉5次）。\n**概率:** 中（`LLM` 已表现出此行为）\n**影响:** 中（降低遗传多样性）\n\n**建议改进:** 在 `run_crossover` 中添加血统检查：\n```python\n# 简单的近亲繁殖检测\ndef _count_shared_lineage(a, b, depth=3):\n    \"\"\"Check if a and b share recent common ancestors.\"\"\"\n    # 从 git log 中提取 parent 信息\n    # 如果共享祖先在 depth 代内，返回 True\n    ...\n```\n\n---\n\n## 3. 设计改进建议（设计改进建议）\n\n### D1: 统一检查点阶段管理\n\n当前 `timed_out` 阶段在多个文件中需要特殊处理（`STAGE_ORDER`、`STAGE_GATE_ALLOWLIST`、`stage_hints`、`_startup_recovery`、`_cleanup_incomplete`），但每个地方都独立判断。建议引入统一的阶段分类：\n\n```python\n# evolution_infra.py 中添加\nSTAGE_CATEGORIES = {\n    \"terminal\": {\"archived\"},           # 终态，可清除\n    \"recoverable\": {\"prepared\", \"direction_audited\", \"master_planned\",\n                    \"workers_done\", \"quality_passed\", \"reviewed\",\n                    \"critic_checked\", \"verified\"},\n    \"abnormal\": {\"timed_out\"},          # 异常态，需要特殊处理\n}\n\ndef is_terminal_stage(stage):\n    return stage in STAGE_CATEGORIES[\"terminal\"]\n\ndef is_recoverable_stage(stage):\n    return stage in STAGE_CATEGORIES[\"recoverable\"]\n\ndef is_abnormal_stage(stage):\n    return stage in STAGE_CATEGORIES[\"abnormal\"]\n```\n\n所有使用阶段判断的地方统一调用这些函数，避免分散的集合比较。\n\n### D2: `.completed` 哨兵与 `git tag` 强一致性\n\n当前 `.completed` 和 `git tag` 是两个独立的完成标记，可能导致不一致。建议：\n\n1. 只在 `commit_bot` 成功后（`git tag` 创建后）写入 `.completed`\n2. `find_current_v()` 只信任 `git tag`\n3. 添加启动时的不一致性检查：\n```python\ndef validate_bot_consistency():\n    \"\"\"Check for .completed without git tag, or vice versa.\"\"\"\n    inconsistencies = []\n    for d in BOTS_DIR.iterdir():\n        if d.name.startswith(\"claude_v\") and d.is_dir():\n            v = int(d.name.split(\"_v\")[1])\n            has_completed = (d / \".completed\").exists()\n            has_tag = git_has_tag(v)\n            if has_completed and not has_tag:\n                inconsistencies.append(f\"v{v}: .completed exists but no git tag\")\n                (d / \".completed\").unlink()  # 清理不一致的哨兵\n            elif has_tag and not has_completed:\n                (d / \".completed\").touch()  # 补全缺失的哨兵\n    return inconsistencies\n```\n\n### D3: `Critic` 拒绝后的 `Master` 重新规划机制\n\n当前 `critic` 拒绝后只能重试 `workers`（使用相同 `plan`）。建议添加 \"重新规划\" 路径：\n\n1. 在 `orchestrator.md` 中明确：`critic` 第二次拒绝后必须重新调用 `run_master`\n2. `run_master` 接受可选的 `critic_feedback` 参数\n3. `Master prompt` 在有 `critic_feedback` 时，生成完全不同的 `plan`\n4. 新 `plan` 覆盖检查点中的旧 `master_plan`\n\n### D4: `Daemon` 保存操作的 `Atomic` 写入\n\n将 `save_h2h`、`save_bot_stats`、`save_stats` 从 `truncate+write` 模式改为 `tmp+fsync+rename` 模式：\n\n```python\ndef _atomic_save(filepath, data):\n    \"\"\"Atomic JSON save.\"\"\"\n    tmp = filepath.with_suffix(\".tmp\")\n    with open(tmp, \"w\") as f:\n        json.dump(data, f)\n        f.flush()\n        os.fsync(f.fileno())\n    os.replace(str(tmp), str(filepath))\n```\n\n### D5: `ProcessPoolExecutor` 使用 `spawn` 代替 `forkserver`\n\n```python\n# elo_daemon.py 中修改 ProcessPoolExecutor 创建\nimport multiprocessing\nmp_ctx = multiprocessing.get_context('spawn')\nexecutor = ProcessPoolExecutor(max_workers=num_workers, mp_context=mp_ctx)\n```\n\n`spawn` 方法不依赖 `forkserver` 进程，消除了单点故障。虽然启动稍慢，但 `daemon` 是长驻进程，影响可忽略。\n\n### D6: 添加 `Crossover` 血统追踪\n\n```python\n# evolution_infra.py 中添加\ndef get_bot_lineage(v):\n    \"\"\"Extract parent info from git commit messages.\"\"\"\n    try:\n        msg = _git(\"log\", \"-1\", \"--format=%s\", f\"bot-v{v}\", check=False).strip()\n        # 解析 \"Evolve claude_vN\" 或 \"Crossover claude_vA x claude_vB\"\n        if \"Crossover\" in msg:\n            parts = msg.split(\"x\")\n            parent_a = int(parts[0].split(\"_v\")[1])\n            parent_b = int(parts[1].strip().split()[0].split(\"_v\")[1])\n            return (parent_a, parent_b)\n        elif \"Evolve\" in msg:\n            parent = int(msg.split(\"_v\")[1].split()[0])\n            return (parent,)\n    except Exception:\n        pass\n    return None\n\ndef _inbreeding_score(parent_a, parent_b, max_depth=4):\n    \"\"\"0.0 = no shared lineage, 1.0 = identical ancestry.\"\"\"\n    lineage_a = _get_ancestors(parent_a, max_depth)\n    lineage_b = _get_ancestors(parent_b, max_depth)\n    shared = lineage_a & lineage_b\n    return len(shared) / max(len(lineage_a | lineage_b), 1)\n```\n\n---\n\n## 4. 修复优先级排序（修复优先级排序）\n\n### 第一优先级（立即修复 -- 阻断性问题）\n\n| 序号 | `Bug ID` | 描述 | 预计工时 |\n|------|----------|------|----------|\n| 1 | `C1` | `timed_out` 死锁（4处联动修复） | 30分钟 |\n| 2 | `C2` | `.completed` 无 `tag` 导致从未提交代码演化 | 20分钟 |\n| 3 | `C3` | 529重试无超时保护 | 15分钟 |\n| 4 | `C4` | `inline eval` 的 `NameError`（一行修复） | 5分钟 |\n\n### 第二优先级（尽快修复 -- 影响质量/效率）\n\n| 序号 | `Bug ID` | 描述 | 预计工时 |\n|------|----------|------|----------|\n| 5 | `C5` | `Worker` 重试未实际重置代码 | 20分钟 |\n| 6 | `C6` | `Critic` 重试使用相同 `Master Plan` | 30分钟（含提示词修改） |\n| 7 | `C7` | `Daemon` 恢复代码异常传播 | 25分钟 |\n| 8 | `C8` | `Daemon` 重启计数器永不累积 | 20分钟 |\n| 9 | `C11` | `Crossover` 不验证父代完整性 | 20分钟 |\n\n### 第三优先级（近期修复 -- 正确性保障）\n\n| 序号 | `Bug ID` | 描述 | 预计工时 |\n|------|----------|------|----------|\n| 10 | `C10` | 停滞检测覆盖 `recommended_source` | 15分钟 |\n| 11 | `C12` | `Daemon` 崩溃检测不完整 | 15分钟 |\n| 12 | `C9` | 超时错误消息不准确（5分钟修复） | 5分钟 |\n\n### 第四优先级（计划改进 -- 架构优化）\n\n| 序号 | 设计 `ID` | 描述 | 预计工时 |\n|------|-----------|------|----------|\n| 13 | `D1` | 统一阶段管理 | 45分钟 |\n| 14 | `D2` | `.completed`/`tag` 强一致性 | 30分钟 |\n| 15 | `D3` | `Critic` 拒绝后重新规划 | 60分钟 |\n| 16 | `D4` | `Daemon atomic save` | 30分钟 |\n| 17 | `D5` | `ProcessPoolExecutor spawn` 模式 | 15分钟 |\n| 18 | `D6` | `Crossover` 血统追踪 | 60分钟 |\n\n---\n\n## 5. 重启前必须修复的问题（重启前必须修复的问题）\n\n在重启演化循环之前，以下问题必须修复：\n\n### 必须修复\n\n**1. `Bug C1` -- `timed_out` 死锁**\n当前系统在超时后无法自动恢复。如果重启后遇到超时，系统将陷入死锁直到30分钟过期。修复涉及4个文件但改动都很小（每处1-5行）。\n\n**2. `Bug C2` -- `.completed` 无 `tag`**\n当前 `v17` 有 `.completed` 无 `tag`。重启后 `find_current_v()` 返回 `17`，系统会从 `v17` 演化 `v18`，但 `v17` 可能包含不完整或有问题的代码。\n修复方案之外，还需要手动清理当前状态：\n```bash\n# 清理 v17 的不一致哨兵\nrm bots/claude_v17/.completed  # 因为没有对应的 git tag\n```\n\n**3. `Bug C4` -- `inline eval` 崩溃**\n一行修复。如果不修复，任何调用 `run_inline_eval` 的尝试都会崩溃。\n\n### 强烈建议同时修复\n\n**4. `Bug C3` -- 529重试无超时**\n如果在重启后遇到 `API` 限流，编排器可能无限阻塞。修改量小（约15分钟）。\n\n**5. `Bug C5` -- `Worker` 重试未重置代码**\n如果不修复，`critic` 拒绝后的 `worker` 重试会在脏代码上叠加修改，导致不可预测的结果。\n\n**6. `Bug C8` -- `Daemon` 重启计数器**\n如果不修复，`daemon` 崩溃时会无限重启，浪费资源。\n\n### 当前状态清理\n\n在应用修复并重启之前，需要清理当前的不一致状态：\n\n```bash\n# 1. 检查当前 pipeline_state.json\ncat web/core/results/pipeline_state.json\n# 如果 stage 不是 \"archived\" 或 \"timed_out\"，手动清理：\npython -c \"\nimport json, pathlib\np = pathlib.Path('web/core/results/pipeline_state.json')\nif p.exists():\n    data = json.loads(p.read_text())\n    print(f'Current state: stage={data.get(\\\"stage\\\")}, next_v={data.get(\\\"next_v\\\")}')\n    if data.get('stage') not in ('archived', None):\n        p.unlink()\n        print('Cleared stale checkpoint')\n\"\n\n# 2. 清理无 tag 的 .completed 哨兵\npython -c \"\nimport pathlib, subprocess\nbots = pathlib.Path('bots')\nfor d in sorted(bots.iterdir()):\n    if d.name.startswith('claude_v') and d.is_dir() and (d / '.completed').exists():\n        v = d.name.split('_v')[1]\n        result = subprocess.run(['git', 'tag', '-l', f'bot-v{v}'], capture_output=True, text=True)\n        if not result.stdout.strip():\n            print(f'WARNING: {d.name} has .completed but no git tag, removing .completed')\n            (d / '.completed').unlink()\n\"\n\n# 3. 清理编排器会话\nrm -f web/core/results/orchestrator_session.json\n```"
}