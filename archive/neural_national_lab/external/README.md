# External References

Use this ignored directory for external poker-AI reference clones while scanning
research and open-source implementations. Full clones are fine when disk space
is available; this directory's `.gitignore` keeps the vendored repositories out
of the project commit.

Commit distilled notes in `../research.md` or `../README.md`, not third-party
source trees or their nested git histories.
