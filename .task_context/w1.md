# Worker 1 Task Context: Check-Raise-Frequency Opponent Detector

## Why This Task (H2H Evidence)
v118 is the peak (h2h_avg_wr ~55%, Glicko r=1583 rd=97). The bot has a UNIVERSAL 0% postflop fold leak (confirmed v14→v123 across ~450 mirror halves). This caps win rate vs value-heavy opponents who barrel and check-raise for value:

| Opponent | H2H WR | Games | Style |
|----------|--------|-------|-------|
| v108 | 45.0% | 60 | value-barreler (AF=14 flop) |
| v96 | 45.7% | 70 | value-barreler (AF=16 flop) |
| v92 | 46.7% | 60 | SPR-commitment gate |
| v107 | 46.7% | 60 | value-heavy |
| v117 | 46.0% | 50 | thin-value raise tier |

River call-down rate is 60-86% across the field. Fat-loss count is the ONLY robust net predictor (~450 halves, 11 cases): fewer big-losses wins. The bot currently cannot fold marginal hands facing check-raises because:
1. `check_probe_resistance_margin()` only adds 0.035 to call margin (too small to flip decisions)
2. No HISTORICAL check-raise frequency tracking exists to distinguish value-check-raisers from bluff-check-raisers
3. The fold gates in `should_fold_postflop()` don't check for live check-raise spots

## EXHAUSTED Context (DO NOT DO These)
- Threshold/constant tuning (BLUFF_OPP, river_value_raise, sizing floors) — exhausted ≥6 gens
- Defensive fold-gate accumulation as a direction — prefer the NEW structural signal
- facing_barrel_continuation re-import — exhausted axis
- Crossover-as-default — requires NEW delivered offense to earn
- Broad modified constants in choose_raise() — saturated

## Birth Requirements (from experience_pool BLUFF_CALIBRATION)
NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs. Your implementation satisfies all 6:
1. NEW detector: `_count_opp_check_raises()`
2. NEW signal: `check_raise_freq` in opponent model + live `opp_current_round_check_then_bet` flag
3. ≥3 wired sites: `should_fold_postflop()` + `call_margin` + `jam_buffer`/`shove_buffer`
4. Cited folds: G4H31 (-20000 called turn all-in with pair-J), G9H55 (-8382 river all-in Q-high)
5. ≥30g gate: `opponent_model["confidence"] >= 0.20` (confidence is clamped at (total_actions-5)/35, so ≥0.20 ≈ ≥12 opp actions ≈ ≥30g)
6. Firing-rate logs: stderr telemetry in `_count_opp_check_raises` (optional)

## Replay Evidence (from match analysis spotlight)
- G4H31: turn all-in called with pair-of-Jacks weak kicker → -20000 (no fold despite check-raise signal)
- G9H55: river all-in shoved with Q-high no pair → -8382 (aggression into check-raiser)
- G6H13/G7H13: symmetric +20000/-20000 swings on check-call-check-call lines (variance, not skill)

## Measurement Targets
After implementation, expected impact:
- vs v108: 45% → 48%+ (≥60g H2H)
- vs v96: 45.7% → 49%+ (≥70g H2H)
- vs v92: 46.7% → 50%+ (≥60g H2H)
- Mirror vs parent v118: no regression (paired net-chips CI lower bound > -500)
- Firing rate: detector should fire in 5-15% of postflop decisions vs aggressive opponents
