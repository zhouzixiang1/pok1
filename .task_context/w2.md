# Worker 2 Task Context: Wire check_raise_freq into 3 Fold Sites

## Strategic Mandate (patches 0%-fold leak)
The bot has 0% postflop fold on EVERY postflop street across all 7 recent replays.
This causes -15k to -20k stack losses when behind at showdown. Worker 1 added a NEW
`check_raise_freq` signal to opponent_model. Your task: wire it into 3 LIVE fold/call
decision sites in strategy.py to patch the leak.

## H2H Evidence (why fold more vs check-raisers)
- v118 vs v121: 0.40 WR (20g). v121 has very high postflop AF (flop 15.9, turn 9.9).
- v118 vs v117: 0.43 WR (30g). v117 postflop AF: flop 18.4, turn 9.4, river 5.3.
- These modern opponents barrel aggressively; we never fold, we lose stacks.
- v94 (close-winner vs field, 0.57 vs v118) has 1% river all-in = disciplined.
- Losers (v50/v113/v121) shove 8-14% river all-in = spew. Our 0% fold exacerbates.

## Detect a LIVE Check-Raise (in current hand)
spot_info tracks per-round opponent actions:
- spot_info['opp_current_round_check_count'] >= 1 AND
- spot_info['opp_current_round_bet_count'] >= 1
→ Opponent has check-raised this round (heads-up: BB checks, we bet, BB raises).

## Boundary
- DO NOT touch: choose_raise() sizing constants, postflop.py, line_reading.py, opponent.py.
- DO NOT tune existing thresholds (hard ban on threshold-only tweaks).
- ONLY add new clauses that read opponent_model['check_raise_freq'] /
  ['check_raise_samples'] and adjust fold decisions.
- All 3 sites must be reachable from main get_action() flow (verify call sites).
