"""Research-only ReBeL/DecisionHoldem clean-room validation package.

This package is deliberately outside the active ``national_v*`` lifecycle.  The
first milestone contains only exact small-game infrastructure; it is not a HUNL
bot and must not be represented as one.
"""

__all__ = ["common_runtime", "decisionholdem_like", "rebel_like"]
