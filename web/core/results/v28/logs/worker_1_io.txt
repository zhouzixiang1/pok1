
[WORKER 1 (Algorithmic Logic Architect) PROMPT]
=============================
# System Rule
You are a highly skilled Coding Worker Agent playing the role of: **Algorithmic Logic Architect**.
You must directly edit the source files in `bots/claude_v28/` to implement the Master's instructions.
The bot MUST correctly interface with the game engine via `main.py` (reads JSON from stdin, writes JSON `{"response": int}` to stdout).

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files. Example: Read `bots/claude_v28/strategy.py`
- **Use the Bash tool** to run compile checks, smoke tests, and git commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Role Boundary Rules
Your role determines what you CAN and CANNOT do:

## If you are a "Hyperparameter Tuner":
- **ALLOWED**: Changing numeric constants (e.g., `BLUFF_THRESHOLD = 0.15` → `0.20`)
- **ALLOWED**: Adjusting float thresholds (e.g., `POT_ODDS_CUTOFF = 2.0` → `2.3`)
- **ALLOWED**: Modifying integer conditions (e.g., `MIN_RAISE_HAND = 8` → `10`)
- **FORBIDDEN**: Adding new functions, classes, or methods
- **FORBIDDEN**: Adding new `if/for/while` blocks or changing control flow
- **FORBIDDEN**: Importing new modules
- If you accidentally add any non-numeric changes, revert with `git checkout -- <file>`

## If you are an "Algorithmic Logic Architect":
- **ALLOWED**: Adding new functions and methods
- **ALLOWED**: Refactoring existing logic and algorithms
- **ALLOWED**: Adding imports and new modules
- **FORBIDDEN**: Changing well-tuned constants unless structurally required
- Example of "structurally required": If your new function changes how a constant is used, you may adjust it. But don't randomly tune `OPENING_RANGE_THRESHOLD` or `VPIP_PRIORS`.

## If you are an "Opponent Modeler":
- **ALLOWED**: Adding/modifying opponent tracking data structures and statistics
- **ALLOWED**: Per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
- **ALLOWED**: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list (cap at 20), `statistics.median()`
- **ALLOWED**: Exploitative adjustments based on detected opponent tendencies
- **FORBIDDEN**: Changing overall decision flow or non-opponent-model logic
- **FORBIDDEN**: Modifying any constant that is not opponent-model related

You have access to `web/core/reference_bots/` containing 6 strong bots (`bot1` to `bot6`). You may read them as reference.

**Important — Parallel Execution**: You may be running concurrently with other Worker Agents on the same codebase. Your `target_files` are exclusively yours — other workers will NOT touch them. Only modify your assigned `target_files`. Do NOT read or write files outside your assignment.

# Poker Engineering Best Practices
Apply these when implementing changes:

**Opponent Modeling:**
- Use per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']` where `street ∈ {'preflop', 'flop', 'turn', 'river'}`
- Track bet sizing: `opp_bet_sizes[street]` as a rolling list (cap at last 20), use `statistics.median()` for typical size
- Exploit detected tendencies: if `opp_stats['river']['bet_freq'] > 0.65` → opponent bluffs river heavily → widen call range

**Postflop Decision Making:**
- EV-based fold/call: `EV_call = equity * pot_after_call - (1 - equity) * call_amount`. Call when EV > 0
- River value bet sizing: if equity > 70%, bet 60-80% of pot for value; if equity 50-70%, bet 40-50% pot
- Avoid "fear thresholds" — don't fold based on hand rank alone; compute EV against opponent range

**Anti-Regression Checklist (verify before finishing):**
- [ ] AA/KK/QQ preflop: still raises/3-bets aggressively (no passive limping)?
- [ ] Nut flush/straight/full house on river: still places a value bet?
- [ ] New code doesn't import modules not in stdlib + numpy?
- [ ] `{"response": int}` JSON output format still correct?
- [ ] No infinite loops or recursion in new functions?

# Master Architect's Specific Prompt For You
You are an Algorithmic Logic Architect. You are working on a Texas Hold'em poker bot for Botzone (heads-up No-Limit). The bot has been branched from claude_v8 (highest rated at 1631.6) to break a 9-generation stagnation plateau.

## Problem
The current bot pool cannot beat mid-tier opponents. Against bots like v7, the win rate is only ~50%. The population has converged around a narrow strategic niche and needs a fundamentally different approach.

## Your Task: Add Board Texture Analysis + Position-Aware Postflop Aggression

### Step 1: Read the current bot code
Read `bots/claude_v8/main.py` thoroughly. Understand the existing decision pipeline.

### Step 2: Add Board Texture Classification
Create a new function `classify_board_texture(board_cards)` that returns one of: 'dry', 'semi-wet', 'wet', 'completed'. Logic:
- Count flush draws (3+ cards of same suit on board = wet)
- Count straight draws (connected ranks within 4-gap = semi-wet)
- Count paired boards (paired = semi-wet, trips+ = completed)
- Dry boards: rainbow, unconnected (e.g., K-7-2 rainbow)

### Step 3: Add Position-Aware Aggression Scaling
Create a function `get_position_aggression_factor(my_hand, board_cards, street, pot_size, is_sb)` that:
- On dry boards: INCREASE bluff/semi-bluff frequency (opponent less likely to have connected)
- On wet boards: DECREASE bluff frequency, tighten value betting range
- As SB (acting first postflop): use more check-raise lines on dry boards
- As BB: use more donk-bet lines on wet boards to deny free cards
- On completed boards (straight/flush possible): play purely value-oriented, no bluffs

### Step 4: Integrate Into Decision Pipeline
Find where the bot makes postflop decisions. Insert the board texture check BEFORE the bet sizing calculation. The decision flow should be:
1. Evaluate hand strength (existing)
2. Classify board texture (NEW)
3. Get position-aware aggression factor (NEW)
4. Combine hand_strength * aggression_factor to determine action

### Critical Rules
- DO NOT modify preflop logic (Worker 2 handles constants)
- DO NOT change any numeric constants or thresholds — only add NEW functions and call them
- Keep the file under 1000 lines
- The bot protocol: JSON on stdin/stdout, 0=check/call, -1=fold, -2=all-in, >0=raise amount
- Cards are integers 0-51. number = card // 4 + 2, suit = card % 4
- Test with: `python -c "import py_compile; py_compile.compile('bots/claude_v8/main.py', doraise=True)"`

# Recent Worker Failures (avoid repeating these mistakes):
- Gen 22 Worker 1 (Algorithmic Logic Architect): unknown
- Gen 25 Worker 1 (Algorithmic Logic Architect): unknown

# Action
Please write the Python code to fulfill your objective. After editing:

1. **Verify your changes**: Run `git diff bots/claude_v28/` to review what you changed.
   Ensure no unintended modifications to files outside your assigned `target_files`.
2. If you see unexpected changes, fix them before finishing.
3. **Run quality checks**:
   - Compile check: `python -m py_compile bots/claude_v28/main.py` (and other .py files you changed)
   - Smoke test: `python web/core/smoke_tester.py bots/claude_v28/main.py`
   - If any check fails, fix the errors before finishing.

# Self-Review Before Finishing
After all quality checks pass, run these final checks:

1. **Role Boundary Check**: Run `git diff bots/claude_v28/` one more time and review your changes carefully.
   - If you are a **Hyperparameter Tuner**: Verify that ALL your changes are limited to numeric constants, thresholds, or magic numbers. If you added new functions, classes, or control flow logic (if/for/while blocks), REMOVE them immediately.
   - If you are an **Algorithmic Logic Architect**: Verify that you did not change well-tuned constants (thin_cap, open thresholds, VPIP/PFR priors) unless structurally required by your new algorithm.

2. **Target File Check**: Ensure you ONLY modified files in your assigned `target_files`. If you touched other files, revert those changes with `git checkout -- <file>`.

3. **Protocol Check**: Verify the bot still outputs `{"response": <int>}` via stdout. The action encoding is: 0=call/check, -1=fold, -2=all-in, >0=raise amount.

4. If you find any violations, fix them and re-run the quality checks.

=============================
[CLAUDE OUTPUT]
Let me read the files using Bash since they're on the local filesystem.
Let me read the rest of the strategy.py and postflop.py files, and also the other supporting files:
Now I have a thorough understanding of the codebase. Let me identify exact integration points:
Now I have a complete picture. Let me create the new file and modify strategy.py. Here's my plan:

1. **Create** `bots/claude_v28/board_texture.py` — new module with `classify_board_texture()` and `get_position_aggression_factor()`
2. **Edit** `bots/claude_v28/strategy.py` — import the new module and integrate the aggression factor

Let me start:
Now modify `strategy.py` to import and integrate the new module:
Now add the aggression factor computation right after the `board_texture` line. Let me find the exact location:
Now add the threshold adjustment after the anti_bot4 adjustments:
Now adjust the bluff thresholds. Let me find the exact lines:
Now let's verify the changes compile and the file sizes are within limits:
All files compile and are under 1000 lines. Now let's run the smoke test:
Excellent! Smoke test passes. Let me now review the diff:
Good - the v28 directory is untracked (new bot). Let me verify only my intended files were changed:
The diff looks clean. Let me do the final self-review checklist:

**✅ Role Boundary Check (Algorithmic Logic Architect):**
- Added NEW functions: `classify_board_texture()`, `get_position_aggression_factor()` → ✅ Allowed
- Added NEW file: `board_texture.py` → ✅ Allowed
- Added import in strategy.py → ✅ Allowed
- Did NOT change any well-tuned constants (strong/medium base values, VPIP priors, etc.) → ✅ Only adjusting thresholds dynamically via the new factor
- Did NOT modify preflop logic → ✅

**✅ Target File Check:**
- Only created/modified files in `bots/claude_v28/` → ✅

**✅ Protocol Check:**
- Bot still outputs `{"response": <int>}` via stdout (no changes to main.py) → ✅
- Action encoding unchanged (0=call/check, -1=fold, -2=all-in, >0=raise) → ✅

**✅ Quality Checks:**
- Compile check: all files pass → ✅
- Smoke test: passed → ✅
- strategy.py: 977 lines (under 1000) → ✅
- board_texture.py: 134 lines (under 1000) → ✅
- postflop.py: 999 lines (unchanged, under 1000) → ✅
- No new non-stdlib imports → ✅ (only uses `card_utils` from local modules)

**✅ Anti-Regression Checklist:**
- AA/KK/QQ preflop: untouched, still raises/3-bets aggressively → ✅
- Nut hands on river: untouched, still places value bets → ✅
- No infinite loops or recursion → ✅

Let me run one more quick test to make sure the bot handles edge cases correctly:
All classifications and aggression factors are working correctly:

| Board | Position | Factor | Effect |
|-------|----------|--------|--------|
| K-7-2 rainbow (dry) | SB | 1.11 | +11% wider range |
| A-K-7 same suit (wet) | BB | 0.97 | -3% tighter range |
| K-K-K trips (completed) | SB | 0.86 | -14% much tighter |
| Preflop | - | 1.0 | Neutral (unchanged) |

The implementation is complete. Here's a summary of what was added:

### New File: `bots/claude_v28/board_texture.py` (134 lines)
- **`classify_board_texture(board_cards)`** — Returns `'dry'`, `'semi-wet'`, `'wet'`, or `'completed'` based on flush draws (3+ same suit), straight draws (4+ connected ranks within 4-gap), paired boards, trips/quads, and completed straights
- **`get_position_aggression_factor(my_hand, board_cards, street, pot_size, is_sb)`** — Returns a multiplier (0.86–1.14) that adjusts strategy based on texture × position:
  - Dry boards: +7–14% aggression (opponent likely missed)
  - Wet boards: -3–10% caution (draws present)
  - Completed boards: -10–14% (purely value-oriented)

### Modified: `bots/claude_v28/strategy.py` (+17 lines, total 977)
1. **Import** of new module
2. **Board classification + aggression factor** computed right after existing `board_texture_profile()`
3. **Threshold adjustment**: `strong` and `medium` thresholds scaled by aggression factor (postflop only)
4. **Bluff threshold adjustment**: River bluff, probe, and semi-bluff thresholds scaled inversely by texture (dry = easier to bluff, wet = harder)
