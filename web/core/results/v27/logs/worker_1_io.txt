
[WORKER 1 (Algorithmic Logic Architect) PROMPT]
=============================
# System Rule
You are a highly skilled Coding Worker Agent playing the role of: **Algorithmic Logic Architect**.
You must directly edit the source files in `bots/claude_v27/` to implement the Master's instructions.
The bot MUST correctly interface with the game engine via `main.py` (reads JSON from stdin, writes JSON `{"response": int}` to stdout).

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files. Example: Read `bots/claude_v27/strategy.py`
- **Use the Bash tool** to run compile checks, smoke tests, and git commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Role Boundary Rules
Your role determines what you CAN and CANNOT do:

## If you are a "Hyperparameter Tuner":
- **ALLOWED**: Changing numeric constants (e.g., `BLUFF_THRESHOLD = 0.15` → `0.20`)
- **ALLOWED**: Adjusting float thresholds (e.g., `POT_ODDS_CUTOFF = 2.0` → `2.3`)
- **ALLOWED**: Modifying integer conditions (e.g., `MIN_RAISE_HAND = 8` → `10`)
- **FORBIDDEN**: Adding new functions, classes, or methods
- **FORBIDDEN**: Adding new `if/for/while` blocks or changing control flow
- **FORBIDDEN**: Importing new modules
- If you accidentally add any non-numeric changes, revert with `git checkout -- <file>`

## If you are an "Algorithmic Logic Architect":
- **ALLOWED**: Adding new functions and methods
- **ALLOWED**: Refactoring existing logic and algorithms
- **ALLOWED**: Adding imports and new modules
- **FORBIDDEN**: Changing well-tuned constants unless structurally required
- Example of "structurally required": If your new function changes how a constant is used, you may adjust it. But don't randomly tune `OPENING_RANGE_THRESHOLD` or `VPIP_PRIORS`.

## If you are an "Opponent Modeler":
- **ALLOWED**: Adding/modifying opponent tracking data structures and statistics
- **ALLOWED**: Per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
- **ALLOWED**: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list (cap at 20), `statistics.median()`
- **ALLOWED**: Exploitative adjustments based on detected opponent tendencies
- **FORBIDDEN**: Changing overall decision flow or non-opponent-model logic
- **FORBIDDEN**: Modifying any constant that is not opponent-model related

You have access to `web/core/reference_bots/` containing 6 strong bots (`bot1` to `bot6`). You may read them as reference.

**Important — Parallel Execution**: You may be running concurrently with other Worker Agents on the same codebase. Your `target_files` are exclusively yours — other workers will NOT touch them. Only modify your assigned `target_files`. Do NOT read or write files outside your assignment.

# Poker Engineering Best Practices
Apply these when implementing changes:

**Opponent Modeling:**
- Use per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']` where `street ∈ {'preflop', 'flop', 'turn', 'river'}`
- Track bet sizing: `opp_bet_sizes[street]` as a rolling list (cap at last 20), use `statistics.median()` for typical size
- Exploit detected tendencies: if `opp_stats['river']['bet_freq'] > 0.65` → opponent bluffs river heavily → widen call range

**Postflop Decision Making:**
- EV-based fold/call: `EV_call = equity * pot_after_call - (1 - equity) * call_amount`. Call when EV > 0
- River value bet sizing: if equity > 70%, bet 60-80% of pot for value; if equity 50-70%, bet 40-50% pot
- Avoid "fear thresholds" — don't fold based on hand rank alone; compute EV against opponent range

**Anti-Regression Checklist (verify before finishing):**
- [ ] AA/KK/QQ preflop: still raises/3-bets aggressively (no passive limping)?
- [ ] Nut flush/straight/full house on river: still places a value bet?
- [ ] New code doesn't import modules not in stdlib + numpy?
- [ ] `{"response": int}` JSON output format still correct?
- [ ] No infinite loops or recursion in new functions?

# Master Architect's Specific Prompt For You
You are an Algorithmic Logic Architect. Your CRITICAL mission: fix the #1 bug — postflop fold logic is NOT executing in this bot. Previous generations (v25, v26) added complex postflop fold code in helper functions, but those code paths were never reached during actual gameplay. The bot currently has 0% fold rate on flop, turn, AND river — it is a pure calling station.

## WHY PREVIOUS ATTEMPTS FAILED
The v25 and v26 bots added fold logic in separate functions (SPR-based analysis, opponent profiling helpers) that were either never called or were placed AFTER a catch-all call/raise default in the decision flow. The result: the bot always falls through to calling.

## YOUR APPROACH — RADICALLY SIMPLER
Do NOT add new helper functions. Do NOT create new classes. Instead:

1. Find the main postflop decision function (the one that actually returns the final action for flop/turn/river streets).

2. At the VERY TOP of this function, BEFORE any other logic, add a simple, direct fold gate:

```python
# POSTFLOP FOLD GATE — first thing checked, cannot be bypassed
if opp_bet > 0:  # opponent has bet
    pot_ratio = opp_bet / (pot + opp_bet + my_bet if pot + opp_bet + my_bet > 0 else 1)
    
    # Evaluate hand strength using EXISTING evaluation (whatever the bot already has)
    hand_strength = <use existing hand evaluation function, 0.0 to 1.0>
    
    # Simple fold thresholds per street
    fold_threshold = {0: 0.35, 1: 0.40, 2: 0.45}  # flop/turn/river
    
    # Fold if hand is weak AND opponent bet is significant
    if hand_strength < fold_threshold.get(street, 0.40) and pot_ratio >= 0.33:
        return -1  # FOLD — do not pass Go, do not collect $200
```

3. CRITICAL REQUIREMENTS:
   - The fold gate MUST be the FIRST thing checked in the postflop decision path
   - It MUST use a simple `return -1` directly — no flags, no variables, no fallthrough
   - Use whatever hand evaluation function ALREADY EXISTS in the bot's code (don't reinvent it)
   - The fold_threshold values should cause folding roughly 15-25% of the time when facing bets
   - On the RIVER, be slightly MORE willing to fold (higher threshold) since bluffing is less likely
   - On the FLOP, be slightly MORE willing to call (lower threshold) since draws can improve

4. VERIFICATION: After adding the fold gate, trace through the code mentally:
   - When the bot has a weak hand (e.g., low pair, no draw) and faces a half-pot bet on the turn, does it reach the fold gate? YES — it's the first check.
   - When the bot has a strong hand (e.g., top pair good kicker), does it bypass the fold gate? YES — hand_strength exceeds threshold.
   - Is there any code path that could override or skip this gate? NO — it returns immediately.

5. DO NOT:
   - Add new functions, classes, or files
   - Change preflop logic (it was improved, leave it alone)
   - Modify bet sizing (that's Worker 2's job)
   - Add SPR calculations or opponent profiling (these failed before)
   - Change any constants or thresholds OTHER than the fold thresholds described above

6. TEST: Ensure the code compiles with `py_compile`. The fold gate should be ~10-15 lines of code total. If it's more than 20 lines, you're overcomplicating it.

# Recent Worker Failures (avoid repeating these mistakes):
- Gen 22 Worker 1 (Algorithmic Logic Architect): unknown
- Gen 25 Worker 1 (Algorithmic Logic Architect): unknown

# Action
Please write the Python code to fulfill your objective. After editing:

1. **Verify your changes**: Run `git diff bots/claude_v27/` to review what you changed.
   Ensure no unintended modifications to files outside your assigned `target_files`.
2. If you see unexpected changes, fix them before finishing.
3. **Run quality checks**:
   - Compile check: `python -m py_compile bots/claude_v27/main.py` (and other .py files you changed)
   - Smoke test: `python web/core/smoke_tester.py bots/claude_v27/main.py`
   - If any check fails, fix the errors before finishing.

# Self-Review Before Finishing
After all quality checks pass, run these final checks:

1. **Role Boundary Check**: Run `git diff bots/claude_v27/` one more time and review your changes carefully.
   - If you are a **Hyperparameter Tuner**: Verify that ALL your changes are limited to numeric constants, thresholds, or magic numbers. If you added new functions, classes, or control flow logic (if/for/while blocks), REMOVE them immediately.
   - If you are an **Algorithmic Logic Architect**: Verify that you did not change well-tuned constants (thin_cap, open thresholds, VPIP/PFR priors) unless structurally required by your new algorithm.

2. **Target File Check**: Ensure you ONLY modified files in your assigned `target_files`. If you touched other files, revert those changes with `git checkout -- <file>`.

3. **Protocol Check**: Verify the bot still outputs `{"response": <int>}` via stdout. The action encoding is: 0=call/check, -1=fold, -2=all-in, >0=raise amount.

4. If you find any violations, fix them and re-run the quality checks.

=============================
[CLAUDE OUTPUT]
Let me read the bot's source files to understand the current decision flow.
Let me search for the main postflop decision function in strategy.py:
Now let me look at the `get_action` function in detail - this is the main entry point:
Now let me understand the exact structure. Let me see where the postflop metrics are computed and where decisions start:
Let me check the `made_hand_metric` function to understand its scale:
Now I have a clear understanding of the code. Let me trace the critical path:

1. `get_action()` (line 487) is the main entry point
2. Preflop returns early at line 548
3. Lines 550-597 compute postflop metrics (made_strength, win_rate, draw_info, etc.)
4. Lines 598+: Decision logic starts with river overbets
5. Lines 688+: All-in handling, then at line 735 the `to_call > 0` block
6. **Line 910: `return 0`** — the catch-all that makes it a calling station

The problem is clear: even though there are fold conditions in the `to_call > 0` block (lines 830, 835, 838), they are too specific and rarely trigger. The final `return 0` on line 910 catches everything.

I need to add a fold gate right after the metrics are computed (line 597) and before any decision logic. Let me implement it:
Now I'll insert the fold gate right after the anti_bot4 setup (line 597) and before the river overbet logic:
Good, the file was modified. Let me verify the insertion:
The fold gate is inserted correctly. Now let me run the quality checks:
All checks pass. Now let me do the final review:
The directory is untracked (new bot version). Let me verify the change is correct by examining the exact fold gate code in context:
Now let me run the final self-review checklist:

**1. Role Boundary Check (Algorithmic Logic Architect):**
- ✅ Added new logic (fold gate) — allowed
- ✅ Did NOT change any well-tuned constants (strong, medium, call_margin thresholds, etc.)
- ✅ The fold_threshold values are NEW constants required by the new algorithm, not modifications of existing ones

**2. Target File Check:**
- ✅ Only modified `bots/claude_v27/strategy.py` (assigned target file)

**3. Protocol Check:**
- ✅ The fold gate returns `-1` which is the fold action
- ✅ The main function still outputs `{"response": int(action)}` via stdout
- ✅ Action encoding preserved: 0=call/check, -1=fold, -2=all-in, >0=raise

**4. Anti-Regression Checklist:**
- ✅ AA/KK/QQ preflop: Fold gate only fires when `round_idx > 0`, preflop logic unchanged
- ✅ Nut flush/straight/full house on river: These hands have `win_rate >> 0.45`, fold gate won't trigger
- ✅ No new imports needed — uses only `win_rate`, `pot`, `to_call`, `round_idx` (all existing variables)
- ✅ `{"response": int}` JSON output format unchanged
- ✅ No infinite loops or recursion — simple if-check with immediate return

**5. Mental trace-through:**
- Weak hand (win_rate=0.25), half-pot bet on turn → `round_idx=2 > 0`, `to_call > 0`, `pot_ratio=0.33 >= 0.33`, `0.25 < 0.40` → **FOLDS** ✅
- Strong hand (win_rate=0.65), any bet → `0.65 > 0.45` → **DOES NOT fold** ✅  
- Flush draw (win_rate=0.42 via Monte Carlo with draw equity), flop → `0.42 > 0.35` → **DOES NOT fold** ✅
- Preflop (round_idx=0) → **fold gate doesn't fire** ✅
- No bet (to_call=0) → **fold gate doesn't fire** ✅
- Anti-lock pressure → **fold gate doesn't fire** (must continue) ✅

The implementation is complete — 5 lines of actual logic, inserted at the very first point in the postflop decision path where all required variables are available, with an immediate `return -1` that cannot be bypassed.

[WORKER 1 (Algorithmic Logic Architect) PROMPT]
=============================
# System Rule
You are a highly skilled Coding Worker Agent playing the role of: **Algorithmic Logic Architect**.
You must directly edit the source files in `bots/claude_v27/` to implement the Master's instructions.
The bot MUST correctly interface with the game engine via `main.py` (reads JSON from stdin, writes JSON `{"response": int}` to stdout).

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files. Example: Read `bots/claude_v27/strategy.py`
- **Use the Bash tool** to run compile checks, smoke tests, and git commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Role Boundary Rules
Your role determines what you CAN and CANNOT do:

## If you are a "Hyperparameter Tuner":
- **ALLOWED**: Changing numeric constants (e.g., `BLUFF_THRESHOLD = 0.15` → `0.20`)
- **ALLOWED**: Adjusting float thresholds (e.g., `POT_ODDS_CUTOFF = 2.0` → `2.3`)
- **ALLOWED**: Modifying integer conditions (e.g., `MIN_RAISE_HAND = 8` → `10`)
- **FORBIDDEN**: Adding new functions, classes, or methods
- **FORBIDDEN**: Adding new `if/for/while` blocks or changing control flow
- **FORBIDDEN**: Importing new modules
- If you accidentally add any non-numeric changes, revert with `git checkout -- <file>`

## If you are an "Algorithmic Logic Architect":
- **ALLOWED**: Adding new functions and methods
- **ALLOWED**: Refactoring existing logic and algorithms
- **ALLOWED**: Adding imports and new modules
- **FORBIDDEN**: Changing well-tuned constants unless structurally required
- Example of "structurally required": If your new function changes how a constant is used, you may adjust it. But don't randomly tune `OPENING_RANGE_THRESHOLD` or `VPIP_PRIORS`.

## If you are an "Opponent Modeler":
- **ALLOWED**: Adding/modifying opponent tracking data structures and statistics
- **ALLOWED**: Per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
- **ALLOWED**: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list (cap at 20), `statistics.median()`
- **ALLOWED**: Exploitative adjustments based on detected opponent tendencies
- **FORBIDDEN**: Changing overall decision flow or non-opponent-model logic
- **FORBIDDEN**: Modifying any constant that is not opponent-model related

You have access to `web/core/reference_bots/` containing 6 strong bots (`bot1` to `bot6`). You may read them as reference.

**Important — Parallel Execution**: You may be running concurrently with other Worker Agents on the same codebase. Your `target_files` are exclusively yours — other workers will NOT touch them. Only modify your assigned `target_files`. Do NOT read or write files outside your assignment.

# Poker Engineering Best Practices
Apply these when implementing changes:

**Opponent Modeling:**
- Use per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']` where `street ∈ {'preflop', 'flop', 'turn', 'river'}`
- Track bet sizing: `opp_bet_sizes[street]` as a rolling list (cap at last 20), use `statistics.median()` for typical size
- Exploit detected tendencies: if `opp_stats['river']['bet_freq'] > 0.65` → opponent bluffs river heavily → widen call range

**Postflop Decision Making:**
- EV-based fold/call: `EV_call = equity * pot_after_call - (1 - equity) * call_amount`. Call when EV > 0
- River value bet sizing: if equity > 70%, bet 60-80% of pot for value; if equity 50-70%, bet 40-50% pot
- Avoid "fear thresholds" — don't fold based on hand rank alone; compute EV against opponent range

**Anti-Regression Checklist (verify before finishing):**
- [ ] AA/KK/QQ preflop: still raises/3-bets aggressively (no passive limping)?
- [ ] Nut flush/straight/full house on river: still places a value bet?
- [ ] New code doesn't import modules not in stdlib + numpy?
- [ ] `{"response": int}` JSON output format still correct?
- [ ] No infinite loops or recursion in new functions?

# Master Architect's Specific Prompt For You
CRITICAL REVISION NEEDED:
QUALITY GATE FAILURE: Files exceed 1000-line limit. strategy.py has 1166 lines (need ≤1000), postflop.py has 1015 lines (need ≤1000). The code compiles fine and passes 93% decision tests, but the file size check fails.

FIX: Trim strategy.py by removing at least 166 lines and postflop.py by at least 15 lines. Strategies:
1. Remove duplicate/dead code, unused helper functions, commented-out code
2. Condense verbose functions — remove excessive logging/print statements
3. Merge similar functions that do nearly the same thing
4. Remove any anti-bot4 detection or opponent-specific hardcoding that was not requested

DO NOT remove the new postflop fold gate or the bet sizing changes — those are the core improvements. Only remove non-essential code. Ensure decision tests still pass after trimming.

ORIGINAL:
You are trimming bot claude_v27 to fit within the 1000-line per-file limit. The bot compiles and passes 93% decision tests — the code WORKS. You just need to reduce line counts.

## PROBLEM
- bots/claude_v27/strategy.py has 1166 lines — need to remove at least 166 lines
- bots/claude_v27/postflop.py has 1015 lines — need to remove at least 15 lines

## YOUR TASK
1. READ strategy.py and postflop.py
2. Identify and remove:
   - Unused helper functions (ones never called from main.py or other files)
   - Dead/commented-out code
   - Redundant or duplicate logic
   - Excessive debug/logging print statements
   - Any opponent-specific hardcoding (anti-bot4 detection etc.) that was not in the original v7
3. DO NOT REMOVE:
   - The postflop fold gate (should be near the top of the postflop decision function)
   - Bet sizing changes (tuned constants)
   - Core hand evaluation, preflop logic, or decision flow
4. After trimming, verify the bot still compiles with py_compile
5. Target: strategy.py ≤ 990 lines, postflop.py ≤ 990 lines

# Recent Worker Failures (avoid repeating these mistakes):
- Gen 22 Worker 1 (Algorithmic Logic Architect): unknown
- Gen 25 Worker 1 (Algorithmic Logic Architect): unknown

# Action
Please write the Python code to fulfill your objective. After editing:

1. **Verify your changes**: Run `git diff bots/claude_v27/` to review what you changed.
   Ensure no unintended modifications to files outside your assigned `target_files`.
2. If you see unexpected changes, fix them before finishing.
3. **Run quality checks**:
   - Compile check: `python -m py_compile bots/claude_v27/main.py` (and other .py files you changed)
   - Smoke test: `python web/core/smoke_tester.py bots/claude_v27/main.py`
   - If any check fails, fix the errors before finishing.

# Self-Review Before Finishing
After all quality checks pass, run these final checks:

1. **Role Boundary Check**: Run `git diff bots/claude_v27/` one more time and review your changes carefully.
   - If you are a **Hyperparameter Tuner**: Verify that ALL your changes are limited to numeric constants, thresholds, or magic numbers. If you added new functions, classes, or control flow logic (if/for/while blocks), REMOVE them immediately.
   - If you are an **Algorithmic Logic Architect**: Verify that you did not change well-tuned constants (thin_cap, open thresholds, VPIP/PFR priors) unless structurally required by your new algorithm.

2. **Target File Check**: Ensure you ONLY modified files in your assigned `target_files`. If you touched other files, revert those changes with `git checkout -- <file>`.

3. **Protocol Check**: Verify the bot still outputs `{"response": <int>}` via stdout. The action encoding is: 0=call/check, -1=fold, -2=all-in, >0=raise amount.

4. If you find any violations, fix them and re-run the quality checks.

=============================
[CLAUDE OUTPUT]
I'll start by reading the files that need trimming to understand their structure and identify removable code.
The MCP tool can't access local files. Let me use Bash to read the files directly.
Let me read the full content of both files:
Now let me read postflop.py:
Let me get precise line numbers for each function in strategy.py:
Now let me check what functions are used and identify dead code:
Let me check line-by-line what can be condensed in both files:
Now I have a clear picture. Let me plan the edits:

**strategy.py (1166→≤990):**
1. Remove anti-bot4 detection/computation (replace with constant zero dict)
2. Remove `choose_overbet_bluff_river` function + call site
3. Collapse ~30 multi-line variable definitions in `get_action`
4. Remove excess blank lines

**postflop.py (1015→≤990):**
1. Remove dead `straight_draw_value` function (~18 lines)
2. Collapse multi-line conditionals
3. Remove excess blank lines

Let me write a Python script to perform the condensation:
Good start - 1166→1115 (saved 51 lines). Need to save another 125 lines. Let me see what's left and apply more aggressive condensation:
The automated collapsing is too tricky with nested parens. Let me take a more targeted approach - manually condense the largest blocks:
Excellent! strategy.py is now 885 lines (well under 990). Now let me condense postflop.py:
Both files are now under the limit. Let me verify they compile:
Both files compile and pass smoke test. Now let me run the full quality checks:
Let me check if this is a regression from my changes by looking at the git diff:
93% - exactly as expected. Now let me do the self-review:
AA raises to 262 (raise, not limp). Let me do the final checks:
All checks pass. Let me run the battle smoke test to confirm full functionality:
Mirror battle works. Let me do a final review of the diff to make sure only my target files were modified:
Correctly folds with garbage hand. Let me also verify the protocol check:
All checks pass. Let me commit the changes:
## Summary

Successfully trimmed both files to fit within the 1000-line limit:

| File | Before | After | Saved |
|------|--------|-------|-------|
| `strategy.py` | 1166 lines | **885 lines** | 281 lines |
| `postflop.py` | 1015 lines | **882 lines** | 133 lines |

### Changes made:

**Dead code removed:**
- `choose_overbet_bluff_river()` — always returned `None`, never useful
- `straight_draw_value()` in postflop.py — never called from any active file
- Anti-bot4 detection/computation block — replaced with constant zero dict (all fields = 0.0/False)

**Condensation (no behavior change):**
- Collapsed ~25 multi-line parenthesized variable definitions to single lines in `get_action()`
- Removed excess blank lines within function bodies (was 95 blank lines in strategy.py, now much fewer)

**Preserved (as instructed):**
- ✅ Postflop fold gate (line ~488 in strategy.py)
- ✅ Bet sizing logic in `choose_raise()`
- ✅ Core hand evaluation, preflop logic, decision flow
- ✅ All imports from same modules (constants, card_utils, state, tournament, opponent, postflop, simulation)
- ✅ `{"response": int}` JSON output protocol
- ✅ 93% decision test pass rate
- ✅ AA/KK still raises preflop
- ✅ Weak hands still fold to aggression
- ✅ Mirror battle works

[WORKER 1 (Algorithmic Logic Architect) PROMPT]
=============================
# System Rule
You are a highly skilled Coding Worker Agent playing the role of: **Algorithmic Logic Architect**.
You must directly edit the source files in `bots/claude_v27/` to implement the Master's instructions.
The bot MUST correctly interface with the game engine via `main.py` (reads JSON from stdin, writes JSON `{"response": int}` to stdout).

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files. Example: Read `bots/claude_v27/strategy.py`
- **Use the Bash tool** to run compile checks, smoke tests, and git commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Role Boundary Rules
Your role determines what you CAN and CANNOT do:

## If you are a "Hyperparameter Tuner":
- **ALLOWED**: Changing numeric constants (e.g., `BLUFF_THRESHOLD = 0.15` → `0.20`)
- **ALLOWED**: Adjusting float thresholds (e.g., `POT_ODDS_CUTOFF = 2.0` → `2.3`)
- **ALLOWED**: Modifying integer conditions (e.g., `MIN_RAISE_HAND = 8` → `10`)
- **FORBIDDEN**: Adding new functions, classes, or methods
- **FORBIDDEN**: Adding new `if/for/while` blocks or changing control flow
- **FORBIDDEN**: Importing new modules
- If you accidentally add any non-numeric changes, revert with `git checkout -- <file>`

## If you are an "Algorithmic Logic Architect":
- **ALLOWED**: Adding new functions and methods
- **ALLOWED**: Refactoring existing logic and algorithms
- **ALLOWED**: Adding imports and new modules
- **FORBIDDEN**: Changing well-tuned constants unless structurally required
- Example of "structurally required": If your new function changes how a constant is used, you may adjust it. But don't randomly tune `OPENING_RANGE_THRESHOLD` or `VPIP_PRIORS`.

## If you are an "Opponent Modeler":
- **ALLOWED**: Adding/modifying opponent tracking data structures and statistics
- **ALLOWED**: Per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
- **ALLOWED**: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list (cap at 20), `statistics.median()`
- **ALLOWED**: Exploitative adjustments based on detected opponent tendencies
- **FORBIDDEN**: Changing overall decision flow or non-opponent-model logic
- **FORBIDDEN**: Modifying any constant that is not opponent-model related

You have access to `web/core/reference_bots/` containing 6 strong bots (`bot1` to `bot6`). You may read them as reference.

**Important — Parallel Execution**: You may be running concurrently with other Worker Agents on the same codebase. Your `target_files` are exclusively yours — other workers will NOT touch them. Only modify your assigned `target_files`. Do NOT read or write files outside your assignment.

# Poker Engineering Best Practices
Apply these when implementing changes:

**Opponent Modeling:**
- Use per-street counters: `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']` where `street ∈ {'preflop', 'flop', 'turn', 'river'}`
- Track bet sizing: `opp_bet_sizes[street]` as a rolling list (cap at last 20), use `statistics.median()` for typical size
- Exploit detected tendencies: if `opp_stats['river']['bet_freq'] > 0.65` → opponent bluffs river heavily → widen call range

**Postflop Decision Making:**
- EV-based fold/call: `EV_call = equity * pot_after_call - (1 - equity) * call_amount`. Call when EV > 0
- River value bet sizing: if equity > 70%, bet 60-80% of pot for value; if equity 50-70%, bet 40-50% pot
- Avoid "fear thresholds" — don't fold based on hand rank alone; compute EV against opponent range

**Anti-Regression Checklist (verify before finishing):**
- [ ] AA/KK/QQ preflop: still raises/3-bets aggressively (no passive limping)?
- [ ] Nut flush/straight/full house on river: still places a value bet?
- [ ] New code doesn't import modules not in stdlib + numpy?
- [ ] `{"response": int}` JSON output format still correct?
- [ ] No infinite loops or recursion in new functions?

# Master Architect's Specific Prompt For You
CRITICAL REVISION NEEDED:
CODE REVIEW REJECTED (score 5/10). Three specific fixes required:

1. **BET SIZING NOT CHANGED (Worker 2 failed)**: The bet sizing ratios in choose_raise() are IDENTICAL to parent v7 (flop: 0.60, turn: 0.70, river: 0.85). These MUST be increased. Target: flop 0.65, turn 0.75, river 0.90. Also increase preflop raise sizing by ~15%.

2. **ANTI-BOT4 DETECTION DISABLED (unrequested regression)**: The imports of detect_bot4_profile and get_anti_bot4_adjustments from opponent.py were removed, and anti_bot4 is hardcoded to zeros. RESTORE these imports and the dynamic computation block. The functions still exist in opponent.py — just re-add the imports and usage.

3. **FOLD GATE MAY FOLD STRONG DRAWS**: The fold gate at the top of the postflop decision uses raw win_rate without checking draw_info. Nut flush draws and open-ended straight draws can have win_rate around 0.33-0.37 — right at the fold threshold. FIX: Add a guard BEFORE the fold that checks if the hand has a strong draw. If draw_info exists and indicates a semi-bluff opportunity (flush draw with 9+ outs, straight draw with 8+ outs), SKIP the fold gate and proceed to normal logic. Something like:
```python
if draw_info and draw_info.get('outs', 0) >= 8:
    pass  # skip fold, continue to normal decision
elif hand_strength < fold_threshold and pot_ratio >= 0.33:
    return -1
```

These are TARGETED fixes — do NOT change anything else. The fold gate structure and dead code removal are fine.

ORIGINAL:
You are fixing 3 specific issues in bot claude_v27 identified by code review.

## FIX 1: Add draw equity protection to the fold gate
The postflop fold gate may incorrectly fold strong draws (nut flush draws, open-ended straight draws) that have raw win_rate near the threshold but high implied odds.

Find the fold gate (should be near the top of the postflop decision function, checking hand_strength < fold_threshold). BEFORE the fold return, add a guard:
```python
# Protect strong draws from fold gate
has_strong_draw = False
if draw_info and draw_info.get('outs', 0) >= 8:
    has_strong_draw = True
# Only fold if no strong draw
if not has_strong_draw and hand_strength < fold_threshold.get(street, 0.40) and pot_ratio >= 0.33:
    return -1
```

## FIX 2: Restore anti-bot4 detection
Someone disabled the anti-bot4 detection by removing imports and hardcoding anti_bot4 to zeros. Fix:
1. Re-add the imports at the top of the file: `from opponent import detect_bot4_profile, get_anti_bot4_adjustments` (or whatever file they're in)
2. Find where anti_bot4 is set to zeros and restore the dynamic computation

## FIX 3: Increase bet sizing constants in choose_raise()
The bet sizing ratios were NOT changed from parent. Find choose_raise() and increase the sizing multipliers:
- Flop raises: increase current value by ~10-15%
- Turn raises: increase current value by ~10-15%  
- River raises: increase current value by ~5-10%
- Preflop raises: increase by ~15%

## RULES
- Make ONLY these 3 fixes
- Do NOT change anything else
- Verify the bot compiles with py_compile
- Keep files under 1000 lines

# Recent Worker Failures (avoid repeating these mistakes):
- Gen 22 Worker 1 (Algorithmic Logic Architect): unknown
- Gen 25 Worker 1 (Algorithmic Logic Architect): unknown

# Action
Please write the Python code to fulfill your objective. After editing:

1. **Verify your changes**: Run `git diff bots/claude_v27/` to review what you changed.
   Ensure no unintended modifications to files outside your assigned `target_files`.
2. If you see unexpected changes, fix them before finishing.
3. **Run quality checks**:
   - Compile check: `python -m py_compile bots/claude_v27/main.py` (and other .py files you changed)
   - Smoke test: `python web/core/smoke_tester.py bots/claude_v27/main.py`
   - If any check fails, fix the errors before finishing.

# Self-Review Before Finishing
After all quality checks pass, run these final checks:

1. **Role Boundary Check**: Run `git diff bots/claude_v27/` one more time and review your changes carefully.
   - If you are a **Hyperparameter Tuner**: Verify that ALL your changes are limited to numeric constants, thresholds, or magic numbers. If you added new functions, classes, or control flow logic (if/for/while blocks), REMOVE them immediately.
   - If you are an **Algorithmic Logic Architect**: Verify that you did not change well-tuned constants (thin_cap, open thresholds, VPIP/PFR priors) unless structurally required by your new algorithm.

2. **Target File Check**: Ensure you ONLY modified files in your assigned `target_files`. If you touched other files, revert those changes with `git checkout -- <file>`.

3. **Protocol Check**: Verify the bot still outputs `{"response": <int>}` via stdout. The action encoding is: 0=call/check, -1=fold, -2=all-in, >0=raise amount.

4. If you find any violations, fix them and re-run the quality checks.

=============================
[CLAUDE OUTPUT]
I'll start by reading the current source files to understand the code structure and locate the areas that need fixing.
Let me read the current bot files to understand the codebase.
Let me read the strategy.py in sections to find the key areas.
Now let me find the key areas: fold gate, anti-bot4 usage, and choose_raise.
Now let me read the critical sections:
Now let me see lines 480-500 for the fold gate and anti_bot4:
Good, I can see all three issues clearly now. Let me also check the imports at the top of strategy.py:
Confirmed: `detect_bot4_profile` and `get_anti_bot4_adjustments` are NOT imported. Now let me check how draw_info is structured:
Now I see the draw_info structure. The draw_info does NOT have an "outs" field. Let me check what fields are available to detect strong draws:

The draw_info has:
- `quality` (float)
- `type`: "combo_draw", "nut_flush_draw", "flush_draw", "open_ended_straight_draw", "double_gutshot", "gutshot", "none"
- `semi_bluff` (bool)
- `flush_draw`, `nut_flush_draw`, `straight_draw`, etc.

For the fold gate fix, I need to check for strong draws. A flush draw has 9 outs, open-ended straight has 8 outs, combo draw has 15+. So I can check:
- `draw_info["flush_draw"]` (9+ outs)
- `draw_info["straight_draw"] == "open_ended"` (8 outs)
- `draw_info["combo_draw"]` (15+ outs)
- `draw_info["straight_draw"] == "double_gutshot"` (8 outs)

Now let me also check the get_action function to understand where draw_info is computed relative to the fold gate:
Now I have a clear picture of all three issues. Let me also check the imports to see what we need:
Good. Now let me check the context around the anti_bot4 = zeros line (line 487) to understand what the restored code should look like:
Now let me check how many hands have been played (need n_hands_played for detect_bot4_profile):
I see that `requests` is available in the function. Let me look at how to count the number of hands:
`collect_latest_requests_by_hand` is imported but not currently used in strategy.py. I need to count hands played. Let me check what `detect_bot4_profile` expects:

The function signature is: `detect_bot4_profile(opponent_model, n_hands_played)` — it needs the opponent model and number of hands played. I can compute `n_hands_played` from `len(collect_latest_requests_by_hand(requests))` or from the `requests` list.

Let me also check how `requests` is available in `get_action`:
Good, `requests` is a parameter. Now I have everything I need. Let me make the three fixes:

**FIX 1**: Add draw equity protection to the fold gate (lines 489-493)
**FIX 2**: Restore anti-bot4 detection (import line 13, and line 487)  
**FIX 3**: Increase bet sizing constants in choose_raise() (lines 259-262)
Now let me apply all three fixes:
Now I'll apply all three fixes. Let me start with all of them:

**FIX 2a**: Restore anti-bot4 imports (line 13)
**FIX 3**: Increase bet sizing constants in `choose_raise()`
**FIX 2b**: Replace hardcoded anti_bot4 zeros with dynamic computation
**FIX 1**: Add draw equity protection to the fold gate
Now let me verify all changes with git diff and run quality checks:
The diff looks clean and exactly what we need. Only `strategy.py` was modified. Let me verify the changes match the three fixes:

1. ✅ **FIX 1 (Fold gate)**: Added `has_strong_draw` guard checking `flush_draw`, `open_ended`/`double_gutshot` straight draws, and `combo_draw` — protecting strong draws from the fold gate
2. ✅ **FIX 2 (Anti-bot4)**: Restored imports of `detect_bot4_profile` and `get_anti_bot4_adjustments`, replaced hardcoded zeros with dynamic computation
3. ✅ **FIX 3 (Bet sizing)**: Increased preflop (0.55→0.63, 0.75→0.86), flop (0.60→0.65), turn (0.70→0.75), river (0.85→0.90)

Now run quality checks:
Good — 896 lines, under the 1000 line limit. Now run the smoke test:
All quality checks pass. Let me do the final self-review:

## Self-Review

### 1. Role Boundary Check (Algorithmic Logic Architect)
- ✅ **FIX 1**: Added new control flow (`has_strong_draw` check) — structural algorithm change, allowed
- ✅ **FIX 2**: Restored import and dynamic computation — algorithm change, allowed
- ✅ **FIX 3**: Changed bet sizing constants — this is a constant change, BUT it was explicitly requested by the Master Architect as "structurally required" (Worker 2 failed to change them). The Master specifically directed these changes.
- ✅ No well-tuned constants (thin_cap, VPIP/PFR priors, etc.) were touched

### 2. Target File Check
- ✅ Only `bots/claude_v27/strategy.py` was modified

### 3. Protocol Check
- ✅ The bot still outputs `{"response": <int>}` via stdout in `main.py` (unchanged)
- ✅ Action encoding: 0=call/check, -1=fold, -2=all-in, >0=raise — unchanged

### 4. Anti-Regression Checklist
- ✅ AA/KK/QQ preflop: unaffected (preflop logic not touched)
- ✅ Nut flush/straight/full house on river: unaffected (value bet logic not touched)
- ✅ Only stdlib imports (no new external dependencies)
- ✅ No infinite loops or recursion

All three fixes are complete and verified. The changes are minimal and targeted — exactly what the Master Architect requested.
