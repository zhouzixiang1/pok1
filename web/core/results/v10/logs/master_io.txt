
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
{"top_rating": 1646.2, "current_rating": 1619.3, "gap": -26.9, "rank": 7, "active_bots": 17, "top_bots": ["v13: 1646.2", "v7: 1645.7", "v2: 1643.8", "v5: 1639.9", "v14: 1638.8", "v16: 1637.8"], "match_data_available": false, "note": "No match history data available. Focus on structural improvements and closing the 27-point gap to the leader."}

# Recent Match Analysis
{"weaknesses": ["No match data available for specific loss analysis"], "recommendations": ["Review v9 strategy code for obvious weaknesses", "Compare approach with top-rated bots (v13, v7, v2)", "Focus on areas where v9 may be losing EV: river play, bluff frequency, value bet sizing", "Consider improving opponent modeling accuracy", "Review postflop decision quality"]}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v9 → v10
Bot directory: bots/claude_v9/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me start by reading all the essential data files and understanding the current state.
Now let me read the v9 source code and compare with the top-rated bots (v13, v7, v2):
Now let me look at the top-rated bots and the remaining v9 files:
Let me now read the critical betting files and diff the bots:
Now let me look at the remaining key files and the diff between the state/simulation modules:
Now I have a comprehensive understanding. Let me update the experience pool and then write the final output.
Now I have all the data I need. Let me update the experience pool with the new findings, then create the JSON output.
Now let me write the final JSON output:

```json
{
  "analysis": "## Analysis: v9 → v10 Evolution (Branching from v13)\n\n### Current State\n- v9 rating: 1619.3 (rank 7/17)\n- v13 rating: 1646.2 (rank 1, leader)\n- Gap: 27 points to close\n- Rating trend: v9 has been slowly climbing (1500→1619 over 20 periods), but v13 has been declining (1860→1646). This suggests v13's initial rating was inflated but its strategy is still strongest.\n\n### Why v13 Leads (Structural Advantages)\nAfter deep comparison of v13 vs v9 source code, v13 has 5 structural advantages that v9 lacks:\n1. **Complete preflop logic**: v13 handles bb_vs_raise (with 3bet bluffing based on fold_to_raise) and sb_vs_reraise (with 4bet logic). v9's choose_preflop_spot_action only has sb_open and bb_vs_limp — it returns None for the other spots and falls through to generic postflop-style logic for 3bet/4bet situations. This costs EV every time v9 faces a preflop raise.\n2. **Gift balance tracking + exploit lambda**: v13's track_opponent_gift() and safe_exploitation_lambda() modulate between GTO and exploit modes. When the opponent has been losing big pots, v13 exploits harder. This adds ~5-10pts of rating.\n3. **Concept drift detection**: v13 detects when opponent behavior shifts (abs(recent_vpip - all_time_vpip) > 0.15) and uses recent-10 data. This prevents stale opponent models.\n4. **CBet rate adjustments**: v13 tracks cbet_rate and adjusts call margins (cbet>0.65 → call_margin-=0.02, cbet<0.40 → call_margin+=0.02). Exploits cbet frequency patterns.\n5. **OOP draw EQR discount**: v13 has a dedicated path for OOP draws with no made hand (draw_str>=0.08, made<0.18, OOP), capping EQR at 0.85(flop)/0.75(turn+). v9 lacks this.\n6. **Wetness-aware thin_cap**: v13's thin_cap = 0.46+0.08*wetness+0.05*round vs v9's flat 0.30/0.38. More flexible sizing on wet boards.\n\n### What v9 Has That v13 Doesn't (Proven Features to Port)\n1. **River exact equity override** (experience lesson 22): On river with 5 cards, use 0 simulations for exact equity. Force raise if wr>0.85, fold if wr<0.15 minus pot odds margin. Pure +EV.\n2. **big_pot_safety_guard** (lesson 21): Prevents thin/marginal barreling when pot>7000 on turn/river with no draw. Prevents catastrophic losses.\n3. **must_continue_vs_raise draw extension** (lesson 20): Protects strong combo draws (draw_strength>=0.20, pot_odds<=0.38) from folding to aggression.\n4. **choose_overbet_river** (lesson 3): 1.5-2.2x pot overbet for nut hands on dry rivers. Proven value extraction.\n5. **min_raise_action .get() fallback** (lesson 23): Safety fix using state.get('min_raise_action', state['round_raise']).\n\n### What NOT to Port from v9\n- Anti-bot4 detection: Unclear value vs diverse field, adds complexity\n- Style classification (classify_opponent_style): Uncertain value, v13 leads without it\n- choose_overbet_bluff_river: Rarely triggered, risky\n- Lower EQR values (lesson 26): v13's higher values work better in its context\n\n### Revised Experience Pool\nSeveral old lessons were WRONG:\n- Lesson 8 (cbet_rate, drift detection, gift_balance are 'dead weight'): v13 proves they're valuable\n- Lesson 10 (bb_vs_raise: let simulation decide): v13 proves explicit preflop logic matters\n- Lesson 26 (lower EQR always better): Context-dependent, v13's higher EQR works\n\n### Decision: Branch from v13\nPer experience lesson 24: 'Starting from #1 bot + targeted tweaks > complex rewrites.' Branch from v13's codebase, port v9's 5 proven features as additive-only changes.",
  "branch_from": "claude_v13",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect working on a Texas Hold'em poker bot. Your base is claude_v13 (the #1 rated bot at 1646 Glicko). You are creating v10 by ADDING proven features from v9. CRITICAL: Do NOT modify any existing v13 logic. Only ADD new code paths.\n\n## Context\nv13's strategy.py is a single ~1246-line file containing all decision logic. You are modifying this file. The function get_action() at the bottom is the main entry point.\n\n## Task 1: Add min_raise_action safety fix (EASIEST)\nThroughout strategy.py, replace ALL occurrences of `state[\"min_raise_action\"]` with `state.get(\"min_raise_action\", state[\"round_raise\"])`. This is a safety fallback in case the state dict is missing min_raise_action. There are approximately 5-6 occurrences in the choose_raise call sites within get_action() and choose_preflop_spot_action().\n\n## Task 2: Add river exact equity override block\nIn get_action(), AFTER the river overbet block (after line ~722 where river overbet for nuts is checked, or if no river overbet block exists, after all profiles are computed around line 690), ADD this new decision block BEFORE the main decision logic:\n\n```python\n# River exact equity override (from v9 proven feature)\nif round_idx == 3 and len(public_cards) == 5:\n    exact_wr = estimate_weighted_win_rate(my_cards, public_cards, combos, weights, 0)\n    if exact_wr > 0.85 and to_call > 0:\n        raise_amount = choose_raise(\n            state.get(\"min_raise_action\", state[\"round_raise\"]),\n            my_chips, state[\"my_round_bet\"],\n            to_call, pot, exact_wr, round_idx,\n            spot_info[\"preflop_spot\"], preflop_strength,\n            spot_info[\"has_position\"], opponent_model,\n            value_profile=value_profile, value_plan=value_plan,\n            board_texture=board_texture,\n            nutted_risk_score=nutted_risk[\"risk\"],\n            match_sizing_delta=match_profile[\"sizing_delta\"],\n        )\n        if raise_amount is not None and raise_amount > to_call:\n            return raise_amount\n        return 0\n    if exact_wr < 0.15 and to_call > 0:\n        min_call_odds = to_call / (pot + to_call) if to_call > 0 else 1.0\n        if exact_wr < min_call_odds - 0.10:\n            return -1\n```\n\nPlace this block AFTER all profiles (value_profile, board_texture, etc.) are computed, and BEFORE the main strong/medium threshold logic. This is an early-exit path for river decisions.\n\n## Task 3: Extend must_continue_vs_raise with draw_strength\nThe current v13 function is:\n```python\ndef must_continue_vs_raise(value_profile, made_strength, pot_odds, nutted_risk, board_texture):\n```\n\nAdd a `draw_strength=0.0` parameter. Before the final `return False`, add:\n```python\n    # Protect strong combo draws facing aggression\n    if draw_strength >= 0.20 and pot_odds <= 0.38:\n        if not extreme_texture:\n            return True\n    return False\n```\n\nThen update ALL call sites of must_continue_vs_raise in get_action() to pass `draw_strength=draw_strength` as a keyword argument.\n\n## Task 4: Add big_pot_safety_guard function and call site\nAdd this function before get_action():\n```python\ndef big_pot_safety_guard(pot, my_chips, value_profile, made_strength, round_idx, to_call, draw_strength):\n    if round_idx < 2:\n        return False\n    if to_call > 0:\n        return False\n    if value_profile is None:\n        return False\n    tier = value_profile.get(\"tier\", \"none\")\n    if tier in (\"nut\", \"strong\"):\n        return False\n    if made_strength >= 0.65:\n        return False\n    if pot < 7000:\n        return False\n    if tier == \"thin\" and draw_strength < 0.15:\n        return True\n    if 0.30 <= made_strength <= 0.50 and pot >= 10000 and draw_strength < 0.15:\n        return True\n    return False\n```\n\nIn get_action(), in the `to_call == 0` section (the second half), AFTER the thin_static_showdown_control check and BEFORE the river_bluff_threshold calculations, add:\n```python\n    if big_pot_safety_guard(pot, my_chips, value_profile, made_strength, round_idx, 0, draw_strength):\n        return 0\n```\n\n## Task 5: Add choose_overbet_river function and call site\nAdd this function before get_action():\n```python\ndef choose_overbet_river(min_raise, my_chips, my_round_bet, to_call, pot, win_rate, value_profile, board_texture, spot_info, opponent_model):\n    if value_profile is None or value_profile[\"tier\"] != \"nut\":\n        return None\n    if board_texture is not None and board_texture[\"wetness\"] > 0.35:\n        return None\n    if pot < 400:\n        return None\n    pot_after_call = pot + to_call\n    ratio = 1.5 + 0.3 * max(0.0, win_rate - 0.70)\n    if not spot_info.get(\"has_position\", False):\n        ratio = max(1.3, ratio - 0.2)\n    ratio = min(ratio, 2.2)\n    amount = int(to_call + pot_after_call * ratio)\n    if amount >= my_chips:\n        return -2\n    amount = min(amount, my_chips - 1)\n    if amount <= to_call or amount < min_raise:\n        return None\n    return amount\n```\n\nIn get_action(), BEFORE the river exact equity block (Task 2), add the call site:\n```python\n    # River overbet with nut hands on dry boards\n    if round_idx == 3 and to_call == 0 and len(public_cards) >= 3 and value_profile is not None and value_profile[\"tier\"] == \"nut\":\n        overbet = choose_overbet_river(\n            state.get(\"min_raise_action\", state[\"round_raise\"]),\n            my_chips, state[\"my_round_bet\"],\n            to_call, pot, win_rate, value_profile, board_texture, spot_info, opponent_model\n        )\n        if overbet is not None:\n            return overbet\n```\n\n## CRITICAL RULES\n1. Do NOT modify any existing v13 logic, thresholds, or parameters\n2. Only ADD new code (functions and call sites)\n3. Preserve all existing imports\n4. Use state.get('min_raise_action', state['round_raise']) for ALL raise action references\n5. Test that the file is syntactically valid Python"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py", "strategy.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner working on a Texas Hold'em poker bot. Your base is claude_v13 (the #1 rated bot at 1646 Glicko). You are ONLY modifying numeric constants and thresholds. FORBIDDEN from adding new functions, classes, or changing control flow.\n\n## Context\nYou are tuning parameters in two files: constants.py and the numeric thresholds in strategy.py. The bot uses v13's proven calibration as a base.\n\n## Task 1: Add river threshold constants to constants.py\nAdd these three constants at the end of constants.py (after the PREFLOP_STRENGTH_TABLE):\n\n```python\n# River refinement thresholds (proven from v9)\nRIVER_EXACT_RAISE_THRESHOLD = 0.85\nRIVER_EXACT_FOLD_THRESHOLD = 0.15\nRIVER_EXACT_FOLD_POT_ODDS_MARGIN = 0.10\n```\n\n## Task 2: Lower thin_static_showdown_control round threshold\nIn strategy.py, find the thin_static_showdown_control variable definition. It currently reads:\n```python\n    thin_static_showdown_control = (\n        round_idx >= 2\n        and value_profile is not None\n```\n\nChange `round_idx >= 2` to `round_idx >= 1`. This activates showdown control one round earlier, preventing thin value bets on the turn that lose to raises.\n\n## Task 3: Lower jam_buffer thin tier bonus\nIn strategy.py, in the opponent_allin section (search for `jam_buffer`), find the line:\n```python\n        if value_profile is not None and value_profile[\"tier\"] == \"thin\":\n            jam_buffer += 0.04\n```\n\nChange `0.04` to `0.02`. This reduces the tendency to call all-ins with thin-value hands. Experience shows thin-jam losses were catastrophic.\n\nAlso find the corresponding line in the to_call >= my_chips section:\n```python\n        if value_profile is not None and value_profile[\"tier\"] == \"thin\":\n            shove_buffer += 0.04\n```\n\nChange this `0.04` to `0.02` as well.\n\n## Task 4: Lower jam_buffer cap\nIn the opponent_allin section, find:\n```python\n        jam_buffer = clamp(jam_buffer, -0.05 if anti_lock_pressure else 0.0, 0.14)\n```\n\nChange `0.14` to `0.11`. This limits the maximum safety buffer for calling all-ins, preventing calls with win rates as low as 0.45+0.14=0.59 where the bot is behind.\n\nSimilarly in the to_call >= my_chips section, find:\n```python\n        shove_buffer = clamp(shove_buffer, -0.05 if anti_lock_pressure else 0.0, 0.14)\n```\n\nChange `0.14` to `0.11` as well.\n\n## CRITICAL RULES\n1. Do NOT add any new functions, classes, or decision paths\n2. Do NOT change control flow (if/elif/else structure)\n3. ONLY modify numeric constants and thresholds\n4. Preserve all imports\n5. If you see a constant that's already correct, leave it alone\n6. Document each change with a brief comment"
    }
  ]
}
```
