
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `web/core/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `web/core/results/glicko_ratings.json` — All bot Elo ratings (r, rd) for overall ranking
- `web/core/results/head_to_head.json` — **Head-to-Head matrix**: per-opponent W/L data. Shows who beats whom.
- `web/core/results/bot_stats.json` — Per-bot stats: wins, losses, games, win_rate
- `web/core/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `web/core/experience_pool.md` — Accumulated strategic lessons from past generations (**THIS is the active pool, not evolution_workspace/experience_pool.md**)
- `bots/claude_v<source_v>/` — Current source bot code (actual version number given in the context appended below)
- `web/core/reference_bots/bot1/` … `web/core/reference_bots/bot6/` — 6 strong reference bots. Read whichever is relevant.

**H2H Matrix Usage:** The head_to_head.json file is the most important data for identifying weaknesses. For each bot, check which opponents it loses to (< 40% WR = WEAKNESS) and which it beats (> 60% WR = STRENGTH). Focus improvements on closing weakness gaps.

Use Bash tool with `git log` and `git diff` to understand evolution history.

**When reading `experience_pool.md`, prioritise in this order:**
1. `## RECENT_LESSONS` — what just happened last 1-3 gens
2. `## OPPONENT_MODELING` — opponent model improvements (often highest EV)
3. Any entry with `[POSSIBLY EXHAUSTED]` — **avoid repeating these directions**

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the performance verification report (provided below) for objective trend analysis.
3. Read the experience pool to learn from past iterations.
4. Read the current bot's source code and reference bots' code to identify weaknesses.
5. Dynamically assign 1–3 Developer Sub-Agents (Workers) to implement your strategy.
6. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

Prefer one dominant hypothesis per generation. If changing multiple subsystems,
explain why they are inseparable. Otherwise split into separate generations.

Every plan must make attribution possible:
- `targeted_failure`: the single failure pattern this generation targets, with H2H/replay/evidence if available.
- `expected_behavior_change`: what concrete decisions should change at the table.
- `do_not_touch`: files, functions, streets, or subsystems workers must avoid.
- `measurement_plan`: how quality gates, critical scenarios, H2H, or precommit eval should prove the change is not a regression.

# Worker Count Decision
Choose 1–3 workers based on the current bot's total games played:
- **games < 50** (very uncertain): **1 worker only** — conservative change, avoid big structural risk
- **games 50–200** (moderate confidence): **2 workers** — standard plan (Direction A + Direction B)
- **games ≥ 200** (well-evaluated): **up to 3 workers** — can explore bolder, parallel improvements

# Worker Directions (assign to each worker)
- **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation, opponent modeling.
- **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
- **Direction C (Opponent Modeler):** *(Only use as 3rd worker when games ≥ 200)*
  - ALLOWED: Adding/modifying opponent tracking data structures
  - ALLOWED: Per-street statistics (preflop/flop/turn/river aggression separately): `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
  - ALLOWED: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list, use median
  - FORBIDDEN: Changing overall decision flow or non-opponent-model logic

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Performance Verification Report (SATLUTION-style objective analysis)
{"trend": "stagnant", "diversity_needed": true, "persistent_weaknesses": ["Rating locked at 1625 for 10 periods", "Severe exploit vs claude_v27 (33% win rate)", "Losing to 5 opponents (v27, v10, v5, v26, v17) at sub-44%", "Barely above break-even (51%) despite top rating"], "suggestion": "Study v27's gameplay to identify counter-strategy. Restructure opponent-modeling for dynamic aggression thresholds per-opponent."}
Use `verified_improvements` to avoid duplicating what already worked.
Use `persistent_weaknesses` to prioritise what to fix next.
**If `diversity_needed: true`, you MUST try a substantially different approach this generation.**

# Diversity Injection (Anti-Local-Optima)
If `performance_verification.diversity_needed: true` OR the same type of change has failed 2+ consecutive generations:
- You MUST try a **substantially different approach** this generation
- Examples of "substantially different":
  - Last 2 gens tuned constants → this gen add new opponent model function (Direction A)
  - Last 2 gens added preflop logic → this gen focus on river/showdown decisions
  - Last 2 gens had Logic Architect fail → this gen try only HyperTuner (conservative, 1 worker)
- Explicitly state in your `analysis` field: `"Diversity injection: trying X instead of Y"`
- This prevents getting trapped in a local optimum where the same direction never improves

# Stagnation Decision
v14 (#1 bot) has been stagnant for 10 periods with 0% postflop fold rate — same calling station issue identified in previous analysis. v30 (our latest, branched from v8) already includes river exact equity override and opponent style classification, which should help. For v31, build on v30 by adding postflop folding discipline on the flop and turn (not just river). The river exact equity only helps on the final street; the bot also needs to fold weak hands on the flop and turn. Focus on: (1) Flop fold logic when facing raises with weak holdings, (2) Turn fold logic for weak pairs facing aggression, (3) Study v27's counter-strategy to address the 33% weakness.

# Recent Match Analysis
{"weaknesses": ["0% fold rate on every postflop street across all matches — the bot NEVER folds once it sees the flop, leading to catastrophic losses in big pots", "Extremely high preflop fold rate (59-68%) combined with low raise rate (13-16%) — bleeding chips through blinds", "River raise rate collapses to 8-19% while calling 80-91% — calling station on the river"], "recommendation": "Implement a postflop fold mechanism — even a conservative 15-20% fold rate on the flop/turn/river would dramatically reduce the ~20,000 chip single-game losses. Fold the flop when facing a raise with less than pair+gutshot, fold the turn with just a weak pair, fold the river facing a large bet with less than two pair."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows.
`tasks` array may contain **1, 2, or 3** items based on your Worker Count Decision above.

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What weakness are you targeting? Are we failing due to bad logic or bad parameters? If diversity injection applies, explain: 'Diversity injection: trying X instead of Y'.",
  "targeted_failure": "One dominant failure pattern this generation targets, with the strongest evidence source.",
  "expected_behavior_change": "Specific table behavior that should change, e.g. call AKs all-in preflop more often but do not widen weak offsuit calls.",
  "do_not_touch": ["List files/functions/subsystems that must remain unchanged for attribution."],
  "measurement_plan": "How to verify this hypothesis: critical scenarios, H2H weak opponent, parent comparison, expected small-sample signal.",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

Notes:
- `branch_from` is OPTIONAL. Only include it to override the default evolution source.
- For 1-worker plans, use only Direction A or only Direction B.
- For 3-worker plans, add a Direction C (Opponent Modeler) task as worker_id 3.
- Avoid v29-like broad mixes such as "preflop rewrite + river rewrite + all-street sizing" unless you explicitly prove those changes are inseparable.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.
8. **ATTRIBUTION**: Do not mix unrelated preflop, postflop, and sizing rewrites in one generation. The next evaluation must be able to attribute win/loss movement to this plan.

Current evolution: v30 → v31
Bot directory: bots/claude_v30/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl
Head-to-Head data: web/core/results/head_to_head.json
Bot stats: web/core/results/bot_stats.json
Experience pool: web/core/experience_pool.md  ← READ THIS, not evolution_workspace/experience_pool.md

=============================
[CLAUDE OUTPUT]
Now let me read the current v30 source code and relevant reference bots.
Now let me look at a reference bot to understand alternative approaches, particularly v27 (which exploits the calling station issue):
Now I have a comprehensive understanding of the v30 codebase and the strategic situation. Let me verify the game count for v30:
Now I have all the data needed to formulate the plan. Let me compile my analysis.

```json
{
  "analysis": "v30 (branched from v8, rating 1618) has 0 games and includes river exact equity override + opponent style classification. The critical weakness: the bot NEVER folds postflop — 0% fold rate on every street. Root cause: `postflop_call_margin()` in strategy.py accumulates only POSITIVE margins (+0.012 to +0.050 for weak hands, +0.012 to +0.065 from pair_domination_margin, plus line_strength/check_resistance/nutted_risk contributions). The fold condition `realized_rate < pot_odds + call_margin` almost never triggers because call_margin inflates the threshold to 0.10-0.20 above pot_odds. There is NO counter-balancing negative contribution for weak holdings. Diversity injection: instead of tuning thresholds (which has been tried), we're adding an entirely new fold discipline function that subtracts from call_margin for weak hands facing aggression on flop and turn. This is structural, not parametric.",
  "targeted_failure": "0% postflop fold rate on flop and turn — the bot calls every bet with any pair or draw once it sees the flop, bleeding ~20,000 chips in single-game losses from big-pot calls with weak pairs. Evidence: match analysis shows 0% fold rate across all postflop streets, with extremely high preflop fold (59-68%) meaning the bot only sees flops with strong ranges but then refuses to release weak pairs to aggression.",
  "expected_behavior_change": "The bot should fold the flop when facing a medium/large raise with air (no pair, no draw) — approximately 15-20% fold rate on the flop. The bot should fold the turn with weak pairs (bottom/middle/underpair/board_pair) facing medium/large bets when lacking draws — approximately 20-25% fold rate on the turn. River folding is already handled by the exact equity override added in v30.",
  "do_not_touch": ["constants.py", "simulation.py", "tournament.py", "main.py", "card_utils.py", "state.py", "betting.py", "the river exact equity override block (lines 410-433 in strategy.py)", "classify_opponent_style in opponent.py", "style_deltas propagation (lines 504-505 in strategy.py)"],
  "measurement_plan": "1) Decision test: the bot should fold K7o on A83r flop facing a pot-sized raise (currently calls). 2) Decision test: the bot should fold 87s on K85r turn facing a 2/3 pot bet with no draw (currently calls). 3) Mirror battle vs parent v8: expect win rate to improve by reducing big-pot losses. 4) H2H vs v27: the fold discipline should reduce the chip-bleeding that causes the 33% weakness. 5) Overall: fold rate should increase from 0% to 15-25% on flop/turn while maintaining >50% win rate.",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["postflop.py", "strategy.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to add postflop fold discipline to this poker bot. The bot currently has a 0% postflop fold rate — it NEVER folds once it sees the flop, leading to catastrophic chip losses in big pots.\n\n## CONTEXT\nThe bot is v30 (branched from v8). It already has river exact equity override (exact enumeration when 5 public cards visible) and opponent style classification. What it LACKS is discipline on flop and turn: it calls every bet with weak hands.\n\n## ROOT CAUSE ANALYSIS\nIn strategy.py's calling decision path (around line 566-685), the variable `call_margin` accumulates only POSITIVE contributions from:\n- `postflop_call_margin()`: +0.012 to +0.050 for weak hands (makes calling EASIER)\n- `pair_domination_margin()`: +0.012 to +0.065 for weak pairs (makes calling EASIER)\n- `draw_call_margin()`: variable but mostly positive\n- `line_strength`, `check_resistance`: positive\n- `nutted_risk`: only slightly positive\n\nThe fold condition at line 683 is: `if realized_rate < pot_odds + call_margin:` — but call_margin is so inflated that weak hands almost always call.\n\nThere is NO counter-balancing NEGATIVE contribution for weak holdings. This is what you must fix.\n\n## YOUR TASK\nAdd a `postflop_fold_incentive()` function to `postflop.py` and integrate it into `strategy.py`.\n\n### Step 1: Add to postflop.py\nCreate a function `postflop_fold_incentive(made_strength, draw_strength, pair_profile, round_idx, spot_info, board_texture)` that returns a positive value representing fold pressure. This value will be SUBTRACTED from call_margin, making folding more likely.\n\nThe function should return positive fold incentive in these scenarios:\n\n**A. Air (no pair, no draw) facing aggression:**\n- Flop (round_idx=1): +0.020 for small bets, +0.035 for medium bets, +0.050 for large bets\n- Turn (round_idx=2): +0.035 for small bets, +0.050 for medium/large bets\n- River (round_idx=3): +0.050 for any bet\n- Condition: `made_strength < 0.18 AND draw_strength < 0.08`\n- Use `bet_size_bucket(spot_info['last_raise_pot_ratio'])` for size classification\n\n**B. Weak pair (middle/bottom/underpair/board_pair) on turn/river facing aggression:**\n- Turn (round_idx=2): +0.025 for medium/large bets, +0.020 if opponent has 2+ postflop bets\n- River (round_idx=3): +0.035 for medium/large bets, +0.015 for small bets\n- Condition: `pair_profile['made_class'] == 1 AND pair_profile['pair_type'] in ('middle_pair', 'bottom_pair', 'underpair', 'board_pair') AND draw_strength < 0.10`\n- Exception: Do NOT add incentive if `pair_profile['pair_rank'] >= 11` (Jack or higher pair is not weak enough)\n\n**C. Multi-barrel aggression discount:**\n- If `opp_postflop_bet_count >= 2` AND `made_strength < 0.35` AND `draw_strength < 0.14`: +0.020\n- This captures the scenario where opponent bets flop AND turn — a strong line that weak hands should respect\n\n**D. Dry board confirmation:**\n- If `board_texture` is not dynamic AND `made_strength < 0.30`: +0.010\n- On dry boards, opponent's aggression is more credible\n\nReturn `clamp(incentive, 0.0, 0.08)`.\n\nOnly apply when `spot_info['facing_postflop_aggression'] == True`. Return 0.0 if `round_idx <= 0` or not facing aggression.\n\n### Step 2: Integrate into strategy.py\n1. Import `postflop_fold_incentive` from postflop in the imports section (around line 14-23)\n2. After computing all the call_margin contributions (after line ~608 in the `if to_call > 0:` block), add:\n```python\nfold_incentive = postflop_fold_incentive(\n    made_strength, draw_strength, pair_profile, round_idx, spot_info, board_texture\n)\ncall_margin -= fold_incentive\n```\nPlace this AFTER the existing call_margin accumulation and BEFORE the `if anti_lock_pressure:` check that adjusts call_margin.\n\n### CRITICAL CONSTRAINTS\n1. Do NOT modify any file except postflop.py and strategy.py\n2. Do NOT change the river exact equity block (lines 410-433 in strategy.py)\n3. Do NOT change constants.py, simulation.py, tournament.py, main.py, card_utils.py, state.py, betting.py\n4. Do NOT change the `classify_opponent_style` function or `style_deltas` propagation\n5. Keep each file under 1000 lines\n6. The function must ONLY activate when facing postflop aggression (to_call > 0 and opponent bet/raised)\n7. When the bot has a strong hand (made_strength >= 0.50, nut/strong value tier, or strong draw), the incentive should be 0 — we only want to fold WEAK hands\n8. The anti_lock logic (which prevents folding when behind in tournament) must still work — fold_incentive should NOT apply during anti_lock situations\n\n### WHAT SUCCESS LOOKS LIKE\n- Bot folds air (no pair, no draw) on the flop to medium/large bets ~70-80% of the time\n- Bot folds weak pairs (bottom/middle/underpair) on the turn to medium/large bets ~40-50% of the time\n- Bot still calls with top pair+, overpairs, strong draws, combo draws\n- Big pot losses should decrease by 15-25%\n- Overall win rate should remain >= 50%"
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `web/core/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `web/core/results/head_to_head.json` — **PRIMARY DATA**: Head-to-Head matrix. Use this to compute H2H average win rate per bot (equal-weighted across all opponents). This is the **primary optimization metric**.
- `web/core/results/glicko_ratings.json` — Glicko-2 ratings (r, rd) as secondary reference for statistical context
- `web/core/results/bot_stats.json` — Per-bot stats: wins, losses, games, win_rate (games-weighted, biased by opponent frequency — use H2H average for equal weighting)
- `web/core/results/rating_history.jsonl` — Performance snapshots over time (includes both ratings and win_rates for recent periods)
- `web/core/experience_pool.md` — Accumulated strategic lessons from past generations (**THIS is the active pool, not evolution_workspace/experience_pool.md**)
- `bots/claude_v<source_v>/` — Current source bot code (actual version number given in the context appended below)
- `web/core/reference_bots/bot1/` … `web/core/reference_bots/bot6/` — 6 strong reference bots. Read whichever is relevant.

**H2H Matrix Usage:** The head_to_head.json file is the most important data for identifying weaknesses. For each bot, check which opponents it loses to (< 40% WR = WEAKNESS) and which it beats (> 60% WR = STRENGTH). Focus improvements on closing weakness gaps.

Use Bash tool with `git log` and `git diff` to understand evolution history.

**When reading `experience_pool.md`, prioritise in this order:**
1. `## RECENT_LESSONS` — what just happened last 1-3 gens
2. `## OPPONENT_MODELING` — opponent model improvements (often highest EV)
3. Any entry with `[POSSIBLY EXHAUSTED]` — **avoid repeating these directions**

# Task
Your goal is to:
1. Read the H2H data and analyze the current bot's per-opponent performance. Compute and track H2H average win rate — this is the **primary metric**.
2. Read the performance verification report (provided below) for objective trend analysis.
3. Read the experience pool to learn from past iterations.
4. Read the current bot's source code and reference bots' code to identify weaknesses.
5. Dynamically assign 1–3 Developer Sub-Agents (Workers) to implement your strategy.
6. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

Prefer one dominant hypothesis per generation. If changing multiple subsystems,
explain why they are inseparable. Otherwise split into separate generations.

Every plan must make attribution possible:
- `targeted_failure`: the single failure pattern this generation targets, with H2H/replay/evidence if available.
- `expected_behavior_change`: what concrete decisions should change at the table.
- `do_not_touch`: files, functions, streets, or subsystems workers must avoid.
- `measurement_plan`: how quality gates, critical scenarios, H2H, or precommit eval should prove the change is not a regression.

# Worker Count Decision
Choose 1–3 workers based on the current bot's total games played:
- **games < 50** (very uncertain): **1 worker only** — conservative change, avoid big structural risk
- **games 50–200** (moderate confidence): **2 workers** — standard plan (Direction A + Direction B)
- **games ≥ 200** (well-evaluated): **up to 3 workers** — can explore bolder, parallel improvements

# Worker Directions (assign to each worker)
- **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation, opponent modeling.
- **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
- **Direction C (Opponent Modeler):** *(Only use as 3rd worker when games ≥ 200)*
  - ALLOWED: Adding/modifying opponent tracking data structures
  - ALLOWED: Per-street statistics (preflop/flop/turn/river aggression separately): `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
  - ALLOWED: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list, use median
  - FORBIDDEN: Changing overall decision flow or non-opponent-model logic

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Performance Verification Report (SATLUTION-style objective analysis)
{
  "trend": "stagnant",
  "verified_improvements": [
    "Recent bump from 1654→1660 in periods 1431-1432, ending a 7-period plateau at 1655",
    "Strong 60-77% win rates against legacy bots (v1, v2, v3, v6, v10, v14, v15, v17, v22, v24, v26), confirming v30's baseline is solidly above older generations",
    "Competitive 53% mirror-edge vs claude_v29 (immediate predecessor), confirming incremental progress"
  ],
  "persistent_weaknesses": [
    "catastrophic 30% win rate vs claude_v23 (rated only 1622, 32 pts below v30) — v23 exploits a systematic hole in v30's strategy that 10 generations have not patched",
    "consistent 40% loss rate cluster vs v4, v5, v8, v12, v13 — at least 5 opponents extract value from the same recurring flaw",
    "v13 (top-rated at 1660) holds a 60-40 edge over v30, meaning v30 cannot overtake the leaderboard without solving this matchup",
    "overall win rate only 54% despite being 2nd-highest rated — high variance / thin edge rather than dominant play"
  ],
  "diversity_needed": true,
  "diversity_reason": "Top rating flat at 1655 for 7 consecutive periods, barely reached 1660. Last ~10 generations treading water. v23 weakness persisted across multiple generations without resolution, suggesting current template cannot adapt through incremental tuning alone.",
  "suggestion": "Analyze 20 replay games (6W-14L) against claude_v23 to identify specific exploit pattern — likely aggression or trap strategy that v30's decision tree mishandles. Assign one worker to build targeted counter-strategy. Simultaneously analyze v13 matchup at same decision points."
}
Use `verified_improvements` to avoid duplicating what already worked.
Use `persistent_weaknesses` to prioritise what to fix next.
**If `diversity_needed: true`, you MUST try a substantially different approach this generation.**

# Diversity Injection (Anti-Local-Optima)
If `performance_verification.diversity_needed: true` OR the same type of change has failed 2+ consecutive generations:
- You MUST try a **substantially different approach** this generation
- Examples of "substantially different":
  - Last 2 gens tuned constants → this gen add new opponent model function (Direction A)
  - Last 2 gens added preflop logic → this gen focus on river/showdown decisions
  - Last 2 gens had Logic Architect fail → this gen try only HyperTuner (conservative, 1 worker)
- Explicitly state in your `analysis` field: `"Diversity injection: trying X instead of Y"`
- This prevents getting trapped in a local optimum where the same direction never improves

# Stagnation Decision
STAGNATION CONFIRMED (high confidence). Top rating flat at ~1655 for 10+ periods with only 5pt net movement — well within ±122pt Glicko noise. v13 still leads at r=1660 despite 17 generations of evolution (v13→v30). The lineage has exhausted its evolutionary gradient. RECOMMENDATION: Branch from claude_v13 (r=1660, rd=61.1) — the actual top performer — to explore a radically different strategy. diversity_needed: true. Key weakness: 30% WR vs claude_v23 (r=1622) and 40% loss cluster vs v4/v5/v8/v12/v13 suggests a shared vulnerability being exploited. Critical leak: 0% postflop fold rate across ALL streets — bot literally never folds after preflop, making it a calling station.

# Recent Match Analysis
{
  "weaknesses": [
    "Catastrophic big-pot losses: 19 of 60 games lost >5000 chips, many near -20000 (full buy-in) — poor risk management when facing aggression or holding marginal hands in big pots.",
    "Excessive preflop folding (65-69%) with very low preflop raise sizing (0.6-0.7x pot average) — playing too tight preflop, not capitalizing on position or initiative.",
    "Extreme passivity on later streets: raise rates drop from ~30% flop/turn to only 15-28% river, while call rates balloon to 72-84%. 0% fold rate on every postflop street — maximally exploitable calling station."
  ],
  "street_weaknesses": {
    "preflop": "Fold rate 65-69% far too high for HU (should be ~40-50%). Only 12-25% raise rate and 1-2% all-in. Not 3-betting enough to punish aggressive open-raises.",
    "flop": "0% fold rate means bot never gives up on flop, even with air. ~0.5x pot raise sizing too small to deny equity.",
    "turn": "0% fold, calling too wide on turn, setting up expensive river decisions. avg_raise of 0.4-0.5x pot underbetting strong hands.",
    "river": "Raise rate drops to 15-28%, call rate 71-84%. 0% fold on river — pure payoff station. 0.4-0.6x pot raise sizing not maximizing value."
  },
  "patterns": {
    "big_loss_frequency": "Consistently loses 3-4 full stack (-18000 to -20000) games per match — systemic leak in big-pot play.",
    "postflop_call_station": "0% postflop fold rate across ALL streets in ALL matches — most glaring exploit.",
    "sizing_leak": "Average raise sizing 0.4-0.7x pot across all streets — never makes pot-sized or overbet raise."
  },
  "recommendation": {
    "priority": "CRITICAL — Add postflop fold logic: 0% postflop fold rate is the single biggest leak.",
    "specific_changes": [
      "Implement fold threshold on each postflop street: fold ~15-20% on flop with weak no-pair no-draw hands facing bet ≥0.5x pot; fold ~20-25% on turn when equity <25%; fold ~15-20% on river with bluff-catchers facing ≥pot-sized bets.",
      "Increase river raise sizing to 0.7-1.0x pot for value raises; consider overbet shoving (~1.5x pot) with nutted hands.",
      "Reduce preflop fold rate from ~67% to ~50% by widening BB defending range; increase SB raise sizing to 2.5-3x."
    ]
  }
}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows.
`tasks` array may contain **1, 2, or 3** items based on your Worker Count Decision above.

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What weakness are you targeting? Are we failing due to bad logic or bad parameters? If diversity injection applies, explain: 'Diversity injection: trying X instead of Y'.",
  "targeted_failure": "One dominant failure pattern this generation targets, with the strongest evidence source.",
  "expected_behavior_change": "Specific table behavior that should change, e.g. call AKs all-in preflop more often but do not widen weak offsuit calls.",
  "do_not_touch": ["List files/functions/subsystems that must remain unchanged for attribution."],
  "measurement_plan": "How to verify this hypothesis: critical scenarios, H2H weak opponent, parent comparison, expected small-sample signal.",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

Notes:
- `branch_from` is OPTIONAL. Only include it to override the default evolution source.
- For 1-worker plans, use only Direction A or only Direction B.
- For 3-worker plans, add a Direction C (Opponent Modeler) task as worker_id 3.
- Avoid v29-like broad mixes such as "preflop rewrite + river rewrite + all-street sizing" unless you explicitly prove those changes are inseparable.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the H2H average win rate trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.
8. **ATTRIBUTION**: Do not mix unrelated preflop, postflop, and sizing rewrites in one generation. The next evaluation must be able to attribute win/loss movement to this plan.

Current evolution: v30 → v31
Bot directory: bots/claude_v30/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl
Head-to-Head data: web/core/results/head_to_head.json
Bot stats: web/core/results/bot_stats.json
Experience pool: web/core/experience_pool.md  ← READ THIS, not evolution_workspace/experience_pool.md

=============================
[CLAUDE OUTPUT]
Now let me read the current bot source code and the highest-rated reference bot (v13):
Let me read the reference bots v13 (highest rated) and v23 (catastrophic weakness):
Now I have a comprehensive understanding of the situation. Let me compute the H2H average win rate for v30 and finalize my analysis.

**v30 H2H Win Rates (equal-weighted across 24 opponents):**
- Strong wins (>55%): v1(77%), v10(70%), v15(70%), v26(70%), v24(65%), v2(60%), v3(60%), v6(60%), v14(60%), v17(60%), v22(60%), v25(55%), v29(53%), v11(55%)
- Neutral (45-55%): v7(45%), v9(50%), v19(50%), v20(50%)
- Weaknesses (<45%): v4(40%), v5(40%), v8(40%), v12(40%), v13(40%), v23(30%)

**H2H Average = 54.4%**

The root cause is clear from the match analysis and code: **0% postflop fold rate** — the `postflop_call_margin()` function (line 177-216 in strategy.py) produces margins of only 0.0-0.08, which combined with simulation-based `realized_rate` that rarely falls below `pot_odds + call_margin` for any paired hand, means the bot literally never folds postflop. This single leak explains all 6 weakness matchups (v23, v4, v5, v8, v12, v13) — aggressive opponents exploit the calling station by betting big with value and bluffing freely.

```json
{
  "analysis": "v30 has H2H average 54.4% (r=1654, 340 games, rd=61). The CRITICAL leak is 0% postflop fold rate across ALL streets — confirmed by match analysis of 60 games. The root cause is in strategy.py's call decision flow (lines 566-687): `postflop_call_margin()` returns 0.0-0.08 which is insufficient to make `realized_rate < pot_odds + call_margin` trigger for any paired hand. The bot calls with bottom-pair-no-kicker facing pot-sized river bets because call_margin (max 0.08) doesn't overcome the gap between realized_rate (~0.30 for weak pairs) and pot_odds (~0.33 for pot bet). This calling station tendency is exploited by v23 (30% WR), v4/v5/v8/v12/v13 (all 40% WR) — 6 opponents totaling -124 Glicko points of weakness. Diversity injection: adding a structural `should_fold_postflop()` function (new decision pathway) instead of the parameter tuning that previous generations likely attempted. This generation targets ONLY the postflop fold leak and sizing, leaving all other subsystems untouched for attribution.",
  "targeted_failure": "0% postflop fold rate — the bot never folds after seeing the flop, making it a maximally exploitable calling station. Evidence: match analysis shows 0% fold on flop/turn/river across ALL 60 analyzed games. This directly causes the 6 opponent weakness cluster (v23@30%, v4/v5/v8/v12/v13@40%) where aggressive opponents extract maximum value.",
  "expected_behavior_change": "Bot should fold ~15-20% of postflop spots where it currently calls: specifically folding air (no pair, no draw) facing >=50% pot bets, folding weak pairs (bottom/middle with bad kicker) facing large turn/river bets, and folding bluff-catchers facing >=pot-sized river bets. River raise sizing should increase from 0.4-0.6x pot to 0.7-1.0x pot for value hands. Preflop fold rate should decrease from ~67% to ~50-55% by widening the BB defending range.",
  "do_not_touch": ["opponent.py", "simulation.py", "state.py", "tournament.py", "main.py", "card_utils.py", "trap_nut_slowplay logic", "blocker_bluff_profile logic", "anti_lock_pressure logic", "river_exact_blocker_bluff logic", "nutted_risk_profile logic"],
  "measurement_plan": "1) Quality gate: decision tests must pass >=70% including 'fold weak hand facing big river bet' scenario. 2) Smoke test: mirror battle vs bot6 should not regress. 3) H2H signal: expect v23 WR to improve from 30% toward 42-48%, v4/v5/v8/v12/v13 cluster to improve from 40% toward 48-52%. 4) Overall H2H average should increase from 54.4% toward 56%+. 5) Postflop fold rate should register 10-20% (vs current 0%) in next match analysis. 6) No regression vs strong opponents (v10/v15/v24/v26 should remain >60%).",
  "branch_from": null,
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py"],
      "difficulty": "hard",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to FIX the #1 critical leak in this poker bot: the 0% postflop fold rate.\n\n## THE PROBLEM\nThe bot NEVER folds after seeing the flop. Match analysis of 60 games confirms 0% fold rate on flop, turn, AND river. This makes it a calling station that aggressive opponents exploit ruthlessly (30% WR vs v23, 40% WR vs v4/v5/v8/v12/v13).\n\nThe root cause is in `get_action()` in strategy.py. When `to_call > 0` postflop, the fold decision at line ~683:\n```python\nif realized_rate < pot_odds + call_margin:\n    if not anti_lock_call_continue and not strong_made_continue:\n        return -1\n```\n`call_margin` from `postflop_call_margin()` returns only 0.0-0.08, which is too low. For a weak pair (realized_rate ~0.30) facing a pot-sized bet (pot_odds ~0.33), the threshold is only 0.41, and the bot calls because 0.30 < 0.41 → fold triggers... BUT the realized_postflop_equity function doesn't penalize weak pairs enough — for middle/bottom pair with position, eqr=0.84, so realized_rate = win_rate * 0.84. If win_rate from simulation is 0.40+, realized_rate = 0.34+, and with pot_odds 0.33, the condition 0.34 < 0.33+0.04 = 0.37 may or may not trigger depending on exact values.\n\nThe REAL issue is that weak pairs and air hands need MUCH higher call_margin values when facing aggression, especially on later streets.\n\n## YOUR TASK\nAdd a new function `should_fold_postflop()` in strategy.py (place it near the top, after the imports but before `opponent_pressure_adjustment`). Then integrate it into the `get_action()` function's `to_call > 0` section.\n\n### Function: `should_fold_postflop(round_idx, made_strength, draw_strength, pair_profile, value_profile, board_texture, spot_info, pot, to_call)`\nThis function should return True when the bot should fold. Logic:\n\n1. **Air hands (made_strength < 0.18 AND draw_strength < 0.08) facing any postflop bet >= 40% pot on any street:** fold if no pair and no draw. Exception: don't fold if to_call/pot < 0.25 (tiny bet).\n\n2. **Weak pairs on turn (round_idx == 2) facing large bets:** fold if pair_profile indicates bottom_pair/underpair/board_pair with weak_kicker, AND bet_size_bucket is 'large' (>=0.75 pot ratio), AND draw_strength < 0.12.\n\n3. **Weak pairs on river (round_idx == 3) facing medium+ bets:** fold if pair_profile indicates middle_pair/bottom_pair/underpair/board_pair, AND bet_size_bucket is 'medium' or 'large' (>=0.30 pot ratio), AND the hand is not a 'strong' or 'nut' value tier. Exception: don't fold if kicker >= 12.\n\n4. **Bluff-catchers on river:** fold if made_strength < 0.30, draw_strength < 0.08, facing a >=pot-sized bet (pot_odds >= 0.33), and no blocker_profile eligible. Exception: don't fold if value_profile['tier'] in ('strong', 'nut').\n\n### Integration into get_action()\nIn the `to_call > 0` section of `get_action()`, add a call to `should_fold_postflop()` EARLY, right after the `if to_call > 0:` block starts computing postflop call_margin (around line 573). Add this check:\n```python\n# Early fold for clearly dominated hands\nif round_idx > 0 and not anti_lock_pressure:\n    fold_decision = should_fold_postflop(\n        round_idx, made_strength, draw_strength, pair_profile,\n        value_profile, board_texture, spot_info, pot, to_call\n    )\n    if fold_decision and not anti_lock_call_continue:\n        return -1\n```\nPlace this BEFORE the existing `if realized_rate < pot_odds + call_margin:` check (around line 683). This way, the explicit fold logic fires first for clearly dominated spots, and the existing equity-based logic handles borderline cases.\n\n### Also increase `postflop_call_margin()` values\nIn the `postflop_call_margin()` function (around line 177-216), increase the base margins:\n- `weak_showdown` margin: change 0.012 → 0.025\n- `air_hand` margin: change 0.018 → 0.035\n- For `size_bucket == 'large'` on river: add an extra +0.015 to margin\n- For `round_idx >= 2 and air_hand`: add +0.015 to margin\n- Update the clamp range from `(0.0, 0.08)` to `(0.0, 0.12)` to allow higher margins\n\n### IMPORTANT CONSTRAINTS\n- Do NOT modify any other functions besides `postflop_call_margin()`, `should_fold_postflop()` (new), and `get_action()`\n- Do NOT touch preflop logic, opponent modeling, or tournament logic\n- Do NOT modify any other files besides strategy.py\n- The `should_fold_postflop()` function must handle None values gracefully (pair_profile can be None preflop, board_texture can be None)\n- Keep total line count of strategy.py under 1000 lines\n- Test that the function doesn't fold strong hands (value_profile['tier'] == 'nut' or 'strong')\n\n### EXPECTED OUTCOME\nPostflop fold rate should go from 0% to ~15-20%. This should immediately improve the v23 matchup from 30% toward 45%+ and the v4/v5/v8/v12/v13 cluster from 40% toward 50%+."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py", "betting.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to adjust numeric constants and thresholds. DO NOT add new functions, classes, or change control flow. DO NOT modify any files other than constants.py and betting.py.\n\n## CONTEXT\nThe poker bot has two secondary issues that compound the primary postflop fold leak:\n1. **Underbetting**: Average raise sizing is 0.4-0.7x pot across all streets — too small to deny equity or extract max value\n2. **Over-simulating**: 900/1200/1500 simulations per street is excessive and wastes computation; v13 (the top-rated bot) uses only 500/800/900\n\n## YOUR TASKS\n\n### Task 1: In constants.py — Reduce simulation counts\nChange SIMULATIONS_BY_PUBLIC_COUNT:\n- 0: 900 → 700\n- 3: 1200 → 900\n- 4: 1500 → 1000\n\nChange EXTRA_SIMULATIONS_BY_PUBLIC_COUNT:\n- 0: 300 → 200\n- 3: 350 → 250\n- 4: 300 → 200\n\nThese are proven values from v13 (the highest-rated bot at r=1660) that work well with fewer sims.\n\n### Task 2: In betting.py `choose_raise()` — Increase postflop raise sizing\nIn the `choose_raise()` function, increase the base ratios for postflop streets:\n- Flop (round_idx == 1): change ratio from 0.60 → 0.65\n- Turn (round_idx == 2): change ratio from 0.70 → 0.78\n- River (round_idx == 3): change ratio from 0.85 → 0.95\n\nThese changes make value bets larger, which:\n- Extracts more value from calling stations (matches the meta of the opponent pool)\n- Makes bluffs more credible\n- Aligns with the match analysis recommendation of 0.7-1.0x pot for value raises\n\n### Task 3: In betting.py `choose_raise()` — Increase preflop open sizing\nIn the `choose_raise()` function, for preflop (round_idx == 0):\n- When `to_call == 0` (SB open): change ratio from 0.55 → 0.62\n- When `to_call > 0` (3bet/raise): change ratio from 0.75 → 0.80\n\nIn the SB open section of `choose_preflop_spot_action()` (in betting.py):\n- Change `desired_total` multiplier from 2.5 → 2.8: `(2.8 + max(0.0, preflop_strength - 0.58) * 1.8) * BIG_BLIND`\n\nThis increases the preflop raise from ~2.5BB to ~2.8BB, which is closer to optimal HU sizing.\n\n### Task 4: In betting.py `choose_preflop_spot_action()` — Widen BB defending range\nIn `choose_preflop_spot_action()`, for the `bb_vs_limp` case:\n- Change `iso_threshold` from `0.57 + match_adjust - loose_bonus + match_profile['open_delta']` to `0.52 + match_adjust - loose_bonus + match_profile['open_delta']`\n\nFor the `sb_open` case:\n- Change `open_threshold` from `0.49 + match_adjust + 0.02 + match_profile['open_delta']` to `0.45 + match_adjust + 0.02 + match_profile['open_delta']`\n- Change `limp_threshold` from `0.36 + match_adjust` to `0.32 + match_adjust`\n\nThese changes widen the opening range (play more hands preflop), reducing the preflop fold rate from ~67% toward ~50-55%.\n\n### IMPORTANT CONSTRAINTS\n- ONLY modify numeric values (constants, thresholds, ratios, multipliers)\n- DO NOT add any new functions, classes, or control flow statements\n- DO NOT modify strategy.py, postflop.py, opponent.py, or any other files\n- DO NOT change the clamp bounds in choose_raise (those are structural)\n- Ensure all changes are backward-compatible (same function signatures)"
    }
  ]
}
```
