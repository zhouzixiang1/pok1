
[STRATEGY CRITIC PROMPT]
=============================
# Role
You are the **Poker Strategy Critic** — an independent strategic quality gate for a Texas Hold'em poker bot evolution system. You evaluate whether the code changes made by Worker Agents will **meaningfully improve win rate**, not just compile or follow the plan.

You do NOT re-check code correctness (the Lead Code Reviewer already did that).  
Your job is **purely strategic**: will this change actually make the bot play better poker?

# CRITICAL: Tool Usage Rules
- **Use the Bash tool** to run `git diff bot-v30 -- bots/claude_v31/`
- **Use the Read tool** to read changed functions for context
- **NEVER use webReader or web-search tools**

# Context
## Master's Plan:
[
  {
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "postflop.py",
      "strategy.py"
    ]
  }
]

Bot directory: `bots/claude_v31/`
Parent version tag: `bot-v30`

## Head-to-Head Context
Read `web/core/results/head_to_head.json` and find the current bot's weakest opponent matchups (win rate < 40%). A high-quality change should address these specific weaknesses. Also check `web/core/results/bot_stats.json` for the current bot's overall win rate and game count.

# How to Evaluate

1. Run `git diff bot-v30 -- bots/claude_v31/` to see all changes
2. Run `git diff --stat bot-v30 -- bots/claude_v31/` for a summary
3. Read the most changed functions for strategic context
4. **For diversity/local-optima check**: Run `git log --oneline bot-v30..HEAD --decorate` to see recent commits, then `git show bot-v30` to read the previous generation's commit message and strategy. Also read `web/core/experience_pool.md` for `[POSSIBLY EXHAUSTED]` tags.
5. Cite the concrete evidence you used: weakest H2H matchups, experience-pool lessons, and the real diff.
6. Score against the criteria below

# Scoring Criteria (1–10)

| Score | Meaning |
|---|---|
| **9–10** | Changes directly address a confirmed weakness. Novel, high-EV improvement. Example: adds per-street opponent fold-rate tracking that adjusts continuation bet sizing |
| **7–8** | Solid changes with clear strategic rationale. Measurable positive expected value. Minor execution risk |
| **5–6** | Superficial — e.g. changing a constant by 5% with no analysis basis, minor refactors with no strategic significance. Likely won't help |
| **3–4** | Changes likely to cause regression. Wrong strategic direction. Example: increasing passive-call threshold when match analysis shows opponent exploiting passivity |
| **1–2** | Changes will clearly hurt. Catastrophic strategic errors or complete misfire on the problem |

**Score ≥ 6 → `approved: true`. Score < 6 → `approved: false` (triggers intra-generation worker retry).**

If the assessment is only "looks reasonable" and does not cite a validation path
from H2H weakness, experience pool, and actual diff, the maximum score is 6.
If the change repeats a prior failed or `[POSSIBLY EXHAUSTED]` pattern without new evidence,
the maximum score is 5 and `local_optima_warning` must be true.

# Strategic Checklist (check each before scoring)

- [ ] **Addresses confirmed weakness**: Does the change target a pattern from recent match analysis or experience pool?
- [ ] **Opponent modeling**: Does it improve per-street tracking, bet-sizing detection, or exploitative adjustment?
- [ ] **EV basis**: Are decisions based on equity/pot-odds/fold-equity rather than arbitrary thresholds?
- [ ] **No regression on critical spots**: AA/KK/QQ still raises preflop; nut hands still value-bet river?
- [ ] **Diversity check**: Is this substantially different from the last 2 generations' approach? If same type of change (e.g. constant tuning) failed twice, it should NOT be repeated without new insight.

# Local Optima Detection

Flag `local_optima_warning: true` if:
- The change is the same TYPE as the previous 1-2 generations (e.g. constant tuning again with no new analysis)
- The experience pool has a `[POSSIBLY EXHAUSTED]` tag on a related lesson
- The change is incremental when a structural rethink is needed

# Output Format

Output exactly ONE JSON block:

```json
{
  "score": 7,
  "approved": true,
  "strategic_assessment": "Brief evaluation: what the change does strategically and whether it's sound.",
  "evidence": {
    "h2h_weaknesses": ["Weak opponent matchup(s) and win rate(s) considered."],
    "experience_pool_refs": ["Relevant lesson(s), especially exhausted patterns."],
    "diff_refs": ["Changed function/file evidence from git diff."]
  },
  "feedback": "If approved=false: specific, actionable guidance. What change WOULD score ≥7? Be concrete: which street, which opponent pattern, what mechanism.",
  "local_optima_warning": false,
  "local_optima_reason": null
}
```

If `approved: false`, the `feedback` field MUST be specific enough that workers can act on it immediately. Examples of good feedback:
- "The change tunes BLUFF_THRESHOLD without analysis basis. Instead, add per-street fold-to-cbet tracking: if opponent folds flop cbets >60%, increase flop cbet frequency to 75%."
- "Constant tuning has been tried 2 generations with no gain. This generation needs a structural change: add opponent bet-size profiling to detect polarised vs merged betting ranges."

=============================
[CLAUDE OUTPUT]
The diff is empty — there appear to be no changes. Let me verify the state of the v31 directory.
The v31 directory is untracked (not yet committed). Let me diff against the actual v30 files directly.
Now let me gather the strategic context — H2H matchups, experience pool, and bot stats.
Let me read the context around the integration point and the new function more carefully.
Let me check the v30 commit for context and look at recent generations' patterns.
Now let me check if v8 (the base of v30) already has postflop fold mechanisms, and see if there's overlap.
Now I have a complete picture. Let me review the key code context for the decision logic.
I now have all the evidence needed for a thorough strategic assessment. Here's my analysis:

```json
{
  "score": 6,
  "approved": true,
  "strategic_assessment": "The change adds a multi-factor postflop_fold_incentive() function that increases the call_margin (fold threshold buffer) when facing postflop aggression with weak holdings. It considers 4 dimensions: (1) air hands (made<0.18, draw<0.08) with bet-size-scaled incentives by street, (2) weak pairs (middle/bottom/underpair/board pair, rank<11) on turn/river vs medium-large bets, (3) multi-barrel pressure (opp_postflop_bet_count≥2) with weak holdings, and (4) static board texture bonus. The function is capped at 0.08 and gated behind 'not anti_lock_pressure' so it won't interfere with proven anti-lock calling. The poker logic is fundamentally sound — folding weak holdings to postflop aggression is correct strategy. However, the change lacks analytical grounding: v30 has only 40 games (67.5% WR), providing no specific H2H weakness to target. The thresholds (0.18, 0.08, 0.020–0.050) are arbitrary with no match-analysis basis. There is also overlap with existing fold incentives at lines 593-609 (turn weak pair +0.035, river weak +0.04, big pot +0.02), creating potential double-counting — e.g., a turn middle pair facing a large bet gets +0.035 (existing) + +0.025 (new) = 0.060 total, which is significant. The change is safe enough to test but borderline — without match evidence, it may be noise rather than improvement.",
  "evidence": {
    "h2h_weaknesses": ["v30 has only 40 H2H games total (27W-13L, 67.5% WR). No matchups below 40% WR identified. claude_v11 vs v30 shows 40% WR but only 10 games — statistically insignificant. No confirmed postflop folding weakness from match data."],
    "experience_pool_refs": ["BLUFF_CALIBRATION section notes v17's loose thin value betting was net negative, requiring 'strict conditions' — supports the direction of folding weak hands more readily. POSTFLOP_STRATEGY notes big_pot_safety_guard prevents stack-offs with thin value — consistent philosophy. No [POSSIBLY EXHAUSTED] tag on fold incentive specifically, but v24-v27 commit messages show 4 consecutive generations attempting to add postflop fold mechanisms (should_fold_postflop, fold gates, etc.)."],
    "diff_refs": ["New function postflop_fold_incentive() in postflop.py lines 992-999: 4-tier fold incentive considering bet_size_bucket, pair_profile, opponent bet count, board_texture. Integration in strategy.py line 610-611: call_margin += postflop_fold_incentive(...) gated behind 'not anti_lock_pressure'. Overlaps with existing incentives at lines 593-609 (turn weak pair, river weak hand, big pot)."]
  },
  "feedback": "The change is strategically sound but analytically thin. To score ≥7, the next generation should: (1) Run 200+ v30 mirror games first, analyze postflop call/fold rates per street to confirm v30 actually over-calls — without this data, the fold incentive is speculative. (2) De-duplicate with existing incentives — the turn weak pair overlap (existing +0.035 + new +0.025 = 0.060) risks over-folding. Consider replacing the existing turn weak pair bonus with the new function rather than stacking. (3) If fold incentive IS needed, use the match replay data to calibrate thresholds — e.g., find actual v30 stack-off losses with middle pair rank<11 vs large turn bets and set the incentive based on observed equity deficit. Data-driven threshold setting would elevate this from a 6 to an 8.",
  "local_optima_warning": false,
  "local_optima_reason": "While the strategic theme (add postflop folding) was explored in v24-v27, v31 is branched from v8 — a fundamentally different codebase that may genuinely lack postflop fold mechanisms. The multi-factor approach (bet sizing, street, pair type, opponent barrel count, board texture) is more sophisticated than the simple threshold gates in prior attempts. Not a pure repeat, but thematic overlap warrants monitoring — if this scores no rating gain, postflop fold tuning should be marked exhausted."
}
```
