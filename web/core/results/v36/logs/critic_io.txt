
[STRATEGY CRITIC PROMPT]
=============================
# Role
You are the **Poker Strategy Critic** — an independent strategic quality gate for a Texas Hold'em poker bot evolution system. You evaluate whether the code changes made by Worker Agents will **meaningfully improve win rate**, not just compile or follow the plan.

You do NOT re-check code correctness (the Lead Code Reviewer already did that).  
Your job is **purely strategic**: will this change actually make the bot play better poker?

# CRITICAL: Tool Usage Rules
- **Use the Bash tool** to run `git diff bot-v7 -- bots/claude_v36/`
- **Use the Read tool** to read changed functions for context
- **NEVER use webReader or web-search tools**

# Context
## Master's Plan:
[
  {
    "worker_id": "original-worker-1",
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "bots/claude_v36/postflop.py"
    ],
    "worker_prompt": "Reduce postflop.py from 1015 to \u22641000 lines without changing poker logic"
  },
  {
    "worker_id": "fix-aks-regression",
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "bots/claude_v36/postflop.py",
      "bots/claude_v36/strategy.py"
    ],
    "worker_prompt": "Fix AKs facing all-in regression caused by blank line removal"
  }
]

Bot directory: `bots/claude_v36/`
Parent version tag: `bot-v7`

## Head-to-Head Context
Read `web/core/results/head_to_head.json` and find the current bot's weakest opponent matchups (win rate < 40%). A high-quality change should address these specific weaknesses. Also check `web/core/results/bot_stats.json` for the current bot's overall win rate and game count.

# How to Evaluate

1. Run `git diff bot-v7 -- bots/claude_v36/` to see all changes
2. Run `git diff --stat bot-v7 -- bots/claude_v36/` for a summary
3. Read the most changed functions for strategic context
4. **For diversity/local-optima check**: Run `git log --oneline bot-v7..HEAD --decorate` to see recent commits, then `git show bot-v7` to read the previous generation's commit message and strategy. Also read `web/core/experience_pool.md` for `[POSSIBLY EXHAUSTED]` tags.
5. Cite the concrete evidence you used: weakest H2H matchups, experience-pool lessons, and the real diff.
6. Score against the criteria below

# Scoring Criteria (1–10)

| Score | Meaning |
|---|---|
| **9–10** | Changes directly address a confirmed weakness. Novel, high-EV improvement. Example: adds per-street opponent fold-rate tracking that adjusts continuation bet sizing |
| **7–8** | Solid changes with clear strategic rationale. Measurable positive expected value. Minor execution risk |
| **5–6** | Superficial — e.g. changing a constant by 5% with no analysis basis, minor refactors with no strategic significance. Likely won't help |
| **3–4** | Changes likely to cause regression. Wrong strategic direction. Example: increasing passive-call threshold when match analysis shows opponent exploiting passivity |
| **1–2** | Changes will clearly hurt. Catastrophic strategic errors or complete misfire on the problem |

**Score ≥ 6 → `approved: true`. Score < 6 → `approved: false` (triggers intra-generation worker retry).**

If the assessment is only "looks reasonable" and does not cite a validation path
from H2H weakness, experience pool, and actual diff, the maximum score is 6.
If the change repeats a prior failed or `[POSSIBLY EXHAUSTED]` pattern without new evidence,
the maximum score is 5 and `local_optima_warning` must be true.

# Strategic Checklist (check each before scoring)

- [ ] **Addresses confirmed weakness**: Does the change target a pattern from recent match analysis or experience pool?
- [ ] **Opponent modeling**: Does it improve per-street tracking, bet-sizing detection, or exploitative adjustment?
- [ ] **EV basis**: Are decisions based on equity/pot-odds/fold-equity rather than arbitrary thresholds?
- [ ] **No regression on critical spots**: AA/KK/QQ still raises preflop; nut hands still value-bet river?
- [ ] **Diversity check**: Is this substantially different from the last 2 generations' approach? If same type of change (e.g. constant tuning) failed twice, it should NOT be repeated without new insight.

# Local Optima Detection

Flag `local_optima_warning: true` if:
- The change is the same TYPE as the previous 1-2 generations (e.g. constant tuning again with no new analysis)
- The experience pool has a `[POSSIBLY EXHAUSTED]` tag on a related lesson
- The change is incremental when a structural rethink is needed

# Output Format

Output exactly ONE JSON block:

```json
{
  "score": 7,
  "approved": true,
  "strategic_assessment": "Brief evaluation: what the change does strategically and whether it's sound.",
  "evidence": {
    "h2h_weaknesses": ["Weak opponent matchup(s) and win rate(s) considered."],
    "experience_pool_refs": ["Relevant lesson(s), especially exhausted patterns."],
    "diff_refs": ["Changed function/file evidence from git diff."]
  },
  "feedback": "If approved=false: specific, actionable guidance. What change WOULD score ≥7? Be concrete: which street, which opponent pattern, what mechanism.",
  "local_optima_warning": false,
  "local_optima_reason": null
}
```

If `approved: false`, the `feedback` field MUST be specific enough that workers can act on it immediately. Examples of good feedback:
- "The change tunes BLUFF_THRESHOLD without analysis basis. Instead, add per-street fold-to-cbet tracking: if opponent folds flop cbets >60%, increase flop cbet frequency to 75%."
- "Constant tuning has been tried 2 generations with no gain. This generation needs a structural change: add opponent bet-size profiling to detect polarised vs merged betting ranges."

=============================
[CLAUDE OUTPUT]
Now let me read the key changed areas for strategic context:
Now I have a complete picture. Let me compile the assessment.

```json
{
  "score": 6,
  "approved": true,
  "strategic_assessment": "This is a large structural change that adds an EXP3 bandit meta-learner for style selection, CBet/fold-to-CBet tracking, concept drift detection, and style-dependent threshold/sizing modulation throughout the decision tree. The changes go well beyond the Master's two tasks (line reduction + AKs fix). Strategically, the EXP3 concept is sound for adapting to different opponent types, and the opponent modeling additions (CBet tracking, drift detection, classify_opponent_style) are genuinely valuable. However, the EXP3 reward signal (per-hand chip_delta / INITIAL_CHIPS) is extremely noisy for 50-hand matches — a single all-in hand can swing ±20k chips, causing wild weight updates. With gamma=0.10 exploration across 5 styles, the bandit will not reliably converge within a match. The style modulation is applied to ALL decision points (open thresholds, 3bet width, bluff frequency, call margins, sizing ratios, trap probability) creating many interacting degrees of freedom. The unified all-in block merges two previously distinct code paths (opponent_allin vs to_call>=my_chips) that had meaningfully different buffer calculations, potentially causing subtle regressions in shove/call decisions.",
  "evidence": {
    "h2h_weaknesses": [
      "v36 vs v7: 0/10 = 0% win rate (catastrophic — but v36 IS built from v7, so this is bot-v7's parent being tested against itself in older version)",
      "v20 vs v36: 14/20 = 30% (v36 loses badly)",
      "v22 vs v36: 13/20 = 35% (v36 loses badly)",
      "v36 vs v8: 3/10 = 30% (v36 loses)"
    ],
    "experience_pool_refs": [
      "classify_opponent_style returns threshold deltas — +2-3 pts adaptation (experience pool says this is proven). The new classify_opponent_style function implements this.",
      "style_deltas MUST propagate to ALL bluff thresholds AND strong/medium decision thresholds — the new style_params propagation does this across all decision paths.",
      "bb_vs_raise/sb_vs_raise: let simulation decide. Hardcoded 3bet bluff spots are net negative — but the new code ADDS hardcoded bb_vs_raise 3bet/bluff logic (lines 487-523 in strategy diff), which contradicts this lesson.",
      "Deterministic hash for blocker bluff frequency is preferred over Random.random() — the new bb_vs_raise bluff uses a deterministic freq_token hash, consistent with this lesson."
    ],
    "diff_refs": [
      "postflop.py: 20 blank lines removed (1015→995 lines). Zero logic changes. Task 1 complete.",
      "strategy.py: +EXP3MetaLearner class in main.py, style_params parameter added to choose_preflop_spot_action, compute_postflop_decision, and all sizing functions.",
      "strategy.py: Unified all-in block (lines 757-767 in diff) merges opponent_allin and to_call>=my_chips paths into one block.",
      "strategy.py: New bb_vs_raise 3bet/bluff logic (diff lines 68-104) adds ~36 lines of hardcoded preflop code.",
      "opponent.py: +_track_hand_stats, CBet/fold-to-CBet tracking, classify_opponent_style (lines 13-240 in diff).",
      "constants.py: +STYLE_NAMES, STYLE_PARAMS (5 styles: gto, exploit_fold, exploit_call, exploit_maniac, exploit_nit), SIZING_TABLE, SIZING_TIER_RATIOS.",
      "main.py: sanitize_action now returns 0 for opponent_allin case (was -1). EXP3 state serialized via 'data' field in JSON response."
    ]
  },
  "feedback": "Score is 6 (borderline approved) due to three specific issues that must be addressed in the next iteration:\n\n1. **EXP3 reward noise**: Per-hand chip_delta / 20000 is too noisy for 50-hand matches. A single all-in hand swings the reward by ±1.0. FIX: Use a moving average of the last 3-5 hands' chip deltas, or switch to win/loss (±0.1) rather than chip-proportional rewards. Alternatively, update weights only every 5 hands.\n\n2. **Contradicts experience pool**: The new bb_vs_raise 3bet/bluff logic (36 lines of hardcoded conditions) contradicts the lesson 'bb_vs_raise: let simulation decide. Hardcoded 3bet bluff spots are net negative.' Either remove this block and let simulation handle BB defense, or add strong evidence from match analysis showing BB defense was a specific leak.\n\n3. **Unified all-in may regress**: The old code had separate paths for opponent_allin (with specific jam_buffer logic) vs to_call>=my_chips (with separate shove_buffer including thin value +0.04). These were collapsed into one path that loses the thin value buffer. If v36 had thin-value shove issues, this could hurt. Verify with targeted all-in spot testing.\n\nFor the next generation, focus on SIMPLIFYING the EXP3 integration: use it only for preflop style selection (open threshold + 3bet width), not postflop bet sizing where the interactions are complex. Let postflop sizing remain style-agnostic until EXP3 convergence is validated.",
  "local_optima_warning": false,
  "local_optima_reason": null
}
```

[STRATEGY CRITIC PROMPT]
=============================
# Role
You are the **Poker Strategy Critic** — an independent strategic quality gate for a Texas Hold'em poker bot evolution system. You evaluate whether the code changes made by Worker Agents will **meaningfully improve win rate**, not just compile or follow the plan.

You do NOT re-check code correctness (the Lead Code Reviewer already did that).  
Your job is **purely strategic**: will this change actually make the bot play better poker?

# CRITICAL: Tool Usage Rules
- **Use the Bash tool** to run `git diff bot-v7 -- bots/claude_v36/`
- **Use the Read tool** to read changed functions for context
- **NEVER use webReader or web-search tools**

# Context
## Master's Plan:
[
  {
    "worker_id": "revert-overreach",
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "bots/claude_v36/postflop.py",
      "bots/claude_v36/constants.py",
      "bots/claude_v36/main.py",
      "bots/claude_v36/opponent.py",
      "bots/claude_v36/strategy.py"
    ],
    "worker_prompt": "Revert EXP3 meta-learning overreach, restore v7 originals with only AKs fix + postflop line reduction"
  },
  {
    "worker_id": "trim-strategy",
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "bots/claude_v36/strategy.py"
    ],
    "worker_prompt": "Reduce strategy.py from 1162 to \u22641000 lines without changing poker logic"
  }
]

Bot directory: `bots/claude_v36/`
Parent version tag: `bot-v7`

## Head-to-Head Context
Read `web/core/results/head_to_head.json` and find the current bot's weakest opponent matchups (win rate < 40%). A high-quality change should address these specific weaknesses. Also check `web/core/results/bot_stats.json` for the current bot's overall win rate and game count.

# How to Evaluate

1. Run `git diff bot-v7 -- bots/claude_v36/` to see all changes
2. Run `git diff --stat bot-v7 -- bots/claude_v36/` for a summary
3. Read the most changed functions for strategic context
4. **For diversity/local-optima check**: Run `git log --oneline bot-v7..HEAD --decorate` to see recent commits, then `git show bot-v7` to read the previous generation's commit message and strategy. Also read `web/core/experience_pool.md` for `[POSSIBLY EXHAUSTED]` tags.
5. Cite the concrete evidence you used: weakest H2H matchups, experience-pool lessons, and the real diff.
6. Score against the criteria below

# Scoring Criteria (1–10)

| Score | Meaning |
|---|---|
| **9–10** | Changes directly address a confirmed weakness. Novel, high-EV improvement. Example: adds per-street opponent fold-rate tracking that adjusts continuation bet sizing |
| **7–8** | Solid changes with clear strategic rationale. Measurable positive expected value. Minor execution risk |
| **5–6** | Superficial — e.g. changing a constant by 5% with no analysis basis, minor refactors with no strategic significance. Likely won't help |
| **3–4** | Changes likely to cause regression. Wrong strategic direction. Example: increasing passive-call threshold when match analysis shows opponent exploiting passivity |
| **1–2** | Changes will clearly hurt. Catastrophic strategic errors or complete misfire on the problem |

**Score ≥ 6 → `approved: true`. Score < 6 → `approved: false` (triggers intra-generation worker retry).**

If the assessment is only "looks reasonable" and does not cite a validation path
from H2H weakness, experience pool, and actual diff, the maximum score is 6.
If the change repeats a prior failed or `[POSSIBLY EXHAUSTED]` pattern without new evidence,
the maximum score is 5 and `local_optima_warning` must be true.

# Strategic Checklist (check each before scoring)

- [ ] **Addresses confirmed weakness**: Does the change target a pattern from recent match analysis or experience pool?
- [ ] **Opponent modeling**: Does it improve per-street tracking, bet-sizing detection, or exploitative adjustment?
- [ ] **EV basis**: Are decisions based on equity/pot-odds/fold-equity rather than arbitrary thresholds?
- [ ] **No regression on critical spots**: AA/KK/QQ still raises preflop; nut hands still value-bet river?
- [ ] **Diversity check**: Is this substantially different from the last 2 generations' approach? If same type of change (e.g. constant tuning) failed twice, it should NOT be repeated without new insight.

# Local Optima Detection

Flag `local_optima_warning: true` if:
- The change is the same TYPE as the previous 1-2 generations (e.g. constant tuning again with no new analysis)
- The experience pool has a `[POSSIBLY EXHAUSTED]` tag on a related lesson
- The change is incremental when a structural rethink is needed

# Output Format

Output exactly ONE JSON block:

```json
{
  "score": 7,
  "approved": true,
  "strategic_assessment": "Brief evaluation: what the change does strategically and whether it's sound.",
  "evidence": {
    "h2h_weaknesses": ["Weak opponent matchup(s) and win rate(s) considered."],
    "experience_pool_refs": ["Relevant lesson(s), especially exhausted patterns."],
    "diff_refs": ["Changed function/file evidence from git diff."]
  },
  "feedback": "If approved=false: specific, actionable guidance. What change WOULD score ≥7? Be concrete: which street, which opponent pattern, what mechanism.",
  "local_optima_warning": false,
  "local_optima_reason": null
}
```

If `approved: false`, the `feedback` field MUST be specific enough that workers can act on it immediately. Examples of good feedback:
- "The change tunes BLUFF_THRESHOLD without analysis basis. Instead, add per-street fold-to-cbet tracking: if opponent folds flop cbets >60%, increase flop cbet frequency to 75%."
- "Constant tuning has been tried 2 generations with no gain. This generation needs a structural change: add opponent bet-size profiling to detect polarised vs merged betting ranges."

=============================
[CLAUDE OUTPUT]
The git diff returned empty. Let me verify this and check if the v36 files exist.
The diff is empty because bot-v7 was for `bots/claude_v7/`, not `bots/claude_v36/`. Let me check what the parent of v36 is and get the correct diff.
The v36 files are untracked (not yet committed). The parent is bot-v7 (which was `bots/claude_v7/`). Let me diff v36 against the v7 files directly.
Now let me check the remaining files and understand the full scope of changes.
Now let me check the experience pool for any relevant lessons about AKs, all-in calling, or preflop changes.
Now I have a complete picture. Let me compile my assessment.
```json
{
  "score": 4,
  "approved": false,
  "strategic_assessment": "v36 is nearly identical to its parent v7 — opponent.py, constants.py, main.py, simulation.py, state.py, tournament.py are byte-for-byte identical. postflop.py differs only by removed blank lines between functions (1015→995 lines, zero logic changes). strategy.py (1158→986 lines) has one real behavioral change: adding a preflop all-in call (`if state['opponent_allin'] and preflop_strength >= 0.55: return -2`). Everything else is cosmetic: semicolon one-liners, dict-lookup refactors, inlined variables, dead `choose_overbet_bluff_river` removal (it already returned None). The line count reduction from '≤1000' goal was achieved through formatting, not strategic restructuring. The single AKs fix has no analytical basis, no H2H validation, and directly contradicts experience pool lesson: 'When changing preflop eval, recalibrate ALL downstream thresholds. Parameter changes compound synergistically.' No downstream recalibration was done.",
  "evidence": {
    "h2h_weaknesses": [
      "v36 vs v7: 0-10 (0% win rate) — parent bot crushes v36 despite near-identical code. The one behavioral change may be actively harmful.",
      "v36 vs v23: 2-8 (20% win rate, 10 games) — severe weakness not addressed.",
      "v36 vs v1: 3-27 (10% win rate, 30 games) — large-sample confirmed weakness.",
      "v36 vs v20: 6-14 (30% win rate, 20 games) — moderate weakness.",
      "v36 vs v22: 7-13 (35% win rate, 20 games) — moderate weakness.",
      "Overall v36 win rate: 51.3% (118-112 in 230 games) — barely above parity."
    ],
    "experience_pool_refs": [
      "CRITICAL: 'When changing preflop eval, recalibrate ALL downstream thresholds. Parameter changes compound synergistically.' — v36 adds preflop all-in call without any downstream recalibration.",
      "v17's higher jam/shove buffer caps caused more aggressive all-in with marginal hands — warns against aggressive all-in thresholds.",
      "'Wholesale copy fails. Incremental targeted port wins. Fix ALL parameter issues simultaneously.' — v36 is effectively a wholesale copy with one additive line."
    ],
    "diff_refs": [
      "strategy.py line ~152 (new): `if state['opponent_allin'] and preflop_strength >= 0.55: return -2` — only behavioral change, no analysis basis for 0.55 threshold.",
      "strategy.py: choose_anti_lock_pressure_action — if/elif chain → dict-lookup `ratio_map = {0: ..., 1: ..., 2: ..., 3: ...}`, cosmetic refactor.",
      "strategy.py: postflop_call_margin — `weak_showdown` variable inlined, elif→ternary for size_bucket, cosmetic.",
      "strategy.py: realized_postflop_equity — early return refactored, `pair_type`→`pt` variable rename, cosmetic.",
      "strategy.py: choose_raise — parameter formatting, dict-lookup for ratio, semicolon one-liners, cosmetic.",
      "strategy.py: removed `choose_overbet_bluff_river` (always returned None) and its call site — dead code removal, no behavioral change.",
      "strategy.py: ~6 guard conditions collapsed into single `if` with `or` — cosmetic, no logic change.",
      "postflop.py: only blank line removal between functions (1015→995 lines), zero logic changes."
    ]
  },
  "feedback": "The core failure is that v36 is essentially v7 with cosmetic reformatting + one unsupported threshold change. To score ≥7, workers need to: (1) If the goal is 'revert EXP3 overreach to v7', confirm v36 already matches v7 and explain WHY adding AKs helps against specific H2H weaknesses. The 0-10 record vs v7 suggests this change may hurt. (2) Address actual H2H weaknesses: v36 loses badly to v23 (20% win rate) and v20 (30%). Analyze WHY — is it preflop play, postflop aggression, river decisions? Add targeted code for the specific weakness, not generic threshold changes. (3) If adding preflop all-in call logic, follow the experience pool lesson: recalibrate downstream thresholds. At minimum, adjust postflop EQR or continuation bet sizing for hands that now call all-in preflop but previously folded. (4) The line reduction strategy of reformatting existing code adds review risk without strategic value. Instead, identify and remove actual dead code paths or merge truly redundant decision branches.",
  "local_optima_warning": false,
  "local_optima_reason": null
}
```
