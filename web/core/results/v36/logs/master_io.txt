
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `web/core/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `web/core/results/head_to_head.json` — **PRIMARY DATA**: Head-to-Head matrix. Use this to compute H2H average win rate per bot (equal-weighted across all opponents). This is the **primary optimization metric**.
- `web/core/results/glicko_ratings.json` — Glicko-2 ratings (r, rd) as secondary reference for statistical context
- `web/core/results/bot_stats.json` — Per-bot stats: wins, losses, games, win_rate (games-weighted, biased by opponent frequency — use H2H average for equal weighting)
- `web/core/results/rating_history.jsonl` — Performance snapshots over time (includes both ratings and win_rates for recent periods)
- `web/core/experience_pool.md` — Accumulated strategic lessons from past generations (**THIS is the active pool, not evolution_workspace/experience_pool.md**)
- `bots/claude_v35/` — Current source bot code
- `web/core/reference_bots/bot1/` … `web/core/reference_bots/bot6/` — 6 strong reference bots. Read whichever is relevant.

**H2H Matrix Usage:** The head_to_head.json file is the most important data for identifying weaknesses. For each bot, check which opponents it loses to (< 40% WR = WEAKNESS) and which it beats (> 60% WR = STRENGTH). Focus improvements on closing weakness gaps.

Use Bash tool with `git log` and `git diff` to understand evolution history.

**When reading `experience_pool.md`, prioritise in this order:**
1. `## RECENT_LESSONS` — what just happened last 1-3 gens
2. `## OPPONENT_MODELING` — opponent model improvements (often highest EV)
3. Any entry with `[POSSIBLY EXHAUSTED]` — **avoid repeating these directions**

# Task
Your goal is to:
1. Read the H2H data and analyze the current bot's per-opponent performance. Compute and track H2H average win rate — this is the **primary metric**.
2. Read the performance verification report (provided below) for objective trend analysis.
3. Read the experience pool to learn from past iterations.
4. Read the current bot's source code and reference bots' code to identify weaknesses.
5. Dynamically assign 1–3 Developer Sub-Agents (Workers) to implement your strategy.
6. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

Prefer one dominant hypothesis per generation. If changing multiple subsystems,
explain why they are inseparable. Otherwise split into separate generations.

Every plan must make attribution possible:
- `targeted_failure`: the single failure pattern this generation targets, with H2H/replay/evidence if available.
- `expected_behavior_change`: what concrete decisions should change at the table.
- `do_not_touch`: files, functions, streets, or subsystems workers must avoid.
- `measurement_plan`: how quality gates, critical scenarios, H2H, or precommit eval should prove the change is not a regression.

# Worker Count Decision
Choose 1–3 workers based on the current bot's total games played:
- **games < 50** (very uncertain): **1 worker only** — conservative change, avoid big structural risk
- **games 50–200** (moderate confidence): **2 workers** — standard plan (Direction A + Direction B)
- **games ≥ 200** (well-evaluated): **up to 3 workers** — can explore bolder, parallel improvements

# Worker Directions (assign to each worker)
- **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation, opponent modeling.
- **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
- **Direction C (Opponent Modeler):** *(Only use as 3rd worker when games ≥ 200)*
  - ALLOWED: Adding/modifying opponent tracking data structures
  - ALLOWED: Per-street statistics (preflop/flop/turn/river aggression separately): `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
  - ALLOWED: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list, use median
  - FORBIDDEN: Changing overall decision flow or non-opponent-model logic

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Performance Verification Report (SATLUTION-style objective analysis)
{"trend": "stagnant", "verified_improvements": [], "persistent_weaknesses": ["Seven consecutive periods at 56.20% H2H win rate with zero movement", "claude_v35 absent from top-5 leaderboard (led by claude_v7 at 56.39%)", "The 0.19% bump at period 1262 is within noise range"], "diversity_needed": true, "diversity_reason": "Seven flat periods signal the same type of incremental tuning has been applied repeatedly without breaking through the plateau. The current gene pool is dominated by a narrow family of strategies.", "suggestion": "Force the next generation to evolve from an underrepresented ancestor (not claude_v7 or v23) and assign a worker to introduce a qualitatively different strategic axis."}
Use `verified_improvements` to avoid duplicating what already worked.
Use `persistent_weaknesses` to prioritise what to fix next.
**If `diversity_needed: true`, you MUST try a substantially different approach this generation.**

# Diversity Injection (Anti-Local-Optima)
If `performance_verification.diversity_needed: true` OR the same type of change has failed 2+ consecutive generations:
- You MUST try a **substantially different approach** this generation
- Examples of "substantially different":
  - Last 2 gens tuned constants → this gen add new opponent model function (Direction A)
  - Last 2 gens added preflop logic → this gen focus on river/showdown decisions
  - Last 2 gens had Logic Architect fail → this gen try only HyperTuner (conservative, 1 worker)
- Explicitly state in your `analysis` field: `"Diversity injection: trying X instead of Y"`
- This prevents getting trapped in a local optimum where the same direction never improves

# Stagnation Decision
CONFIRMED STAGNATION: 7 consecutive periods at ~56.2% H2H win rate with zero movement — classic local-optimum plateau. Top bot claude_v7 leads at 56.39%. diversity_needed: true. The gene pool is dominated by a narrow family of strategies. Suggestion: evolve from an underrepresented ancestor (not v7 or v23) and introduce a qualitatively different strategic axis — e.g., opponent-adaptive exploit frequencies or stack-depth-dependent aggression curves — rather than further tuning static constants.

# Recent Match Analysis
No match analysis data available for v35 (0 games played). Analyze top bots' strategies and look for structural weaknesses that can be exploited with a fundamentally different approach.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows.
`tasks` array may contain **1, 2, or 3** items based on your Worker Count Decision above.

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What weakness are you targeting? Are we failing due to bad logic or bad parameters? If diversity injection applies, explain: 'Diversity injection: trying X instead of Y'.",
  "targeted_failure": "One dominant failure pattern this generation targets, with the strongest evidence source.",
  "expected_behavior_change": "Specific table behavior that should change, e.g. call AKs all-in preflop more often but do not widen weak offsuit calls.",
  "do_not_touch": ["List files/functions/subsystems that must remain unchanged for attribution."],
  "measurement_plan": "How to verify this hypothesis: critical scenarios, H2H weak opponent, parent comparison, expected small-sample signal.",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

Notes:
- `branch_from` is OPTIONAL. Only include it to override the default evolution source.
- For 1-worker plans, use only Direction A or only Direction B.
- For 3-worker plans, add a Direction C (Opponent Modeler) task as worker_id 3.
- Avoid v29-like broad mixes such as "preflop rewrite + river rewrite + all-street sizing" unless you explicitly prove those changes are inseparable.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the H2H average win rate trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.
8. **ATTRIBUTION**: Do not mix unrelated preflop, postflop, and sizing rewrites in one generation. The next evaluation must be able to attribute win/loss movement to this plan.

Current evolution: v35 → v36
Bot directory: bots/claude_v35/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl
Head-to-Head data: web/core/results/head_to_head.json
Bot stats: web/core/results/bot_stats.json
Experience pool: web/core/experience_pool.md  ← READ THIS, not evolution_workspace/experience_pool.md

=============================
[CLAUDE OUTPUT]
Now let me read the rest of the H2H data and the current bot's source code:
Let me look at the v35 bot directory structure and code, plus the top-performing bots:
Now let me read the core v35 strategy files and compare with top performers:
Now I have a comprehensive understanding of the situation. Let me compile the full analysis and write the plan.
Now I have all the data I need. Let me produce the final analysis and plan.

```json
{
  "analysis": "CONFIRMED STAGNATION: 7 consecutive periods at ~56.2% H2H win rate with zero movement. Top bot claude_v7 leads at 56.51% (r=1657). v35 (current bot) has 0 games played — it was branched from v3 (48.61% WR) and was tagged at bot-v35 with fixes (all-in sanitize bug, widened preflop defense, lower EQR) but never got evaluated before the daemon rotated it out.\n\nSTUDIED: v7 (56.51% WR, r=1657) in depth — its full 1158-line strategy.py, 1015-line postflop.py, 249-line opponent.py. Also studied v35's 611-line strategy.py with EXP3MetaLearner, 856-line postflop.py, 362-line opponent.py.\n\nROOT CAUSE OF PLATEAU: Every recent bot uses the same static threshold architecture (v7-family). Even v35, which has multi-style EXP3, was built on v3's broken foundation (wrong priors 0.52/0.24 vs v7's 0.58/0.28, missing anti-lock, missing paired_board_stackoff, missing must_continue_vs_raise). The EXP3 approach was never tested with v7's defensive systems.\n\nDIVERSITY INJECTION: Merge v7's complete defensive infrastructure with v35's multi-style EXP3 meta-learning — a qualitatively different strategic axis. The EXP3 system provides REAL-TIME opponent classification (nit/maniac/station/fold-heavy) that dynamically adjusts ALL thresholds mid-match. No existing bot does this. v35 also has unique concept drift detection and cbet_rate/fold_to_cbet tracking that v7 lacks.\n\nBRANCHING: From v7 (not v35), since v35 was built on v3's broken foundation. Port v35's innovations INTO v7's working codebase.\n\nWHY 2 WORKERS ARE INSEPARABLE: Worker 1 creates the style-adaptive framework that Worker 2's constants tune. Without Worker 1's structural changes, Worker 2 has nothing to tune. Without Worker 2's tuned parameters, Worker 1's framework uses defaults that may regress. However, they CAN be split by file ownership: Worker 1 owns opponent.py + strategy.py (structure), Worker 2 owns constants.py (parameters). They operate on non-overlapping files.",
  "targeted_failure": "7-generation stagnation at 56.2% H2H average win rate. All recent bots use static thresholds — the same decision framework regardless of opponent type. v7 beats most opponents but can't break through because it uses identical thresholds vs nits (who fold too much) and calling stations (who call too much). v35's EXP3 multi-style approach could break this but was built on v3's broken foundation (wrong priors, missing defensive systems) and was never evaluated.",
  "expected_behavior_change": "1) Against nits (low VPIP <0.35, high fold_to_raise >0.50): bluff frequency increases by ~60% (exploit_fold style), open threshold lowers by 0.03, 3bet range widens by +0.08. 2) Against calling stations (high VPIP >0.55, low fold_to_raise <0.38): bluff frequency drops by 70% (exploit_call style), value betting increases, thin value bets enabled. 3) Against maniacs (high aggr >0.45): calling range widens significantly (call_threshold_delta -0.04), trap probability rises to 30%. 4) All changes layered on top of v7's anti-lock, paired_board_stackoff, must_continue, and safety guard systems.",
  "do_not_touch": ["card_utils.py", "simulation.py", "state.py", "tournament.py", "postflop.py (read-only reference only — all postflop functions are imported unchanged from v7)"],
  "measurement_plan": "1) Quality gates: py_compile, 1 smoke battle, decision tests ≥70%. 2) H2H evaluation: compare vs v7, v28, v4, v6 as primary opponents (these represent diverse strategy types). 3) Critical scenarios: (a) vs nit-like bots — bluff success rate should increase, (b) vs calling stations — value extraction should increase, (c) vs balanced bots — should not regress. 4) EXP3 weight convergence: after 50 hands, check if style selection is non-uniform (evidence of learning). 5) Attribution: if this bot improves, it's because of style adaptation — static threshold changes alone should not produce this effect.",
  "branch_from": "claude_v7",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["opponent.py", "strategy.py", "constants.py"],
      "difficulty": "hard",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your goal is to integrate v35's multi-style EXP3 meta-learning system into v7's proven codebase to create an opponent-adaptive bot.\n\n## WHAT YOU'RE BUILDING\nMerge two evolution lines:\n- v7 (56.51% WR, r=1657): has anti-lock pressure, paired_board_stackoff, must_continue_vs_raise, anti-bot4 detection, overbet river, safety guards. All these systems MUST be preserved.\n- v35 (0 games, from v3): has EXP3MetaLearner with 5 style strategies (gto, exploit_fold, exploit_call, exploit_maniac, exploit_nit), classify_opponent_style, cbet_rate/fold_to_cbet tracking, concept drift detection. These must be PORTED IN.\n\n## CRITICAL BUGS TO FIX FROM V35\n1. Wrong opponent model priors: v35 uses vpip=0.52, pfr=0.24. v7 uses vpip=0.58, pfr=0.28. The v7 values are PROVEN correct. Use v7's priors.\n2. v35's EXP3 get_effective_lambda uses 'gift_balance' which is HARMFUL per experience pool. Instead, use confidence directly: if confidence < 0.25, lambda=0 (no exploitation). Otherwise lambda = confidence * 0.5.\n3. v35's main.py uses v3's format. IGNORE v35's main.py entirely — use v7's main.py unchanged.\n\n## STEP-BY-STEP INSTRUCTIONS\n\n### Step 1: Read the source files\nRead these files FIRST:\n- `/Users/zhouzixiang/Documents/pok/bots/claude_v7/strategy.py` (1158 lines)\n- `/Users/zhouzixiang/Documents/pok/bots/claude_v7/opponent.py` (249 lines)\n- `/Users/zhouzixiang/Documents/pok/bots/claude_v7/constants.py` (63 lines)\n- Run: `git show bot-v35:bots/claude_v35/strategy.py` to see EXP3MetaLearner\n- Run: `git show bot-v35:bots/claude_v35/opponent.py` to see classify_opponent_style and cbet tracking\n- Run: `git show bot-v35:bots/claude_v35/constants.py` to see STYLE_PARAMS and SIZING_TABLE\n\n### Step 2: Add to constants.py\nAdd the following to `/Users/zhouzixiang/Documents/pok/bots/claude_v7/constants.py`:\n\n```python\n# Multi-Style Strategy Portfolio (from v35 EXP3 system)\nSTYLE_NAMES = ('gto', 'exploit_fold', 'exploit_call', 'exploit_maniac', 'exploit_nit')\nN_STYLES = len(STYLE_NAMES)\n\nSTYLE_PARAMS = {\n    'gto': {\n        'open_threshold_delta': 0.00,\n        'call_threshold_delta': 0.00,\n        'bluff_frequency_mult': 1.0,\n        'sizing_tier': 'standard',\n        'threebet_range_width_delta': 0.00,\n        'trap_probability': 0.00,\n    },\n    'exploit_fold': {\n        'open_threshold_delta': -0.03,\n        'call_threshold_delta': 0.02,\n        'bluff_frequency_mult': 1.4,\n        'sizing_tier': 'large',\n        'threebet_range_width_delta': 0.08,\n        'trap_probability': 0.05,\n    },\n    'exploit_call': {\n        'open_threshold_delta': 0.01,\n        'call_threshold_delta': -0.02,\n        'bluff_frequency_mult': 0.3,\n        'sizing_tier': 'polar',\n        'threebet_range_width_delta': -0.04,\n        'trap_probability': 0.15,\n    },\n    'exploit_maniac': {\n        'open_threshold_delta': 0.04,\n        'call_threshold_delta': -0.04,\n        'bluff_frequency_mult': 0.2,\n        'sizing_tier': 'medium',\n        'threebet_range_width_delta': 0.05,\n        'trap_probability': 0.30,\n    },\n    'exploit_nit': {\n        'open_threshold_delta': -0.06,\n        'call_threshold_delta': 0.04,\n        'bluff_frequency_mult': 1.6,\n        'sizing_tier': 'small',\n        'threebet_range_width_delta': 0.15,\n        'trap_probability': 0.00,\n    },\n}\n\nSIZING_TIER_RATIOS = {\n    'small': -0.06, 'medium': -0.02, 'standard': 0.00, 'large': 0.05, 'polar': 0.03,\n}\n\nSIZING_TABLE = {\n    (0, 'none'): 0.55, (0, 'thin'): 0.60, (0, 'strong'): 0.75, (0, 'nut'): 1.00,\n    (1, 'none'): 0.50, (1, 'thin'): 0.55, (1, 'strong'): 0.70, (1, 'nut'): 0.85,\n    (2, 'none'): 0.55, (2, 'thin'): 0.60, (2, 'strong'): 0.75, (2, 'nut'): 0.90,\n    (3, 'none'): 0.60, (3, 'thin'): 0.65, (3, 'strong'): 0.80, (3, 'nut'): 1.10,\n}\n```\n\n### Step 3: Modify opponent.py\nIn `/Users/zhouzixiang/Documents/pok/bots/claude_v7/opponent.py`:\n\na) Add `_track_hand_stats()` function from v35 — tracks per-hand stats including cbet_opportunities, cbet_count, fold_to_cbet_opportunities, fold_to_cbet_count.\n\nb) Modify `build_opponent_model()` to use `_track_hand_stats()` and return additional fields: 'cbet_rate', 'fold_to_cbet'. Use v7's priors (0.58/0.28), NOT v35's (0.52/0.24).\n\nc) Add `classify_opponent_style()` function from v35 that takes opponent_model and returns initial EXP3 weights based on VPIP/PFR/fold_to_raise patterns.\n\nd) Add concept drift detection from v35: when 12+ hands, compare recent 10-hand window to all-time. If VPIP drift >0.15 or PFR drift >0.12, blend recent-window stats into model.\n\ne) Keep detect_bot4_profile and get_anti_bot4_adjustments unchanged.\n\n### Step 4: Add EXP3MetaLearner to strategy.py\nAdd the EXP3MetaLearner class (from v35) to strategy.py. Key modifications:\n- Import STYLE_NAMES, N_STYLES, STYLE_PARAMS, SIZING_TABLE, SIZING_TIER_RATIOS from constants\n- Import classify_opponent_style from opponent\n- Add module-level `_exp3_learner = EXP3MetaLearner()`\n- Add `style_sizing_ratio()` function\n- The to_dict/from_dict serialization for persistence across hands\n\n### Step 5: Modify get_action() in strategy.py\nIn the `get_action()` function:\n\na) After building opponent_model and spot_info, choose style:\n```python\n# Initialize EXP3 bias from opponent classification\ninit_weights = classify_opponent_style(opponent_model)\n_exp3_learner.initialize_bias(init_weights)\nrng = random.Random(hash((my_id, len(requests))))\nstyle_idx = _exp3_learner.choose_style(rng)\nstyle_params = STYLE_PARAMS[STYLE_NAMES[style_idx]]\n```\n\nb) After computing strong/medium thresholds, apply style deltas:\n```python\n# Apply EXP3 style adaptation to thresholds\nstrong += style_params['open_threshold_delta']\nmedium += style_params['open_threshold_delta'] * 0.5\n# Style-based bluff frequency (already divided by bluff_frequency_mult in threshold comparisons below)\n```\n\nc) In ALL bluff threshold computations, divide by style_params['bluff_frequency_mult']:\n```python\nraise_fold_threshold = (0.56 - 0.30 * match_profile['bluff_delta']) / style_params['bluff_frequency_mult']\nblocker_raise_threshold = (0.55 - 0.32 * match_profile['bluff_delta']) / style_params['bluff_frequency_mult']\nriver_bluff_threshold = (0.62 - 0.28 * match_profile['bluff_delta'] - anti_bot4['bluff_freq_bonus']) / style_params['bluff_frequency_mult']\nprobe_fold_threshold = (0.56 - 0.32 * match_profile['bluff_delta'] - anti_bot4['bluff_freq_bonus']) / style_params['bluff_frequency_mult']\nsemi_bluff_threshold = (0.58 - 0.28 * match_profile['bluff_delta'] - anti_bot4['bluff_freq_bonus']) / style_params['bluff_frequency_mult']\n```\n\nd) In choose_raise calls, pass style_params:\n```python\nraise_amount = choose_raise(\n    ..., style_params=style_params,\n    match_sizing_delta=match_profile['sizing_delta'],\n    ...\n)\n```\n\ne) Add trap probability slowplay for nut hands (from v35):\n```python\nif (round_idx in (1, 2) and value_profile is not None and value_profile['tier'] == 'nut'\n    and board_texture is not None and not board_texture['dynamic']\n    and nutted_risk['risk'] <= 0.05\n    and style_params['trap_probability'] > 0.0\n    and rng.random() < style_params['trap_probability']):\n    return 0\n```\n\nf) After each hand result, update EXP3 learner. Add at the END of get_action() (before return):\nNOTE: Since get_action doesn't have access to the result, you'll need to handle EXP3 reward update in main.py's decide_action. Add to main.py:\n```python\n# After computing action, prepare for reward update on next hand\nsaved_data = payload.get('data') if isinstance(payload, dict) else None\nif saved_data:\n    try:\n        strategy._exp3_learner = strategy.EXP3MetaLearner.from_dict(\n            saved_data if isinstance(saved_data, dict) else json.loads(saved_data))\n    except Exception:\n        strategy._exp3_learner = strategy.EXP3MetaLearner()\n# After action computation:\nreturn int(action), strategy._exp3_learner.to_dict()\n```\nAlso modify main.py's main() to serialize EXP3 state:\n```python\ndef main():\n    payload = json.loads(input())\n    action, exp3_state = decide_action(payload)\n    print(json.dumps({'response': int(action), 'data': exp3_state}))\n```\n\n### Step 6: Modify choose_raise() in strategy.py\nAdd style_params parameter (default None). When style_params is not None, apply sizing tier:\n```python\n# Add to parameters:\ndef choose_raise(..., style_params=None):\n    ...\n    if style_params is not None:\n        tier = value_profile.get('tier', 'none') if value_profile else 'none'\n        style_name = 'gto'\n        for sn, sp in STYLE_PARAMS.items():\n            if sp is style_params:\n                style_name = sn\n                break\n        style_ratio = style_sizing_ratio(round_idx, tier, style_name, wetness)\n        ratio = 0.80 * ratio + 0.20 * style_ratio\n```\n\n### Step 7: Wire up EXP3 reward in main.py\nModify v7's main.py to:\n1. Load EXP3 state from payload['data'] at start of decide_action()\n2. Return (action, exp3_state) from decide_action()\n3. Print {'response': int(action), 'data': exp3_state} in main()\n4. Add reward observation: after each hand, track chip delta and call _exp3_learner.observe_reward()\n\nNOTE: Since we can't observe the reward until the NEXT request (which contains total_win_chips), compute the reward from the PREVIOUS hand's total_win_chips at the start of decide_action():\n```python\n# Compute reward from previous hand if available\nif saved_data and 'prev_style_idx' in (saved_data if isinstance(saved_data, dict) else {}):\n    prev_idx = saved_data.get('prev_style_idx', 0)\n    # Look at total_win_chips delta\n    total_win = payload.get('requests', [{}])[-1].get('total_win_chips', [0, 0])\n    my_id = payload['requests'][-1].get('my_id', 0)\n    my_delta = total_win[my_id] if len(total_win) > my_id else 0\n    reward = 1.0 if my_delta > 0 else (-0.5 if my_delta < 0 else 0.0)\n    strategy._exp3_learner.observe_reward(reward, prev_idx)\n```\nAlso save the current style_idx in the state dict:\n```python\ne3state = strategy._exp3_learner.to_dict()\ne3state['prev_style_idx'] = style_idx\nreturn int(action), e3state\n```\n\n## CRITICAL CONSTRAINTS\n- DO NOT modify postflop.py, card_utils.py, simulation.py, state.py, tournament.py — these are READ-ONLY\n- Preserve ALL of v7's safety guards: big_pot_safety_guard, bad_river_bluff_candidate, weak_bottom_pair_barrel, fragile_river_raise_fold, paired_board_stackoff, anti-lock, etc.\n- Use v7's opponent model priors (vpip=0.58, pfr=0.28), NOT v35's (0.52/0.24)\n- Keep file sizes under 1000 lines each. strategy.py is currently 1158 lines — you may need to extract EXP3MetaLearner into a separate exp3.py module.\n- The main.py output format MUST be: json.dumps({'response': int(action), 'data': exp3_state}) for Botzone compatibility\n- Include .completed sentinel file"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. DO NOT change algorithmic flow. DO NOT add new functions. ONLY modify numeric constants and thresholds in constants.py.\n\n## CONTEXT\nWorker 1 is adding a multi-style EXP3 meta-learning system to the bot. The style parameters in STYLE_PARAMS control how aggressively each style exploits. Your job is to ensure the initial values are well-calibrated.\n\n## WHAT TO TUNE\nRead `/Users/zhouzixiang/Documents/pok/bots/claude_v7/constants.py` first (it will have been modified by Worker 1 to include STYLE_PARAMS).\n\nThe key insight from the experience pool: 'Anti-lock proven values: chase=0.90, threshold=-0.075, sizing=0.18, bluff=0.13.' The EXP3 system's style parameters interact with these proven values.\n\n### STYLE_PARAMS calibration:\n\n1. **exploit_fold**: For opponents who fold too much (>52% fold_to_raise)\n   - 'open_threshold_delta': -0.03 (open wider to steal more)\n   - 'call_threshold_delta': 0.02 (tighter calls since we're bluffing more)\n   - 'bluff_frequency_mult': 1.4 (bluff 40% more often)\n   - 'sizing_tier': 'large' (bigger bets to push folds)\n   - 'threebet_range_width_delta': 0.08 (wider 3bet range)\n   - 'trap_probability': 0.05 (rare trapping)\n\n2. **exploit_call**: For calling stations (high VPIP, low fold_to_raise <38%)\n   - 'open_threshold_delta': 0.01 (slightly tighter — they call anyway)\n   - 'call_threshold_delta': -0.02 (wider calls to see showdowns)\n   - 'bluff_frequency_mult': 0.3 (bluff 70% LESS — they don't fold)\n   - 'sizing_tier': 'polar' (big value bets, no bluffs)\n   - 'threebet_range_width_delta': -0.04 (tighter 3bet — they call)\n   - 'trap_probability': 0.15 (trap with strong hands)\n\n3. **exploit_maniac**: For hyper-aggressive opponents (high aggr >0.45)\n   - 'open_threshold_delta': 0.04 (tighter opens — they'll 3bet)\n   - 'call_threshold_delta': -0.04 (wider calls to catch bluffs)\n   - 'bluff_frequency_mult': 0.2 (almost never bluff — they raise)\n   - 'sizing_tier': 'medium' (standard sizing)\n   - 'threebet_range_width_delta': 0.05 (moderate 3bet with strong hands)\n   - 'trap_probability': 0.30 (frequent trapping — let them bet)\n\n4. **exploit_nit**: For ultra-tight opponents (low VPIP <0.35)\n   - 'open_threshold_delta': -0.06 (open VERY wide — they fold blinds)\n   - 'call_threshold_delta': 0.04 (tighter calls — they have it when they bet)\n   - 'bluff_frequency_mult': 1.6 (bluff 60% more)\n   - 'sizing_tier': 'small' (small bets, they fold to anything)\n   - 'threebet_range_width_delta': 0.15 (wide 3bet range)\n   - 'trap_probability': 0.00 (don't trap — they won't bet)\n\n5. **gto**: Default balanced strategy\n   - All deltas = 0.0, mult = 1.0, sizing_tier = 'standard', trap = 0.0\n\n### SIZING_TABLE calibration:\nThese should match v7's existing sizing logic. The values above are reasonable starting points.\n\n### SIZING_TIER_RATIOS:\nThese offset the base sizing ratio by style tier:\n- 'small': -0.06 (smaller bets)\n- 'medium': -0.02\n- 'standard': 0.00\n- 'large': +0.05\n- 'polar': +0.03\n\n### EXP3 Parameters:\nThe EXP3MetaLearner gamma parameter controls exploration vs exploitation:\n- gamma = 0.10 means 10% exploration (always try all styles)\n- This is good for early learning, can be lowered to 0.07 after convergence\n\n## DO NOT TOUCH\n- Any file other than constants.py\n- Any function definitions\n- HAND_CLASS_SCORE, SIMULATIONS_BY_PUBLIC_COUNT, PREFLOP_STRENGTH_TABLE\n- N_PLAYERS, INITIAL_CHIPS, BIG_BLIND, TOTAL_HANDS, LOCK_WIN_MARGIN\n\n## VERIFICATION\nAfter your changes, the constants.py should still be valid Python with no syntax errors. Run `python -c 'from bots.claude_v7 import constants'` to verify.\n\n## CRITICAL: The values above ARE the target values. Enter them exactly as specified. Do not 'optimize' or 'improve' them — these are based on proven v35 analysis and experience pool lessons. Your job is to ensure they are correctly placed in the right dict entries."
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `web/core/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `web/core/results/head_to_head.json` — **PRIMARY DATA**: Head-to-Head matrix. Use this to compute H2H average win rate per bot (equal-weighted across all opponents). This is the **primary optimization metric**.
- `web/core/results/glicko_ratings.json` — Glicko-2 ratings (r, rd) as secondary reference for statistical context
- `web/core/results/bot_stats.json` — Per-bot stats: wins, losses, games, win_rate (games-weighted, biased by opponent frequency — use H2H average for equal weighting)
- `web/core/results/rating_history.jsonl` — Performance snapshots over time (includes both ratings and win_rates for recent periods)
- `web/core/experience_pool.md` — Accumulated strategic lessons from past generations (**THIS is the active pool, not evolution_workspace/experience_pool.md**)
- `bots/claude_v7/` — Current source bot code
- `web/core/reference_bots/bot1/` … `web/core/reference_bots/bot6/` — 6 strong reference bots. Read whichever is relevant.

**H2H Matrix Usage:** The head_to_head.json file is the most important data for identifying weaknesses. For each bot, check which opponents it loses to (< 40% WR = WEAKNESS) and which it beats (> 60% WR = STRENGTH). Focus improvements on closing weakness gaps.

Use Bash tool with `git log` and `git diff` to understand evolution history.

**When reading `experience_pool.md`, prioritise in this order:**
1. `## RECENT_LESSONS` — what just happened last 1-3 gens
2. `## OPPONENT_MODELING` — opponent model improvements (often highest EV)
3. Any entry with `[POSSIBLY EXHAUSTED]` — **avoid repeating these directions**

# Task
Your goal is to:
1. Read the H2H data and analyze the current bot's per-opponent performance. Compute and track H2H average win rate — this is the **primary metric**.
2. Read the performance verification report (provided below) for objective trend analysis.
3. Read the experience pool to learn from past iterations.
4. Read the current bot's source code and reference bots' code to identify weaknesses.
5. Dynamically assign 1–3 Developer Sub-Agents (Workers) to implement your strategy.
6. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

Prefer one dominant hypothesis per generation. If changing multiple subsystems,
explain why they are inseparable. Otherwise split into separate generations.

Every plan must make attribution possible:
- `targeted_failure`: the single failure pattern this generation targets, with H2H/replay/evidence if available.
- `expected_behavior_change`: what concrete decisions should change at the table.
- `do_not_touch`: files, functions, streets, or subsystems workers must avoid.
- `measurement_plan`: how quality gates, critical scenarios, H2H, or precommit eval should prove the change is not a regression.

# Worker Count Decision
Choose 1–3 workers based on the current bot's total games played:
- **games < 50** (very uncertain): **1 worker only** — conservative change, avoid big structural risk
- **games 50–200** (moderate confidence): **2 workers** — standard plan (Direction A + Direction B)
- **games ≥ 200** (well-evaluated): **up to 3 workers** — can explore bolder, parallel improvements

# Worker Directions (assign to each worker)
- **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation, opponent modeling.
- **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
- **Direction C (Opponent Modeler):** *(Only use as 3rd worker when games ≥ 200)*
  - ALLOWED: Adding/modifying opponent tracking data structures
  - ALLOWED: Per-street statistics (preflop/flop/turn/river aggression separately): `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
  - ALLOWED: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list, use median
  - FORBIDDEN: Changing overall decision flow or non-opponent-model logic

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Performance Verification Report (SATLUTION-style objective analysis)
v36 currently at 51.3% overall win rate (118-112 in 230 games). Critical weaknesses against top opponents: v23 (20%), v1 (10%), v20 (30%), v22 (35%). Only beaten parent v7 0% of the time. The bot needs genuine strategic improvements in postflop play, river decisions, and opponent adaptation — not cosmetic reformatting. The line reduction work is already complete (all files ≤1000 lines). diversity_needed: false — focus on fixing specific leaks against identified opponents.
Use `verified_improvements` to avoid duplicating what already worked.
Use `persistent_weaknesses` to prioritise what to fix next.
**If `diversity_needed: true`, you MUST try a substantially different approach this generation.**

# Diversity Injection (Anti-Local-Optima)
If `performance_verification.diversity_needed: true` OR the same type of change has failed 2+ consecutive generations:
- You MUST try a **substantially different approach** this generation
- Examples of "substantially different":
  - Last 2 gens tuned constants → this gen add new opponent model function (Direction A)
  - Last 2 gens added preflop logic → this gen focus on river/showdown decisions
  - Last 2 gens had Logic Architect fail → this gen try only HyperTuner (conservative, 1 worker)
- Explicitly state in your `analysis` field: `"Diversity injection: trying X instead of Y"`
- This prevents getting trapped in a local optimum where the same direction never improves

# Stagnation Decision
Previous v36 attempt was cosmetic reformat of v7 with no strategic value. Critic rejected (score 4): v36 loses 0-10 to parent v7, 2-8 to v23, 3-27 to v1. Need REAL strategic improvements, not line reduction. Line reduction is already done (postflop.py 995 lines, strategy.py 986 lines). Focus on actual H2H weaknesses. H2H avg WR is the primary metric.

# Recent Match Analysis
v36 has severe H2H weaknesses: 0% WR vs v7 (parent), 20% vs v23, 10% vs v1, 30% vs v20, 35% vs v22. Overall 51.3% win rate. The previous iteration was purely cosmetic — blank line removal and code reformatting with no strategic changes. The single behavioral change (AKs preflop all-in call at 0.55 threshold) has no analytical basis and contradicts the experience pool lesson about recalibrating downstream thresholds.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows.
`tasks` array may contain **1, 2, or 3** items based on your Worker Count Decision above.

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What weakness are you targeting? Are we failing due to bad logic or bad parameters? If diversity injection applies, explain: 'Diversity injection: trying X instead of Y'.",
  "targeted_failure": "One dominant failure pattern this generation targets, with the strongest evidence source.",
  "expected_behavior_change": "Specific table behavior that should change, e.g. call AKs all-in preflop more often but do not widen weak offsuit calls.",
  "do_not_touch": ["List files/functions/subsystems that must remain unchanged for attribution."],
  "measurement_plan": "How to verify this hypothesis: critical scenarios, H2H weak opponent, parent comparison, expected small-sample signal.",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

Notes:
- `branch_from` is OPTIONAL. Only include it to override the default evolution source.
- For 1-worker plans, use only Direction A or only Direction B.
- For 3-worker plans, add a Direction C (Opponent Modeler) task as worker_id 3.
- Avoid v29-like broad mixes such as "preflop rewrite + river rewrite + all-street sizing" unless you explicitly prove those changes are inseparable.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the H2H average win rate trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.
8. **ATTRIBUTION**: Do not mix unrelated preflop, postflop, and sizing rewrites in one generation. The next evaluation must be able to attribute win/loss movement to this plan.

Current evolution: v7 → v36
Bot directory: bots/claude_v7/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl
Head-to-Head data: web/core/results/head_to_head.json
Bot stats: web/core/results/bot_stats.json
Experience pool: web/core/experience_pool.md  ← READ THIS, not evolution_workspace/experience_pool.md

=============================
[CLAUDE OUTPUT]
Now let me read the v36-specific H2H data and the current bot source code. Let me search for v36 entries in the H2H file and read the bot files.
Now let me read the current bot's source code and the parent bot v7's code for comparison:
Now let me read the key reference bot files and look at what v7 and v23 do differently:
Now let me look at the top-performing bots to understand what works:
Let me look at v23's opponent modeling and strategy helpers more closely:
Now I have a complete picture. Let me analyze the data and formulate the plan.

**v36 H2H Analysis (extracted from head_to_head.json):**

| Opponent | v36 WR | Games | Status |
|----------|--------|-------|--------|
| v7 (parent) | **0%** | 10 | CRITICAL |
| v23 | **20%** | 10 | CRITICAL |
| v1 | **10%** | 30 | Bad |
| v8 | **30%** | 10 | Weak |
| v29 | **30%** | 10 | Weak |
| v20 | **30%** | 20 | Weak |
| v22 | **35%** | 20 | Weak |
| v21 | 40% | 10 | Below avg |
| v13 | 40% | 10 | Below avg |
| v16 | 40% | 10 | Below avg |
| v10 | 60% | 10 | Good |
| v2/v9/v12/v19/v27/v28 | 60-70% | 10-20 | Good |
| v6 | **65%** | 20 | Strong |

**H2H avg WR ≈ 45.4%** (vs v7's 56.4% overall WR — a massive gap)

**Key finding:** v36 is **cosmetically identical** to v7 (blank line removals + formatting only). The sole behavioral changes are: (1) `ratio_map` dict in `choose_anti_lock_pressure_action`, and (2) unconditional AKs preflop all-in at strength ≥ 0.55 — which is **too loose** and has no analytical basis.

**v23 (r=1585, WR=53.5%)** has critical features v7/v36 completely lack:
- `classify_opponent_style()` → style-based threshold deltas
- `cbet_rate` / `fold_to_cbet` tracking
- Concept drift detection
- Wider SB open range (0.43 vs 0.49)
- Wider BB iso range (0.50 vs 0.57)
- `bb_vs_raise` / `sb_vs_reraise` response logic

Here is my plan:

```json
{
  "analysis": "v36 is a cosmetic reformat of v7 with zero strategic value — only blank line removal and formatting. The H2H avg WR is ~45.4% (vs v7's 56.4%), with catastrophic weaknesses: 0% vs parent v7, 20% vs v23, 10% vs v1, 30% vs v8/v20/v29, 35% vs v22. The sole behavioral changes (unconditional AKs all-in at 0.55, ratio_map dict) are regressions. Study of v23 (r=1585, 53.5% WR) reveals critical missing features: (1) classify_opponent_style() with style-based threshold deltas — the experience pool confirms this provides +2-3 pts adaptation and v17's -62 collapse was partly due to lacking it; (2) cbet_rate/fold_to_cbet tracking for better postflop reads; (3) concept drift detection; (4) wider preflop opening ranges (v23 SB open=0.43 vs v7's 0.49, BB iso=0.50 vs v7's 0.57 — significantly tighter in v7); (5) bb_vs_raise / sb_vs_reraise response logic (v7 returns None, falling through to simulation-only decisions). These are all Direction A features — genuinely strategic, not cosmetic. Branching from v7 (the strongest bot at 56.4% WR) rather than v36 (which is just a broken copy). Diversity injection: trying v23 feature port instead of yet another reformat/constants tweak.",
  "targeted_failure": "v36's catastrophic H2H weaknesses stem from zero opponent adaptation. Against aggressive opponents (v23 WR=53.5%, r=1585) the bot cannot adjust bluff frequency or calling thresholds. Against the parent v7 (0% WR) it plays identically but with a worse AKs all-in threshold. The opponent model in v7/v36 tracks only aggregate stats — no style classification, no drift detection, no cbet tracking. This means all postflop decisions use the same thresholds regardless of opponent type.",
  "expected_behavior_change": "1) Bot classifies opponents as nit/maniac/station/fold-heavy and propagates style deltas to bluff_freq, call_threshold, fold_threshold — reducing bluffs vs stations, increasing bluffs vs nits. 2) Bot tracks cbet_rate and fold_to_cbet for more accurate postflop reads. 3) Bot detects concept drift (opponent style changes mid-match) and adapts by shifting to recent stats. 4) Wider SB open range (0.43 vs 0.49) and BB iso range (0.50 vs 0.57) to play more hands in position. 5) Proper bb_vs_raise and sb_vs_reraise response logic instead of falling through to simulation. 6) Remove the unconditional AKs all-in at 0.55 threshold — replace with proper conditional premium hand logic.",
  "do_not_touch": ["postflop.py", "simulation.py", "card_utils.py", "state.py", "tournament.py", "main.py", "main_backup.py"],
  "measurement_plan": "Quality gates: (1) py_compile check on all .py files. (2) Decision test pass rate ≥70%. (3) 1 mirror battle smoke test. (4) Post-evaluation: H2H avg WR should improve from ~45.4%. Target: reduce losses to v23 (from 20%), v1 (from 10%), v8 (from 30%), v20 (from 30%). Parent v7 comparison: must not lose 0-10 again. Key critical scenarios: SB open with medium-strength hands (0.43-0.49 range), BB vs raise with premium hands, facing aggression from aggressive opponents (style=maniac should trigger wider calling).",
  "branch_from": "claude_v7",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["opponent.py", "strategy.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your goal is to add opponent style classification and adaptive strategy to bots/claude_v36/. You must modify ONLY opponent.py and strategy.py. Do NOT touch constants.py, postflop.py, simulation.py, card_utils.py, state.py, tournament.py, or main.py.\n\n## Context\nThe current bot (v36/v7) has a basic opponent model in opponent.py that tracks aggregate stats (vpip, pfr, fold_to_raise, etc.) but does NOT classify opponent style or adapt thresholds based on opponent type. Reference bot v23 (at bots/claude_v23/) has a classify_opponent_style() function that provides +2-3 percentage points of adaptation. The experience pool confirms this is critical — v17's -62 rating collapse was partly due to lacking style classification.\n\n## Task 1: Add classify_opponent_style() to opponent.py\n\nAdd this function to bots/claude_v36/opponent.py (after build_opponent_model):\n\n```python\ndef classify_opponent_style(opponent_model):\n    \"\"\"Classify opponent as nit/maniac/station/fold-heavy and return threshold deltas.\"\"\"\n    deltas = {\n        'style': 'unknown',\n        'bluff_freq_bonus': 0.0,\n        'call_threshold_delta': 0.0,\n        'fold_threshold_delta': 0.0,\n    }\n    confidence = opponent_model.get('confidence', 0.0)\n    if confidence < 0.15:\n        return deltas\n\n    vpip = opponent_model.get('vpip', 0.58)\n    pfr = opponent_model.get('pfr', 0.28)\n    postflop_aggr = opponent_model.get('postflop_aggr', 0.36)\n    fold_to_raise = opponent_model.get('fold_to_raise', 0.44)\n\n    # Nit: low vpip, low pfr\n    if vpip < 0.40 and pfr < 0.20:\n        deltas['style'] = 'nit'\n        deltas['bluff_freq_bonus'] = 0.04\n        deltas['fold_threshold_delta'] = 0.02\n        deltas['call_threshold_delta'] = 0.015\n    # Maniac: high aggression, high vpip\n    elif vpip > 0.70 and postflop_aggr > 0.45:\n        deltas['style'] = 'maniac'\n        deltas['bluff_freq_bonus'] = -0.03\n        deltas['call_threshold_delta'] = -0.02\n        deltas['fold_threshold_delta'] = -0.02\n    # Station: high vpip, low fold_to_raise\n    elif vpip > 0.62 and fold_to_raise < 0.35:\n        deltas['style'] = 'station'\n        deltas['bluff_freq_bonus'] = -0.04\n        deltas['call_threshold_delta'] = -0.01\n        deltas['fold_threshold_delta'] = -0.01\n    # Fold-heavy: high fold_to_raise\n    elif fold_to_raise > 0.55:\n        deltas['style'] = 'fold_heavy'\n        deltas['bluff_freq_bonus'] = 0.05\n        deltas['call_threshold_delta'] = 0.02\n        deltas['fold_threshold_delta'] = 0.02\n\n    # Scale by confidence\n    for key in ('bluff_freq_bonus', 'call_threshold_delta', 'fold_threshold_delta'):\n        deltas[key] *= confidence\n\n    return deltas\n```\n\nAlso enhance build_opponent_model() to track cbet_rate and fold_to_cbet:\n- Add accumulators: `cbet_opportunities = 0`, `cbet_count = 0`, `fold_to_cbet_opportunities = 0`, `fold_to_cbet_count = 0`\n- When the first action on flop (round_idx == 1) is by opponent who raised preflop, increment cbet_opportunities; if they raise/allin, increment cbet_count\n- When we face a cbet (facing_cbet flag), increment fold_to_cbet_opportunities; if we fold, increment fold_to_cbet_count\n- Add to the result dict: `cbet_rate: smooth_rate(cbet_count, cbet_opportunities, 0.55, 4.0)`, `fold_to_cbet: smooth_rate(fold_to_cbet_count, fold_to_cbet_opportunities, 0.40, 3.0)`\n- Add `drift_detected: False` to the result\n- After the hand loop, add drift detection: if ≥12 hands played, compare recent 10-hand vpip/pfr to all-time; if drift > 15% vpip or > 12% pfr, set drift_detected=True and override stats with recent values\n\n## Task 2: Propagate style deltas in strategy.py\n\nIn bots/claude_v36/strategy.py:\n\n1. Add import: `from opponent import build_opponent_model, analyze_current_spot, classify_opponent_style, detect_bot4_profile, get_anti_bot4_adjustments` (add classify_opponent_style to the existing import)\n\n2. After `opponent_model = build_opponent_model(requests, my_id)` in get_action(), add:\n   `style_deltas = classify_opponent_style(opponent_model)`\n\n3. In the `to_call > 0` section (around line 585), after computing `call_margin` for postflop:\n   - Subtract style_deltas['call_threshold_delta'] from the effective threshold (this makes calling wider vs maniacs, tighter vs nits)\n   - For bluff decisions: subtract style_deltas['bluff_freq_bonus'] from bluff thresholds (more bluffs vs nits/fold-heavy, fewer vs stations/maniacs)\n\n4. For the raise/fold decision thresholds (strong/medium computation, around line 483):\n   - `strong += style_deltas['fold_threshold_delta']` (fold more vs tight opponents)\n   - `medium += style_deltas['fold_threshold_delta'] * 0.8`\n\n5. For blocker bluff and semi-bluff decisions, incorporate style_deltas['bluff_freq_bonus'] into the allow_low_frequency_blocker_bluff and semi_bluff threshold calculations.\n\n6. CRITICAL: Remove the unconditional AKs all-in at line 286-287 (`if state['opponent_allin'] and preflop_strength >= 0.55: return -2`). Replace with conditional logic that only calls all-in with premium hands (preflop_strength >= 0.72 for all-in calls, or >= 0.65 when in bb_vs_raise/sb_vs_reraise spots specifically).\n\n## Safety rules\n- Do NOT modify any function signatures\n- Do NOT change constants, thresholds, or magic numbers EXCEPT the AKs fix above\n- Do NOT add new files\n- Do NOT touch postflop.py, simulation.py, constants.py, card_utils.py, state.py, tournament.py\n- Keep all existing safety guards and anti-lock logic intact\n- The style_deltas should ADJUST existing thresholds, not replace them"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["strategy.py", "constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. You may ONLY modify numeric constants and thresholds. Do NOT add new functions, classes, or change control flow. You must modify ONLY strategy.py and constants.py. Do NOT touch opponent.py, postflop.py, simulation.py, card_utils.py, state.py, tournament.py, or main.py.\n\n## Context\nReference bot v23 (r=1585, 53.5% WR) uses wider preflop opening ranges than v7/v36. The current bot's preflop ranges are too tight, causing it to miss profitable situations. Additionally, v36 introduced an unconditional AKs all-in threshold at preflop_strength >= 0.55 that is too loose. The postflop EQR values also need recalibration — experience pool says v9's LOWER EQR values outperformed (air: 0.65/0.53 vs v36's 0.72/0.62). Higher EQR over-realizes weak hands → calls too light.\n\n## Task 1: Widen preflop opening ranges in strategy.py choose_preflop_spot_action()\n\nModify these EXACT values in bots/claude_v36/strategy.py:\n\nIn choose_preflop_spot_action(), for the sb_open spot:\n- Change open_threshold from `0.49 + match_adjust + 0.02 + match_profile['open_delta']` to `0.44 + match_adjust + 0.02 + match_profile['open_delta']` (lower by 0.05, matching v23's 0.43)\n- Change limp_threshold from `0.36 + match_adjust` to `0.29 + match_adjust` (lower by 0.07, matching v23's 0.27)\n\nFor the bb_vs_limp spot:\n- Change iso_threshold base from `0.57 + match_adjust` to `0.52 + match_adjust` (lower by 0.05, closer to v23's 0.50)\n\n## Task 2: Fix AKs all-in threshold\n\nIn choose_preflop_spot_action(), remove or tighten the line:\n`if state['opponent_allin'] and preflop_strength >= 0.55: return -2`\n\nReplace with: only call all-in with genuinely premium hands:\n`if state['opponent_allin'] and preflop_strength >= 0.70: return -2`\n\n(0.70 corresponds to QQ+ in the preflop strength table, which is the correct threshold for calling all-in preflop)\n\n## Task 3: Recalibrate postflop EQR values in strategy.py realized_postflop_equity()\n\nThe current EQR values are too high. Apply these changes:\n- Air hand IP EQR: change from 0.72 to 0.65\n- Air hand OOP EQR: change from 0.62 to 0.53\n- Middle/bottom pair IP EQR: change from 0.86 to 0.82\n- Middle/bottom pair OOP EQR: change from 0.78 to 0.73\n- Top pair weak kicker IP EQR: change from 0.92 to 0.90\n- Top pair weak kicker OOP EQR: change from 0.86 to 0.84\n- Add pot > 3000 penalty: after computing eqr for air_hands and weak pairs, if pot > 3000 (or spot_info includes pot context — check if available), subtract 0.03\n\nIMPORTANT: These are EXACT numeric substitutions. Do not change any function logic, control flow, or add new code blocks.\n\n## Task 4: Add big pot penalty to EQR\n\nIn the realized_postflop_equity() function, for the air_hand branch AND the middle/bottom/underpair branch, add after the existing eqr adjustments:\n```python\n# Note: use a heuristic — if spot_info['last_raise_pot_ratio'] * 2 > 0.5, treat as big pot\nif spot_info.get('last_raise_pot_ratio', 0) > 0.35:\n    eqr -= 0.03\n```\nThis applies the 'pot>3000 penalty' from the experience pool without needing the actual pot value.\n\n## Safety rules\n- ONLY change numeric values. Do not add, remove, or restructure code.\n- Do NOT add new functions or classes.\n- Do NOT change function signatures.\n- Do NOT modify import statements.\n- Do NOT touch opponent.py, postflop.py, simulation.py, card_utils.py, state.py, tournament.py\n- Each change must be a simple value substitution (e.g., 0.49 → 0.44)"
    }
  ]
}
```

This plan addresses the core weaknesses systematically:

1. **Worker A (Algorithmic Logic Architect)** targets the #1 gap: **zero opponent adaptation**. v36 plays the same strategy against every opponent. Adding `classify_opponent_style()` + style delta propagation means the bot will:
   - Bluff more against nits (fold_to_raise > 0.55) 
   - Call wider against maniacs (vpip > 0.70)
   - Bluff less against stations (vpip > 0.62, fold_to_raise < 0.35)
   - Also removes the flawed unconditional AKs all-in at 0.55

2. **Worker B (Hyperparameter Tuner)** targets the #2 gap: **preflop ranges too tight + EQR too high**. v23 opens SB at 0.43 while v7 opens at 0.49 — a massive gap. Lower EQR (per experience pool lesson: "v9's LOWER values outperform") prevents over-realizing weak hands.

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `web/core/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `web/core/results/head_to_head.json` — **PRIMARY DATA**: Head-to-Head matrix. Use this to compute H2H average win rate per bot (equal-weighted across all opponents). This is the **primary optimization metric**.
- `web/core/results/glicko_ratings.json` — Glicko-2 ratings (r, rd) as secondary reference for statistical context
- `web/core/results/bot_stats.json` — Per-bot stats: wins, losses, games, win_rate (games-weighted, biased by opponent frequency — use H2H average for equal weighting)
- `web/core/results/rating_history.jsonl` — Performance snapshots over time (includes both ratings and win_rates for recent periods)
- `web/core/experience_pool.md` — Accumulated strategic lessons from past generations (**THIS is the active pool, not evolution_workspace/experience_pool.md**)
- `bots/claude_v7/` — Current source bot code
- `web/core/reference_bots/bot1/` … `web/core/reference_bots/bot6/` — 6 strong reference bots. Read whichever is relevant.

**H2H Matrix Usage:** The head_to_head.json file is the most important data for identifying weaknesses. For each bot, check which opponents it loses to (< 40% WR = WEAKNESS) and which it beats (> 60% WR = STRENGTH). Focus improvements on closing weakness gaps.

Use Bash tool with `git log` and `git diff` to understand evolution history.

**When reading `experience_pool.md`, prioritise in this order:**
1. `## RECENT_LESSONS` — what just happened last 1-3 gens
2. `## OPPONENT_MODELING` — opponent model improvements (often highest EV)
3. Any entry with `[POSSIBLY EXHAUSTED]` — **avoid repeating these directions**

# Task
Your goal is to:
1. Read the H2H data and analyze the current bot's per-opponent performance. Compute and track H2H average win rate — this is the **primary metric**.
2. Read the performance verification report (provided below) for objective trend analysis.
3. Read the experience pool to learn from past iterations.
4. Read the current bot's source code and reference bots' code to identify weaknesses.
5. Dynamically assign 1–3 Developer Sub-Agents (Workers) to implement your strategy.
6. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

Prefer one dominant hypothesis per generation. If changing multiple subsystems,
explain why they are inseparable. Otherwise split into separate generations.

Every plan must make attribution possible:
- `targeted_failure`: the single failure pattern this generation targets, with H2H/replay/evidence if available.
- `expected_behavior_change`: what concrete decisions should change at the table.
- `do_not_touch`: files, functions, streets, or subsystems workers must avoid.
- `measurement_plan`: how quality gates, critical scenarios, H2H, or precommit eval should prove the change is not a regression.

# Worker Count Decision
Choose 1–3 workers based on the current bot's total games played:
- **games < 50** (very uncertain): **1 worker only** — conservative change, avoid big structural risk
- **games 50–200** (moderate confidence): **2 workers** — standard plan (Direction A + Direction B)
- **games ≥ 200** (well-evaluated): **up to 3 workers** — can explore bolder, parallel improvements

# Worker Directions (assign to each worker)
- **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation, opponent modeling.
- **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
- **Direction C (Opponent Modeler):** *(Only use as 3rd worker when games ≥ 200)*
  - ALLOWED: Adding/modifying opponent tracking data structures
  - ALLOWED: Per-street statistics (preflop/flop/turn/river aggression separately): `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
  - ALLOWED: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list, use median
  - FORBIDDEN: Changing overall decision flow or non-opponent-model logic

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Performance Verification Report (SATLUTION-style objective analysis)
{"trend": "stagnant", "verified_improvements": ["v7 holds #1 H2H avg win rate (57.47%) with stable Glicko rating (r=1661, rd=61)", "Dominates weak opponents decisively (v1: 97%, v4: 62%)", "Consistent 52-60% win rates against middle tier (v9-v19)"], "persistent_weaknesses": ["Loses to claude_v6 (47%) — likely overfitted to exploit weaker bots at cost of robustness against well-tuned opponents", "Dead even vs claude_v20 (50%) and barely positive vs v22 (51%) — no edge against newer evolved bots", "top_h2h_wr flat at ~57.4% for 10 straight periods — plateau reached with current approach"], "diversity_needed": true, "diversity_reason": "Performance stagnant for 10+ periods. v7 weaknesses cluster against newer evolved bots (v6, v20, v22) suggesting iterative incremental tuning cannot break through — structural paradigm shift needed.", "suggestion": "Branch from v6 instead of v7: v7 loses to v6 (47%), meaning v6 core approach is strategically sounder in peer matchups. Build on v6 base but harden against v7-style exploitation."}
Use `verified_improvements` to avoid duplicating what already worked.
Use `persistent_weaknesses` to prioritise what to fix next.
**If `diversity_needed: true`, you MUST try a substantially different approach this generation.**

# Diversity Injection (Anti-Local-Optima)
If `performance_verification.diversity_needed: true` OR the same type of change has failed 2+ consecutive generations:
- You MUST try a **substantially different approach** this generation
- Examples of "substantially different":
  - Last 2 gens tuned constants → this gen add new opponent model function (Direction A)
  - Last 2 gens added preflop logic → this gen focus on river/showdown decisions
  - Last 2 gens had Logic Architect fail → this gen try only HyperTuner (conservative, 1 worker)
- Explicitly state in your `analysis` field: `"Diversity injection: trying X instead of Y"`
- This prevents getting trapped in a local optimum where the same direction never improves

# Stagnation Decision
PERFORMANCE STAGNATION CONFIRMED. v7's top h2h_avg_wr has been flat at ~57.4% for 10+ evaluation periods (0.07pp total movement). diversity_needed: true. The performance verification suggests branching from v6 instead of v7, since v6 beats v7 head-to-head at 53%. However, v7 still has the best overall H2H avg WR across all opponents. Consider: (1) Continue from v7 but add postflop fold mechanism — the 0% postflop fold rate is the primary weakness causing -17000 to -20000 chip losses. (2) Branch from v6 and add anti-v7 exploitation hardening. The key structural change needed is a fold mechanism postflop — the bot NEVER folds on flop/turn/river, making it a calling station that pays off every value bet.

# Recent Match Analysis
{"weaknesses": ["Extremely passive preflop: all bots fold 61-69% preflop while raising only 16-24%, surrendering too many blinds without contest", "Catastrophic big-loss exposure: every match features 3-5 games with chip losses exceeding -17000, indicating the bot commits too many chips on weak-to-marginal hands and fails to fold early when dominated", "Near-zero postflop fold rate (0% on flop/turn/river across all matches) — the bot cannot disengage from pots it is losing, leading to huge river payoffs when behind"], "street_weaknesses": {"preflop": "Fold rate 61-69% reasonable but only 16-24% raises and 0.6-0.7x pot average raise size — under-aggro preflop, limps/calls too wide, raises too small", "flop": "0% fold rate — NEVER folds to a flop bet. 60-66% call rate is a calling station. 32-39% raise rate decent but 0.5x pot sizing too small to protect or deny equity", "turn": "0% fold rate continues. 25-36% raise with 0.4-0.5x pot sizing rarely applies real pressure", "river": "0% fold rate is fatal. 69-88% call rate pays off every value bet. Only 5-22% raise rate with 0.4-0.6x pot sizing fails to extract maximum value"}, "recommendation": "Implement postflop fold mechanism — fold on flop/turn/river when facing aggression with weak made hands (below top pair weak kicker) or whiffed draws. Increase average raise sizing from 0.5x pot to 0.75-1.0x pot on flop and turn to deny equity and extract value. On river, fold weak showdown hands (below mid-pair) to large bets instead of calling 88% of the time."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows.
`tasks` array may contain **1, 2, or 3** items based on your Worker Count Decision above.

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What weakness are you targeting? Are we failing due to bad logic or bad parameters? If diversity injection applies, explain: 'Diversity injection: trying X instead of Y'.",
  "targeted_failure": "One dominant failure pattern this generation targets, with the strongest evidence source.",
  "expected_behavior_change": "Specific table behavior that should change, e.g. call AKs all-in preflop more often but do not widen weak offsuit calls.",
  "do_not_touch": ["List files/functions/subsystems that must remain unchanged for attribution."],
  "measurement_plan": "How to verify this hypothesis: critical scenarios, H2H weak opponent, parent comparison, expected small-sample signal.",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

Notes:
- `branch_from` is OPTIONAL. Only include it to override the default evolution source.
- For 1-worker plans, use only Direction A or only Direction B.
- For 3-worker plans, add a Direction C (Opponent Modeler) task as worker_id 3.
- Avoid v29-like broad mixes such as "preflop rewrite + river rewrite + all-street sizing" unless you explicitly prove those changes are inseparable.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the H2H average win rate trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.
8. **ATTRIBUTION**: Do not mix unrelated preflop, postflop, and sizing rewrites in one generation. The next evaluation must be able to attribute win/loss movement to this plan.

Current evolution: v7 → v36
Bot directory: bots/claude_v7/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl
Head-to-Head data: web/core/results/head_to_head.json
Bot stats: web/core/results/bot_stats.json
Experience pool: web/core/experience_pool.md  ← READ THIS, not evolution_workspace/experience_pool.md

=============================
[CLAUDE OUTPUT]
Now let me read the remaining H2H data and the current bot source code:
Now let me read the current v7 bot source code and the v6 bot for comparison:
Now let me read the remaining v7 files and look at v6 for comparison:
Let me compute v7's precise H2H average and check a reference bot for fold logic:
Now I have a complete picture. Let me analyze the data and formulate the evolution plan.

**Key findings:**
- v7 H2H avg: **57.58%** (best in pool, 30 opponents)
- v6 beats v7 head-to-head (52.9%) but v6's overall H2H avg is only 48.89% (much worse)
- v7's weaknesses cluster around: v6 (47.1%), v20 (50.0%), v22 (52.5%)
- Match analysis confirms: **0% postflop fold rate on flop/turn/river**, 69-88% river call rate
- 3-5 catastrophic -17000 to -20000 chip losses per match
- All 30 bots in the pool (including reference bots) lack postflop folding — adding it is a genuine paradigm shift

```json
{
  "analysis": "Diversity injection: trying STRUCTURAL PARADIGM SHIFT (postflop fold mechanism) instead of incremental parameter tuning. v7 has been stagnant at ~57.5% H2H avg for 10+ evaluation periods. Match analysis reveals the root cause: 0% postflop fold rate on flop/turn/river. The bot NEVER folds postflop — it calls 69-88% on river, pays off every value bet, and suffers 3-5 catastrophic -17000 to -20000 chip losses per match. I studied reference bot5 and evolved bots v27/v28 — NONE have a postflop fold mechanism. The existing `postflop_call_margin()` function paradoxically ADDS margin for weak hands (0.012 for weak_showdown, 0.018 for air), which barely increases the fold threshold. More critically, the `realized_postflop_equity()` function doesn't discount weak hands enough (EQR 0.86 for position pair with weak kicker), and the default return in the to_call>0 branch is `return 0` (call), so without explicit fold conditions the bot always calls. Continuing from v7 (not branching from v6) because v7 has the best overall H2H avg (57.58% vs v6's 48.89%) and the fold mechanism is an additive structural change that doesn't require a different codebase foundation.",
  "targeted_failure": "0% postflop fold rate: the bot NEVER folds on flop/turn/river, acting as a calling station that pays off every value bet. This causes 3-5 catastrophic -17000 to -20000 chip losses per match. Evidence: match analysis shows 0% fold rate on all postflop streets, 69-88% river call rate, and H2H losses cluster against well-tuned opponents (v6: 47%, v20: 50%) who exploit this calling tendency.",
  "expected_behavior_change": "On flop/turn/river, the bot should FOLD weak made hands (bottom pair, underpair, middle pair weak kicker) when facing medium-to-large bets from aggressive opponents, especially on later streets. Expected fold rate increase from 0% to ~15-25% on postflop streets. River call rate should drop from 69-88% to ~50-60%. This should reduce catastrophic chip losses from -17000+ to more manageable levels. Against v6 specifically, the bot should lose fewer chips in dominated spots, potentially moving the matchup from 47% toward 50%+.",
  "do_not_touch": [
    "constants.py game constants (N_PLAYERS, INITIAL_CHIPS, etc.)",
    "tournament.py (anti-lock, match risk — these work correctly)",
    "opponent.py (opponent modeling — priors are correct at 0.58/0.28)",
    "simulation.py (Monte Carlo estimation — unchanged)",
    "card_utils.py (card evaluation — unchanged)",
    "state.py (state reconstruction — unchanged)",
    "main.py (entry point and sanitize_action — unchanged)",
    "strategy.py preflop logic (choose_preflop_spot_action, lines 396-449)",
    "strategy.py choose_raise function signature and sizing logic structure",
    "strategy.py anti-lock pressure logic (choose_anti_lock_pressure_action)",
    "All existing safety guards (fragile_river_raise_fold, hard_repressure_fold, paired_board_stackoff, bad_river_bluff_candidate, etc.)"
  ],
  "measurement_plan": "1) Quality gates: py_compile check must pass. Decision tests ≥70% pass rate. 2) Smoke test: mirror battle must not crash. 3) Key behavior check: in smoke test, verify the bot actually returns -1 (fold) in at least some postflop scenarios — the whole point is going from 0% to >0% fold rate. 4) H2H vs v6: the matchup should improve from 47% (currently losing) — even 49% would be a positive signal since v6 is the toughest opponent. 5) H2H vs weak opponents (v1, v4): should not regress — v7 currently dominates these and folding more should not hurt. 6) Overall H2H avg: should remain ≥56% (current 57.58%) — some regression is acceptable if v6/v20/v22 matchups improve.",
  "branch_from": null,
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["postflop.py", "strategy.py"],
      "difficulty": "hard",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to add a POSTFLOP FOLD MECHANISM to the Texas Hold'em bot. This is the single most impactful structural change needed — the bot currently has a 0% postflop fold rate and pays off every value bet, causing catastrophic -17000+ chip losses.\n\n## CONTEXT\nRead the current bot code in bots/claude_v7/ first. Focus on strategy.py and postflop.py.\n\nThe bot's decision flow in get_action() (strategy.py) for postflop spots with to_call > 0:\n1. Computes call_margin via postflop_call_margin()\n2. Computes realized_rate via realized_postflop_equity()\n3. Folds only if: realized_rate < pot_odds + call_margin AND not anti_lock_call_continue AND not strong_made_continue\n4. Otherwise defaults to return 0 (call)\n\nThe problem: call_margin is tiny (0.01-0.08 range) and the realized equity for weak hands is often still above pot_odds + call_margin, so the bot never folds postflop.\n\n## TASK 1: Add `should_fold_postflop()` function in postflop.py\n\nAdd a new function with this signature:\n```python\ndef should_fold_postflop(\n    value_profile, made_strength, draw_strength, draw_info,\n    pair_profile, board_texture, spot_info, opponent_model,\n    round_idx, pot, to_call, win_rate, nutted_risk\n):\n```\n\nThis function returns True when the bot should fold postflop. Logic:\n\n### Tier-based folding (highest priority):\n- If value_profile tier is 'none' (no made hand) AND draw_strength < 0.12 AND round_idx >= 2 AND facing aggression: FOLD if bet_size >= 0.5x pot\n- If value_profile tier is 'thin' AND round_idx == 3 (river) AND made_strength < 0.35 AND facing medium/large bet: FOLD\n- If pair_profile pair_type is 'bottom_pair' or 'underpair' or 'board_pair' AND round_idx >= 2 AND bet_size_bucket is 'large' AND draw_strength < 0.12: FOLD\n\n### Equity-based folding:\n- If win_rate < 0.28 AND round_idx >= 2 AND facing postflop aggression AND bet_size_bucket in ('medium', 'large'): FOLD\n- If realized equity (approximated as win_rate * EQR for weak hands) is less than pot_odds - 0.05 (i.e., even with a small positive margin, the hand is clearly behind): FOLD\n\n### Aggression-pattern folding:\n- If opp_postflop_bet_count >= 2 (multi-barrel) AND made_strength < 0.30 AND round_idx >= 2 AND draw_strength < 0.12: FOLD\n- If opp_current_round_bet_count >= 2 (same street re-raise) AND value_profile tier is not 'strong' or 'nut' AND bet_size_bucket in ('medium', 'large'): FOLD\n\n### Safety exceptions (do NOT fold):\n- NEVER fold if value_profile tier is 'nut'\n- NEVER fold if draw_info has combo_draw or nut_flush_draw\n- NEVER fold if made_strength >= 0.55 (strong made hand)\n- NEVER fold if anti_lock_pressure would apply (handled separately)\n- NEVER fold if opponent_model confidence < 0.10 (too little data)\n\n## TASK 2: Integrate should_fold_postflop() into strategy.py get_action()\n\nIn the `to_call > 0` branch of get_action() (around line 745), AFTER computing all the profiles but BEFORE the existing fold conditions, add:\n\n```python\n# New: postflop fold mechanism\nif round_idx > 0 and not anti_lock_pressure:\n    fold_decision = should_fold_postflop(\n        value_profile, made_strength, draw_strength, draw_info,\n        pair_profile, board_texture, spot_info, opponent_model,\n        round_idx, pot, to_call, win_rate, nutted_risk\n    )\nif fold_decision:\n        return -1\n```\n\nPlace this AFTER the line `call_margin += 0.50 * nutted_risk['risk']` (around line 781) but BEFORE the `if anti_lock_pressure:` check (around line 795). This ensures all profiles are computed before the fold decision.\n\n## TASK 3: Fix postflop_call_margin() for weak hands\n\nIn the `postflop_call_margin()` function in strategy.py, the current logic ADDS margin for weak hands, making them call MORE. This is backwards. Change:\n\n```python\n# BEFORE (wrong — makes weak hands call more):\nif weak_showdown:\n    margin += 0.012\nif air_hand:\n    margin += 0.018\n\n# AFTER (correct — makes weak hands fold more):\nif weak_showdown:\n    margin += 0.035\nif air_hand:\n    margin += 0.055\n```\n\nAlso increase the river-specific margins:\n```python\n# BEFORE:\nif round_idx == 3 and made_strength < 0.40 and not (...):\n    call_margin += 0.04\n\n# AFTER:\nif round_idx == 3 and made_strength < 0.40 and not (...):\n    call_margin += 0.065\n```\n\nAnd increase the postflop aggression margins:\n```python\n# BEFORE:\nif spot_info['facing_postflop_aggression']:\n    margin += 0.008\n\n# AFTER:\nif spot_info['facing_postflop_aggression']:\n    margin += 0.015\n```\n\n## TASK 4: Increase postflop raise sizing\n\nIn the `choose_raise()` function, increase the base ratios for postflop streets to make the bot's aggression more credible and compensate for folding more:\n\n```python\n# BEFORE:\nelif round_idx == 1:\n    ratio = 0.60\nelif round_idx == 2:\n    ratio = 0.70\nelse:\n    ratio = 0.85\n\n# AFTER:\nelif round_idx == 1:\n    ratio = 0.68\nelif round_idx == 2:\n    ratio = 0.78\nelse:\n    ratio = 0.92\n```\n\n## CRITICAL RULES\n1. Do NOT touch any other files besides postflop.py and strategy.py\n2. Do NOT modify the preflop logic (choose_preflop_spot_action)\n3. Do NOT modify anti-lock pressure logic\n4. Do NOT change the opponent model or simulation\n5. The should_fold_postflop function must be conservative — it should fold CLEARLY dominated spots, not marginal ones. When in doubt, call (the existing behavior). A fold rate of 15-25% postflop is the target, not 50%+.\n6. Import should_fold_postflop in strategy.py's imports from postflop\n7. Keep total line count under 1000 per file\n8. Verify the code compiles with py_compile before finishing"
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `web/core/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `web/core/results/head_to_head.json` — **PRIMARY DATA**: Head-to-Head matrix. Use this to compute H2H average win rate per bot (equal-weighted across all opponents). This is the **primary optimization metric**.
- `web/core/results/glicko_ratings.json` — Glicko-2 ratings (r, rd) as secondary reference for statistical context
- `web/core/results/bot_stats.json` — Per-bot stats: wins, losses, games, win_rate (games-weighted, biased by opponent frequency — use H2H average for equal weighting)
- `web/core/results/rating_history.jsonl` — Performance snapshots over time (includes both ratings and win_rates for recent periods)
- `web/core/experience_pool.md` — Accumulated strategic lessons from past generations (**THIS is the active pool, not evolution_workspace/experience_pool.md**)
- `bots/claude_v7/` — Current source bot code
- `web/core/reference_bots/bot1/` … `web/core/reference_bots/bot6/` — 6 strong reference bots. Read whichever is relevant.

**H2H Matrix Usage:** The head_to_head.json file is the most important data for identifying weaknesses. For each bot, check which opponents it loses to (< 40% WR = WEAKNESS) and which it beats (> 60% WR = STRENGTH). Focus improvements on closing weakness gaps.

Use Bash tool with `git log` and `git diff` to understand evolution history.

**When reading `experience_pool.md`, prioritise in this order:**
1. `## RECENT_LESSONS` — what just happened last 1-3 gens
2. `## OPPONENT_MODELING` — opponent model improvements (often highest EV)
3. Any entry with `[POSSIBLY EXHAUSTED]` — **avoid repeating these directions**

# Task
Your goal is to:
1. Read the H2H data and analyze the current bot's per-opponent performance. Compute and track H2H average win rate — this is the **primary metric**.
2. Read the performance verification report (provided below) for objective trend analysis.
3. Read the experience pool to learn from past iterations.
4. Read the current bot's source code and reference bots' code to identify weaknesses.
5. Dynamically assign 1–3 Developer Sub-Agents (Workers) to implement your strategy.
6. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

Prefer one dominant hypothesis per generation. If changing multiple subsystems,
explain why they are inseparable. Otherwise split into separate generations.

Every plan must make attribution possible:
- `targeted_failure`: the single failure pattern this generation targets, with H2H/replay/evidence if available.
- `expected_behavior_change`: what concrete decisions should change at the table.
- `do_not_touch`: files, functions, streets, or subsystems workers must avoid.
- `measurement_plan`: how quality gates, critical scenarios, H2H, or precommit eval should prove the change is not a regression.

# Worker Count Decision
Choose 1–3 workers based on the current bot's total games played:
- **games < 50** (very uncertain): **1 worker only** — conservative change, avoid big structural risk
- **games 50–200** (moderate confidence): **2 workers** — standard plan (Direction A + Direction B)
- **games ≥ 200** (well-evaluated): **up to 3 workers** — can explore bolder, parallel improvements

# Worker Directions (assign to each worker)
- **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation, opponent modeling.
- **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
- **Direction C (Opponent Modeler):** *(Only use as 3rd worker when games ≥ 200)*
  - ALLOWED: Adding/modifying opponent tracking data structures
  - ALLOWED: Per-street statistics (preflop/flop/turn/river aggression separately): `opp_stats[street]['vpip']`, `opp_stats[street]['aggression_factor']`, `opp_stats[street]['fold_to_cbet']`
  - ALLOWED: Bet sizing pattern detection: `opp_bet_sizes[street]` as rolling list, use median
  - FORBIDDEN: Changing overall decision flow or non-opponent-model logic

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Performance Verification Report (SATLUTION-style objective analysis)
{"trend": "stagnant", "verified_improvements": ["Dominant vs early-gen bots (v1 97%, v4 62%) confirms foundational strategy is sound", "Solid >55% win rate against most mid-tier opponents"], "persistent_weaknesses": ["Losing matchup vs claude_v6 (47%) — predecessor exploits v7's patterns", "Break-even vs claude_v20 (50%) and claude_v22 (51%) — newer gens have caught up", "Barely above water vs claude_v8 (51%)", "No dominant edge against any recent-gen bot"], "diversity_needed": true, "suggestion": "Focus on closing the v6 weakness — study v6's play patterns and introduce counter-strategy. Experiment with preflop opening ranges against aggressive opponents since edge vs newer bots is dangerously thin."}
Use `verified_improvements` to avoid duplicating what already worked.
Use `persistent_weaknesses` to prioritise what to fix next.
**If `diversity_needed: true`, you MUST try a substantially different approach this generation.**

# Diversity Injection (Anti-Local-Optima)
If `performance_verification.diversity_needed: true` OR the same type of change has failed 2+ consecutive generations:
- You MUST try a **substantially different approach** this generation
- Examples of "substantially different":
  - Last 2 gens tuned constants → this gen add new opponent model function (Direction A)
  - Last 2 gens added preflop logic → this gen focus on river/showdown decisions
  - Last 2 gens had Logic Architect fail → this gen try only HyperTuner (conservative, 1 worker)
- Explicitly state in your `analysis` field: `"Diversity injection: trying X instead of Y"`
- This prevents getting trapped in a local optimum where the same direction never improves

# Stagnation Decision
STAGNATION CONFIRMED: v7's top H2H avg WR has plateaued at 0.5740 for 5+ consecutive evaluation periods with zero movement. Performance verification flags diversity_needed=true. Iterative tuning appears exhausted — need structural strategic changes. v7 loses to v6 (47%) and has thin edges vs recent gens (v20 50%, v22 51%, v8 51%). Only dominant edge is vs early-gen bots.

# Recent Match Analysis
{"weaknesses": ["Extremely tight preflop in heads-up: 61-69% fold rate bleeds blinds relentlessly. In HU NLHE folding more than ~30% preflop is a major leak — the bot surrenders its small blind ~2/3 of the time without a fight.", "Zero postflop folding (0% on flop/turn/river across all matches): once the bot sees a flop it never folds, making it a pure calling station that opponents can value-bet into with impunity.", "Chronic underbetting on every street (0.4-0.7x pot avg raise): the bot builds tiny pots with strong hands and gives opponents correct odds to chase draws, then pays off when they get there."], "street_weaknesses": {"preflop": "Fold 61-69%, raise only 16-24%, avg raise 0.6-0.7x pot. In heads-up this is far too nitty — the bot should be raising 40-50%+ of hands from the button and defending most of the BB.", "flop": "0% fold (never gives up postflop), raise 32-39% but at only 0.5x pot avg — too small to protect against draws or extract from weaker hands.", "turn": "65-74% call rate with 0.4-0.5x pot avg raise. Passively calling with oversized ranges while underbetting strong hands.", "river": "Calling 69-88% with near-zero folding. Never folds and rarely raises — payoff wizard. 0% fold rate means it calls with everything including losing bluff-catchers."}, "recommendation": "Three-pronged fix: (1) Preflop: slash fold rate from ~64% to ~35%, widen open-raising to ~40%, defend BB with ~80%. (2) Postflop: implement fold mechanism — target 10-15% fold rates on flop/turn vs aggression with weak showdown value. (3) Raise sizing: increase from 0.4-0.7x pot to 0.65-0.85x pot to protect and extract."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows.
`tasks` array may contain **1, 2, or 3** items based on your Worker Count Decision above.

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What weakness are you targeting? Are we failing due to bad logic or bad parameters? If diversity injection applies, explain: 'Diversity injection: trying X instead of Y'.",
  "targeted_failure": "One dominant failure pattern this generation targets, with the strongest evidence source.",
  "expected_behavior_change": "Specific table behavior that should change, e.g. call AKs all-in preflop more often but do not widen weak offsuit calls.",
  "do_not_touch": ["List files/functions/subsystems that must remain unchanged for attribution."],
  "measurement_plan": "How to verify this hypothesis: critical scenarios, H2H weak opponent, parent comparison, expected small-sample signal.",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

Notes:
- `branch_from` is OPTIONAL. Only include it to override the default evolution source.
- For 1-worker plans, use only Direction A or only Direction B.
- For 3-worker plans, add a Direction C (Opponent Modeler) task as worker_id 3.
- Avoid v29-like broad mixes such as "preflop rewrite + river rewrite + all-street sizing" unless you explicitly prove those changes are inseparable.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the H2H average win rate trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.
8. **ATTRIBUTION**: Do not mix unrelated preflop, postflop, and sizing rewrites in one generation. The next evaluation must be able to attribute win/loss movement to this plan.

Current evolution: v7 → v36
Bot directory: bots/claude_v7/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl
Head-to-Head data: web/core/results/head_to_head.json
Bot stats: web/core/results/bot_stats.json
Experience pool: web/core/experience_pool.md  ← READ THIS, not evolution_workspace/experience_pool.md

=============================
[CLAUDE OUTPUT]
I'll start by reading all the essential data files in parallel to understand the current state.
Let me read the rest of the H2H data and the current bot source code.
Now let me read the current bot v7 source code and compute H2H averages. Let me also look at what's happening with recent evolution attempts.
Now let me read some reference bots and the current bot's remaining files to understand the full picture.
Now let me look at some reference bots, particularly those that may address the preflop tightness and postflop calling station issues.
Let me check the reference bots that might have different approaches.
Now I have a comprehensive picture. Let me compute the H2H data precisely.
Now I have a complete picture. Let me also check how the reference bots handle preflop defense to inform the worker prompt.
Now I have a comprehensive understanding of the situation. Let me formulate my analysis and worker plan.

**Key findings:**
- v7 H2H average: **0.5747** across 30 opponents (2260 games)
- 1 clear weakness: v6 at 47.14% WR
- 10 thin-edge matchups at 50-53% WR (v20, v8, v22, v5, v27, v19, v13, v25, v29, v21)
- Only dominant vs early-gen bots (v1 97%, v4 62%)
- Match analysis confirms: 61-69% preflop fold rate, 0% postflop fold rate, 0.4-0.7x pot raise sizing
- strategy.py is 1158 lines, postflop.py is 1015 lines (both over 1000-line limit)
- 30 failed evolution attempts (v8-v36) never addressed the root cause

```json
{
  "analysis": "v7 is stagnant at H2H avg 0.5747 for 30+ generations. Match analysis reveals a single SYNDROME — not three independent leaks: the bot is pathologically overconservative. (1) Preflop: choose_preflop_spot_action only handles sb_open and bb_vs_limp; bb_vs_raise and sb_vs_reraise fall through to general postflop logic with thresholds strong=0.69/medium=0.54 at round_idx=0 — these are appropriate for river decisions but catastrophically tight for preflop defense, causing 61-69% fold rate in HU where ~30% is correct. (2) Postflop: postflop_call_margin accumulates 8-15 positive margin terms (air_hand +0.018, weak_showdown +0.012, facing_aggr +0.008, etc.) that sum to 0.08-0.15, making it mathematically impossible for the bot to fold any hand — the 0% postflop fold rate is a direct consequence. (3) Underbetting: base raise ratios (0.55 flop, 0.60 turn, 0.70 river) are 20-30% below optimal. All three stem from the same root: thresholds designed for full-ring conservatism applied to HU NLHE where aggression is mandatory. The v6 weakness (47% WR) is explained by v6's aggression exploiting v7's calling station + tight preflop — v6 opens wide (stealing blinds) then value-bets relentlessly (exploiting 0% fold). Diversity injection: instead of adding features (which failed v8-v36), this generation makes BOLD threshold restructuring — the magnitude of changes (20-40% shifts) is what's substantially different, not the direction. Reference bot study: examined bot5 and bot6 for preflop defense patterns.",
  "targeted_failure": "Overconservative threshold syndrome: 61-69% preflop fold rate bleeds blinds; 0% postflop fold rate makes the bot a payoff wizard. Evidence: match analysis across multiple replays shows consistent 0% fold on all postflop streets, and preflop fold rates double the HU-optimal ~30%.",
  "expected_behavior_change": "Preflop fold rate drops from ~65% to ~30-35%: the bot defends BB with ~60-65% of hands vs opens, and opens ~50-55% from SB. Postflop fold rate rises from 0% to 10-15% on turn/river: air hands (no pair, no draw) fold to half-pot+ bets. Raise sizing increases from 0.4-0.7x pot to 0.65-0.90x pot. Net effect: v7 stops bleeding blinds, stops paying off every bet, and charges opponents correctly for draws.",
  "do_not_touch": ["card_utils.py", "constants.py", "state.py", "tournament.py", "simulation.py", "opponent.py", "main.py", "main_backup.py"],
  "measurement_plan": "1) H2H vs v6 should improve from 47% to >50% — fewer blind surrenders + fewer payoff calls directly counter v6's exploitation. 2) H2H vs v20/v8/v22 (currently 50-51%) should improve to 53%+ with better preflop defense. 3) Decision tests must pass ≥70% (the threshold changes are conservative in early streets, aggressive only where match data proves the leak). 4) Mirror battle vs v7 parent: expect tight margin (0-3% shift) since changes address specific spots, not wholesale rewrite. 5) If H2H avg improves from 0.5747 to 0.58+, the hypothesis is confirmed.",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py"],
      "difficulty": "hard",
      "worker_prompt": "You are an Algorithmic Logic Architect fixing v7's critical preflop and postflop leaks. The bot folds 61-69% preflop (HU optimal is ~30%) and folds 0% postflop (calling station). Your job is to restructure the decision thresholds in strategy.py.\n\n## CRITICAL CONTEXT\n- strategy.py is currently 1158 lines. To add new logic without exceeding reasonable size, first extract the raise sizing functions into a new file:\n  - Extract `choose_raise` (lines ~269-393), `choose_overbet_river` (lines ~452-476), and `choose_overbet_bluff_river` (lines ~479-484) into a new file `raise_sizing.py`\n  - Add `from raise_sizing import choose_raise, choose_overbet_river, choose_overbet_bluff_river` at the top of strategy.py\n  - This removes ~130 lines from strategy.py, leaving ~1028 lines for your changes\n\n## TASK 1: Add BB Preflop Defense (choose_preflop_spot_action)\n\nThe function `choose_preflop_spot_action` (line ~396) currently only handles `sb_open` and `bb_vs_limp`. It returns None for `bb_vs_raise` and `sb_vs_reraise`, causing these spots to fall through to general postflop logic with thresholds (strong=0.69, medium=0.54) that are WAY too tight for preflop.\n\nAdd handling for `bb_vs_raise` spot AFTER the existing `bb_vs_limp` block:\n```python\nif spot_info['preflop_spot'] == 'bb_vs_raise':\n    # We're BB facing a raise from SB — defend wide in HU\n    # Pot odds typically ~33-40% for standard raises\n    defense_threshold = 0.35 + match_adjust - loose_bonus\n    threebet_threshold = 0.62 + match_adjust\n    \n    # Fold only worst hands\n    if preflop_strength < defense_threshold and not is_preflop_3bet_candidate(req['my_cards']):\n        if trash_hand:\n            return -1\n        # Even trash gets a chance if pot odds are good\n        pot_odds = to_call / (state['pot'] + to_call) if to_call > 0 else 0\n        if preflop_strength < pot_odds - 0.08:\n            return -1\n    \n    # 3bet with premium hands\n    if preflop_strength >= threebet_threshold or is_preflop_3bet_candidate(req['my_cards']):\n        raise_amount = choose_raise(\n            state.get('min_raise_action', state['round_raise']),\n            my_chips, state['my_round_bet'], to_call, state['pot'],\n            max(win_rate, preflop_strength), 0,\n            spot_info['preflop_spot'], preflop_strength,\n            spot_info['has_position'], opponent_model,\n            match_sizing_delta=match_profile['sizing_delta'],\n        )\n        if raise_amount is not None:\n            return raise_amount\n    \n    # Call with wide range\n    return 0\n```\n\nAlso add `sb_vs_reraise` handling:\n```python\nif spot_info['preflop_spot'] == 'sb_vs_reraise':\n    # We opened SB and got 3bet — defend selectively\n    defend_threshold = 0.48 + match_adjust\n    if preflop_strength < defend_threshold and not is_preflop_3bet_candidate(req['my_cards']):\n        return -1\n    if preflop_strength >= 0.65 + match_adjust:\n        raise_amount = choose_raise(\n            state.get('min_raise_action', state['round_raise']),\n            my_chips, state['my_round_bet'], to_call, state['pot'],\n            max(win_rate, preflop_strength), 0,\n            spot_info['preflop_spot'], preflop_strength,\n            spot_info['has_position'], opponent_model,\n            match_sizing_delta=match_profile['sizing_delta'],\n        )\n        if raise_amount is not None:\n            return raise_amount\n    return 0\n```\n\n## TASK 2: Lower Preflop Thresholds in Existing Code\n\nIn `choose_preflop_spot_action`:\n- Line with `open_threshold = 0.49 + ...`: Change `0.49` to `0.40`\n- Line with `limp_threshold = 0.36 + ...`: Change `0.36` to `0.28`\n- Line with `iso_threshold = 0.57 + ...`: Change `0.57` to `0.48`\n\n## TASK 3: Lower General Preflop Thresholds in get_action\n\nIn the `get_action` function, find the lines (around line 643-644):\n```python\nstrong = 0.69 if round_idx == 0 else 0.65 if round_idx == 1 else 0.61 if round_idx == 2 else 0.59\nmedium = 0.54 if round_idx == 0 else 0.50 if round_idx == 1 else 0.48\n```\nChangeto:\n```python\nstrong = 0.58 if round_idx == 0 else 0.65 if round_idx == 1 else 0.61 if round_idx == 2 else 0.59\nmedium = 0.46 if round_idx == 0 else 0.50 if round_idx == 1 else 0.48\n```\nThis makes preflop calling/raising much more liberal without touching postflop behavior.\n\n## TASK 4: Fix Preflop Call Margin for Positional Defense\n\nFind the preflop call_margin section (around line 747):\n```python\nif round_idx == 0:\n    call_margin = 0.005 + (0.010 if not spot_info['has_position'] else 0.0)\n```\nChange to:\n```python\nif round_idx == 0:\n    # BB defends wider (has position), SB facing 3bet defends tighter\n    call_margin = -0.04 + (0.07 if spot_info['has_position'] else 0.0)\n```\nThis gives BB a call_margin of +0.03 (defend wide) and SB facing 3bet a call_margin of -0.04 (fold more marginal hands). With pot_odds ~0.36 for a 3x raise, BB calls with win_rate >= 0.33, defending ~65% of hands.\n\n## TASK 5: Increase Base Raise Ratios in choose_raise (now in raise_sizing.py)\n\nAfter extracting choose_raise to raise_sizing.py, modify the base ratios:\n- Flop (round_idx == 1): `ratio = 0.60` → `ratio = 0.68`\n- Turn (round_idx == 2): `ratio = 0.70` → `ratio = 0.78`\n- River (round_idx >= 3): `ratio = 0.85` → `ratio = 0.92`\n- SB open (round_idx == 0, to_call == 0): `ratio = 0.55` → `ratio = 0.62`\n- SB facing action (round_idx == 0, to_call > 0): `ratio = 0.75` → `ratio = 0.80`\n\n## TASK 6: Add Air-Fold Early Exit in get_action\n\nIn the `to_call > 0` section of get_action, BEFORE the existing fold logic (before `if realized_rate < pot_odds + call_margin`), add an air-fold check:\n```python\n# Air-fold: fold absolute garbage on turn/river to reasonable bets\nif round_idx >= 2 and made_strength < 0.15 and draw_strength < 0.08:\n    if not (draw_info and draw_info['semi_bluff']):\n        if not (blocker_profile and blocker_profile['eligible']):\n            bet_ratio = spot_info['last_raise_pot_ratio']\n            if bet_ratio >= 0.40 and not anti_lock_pressure:\n                if not strong_made_continue:\n                    return -1\n```\nThis gives the bot a mechanism to fold hands with no pair, no draw, no blockers on turn/river to reasonable-sized bets. The conditions are strict: only pure air folds, never with draws/blockers/anti-lock situations.\n\n## IMPORTANT RULES\n- DO NOT modify postflop.py (Worker 2 owns that file)\n- DO NOT modify any files other than strategy.py and the new raise_sizing.py\n- Ensure all imports are correct after extraction\n- Keep the existing function signatures unchanged — other modules depend on them\n- Test that the code compiles with `python -c 'import strategy'`\n- The changes should make the bot MORE aggressive preflop (wider defense, lower thresholds) and MORE disciplined postflop (fold air on late streets)"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are a Hyperparameter Tuner. DO NOT add new functions, classes, or change control flow. ONLY modify numeric constants, thresholds, and magic numbers in postflop.py.\n\n## PROBLEM\nv7 has 0% postflop fold rate — it NEVER folds on any postflop street. This is because postflop_call_margin, pair_domination_margin, and draw_call_margin all accumulate positive values that make the effective calling threshold too low. The bot calls with everything including pure garbage.\n\n## YOUR TASK: Reduce Postflop Call Margins\n\nAll changes are in `postflop.py`. ONLY change the numeric literals. Do NOT add/remove lines or change control flow.\n\n### 1. postflop_call_margin function (around line 173)\n\nThis function returns a margin added to pot_odds for the calling decision. Currently ALL margins are POSITIVE, meaning the bot calls WIDER than pot odds (never folds). Reduce these values:\n\nCurrent → New values:\n- `if weak_showdown: margin += 0.012` → `margin += 0.000` (no bonus for weak hands)\n- `if air_hand: margin += 0.018` → `margin += -0.010` (PENALTY for air — makes bot fold air)\n- `if spot_info['facing_postflop_aggression']: margin += 0.008` → `margin += 0.004`\n- `if size_bucket == 'small': margin += 0.020` → `margin += 0.008`\n- `elif size_bucket == 'medium': margin += 0.010` → `margin += 0.004`\n- `else: margin += 0.024` → `margin += 0.012`\n- `if opp_postflop_bet_count >= 2: 0.024 if small else 0.014` → `0.010 if small else 0.006`\n- `if round_idx >= 2 and air_hand: margin += 0.010` → `margin += 0.004`\n- `if round_idx == 3 and size_bucket == 'large': margin += 0.020` → `margin += 0.008`\n- `if not has_position: margin += 0.008` → `margin += 0.004`\n\nAlso update the clamp range: `return clamp(margin, 0.0, 0.08)` → `return clamp(margin, -0.02, 0.06)` (allow negative margins so air hands can fold)\n\n### 2. pair_domination_margin function (around line 83)\n\nThese margins are added when the bot has a pair that might be dominated. Reduce them:\n\nCurrent → New:\n- `top_pair weak_kicker: 0.012` → `0.006`\n- `top_pair good kicker: 0.004` → `0.002`\n- `middle_pair: 0.030` → `0.018`\n- `middle_pair weak_kicker: 0.012` → `0.006`\n- `bottom_pair: 0.050` → `0.032`\n- `bottom_pair weak_kicker: 0.012` → `0.006`\n- `underpair: 0.045` → `0.028`\n- `underpair board_overcards: 0.010` → `0.006`\n- `board_pair: 0.065` → `0.040`\n- `facing_postflop_aggression: 0.010` → `0.005`\n- `opp_postflop_bet_count >= 2: 0.012` → `0.006`\n- `river mid/bottom/under/board: 0.015` → `0.008`\n- clamp range: `(0.0, 0.10)` → `(0.0, 0.07)`\n\n### 3. draw_call_margin function (around line 664)\n\nThese margins are added for draw hands. Gutshots and weak flush draws should not get big calling bonuses:\n\nCurrent → New:\n- `gutshot: 0.040` → `0.020`\n- `non-nut flush high: 0.004` → `0.000`\n- `non-nut flush other: 0.020` → `0.008`\n- `turn gutshot: 0.020` → `0.010`\n- `turn flush other: 0.020` → `0.008`\n- `river (all draws): 0.050` → `0.025`\n- `large bet gutshot: 0.018` → `0.008`\n- `large bet flush near_nut: 0.006` → `0.003`\n- `large bet flush high: 0.012` → `0.006`\n- `large bet flush other: 0.018` → `0.008`\n- clamp range: `(-0.04, 0.08)` → `(-0.04, 0.06)`\n\n## RATIONALE\nThe current margins sum to ~0.10-0.15 for typical spots, meaning the bot calls with hands that have 25-35% equity when it needs 40%+. By reducing these margins by 40-50% overall and making air_hand margin NEGATIVE, the bot will:\n1. Fold pure air on turn/river (fix 0% fold rate)\n2. Call less with dominated pairs\n3. Be more selective with weak draw calls\n\n## IMPORTANT RULES\n- ONLY modify numeric literals in postflop.py\n- DO NOT add or remove any lines\n- DO NOT change any function signatures or control flow\n- DO NOT modify strategy.py or any other file\n- After changes, verify: `python -c 'import postflop'` compiles successfully"
    }
  ]
}
```
