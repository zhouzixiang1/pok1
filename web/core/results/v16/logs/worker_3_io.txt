
[WORKER 3 (Equity Modeling Specialist) PROMPT]
=============================
# System Rule
You are a highly skilled Coding Worker Agent playing the role of: **Equity Modeling Specialist**.
You must directly edit the source files in `bots/claude_v16/` to implement the Master's instructions.
The bot MUST correctly interface with the game engine via `main.py` (reads JSON from stdin, writes JSON `{"response": int}` to stdout).

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files. Example: Read `bots/claude_v16/strategy.py`
- **Use the Bash tool** to run compile checks, smoke tests, and git commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Role Boundary Rules
Your role determines what you CAN and CANNOT do:

## If you are a "Hyperparameter Tuner":
- **ALLOWED**: Changing numeric constants (e.g., `BLUFF_THRESHOLD = 0.15` → `0.20`)
- **ALLOWED**: Adjusting float thresholds (e.g., `POT_ODDS_CUTOFF = 2.0` → `2.3`)
- **ALLOWED**: Modifying integer conditions (e.g., `MIN_RAISE_HAND = 8` → `10`)
- **FORBIDDEN**: Adding new functions, classes, or methods
- **FORBIDDEN**: Adding new `if/for/while` blocks or changing control flow
- **FORBIDDEN**: Importing new modules
- If you accidentally add any non-numeric changes, revert with `git checkout -- <file>`

## If you are an "Algorithmic Logic Architect":
- **ALLOWED**: Adding new functions and methods
- **ALLOWED**: Refactoring existing logic and algorithms
- **ALLOWED**: Adding imports and new modules
- **FORBIDDEN**: Changing well-tuned constants unless structurally required
- Example of "structurally required": If your new function changes how a constant is used, you may adjust it. But don't randomly tune `OPENING_RANGE_THRESHOLD` or `VPIP_PRIORS`.

You have access to `evolution_workspace/reference_bots/` containing 6 strong bots (`bot1` to `bot6`). You may read them as reference.
Other Worker Agents may have recently modified the codebase before you. Read the attached Context Files carefully.

# Master Architect's Specific Prompt For You
CRITICAL REVISION NEEDED:
First pass implementation, no reviewer feedback yet.

ORIGINAL:
Improve the realized_postflop_equity function (lines 208-283) in strategy.py. Add a case for top pair with strong kicker:

1. Add `board_texture=None` as an optional parameter to the function signature.

2. Add this case after the existing top_pair weak_kicker case (after line 281):
```python
if pair_type == 'top_pair' and not pair_profile['weak_kicker']:
    eqr = 0.95 if has_position else 0.91
    if board_texture is not None and board_texture.get('dynamic', False):
        eqr -= 0.05
    if double_barrel:
        eqr -= 0.03
        if not has_position:
            eqr -= 0.02
    if round_idx == 3:
        eqr -= 0.03
    if big_pot:
        eqr -= 0.02
    eqr = clamp(eqr, 0.78, 0.98)
    return win_rate * eqr
```

3. Update the call site around line 584-593 to pass board_texture=board_texture.

This is important because top pair strong kicker currently gets raw win_rate (no equity realization discount), which overestimates its value against aggressive opponents. The equity realization factor should be slightly less than 1.0 because even strong top pairs can lose to rivered draws or be behind a disguised monster.

Keep all existing code intact. Do NOT change any other function signatures or behavior.

# Action
Please write the Python code to fulfill your objective. After editing:

1. **Verify your changes**: Run `git diff bots/claude_v16/` to review what you changed.
   Ensure no unintended modifications to files outside your assigned `target_files`.
2. If you see unexpected changes, fix them before finishing.
3. **Run quality checks**:
   - Compile check: `python -m py_compile bots/claude_v16/main.py` (and other .py files you changed)
   - Smoke test: `python evolution_workspace/smoke_tester.py bots/claude_v16/main.py`
   - If any check fails, fix the errors before finishing.

# Self-Review Before Finishing
After all quality checks pass, run these final checks:

1. **Role Boundary Check**: Run `git diff bots/claude_v16/` one more time and review your changes carefully.
   - If you are a **Hyperparameter Tuner**: Verify that ALL your changes are limited to numeric constants, thresholds, or magic numbers. If you added new functions, classes, or control flow logic (if/for/while blocks), REMOVE them immediately.
   - If you are an **Algorithmic Logic Architect**: Verify that you did not change well-tuned constants (thin_cap, open thresholds, VPIP/PFR priors) unless structurally required by your new algorithm.

2. **Target File Check**: Ensure you ONLY modified files in your assigned `target_files`. If you touched other files, revert those changes with `git checkout -- <file>`.

3. **Protocol Check**: Verify the bot still outputs `{"response": <int>}` via stdout. The action encoding is: 0=call/check, -1=fold, -2=all-in, >0=raise amount.

4. If you find any violations, fix them and re-run the quality checks.

=============================
[CLAUDE OUTPUT]
Let me start by reading the relevant files to understand the current code.
Let me use Bash to read the files instead.
Good, the files exist. Let me read the `realized_postflop_equity` function in `strategy.py`:
Now let me find the call site around line 584-593:
I can see the call site at lines 589-598. Now let me check if `board_texture` is available in the scope where the call is made:
Good. `board_texture` is available in the scope. Now let me also check if the function is called anywhere else:
Good, only one call site. Let me also check if it's imported elsewhere:
Good - the backup file is separate and won't be touched. Now I have a clear picture. Let me make the three changes:

1. Add `board_texture=None` to the function signature
2. Add the top pair strong kicker case after line 281
3. Update the call site to pass `board_texture=board_texture`
Now let me apply the changes:
The sed didn't add the parameter. Let me use Python to make all the edits precisely:
Now let me verify all three changes:
The diff looks exactly right. Three clean changes:

1. **Function signature** (line 290): Added `board_texture=None` parameter
2. **New case** (lines 354-369): Added top pair with strong kicker equity realization block
3. **Call site** (line 896): Added `board_texture,` argument to the function call

Now let me run the quality checks:
All quality checks pass. Let me do the final self-review:
There are changes in `opponent.py` and `postflop.py` that I didn't make (likely from a previous Worker Agent). Let me check if those changes are mine or pre-existing:
Those are from another Worker Agent's changes, not mine. My changes are only to `strategy.py`. The `.completed` file change is just a mode change (0 bytes). Let me verify my strategy.py diff is exactly what I intended:
I see extra changes in the diff that are from other Worker Agents (not mine). My specific changes are exactly the three requested modifications:

1. ✅ **Line 290**: Added `board_texture=None` to function signature
2. ✅ **Lines 354-369**: Added top pair strong kicker equity realization block
3. ✅ **Line 903**: Added `board_texture` to the call site

Let me do the final self-review checks:

**Role Boundary Check** (Algorithmic Logic Architect):
- ✅ Added a new code block (new conditional case) - ALLOWED
- ✅ Added a new parameter to function signature - ALLOWED
- ✅ Added a new argument at call site - ALLOWED
- ✅ Did NOT change well-tuned constants in unrelated code - PASS
- ✅ Did NOT import new modules - PASS

**Target File Check**:
- ✅ Only modified `bots/claude_v16/strategy.py` (other diffs in opponent.py/postflop.py are from prior Worker Agents)

**Protocol Check**:
- ✅ The bot still outputs `{"response": <int>}` via stdout - the `get_action` return values are unchanged
- ✅ Smoke test passed, compile check passed

All three changes are complete and verified. The implementation adds proper equity realization discounting for top pair with strong kicker hands, which previously received raw `win_rate` (1.0x multiplier) with no adjustment for positional disadvantage, board texture dynamics, or pot size considerations.
