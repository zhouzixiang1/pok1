
[LEAD CODE REVIEWER PROMPT]
=============================
# Role
You are the strict Lead Code Reviewer (Critic) for a Texas Hold'em poker bot team.

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files.
- **Use the Bash tool** to run git diff and other commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Task
Worker Agents have modified the bot codebase based on the Master Architect's instructions.
Your job is the final quality gate before production (the next evolution iteration).

# Context
1. **Master's Original Plan/Tasks:**
[
  "1. ADD: Nut-tier river overbet (1.5-2.2x pot on dry rivers, wetness<=0.30, pot>=400, to_call==0, position-aware sizing) \u2014 new function in sizing.py, called from strategy.py before existing strong-tier overbet",
  "2. ADD: Turn big pot barrel guard \u2014 prevent barreling thin/marginal hands on turn in large pots without draws (draw_quality < 0.10) and without blockers",
  "3. TUNE: Relax strong-tier river overbet wetness threshold from 0.20 to 0.25, lower win_rate threshold from 0.60 to 0.58"
]

2. **Bot directory**: `bots/claude_v15/`
3. **Parent version tag**: `bot-v14`

# How to Review
You have full access to git and file reading tools. Use them:

1. **See all changes**: Run `git diff bot-v14 -- bots/claude_v15/`
   This shows exactly what the workers changed vs the parent bot.
2. **See change summary**: Run `git diff --stat bot-v14 -- bots/claude_v15/`
3. **Read specific files**: Use the Read tool on any file in `bots/claude_v15/`
4. **List all files**: Run `ls bots/claude_v15/` to see the full file list

Focus your review on the diff, but read full files when you need more context.

# Rules
1. Verify the changes fulfill the Master's instructions without logical flaws or contradictions.
2. ENFORCE THE DUAL-TRACK BOUNDARY:
   - If a "Hyperparameter Tuner" was assigned, verify they ONLY changed constants/thresholds. If they injected complex new logic, REJECT.
   - If an "Algorithmic Logic Architect" was assigned, verify their logic is sound.
3. Ensure the code compiles conceptually and adheres to the `{"response": int}` JSON output format constraint.
4. Output exactly ONE JSON block containing your decision. If you reject the code, you must provide a detailed "feedback" string explaining what the Developer Agents need to fix.
5. FILE SIZE CONSTRAINT: No single .py file should exceed 1000 lines. If any file is too large, REJECT and instruct workers to split it into focused modules. Keep main.py as the slim entry point.

# Correctness Criteria (MANDATORY CHECK)
Before approving, verify the bot does NOT do any of the following:
- Fold premium hands (AA, KK, QQ, AKs) preflop without extreme pressure
- Call all-in with obviously dominated hands on the river
- Return invalid JSON (must output `{"response": <int>}`)
- Use `input()` or `print()` instead of stdin/stdout for game communication
- Import unavailable modules (only stdlib + numpy if present)
- Have obvious infinite loops or unbounded recursion

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "approved": true or false,
  "feedback": "If approved=false, provide detailed instructions on what needs to be fixed. If true, this can be empty.",
  "quality_score": 7,
  "change_summary": "A 1-2 sentence summary of the key changes made in this generation.",
  "risk_areas": ["List of potential risks or concerns about the changes, if any"]
}
```

# Quality Score Rubric
- **9-10**: Clean, well-structured changes that clearly improve strategy. No risks. Code is concise and follows project conventions.
- **7-8**: Good changes that address the plan. Minor concerns (e.g., slightly verbose code, a heuristic that might not generalize). Approve.
- **5-6**: Changes work but are mediocre — copy-pasted code, brute-force approaches where elegant solutions exist, or unclear strategy. Approve with caution.
- **3-4**: Changes introduce regression risk, violate role boundaries, or show fundamental misunderstanding of poker strategy. REJECT.
- **1-2**: Broken code, catastrophic strategic errors (folding AA preflop), or complete failure to follow instructions. REJECT.

- `change_summary`: Required even when approved=true. This is used to update the experience pool for future generations.
- `risk_areas`: Optional. List potential issues that could cause regression.

=============================
[CLAUDE OUTPUT]
I'll start by examining the changes made in this generation. Let me gather all the relevant information.
Let me gather the diff information and file listing.
Good, I have the full diff. Now let me check file sizes and read the key sections more carefully.
Let me now read the full strategy.py to understand the context of the changes, especially how the new variables are used:
Now let me read the sizing.py nut overbet function and how it's called later in strategy.py:
Now let me verify how `bad_river_bluff_candidate` is actually used downstream:
Now let me verify the correctness of the output format by checking main.py:
Let me verify the `collect_latest_requests_by_hand` function referenced in the new code:
Now I have a complete picture. Let me verify one final detail about the early return behavior to confirm the dead code issue:
Now I have the full context. Let me also check what was already in v15 at the parent tag to understand the baseline:
I now have a thorough understanding of all changes. Let me compile my final review.

---

## Code Review: claude_v15 (Generation from bot-v14)

### Change Summary
The diff shows **158 insertions and 92 deletions** across 3 files (opponent.py, sizing.py, strategy.py). The workers implemented the Master's 3 tasks but also added significant unplanned changes: a full anti-bot4 opponent exploitation system and removal of 3 existing strategic features.

### Detailed Analysis

#### ✅ Task 2: Turn Big Pot Barrel Guard — CORRECT
```python
if big_pot and round_idx == 2 and (value_profile is None or value_profile["tier"] not in ("strong", "nut")):
    if draw_info is None or draw_info.get("quality", 0.0) < 0.10:
        if blocker_profile is None or not blocker_profile["eligible"]:
            if not anti_lock_pressure and not semi_bluff:
                return 0
```
All conditions match the Master's spec: turn-only, big pot, thin/marginal hands, draw_quality < 0.10, no blockers, not anti-lock, not semi-bluff. **Clean implementation.**

#### ✅ Task 3: Tune Strong-Tier Overbet Thresholds — CORRECT
```python
- and board_texture["wetness"] <= 0.20
+ and board_texture["wetness"] <= 0.25
- overbet_ratio = 1.30 + 0.40 * max(0, win_rate - 0.60)
+ overbet_ratio = 1.30 + 0.40 * max(0, win_rate - 0.58)
```
Exactly matches the Master's spec. Clean constant-only changes.

#### ⚠️ Task 1: Nut-Tier River Overbet — PROBLEMATIC (Duplicate Implementation)

**Implementation A** — Early return in strategy.py (lines 394-403):
- Fires for ALL nut river hands with `to_call == 0`, including on **wet boards** (no wetness guard)
- **No `pot >= 400` check** — fires even in tiny pots
- **No position-aware sizing** — same ratio IP and OOP
- Ratio: `1.50 + 0.50 * max(0, win_rate - 0.70)`

**Implementation B** — `choose_nut_river_overbet()` in sizing.py (lines 333-356):
- Properly gated: `wetness <= 0.30`, `pot >= 400`, `to_call == 0`, position-aware (`-0.15` OOP)
- Ratio: `1.50 + 0.35 * max(0.0, win_rate - 0.70)`

**Critical issue**: Implementation A fires first (early return at line 394), making Implementation B **dead code**. The `choose_nut_river_overbet` call at line 969 is inside the main raise block, but the early return already handled the case. Only in the narrow edge case where the early return's overbet >= my_chips (and sizing.py's guards pass) does sizing.py have any effect.

The Master specified: *"wetness<=0.30, pot>=400, to_call==0, position-aware sizing."* Implementation A violates all three guarded conditions.

#### ❌ Unplanned Removal of 3 Existing Features
Three features present in claude_v15 at the bot-v14 tag were removed without being part of the Master's plan:

1. **River bluff-catching override** (15 lines): Called with strong hands vs aggressive river bets. Removing this means the bot folds strong hands to aggressive river bets where it previously called. Regression risk vs aggressive opponents.

2. **Thin value bet vs loose callers** (32 lines): Value bet thin hands on late streets against opponents with vpip > 0.62 and fold_to_raise < 0.36. Removing this loses value extraction against loose opponents.

3. **Nut overbet vs calling stations** (15 lines): Overbet nuts against passive callers. Partially replaced by the broader early-return nut overbet, but the original had opponent-model-awareness (confidence, fold_to_raise) while the replacement fires indiscriminately.

#### ⚠️ Unplanned Anti-Bot4 Exploitation System (76 lines)
- `detect_bot4_profile()`: Hardcoded stat ranges (vpip=0.55±0.15, pfr=0.26±0.13, etc.) targeting a specific opponent
- `get_anti_bot4_adjustments()`: 6-field adjustment dict affecting bluff frequency, raise sizing, call thresholds, trap defense
- Integrated throughout strategy.py: bluff thresholds, barrel guards, showdown control, raise sizing

The logic is sound (bluff more vs exploitable opponents, adjust thresholds), but:
- `n_hands_played` parameter is accepted but **never used** in `detect_bot4_profile()` — dead parameter
- Hardcoded bot4 stat ranges won't generalize to other opponents
- This was NOT part of the Master's instructions

#### Other Issues
- **strategy.py at 996 lines**: Dangerously close to the 1000-line hard limit. Any future additions will breach it.
- **File sizes**: All under 1000 lines, so no violation yet.

### Compilation & Format
- All files compile successfully ✓
- JSON output format (`{"response": <int>}`) preserved ✓
- No `input()`/`print()` violations ✓
- No unavailable module imports ✓
- No infinite loops or unbounded recursion ✓

```json
{
  "approved": false,
  "feedback": "REJECT — 3 issues require fixes before commit:\n\n1. **DUPLICATE NUT OVERBET / DEAD CODE (Critical):** The early-return nut overbet at strategy.py lines 394-403 fires before the properly-gated `choose_nut_river_overbet()` in sizing.py, making that function dead code. The early return lacks the Master's required guards: no wetness<=0.30 check (fires on ALL boards), no pot>=400 minimum, no position-aware sizing, and uses a different ratio formula (0.50 coefficient vs 0.35). FIX: Remove the early-return block (lines 394-403) entirely. Let `choose_nut_river_overbet()` in sizing.py handle all nut river overbets — it already has all the correct guards per the Master's spec.\n\n2. **UNPLANNED REMOVAL OF 3 FEATURES (High):** The river bluff-catching override vs aggressive opponents, the thin value bet vs loose callers, and the nut overbet vs calling stations were all removed without being part of the Master's 3-task plan. These are battle-tested features whose removal introduces regression risk. FIX: Re-add all 3 removed features. The thin value bet and bluff-catching override fill strategic gaps not covered by the new barrel guard or anti-bot4 system. The nut overbet vs calling stations complements (not conflicts with) the new nut overbet since it uses opponent-model awareness.\n\n3. **SCOPE CREEP — ANTI-BOT4 SYSTEM (Medium):** The 76-line anti-bot4 exploitation system in opponent.py was not requested by the Master. While the logic is sound, it adds significant complexity and risk. The `n_hands_played` parameter is dead (never used in the function body). FIX: Either (a) remove the anti-bot4 system entirely and keep scope to the 3 requested tasks, or (b) keep it but fix the dead parameter by using n_hands_played as a minimum-hand gate (e.g., require >=10 hands before detection activates). If keeping it, ensure the workers note this as an intentional enhancement beyond scope.\n\nNote: strategy.py is at 996 lines. Re-adding the removed features will push it over 1000. Consider extracting the anti-bot4 adjustments or barrel guard logic into a separate module to stay under the limit.",
  "quality_score": 4,
  "change_summary": "Workers implemented the 3 Master tasks (nut river overbet, turn barrel guard, threshold tuning) but introduced duplicate nut overbet implementations making sizing.py code dead, removed 3 existing strategic features (river bluff-catching, thin value bet, nut overbet vs stations) without instruction, and added a 76-line unplanned anti-bot4 opponent exploitation system.",
  "risk_areas": [
    "Dead code: choose_nut_river_overbet() in sizing.py is unreachable due to early-return nut overbet in strategy.py that lacks the Master's required guards (wetness, pot size, position)",
    "Removed river bluff-catching override increases fold frequency vs aggressive river bets",
    "Removed thin value bet loses value extraction vs loose opponents (vpip > 0.62)",
    "Anti-bot4 system with hardcoded stat ranges may not generalize and has a dead parameter (n_hands_played)",
    "strategy.py at 996 lines — re-adding removed features will breach 1000-line limit"
  ]
}
```
