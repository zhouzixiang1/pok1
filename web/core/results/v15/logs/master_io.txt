
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
{"stagnant": false, "reason": "Ratings unreliable (rd=44.8), cannot determine stagnation. v14 rated 1507.7 which is ~120pts below top bot v7 (1630.8), but this may improve with more matches. Proceeding with incremental improvements."}

# Recent Match Analysis
{"available": false, "reason": "No match history available for v14 (insufficient matches played). Proceeding based on code review and experience pool lessons."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v14 → v15
Bot directory: bots/claude_v14/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
I'll start by reading the essential data files to understand the current state. Let me gather all the information I need.
Now let me read all the essential data files in parallel.
Let me read the essential data files using Bash and the file reading tools.
Let me read the key data files simultaneously.
The MCP file reader can't access local files. Let me use Bash to read these files instead, as instructed.
Interesting - v14 is the lowest-rated bot at 1509! Let me find the actual bot files and examine the top performers for reference.
I'm in the `web/` subdirectory. The bots are in the parent repo. Let me look at the actual bot files.
Now let me read the v14 bot source code and the top-rated bots (v4 at 1623, v5 at 1611, v2 at 1614) for comparison.
Now let me read the remaining source files and the top-rated bots for reference.
Now let me look at the top-rated bots for comparison. v4 (1623) is highest, let me also check v2 (1614) and v7 (1607) which were historically strong.
Now let me look at the key differences between v14 and v4, particularly in the strategy.py file where the main decision logic lives. Let me also read the full v14 strategy.py.
Now let me compare key differences with v4 (highest rated at 1623) - the v4 strategy:
Now I have a comprehensive understanding. Let me check the key differences more carefully. The critical insight: v14 is **the worst bot** at 1509 (latest reading: 1513). Let me compare v14 vs v4 on the key decision functions.
Now I have a thorough understanding. Let me analyze the rating trend data more carefully to see v14's trajectory.
Now I have a complete picture. Let me analyze the critical findings:

**Key insight: v14 is the WORST bot at ~1509 (collapsed from ~1608), while v4 is the BEST at ~1623.** The anti-bot4 detection system in v14 uses incredibly loose criteria (e.g., `abs(vpip - 0.55) < 0.15` matches almost every opponent), causing aggressive bluff adjustments against non-bot4 opponents. Combined with exploit_lambda corrupting base thresholds, this has been catastrophic.

Let me now update the experience pool and produce the final output.
Good. Now let me produce the final strategic output.

```json
{
  "analysis": "## Critical Finding: v14 is the WORST bot (1509), v4 is the BEST (1623)\n\nv14 collapsed ~100 points from its peak (~1608→1509), making it the lowest-rated bot in the entire pool. Root cause analysis by comparing v14 vs v4 source code reveals three systemic problems:\n\n1. **Anti-bot4 detection with catastrophically loose criteria**: `detect_bot4_profile()` matches `abs(vpip-0.55)<0.15` which means vpip 0.40-0.70 triggers — that's virtually every opponent once priors converge. This causes `bluff_freq_bonus` of +0.15*score on dynamic boards, +0.10 on paired boards, +0.12 on river checks — massive aggressive adjustments applied against nearly ALL opponents, not just bot4. This single feature likely accounts for most of the -100pt collapse.\n\n2. **exploit_lambda corrupts GTO base thresholds**: `safe_exploitation_lambda()` blends gift_balance into the strong/medium thresholds: `strong = (1-exploit_lambda)*gto_strong + exploit_lambda*strong`. When gift_balance is noisy (which it often is), this shifts thresholds unpredictably. v4 (#1 at 1623) has NO exploit_lambda — it uses pure simulation-based decisions.\n\n3. **Over-conservative EQR with compounding penalties**: v14 lowered air EQR from v4's 0.72/0.62 to 0.68/0.56, then added extra OOP double-barrel penalty (-0.05), big_pot discount (-0.03), and lowered the floor from 0.45 to 0.40. These compound, causing systematic over-folding of air hands.\n\n**What v14 got RIGHT** (should be ported to v4):\n- CBet tracking in opponent model (lesson #17: useful for flop decisions)\n- River nut overbet at 1.5-2.0x pot (lesson #3: proven edge)\n- Drift detection for adapting to changing opponents\n- EXTRA_SIMULATIONS_BY_PUBLIC_COUNT for critical spot refinement\n\n**What must NOT be ported**: anti-bot4 detection (unless thresholds tightened to ±0.08), exploit_lambda, preflop 3bet bluff spots, wet thin_cap formula, extra EQR penalties.\n\n**Decision: Branch from v4 (highest rated at 1623)**. Port only the 3-4 highest-confidence additions. The field is compressed to 1590-1623 (~30pt range), so every change must be carefully validated.",
  "branch_from": "claude_v4",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "opponent.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect evolving poker bot claude_v4 → v15. You are branching from claude_v4, which is the HIGHEST-RATED bot at 1623 Glicko. Your job is to add 3 targeted, proven features to v4's clean codebase. DO NOT break any existing v4 logic — only ADD new features.\n\n## Context\nThe v14 bot (evolved from v4's lineage) collapsed 100 points because it added too many features that corrupted base thresholds. We are going back to v4 (the #1 bot) and adding ONLY the highest-confidence improvements. The field is compressed to 1590-1623, so small changes matter enormously.\n\n## Task 1: Add CBet Tracking to opponent.py\nPort the cbet tracking system from claude_v14's opponent.py into claude_v4's opponent.py. Specifically:\n\nIn `build_opponent_model()`:\n1. Add variables at the top: `cbet_opportunities=0`, `cbet_count=0`, `fold_to_cbet_opportunities=0`, `fold_to_cbet_count=0`\n2. Inside the per-hand loop, add tracking variables: `opp_raised_preflop_this_hand=False`, `first_flop_action_seen=False`, `facing_cbet=False`\n3. After the round_idx==0 raise detection, set `opp_raised_preflop_this_hand=True` when opponent raises preflop\n4. When `round_idx==1 and not first_flop_action_seen`, set `first_flop_action_seen=True`. If the opponent was the preflop raiser and now bets/raises the flop, increment `cbet_opportunities` and `cbet_count`\n5. When `pid==my_id and facing_cbet`, increment `fold_to_cbet_opportunities` and if folded increment `fold_to_cbet_count`, then reset `facing_cbet=False`\n6. Add to the returned dict: `\"cbet_rate\": smooth_rate(cbet_count, cbet_opportunities, 0.55, 4.0)` and `\"fold_to_cbet\": smooth_rate(fold_to_cbet_count, fold_to_cbet_opportunities, 0.40, 3.0)`\n\nIMPORTANT: Keep v4's priors (vpip=0.58, pfr=0.28). Do NOT change any existing v4 opponent model fields.\n\n## Task 2: Add CBet-Aware Flop Calling in strategy.py\nIn `get_action()`, inside the `to_call > 0` section where `round_idx >= 1`, add a cbet_rate adjustment:\n\nAfter the existing `call_margin` is computed from `postflop_call_margin()`, add:\n```python\ncbet_rate = opponent_model.get(\"cbet_rate\", 0.55)\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"]:\n    if cbet_rate > 0.60:\n        call_margin -= 0.025  # opponent cbets a lot -> wider calling range\n    elif cbet_rate < 0.35:\n        call_margin += 0.025  # opponent rarely cbets -> respect their flop bet more\n```\n\nThis is the ONLY change to get_action()'s calling logic. Do not touch anything else.\n\n## Task 3: Add River Nut Overbet in strategy.py\nIn `get_action()`, in the `to_call == 0` (betting initiative) section, BEFORE the existing `if win_rate >= medium or ...` block, add a river nut overbet:\n\n```python\n# River nut overbet: proven edge (lesson #3)\nif (round_idx == 3 and to_call == 0 and len(public_cards) >= 3\n    and value_profile is not None and value_profile[\"tier\"] == \"nut\"\n    and not anti_lock_pressure):\n    overbet_ratio = 1.50 + 0.50 * max(0, win_rate - 0.70)\n    if board_texture is not None and board_texture[\"wetness\"] <= 0.30:\n        overbet_ratio += 0.15\n    overbet_target = int(pot * overbet_ratio)\n    overbet_target = max(overbet_target, state[\"min_raise_action\"])\n    overbet_target = min(overbet_target, my_chips - 1)\n    if overbet_target > to_call and overbet_target < my_chips:\n        return overbet_target\n```\n\nAlso add EXTRA_SIMULATIONS_BY_PUBLIC_COUNT to constants.py:\n```python\nEXTRA_SIMULATIONS_BY_PUBLIC_COUNT = {\n    0: 200,\n    3: 220,\n    4: 180,\n}\n```\n\nAnd in get_action(), after the initial `win_rate = estimate_weighted_win_rate(...)` call, add the critical-spot refinement (this is already in v4's strategy.py — verify it exists and uses EXTRA_SIMULATIONS_BY_PUBLIC_COUNT correctly).\n\n## CRITICAL RULES\n- DO NOT add anti-bot4 detection, exploit_lambda, preflop 3bet bluff spots, drift detection, or any feature from v14 that isn't explicitly listed above\n- DO NOT change v4's EQR values, thin_cap, priors, or threshold constants\n- DO NOT modify choose_raise() or choose_anti_lock_pressure_action()\n- DO NOT create new files — all changes go in existing strategy.py and opponent.py\n- Keep each file under 1000 lines\n- Test that the code compiles with py_compile"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner evolving poker bot claude_v4 → v15. You are branching from claude_v4 (the #1 bot at 1623 Glicko). Your ONLY job is to adjust numeric constants and thresholds. You MUST NOT add new functions, classes, control flow, or change any algorithmic logic.\n\n## Context\nThe field is compressed to 1590-1623 (~30pts). v4 is the #1 bot. We are making very conservative parameter adjustments only. Previous generations showed that aggressive parameter changes (e.g., thin_cap from 0.30 to 0.46+) caused catastrophic regressions.\n\n## Your Target File: constants.py\n\nv4's current constants.py has:\n```python\nSIMULATIONS_BY_PUBLIC_COUNT = {\n    0: 500,\n    3: 700,\n    4: 900,\n    5: 0,\n}\n```\n\n### Task: Tune Simulation Counts\n\nChange SIMULATIONS_BY_PUBLIC_COUNT to:\n```python\nSIMULATIONS_BY_PUBLIC_COUNT = {\n    0: 500,\n    3: 800,\n    4: 900,\n    5: 0,\n}\n```\n\nRationale: Increasing flop simulations from 700→800 gives better equity estimates on the most complex street (3 community cards, most combinations to evaluate). v14 used 800 for flop sims and this was NOT one of the features that caused its collapse. The flop is the most impactful decision point (sets up turn/river play) so 14% more simulations there is a reasonable investment.\n\n### ALSO ADD: Extra Simulation Constants\n\nAdd this constant to constants.py (the Logic Architect worker will use it in strategy.py):\n```python\nEXTRA_SIMULATIONS_BY_PUBLIC_COUNT = {\n    0: 200,\n    3: 220,\n    4: 180,\n}\n```\n\nThis enables extra simulation runs for critical decision spots (large bets, all-in situations) where decision accuracy matters most.\n\n## CRITICAL RULES\n- ONLY modify numeric constants in constants.py\n- DO NOT change HAND_CLASS_SCORE values\n- DO NOT change INITIAL_CHIPS, BIG_BLIND, SMALL_BLIND, TOTAL_HANDS, LOCK_WIN_MARGIN, N_PLAYERS\n- DO NOT add any new functions, classes, or control flow\n- DO NOT modify any other files\n- The change is small: just bump 700→800 for flop sims and add the EXTRA_SIMULATIONS constant"
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
{"stagnant": false, "reason": "Ratings unreliable (rd=44.8), cannot determine stagnation. v14 rated 1507.7 is ~120pts below top bot v7 (1630.8), but may improve with more matches. Proceeding with incremental improvements."}

# Recent Match Analysis
{"available": false, "reason": "No match history available for v14 (insufficient matches played). Proceeding based on code review and experience pool lessons."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v14 → v15
Bot directory: bots/claude_v14/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
I'll start by reading the essential data files to understand the current state.
Now let me read all the essential data files and examine the codebase.
The MCP tool looks for remote repos. These are local files — I need to use Bash to read them. Let me do that.
Let me use Bash to read the essential files.
Now let me read the essential files using Bash:
Now let me examine the current bot code and reference bots:
Let me read the files in chunks and focus on the key differences:
Now let me read v7's strategy.py for comparison:
Let me check the key differences in postflop EQR and the postflop_call_margin function:
Now let me check the key difference - v4's strategy vs v7's:
Now I have a complete understanding. Let me check the current experience pool size and update it:
Now let me update the experience pool with new insights from this analysis:
Now I have a thorough understanding of the codebase, ratings, and strategic landscape. Let me formulate the final plan.

```json
{
  "analysis": "## Strategic Analysis for v15\n\n### Current Ratings Landscape\n- **v7 (current #1):** 1615.1 (rd=41.2) — stable, reliable\n- **v4 (historical peak):** 1600.0 (rd=40.5) — peaked at 1623\n- **v14 (current ancestor):** 1513.5 (rd=41.2) — **catastrophically worst**, 100pts below field\n- Field compressed to ~1590-1623 (30pts). Small edges matter enormously.\n\n### Why v14 Collapsed (Root Cause Analysis)\nv14 = v4 base + multiple additions. Of these, only CBet tracking and river overbet were net positive. The collapse came from **compounding threshold corruption** (lesson #22):\n1. **Lower air EQR** (0.68/0.56 vs v7's 0.72/0.62) → systematic over-folding\n2. **OOP draw EQR path** (0.85/0.75) → over-conservative draw play (lesson #20)\n3. **Extra OOP penalties** (double barrel -0.05, big pot -0.03, lower floor 0.40) → compounding over-fold\n4. **exploit_lambda** blends noisy gift_balance into strong/medium thresholds (lesson #8)\n5. **Preflop token-hash 3bet bluff spots** (bb_vs_raise, sb_vs_reraise) → net negative speculation (lesson #10)\n6. **Higher sim counts** (900/1200/1500) → slower without benefit (lesson #4)\n\n### Why v7 is #1\nv7 = v4 base + anti-bot4 detection + river overbet + check_probe_resistance + must_continue_vs_raise. Critically, v7 has the anti-bot4 detection but **lacks** exploit_lambda, lower EQR, and preflop bluff spots. The anti-bot4 adjustments alone are manageable — they only become catastrophic when compounded with exploit_lambda that corrupts the GTO base thresholds.\n\n### Strategy: Branch from v7, Port CBet Tracking\nv7's only missing proven feature is **CBet tracking** (lesson #17). This adjusts call margin by ±0.025 based on whether the opponent's cbet_rate is above 0.60 or below 0.35. This is a small, safe, targeted addition.\n\nFor Direction B, I'll tighten the anti-bot4 detection thresholds from ±0.15/±0.13 to ±0.10/±0.08 (lesson #2 says 'Only port with VERY tight thresholds ±0.08'). Even though v7 handles loose detection better than v14, reducing false positives should marginally improve performance across the diverse opponent pool.",
  "branch_from": "claude_v7",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["opponent.py", "strategy.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to port CBet (continuation bet) tracking from v14 to v7, which is the ONLY proven positive feature from v14 that v7 lacks.\n\n## Context\nWe are evolving from v7 (current #1 bot at 1615 rating). v7 does not track opponent CBet tendency. v14 has CBet tracking which is proven to add value (experience pool lesson #17). We need to add this to v7's codebase.\n\n## What to implement\n\n### Step 1: Modify `opponent.py` — Add CBet tracking to `build_opponent_model()`\n\nInside `build_opponent_model()`, add tracking for CBet statistics. Here is the EXACT logic to port from v14's opponent.py:\n\n1. Add accumulators before the main loop:\n```python\ncbet_opportunities = 0\ncbet_count = 0\nfold_to_cbet_opportunities = 0\nfold_to_cbet_count = 0\n```\n\n2. Inside the per-action loop, add CBet detection logic:\n```python\nfacing_cbet = False\n```\nThen when processing preflop raiser actions on the flop (round==1):\n- If the opponent was the preflop raiser (they raised in round 0) and they bet/raise in round 1: increment cbet_count and cbet_opportunities, set facing_cbet = True\n- If our bot folds to a CBet: increment fold_to_cbet_count and fold_to_cbet_opportunities\n\n3. Add to the returned dictionary:\n```python\n\"cbet_rate\": smooth_rate(cbet_count, cbet_opportunities, 0.55, 4.0),\n\"fold_to_cbet\": smooth_rate(fold_to_cbet_count, fold_to_cbet_opportunities, 0.40, 3.0),\n```\n\nStudy v14's `bots/claude_v14/opponent.py` lines 34-37, 57, 72-81, 146-147 for the exact implementation. Port this logic into v7's `bots/claude_v7/opponent.py` in the `build_opponent_model()` function.\n\n### Step 2: Modify `strategy.py` — Add CBet-based call margin adjustments\n\nIn v7's `strategy.py`, inside the `get_action()` function, in the `to_call > 0` block, in the `else` (postflop) branch of the call_margin calculation, add AFTER the existing call_margin adjustments and BEFORE the `realized_rate` computation:\n\n```python\ncbet_rate = opponent_model.get(\"cbet_rate\", 0.55)\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"] and cbet_rate > 0.60:\n    call_margin -= 0.025\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"] and cbet_rate < 0.35:\n    call_margin += 0.025\n```\n\nThis means: if the opponent CBets too much (>60%), reduce our folding threshold slightly (we know their CBet range is wide). If they CBet too little (<35%), increase our folding threshold (their CBet means strength).\n\n### Important rules:\n- DO NOT modify any other functions or logic\n- DO NOT change any numeric constants or thresholds (that's Direction B's job)\n- DO NOT modify files other than opponent.py and strategy.py\n- Keep the code style consistent with v7's existing patterns\n- The `smooth_rate` function already exists in v7's opponent.py\n- Ensure cbet_rate and fold_to_cbet keys are added to the returned opponent model dictionary"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["opponent.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to modify numeric constants and thresholds. DO NOT add new functions, classes, or change control flow. DO NOT modify algorithmic logic.\n\n## Context\nWe are evolving from v7 (current #1 bot at 1615). The field is compressed to ~30pts, so small threshold improvements can make a difference. The anti-bot4 detection in v7 uses loose thresholds (±0.15/±0.13) that match ~60-80% of the opponent pool, causing false-positive exploitation adjustments against non-bot4 opponents.\n\n## Task: Tighten anti-bot4 detection thresholds in opponent.py\n\nIn `bots/claude_v7/opponent.py`, find the `detect_bot4_profile()` function. Currently it has:\n```python\nif abs(vpip - 0.58) < 0.15:      # VPIP threshold\n    score += 0.20\nif abs(pfr - 0.28) < 0.13:        # PFR threshold\n    score += 0.20\nif abs(post_aggr - 0.36) < 0.15:  # Postflop aggression threshold\n    score += 0.20\nif abs(fold_raise - 0.44) < 0.15: # Fold to raise threshold\n    score += 0.15\nif abs(aggr - 0.30) < 0.13:       # Overall aggression threshold\n    score += 0.15\n```\n\nChange these thresholds to be TIGHTER (experience pool lesson #2: 'Only port with VERY tight thresholds (±0.08)'). Here are the specific changes:\n\n```python\nif abs(vpip - 0.58) < 0.10:      # VPID: ±0.15 → ±0.10\n    score += 0.20\nif abs(pfr - 0.28) < 0.10:       # PFR: ±0.13 → ±0.10\n    score += 0.20\nif abs(post_aggr - 0.36) < 0.10: # Post aggr: ±0.15 → ±0.10\n    score += 0.20\nif abs(fold_raise - 0.44) < 0.10: # Fold to raise: ±0.15 → ±0.10\n    score += 0.15\nif abs(aggr - 0.30) < 0.08:      # Aggression: ±0.13 → ±0.08\n    score += 0.15\n```\n\nThis reduces false-positive bot4 detection from ~60-80% of opponents to ~30-40%, meaning the exploitation adjustments (bluff_freq_bonus, raise_size_bonus, etc.) will only apply when we're genuinely confident the opponent matches bot4's profile.\n\n## Important rules:\n- ONLY change the numeric threshold values in the comparison operators\n- DO NOT change any function signatures, add/remove code, or modify control flow\n- DO NOT change the center values (0.58, 0.28, 0.36, 0.44, 0.30) — these align with priors\n- DO NOT change the score weights (0.20, 0.20, 0.20, 0.15, 0.15)\n- DO NOT change the minimum score threshold (0.20)\n- DO NOT modify any other functions or files"
    }
  ]
}
```
