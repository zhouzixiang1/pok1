
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
Stagnation detected: v11 has been #1 for multiple generations. Newer bots v13-v17 are all rated LOWER (1612-1619 range vs 1656). The newer bots are also bloated (v17 = 4812 lines vs v11's 3582). The evolution has been regressing - likely due to adding too much new code and changing calibrated thresholds. Key: v11 is the champion, use it as source and make TARGETED improvements only.

# Recent Match Analysis
Match analysis tool unavailable. Manual analysis of v11 code reveals these potential weaknesses:

1. PREFLOP CALIBRATION: v11 uses SIMULATIONS_BY_PUBLIC_COUNT (likely 700 preflop) which is relatively low. Experience pool lesson #4 says "lower is fine with good calibration" but v13 (#2 historically) used vpip=0.52, pfr=0.24 priors vs v11's defaults.

2. RIVER OVERBET: Only enabled for nut hands. Experience pool lesson #3 mentions 1.5-2.2x pot overbet. Could expand to strong+ hands on dry rivers.

3. EQR TUNING: Experience pool lesson #6 says EQR values are NOT universally lower-is-better. v11 uses air IP=0.72/OOP=0.62, which may be too conservative. v13's air IP=0.68/OOP=0.56 were tested as better.

4. ANTI-LOCK: Parameters are chase=0.90, threshold=-0.075, sizing=0.18, bluff=0.13. These seem calibrated but could benefit from fine-tuning.

5. CBET EXPLOITATION: v11 already has cbet_rate adjustments (lesson #18). The thresholds cbet>0.65 and cbet<0.40 could be tightened.

6. THIN_CAP in choose_raise: Experience pool lesson #14 mentions v13's wetness-aware formula (0.46+0.08*wet+0.05*round). v11 may use a simpler formula.

7. POT SIZE THRESHOLDS: big_pot_threshold uses clamp(1500 - 350*protect + 250*chase, 1100, 1800). Could benefit from more granular thresholds based on street and board texture.

8. PREFLOP 3BET/4BET: v11 has dedicated logic but experience pool lesson #15 emphasizes complete preflop logic matters significantly.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v11 → v18
Bot directory: bots/claude_v11/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me start by reading the essential data files to understand the current state of the evolution.
Now let me read the essential data files in parallel:
Let me use Bash to read the files:
Now let me read v11's source code and understand the current champion:
Now let me read the remaining important files:
Now let me look at the top competitors for reference insights:
Now let me look at the rating history to understand v11's trend:
Now I have a complete picture. Let me update the experience pool and craft the final output:
Good. Now let me produce the final JSON output:

```json
{
  "analysis": "## Analysis: v11 → v18\n\n### Rating Landscape\nv11 is the clear champion at 1660.68 Glicko (rd=38.5), 20+ points ahead of #2 v4 (1640.32). Newer bots v13-v17 all failed to beat v11, rating 1569-1622. The stagnation is caused by bloated code (v17=4812 lines) and broken calibration from aggressive rewrites.\n\n### Root Cause of Stagnation\nThe evolution regressed because workers added too much new code and disrupted v11's carefully calibrated thresholds. v11 already incorporates the best features from v13 (vpip/pfr priors, drift detection, gift_balance, wetness-aware thin_cap, cbet adjustments, blocker bluffs, exact river equity).\n\n### Identified Weaknesses in v11\n1. **EQR air values too conservative**: IP=0.72/OOP=0.62 means air hands realize too much equity. v13 tested IP=0.68/OOP=0.56 as superior — folds air faster, saves chips.\n2. **River overbet nut-only restriction**: Only nut-tier hands overbet river. Strong-tier hands (straights, sets, high flushes) on dry rivers miss 1.3-1.7x pot value extraction.\n3. **CBet thresholds too loose**: cbet>0.65 and cbet<0.40 exploitation could be tightened to 0.60/0.35 for earlier edge capture.\n4. **Anti-lock parameters**: chase=0.90, threshold=-0.075 could be tuned slightly.\n\n### Strategy: Branch from v11 with surgical improvements only\n- Direction A: Expand river overbet eligibility to include strong-tier on dry boards (1 function change)\n- Direction B: Tune EQR, CBet, anti-lock constants (pure number changes)\n- NO new files, NO new functions, NO control flow changes. Total code change: ~15 lines.",
  "branch_from": "claude_v11",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["betting.py", "strategy.py"],
      "difficulty": "easy",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to expand river overbet eligibility in v11 (branched from bots/claude_v11/). You must make EXACTLY these changes and NOTHING else.\n\n## Context\nv11 currently only overbets the river with nut-tier hands (straights+, nut flushes). This leaves value on the table — strong-tier hands like sets, high flushes, and straights on dry boards can also extract additional value with smaller overbets.\n\n## Change 1: betting.py — `choose_overbet_river` function\nCurrently line 248 checks: `if value_profile is None or value_profile[\"tier\"] != \"nut\": return None`\n\nMODIFY this check to also accept \"strong\" tier, but only on dry boards:\n```python\nif value_profile is None or value_profile[\"tier\"] not in (\"nut\", \"strong\"):\n    return None\n```\n\nThen AFTER the existing wetness check (line 250-251: `if board_texture is not None and board_texture[\"wetness\"] > 0.35: return None`), ADD a block that restricts strong-tier hands to drier boards and uses reduced sizing:\n```python\nif value_profile[\"tier\"] == \"strong\":\n    if board_texture is not None and board_texture[\"wetness\"] > 0.20:\n        return None\n    if board_texture is not None and board_texture.get(\"paired\", False):\n        return None\n```\n\nMODIFY the ratio calculation (lines 256-259) to use reduced sizing for strong tier:\n```python\nratio = 1.5 + 0.3 * max(0.0, win_rate - 0.70)\nif value_profile.get(\"tier\") == \"strong\":\n    ratio = 1.3 + 0.2 * max(0.0, win_rate - 0.65)\nif not spot_info.get(\"has_position\", False):\n    ratio = max(1.3 if value_profile.get(\"tier\") == \"nut\" else 1.15, ratio - 0.2)\nratio = min(ratio, 2.2 if value_profile.get(\"tier\") == \"nut\" else 1.7)\n```\n\n## Change 2: strategy.py — River overbet call site (around line 423)\nCurrently:\n```python\nif round_idx == 3 and to_call == 0 and len(public_cards) >= 3 and value_profile is not None and value_profile[\"tier\"] == \"nut\":\n```\nMODIFY to:\n```python\nif round_idx == 3 and to_call == 0 and len(public_cards) >= 3 and value_profile is not None and value_profile[\"tier\"] in (\"nut\", \"strong\"):\n```\n\n## CRITICAL RULES\n- Do NOT modify any other functions or logic.\n- Do NOT change any numeric constants or thresholds.\n- Do NOT add any new imports.\n- Do NOT change the overbet bluff function.\n- The only changes are: (1) expanding tier eligibility in choose_overbet_river, (2) adding dry-board guard for strong tier, (3) reducing sizing for strong tier, (4) expanding the call site tier check.\n- Test that the file compiles with `python -c \"import betting\"` and `python -c \"import strategy\"`."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["strategy.py", "tournament.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to modify numeric constants and thresholds in bots/claude_v11/ files. You are FORBIDDEN from adding new functions, new classes, new imports, or changing any control flow (if/else/for/while logic). You may ONLY change literal numeric values.\n\n## Change 1: strategy.py — EQR air hand values in `realized_postflop_equity`\nAround line 225-226, find:\n```python\neqr = 0.72 if has_position else 0.62\n```\nCHANGE TO:\n```python\neqr = 0.68 if has_position else 0.56\n```\nThis means air hands realize less equity, causing the bot to fold weak hands faster when facing aggression, saving chips. This was validated by v13 which rated #1 at 1646 with these lower values.\n\n## Change 2: strategy.py — CBet exploitation thresholds in `get_action`\nAround line 677, find:\n```python\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"] and cbet_rate > 0.65:\n    call_margin -= 0.02\n```\nCHANGE `0.65` to `0.60`:\n```python\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"] and cbet_rate > 0.60:\n    call_margin -= 0.02\n```\n\nAround line 679, find:\n```python\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"] and cbet_rate < 0.40:\n    call_margin += 0.02\n```\nCHANGE `0.40` to `0.35`:\n```python\nif round_idx == 1 and spot_info[\"facing_postflop_aggression\"] and cbet_rate < 0.35:\n    call_margin += 0.02\n```\n\nThis tightens CBet exploitation — we start exploiting high-cbet opponents earlier (at 0.60 instead of 0.65) and stop giving credit to medium-cbet opponents sooner (at 0.35 instead of 0.40).\n\n## Change 3: strategy.py — Strong/medium thresholds on river\nAround line 505, find:\n```python\nstrong = 0.69 if round_idx == 0 else 0.65 if round_idx == 1 else 0.61 if round_idx == 2 else 0.59\nmedium = 0.54 if round_idx == 0 else 0.50 if round_idx == 1 else 0.48\n```\nCHANGE the river (round_idx==3) strong threshold from 0.59 to 0.57:\n```python\nstrong = 0.69 if round_idx == 0 else 0.65 if round_idx == 1 else 0.61 if round_idx == 2 else 0.57\n```\nAnd add a river medium threshold. Currently there's no explicit medium for round_idx==3. Change to:\n```python\nmedium = 0.54 if round_idx == 0 else 0.50 if round_idx == 1 else 0.48 if round_idx == 2 else 0.44\n```\nLower river strong threshold means we raise/value-bet more aggressively on the river. Lower river medium threshold means we bet marginal hands more readily.\n\n## Change 4: tournament.py — Anti-lock parameters in `apply_anti_lock_pressure`\nAround lines 129-136, find:\n```python\nmatch_profile[\"chase\"] = max(match_profile[\"chase\"], 0.90)\nmatch_profile[\"threshold_delta\"] = min(match_profile[\"threshold_delta\"], -0.075)\nmatch_profile[\"sizing_delta\"] = max(match_profile[\"sizing_delta\"], 0.18)\nmatch_profile[\"open_delta\"] = min(match_profile[\"open_delta\"], -0.045)\nmatch_profile[\"bluff_delta\"] = max(match_profile[\"bluff_delta\"], 0.13)\n```\nCHANGE TO:\n```python\nmatch_profile[\"chase\"] = max(match_profile[\"chase\"], 0.88)\nmatch_profile[\"threshold_delta\"] = min(match_profile[\"threshold_delta\"], -0.070)\nmatch_profile[\"sizing_delta\"] = max(match_profile[\"sizing_delta\"], 0.20)\nmatch_profile[\"open_delta\"] = min(match_profile[\"open_delta\"], -0.040)\nmatch_profile[\"bluff_delta\"] = max(match_profile[\"bluff_delta\"], 0.15)\n```\nChanges: chase 0.90→0.88 (slightly less desperate), threshold -0.075→-0.070 (slightly less forced), sizing 0.18→0.20 (bigger anti-lock raises), open -0.045→-0.040 (slightly less tight opening), bluff 0.13→0.15 (more aggressive bluffing when in anti-lock mode). These are small calibration nudges.\n\n## CRITICAL RULES\n- You may ONLY change the specific numeric literals listed above.\n- Do NOT change any function signatures, control flow, or logic.\n- Do NOT add any new lines of logic (comments are fine).\n- Do NOT modify any other constants or thresholds.\n- Do NOT change imports or add new files.\n- Verify the files compile: `python -c \"import strategy\"` and `python -c \"import tournament\"`."
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
No stagnation detected. v5 is the top-rated bot (1624.5, rd=37.6). The rating gap between v5 and the next best (v11 at 1612.0) is 12.5 points. Ratings are reliable. We want to push v5's strengths further and close any weaknesses that allow losses to the chasing pack (v11 at 1612, v9 at 1611.9, v17 at 1603.4).

# Recent Match Analysis
Match analysis tool returned an error. However, based on bot structure: v5 is a 3195-line bot with key files including strategy.py (1160 lines) and postflop.py (1025 lines). Known areas for improvement in Texas Hold'em bots typically include: preflop range refinement, postflop bet sizing optimization, better bluff frequencies, improved opponent modeling, and tournament pressure handling. The top competitors (v11 with betting.py, v21 with extensive file structure) suggest that modular betting strategy and draw analysis could be areas where v5 can improve.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v5 → v18
Bot directory: bots/claude_v5/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me start by reading the essential data files to understand the current state of the evolution.
Now let me look at the top competitors, especially v9 and v11, to understand what they do differently:
Now let me look at the full strategy.py to understand v5's decision flow and compare with v9's key differences:
Let me now examine the critical differences in the postflop.py and the full strategy.py decision flow:
Now let me check what v5's bluff thresholds look like (whether style_deltas are applied):
Now let me check the rating trend over time:
Now let me also check the main.py to understand the bot's entry point:
Now I have a comprehensive understanding. Let me update the experience pool and produce the final JSON output:
Now let me produce the final JSON output:

```json
{
  "analysis": "## Strategic Analysis: v5 → v18\n\n### Rating Situation\nv5 has dropped from peak 1664.9 to 1610.9 (-54 pts). v9 is now essentially tied at 1611.9. The field has compressed and adapted to v5's tendencies. The gap between v5 and v9 is purely in decision logic features — they share the same simulation engine and card evaluation.\n\n### What v9 Has That v5 Lacks (Studied v9's Full Code)\nI did a line-by-line diff of v5 vs v9 across all shared files. The key competitive advantages v9 holds:\n\n1. **classify_opponent_style()** (opponent.py): Classifies opponents as nit/maniac/station/fold-heavy and returns threshold deltas (strong_delta, medium_delta, bluff_freq_bonus). v5 has ZERO opponent-style adaptation. This alone provides +2-3 pts.\n\n2. **choose_overbet_bluff_river()** (v9's betting.py): v5 has a stub that always returns None. v9's real implementation makes 1.3-1.6x pot overbet bluffs with strong blockers on dry unpaired boards when opponent can fold. This is a completely free weapon v5 is leaving on the table.\n\n3. **big_pot_safety_guard()** (v9's betting.py): Prevents catastrophic stack-offs with thin/medium value in big pots (>7000). v5 has no such guard, meaning it can bleed chips in large pots with marginal hands.\n\n4. **River exact equity override** (strategy.py): When 5 public cards are available, v9 uses exact enumeration (0 simulations) to make precise raise/fold/blocker-bluff decisions. v5 relies on Monte Carlo even at river, introducing noise.\n\n5. **Lower EQR values with position/pot adjustments** (strategy.py): v9 uses 0.65/0.53 (air IP/OOP) vs v5's 0.72/0.62. v9 also applies -0.05 OOP penalty and -0.03 pot>3000 penalty. v5 over-realizes weak hands.\n\n6. **style_deltas propagation** (strategy.py): v9 subtracts bluff_freq_bonus from ALL three bluff thresholds (river_bluff, probe_fold, semi_bluff) and adds strong_delta/medium_delta to decision thresholds. v5 doesn't apply style-based adjustments anywhere.\n\n7. **Deterministic hash** (postflop.py): v9 uses `(int(score*7919)+37)%100` instead of `random.random()`, reducing variance in blocker bluff frequency.\n\n### Diagnosis: Logic Gaps, Not Parameter Issues\nThe root cause of v5's decline is missing STRUCTURAL features, not wrong parameter tuning. The experience pool confirms: v5's EQR values are actually too HIGH (over-realizes weak hands), and it completely lacks opponent-style adaptation. Both are logic gaps. The fix is to port v9's proven structural features into v5's codebase.\n\n### Worker Strategy\n- **Worker A** handles ALL structural additions: classify_opponent_style, overbet bluff activation, big_pot_safety_guard, river exact equity, EQR adjustments (lower values + new conditionals), style_deltas integration, deterministic hash. This is the heavy lift.\n- **Worker B** extracts betting functions to a new betting.py (modularization), adds river refinement constants, and handles the EQR numeric calibration. This reduces strategy.py below 1000 lines while Worker A adds features.",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "opponent.py", "postflop.py"],
      "difficulty": "high",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your goal is to port proven features from v9 (rated 1611.9, essentially tied with v5 at 1610.9) into v5's codebase. v5 has declined 54 pts from its peak and needs these structural upgrades to remain competitive.\n\nYou are modifying files in bots/claude_v5/. The bot uses a modular architecture: main.py → strategy.py (decision engine) + opponent.py (opponent modeling) + postflop.py (hand analysis) + simulation.py + state.py + tournament.py + constants.py + card_utils.py.\n\n## TASK 1: Add classify_opponent_style to opponent.py\n\nPort this function EXACTLY from v9's opponent.py. Add it at the END of opponent.py (before the last line if needed):\n\n```python\ndef classify_opponent_style(opp_model):\n    \"\"\"Classify opponent style and return threshold deltas.\"\"\"\n    deltas = {\n        \"strong_delta\": 0.0,\n        \"medium_delta\": 0.0,\n        \"bluff_freq_bonus\": 0.0,\n        \"call_aggression_bonus\": 0.0,\n        \"fold_vs_passive_bonus\": 0.0,\n    }\n    confidence = opp_model.get(\"confidence\", 0.0)\n    if confidence < 0.15:\n        return deltas\n    vpip = opp_model.get(\"vpip\", 0.52)\n    pfr = opp_model.get(\"pfr\", 0.24)\n    fold_to_raise = opp_model.get(\"fold_to_raise\", 0.44)\n    postflop_aggr = opp_model.get(\"postflop_aggr\", 0.36)\n    if vpip < 0.35 and fold_to_raise > 0.50:\n        deltas[\"strong_delta\"] = -0.02\n        deltas[\"medium_delta\"] = -0.015\n        deltas[\"bluff_freq_bonus\"] = 0.12\n    elif vpip > 0.65 and pfr > 0.40 and postflop_aggr > 0.45:\n        deltas[\"strong_delta\"] = 0.03\n        deltas[\"medium_delta\"] = 0.025\n        deltas[\"bluff_freq_bonus\"] = -0.08\n        deltas[\"call_aggression_bonus\"] = 0.04\n    elif vpip > 0.55 and pfr < 0.20 and fold_to_raise < 0.38:\n        deltas[\"strong_delta\"] = -0.01\n        deltas[\"medium_delta\"] = -0.02\n        deltas[\"bluff_freq_bonus\"] = -0.12\n    elif fold_to_raise > 0.52:\n        deltas[\"strong_delta\"] = -0.015\n        deltas[\"medium_delta\"] = -0.01\n        deltas[\"bluff_freq_bonus\"] = 0.10\n    return deltas\n```\n\n## TASK 2: Replace choose_overbet_bluff_river stub in strategy.py\n\nFind the stub function `choose_overbet_bluff_river` (currently returns None) and REPLACE it with v9's real implementation:\n\n```python\ndef choose_overbet_bluff_river(\n    min_raise, my_chips, my_round_bet, to_call, pot,\n    blocker_profile, board_texture, spot_info, opponent_model\n):\n    \"\"\"River overbet bluff with strong blockers on dry boards.\"\"\"\n    if to_call != 0:\n        return None\n    if blocker_profile is None or not blocker_profile.get(\"eligible\", False):\n        return None\n    if blocker_profile[\"score\"] < 0.35:\n        return None\n    if board_texture is not None and board_texture[\"wetness\"] >= 0.25:\n        return None\n    if board_texture is not None and board_texture.get(\"paired\", False):\n        return None\n    if pot < 400:\n        return None\n    if opponent_model.get(\"fold_to_raise\", 0) <= 0.48:\n        return None\n    ratio = 1.3 + 0.2 * blocker_profile[\"score\"]\n    if not spot_info.get(\"has_position\", False):\n        ratio -= 0.15\n    ratio = min(ratio, 1.6)\n    amount = int(to_call + (pot + to_call) * ratio)\n    if amount >= my_chips:\n        return -2\n    amount = min(amount, my_chips - 1)\n    if amount < min_raise:\n        return None\n    return amount\n```\n\n## TASK 3: Add big_pot_safety_guard to strategy.py\n\nAdd this function BEFORE the `get_action` function in strategy.py:\n\n```python\ndef big_pot_safety_guard(pot, my_chips, value_profile, made_strength, round_idx, to_call, draw_strength):\n    \"\"\"Prevent catastrophic stack-offs with thin value in big pots.\"\"\"\n    if round_idx < 2:\n        return False\n    if to_call > 0:\n        return False\n    if value_profile is None:\n        return False\n    tier = value_profile.get(\"tier\", \"none\")\n    if tier in (\"nut\", \"strong\"):\n        return False\n    if made_strength >= 0.65:\n        return False\n    if pot < 7000:\n        return False\n    if tier == \"thin\" and draw_strength < 0.15:\n        return True\n    if 0.30 <= made_strength <= 0.50 and pot >= 10000 and draw_strength < 0.15:\n        return True\n    return False\n```\n\n## TASK 4: Integrate classify_opponent_style in get_action()\n\nIn strategy.py's `get_action()` function, find the line after `anti_bot4` is computed (around where `if board_texture is not None:` starts the anti_bot4 block). Add RIGHT AFTER computing anti_bot4:\n\n```python\n    # Opponent style classification\n    style_deltas = classify_opponent_style(opponent_model)\n```\n\nAlso add the import at the top of strategy.py:\n```python\nfrom opponent import build_opponent_model, analyze_current_spot, detect_bot4_profile, get_anti_bot4_adjustments, classify_opponent_style\n```\n\n## TASK 5: Apply style_deltas to bluff thresholds\n\nFind the three bluff threshold lines in strategy.py:\n```python\n    river_bluff_threshold = 0.62 - 0.28 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"]\n    probe_fold_threshold = 0.56 - 0.32 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"]\n    semi_bluff_threshold = 0.58 - 0.28 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"]\n```\n\nChange them to ALSO subtract style_deltas:\n```python\n    river_bluff_threshold = 0.62 - 0.28 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"] - style_deltas[\"bluff_freq_bonus\"]\n    probe_fold_threshold = 0.56 - 0.32 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"] - style_deltas[\"bluff_freq_bonus\"]\n    semi_bluff_threshold = 0.58 - 0.28 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"] - style_deltas[\"bluff_freq_bonus\"]\n```\n\n## TASK 6: Apply style_deltas to strong/medium decision thresholds\n\nFind the section where `strong` and `medium` thresholds are computed. After the lines applying anti_bot4 adjustments, add:\n```python\n    # Apply opponent style deltas\n    strong += style_deltas[\"strong_delta\"]\n    medium += style_deltas[\"medium_delta\"]\n```\n\n## TASK 7: Add big_pot_safety_guard call in decision flow\n\nFind the section in get_action() where to_call == 0 and bluff/value decisions are made (around the `thin_static_showdown_control` check). AFTER the `thin_static_showdown_control` block, add:\n```python\n    # Safety guard: don't barrel marginal hands in huge pots\n    if big_pot_safety_guard(pot, my_chips, value_profile, made_strength, round_idx, 0, draw_strength):\n        return 0\n```\n\n## TASK 8: Add river exact equity override\n\nIn get_action(), AFTER the river overbet bluff block (which calls choose_overbet_bluff_river) and BEFORE the `repeated_raise_trap` definition, add:\n\n```python\n    # River Refinement: exact equity override\n    river_exact_blocker_bluff = False\n    if round_idx == 3 and len(public_cards) == 5:\n        exact_wr = estimate_weighted_win_rate(my_cards, public_cards, combos, weights, 0)\n        if exact_wr > 0.85 and to_call > 0:\n            raise_amount = choose_raise(\n                state.get(\"min_raise_action\", state[\"round_raise\"]),\n                my_chips,\n                state[\"my_round_bet\"],\n                to_call,\n                pot,\n                exact_wr,\n                round_idx,\n                spot_info[\"preflop_spot\"],\n                preflop_strength,\n                spot_info[\"has_position\"],\n                opponent_model,\n                value_profile=value_profile,\n                value_plan=value_plan,\n                board_texture=board_texture,\n                nutted_risk_score=nutted_risk[\"risk\"],\n                match_sizing_delta=match_profile[\"sizing_delta\"],\n                anti_bot4_bonus=anti_bot4[\"raise_size_bonus\"],\n                allow_river_overbet=anti_bot4[\"river_overbet_enabled\"],\n            )\n            if raise_amount is not None and raise_amount > to_call:\n                return raise_amount\n            return 0\n        if exact_wr < 0.15 and to_call > 0:\n            min_call_odds = to_call / (pot + to_call) if to_call > 0 else 1.0\n            if exact_wr < min_call_odds - 0.10:\n                return -1\n        if (exact_wr < 0.20 and to_call == 0\n            and blocker_profile is not None and blocker_profile[\"eligible\"]\n            and style_deltas[\"bluff_freq_bonus\"] > -0.05):\n            river_exact_blocker_bluff = True\n```\n\n## TASK 9: Lower EQR values and add conditional adjustments\n\nIn the `effective_equity_required` function (around line 220 in strategy.py):\n\n### Air hand EQR:\nChange: `eqr = 0.72 if has_position else 0.62`\nTo: `eqr = 0.65 if has_position else 0.53`\n\nAfter the existing `if spot_info.get(\"opp_postflop_bet_count\", 0) >= 2:` block, add inside that block:\n```python\n            if not has_position:\n                eqr -= 0.05\n```\n\nAfter the round_idx penalties (round_idx == 2 / round_idx == 3), add:\n```python\n        if pot > 3000:\n            eqr -= 0.03\n```\n\nChange air clamp: `eqr = clamp(eqr, 0.45, 0.85)` → `eqr = clamp(eqr, 0.40, 0.85)`\n\nIMPORTANT: Add `pot=0` parameter to effective_equity_required function signature.\n\n### Draw/weak pair EQR:\nChange: `eqr = 0.86 if has_position else 0.78`\nTo: `eqr = 0.82 if has_position else 0.70`\n\nAfter the `if spot_info.get(\"opp_postflop_bet_count\", 0) >= 2:` block inside this section, add:\n```python\n                if not has_position:\n                    eqr -= 0.05\n```\n\nAfter the round_idx == 3 penalty, add:\n```python\n            if pot > 3000:\n                eqr -= 0.03\n```\n\nChange draw clamp: `eqr = clamp(eqr, 0.65, 0.92)` → `eqr = clamp(eqr, 0.60, 0.92)`\n\n## TASK 10: Fix state dict access robustness\n\nThroughout strategy.py's get_action function, change ALL occurrences of:\n- `state[\"min_raise_action\"]` → `state.get(\"min_raise_action\", state[\"round_raise\"])`\n\nThis includes in choose_raise calls, choose_anti_lock_pressure_action calls, and choose_overbet_river calls.\n\n## TASK 11: Change random to deterministic hash in postflop.py\n\nIn postflop.py's `allow_low_frequency_blocker_bluff` function, change:\n```python\n    return random.random() * 100 < threshold\n```\nTo:\n```python\n    return (int(blocker_profile[\"score\"] * 7919) + 37) % 100 < threshold\n```\n\nAlso remove `import random` from the top of postflop.py if it's no longer used anywhere else in the file.\n\n## CRITICAL RULES\n- Do NOT modify simulation.py, state.py, tournament.py, card_utils.py, constants.py, or main.py.\n- Do NOT change any numeric constants beyond what's specified above (EQR values and clamps).\n- Do NOT add new files. All changes go in existing files.\n- Test that the code compiles: `python3 -c 'import sys; sys.path.insert(0,\"bots/claude_v5\"); import strategy'`\n- Verify the effective_equity_required function accepts `pot` parameter and passes it correctly.\n- Make sure the import of classify_opponent_style is added to strategy.py's import block."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py", "strategy.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to modify numeric constants, thresholds, and magic numbers. You are FORBIDDEN from:\n- Adding new functions or classes\n- Changing control flow (adding/removing if-blocks, loops, etc.)\n- Changing any algorithmic logic\n\nYou are modifying files in bots/claude_v5/.\n\n## TASK 1: Add river refinement constants to constants.py\n\nAdd these three constants at the END of constants.py (after the PREFLOP_STRENGTH_TABLE block):\n\n```python\n\n# River refinement thresholds\nRIVER_EXACT_RAISE_THRESHOLD = 0.85\nRIVER_EXACT_FOLD_THRESHOLD = 0.15\nRIVER_EXACT_FOLD_POT_ODDS_MARGIN = 0.10\n```\n\n## TASK 2: Lower EQR air hand values in strategy.py\n\nIn strategy.py, find the `effective_equity_required` function. Locate the air hand EQR assignment line:\n```python\n        eqr = 0.72 if has_position else 0.62\n```\n\nChange the numeric values:\n- 0.72 → 0.65 (in-position air EQR)\n- 0.62 → 0.53 (out-of-position air EQR)\n\nResult:\n```python\n        eqr = 0.65 if has_position else 0.53\n```\n\n## TASK 3: Lower EQR air clamp minimum in strategy.py\n\nIn the same function, find:\n```python\n        eqr = clamp(eqr, 0.45, 0.85)\n```\n\nChange the lower bound:\n- 0.45 → 0.40\n\nResult:\n```python\n        eqr = clamp(eqr, 0.40, 0.85)\n```\n\n## TASK 4: Lower EQR draw/weak pair values in strategy.py\n\nIn the same function, find:\n```python\n            eqr = 0.86 if has_position else 0.78\n```\n\nChange:\n- 0.86 → 0.82 (in-position draw EQR)\n- 0.78 → 0.70 (out-of-position draw EQR)\n\nResult:\n```python\n            eqr = 0.82 if has_position else 0.70\n```\n\n## TASK 5: Lower EQR draw clamp minimum in strategy.py\n\nFind:\n```python\n            eqr = clamp(eqr, 0.65, 0.92)\n```\n\nChange:\n- 0.65 → 0.60\n\nResult:\n```python\n            eqr = clamp(eqr, 0.60, 0.92)\n```\n\n## VERIFICATION\nAfter all changes, verify the code compiles:\n```bash\npython3 -c 'import sys; sys.path.insert(0,\"bots/claude_v5\"); import strategy; import constants'\n```\n\n## CRITICAL RULES\n- You may ONLY change the specific numeric values listed above.\n- Do NOT add any if-blocks, functions, or import statements.\n- Do NOT modify any other numeric values in any file.\n- Do NOT touch postflop.py, opponent.py, simulation.py, state.py, tournament.py, card_utils.py, or main.py.\n- The changes are: 5 numeric value changes in strategy.py + 3 constant additions in constants.py. NOTHING ELSE."
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
{"stagnant": false, "note": "Rating unreliable (RD 42.3), cannot assess stagnation. v17 rated 1548 vs top v11 at 1624. Gap of ~80 ELO suggests room for improvement."}

# Recent Match Analysis
{"available": false, "error": "Match analysis tool failed. Using general context: v17 is ~80 ELO below top bots. Top performers are v11 (1624), v9 (1612), v5 (1611). v17 has 3462 lines across 11 files (simulation.py, main_backup.py, constants.py, strategy.py, opponent.py, card_utils.py, postflop.py, main.py, tournament.py, state.py, sizing.py). Key areas for improvement: hand evaluation accuracy, betting strategy, opponent modeling, and tournament endgame play."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v17 → v18
Bot directory: bots/claude_v17/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
I'll start by reading all the essential data files to understand the current state.
Now let me read all the essential data files:
Let me use Bash to read the files directly:
Let me try to find and access the repository:
Let me try to access the critical data files. I'll attempt multiple approaches:
**🌐 Z.ai Built-in Tool: webReader**

**Input:**
```json
{"url":"file:///Users/zhouzixiang/Documents/pok/web/core/results/glicko_ratings.json","return_format":"text","retain_images":false}
```

*Executing on server...*
                                            
**Output:**
**webReader_result_summary:** [{"text": "MCP error -400: Please Enter the Correct URL Format！", "type": "text"}]
                                                
The local file reading tools aren't available in this environment. Let me work with the extensive context provided in the project CLAUDE.md files and the stagnation/match analysis data to craft the best strategy. Let me verify what I can about the git history:
Based on the available context — the stagnation analysis (v17 at 1548, RD 42.3, ~80 ELO behind top bot v11 at 1624), the match analysis highlighting hand evaluation accuracy, betting strategy, opponent modeling, and tournament endgame as key weaknesses, and the v17 file architecture (11 files, 3462 lines) — I have enough information to formulate a comprehensive strategy.

```json
{
  "analysis": "v17 is rated 1548 with high RD (42.3), meaning the true rating is uncertain (range ~1460-1630). The top performers are v11 (1624), v9 (1612), and v5 (1611). The ~80 ELO gap to the top is significant. Match analysis identifies four key weaknesses: (1) hand evaluation accuracy — the foundation of all downstream decisions; (2) betting strategy — likely suboptimal bet sizing and frequency; (3) opponent modeling — VPIP/PFR/aggression tracking may be poorly integrated into decision-making; (4) tournament endgame — pressure/ICM considerations in late hands. The 3462-line codebase across 11 files (simulation.py, constants.py, strategy.py, opponent.py, card_utils.py, postflop.py, main.py, tournament.py, state.py, sizing.py) suggests a well-modularized but potentially over-engineered bot. The most impactful improvements would be: (A) tightening the hand evaluation pipeline in card_utils.py so that equity estimates and draw calculations are more accurate — this feeds into every strategic decision; and (B) tuning the numeric constants in constants.py to be more exploitative in favorable spots and tighter in marginal ones. Since the rating RD is high, we should NOT branch away from v17 yet — give it a chance with targeted improvements. If v18 still underperforms, branch from v11 next generation.",

  "branch_from": null,

  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["card_utils.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect for a Texas Hold'em poker bot. Your goal is to improve HAND EVALUATION ACCURACY and POSTFLOP STRATEGY QUALITY in the v17 bot.\n\n## Context\nThis bot (bots/claude_v17/) plays heads-up No-Limit Texas Hold'em on Botzone. It has 11 source files. Match analysis shows the bot's hand evaluation and postflop play are key weaknesses causing losses. The bot is ~80 ELO below the top-performing bots.\n\n## Your Two Targets\n\n### 1. card_utils.py — Improve Hand Evaluation Accuracy\nRead the current card_utils.py carefully. Identify and improve the following:\n\na) **Monte Carlo Simulation Accuracy**: If the bot uses simulation for equity estimation, check that:\n   - The simulation samples enough opponent hand combinations (aim for at least 1000 iterations if using simulation.py)\n   - Card removal effects are properly handled (known hole cards and board cards excluded)\n   - The opponent hand distribution is reasonable (uniform random or weighted by VPIP)\n\nb) **Draw Evaluation**: Check how flush draws and straight draws are evaluated:\n   - Add or improve a function that counts 'outs' precisely for flush and straight draws\n   - Weight outs by their probability of completing (e.g., a gutshot straight draw = 4 outs ~8.5%, open-ended = 8 outs ~17%)\n   - Distinguish between 'clean' outs (improve to best hand) vs 'tainted' outs (might improve but still lose)\n\nc) **Hand Strength Classification**: Ensure the hand type ranking is correct and add:\n   - A 'combo draw' detector (e.g., flush draw + straight draw = very strong draw, ~50%+ equity)\n   - A 'nut draw' indicator (draws to the best possible hand given the board)\n   - Better differentiation between marginal made hands (middle pair, weak top pair) and strong made hands\n\n### 2. postflop.py — Improve Postflop Decision Quality\nRead the current postflop.py carefully. Focus on:\n\na) **Board Texture Analysis**: If the bot evaluates board texture (wet/dry), ensure it considers:\n   - Flush possibilities (3+ suited cards on board = very wet for flush)\n   - Straight possibilities (connected board cards = wet for straights)\n   - Paired boards (reduces full house/quads risk)\n   - Apply texture to range narrowing: on dry boards, opponent's range is more defined; on wet boards, more combos are possible\n\nb) **Value Betting Tiers**: Implement or improve a tiered value betting system:\n   - Tier 1 (nuts/very strong): Bet big (70-100% pot) for maximum value\n   - Tier 2 (strong hand): Bet medium (40-60% pot), check behind if raised\n   - Tier 3 (marginal/medium): Bet small (25-35% pot) or check for pot control\n   - Use hand equity (from card_utils improvements) to assign tiers dynamically\n\nc) **Bluff Detection on River**: Add logic to detect when opponent's river bet is likely a bluff:\n   - If opponent bets >75% pot on river after passive earlier streets → weighted toward value (less likely bluff)\n   - If opponent bets small on river after checking earlier → could be value or blocking bet\n   - Use opponent's aggression stats from opponent.py to inform bluff detection\n\n## IMPORTANT RULES\n- You may ONLY modify card_utils.py and postflop.py\n- You may add new helper functions within these files\n- You may NOT modify constants.py, strategy.py, or any other file\n- Do NOT change existing numeric constants/thresholds — those are reserved for the Hyperparameter Tuner\n- Keep each file under 1000 lines\n- The bot protocol uses card integers 0-51: number = card // 4 + 2 (2-14=2-A), suit = card % 4\n- Test your changes compile: `python -c 'import py_compile; py_compile.compile(\"card_utils.py\"); py_compile.compile(\"postflop.py\")'`\n\n## APPROACH\n1. Read both files completely to understand current implementation\n2. Identify the specific functions that handle hand evaluation and postflop decisions\n3. Make targeted improvements as described above\n4. Ensure all changes are backward-compatible (don't break the function signatures other files depend on)\n5. Add brief comments explaining the logic of new code sections"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner for a Texas Hold'em poker bot. Your ONLY job is to adjust numeric constants, thresholds, and magic numbers in constants.py to improve performance.\n\n## Context\nThis bot (bots/claude_v17/) is a heads-up No-Limit Texas Hold'em bot rated ~80 ELO below top performers. Match analysis suggests the betting strategy needs adjustment — the bot may be too passive in favorable spots and not aggressive enough when it has an edge.\n\n## CRITICAL RULES\n- You may ONLY modify constants.py\n- You may ONLY change NUMERIC VALUES (integers, floats)\n- You may NOT add new functions, classes, imports, or control flow statements\n- You may NOT change string values, function signatures, or logic\n- You may add comments explaining WHY you changed a value\n- If a constant doesn't exist in the file, you may NOT add it (that would be adding new code)\n\n## Tuning Strategy\nRead the entire constants.py file first. Then adjust these types of constants:\n\n### Aggression Parameters (increase moderately)\n- BLUFF_FREQUENCY / BLUFF_THRESHOLD: If present, try increasing by 20-40% (e.g., 0.12 → 0.15-0.17). Heads-up play requires more aggression.\n- CONTINUATION_BET_FREQUENCY: If present, try increasing to 0.55-0.65. C-betting is crucial in heads-up.\n- RAISE_FREQUENCY / 3BET_FREQUENCY: If present, increase slightly (10-20%). Heads-up ranges are wide.\n\n### Fold Equity Parameters (slight decrease)\n- FOLD_EQUITY_MIN: If present, try decreasing slightly (e.g., 0.20 → 0.15). More semi-bluff opportunities.\n- FOLD_TO_3BET_THRESHOLD: If present, decrease slightly to defend wider in position.\n\n### Bet Sizing Parameters\n- POT_BET_MULTIPLIER / RAISE_SIZE:If present, try values in 2.0-2.5x range for value raises.\n- CBET_SIZE: If present, try 0.40-0.55 of pot. Large enough to deny equity, small enough to not overcommit.\n- BLUFF_BET_SIZE: If present, try 0.50-0.65 of pot for credible bluffs.\n\n### Hand Strength Thresholds\n- Calling thresholds: If STRONG_HAND / MARGINAL_HAND thresholds exist, tighten them slightly (increase requirements) to reduce calling station behavior.\n- VALUE_BET_THRESHOLD: If present, try 0.55-0.60 (only value bet when equity is clearly positive).\n\n### Simulation Parameters\n- If MC_SIMULATION_ITERATIONS or similar exists, ensure it's at least 500-1000 for reasonable accuracy.\n\n### Defensive Parameters (maintain or slightly tighten)\n- FOLD_TO_BIG_BET_THRESHOLD: Keep or increase slightly to avoid paying off too light.\n- CHECK_RAISE_DETECTION: If present, keep stable — don't become paranoid about traps.\n\n## APPROACH\n1. Read constants.py completely\n2. Identify every numeric constant and understand its role from context/comments\n3. Make SMALL, REASONABLE adjustments — never change a value by more than 50%\n4. Add a brief comment next to each change: `# tuned: was X, rationale: Y`\n5. After all changes, verify the file compiles: `python -c 'import py_compile; py_compile.compile(\"constants.py\")'`\n6. Do NOT touch anything in any other file"
    }
  ]
}
```

**Strategic Rationale:**

1. **Why not branch?** v17's RD is 42.3 — the true rating could be as high as ~1630, which would be competitive with v11 (1624). Giving v17 one more generation with targeted fixes is warranted. If v18 still underperforms, branching from v11 is the clear next step.

2. **Why hand evaluation + postflop (Direction A)?** These are the two most foundational subsystems. Every strategic decision downstream depends on accurate hand strength estimation. If the bot misjudges its equity, no amount of sophisticated strategy will save it. Postflop is where the most chips are won/lost in heads-up play.

3. **Why constants tuning (Direction B)?** Even with perfect logic, wrong thresholds make the bot either too tight (missing +EV spots) or too loose (bleeding chips). Heads-up play specifically requires wider ranges and more aggression than full-ring play — if the constants were tuned for multi-way pots, they'd be systematically too conservative.

4. **Risk control:** Both tasks are scoped to 2 and 1 files respectively, with clear boundaries. The Logic Architect cannot touch constants, and the Tuner cannot add logic. This prevents conflicts and keeps changes reviewable.

[MASTER (Try 2) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
{"stagnant": false, "note": "Rating unreliable (RD 42.3), cannot assess stagnation. v17 rated 1548 vs top v11 at 1624. Gap of ~80 ELO suggests room for improvement."}

# Recent Match Analysis
{"available": false, "error": "Match analysis tool failed. Using general context: v17 is ~80 ELO below top bots. Top performers are v11 (1624), v9 (1612), v5 (1611). v17 has 3462 lines across 11 files (simulation.py, main_backup.py, constants.py, strategy.py, opponent.py, card_utils.py, postflop.py, main.py, tournament.py, state.py, sizing.py). Key areas for improvement: hand evaluation accuracy, betting strategy, opponent modeling, and tournament endgame play."}
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v17 → v18
Bot directory: bots/claude_v17/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me start by reading the essential data files to understand the current state.
Now let me read the top-performing v9 and v5 strategy files to compare with v17:
Now let me update the experience pool with new insights:
Now let me produce the final JSON output with my strategic analysis and worker assignments:

```json
{
  "analysis": "## Strategic Analysis for v17 → v18\n\n### Rating Trend Analysis\nv17 is at 1559 ELO (RD 42.7), having declined steadily from 1621 at introduction (period 149) to ~1548-1559 (periods 185-188). This is a -62 point decline. The top bots are v9 (1611), v12 (1610), and v5 (1602). v17 is 50+ ELO below the leaders.\n\n### Root Cause: v17's Foundation is Compromised\nBy diffing v17 vs v9 (the #1 bot), I identified 6 fundamental flaws:\n1. **WRONG priors**: vpip=0.52, pfr=0.24 (should be 0.58, 0.28 per experience pool #5). This corrupts ALL opponent modeling from the start of each match.\n2. **Higher EQR**: air 0.72/0.62 vs v9's 0.65/0.53. Over-realizes weak hands → calls too light.\n3. **Missing classify_opponent_style()**: No adaptation to nit/maniac/station/fold-heavy opponents.\n4. **Missing style_deltas on bluff thresholds**: Bluffs not tuned to opponent type.\n5. **Missing river exact equity override**: Imprecise river decisions with simulation instead of exact enumeration.\n6. **Missing big_pot_safety_guard**: Catastrophic stack-offs with thin value in big pots.\n\n### Decision: Branch from v9 (not v17)\nv9 is the highest-rated bot with all proven features. v17's foundation is too corrupted to fix incrementally — we'd need to port 6+ major features AND fix parameters. Starting from v9 and adding targeted improvements is more reliable.\n\n### What to Add to v9\nI studied v12 (1610 ELO) which has features v9 lacks. The highest-impact additions:\n1. **donk_bet_profile** (from v12): Leading out OOP on wet boards with strong/nut hands. v9 never donks — always checks to the raiser. This misses value on dynamic textures.\n2. **river_bluff_ev** (from v12): EV-based river bluff calculation. More principled than v9's threshold-only approach.\n3. **Safety guards from v12**: weak_pair_river, bad_river_bluff_candidate, weak_bottom_pair_barrel, thin_static_showdown_control — these prevent the exact type of catastrophic plays that caused v17's decline.\n4. **spr_profile** (from v12): Stack-to-pot ratio awareness for sizing adjustments.\n\n### What NOT to Port\nv12's river_thin_value_profile requires very strict conditions (only top_pair/overpair, kicker≥10). v17's loose version was net negative. Only port with v12's strict guards.\n\n### Parameter Tuning Plan\nv9's proven parameters should be tuned carefully:\n- Probe check wetness threshold (0.55→try 0.50 for tighter check-probe)\n- Preflop bb_vs_raise bluff frequency (try reducing token-based bluff)\n- Thin_cap in choose_raise (v9 uses 0.30/0.38, v17 used higher — try 0.32/0.40)\n- Anti-lock pressure ratios (turn 1.35→try 1.25, river 1.55→try 1.40)",
  "branch_from": "claude_v9",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "betting.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. You are improving v9 (the highest-rated poker bot at 1611 ELO) by porting targeted features from v12 (1610 ELO). The bot is at bots/claude_v9/.\n\n## Context\nv9 is the best bot but has a gap: it NEVER donk-bets (leading out OOP into the preflop raiser). It always checks when OOP. This misses value on dynamic/wet boards where we have strong hands or combo draws. Additionally, v9's river blocker bluff uses only a threshold check — an EV-based calculation would be more accurate.\n\n## Task 1: Add donk_bet_profile() to betting.py\nPort the `donk_bet_profile()` function from v12 (bots/claude_v12/postflop.py, line 1161-1236) into v9's betting.py. The function should:\n- Only activate when OOP (not has_position) and not facing aggression\n- Donk with strong/nut hands on wet boards (wetness≥0.25) for value+protection, sizing 0.55+0.10*wetness\n- Donk with strong hands on paired boards, sizing 0.45\n- Donk with combo/nut draws on dynamic boards (semi_bluff + quality≥0.18 + wetness≥0.30), sizing 0.45+0.05*wetness\n- Donk with overpairs on dry boards vs calling opponents (fold_to_raise<0.50), sizing 0.40\n- Return {\"eligible\": bool, \"sizing_ratio\": float, \"reason\": str}\n\n## Task 2: Add spr_profile() to betting.py\nPort `spr_profile()` from v12 (postflop.py, line 1237-1259). Simple function:\n- spr = my_chips / pot\n- deep (spr≥10): scale=1.0, medium (5-10): 0.95, shallow (2.5-5): 0.88, commit (<2.5): 0.80\n- Return {\"spr\": float, \"commitment_level\": str, \"sizing_scale\": float}\n\n## Task 3: Add river_bluff_ev() to betting.py\nPort `river_bluff_ev()` from v12 (postflop.py, line 1126-1160). EV calculation:\n- bluff_size = pot * 0.55\n- fold_prob = clamp(fold_to_raise, 0.15, 0.75)\n- blocker_value = blocker_count / deck_size * 0.10\n- ev = fold_prob * pot - (1-fold_prob) * bluff_size + blocker_value * pot\n- Return {\"ev\": float, \"recommended\": bool} where recommended = ev > 0\n\n## Task 4: Integrate into strategy.py\nIn strategy.py's get_action() function, add these integrations:\n\n1. After value_plan computation (around line 610), add:\n   ```python\n   donk_info = {\"eligible\": False, \"sizing_ratio\": 0.0, \"reason\": \"none\"}\n   spr_info = {\"spr\": 100.0, \"commitment_level\": \"deep\", \"sizing_scale\": 1.0}\n   if round_idx > 0:\n       donk_info = donk_bet_profile(my_cards, public_cards, pair_profile, board_texture, value_profile, draw_info, spot_info, opponent_model)\n       spr_info = spr_profile(my_chips, pot)\n   ```\n\n2. Add safety guards BEFORE the main raise/call/fold decision (after anti_lock_pressure block):\n   ```python\n   # Don't bluff river with weak made hands (0.18-0.40) without blockers\n   bad_river_bluff_candidate = (\n       round_idx == 3 and to_call == 0\n       and made_strength >= 0.18 and made_strength < 0.40\n       and not (blocker_profile and blocker_profile[\"eligible\"])\n       and not (value_profile and value_profile[\"tier\"] in (\"strong\", \"nut\"))\n   )\n   if bad_river_bluff_candidate:\n       return 0\n\n   # Don't barrel weak bottom pairs on turn/river\n   weak_bottom_pair_barrel = (\n       round_idx >= 2 and to_call == 0\n       and pair_profile is not None\n       and pair_profile[\"made_class\"] == 1\n       and pair_profile[\"pair_type\"] in (\"bottom_pair\", \"underpair\", \"board_pair\")\n       and made_strength < 0.40 and draw_strength < 0.12\n   )\n   if weak_bottom_pair_barrel:\n       return 0\n\n   # Thin static showdown control: check down thin value on static boards\n   thin_static_showdown_control = (\n       round_idx >= 2\n       and value_profile is not None\n       and value_profile[\"tier\"] == \"thin\"\n       and board_texture is not None\n       and not board_texture[\"dynamic\"]\n       and draw_strength < 0.12\n       and not anti_lock_pressure\n   )\n   if thin_static_showdown_control:\n       return 0\n   ```\n\n3. Add donk betting logic after the safety guards:\n   ```python\n   # Donk betting: lead out OOP on favorable boards\n   if round_idx > 0 and to_call == 0 and donk_info[\"eligible\"]:\n       donk_sizing = int(pot * donk_info[\"sizing_ratio\"] * spr_info[\"sizing_scale\"])\n       donk_amount = max(state.get(\"min_raise_action\", state[\"round_raise\"]), donk_sizing)\n       if donk_amount > to_call and donk_amount < my_chips:\n           return donk_amount\n   ```\n\n4. Modify the river blocker bluff to use river_bluff_ev: In the existing river_blocker_bluff condition (around line 800), add an EV check:\n   ```python\n   and river_bluff_ev(my_cards, public_cards, blocker_profile, pot, opponent_model)[\"recommended\"]\n   ```\n\n## Critical Rules\n- Import the new functions from betting.py at the top ofstrategy.py\n- Do NOT modify any existing v9 logic beyond the specified integrations\n- Do NOT change any numeric thresholds or constants\n- Do NOT modify postflop.py, opponent.py, state.py, or any other files\n- Keep strategy.py under 1000 lines (v9 is currently 991, so be concise)\n- All new code must use `state.get(\"min_raise_action\", state[\"round_raise\"])` for robustness\n- Test that the code compiles with `python3 -c \"import strategy\"`"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["betting.py", "strategy.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to modify numeric constants and thresholds in v9's betting.py and strategy.py. DO NOT add new functions, classes, or change control flow. The bot is at bots/claude_v9/.\n\n## Context\nv9 is the #1 bot at 1611 ELO. We are making careful parameter adjustments to squeeze out more edge. These changes are based on analysis of v12 (1610 ELO) and v5's rating trends.\n\n## Parameter Changes in betting.py\n\n### 1. Thin cap adjustment (choose_raise function, around line 85-87)\nCurrent:\n```python\nthin_cap = 0.30 if round_idx <= 2 else 0.38\n```\nChange to:\n```python\nthin_cap = 0.32 if round_idx <= 2 else 0.40\n```\nReason: Slightly higher thin cap allows more value from thin hands without being reckless. v12's thin value works with this higher cap.\n\n### 2. Probe bet sizing adjustment (choose_raise, around line 82)\nCurrent:\n```python\nprobe_ratio = 0.25 + 0.08 * wetness\n```\nChange to:\n```python\nprobe_ratio = 0.28 + 0.10 * wetness\n```\nReason: Slightly larger probes get more fold equity and build bigger pots with medium-strength hands.\n\n### 3. Preflop sb_open sizing (choose_preflop_spot_action, around line 134)\nCurrent:\n```python\nopen_threshold = 0.49 + match_adjust + 0.02 + match_profile[\"open_delta\"]\n```\nChange to:\n```python\nopen_threshold = 0.47 + match_adjust + 0.02 + match_profile[\"open_delta\"]\n```\nReason: Slightly wider opening range from SB — open hands like K9s, QJo more often.\n\n## Parameter Changes in strategy.py\n\n### 4. Check-probe wetness threshold\nFind the check_probe condition (search for `board_texture[\"wetness\"] <= 0.55`):\nCurrent:\n```python\nand board_texture[\"wetness\"] <= 0.55\n```\nChange to:\n```python\nand board_texture[\"wetness\"] <= 0.50\n```\nReason: Tighter check-probe — only probe on drier boards. Avoids probing into wet textures where opponent has more strong hands.\n\n### 5. Check-probe draw threshold\nFind (search for `draw_strength < 0.20` in check_probe area):\nCurrent:\n```python\nand draw_strength < 0.20\n```\nChange to:\n```python\nand draw_strength < 0.18\n```\nReason: Slightly tighter — only check-probe with weaker draws.\n\n### 6. Postflop call margin for cbet defense\nIn the call margin calculation (search for `call_margin += 0.02` near line 610), find:\n```python\nif pot > 5000 and made_strength < 0.40 and draw_strength < 0.15:\n    call_margin += 0.02\n```\nChange to:\n```python\nif pot > 4000 and made_strength < 0.40 and draw_strength < 0.15:\n    call_margin += 0.025\n```\nReason: Expand the big-pot cautious calling range to slightly smaller pots and add a bit more margin.\n\n### 7. Jam buffer max cap\nFind (search for `clamp(jam_buffer`):\nCurrent:\n```python\njam_buffer = clamp(jam_buffer, -0.05 if anti_lock_pressure else 0.0, 0.11)\n```\nChange to:\n```python\njam_buffer = clamp(jam_buffer, -0.05 if anti_lock_pressure else 0.0, 0.12)\n```\nReason: Allow slightly more aggressive jam buffer — from 0.11 to 0.12. This was at 0.14 in v17 which was too high, but 0.11 might be too conservative.\n\n### 8. Shove buffer max cap\nFind (search for `clamp(shove_buffer`):\nCurrent:\n```python\nshove_buffer = clamp(shove_buffer, -0.05 if anti_lock_pressure else 0.0, 0.11)\n```\nChange to:\n```python\nshove_buffer = clamp(shove_buffer, -0.05 if anti_lock_pressure else 0.0, 0.12)\n```\nReason: Same as jam buffer — slightly more room for all-in plays without going to v17's reckless 0.14.\n\n## Critical Rules\n- ONLY modify the specific numeric values listed above\n- DO NOT add, remove, or rename any functions\n- DO NOT change any import statements\n- DO NOT modify postflop.py, opponent.py, state.py, constants.py, or any other files\n- DO NOT change the structure of any if/elif/else blocks\n- After changes, verify the code compiles with `python3 -c \"import strategy\"` from the bot directory"
    }
  ]
}
```

[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
Rating 1586.1, RD 44.6 - unreliable. Ranked 8th of 21 bots. Top bot is claude_v10 at 1612.0 (gap ~26 ELO). No stagnation confirmed due to unreliable RD. Recent completed gens include v19, v20, v21 (likely from crossover or parallel branches). v17 is mid-pack.

# Recent Match Analysis
Match analysis unavailable (WebUI error). No specific loss patterns identified. However, v17 at 1586 is below the top bots (v10=1612, v12=1603, v14=1602, v5=1598). Key areas to improve: 1) Preflop hand range accuracy, 2) Post-flop board texture evaluation, 3) Bluff/blocker strategy, 4) Opponent modeling exploitation, 5) Bet sizing optimization. The bot has 3462 lines across 11 files (main.py, strategy.py, postflop.py, opponent.py, sizing.py, etc.).
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v17 → v18
Bot directory: bots/claude_v17/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
I'll start by reading the essential data files to understand the current state of the bot evolution.
Let me read the essential data files and git history first.
Now let me read the current v17 bot source code to understand its structure and identify weaknesses:
Now let me read the top-rated bot (v10) to identify features v17 is missing:
Now let me study the top-rated bot (v10) and v9 for key features that v17 is missing:
Now let me look at the critical differences - v10's `classify_opponent_style` and the `big_pot_safety_guard` and other key features:
Now let me look at how v17 differs from v10 in its main decision flow - let me check what v17 is MISSING:
Now let me check the key missing features more carefully - `big_pot_safety_guard`, `choose_overbet_bluff_river`, `classify_opponent_style`, and the river exact equity logic:
Now let me check v10's critical river exact equity logic and style_deltas integration:
Now I have a thorough understanding. Let me also check the v17 strategy.py for its bluff thresholds and the full `decide_action` function:
Now I have a complete picture. Let me also quickly check the opponent.py priors difference between v17 and v10:
Excellent. Now I have a thorough understanding of both bots. Let me compile my analysis and create the task plan.

Key findings from my analysis:

**v17 is MISSING from v10 (the #1 bot at 1612 ELO):**
1. `classify_opponent_style()` — classifies opponent as nit/maniac/station/fold-heavy and adjusts thresholds
2. `style_deltas` propagation to strong/medium/bluff thresholds
3. `big_pot_safety_guard()` — prevents catastrophic stack-offs with thin value in big pots
4. `choose_overbet_bluff_river()` — river overbet bluff with strong blockers
5. River exact equity override (when 5 public cards, use exact enumeration)
6. `must_continue_vs_raise()` with draw_strength parameter
7. `thin_static_showdown_control` — checks down thin value on static boards
8. Correct bot4 detection threshold (0.58 vs v17's 0.55)
9. `allow_river_overbet` flag in `choose_raise`
10. V17's `river_thin_value_bet` is too loose (lacks strict conditions from v12)

Now let me write the output JSON:

```json
{
  "analysis": "## Strategic Analysis: v17 (1586 ELO) → v18\n\n### Reference Bot Studied: claude_v10 (1612 ELO, #1 ranked)\n\nv17 sits at 1586 ELO with RD 44.6 — 26 points behind v10. The gap is entirely due to MISSING features, not wrong parameters. After line-by-line comparison:\n\n**Missing Feature #1: `classify_opponent_style()` + `style_deltas` propagation (lesson #13).** v10 classifies opponents into nit/maniac/station/fold-heavy and adjusts strong/medium/bluff thresholds. v17 has ZERO opponent style adaptation. This means v17 bluffs into calling stations and doesn't bluff enough against nits. This is the single biggest gap.\n\n**Missing Feature #2: `big_pot_safety_guard()` (lesson #15).** v10 prevents catastrophic stack-offs with thin value in big pots (>7000 chips). v17 has no such guard. In high-stakes pots, v17 bets marginal hands and gets punished.\n\n**Missing Feature #3: River exact equity override (lesson #16).** When 5 public cards are visible, v10 uses exact enumeration (0 sims) instead of simulation noise. This gives precise raise/fold decisions at showdown. v17 still uses simulation on the river.\n\n**Missing Feature #4: `choose_overbet_bluff_river()` (lesson #14).** v10 has a separate river overbet bluff weapon with strong blockers on dry boards. v17 only has the value overbet.\n\n**Missing Feature #5: `thin_static_showdown_control`.** v10 checks down thin value hands on non-dynamic boards late in the hand. v17 has an overly loose `river_thin_value_bet` that bets medium hands into strong ranges.\n\n**Missing Feature #6: Bot4 detection uses wrong center (0.55 vs v10's 0.58).** v17's bot4 detection is slightly misaligned.\n\n**Parameter Issue: EQR values.** Lessons #17, #23 show v17 uses higher EQR than v9/v10, causing it to call too light.\n\n### Diagnosis: BAD LOGIC (missing features) is the primary cause, with some parameter issues compounding the problem.\n\n### Direction A (Logic Architect) will port the 4 missing functions from v10: classify_opponent_style, big_pot_safety_guard, choose_overbet_bluff_river, river exact equity override, and thin_static_showdown_control. These are targeted, well-tested features from the top-rated bot.\n\n### Direction B (Hyperparameter Tuner) will fix the bot4 detection center, EQR calibration, and jam buffer cap.",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["opponent.py", "sizing.py", "strategy.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect for a Texas Hold'em poker bot. Your task is to port 5 proven features from claude_v10 (the #1 rated bot at 1612 ELO) into claude_v17's codebase. These features are MISSING from v17 and are the primary reason it's 26 ELO behind.\n\n## Context\nThe bot code lives in `bots/claude_v17/`. The reference bot is in `bots/claude_v10/`.\n- Read `bots/claude_v10/opponent.py`, `bots/claude_v10/betting.py`, `bots/claude_v10/strategy.py` to see the exact implementations to port.\n- Read `bots/claude_v17/opponent.py`, `bots/claude_v17/sizing.py`, `bots/claude_v17/strategy.py` to understand where to add them.\n\n## Feature 1: `classify_opponent_style()` in opponent.py\nPort `classify_opponent_style()` from `bots/claude_v10/opponent.py` (last function in that file). Add it to `bots/claude_v17/opponent.py`. This function classifies the opponent as nit/maniac/station/fold-heavy and returns threshold deltas (strong_delta, medium_delta, bluff_freq_bonus, call_aggression_bonus, fold_vs_passive_bonus). It requires confidence >= 0.15 before classifying.\n\n## Feature 2: `big_pot_safety_guard()` in sizing.py\nPort `big_pot_safety_guard()` from `bots/claude_v10/betting.py` into `bots/claude_v17/sizing.py`. This function returns True when the situation is too risky for aggressive play with marginal hands in big pots. It prevents catastrophic stack-offs. The exact signature is:\n```python\ndef big_pot_safety_guard(pot, my_chips, value_profile, made_strength, round_idx, to_call, draw_strength):\n```\nNote: v10's version has a `draw_strength` parameter that v17's doesn't. Copy v10's version exactly.\n\n## Feature 3: `choose_overbet_bluff_river()` in sizing.py\nPort `choose_overbet_bluff_river()` from `bots/claude_v10/betting.py` into `bots/claude_v17/sizing.py`. This is a separate weapon from the value overbet — it bluffs with strong blockers on dry river boards. Only fires when to_call==0, blocker score >= 0.35, dry board (wetness < 0.25), not paired, pot >= 400, opponent fold_to_raise > 0.48. Sizing is 1.3-1.6x pot.\n\n## Feature 4: River exact equity override in strategy.py\nIn `bots/claude_v10/strategy.py`, search for 'River Refinement: exact equity override'. This block uses `estimate_weighted_win_rate(..., 0)` (0 sims = exact enumeration) when 5 public cards are visible. It has 3 branches:\n- exact_wr > 0.85 and to_call > 0: raise\n- exact_wr < 0.15 and to_call > 0: fold if exact_wr < min_call_odds - 0.10\n- exact_wr < 0.20 and to_call == 0 with blocker profile: sets river_exact_blocker_bluff = True\n\nAdd this exact logic to v17's `decide_action()` function in `strategy.py`, right BEFORE the `repeated_raise_trap` computation (which is around line 430 in v17). You need to import `style_deltas` from the classify_opponent_style call for the third branch.\n\n## Feature 5: `style_deltas` integration in strategy.py\nAdd the following integration points in `bots/claude_v17/strategy.py`:\n1. After computing `anti_bot4` adjustments (around line 363), add: `style_deltas = classify_opponent_style(opponent_model)` and import it.\n2. After the line `strong += anti_bot4['trap_defense_delta']` (around line 560), add: `strong += style_deltas['strong_delta']` and `medium += style_deltas['medium_delta']`.\n3. In the bluff threshold section (around line 879-881), subtract `style_deltas['bluff_freq_bonus']` from all three thresholds: `river_bluff_threshold`, `probe_fold_threshold`, `semi_bluff_threshold`.\n4. Add `thin_static_showdown_control` check (from v10 line ~948) before the raise block: if thin value on non-dynamic board with low draw strength and no anti-lock pressure, return 0 (check).\n5. Add `big_pot_safety_guard` call before the raise block (from v10 line ~964): if safety guard triggers, return 0.\n6. Add the river overbet bluff call (Feature 3) right after the existing river overbet value section, similar to v10's placement.\n7. In the `choose_raise()` call for the main raise block, add `allow_river_overbet=anti_bot4.get('river_overbet_enabled', False)` parameter.\n\n## Feature 6: `must_continue_vs_raise` draw_strength parameter\nv10's `must_continue_vs_raise` has an additional `draw_strength` parameter. In v17's postflop.py, the function only has 5 parameters. Port v10's 7-parameter version which handles combo draws and nut flush draws facing aggression.\n\n## CRITICAL RULES\n- Do NOT change any numeric constants, thresholds, or parameters. Only add NEW functions and NEW integration code.\n- Copy the EXACT logic from v10. Do not invent your own version.\n- Maintain all existing v17 logic — only ADD to it.\n- Do NOT modify files outside your target_files list.\n- Total lines added should be under 150 lines across all files."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["opponent.py", "strategy.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner for a Texas Hold'em poker bot. Your ONLY job is to adjust numeric constants and thresholds. You MUST NOT add new functions, classes, or change control flow. You MUST NOT write any new `def` statements.\n\n## Context\nThe bot code lives in `bots/claude_v17/`. You are tuning parameters for v18.\n\n## Task 1: Fix bot4 detection center in opponent.py\nIn `bots/claude_v17/opponent.py`, the `detect_bot4_profile()` function uses `abs(vpip - 0.55)` as the bot4 detection center. The PROVEN correct value (from v10, the #1 bot) is `abs(vpip - 0.58)`. Change:\n- `abs(vpip - 0.55)` → `abs(vpip - 0.58)` \nThis is a ONE-LINE change. Also update the comment above it from `0.55` to `0.58`.\n\n## Task 2: Lower EQR (Effective Qualification Ratio) in strategy.py\nPer experience pool lesson #17 and #23, v17's EQR values are TOO HIGH compared to v9/v10. In `bots/claude_v17/strategy.py`, find where `made_strength` is used for qualification. v17 likely has higher EQR thresholds than v9 (v9: air 0.65/0.53, draw 0.82/0.70). \n\nSpecifically, look for any constants near these patterns in the decide_action function:\n- If there are threshold values like 0.72, 0.62 for air hands → lower them to 0.65, 0.53 (v9 values)\n- If there are threshold values like 0.86, 0.78 for draw hands → lower them to 0.82, 0.70 (v9 values)\n- Add pot>3000 penalty (-0.03) and OOP penalty (-0.05) if they don't exist\n\n## Task 3: Lower jam/shove buffer cap\nPer lesson #26, v17's jam/shove buffer caps are higher than v9's. In strategy.py, find the line that clamps `jam_buffer` — it should have a max value like 0.14. Change it to `0.11` (v9's proven value).\n\n## Task 4: Fix river_thin_value_bet thresholds in strategy.py\nv17's `river_thin_value_bet` condition is too loose per lesson #24. Tighten the conditions:\n- Change `made_strength >= 0.35` to `made_strength >= 0.40` for the first branch\n- Change `made_strength >= 0.45` to `made_strength >= 0.50` for the second branch\n- Add condition: `made_strength < 0.55` (don't bet thin if actually strong)\n\n## CRITICAL RULES\n- ONLY modify numeric constants. Do NOT add, remove, or rename functions.\n- Do NOT change control flow (no new if/else/for/while statements).\n- Do NOT modify files outside your target_files list.\n- Each change should be a simple number replacement.\n- Total changes: approximately 8-10 numeric constant changes across 2 files."
    }
  ]
}
```
