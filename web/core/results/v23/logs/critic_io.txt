
[STRATEGY CRITIC PROMPT]
=============================
# Role
You are the **Poker Strategy Critic** — an independent strategic quality gate for a Texas Hold'em poker bot evolution system. You evaluate whether the code changes made by Worker Agents will **meaningfully improve win rate**, not just compile or follow the plan.

You do NOT re-check code correctness (the Lead Code Reviewer already did that).  
Your job is **purely strategic**: will this change actually make the bot play better poker?

# CRITICAL: Tool Usage Rules
- **Use the Bash tool** to run `git diff bot-v22 -- bots/claude_v23/`
- **Use the Read tool** to read changed functions for context
- **NEVER use webReader or web-search tools**

# Context
## Master's Plan:
[
  {
    "difficulty": "medium",
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "opponent.py",
      "strategy.py",
      "strategy_helpers.py"
    ],
    "worker_id": 1,
    "worker_prompt": "Fix 5 critical parameter bugs from experience pool: wrong opponent priors, harmful exploit_lambda, too-high EQR values, missing classify_opponent_style, and jam buffer cap"
  }
]

Bot directory: `bots/claude_v23/`
Parent version tag: `bot-v22`

## Head-to-Head Context
Read `web/core/results/head_to_head.json` and find the current bot's weakest opponent matchups (win rate < 40%). A high-quality change should address these specific weaknesses. Also check `web/core/results/bot_stats.json` for the current bot's overall win rate and game count.

# How to Evaluate

1. Run `git diff bot-v22 -- bots/claude_v23/` to see all changes
2. Run `git diff --stat bot-v22 -- bots/claude_v23/` for a summary
3. Read the most changed functions for strategic context
4. **For diversity/local-optima check**: Run `git log --oneline bot-v22..HEAD --decorate` to see recent commits, then `git show bot-v22` to read the previous generation's commit message and strategy. Also read `web/core/experience_pool.md` for `[POSSIBLY EXHAUSTED]` tags.
5. Score against the criteria below

# Scoring Criteria (1–10)

| Score | Meaning |
|---|---|
| **9–10** | Changes directly address a confirmed weakness. Novel, high-EV improvement. Example: adds per-street opponent fold-rate tracking that adjusts continuation bet sizing |
| **7–8** | Solid changes with clear strategic rationale. Measurable positive expected value. Minor execution risk |
| **5–6** | Superficial — e.g. changing a constant by 5% with no analysis basis, minor refactors with no strategic significance. Likely won't help |
| **3–4** | Changes likely to cause regression. Wrong strategic direction. Example: increasing passive-call threshold when match analysis shows opponent exploiting passivity |
| **1–2** | Changes will clearly hurt. Catastrophic strategic errors or complete misfire on the problem |

**Score ≥ 6 → `approved: true`. Score < 6 → `approved: false` (triggers intra-generation worker retry).**

# Strategic Checklist (check each before scoring)

- [ ] **Addresses confirmed weakness**: Does the change target a pattern from recent match analysis or experience pool?
- [ ] **Opponent modeling**: Does it improve per-street tracking, bet-sizing detection, or exploitative adjustment?
- [ ] **EV basis**: Are decisions based on equity/pot-odds/fold-equity rather than arbitrary thresholds?
- [ ] **No regression on critical spots**: AA/KK/QQ still raises preflop; nut hands still value-bet river?
- [ ] **Diversity check**: Is this substantially different from the last 2 generations' approach? If same type of change (e.g. constant tuning) failed twice, it should NOT be repeated without new insight.

# Local Optima Detection

Flag `local_optima_warning: true` if:
- The change is the same TYPE as the previous 1-2 generations (e.g. constant tuning again with no new analysis)
- The experience pool has a `[POSSIBLY EXHAUSTED]` tag on a related lesson
- The change is incremental when a structural rethink is needed

# Output Format

Output exactly ONE JSON block:

```json
{
  "score": 7,
  "approved": true,
  "strategic_assessment": "Brief evaluation: what the change does strategically and whether it's sound.",
  "feedback": "If approved=false: specific, actionable guidance. What change WOULD score ≥7? Be concrete: which street, which opponent pattern, what mechanism.",
  "local_optima_warning": false,
  "local_optima_reason": null
}
```

If `approved: false`, the `feedback` field MUST be specific enough that workers can act on it immediately. Examples of good feedback:
- "The change tunes BLUFF_THRESHOLD without analysis basis. Instead, add per-street fold-to-cbet tracking: if opponent folds flop cbets >60%, increase flop cbet frequency to 75%."
- "Constant tuning has been tried 2 generations with no gain. This generation needs a structural change: add opponent bet-size profiling to detect polarised vs merged betting ranges."

=============================
[CLAUDE OUTPUT]
I'll start by gathering all the necessary information: the diff, head-to-head data, experience pool, and recent commit history.
The git diff returned no output — let me check if the bot directory exists and look at the commit history.
No diff output means the files haven't changed since bot-v22. Let me check if this is a new directory or the files match exactly:
Now I can see the picture. The `claude_v23/` directory is untracked (not yet committed), and there are actual file changes in `opponent.py`, `strategy.py`, and `strategy_helpers.py` compared to v22. Let me diff them directly:
Now let me get the experience pool and previous generation context for the diversity check:
Now let me check the v21 commit message and the v22 specific context more closely:
Excellent. Now let me also check the v22 head-to-head for weak matchups specifically:
Now let me look at the v15 matchup loss more closely since that's v22's weakest matchup:
Now I have a complete picture. Let me compile my assessment.

**Summary of changes from v22 → v23:**

1. **`opponent.py`** — Fixed VPIP prior from 0.52→0.58, PFR prior from 0.24→0.28, prior_weight from 4.0→3.5. Added `classify_opponent_style()` function (50 lines) that classifies opponents as nit/maniac/station/fold-heavy and returns threshold deltas scaled by confidence.

2. **`strategy.py`** — 
   - Removed `exploit_lambda` / `gift_balance` blending (experience pool says it's harmful, corrupts GTO base)
   - Removed the GTO blending lines that mixed `gto_strong`/`gto_medium` with adaptive thresholds
   - Added `style_deltas` propagation to `strong` and `medium` thresholds and all bluff thresholds
   - Lowered jam_buffer/shove_buffer cap from 0.14→0.11 (experience pool says v9's 0.11 outperformed)

3. **`strategy_helpers.py`** — Lowered EQR values: IP from 0.68→0.65, OOP from 0.56→0.53 (experience pool says lower EQR outperforms)

Now let me evaluate:

```json
{
  "score": 8,
  "approved": true,
  "strategic_assessment": "This generation directly addresses 5 confirmed parameter bugs documented in the experience pool — the same bugs that caused v17's -62 rating collapse. (1) VPIP/PFR priors corrected from wrong v17 values (0.52/0.24) to correct ones (0.58/0.28), which fixes all downstream opponent modeling. (2) Harmful exploit_lambda removed entirely and replaced with the previously-missing classify_opponent_style() that returns confidence-scaled deltas for 4 opponent archetypes (nit/maniac/station/fold-heavy), with deltas properly propagated to both decision thresholds AND all bluff thresholds — this was identified as a key gap. (3) EQR lowered from 0.68/0.56 to 0.65/0.53, aligning with proven v9 values to reduce over-realization of weak hands. (4) Jam/shove buffer caps lowered from 0.14 to 0.11, matching proven v9 values to reduce marginal all-in. All changes are grounded in documented experience pool lessons, address confirmed weaknesses (v22's weakest matchup is vs v15 at 40% WR where opponent modeling accuracy matters), and represent a structural fix (opponent classification system) rather than just constant tuning. The changes are different in nature from v21 (bug fix for AKs+ preflop folding) and v22 (porting v12 features + preflop threshold tuning). Risk is low: these are restoring proven parameter values from v9 that were incorrectly inflated in v17.",
  "feedback": null,
  "local_optima_warning": false,
  "local_optima_reason": null
}
```

**Strategic checklist:**

- ✅ **Addresses confirmed weakness**: All 5 changes directly map to documented experience pool lessons about v17's parameter bugs. v22's weakest matchup (vs v15, 40% WR) would benefit from better opponent modeling.
- ✅ **Opponent modeling**: Adds `classify_opponent_style()` — a genuine new opponent classification system (nit/maniac/station/fold-heavy) with per-style threshold deltas propagated to bluff, call, and fold decisions. This is a structural improvement, not just constant tuning.
- ✅ **EV basis**: EQR correction is grounded in empirical comparison (v9's lower values outperformed). Removing exploit_lambda eliminates a mechanism that blended away the GTO base without EV justification. Style deltas are scaled by opponent model confidence.
- ✅ **No regression on critical spots**: The changes don't touch preflop premium hand protection (v21's fix), nut hand value-betting, or river overbet logic. The jam buffer cap reduction actually *protects* against marginal stack-offs.
- ✅ **Diversity check**: v21 was a bug fix (AKs+ folding), v22 was a feature port + preflop threshold tuning. This generation is a *parameter correction + opponent classification system* — structurally different. No `[POSSIBLY EXHAUSTED]` tags in the experience pool.
