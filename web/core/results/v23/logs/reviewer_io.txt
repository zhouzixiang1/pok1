
[LEAD CODE REVIEWER PROMPT]
=============================
# Role
You are the strict Lead Code Reviewer (Critic) for a Texas Hold'em poker bot team.

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files.
- **Use the Bash tool** to run git diff and other commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Task
Worker Agents have modified the bot codebase based on the Master Architect's instructions.
Your job is the final quality gate before production (the next evolution iteration).

# Context
1. **Master's Original Plan/Tasks:**
[
  {
    "difficulty": "medium",
    "role": "Algorithmic Logic Architect",
    "target_files": [
      "opponent.py",
      "strategy.py",
      "strategy_helpers.py"
    ],
    "worker_id": 1,
    "worker_prompt": "Fix 5 critical parameter bugs from experience pool: wrong opponent priors, harmful exploit_lambda, too-high EQR values, missing classify_opponent_style, and jam buffer cap"
  }
]

2. **Bot directory**: `bots/claude_v23/`
3. **Parent version tag**: `bot-v22`

# How to Review
You have full access to git and file reading tools. Use them:

1. **See all changes**: Run `git diff bot-v22 -- bots/claude_v23/`
   This shows exactly what the workers changed vs the parent bot.
2. **See change summary**: Run `git diff --stat bot-v22 -- bots/claude_v23/`
3. **Read specific files**: Use the Read tool on any file in `bots/claude_v23/`
4. **List all files**: Run `ls bots/claude_v23/` to see the full file list

Focus your review on the diff, but read full files when you need more context.

# Rules
1. Verify the changes fulfill the Master's instructions without logical flaws or contradictions.
2. ENFORCE THE DUAL-TRACK BOUNDARY:
   - If a "Hyperparameter Tuner" was assigned, verify they ONLY changed constants/thresholds. If they injected complex new logic, REJECT.
   - If an "Algorithmic Logic Architect" was assigned, verify their logic is sound.
3. Ensure the code compiles conceptually and adheres to the `{"response": int}` JSON output format constraint.
4. Output exactly ONE JSON block containing your decision. If you reject the code, you must provide a detailed "feedback" string explaining what the Developer Agents need to fix.
5. FILE SIZE CONSTRAINT: No single .py file should exceed 1000 lines. If any file is too large, REJECT and instruct workers to split it into focused modules. Keep main.py as the slim entry point.

# Correctness Criteria (MANDATORY CHECK)
Before approving, verify the bot does NOT do any of the following:
- Fold premium hands (AA, KK, QQ, AKs) preflop without extreme pressure
- Call all-in with obviously dominated hands on the river
- Return invalid JSON (must output `{"response": <int>}`)
- Use `input()` or `print()` instead of stdin/stdout for game communication
- Import unavailable modules (only stdlib + numpy if present)
- Have obvious infinite loops or unbounded recursion

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "approved": true or false,
  "feedback": "If approved=false, provide detailed instructions on what needs to be fixed. If true, this can be empty.",
  "quality_score": 7,
  "change_summary": "A 1-2 sentence summary of the key changes made in this generation.",
  "risk_areas": ["List of potential risks or concerns about the changes, if any"]
}
```

# Quality Score Rubric
- **9-10**: Clean, well-structured changes that clearly improve strategy. No risks. Code is concise and follows project conventions.
- **7-8**: Good changes that address the plan. Minor concerns (e.g., slightly verbose code, a heuristic that might not generalize). Approve.
- **5-6**: Changes work but are mediocre — copy-pasted code, brute-force approaches where elegant solutions exist, or unclear strategy. Approve with caution.
- **3-4**: Changes introduce regression risk, violate role boundaries, or show fundamental misunderstanding of poker strategy. REJECT.
- **1-2**: Broken code, catastrophic strategic errors (folding AA preflop), or complete failure to follow instructions. REJECT.

- `change_summary`: Required even when approved=true. This is used to update the experience pool for future generations.
- `risk_areas`: Optional. List potential issues that could cause regression.

=============================
[CLAUDE OUTPUT]
Now let me read the key modified files in detail to verify correctness:
Now I have a complete picture. Let me compile my review.

**Summary of Changes (3 files modified):**

1. **opponent.py** (4 lines changed + 52 lines added):
   - VPIP prior: `0.52, 4.0` → `0.58, 3.5` (more realistic HU defaults)
   - PFR prior: `0.24, 4.0` → `0.28, 3.5` (more realistic HU defaults)
   - New `classify_opponent_style()` function: classifies opponents into nit/maniac/station/fold_heavy and returns confidence-scaled threshold deltas

2. **strategy.py** (net -8 lines):
   - Removed `gift_balance`, `exploit_lambda`, `gto_strong`, `gto_medium` and the blending logic (the harmful exploit_lambda fix)
   - Integrated `style_deltas` from `classify_opponent_style()` into `strong`/`medium` thresholds and bluff thresholds
   - Tightened jam/shove buffer cap from `0.14` → `0.11`

3. **strategy_helpers.py** (2 lines changed):
   - Air-hand EQR lowered: `0.68/0.56` → `0.65/0.53` (IP/OOP)

**Verification of Master's 5 Bug Fixes:**
| Bug | Status | Detail |
|-----|--------|--------|
| Wrong opponent priors | ✅ Fixed | VPIP 0.52→0.58, PFR 0.24→0.28 — realistic HU values |
| Harmful exploit_lambda | ✅ Fixed | Completely removed unstable GTO/exploitive blending |
| Too-high EQR values | ✅ Fixed | Air hand EQR lowered ~0.03 across IP/OOP |
| Missing classify_opponent_style | ✅ Fixed | New function with proper confidence scaling, applied to call/bluff thresholds |
| Jam buffer cap | ✅ Fixed | 0.14→0.11 for both jam and shove buffers |

**Correctness Checks:**
- ✅ Premium hand protection intact (line 234-236: preflop_strength ≥ 0.72 → always call preflop all-in)
- ✅ JSON output format `{"response": int}` correct in main.py
- ✅ stdin/stdout communication via `json.loads(input())` / `print(json.dumps())`
- ✅ All imports are stdlib + local modules only
- ✅ No infinite loops or unbounded recursion
- ✅ All files compile cleanly
- ✅ All files ≤ 1000 lines (largest: strategy.py at 744)
- ✅ `classify_opponent_style` smoke-tested: all 4 style categories classify correctly, confidence scaling works properly

```json
{
  "approved": true,
  "feedback": "",
  "quality_score": 8,
  "change_summary": "Fixed 5 critical parameter bugs: corrected opponent VPIP/PFR priors to realistic HU values (0.58/0.28), removed harmful exploit_lambda GTO/exploitive blending, lowered air-hand EQR (~0.03), added classify_opponent_style() with confidence-scaled threshold deltas for nit/maniac/station/fold_heavy exploitation, and tightened jam/shove buffer cap from 0.14 to 0.11.",
  "risk_areas": [
    "Higher VPIP/PFR priors (0.58/0.28) mean the bot assumes more active opponents by default; against unusually tight opponents with limited data, early opponent modeling may overestimate aggression slightly.",
    "track_opponent_gift() and safe_exploitation_lambda() are now dead code in strategy_helpers.py — harmless but adds clutter for future maintenance.",
    "classify_opponent_style uses hard thresholds for style classification (e.g., VPIP < 0.40 for nit); opponents near boundary values may oscillate between categories across hands."
  ]
}
```
