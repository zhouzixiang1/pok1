
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
No stagnation detected. v14 is the top-rated bot at 1641.6 ELO. However, the margin is thin (only ~2 points over v10 at 1639.4), suggesting the field is competitive and improvements in any area could widen the gap.

# Recent Match Analysis
Match history returned empty, but based on bot structure analysis: v14 has 3249 lines across 10 files. strategy.py is oversized at 1245 lines, suggesting potential for modular refactoring. Key improvement areas for a competitive heads-up NLHE bot: 1) More sophisticated bluff frequency calibration (GTO-informed), 2) Better river decision-making (thin value vs bluff-catch), 3) Improved opponent exploitation based on bet sizing patterns, 4) Enhanced draw evaluation on wet boards, 5) Position-aware opening range optimization.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v14 → v12
Bot directory: bots/claude_v14/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me read the essential data files first.
Now I can see the bots are at `../bots/`. Let me read the current v14 bot and study the reference bots.
Now let me read the full strategy.py and the remaining key files:
Now let me study the top reference bots (bot4 and bot5 are the strongest reference bots):
Now let me look at what bot5 has that v14 doesn't — specifically the anti-bot4 detection and river overbet logic:
Now let me look at the rating history to understand trends:
Now let me look at what v10 and v7 (the closest competitors) look like, and also study bot5's unique features more carefully:
Now I have a comprehensive understanding. Let me read the full strategy.py of v14 to understand its get_action:
Now I have a comprehensive picture. Let me update the experience pool and craft the JSON output.
Now I have a thorough understanding of the codebase. Let me now update the experience pool and craft the final output.

Key observations from my analysis:

1. **v14 is the top bot at 1641.6, but margin is thin** (~2 pts over v10 at 1639.4, ~3 pts over v7 at 1638.9). The field is extremely competitive.

2. **v14 is essentially bot5's code** with modifications:
   - Removed anti-bot4 detection and exploitation (a proven edge per lesson #2)
   - Removed river overbet for nut hands (lesson #3)
   - Added `exploit_lambda`/`gift_balance` system (unproven, not in top bots)
   - Lowered EQR values (more conservative, lesson #6 says lower EQR = good)
   - Added draw OOP EQR path
   - Changed thin_cap from flat (0.30/0.38) to wetness-dependent formula (could be too generous)
   - Changed blocker bluff from `random.random()` to deterministic token (lesson #7 says random is better)

3. **Key regressions from bot5**:
   - No river overbet for nuts (lesson #3 confirms this is a "proven value extraction" edge)
   - No anti-bot4 detection (lesson #2 says "proven edge")
   - The `exploit_lambda` mixing of GTO/exploitative thresholds is novel but unproven

4. **Simulation counts reduced** from bot5 (0:900→500, 3:1200→800, 4:1500→900). Lesson #4 says "simulation counts matter less than threshold calibration" so this may be fine.

5. **EQR values**: v14 uses more conservative EQR (0.68/0.56 for air IP/OOP vs bot5's 0.72/0.62). Lesson #6 says lower EQR is good. However v14 also added more penalties (big_pot, double_barrel OOP) which may be over-discounting.

Now let me write the experience pool update and the JSON:
Now let me craft the final JSON output with the two worker tasks:

```json
{
  "analysis": "## Strategic Analysis\n\n**Current State:** v14 leads at 1641.6 ELO, but the margin is razor-thin (2 pts over v10 at 1639.4, 3 pts over v7 at 1638.9). The entire field is compressed between 1600-1640. Small edges matter enormously.\n\n**Root Cause Analysis — v14 vs bot5 comparison:**\nv14 is built on bot5's base but REMOVED two proven weapons: (1) river overbet for nut hands (1.5-2.2x pot, lesson #3 says proven edge), and (2) anti-bot4 detection/exploitation (lesson #2 says proven edge). These removals are regressions. Meanwhile, v14 added an unproven `exploit_lambda`/`gift_balance` system that blends GTO and exploitative thresholds — novel but risky since gift_balance noise can corrupt base calibration.\n\n**Key weaknesses identified:**\n1. **Missing river overbet for nuts** — bot5 overbets 1.5-2.2x pot with nut hands on dry rivers. v14 has a flat 1.45 max_ratio cap. This leaves significant value on the table.\n2. **Missing anti-bot4 exploitation** — bot5 detects opponents whose stats match bot4 signatures (VPIP~0.58, PFR~0.28, postflop_aggr~0.36) and adjusts bluff frequency, sizing, and trap defense. v14 has zero opponent-type-specific adjustments.\n3. **thin_cap too generous** — v14 changed thin_cap from flat (0.30 round<=2, 0.38 round==3) to wetness formula (0.46 + 0.08*wetness + 0.05*max(0, round-1)). On dry boards this allows 0.46 ratio thin bets that old 0.30 would block — likely a regression.\n4. **EQR may be over-penalized** — v14 uses 0.68/0.56 for air (vs bot5's 0.72/0.62) plus additional big_pot and OOP double-barrel penalties. Combined these may cause too many folds in marginal spots.\n\n**Strategy:** Restore the two proven edges (river overbet + anti-bot4 detection) via Direction A, and tune thin_cap, EQR, and other parameters via Direction B. The exploit_lambda system should be capped more aggressively to prevent corruption of base thresholds.",
  "branch_from": "claude_v14",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "opponent.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to RESTORE two proven features from bot5 (the reference bot at core/reference_bots/bot5/) that were removed in v14.\n\nYou are modifying files in bots/claude_v14/.\n\n## Task 1: Add River Overbet for Nut Hands (strategy.py)\n\nBot5 has a function `choose_overbet_river` that is MISSING from v14. This function overbets 1.5-2.2x pot with nut hands on dry rivers, which is a PROVEN value extraction edge (experience pool lesson #3).\n\n**What to add in strategy.py:**\n\n1. Add this function BEFORE `get_action()`:\n```python\ndef choose_overbet_river(\n    min_raise, my_chips, my_round_bet, to_call, pot,\n    win_rate, value_profile, board_texture, spot_info, opponent_model\n):\n    \"\"\"River overbet: 1.5-2.2x pot with NUT hands only.\"\"\"\n    if value_profile is None or value_profile[\"tier\"] != \"nut\":\n        return None\n    if board_texture is not None and board_texture[\"wetness\"] > 0.35:\n        return None\n    if pot < 400:\n        return None\n\n    pot_after_call = pot + to_call\n    ratio = 1.5 + 0.3 * max(0.0, win_rate - 0.70)\n    if not spot_info.get(\"has_position\", False):\n        ratio = max(1.3, ratio - 0.2)\n    ratio = min(ratio, 2.2)\n    amount = int(to_call + pot_after_call * ratio)\n\n    if amount >= my_chips:\n        return -2\n    amount = min(amount, my_chips - 1)\n    if amount <= to_call or amount < min_raise:\n        return None\n    return amount\n```\n\n2. In `get_action()`, ADD this block RIGHT AFTER the block that computes `hard_repressure_fold` (around line 720 in the current code) and BEFORE `gift_balance = track_opponent_gift(...)`:\n```python\n    # River overbet with nut hands (proven value extraction edge)\n    if round_idx == 3 and to_call == 0 and len(public_cards) >= 3 and value_profile is not None and value_profile[\"tier\"] == \"nut\":\n        overbet = choose_overbet_river(\n            state[\"min_raise_action\"], my_chips, state[\"my_round_bet\"],\n            to_call, pot, win_rate, value_profile, board_texture, spot_info, opponent_model\n        )\n        if overbet is not None:\n            return overbet\n```\n\n3. Also update the `max_ratio` in `choose_raise()`. Find the line that says:\n```python\n    ratio = clamp(ratio, low_ratio, 1.45)\n```\nChange it to:\n```python\n    max_ratio = 2.2 if (round_idx == 3 and value_profile.get(\"tier\") == \"nut\") else 1.45\n    ratio = clamp(ratio, low_ratio, max_ratio)\n```\nThis is critical — it allows the raise function itself to go up to 2.2x for nut hands on the river.\n\n## Task 2: Add Anti-Bot4 Detection and Exploitation (opponent.py)\n\nBot5 has `detect_bot4_profile` and `get_anti_bot4_adjustments` in its opponent.py. v14's opponent.py is MISSING these functions entirely. This is a proven edge (experience pool lesson #2).\n\n**What to add at the END of opponent.py:**\n\n```python\n\n\ndef detect_bot4_profile(opponent_model, n_hands_played):\n    \"\"\"Detect if opponent exhibits bot_4's characteristic stats.\"\"\"\n    confidence = opponent_model[\"confidence\"]\n    if confidence < 0.10:\n        return False, 0.0\n\n    score = 0.0\n    vpip = opponent_model[\"vpip\"]\n    pfr = opponent_model[\"pfr\"]\n    aggr = opponent_model.get(\"aggression\", 0.30)\n    post_aggr = opponent_model[\"postflop_aggr\"]\n    fold_raise = opponent_model[\"fold_to_raise\"]\n\n    if abs(vpip - 0.58) < 0.15:\n        score += 0.20\n    if abs(pfr - 0.28) < 0.13:\n        score += 0.20\n    if abs(post_aggr - 0.36) < 0.15:\n        score += 0.20\n    if abs(fold_raise - 0.44) < 0.15:\n        score += 0.15\n    if abs(aggr - 0.30) < 0.13:\n        score += 0.15\n\n    score *= confidence\n    return score >= 0.20, score\n\n\ndef get_anti_bot4_adjustments(bot4_score, board_texture, spot_info, round_idx, value_profile):\n    \"\"\"Return strategy adjustments targeting bot_4's weaknesses.\"\"\"\n    adj = {\n        \"bluff_freq_bonus\": 0.0,\n        \"raise_size_bonus\": 0.0,\n        \"call_threshold_delta\": 0.0,\n        \"fold_threshold_delta\": 0.0,\n        \"river_overbet_enabled\": False,\n        \"trap_defense_delta\": 0.0,\n    }\n\n    # Wet board: exploit overfold on dynamic boards\n    if board_texture and board_texture[\"dynamic\"]:\n        adj[\"bluff_freq_bonus\"] += 0.15 * bot4_score\n        adj[\"raise_size_bonus\"] += 0.08 * bot4_score\n\n    # Paired board: exploit paired board caution\n    if board_texture and board_texture[\"paired\"]:\n        adj[\"bluff_freq_bonus\"] += 0.10 * bot4_score\n        adj[\"raise_size_bonus\"] += 0.05 * bot4_score\n\n    # River check exploit: checks too much on river\n    if round_idx == 3 and spot_info.get(\"last_opp_action_type\") == \"check\":\n        adj[\"bluff_freq_bonus\"] += 0.12 * bot4_score\n\n    # Preflop 3-Bet wider\n    if round_idx == 0 and spot_info.get(\"preflop_spot\") in (\"bb_vs_raise\", \"sb_vs_reraise\"):\n        adj[\"call_threshold_delta\"] -= 0.05 * bot4_score\n\n    # Anti-trap: more cautious vs check-raise\n    if spot_info.get(\"opp_current_round_check_count\", 0) > 0 and spot_info[\"facing_raise\"]:\n        adj[\"trap_defense_delta\"] += 0.08 * bot4_score\n\n    # River overbet enabled with strong hands\n    if round_idx == 3 and value_profile and value_profile[\"tier\"] in (\"nut\", \"strong\"):\n        adj[\"river_overbet_enabled\"] = True\n\n    return adj\n```\n\n**Whatto add in strategy.py to USE the anti-bot4 adjustments:**\n\n1. Update the import at the top of strategy.py. Change:\n```python\nfrom opponent import build_opponent_model, analyze_current_spot\n```\nTo:\n```python\nfrom opponent import build_opponent_model, analyze_current_spot, detect_bot4_profile, get_anti_bot4_adjustments\n```\n\n2. In `get_action()`, right after the line `combos, weights = build_opponent_range(...)` and before `simulations = SIMULATIONS_BY_PUBLIC_COUNT...`, add:\n```python\n    # Anti-bot_4 detection and adjustments\n    n_hands = len(collect_latest_requests_by_hand(requests))\n    is_bot4, bot4_score = detect_bot4_profile(opponent_model, n_hands)\n    anti_bot4 = None\n```\n\n3. After the block that computes `hard_repressure_fold` (where all the board analysis is done) and BEFORE the river overbet block you just added, add:\n```python\n    # Compute anti-bot_4 adjustments\n    if board_texture is not None:\n        anti_bot4 = get_anti_bot4_adjustments(bot4_score, board_texture, spot_info, round_idx, value_profile)\n    else:\n        anti_bot4 = {\n            \"bluff_freq_bonus\": 0.0, \"raise_size_bonus\": 0.0,\n            \"call_threshold_delta\": 0.0, \"fold_threshold_delta\": 0.0,\n            \"river_overbet_enabled\": False, \"trap_defense_delta\": 0.0,\n        }\n```\n\n4. Find the section where `strong` and `medium` thresholds are computed (around the line with `strong += 0.45 * nutted_risk[\"risk\"]`). After those threshold computations and BEFORE the `if exploit_lambda > 0.0:` block, add:\n```python\n    # Apply anti-bot_4 adjustments to thresholds\n    strong -= anti_bot4[\"call_threshold_delta\"]\n    medium -= anti_bot4[\"call_threshold_delta\"] * 0.8\n    strong += anti_bot4[\"trap_defense_delta\"]\n```\n\n5. Find the three lines that compute bluff thresholds:\n```python\n    river_bluff_threshold = 0.62 - 0.28 * match_profile[\"bluff_delta\"]\n    probe_fold_threshold = 0.56 - 0.32 * match_profile[\"bluff_delta\"]\n    semi_bluff_threshold = 0.58 - 0.28 * match_profile[\"bluff_delta\"]\n```\nChange them to:\n```python\n    river_bluff_threshold = 0.62 - 0.28 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"]\n    probe_fold_threshold = 0.56 - 0.32 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"]\n    semi_bluff_threshold = 0.58 - 0.28 * match_profile[\"bluff_delta\"] - anti_bot4[\"bluff_freq_bonus\"]\n```\n\n6. Find the `choose_raise` call inside the `if win_rate >= medium or semi_bluff or blocker_bluff or small_probe or check_probe...` block (near the end of the function). Add `anti_bot4_bonus=anti_bot4[\"raise_size_bonus\"],` to its arguments.\n\n7. Update the `choose_raise` function signature to accept `anti_bot4_bonus=0.0` parameter, and add `ratio += anti_bot4_bonus` after the other ratio adjustments (before the clamp).\n\n## IMPORTANT RULES:\n- Do NOT change any numeric constants or thresholds. Only add new functions, new code paths, and new imports.\n- Do NOT modify the exploit_lambda system, EQR values, or any other existing parameter.\n- Make sure all new code follows the exact patterns from bot5 (read core/reference_bots/bot5/strategy.py and core/reference_bots/bot5/opponent.py as reference).\n- Test that the code is syntactically valid after your changes."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py", "strategy.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to adjust numeric constants, thresholds, and magic numbers. You MUST NOT add new functions, classes, imports, or change control flow. You MUST NOT add any if/for/while statements or new code paths.\n\nYou are modifying files in bots/claude_v14/.\n\n## Context\nv14 is the top-rated bot at 1642 ELO, but the margin is thin (2 pts over v10). Analysis shows several parameters may be suboptimal compared to the proven bot5 base.\n\n## Changes to Make\n\n### In constants.py:\n\n1. **Increase simulation counts** — v14 reduced these significantly from bot5. Increase to find a middle ground:\n   - `SIMULATIONS_BY_PUBLIC_COUNT[0]`: Change from 500 to 700\n   - `SIMULATIONS_BY_PUBLIC_COUNT[3]`: Change from 800 to 1000\n   - `SIMULATIONS_BY_PUBLIC_COUNT[4]`: Change from 900 to 1200\n   - `EXTRA_SIMULATIONS_BY_PUBLIC_COUNT[0]`: Change from 200 to 250\n   - `EXTRA_SIMULATIONS_BY_PUBLIC_COUNT[3]`: Change from 220 to 280\n   - `EXTRA_SIMULATIONS_BY_PUBLIC_COUNT[4]`: Change from 180 to 240\n\n### In strategy.py:\n\n2. **Tighten thin_cap in choose_raise()** — The current thin_cap formula is too generous (0.46 on dry boards vs bot5's 0.30). Find this line:\n```python\n        thin_cap = 0.46 + 0.08 * wetness + 0.05 * max(0, round_idx - 1)\n```\nChange to:\n```python\n        thin_cap = 0.34 + 0.08 * wetness + 0.04 * max(0, round_idx - 1)\n```\nThis caps thin value bets at 0.34 ratio on dry boards (closer to bot5's proven 0.30), while still allowing slightly more on wet boards.\n\n3. **Raise EQR values slightly** — v14's EQR values are more conservative than bot5. Find the `realized_postflop_equity` function. Change:\n   - Air IP EQR: `eqr = 0.68` → `eqr = 0.70` (line ~300)\n   - Air OOP EQR: `eqr = 0.56` → `eqr = 0.58` (line ~300)\n   - Marginal pair IP EQR: `eqr = 0.84` → `eqr = 0.85` (line ~331)\n   - Marginal pair OOP EQR: `eqr = 0.73` → `eqr = 0.75` (line ~331)\n   These small increases make the bot slightly more willing to see showdowns with marginal hands, matching closer to bot5's proven calibration.\n\n4. **Cap exploit_lambda more aggressively** — In the `safe_exploitation_lambda` function, find:\n```python\n    return clamp(confidence * min(1.0, max(0, gift_balance) / 2.0), 0.0, 0.85)\n```\nChange the upper bound from 0.85 to 0.45:\n```python\n    return clamp(confidence * min(1.0, max(0, gift_balance) / 2.0), 0.0, 0.45)\n```\nThis prevents the exploit system from corrupting more than 45% of the base GTO thresholds. The gift_balance metric is noisy early in matches.\n\n5. **Tighten bluff thresholds slightly** — In get_action(), find these three lines:\n```python\n    river_bluff_threshold = 0.62 - 0.28 * match_profile[\"bluff_delta\"]\n    probe_fold_threshold = 0.56 - 0.32 * match_profile[\"bluff_delta\"]\n    semi_bluff_threshold = 0.58 - 0.28 * match_profile[\"bluff_delta\"]\n```\nChange the base values (only the leading numbers) to be slightly tighter (require more fold equity to bluff):\n```python\n    river_bluff_threshold = 0.64 - 0.28 * match_profile[\"bluff_delta\"]\n    probe_fold_threshold = 0.58 - 0.32 * match_profile[\"bluff_delta\"]\n    semi_bluff_threshold = 0.60 - 0.28 * match_profile[\"bluff_delta\"]\n```\nThis makes bluffs slightly more selective, reducing bluff-spew against opponents who don't fold enough.\n\n## RULES:\n- ONLY change the specific numeric values listed above.\n- Do NOT add or remove any functions, imports, if-statements, for-loops, or code paths.\n- Do NOT change the structure or logic flow of any function.\n- Preserve all whitespace, comments, and formatting around the changed values."
    }
  ]
}
```
