
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
No stagnation. v17 is the current #1 bot (r=1642.7). However, it's a large bot (4812 lines, 21 files) which experience shows can hurt performance. The experience pool warns: "Bot bloat kills performance." v17 leads #2 (v9 at 1625.9) by ~17 points. Room for improvement through targeted parameter tuning and streamlined logic.

# Recent Match Analysis
Match analysis tool unavailable. Using experience pool insights instead:

KEY WEAKNESSES TO ADDRESS:
1. **Bot bloat**: v17 has 4812 lines across 21 files. strategy.py alone is 1245 lines. Experience lesson #26: "Bot bloat kills performance. v17 at 4812 lines vs v11 at 3582 active lines. More code ≠ better."
2. **EQR air values too conservative**: Lesson #6: "v11 uses IP=0.72/OOP=0.62 — too conservative." Lower values (fold air faster, save chips) tested better.
3. **CBet exploitation thresholds**: Lesson #18: "Can be tightened: 0.65/0.40 → 0.60/0.35 for earlier exploitation."
4. **River overbet limited to nut-only**: Lesson #28: "Leaves EV on table. Strong-tier hands (straights, sets, high flushes) on dry rivers can extract 1.3-1.7x pot." v17 may already have this, verify.
5. **Thin cap formula**: Lesson #14: "wetness-aware formula (0.46+0.08*wet+0.05*round) superior to flat thresholds."
6. **Parameter changes compound**: Lesson #27: "Tuning EQR + CBet thresholds + anti-lock together gives synergistic gains."

STRENGTHS TO PRESERVE:
- v17's modular structure (21 files) is actually well-organized
- Strong draw analysis, bluff profiles, preflop strategy modules
- Anti-lock parameters calibrated
- River overbet logic
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v17 → v20
Bot directory: bots/claude_v17/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me start by reading the essential data files and understanding the current state.
The MCP read_file tool isn't suitable for local files. Let me use bash to read all the necessary files.
Now I have a thorough understanding of the codebase. Let me check one more critical detail — whether the dead files are truly unused:
Now I have complete understanding. Let me update the experience pool with my findings:
Now I have all the data I need. Let me produce the final output:

```json
{
  "analysis": "## Strategic Analysis: v17 → v20\n\n### Rating Landscape\nv17 peaked at 1642.7 (period 146) but dropped to 1621.4 (latest). v9 is remarkably stable at 1625.9 and may overtake v17. The field is compressed to ~25 points — small edges compound enormously. v17's decline is likely Glicko variance, but we can still improve the bot.\n\n### Codebase Audit\n**Critical discovery**: v17 has 6 dead code files (4,107 lines) that are NOT imported anywhere: draws.py, bluffs.py, postflop_decision.py, preflop.py, main_backup.py, odds.py. Active code is only 3,646 lines across 15 files. These dead files risk confusing future workers who might edit the wrong file. Strategy.py at 1,245 lines is the largest active file.\n\n### Root Cause Analysis: Why v17 Regressed\n1. **EQR air over-folding** (experience pool lesson #6): `realized_postflop_equity()` in strategy.py uses air EQR 0.68 IP / 0.56 OOP. Proven optimal is 0.72/0.62. This means v17 folds air hands too aggressively, bleeding chips in marginal spots.\n2. **Missing opponent style classification**: v17 tracks VPIP, PFR, aggression, fold_to_raise but never classifies into nit/maniac/station/fold-heavy. This proven v11 feature provides +2-3 pts of adaptive play.\n3. **CBet exploitation thresholds too loose**: strategy.py uses 0.65/0.40 for CBet rate exploitation. Tighter 0.60/0.35 gives earlier exploitation.\n4. **No river overbet for strong-tier hands**: Only nut hands get river overbet. Straights, sets, high flushes on dry rivers can extract 1.3-1.7x pot — significant missed EV.\n5. **Flat thin_cap formula**: betting.py uses hardcoded 0.30/0.38. Wetness-aware formula (0.46+0.08*wet+0.05*round) is superior.\n\n### Study of v9 (stable #2 at 1625.9)\nv9 has a simpler architecture (6,662 total lines but only ~3,400 active, in 8 files). Its stability comes from well-calibrated parameters rather than complex features. Key insight: v9's EQR values are closer to the proven 0.72/0.62 baseline.\n\n### Strategy for v20\n**Compound parameter fixes + missing features**: The experience pool consistently shows that parameter changes compound synergistically (lesson #25). We fix ALL proven parameter regressions simultaneously AND add the missing opponent style classification + river overbet expansion.",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["opponent.py", "strategy.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect for a Texas Hold'em poker bot. You are modifying bot v17 to create v20.\n\n## CRITICAL RULES\n- You may ONLY modify `opponent.py` and `strategy.py` in the bot directory.\n- You may add NEW functions and refactor EXISTING logic.\n- You must NOT change any numeric constants/thresholds — that is the Hyperparameter Tuner's job.\n- Keep strategy.py under 1,300 lines total. If you add code, consider if any existing code can be simplified.\n- DO NOT create new files. DO NOT modify any other files.\n\n## CONTEXT\nv17 is at bots/claude_v17/. It has 15 active files. The import chain is: main.py → strategy.py → postflop.py (re-exports from hand_evaluation.py, board_analysis.py, draw_analysis.py, bluff_analysis.py), opponent.py, simulation.py, state.py, tournament.py, betting.py.\n\n## TASK 1: Add `classify_opponent_style` to opponent.py\n\nAdd a function `classify_opponent_style(opponent_model)` that returns one of: 'nit', 'maniac', 'station', 'fold_heavy', or 'balanced'.\n\nClassification rules (using opponent_model fields that already exist):\n```python\ndef classify_opponent_style(opponent_model):\n    \"\"\"Classify opponent into behavioral style for adaptive play.\"\"\"\n    vpip = opponent_model['vpip']\n    pfr = opponent_model['pfr']\n    aggr = opponent_model['postflop_aggr']\n    ftr = opponent_model['fold_to_raise']\n    confidence = opponent_model['confidence']\n    \n    # Need minimum confidence to classify\n    if confidence < 0.20:\n        return 'balanced'\n    \n    # Nit: tight preflop, tight postflop\n    if vpip < 0.42 and pfr < 0.18:\n        return 'nit'\n    \n    # Maniac: very loose + aggressive\n    if vpip > 0.68 and aggr > 0.48:\n        return 'maniac'\n    \n    # Station: loose preflop, passive postflop (calls too much)\n    if vpip > 0.62 and aggr < 0.30:\n        return 'station'\n    \n    # Fold-heavy: folds too much to raises\n    if ftr > 0.55:\n        return 'fold_heavy'\n    \n    return 'balanced'\n```\n\n## TASK 2: Integrate opponent style into strategy.py get_action()\n\nIn the `get_action()` function in strategy.py, after building the opponent_model (around line 620), add:\n```python\nopponent_style = classify_opponent_style(opponent_model)\n```\nImport classify_opponent_style from opponent.\n\nThen create a helper function `style_threshold_adjustment(opponent_style, confidence)` that returns threshold deltas:\n```python\ndef style_threshold_adjustment(opponent_style, confidence):\n    \"\"\"Return threshold adjustments based on opponent style.\"\"\"\n    weight = min(1.0, confidence * 1.5)  # Scale by confidence\n    adj = {'bluff_delta': 0.0, 'call_delta': 0.0, 'value_sizing_delta': 0.0}\n    \n    if opponent_style == 'nit':\n        # Bluff more (they fold), value bet smaller (they fold to big bets)\n        adj['bluff_delta'] = 0.04 * weight\n        adj['call_delta'] = 0.02 * weight  # Respect their bets more\n        adj['value_sizing_delta'] = -0.05 * weight\n    elif opponent_style == 'maniac':\n        # Bluff less (they call/raise), value bet bigger (they pay off)\n        adj['bluff_delta'] = -0.06 * weight\n        adj['call_delta'] = -0.03 * weight  # Call lighter\n        adj['value_sizing_delta'] = 0.08 * weight\n    elif opponent_style == 'station':\n        # Never bluff, value bet wider and bigger\n        adj['bluff_delta'] = -0.08 * weight\n        adj['call_delta'] = -0.02 * weight\n        adj['value_sizing_delta'] = 0.06 * weight\n    elif opponent_style == 'fold_heavy':\n        # Bluff more, they fold to raises\n        adj['bluff_delta'] = 0.06 * weight\n        adj['call_delta'] = 0.01 * weight\n        adj['value_sizing_delta'] = -0.03 * weight\n    \n    return adj\n```\n\nIntegrate these deltas into the relevant threshold calculations in get_action():\n- `bluff_delta` adjusts the bluff-related thresholds (river_bluff_threshold, probe_fold_threshold, blocker_raise_threshold, etc.)\n- `call_delta` adjusts call_margin\n- `value_sizing_delta` gets passed to choose_raise via match_sizing_delta (add it to match_profile['sizing_delta'])\n\n## TASK 3: Add river overbet for strong-tier hands on dry rivers\n\nIn strategy.py's `get_action()`, in the NO-RAISE section (to_call == 0, around line 1000+), add logic BEFORE the final `return 0` to allow overbetting with strong-tier hands on dry/static rivers:\n\nAdd a helper function:\n```python\ndef should_river_overbet(value_profile, board_texture, pot, nutted_risk, match_profile, round_idx):\n    \"\"\"Check if we should overbet (1.3-1.7x pot) with a strong hand on a dry river.\"\"\"\n    if round_idx != 3:\n        return False, 0.0\n    if value_profile is None or value_profile['tier'] not in ('strong', 'nut'):\n        return False, 0.0\n    if board_texture is None:\n        return False, 0.0\n    if board_texture['dynamic']:\n        return False, 0.0\n    if board_texture['wetness'] > 0.35:\n        return False, 0.0\n    if nutted_risk is not None and nutted_risk['risk'] > 0.04:\n        return False, 0.0\n    if match_profile['chase'] > 0.50:\n        return False, 0.0\n    \n    wetness = board_texture['wetness']\n    tier = value_profile['tier']\n    \n    if tier == 'nut':\n        overbet_ratio = 1.8 - 0.5 * wetness  # 1.45-1.8x pot\n        return True, overbet_ratio\n    elif tier == 'strong':\n        overbet_ratio = 1.4 - 0.3 * wetness  # 1.1-1.4x pot\n        return True, overbet_ratio\n    \n    return False, 0.0\n```\n\nIn get_action(), just before the final `return 0`, add:\n```python\n# River overbet for strong/nut hands on dry boards\noverbet_ok, overbet_ratio = should_river_overbet(value_profile, board_texture, pot, nutted_risk, match_profile, round_idx)\nif overbet_ok and to_call == 0 and round_idx == 3:\n    overbet_amount = int(pot * overbet_ratio)\n    overbet_amount = max(state['min_raise_action'], overbet_amount)\n    if overbet_amount < my_chips and overbet_amount > 0:\n        return overbet_amount\n```\n\n## TASK 4: Delete dead code files\nDelete these 6 files from the bot directory (they are NOT imported anywhere):\n- draws.py (duplicate of draw_analysis.py)\n- bluffs.py (duplicate of bluff_analysis.py)\n- postflop_decision.py (duplicate of strategy.py helpers)\n- preflop.py (duplicate of preflop_strategy.py)\n- main_backup.py (old monolithic backup)\n- odds.py (not imported by any active file)\n\n## VERIFICATION\nAfter all changes:\n1. Run `python -c \"import sys; sys.path.insert(0, '.'); from strategy import get_action; print('OK')\"` from the bot directory to verify imports work.\n2. Run `python -c \"from opponent import classify_opponent_style; m = {'vpip':0.35,'pfr':0.12,'postflop_aggr':0.25,'fold_to_raise':0.50,'confidence':0.6}; print(classify_opponent_style(m))\"` to verify classification returns 'nit'.\n3. Ensure strategy.py stays under 1,300 lines."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["strategy.py", "betting.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner for a Texas Hold'em poker bot. You are modifying bot v17 to create v20.\n\n## CRITICAL RULES\n- You may ONLY modify numeric constants, thresholds, and magic numbers.\n- You may ONLY modify `strategy.py` and `betting.py`.\n- You MUST NOT add new functions, classes, imports, or change control flow.\n- You MUST NOT modify any algorithmic logic — only numbers.\n- DO NOT create new files. DO NOT modify any other files.\n\n## CONTEXT\nv17 is at bots/claude_v17/. The experience pool has identified specific parameter regressions that compound together. Fix ALL of them.\n\n## CHANGE 1: Fix EQR air values in strategy.py\n\nIn the `realized_postflop_equity()` function in strategy.py (around line 306), change:\n```python\n# OLD (over-folds air hands):\neqr = 0.68 if has_position else 0.56\n\n# NEW (proven optimal from experience pool lesson #6):\neqr = 0.72 if has_position else 0.62\n```\n\nThis is the SINGLE most impactful change. The old values cause over-folding on air hands, bleeding chips.\n\n## CHANGE 2: Fix EQR draw OOP values in strategy.py\n\nIn the same `realized_postflop_equity()` function, in the OOP draw block (around line 318), change:\n```python\n# OLD:\neqr = 0.85 if round_idx == 1 else 0.75\n\n# NEW (experience pool lesson #24):\neqr = 0.88 if round_idx == 1 else 0.78\n```\n\n## CHANGE 3: Fix EQR pair values in strategy.py\n\nIn the same function, in the weak pair block (around line 329), change:\n```python\n# OLD:\neqr = 0.84 if has_position else 0.73\n\n# NEW (experience pool says v11's 0.86/0.78 is better):\neqr = 0.86 if has_position else 0.78\n```\n\n## CHANGE 4: Tighten CBet exploitation thresholds in strategy.py\n\nIn `get_action()`, find the CBet exploitation block (around lines 881-884):\n```python\n# OLD:\ncbet_rate = opponent_model.get('cbet_rate', 0.55)\nif round_idx == 1 and spot_info['facing_postflop_aggression'] and cbet_rate > 0.65:\n    call_margin -= 0.02\nif round_idx == 1 and spot_info['facing_postflop_aggression'] and cbet_rate < 0.40:\n    call_margin += 0.02\n\n# NEW (experience pool lesson #16 — tighter thresholds for earlier exploitation):\ncbet_rate = opponent_model.get('cbet_rate', 0.55)\nif round_idx == 1 and spot_info['facing_postflop_aggression'] and cbet_rate > 0.60:\n    call_margin -= 0.02\nif round_idx == 1 and spot_info['facing_postflop_aggression'] and cbet_rate < 0.35:\n    call_margin += 0.02\n```\n\n## CHANGE 5: Upgrade thin_cap to wetness-aware formula in betting.py\n\nIn `choose_raise()` in betting.py, find the thin_cap block (around line 101-104):\n```python\n# OLD:\nthin_cap = None\nif value_plan.get('thin_control', False) and value_profile.get('tier') != 'nut' and to_call == 0:\n    thin_cap = 0.30 if round_idx <= 2 else 0.38\n    ratio = min(ratio, thin_cap)\n\n# NEW (experience pool lesson #14 — wetness-aware formula):\nthin_cap = None\nif value_plan.get('thin_control', False) and value_profile.get('tier') != 'nut' and to_call == 0:\n    thin_cap = 0.46 + 0.08 * wetness + 0.05 * round_idx\n    ratio = min(ratio, thin_cap)\n```\n\nNote: `wetness` is already available in the function scope (extracted from `board_texture` earlier in the function).\n\n## VERIFICATION\nAfter all changes, from the bot directory run:\n1. `python -c \"import sys; sys.path.insert(0, '.'); from strategy import get_action; print('strategy OK')\"`\n2. `python -c \"import sys; sys.path.insert(0, '.'); from betting import choose_raise; print('betting OK')\"`\n3. Grep to verify: `grep '0.72 if has_position else 0.62' strategy.py` should match\n4. Grep to verify: `grep '0.88 if round_idx' strategy.py` should match\n5. Grep to verify: `grep 'cbet_rate > 0.60' strategy.py` should match\n6. Grep to verify: `grep '0.46 + 0.08' betting.py` should match"
    }
  ]
}
```
