
[WORKER 2 (preflop_specialist) PROMPT]
=============================
# System Rule
You are a highly skilled Coding Worker Agent playing the role of: **preflop_specialist**.
You must directly edit the source files in `bots/claude_v14/` to implement the Master's instructions.
The bot MUST correctly interface with the game engine via `main.py` (reads JSON from stdin, writes JSON `{"response": int}` to stdout).

# CRITICAL: Tool Usage Rules
- **Use the Read tool** to read source files. Example: Read `bots/claude_v14/strategy.py`
- **Use the Bash tool** to run compile checks, smoke tests, and git commands.
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem.

# Role Boundary Rules
Your role determines what you CAN and CANNOT do:

## If you are a "Hyperparameter Tuner":
- **ALLOWED**: Changing numeric constants (e.g., `BLUFF_THRESHOLD = 0.15` → `0.20`)
- **ALLOWED**: Adjusting float thresholds (e.g., `POT_ODDS_CUTOFF = 2.0` → `2.3`)
- **ALLOWED**: Modifying integer conditions (e.g., `MIN_RAISE_HAND = 8` → `10`)
- **FORBIDDEN**: Adding new functions, classes, or methods
- **FORBIDDEN**: Adding new `if/for/while` blocks or changing control flow
- **FORBIDDEN**: Importing new modules
- If you accidentally add any non-numeric changes, revert with `git checkout -- <file>`

## If you are an "Algorithmic Logic Architect":
- **ALLOWED**: Adding new functions and methods
- **ALLOWED**: Refactoring existing logic and algorithms
- **ALLOWED**: Adding imports and new modules
- **FORBIDDEN**: Changing well-tuned constants unless structurally required
- Example of "structurally required": If your new function changes how a constant is used, you may adjust it. But don't randomly tune `OPENING_RANGE_THRESHOLD` or `VPIP_PRIORS`.

You have access to `evolution_workspace/reference_bots/` containing 6 strong bots (`bot1` to `bot6`). You may read them as reference.
Other Worker Agents may have recently modified the codebase before you. Read the attached Context Files carefully.

# Master Architect's Specific Prompt For You
CRITICAL REVISION NEEDED:
""

ORIGINAL:
In state.py, expand is_preflop_3bet_candidate to include: AQ (high=14, low=12 suited), AJ suited (high=14, low=11, suited), KQ suited (high=13, low=12, suited), and QJs (high=12, low=11, suited). In sizing.py, in the bb_vs_raise section of choose_preflop_spot_action, expand the 3-bet bluff range: lower the fold_to_raise threshold from 0.45 to 0.42 and expand the strength range from [0.38, 0.52] to [0.36, 0.54]. Also increase the bluff frequency cap from 0.6 to 0.7.

# Action
Please write the Python code to fulfill your objective. After editing:

1. **Verify your changes**: Run `git diff bots/claude_v14/` to review what you changed.
   Ensure no unintended modifications to files outside your assigned `target_files`.
2. If you see unexpected changes, fix them before finishing.
3. **Run quality checks**:
   - Compile check: `python -m py_compile bots/claude_v14/main.py` (and other .py files you changed)
   - Smoke test: `python evolution_workspace/smoke_tester.py bots/claude_v14/main.py`
   - If any check fails, fix the errors before finishing.

# Self-Review Before Finishing
After all quality checks pass, run these final checks:

1. **Role Boundary Check**: Run `git diff bots/claude_v14/` one more time and review your changes carefully.
   - If you are a **Hyperparameter Tuner**: Verify that ALL your changes are limited to numeric constants, thresholds, or magic numbers. If you added new functions, classes, or control flow logic (if/for/while blocks), REMOVE them immediately.
   - If you are an **Algorithmic Logic Architect**: Verify that you did not change well-tuned constants (thin_cap, open thresholds, VPIP/PFR priors) unless structurally required by your new algorithm.

2. **Target File Check**: Ensure you ONLY modified files in your assigned `target_files`. If you touched other files, revert those changes with `git checkout -- <file>`.

3. **Protocol Check**: Verify the bot still outputs `{"response": <int>}` via stdout. The action encoding is: 0=call/check, -1=fold, -2=all-in, >0=raise amount.

4. If you find any violations, fix them and re-run the quality checks.

=============================
[CLAUDE OUTPUT]
I'll start by reading the target files to understand the current code structure.
Let me read the relevant files first.
Now I have a clear picture of both files. Let me implement the changes:

### Change 1: `state.py` — Expand `is_preflop_3bet_candidate`
Currently it only returns `True` for pairs and AK+ (high=14, low>=12). Need to add: AQ suited, AJ suited, KQ suited, QJs.

### Change 2: `sizing.py` — Expand `bb_vs_raise` 3-bet bluff range
- Lower fold_to_raise threshold from 0.45 to 0.42
- Expand strength range from [0.38, 0.52] to [0.36, 0.54]
- Increase bluff frequency cap from 0.6 to 0.7
Now the sizing.py changes in the `bb_vs_raise` section:
Now I can apply the sizing.py edit:
Good. Now let me verify the changes and run quality checks:
I can see there are changes from other Worker Agents too (opponent.py, postflop.py, strategy.py). My assigned changes are only in `state.py` and `sizing.py`. Let me verify my specific changes are correct, then run the quality checks:
All quality checks pass. Now let me do the self-review, focusing on my specific changes:
Hmm, the sizing.py diff is empty. Let me check if my edit was applied correctly:
My sizing.py changes are correctly in place (verified by grep). The empty git diff for sizing.py is because sizing.py was apparently already at the index state matching my edit (it was a new file added by another worker). Let me verify by checking git status:
Right — `sizing.py` is an untracked file (newly created by another worker). That's why there's no diff for it. The file exists with my edits. Let me do a final self-review:

## Self-Review Summary

### Changes I made:

**1. `state.py` — `is_preflop_3bet_candidate()`** (lines 54-69):
- **Before**: Only recognized pairs and AK+ (high=14, low≥12)
- **After**: Also recognizes suited broadway premiums: AQs (14,12 suited), AJs (14,11 suited), KQs (13,12 suited), QJs (12,11 suited)
- This is structural/algorithmic — expanding the candidate function with new conditional branches, as instructed by the Master.

**2. `sizing.py` — `bb_vs_raise` section** (lines 296-299):
- Strength range: `[0.38, 0.52]` → `[0.36, 0.54]`
- Fold-to-raise threshold: `> 0.45` → `> 0.42`
- Bluff frequency base: `(fold_to_raise - 0.45)` → `(fold_to_raise - 0.42)`
- Bluff frequency cap: `0.6` → `0.7`
- All numeric/threshold changes as instructed.

### Role Boundary Check ✅
- My changes to `state.py` expand the hand candidate logic (structural — adding conditional branches for suited broadway hands). This is appropriate for the Algorithmic Logic Architect role.
- My changes to `sizing.py` are purely numeric threshold adjustments (0.38→0.36, 0.52→0.54, 0.45→0.42, 0.6→0.7). These are appropriate for the preflop specialist assignment.

### Target File Check ✅
- Only modified `state.py` and `sizing.py` — both are my assigned target files.

### Protocol Check ✅
- The bot still outputs `{"response": <int>}` via stdout. No changes to the I/O protocol.

### Quality Checks ✅
- All `.py` files compile successfully
- Smoke test passed
