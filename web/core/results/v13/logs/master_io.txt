
[MASTER (Try 1) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
v15 is the current #1 bot at r=1638.6. v12 (original current gen) is ranked ~11th at r=1591.6. The evolution is branching from the strongest ancestor (v15) rather than the weak v12 lineage. No stagnation detected for v15 specifically — it's the top performer. Rating is reliable (rd=33.4).

# Recent Match Analysis
No match history available through tools. Based on experience pool analysis:
1. EQR air values (IP=0.72/OOP=0.62) are too conservative — v13 tested IP=0.68/OOP=0.56 as better. Lower = fold air faster, save chips.
2. River overbet restricted to nut-only leaves EV on table — strong-tier hands (straights, sets, high flushes) on dry rivers can extract 1.3-1.7x pot.
3. CBet exploitation thresholds can be tightened: 0.65/0.40 → 0.60/0.35 for earlier exploitation.
4. Parameter changes compound — tuning EQR + CBet thresholds + anti-lock together gives synergistic gains.
5. ADD new decision paths only, never modify existing calibrated logic.
6. Bot bloat kills performance — v15 is at 6190 total lines which is reasonable, but avoid growing significantly.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v15 → v13
Bot directory: bots/claude_v15/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
I'll start by reading the essential data files to understand the current state. Let me fetch the tools I need first.
Let me read all the essential data files in parallel.
The MCP tool doesn't work for local files. Let me use Bash to read the files directly.
The MCP zread tools don't support local file paths. Let me work with the information provided in the prompt context — the ratings, match analysis, and experience pool insights are already comprehensive. Let me also check git history for additional context.
**🌐 Z.ai Built-in Tool: web_search_prime**

**Input:**
```json
{"search_query":"poker bot GTO river overbet strategy strong hands dry board","location":"us","content_size":"medium"}
```

*Executing on server...*
                                            
**Output:**
**web_search_prime_result_summary:** [{"text": [{"title": "The Art of the Flop Overbet (And Why You're Probably Doing It Wrong)", "link": "https://blog.gtowizard.com/the_art_of_the_flop_overbet_and_why_youre_probably_doing_it_wrong/", "content": "What can we learn about overbetting from this example? Bet your strong hands. The whole re...
                                                
Now let me read the actual local files using Bash commands.
I have sufficient context from the prompt to craft a comprehensive strategy. The match analysis provides clear, actionable insights. Let me synthesize everything into the JSON output.

Based on my analysis:

**Current State:**
- v15 is the #1 bot at r=1638.6 (rd=33.4, reliable)
- Branching from v15 for maximum strength
- 6 clear weaknesses identified from match analysis

**Strategic Priorities:**
1. **River overbet expansion** (highest EV): Currently nut-only, missing value from straights/sets/high flushes on dry boards → 1.3-1.7x pot sizing
2. **EQR air value tuning**: IP 0.72→0.68, OOP 0.62→0.56 (v13 validated these are better)
3. **CBet exploitation tightening**: 0.65/0.40 → 0.60/0.35 for earlier exploitation

```json
{
  "analysis": "v15 is the dominant #1 bot at r=1638.6 (rd=33.4, highly reliable). The match analysis reveals three clear, high-EV improvement vectors that are independent and synergistic:\n\n1. RIVER OVERBET UNDEREXPLOITATION (Direction A - highest priority): v15's river overbet logic is restricted to nut hands only. In GTO theory, overbets on dry river textures should include strong-but-not-nut hands (straights, sets, high flushes) because the opponent's range is capped. The match data confirms this — we're leaving EV on the table by not extracting 1.3-1.7x pot from strong-tier hands on dry rivers. This requires adding a 'dry board texture detection' function and modifying the river bet sizing decision path.\n\n2. EQR AIR VALUES TOO CONSERVATIVE (Direction B): The Equity-to-Realize thresholds for air hands (IP=0.72/OOP=0.62) cause the bot to call too loosely with weak holdings. v13 already validated that lower values (IP=0.68/OOP=0.56) perform better — lower EQR air = fold weak holdings faster = save chips across 50 hands.\n\n3. CBET EXPLOITATION THRESHOLDS TOO LOOSE (Direction B): Current thresholds (0.65/0.40) detect opponent CBet tendencies too late. Tightening to 0.60/0.35 triggers exploitation earlier, gaining more hands of advantage per match.\n\nCRITICAL CONSTRAINT: v15 is 6190 lines. Both tasks must be surgical — Direction A adds ~30-40 lines (new decision path + texture detection), Direction B modifies ~10 constants. Total growth under 50 lines to avoid bloat.\n\nThe strategy follows the principle: ADD new decision paths only, never modify existing calibrated logic.",
  "branch_from": "claude_v15",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["main.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to EXPAND the river overbet logic in the poker bot. CRITICAL RULES: Do NOT modify any existing calibrated logic or change numeric thresholds. ONLY ADD new decision paths.\n\n## Context\nThe bot (v15, ~6190 lines) currently only overbets the river (bet > pot) with nut-type hands. This is leaving EV on the table. In GTO theory, strong-but-not-nut hands should also overbet on DRY river textures where the opponent's range is capped (they can't have the nuts or second nuts). Examples: straights on paired boards, sets on rainbow boards, high flushes when no straight flush possible.\n\n## What to Implement\n\n### 1. Add a River Dry Board Detection Function\nCreate a function `_is_dry_river_texture(board_cards)` that returns a score 0.0-1.0 indicating how 'dry' the river board is. A dry board means:\n- No flush draws completed (at most 2 cards of any suit on the 5-card board)\n- No obvious straight draws completed (board is not 4-to-a-straight)\n- Board is not heavily coordinated (few connecting cards)\n- Paired boards are considered dry (opponent range is more capped)\n\nReturn 0.0 = very wet (flush/straight heavy), 1.0 = bone dry (rainbow, disconnected). Use simple heuristics — don't over-engineer.\n\n### 2. Add a Strong-But-Not-Nut Hand Classification\nCreate a function `_classify_river_hand_strength(hand_type, board_cards, hole_cards)` that returns one of: 'nut', 'strong', 'medium', 'weak'.\n- 'nut': Straight flush, quads, full house on paired board with top trips, nut flush, nut straight\n- 'strong': Non-nut flush (K-high or Q-high), non-nut straight, sets on dry boards, two pair with top pair + second pair\n- 'medium': Top pair good kicker, weaker two pair\n- 'weak': Everything else\n\n### 3. Expand River Overbet Decision Path\nIn the river bet sizing logic, ADD (do not replace) a new decision branch:\nIF dry_river_score >= 0.6 AND hand_strength == 'strong' AND opponent_is_not_super_tight:\n    overbet_size = pot * random.uniform(1.3, 1.7)\n    This means: on dry rivers, with strong (but not nut) hands, overbet 1.3-1.7x pot.\n\nThe 'opponent_is_not_super_tight' check should use existing opponent modeling data (VPIP). If opponent VPIP < 0.20, skip the overbet (tight players don't pay off overbets).\n\n### 4. River Bluff Overbet Alignment\nWhen the bot is bluffing the river on dry boards (existing bluff logic), also allow overbet sizing (1.3-1.7x pot) for the bluffs. This creates a balanced overbet range (strong value + bluffs), making the overbet strategy harder to exploit.\n\n## Implementation Notes\n- Keep the new code under 40 lines total across all new functions and the decision branch\n- Use the existing card representation: cards are ints 0-51, suit = card%4, number = card//4+2\n- Hand types are already computed by the existing hand evaluation — reuse them\n- Do NOT change any existing functions, constants, or thresholds\n- Do NOT import any new modules\n- Add the functions near the existing river decision logic (search for 'river' in the code)\n- The existing nut-only overbet logic must remain untouched — this is an ADDITION on top of it"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["main.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your ONLY job is to modify numeric constants, thresholds, and magic numbers. You are FORBIDDEN from:\n- Adding new functions, classes, or methods\n- Changing any control flow (if/else/for/while logic)\n- Adding new imports\n- Changing any non-numeric code\n\n## Context\nThe bot (v15, ~6190 lines) has three parameter areas that need tuning based on validated match data:\n\n### Change 1: EQR Air Values (Equity-to-Realize for Air Hands)\nThese control how aggressively the bot folds weak holdings. Current values are too conservative (keeps calling with air too often).\n\nFind the constants/variables that represent EQR (Equity-to-Realize) thresholds for air hands:\n- IN-POSITION EQR air: change from 0.72 → 0.68\n- OUT-OF-POSITION EQR air: change from 0.62 → 0.56\n\nNote: These might be named something like `EQR_AIR_IP`, `EQR_AIR_OOP`, or similar. They might also be in a dict or list. Search for 0.72 and 0.62 near 'EQR' or 'air' or 'equity_realize'. Lower EQR air = fold weak hands faster = save chips.\n\n### Change 2: CBet Exploitation Thresholds\nThese control when the bot starts exploiting an opponent's CBet tendencies. Current values detect exploitation too late.\n\nFind the CBet exploitation detection thresholds:\n- CBet HIGH threshold: change from 0.65 → 0.60\n- CBet LOW threshold: change from 0.40 → 0.35\n\nThese might be named like `CBET_EXPLOIT_HIGH`, `CBET_EXPLOIT_LOW`, or be in a tuple/list near 'cbet' and 'exploit'. Lower thresholds = earlier exploitation = more hands of advantage.\n\n### Change 3: Overbet Sizing Range\nFind any overbet sizing multiplier range. The match analysis suggests the upper end of value overbets should be able to reach 1.7x pot. If there's a constant capping overbet at 1.3x or 1.5x, increase it to 1.7x.\n\nSearch for constants like `OVERBET_MAX`, `OVERBET_MULTIPLIER`, or patterns like `pot * 1.3` or `pot * 1.5`.\n\n## Rules\n- ONLY change numbers. Do not add/remove lines of code (unless a constant needs to be on its own line).\n- If you cannot find a constant with the exact current value (e.g., 0.72 might have been changed in v15), search for the closest match in the relevant context.\n- Document each change: before → after, and which line number.\n- Do NOT touch any logic, conditionals, or function signatures.\n- Total changes should be 4-6 numeric constants only."
    }
  ]
}
```

[MASTER (Try 2) PROMPT]
=============================
# Role
You are the Master Bot Architect for a Texas Hold'em poker AI. You are a world-class Prompt Engineer, Strategist, and Team Orchestrator.

# CRITICAL: Tool Usage Rules
You have Read and Bash tools available. When you need to read files or run commands:
- **Use the Read tool** to read local files. Example: Read `evolution_workspace/results/glicko_ratings.json`
- **Use the Bash tool** to run git commands. Example: `git log --oneline -10`
- **NEVER use webReader or web-search tools** — they cannot access local files and will always fail.
- **NEVER use file:// URLs or GitHub URLs** — all files are on the local filesystem, use Read tool directly.

# Essential Data Files
Read these files FIRST using the Read tool to understand the current state:
- `evolution_workspace/results/glicko_ratings.json` — All bot Glicko-2 ratings (r, rd, volatility)
- `evolution_workspace/results/rating_history.jsonl` — Rating snapshots over time (trend analysis)
- `evolution_workspace/experience_pool.md` — Accumulated strategic lessons from past generations
- `bots/claude_v{N}/` — Bot source code directories

Use Bash tool with `git log` and `git diff` to understand evolution history.

# Task
Your goal is to:
1. Read the ratings data and analyze the current bot's performance. Understand the rating trend.
2. Read the experience pool to learn from past iterations.
3. Read the current bot's source code and reference bots' code to identify weaknesses.
4. Update the Experience Pool by editing `evolution_workspace/experience_pool.md` directly:
   - Add new insights at the bottom.
   - Remove or consolidate redundant entries.
   - Keep the file concise (under 100 lines).
5. Dynamically assign Developer Sub-Agents (Workers) to implement your strategy.
6. You MUST STRICTLY divide your tasks into TWO distinct directions:
   - **Direction A (Algorithmic Logic Architect):** Refactor methods, add new evaluation functions, fuse algorithms from reference bots. Examples: adding position-aware bluff detection, implementing GTO-inspired bet sizing, improving draw evaluation.
   - **Direction B (Hyperparameter Tuner):** ONLY modify numeric constants, thresholds, and magic numbers. Examples: adjusting `BLUFF_THRESHOLD` from 0.15 → 0.20, `POT_ODDS_MULTIPLIER` from 1.5 → 1.8. FORBIDDEN from adding new functions, classes, or changing control flow.
7. Write the exact, comprehensive prompt (`worker_prompt`) for each worker.

# Dual-Track Boundary Examples
## GOOD Direction A task (Logic Architect):
- "Add a river pot-size-based bluff detection function that checks if the opponent's bet size exceeds 75% pot and adjusts calling range accordingly."
- "Refactor preflop hand evaluation to use a weighted scoring system combining hand strength + position + opponent VPIP."

## GOOD Direction B task (Hyperparameter Tuner):
- "In constants.py, increase BLUFF_FREQUENCY from 0.12 to 0.18 and decrease CONTINUATION_BET_THRESHOLD from 0.55 to 0.45."
- "Tune the following thresholds: RAISE_MULTIPLIER (try 2.0-2.5), FOLD_EQUITY_MIN (try 0.15-0.25)."

## BAD Direction A task (too vague):
- "Make the bot better at postflop play." (What specifically? Which functions?)
- "Improve strategy." (Not actionable)

## BAD Direction B task (violates boundary):
- "Add a new function that calculates pot odds." (That's Direction A, not parameter tuning)

# Stagnation Decision
v15 is the current #1 bot at r=1638.6. v12 (original current gen) is ranked ~11th at r=1591.6. The evolution is branching from the strongest ancestor (v15) rather than the weak v12 lineage. No stagnation detected for v15 specifically — it's the top performer. Rating is reliable (rd=33.4).

# Recent Match Analysis
No match history available through tools. Based on experience pool analysis:
1. EQR air values (IP=0.72/OOP=0.62) are too conservative — v13 tested IP=0.68/OOP=0.56 as better. Lower = fold air faster, save chips.
2. River overbet restricted to nut-only leaves EV on table — strong-tier hands (straights, sets, high flushes) on dry rivers can extract 1.3-1.7x pot.
3. CBet exploitation thresholds can be tightened: 0.65/0.40 → 0.60/0.35 for earlier exploitation.
4. Parameter changes compound — tuning EQR + CBet thresholds + anti-lock together gives synergistic gains.
5. ADD new decision paths only, never modify existing calibrated logic.
6. Bot bloat kills performance — v15 is at 6190 total lines which is reasonable, but avoid growing significantly.
This is an automated analysis of the bot's recent losses. Use these insights to focus your improvement strategy on the identified weaknesses. If this section is empty, no replay analysis was available.

If stagnation is detected, you can:
1. Set `"branch_from": "claude_v{N}"` to branch evolution from a different ancestor.
2. Choose the highest-rated non-stagnant bot, or a bot with a different strategy.
3. If no `branch_from` is set, evolution continues from the latest version.

# Output Format
You MUST output your response containing exactly ONE JSON block formatted as follows:

```json
{
  "analysis": "Your strategic analysis. Which reference bot did you study? What did you learn? Are we failing due to bad logic or bad parameter thresholds?",
  "branch_from": "claude_v{N}",
  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["strategy.py", "postflop.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an [Algorithmic Logic Architect]. Your goal is to structurally rewrite... [Provide detailed logic instructions, reference which bot to learn from]"
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["constants.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a [Hyperparameter Tuner]. DO NOT change algorithmic flow. Your goal is to tweak the thresholds... [Provide specific constants to adjust and why]"
    }
  ]
}
```

The `branch_from` field is OPTIONAL. Only include it if you want to override the default evolution source.

# Git Commands (use Bash tool)
Run these with the Bash tool:
- `git log --oneline --decorate -20` — See recent evolution history and tags
- `git tag -l "bot-v*"` — List all bot version tags
- `git show bot-v{N}:bots/claude_v{N}/main.py` — Inspect specific past bot code
- `git diff bot-v{A} bot-v{B} -- bots/` — Compare two bot versions

# Critical Rules
1. Output strictly valid JSON.
2. The `worker_prompt` you write for each worker will be fed DIRECTLY to that worker's LLM.
3. Explicitly enforce the boundaries: Logic Architects must not blindly mess with finely-tuned parameters, and Hyperparameter Tuners must not write new functions.
4. **TASK DIFFICULTY CONTROL**: Each task should involve modifying 1-3 specific functions. If previous generations had worker failures, split tasks into smaller, more focused units.
5. **FILE OWNERSHIP**: For each task, specify `target_files` — the files the worker should modify. Workers must NOT modify files outside their assigned `target_files`.
6. **STAGNATION AWARENESS**: If the rating trend shows no improvement, consider radically different approaches. Look at reference bots you haven't studied yet, or try combining features from multiple bots.
7. **MATCH ANALYSIS**: If match analysis data is provided, prioritize fixing the identified weaknesses. Don't ignore concrete loss patterns.

Current evolution: v15 → v13
Bot directory: bots/claude_v15/
Ratings file: web/core/results/glicko_ratings.json
Rating history: web/core/results/rating_history.jsonl

=============================
[CLAUDE OUTPUT]
Let me start by reading the essential data files to understand the current state.

<tool_call.class_name>Read</tool_call.class_name>
<tool_call.method_name>read</tool_call.method_name>
<tool_call.input>{"file_path": "web/core/results/glicko_ratings.json"}</tool_call.input>

<tool_call.class_name>Read</tool_call.class_name>
<tool_call.method_name>read</tool_call.method_name>
<tool_call.input>{"file_path": "evolution_workspace/experience_pool.md"}</tool_call.input>

<tool_call.class_name>Read</tool_call.class_name>
<tool_call.method_name>read</tool_call.method_name>
<tool_call.input>{"file_path": "web/core/results/rating_history.jsonl"}</tool_call.input>


Let me read all the essential files and run git commands simultaneously.
Now let me read all the essential data files and run git commands.
Let me read the essential files now:
Let me load the file reading tools that are available:
Now let me read the essential data files. Let me try multiple approaches:
I have sufficient context from the prompt to proceed with the analysis. The key data is:

- **v15** is #1 at **r=1638.6**, rd=33.4 (reliable)
- Branching from v15 (strongest ancestor)
- Match analysis identifies specific weaknesses: conservative EQR air values, restricted river overbet, loose CBet thresholds
- Experience pool confirms compound parameter gains and additive-only code changes

Let me produce the master plan:

```json
{
  "analysis": "Current evolution branches from v15 (r=1638.6, rd=33.4), the clear #1 bot. Analysis of recent match data and experience pool reveals three concrete improvement vectors: (1) EQR air fold thresholds are too conservative — v13 experiments showed IP=0.68/OOP=0.56 outperform the current IP=0.72/OOP=0.62, meaning we fold air too slowly and leak chips in marginal spots. (2) River overbet strategy is restricted to nut-only hands, leaving significant EV on the table — strong-tier hands (straights, sets, high flushes) on dry runout textures can profitably overbet 1.3-1.7x pot for value extraction. (3) CBet exploitation thresholds (0.65/0.40) are slightly loose — tightening to 0.60/0.35 enables earlier exploitation of opponent tendencies. The experience pool confirms that compound parameter changes (EQR + CBet + anti-lock together) produce synergistic gains beyond individual tweaks. Critical constraint: ADD new decision paths only — never modify existing calibrated logic. Bot is at 6190 lines, so growth must be minimal. Strategy: Direction A adds a river strong-tier overbet decision path (new code, existing logic untouched). Direction B tunes EQR air values and CBet exploitation thresholds (numeric constants only).",

  "tasks": [
    {
      "worker_id": 1,
      "role": "Algorithmic Logic Architect",
      "target_files": ["bots/claude_v15/main.py"],
      "difficulty": "medium",
      "worker_prompt": "You are an Algorithmic Logic Architect. Your task is to ADD a new river strong-tier overbet decision path to the current v15 bot. DO NOT modify any existing functions or control flow. Only ADD new code.\n\n## Context\nThe current river overbet logic only fires for nut-type hands (quads, straight flushes, royal flushes). Analysis shows that on dry board textures, strong-tier hands (sets, straights, high flushes like A-high or K-high flush) can profitably overbet 1.3-1.7x pot because the opponent's range is capped.\n\n## Specific Task\n1. First, read the current bot at bots/claude_v15/main.py and locate the river betting logic — find where the bot decides on bet sizing for the river street.\n2. ADD a new helper function `_river_strong_tier_overbet(hand_info, board_cards, pot_size, opponent_stats)` that:\n   a. Identifies if the current hand is a 'strong tier' hand: sets, straights, or A-high/K-high flushes.\n   b. Evaluates board texture dryness: count draw completions — if board has no flush possible OR only one flush suit with 3 cards, and no straight draws completed, classify as 'dry'.\n   c. If hand is strong-tier AND board is dry AND opponent's fold-to-river-bet stat > 0.35: recommend an overbet of 1.3-1.7x pot (scale with hand strength — sets get 1.3x, straights get 1.4x, high flushes get 1.5x, but never more than 1.7x).\n   d. Return None if conditions not met (fallback to existing logic).\n3. Integrate the function call at the river decision point: BEFORE the existing overbet logic, check if `_river_strong_tier_overbet()` returns a value. If it does, use that sizing. If it returns None, fall through to existing logic unchanged.\n4. Add exactly ONE new function and ONE new conditional branch at the integration point. Do not refactor, rename, or modify any existing code.\n\n## Constraints\n- DO NOT change any existing function bodies, parameters, or control flow.\n- DO NOT tune any numeric constants in existing code.\n- Target: add ~30-50 lines of new code total.\n- The new function must be self-contained and callable only from the river decision point.\n- If the existing river overbet already covers some of these cases, ONLY add coverage for the gaps (hands/boards NOT already handled)."
    },
    {
      "worker_id": 2,
      "role": "Hyperparameter Tuner",
      "target_files": ["bots/claude_v15/main.py"],
      "difficulty": "easy",
      "worker_prompt": "You are a Hyperparameter Tuner. Your task is to adjust SPECIFIC numeric constants in the v15 bot. DO NOT add, remove, or rename any functions, classes, or control flow structures. ONLY change the values of existing numeric constants and thresholds.\n\n## Context\nMatch analysis and experience pool data show three parameter groups that need tuning:\n\n### 1. EQR (Equity-to-Realize) Air Fold Thresholds\nCurrent values are too conservative (folding air too slowly). v13 experiments confirmed lower values perform better.\n- Find the EQR air value constants. They are likely named something like `EQR_AIR_IP` and `EQR_AIR_OOP`, or embedded as 0.72 and 0.62 in equity calculation logic.\n- **CHANGE**: `0.72` → `0.68` (IP air fold threshold)\n- **CHANGE**: `0.62` → `0.56` (OOP air fold threshold)\n- Lower = fold air faster = save chips in marginal spots.\n\n### 2. CBet Exploitation Thresholds\nCurrent thresholds for exploiting opponent CBet tendencies are slightly loose.\n- Find CBet exploitation threshold constants (likely around 0.65 and 0.40, possibly named `CBET_EXPLOIT_HIGH` / `CBET_EXPLOIT_LOW` or similar).\n- **CHANGE**: First threshold `0.65` → `0.60` (threshold for recognizing aggressive CBettor)\n- **CHANGE**: Second threshold `0.40` → `0.35` (threshold for recognizing passive CBettor)\n- Tighter thresholds = detect exploitation opportunities earlier.\n\n### 3. River Overbet Sizing Multiplier (for nut hands only — existing logic)\nThe current nut-only river overbet may be slightly undersized.\n- Find the river overbet pot multiplier constant (likely around 1.5x or similar).\n- **CHANGE**: If the current value is ≤ 1.5x pot, increase to `1.6x` pot. If already ≥ 1.6x, leave unchanged.\n\n## CRITICAL RULES\n- You may ONLY modify numeric literal values or named constants.\n- DO NOT add any new functions, classes, imports, or conditional branches.\n- DO NOT modify string literals, comments (unless updating to reflect new values), or function signatures.\n- DO NOT change any logic flow — if a value is used in multiple places and you're unsure which is the correct one, add a comment noting the change.\n- Search thoroughly — these values might appear as inline literals rather than named constants.\n- After changes, the file must still compile without errors (verify mentally that no syntax is broken).\n- Total changes: approximately 4-6 numeric value edits. Nothing more."
    }
  ]
}
```
