## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — cite SYMBOLS/PATHS, NEVER line numbers (they drift across gens) or commit hashes. rd=350 = Glicko DEFAULT/unrated, not "live top".
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). NEW gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic, not threshold nudges.
- Detector firing: grep symbols in `web/core/results/bot_telemetry/{bot}.jsonl`. ONLY v146+ trustworthy (v137-v145 = pre-Phase-A-fix daemon, 0 markers by construction).

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW INVARIANT (HARD RULE, AST-enforced)**: any fold/commitment guard's dispatch call-site MUST be BEFORE/inside the `to_call >= my_chips` early-return block, NEVER after it (else unreachable for stack-covering all-ins = the -15.5k/-20k cases). Fix = RELOCATE call-site, do NOT re-tune thresholds.
- **0%-FOLD STACK-OFF LEAK — v148 SHIPPED, UNVERIFIED (do NOT re-implement, DO verify)**: v148 extended `_spr_commitment_gate()` from river-only to ALL postflop streets (round_idx 1,2,3) with street-aware draw protection — 2 gens now shipped unverified. REMAINING = verify ≥100g daemon stack-offs drop; else calibrate SPR/made_strength thresholds. NEW closed-form-math axis NOT covered by fold-side exhaustion.
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v135 rewires REGRESSED; v138/v139 structurally INERT (placement shadow). NOTE: v147/v148 SPR gate is a NEW closed-form-math axis NOT covered here.
- Defensive `_river_*_guard` family [POSSIBLY EXHAUSTED] — v138/139 are call-side FOLD gates, v140/143 are bet-side own-jam BLOCKERS; ALL are DEFENSIVE, NONE are offense. EXHAUSTED per v144 dir-audit (HIGH-conf). The ACTUAL bet-side OFFENSE primitives — `_river_value_extraction_amplifier` (v141), `_vulnerable_made_protection_floor` (v142) — are NOT exhausted and remain open.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v139 FIXTURE≠LIVE lesson). v140+ comply (6/6 birth reqs + grep≥3x). stderr telemetry captured into bot_telemetry/{bot}.jsonl — ONLY v146+ trustworthy.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff) remain open PER AXIS, but each exhausts. pre-v146 offense detectors (v134/v141/v144/v145) show 0 fires in trustworthy telemetry → PENDING-VERIFICATION, not proven-LIVE. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing logs. Tiers: no consumers=dead; consumers but no firing proof=PENDING-VERIFICATION; firing proven=LIVE.
- preflop opponent-sizing-delta pattern (additive choose_raise BB delta, gated by a single opp signal) [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add a 4th variant; require ≥100g H2H. v145 cautions: open_response_conf≤0.25 gate risks inertness (relax to 0.15 if firing <5% per critic mandate); sticky (reduce) + limp-heavy (increase) branches cancel for mixed-profile opps — gate mutual-exclusion; bb_vs_limp delta theory-driven, NOT H2H-anchored.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 9+ gens, NO recorded recovery; measurement anchors (v111/v114) REAPED, verdict un-revalidatable. Treat threshold axis as dead; re-anchor to LIVE opponents first.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 10+ gens v122→v146, never scheduled). Revive only with NEW detector + grep-verified LIVE `opp_current_round_check_count` + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed) — don't reflexively fall back to crossover. Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opp-line signal + birth reqs = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v146 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans (master+worker fallback can masquerade as crossover); direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (10th recurrence v127-v146; cited G#H# IDs DON'T EXIST — files are timestamp-based, e.g. 20260620_*.json). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; hard-gate cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v149**: Critic evidence: H2H weaknesses: v148 vs v127: 3-7 (sub-30g, noise per pool, but documented loss — docstring claims opp folds 99% on flop vs v127 which would justify sizing up, but n=1482 untraceable and only 10g recorded); Experience pool refs: POSTFLOP_STRATEGY: 'offensive primitives (v141 RVA, v142 protection_floor) NOT exhausted' — v149 continues offense axis, PARAMETER_TUNING: preflop opp-sizing-delta [POSSIBLY EXHAUSTED] v144/v145/v146 — v149 is postflop, different region but same arch pattern, GENERAL: 'FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic 10th recurrence v127-v146' — v149 docstring repeats with unverifiable n=1482 claim; Diff refs: strategy_helpers.py L261-299 NEW _street_fold_exploit_sizing_boost (formula + gates), strategy_helpers.py L277-278 application inside choose_raise AFTER all intermediate caps, guarded by not probe_mode/blocker_bluff/inducing_value, strategy.py L1319, L1685 TWO wiring sites in postflop raise paths
- **v148**: SPR gate flop/turn extension UNVERIFIED — v147 river gate must first show stack-off reduction at ≥100g vs v139; v148 does NEITHER verify NOR calibrate, it EXTENDS (8th gen on 0%-fold leak lineage, compounds inertness risk).
- **v148**: Critic #3 mandate UNADDRESSED across v147+v148 — use LIVE classify_sizing_tendency (already consumed by v141) to make +0.03 fold margin opponent-aware (call wider vs 'overbettor'/polarized, fold wider vs 'standard'/merged); fixed GTO baseline doesn't exploit population tendency.
- **v148 evidence**: NO matchups ≥30g for v147 (all 10g = noise); v148 has 0 daemon games. Only documented stack-off evidence is -33K/-22K RIVER-centric leak vs v139. Flop/turn draw thresholds (0.12/0.15) are ad-hoc, NOT outs×2-derived (9-out FD ≈0.36 flop).
- **v148 归档建议**: daemon-evaluate v148 vs v139 ≥100g; confirm flop/turn draw thresholds protect pair+draw without blocking profitable chase-downs; if stack-offs persist, calibrate made_strength 0.55 + wire classify_sizing_tendency per opponent bucket.
- **v147**: PLACEMENT SHADOW CLOSED AFTER 7 GENS — `_river_stackoff_guard` (v135/138/143) structurally inert (sat AFTER `to_call>=my_chips` early return). v147 `_spr_commitment_gate()` wired INSIDE early-return BEFORE shove_odds return. RULE: when stderr telemetry never fires, FIRST verify code-path reachability vs early-return chain — do NOT re-tune thresholds.
- **v147**: stderr TELEMETRY PROXY (reconfirmed) — SPR_FOLD stderr will NOT fire in live daemon (battle.py `_PersistentBot.call()` reads stdout only). Verify via code-reachability proxy + fixture self-test, NOT grep=0 (grep=0 ≠ inert).
- **v146 PREFLOP-SIZING-DELTA EXHAUSTION WATCH [POSSIBLY EXHAUSTED]**: v144/v145/v146 no WR lift; require ≥100g H2H vs sticky (v96/v126) + 3bet-heavy (v117/v129); do NOT add 4th variant. v146 bb_vs_raise_opp_sizing_delta satisfies 5/6 birth reqs (NEW detector + LIVE opp signals + 3 wired sites + ≥30g gate + fixture; missing ≥3 cited replay folds).

