## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — cite SYMBOLS/PATHS, NEVER line numbers (they drift: the early-return anchor shifted ~L1018→~L1048 between v143 and v146) and NEVER commit hashes. rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring (reaped bots' fns persist/toggle across gens).
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). NEW gating with it = PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- Detector firing verification: grep detector symbols in `web/core/results/bot_telemetry/{bot}.jsonl`. ONLY v146+ data is trustworthy — v137-v145 were written by the PRE-Phase-A-fix daemon and contain 0 markers by construction. Declare dead only if v146+ grep shows 0 fires.

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW INVARIANT (HARD RULE, AST-enforced)**: any fold/commitment guard's dispatch call-site MUST be placed BEFORE/inside the `to_call >= my_chips` early-return block, NEVER after it (guards in the `if to_call > 0:` block are unreachable for stack-covering all-ins, the -15.5k/-20k cases). Fix = RELOCATE call-site before the early-return, do NOT re-tune thresholds.
- **0%-FOLD STACK-OFF LEAK — v147 ADDRESSED (verify, don't re-implement)**: v147 ships `_spr_commitment_gate()` (closed-form `pot_odds = to_call/(pot+to_call)` + safety margin; SPR<2→top-pair+ commits, SPR>4→single pair folds vs jam; monte_carlo_weighted_equity iters=300 fixed seed), wired INSIDE `to_call>=my_chips` BEFORE the shove_odds return — satisfying the placement invariant and unblocking the prior INERT `_river_stackoff_guard`. Leak dominated preflop gains (vs v139: -32904 net despite 8-6 wins; -33K/-22K drops). REMAINING = verify via ≥100g daemon that stack-offs drop; else calibrate SPR/made_strength thresholds.
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires REGRESSED (0.5706→0.5306@360g); v138/v139 structurally INERT (placement shadow). NOTE: the v147 SPR-commitment gate is a NEW closed-form-math axis NOT covered by this exhaustion.
- Postflop BET-side offense axis [POSSIBLY EXHAUSTED] — `_river_*_guard` family (v138/139/140/143, 4 attempts) EXHAUSTED & FORBIDDEN per v144 dir-audit (HIGH-conf). Future offense = extend preflop delta pattern or open NEW dimensions. If re-permitted: RELOCATE call-site upstream of _vm_overbet/_rva/_rvr.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fire FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v139 FIXTURE≠LIVE lesson). v140+ comply (6/6 birth reqs + grep≥3x). stderr telemetry is captured into bot_telemetry/{bot}.jsonl — ONLY v146+ trustworthy (v137-v145 = pre-fix daemon, 0 markers by construction).

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, preflop sizing deltas) remain open PER AXIS, but each exhausts. NOTE: pre-v146 offense detectors (v134/v141/v144/v145) show 0 fires in trustworthy telemetry → treat as PENDING-VERIFICATION, not proven-LIVE. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing logs. Tiers: no consumers=dead; consumers but no firing proof=PENDING-VERIFICATION; firing proven=LIVE.
- preflop opponent-sizing-delta pattern (additive choose_raise BB delta wrapped as a new *_opp_sizing_delta() fn, each gated by a single opponent signal) [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) show no WR lift; do NOT add a 4th variant; require ≥100g H2H confirmation before any further work.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 9+ gens, NO recorded recovery of the v111-vs-v134 gap; anchors v114/v111 REAPED and absent from active pool. Kill-switch lapsed → treat threshold axis as dead; re-anchor to LIVE opponents.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 10+ gens v122→v146, never scheduled). Revive only with a NEW detector + grep-verified LIVE `opp_current_round_check_count` signal + birth requirements.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed) — don't reflexively fall back to crossover. Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth reqs = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v146 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans (master+worker fallback can masquerade as crossover); direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (10th recurrence v127-v146; cited G#H# IDs DON'T EXIST — files are timestamp-based, e.g. 20260620_*.json). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; hard-gate cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v147**: Implemented `_spr_commitment_gate()` (closed-form `pot_odds = to_call/(pot+to_call)` + safety margin; SPR<2→top-pair+ commits, SPR>4→single pair folds vs jam), wired INSIDE `to_call>=my_chips` BEFORE the shove_odds return — fixes the placement shadow that made `_river_stackoff_guard` unreachable for true stack-covering all-ins. Addresses the 7-gen 0%-fold stack-off leak (precommit vs v139: -32904 net despite 8-6 wins; -33K/-22K drops); dir-audit fold-side ban lifted per v146 mandate. 3 refs (import+call+comment) satisfy reachability; 6-case fixture self-test. REMAINING: verify via ≥100g daemon that stack-offs drop, else calibrate SPR thresholds.
- **v146 PREFLOP-SIZING-DELTA EXHAUSTION WATCH**: v144 (r=1473, 340g) / v145 (r=1449, 330g) / v146 — no WR lift. Require daemon ≥100g H2H vs sticky (v96/v126) + 3bet-heavy (v117/v129); if no lift, the preflop-sizing-delta axis is EXHAUSTED [POSSIBLY EXHAUSTED] — pivot to river stack-off verification or NEW dimensions. Do NOT add a 4th preflop-sizing variant.
- **v146**: v145 H2H all 10g (sub-30g = noise); v145 overall wr=0.542 over 260g. v146 bb_vs_raise_opp_sizing_delta satisfies 5/6 birth reqs (NEW detector + LIVE opp signals + 3 wired sites + ≥30g gate + fixture; missing ≥3 cited replay folds).
- **v145 inertness**: preflop sizing deltas (sb_open, bb_vs_limp) gated by open_response_conf≤0.25 risk being inert. After daemon ≥30g grep SB_OPEN_OPP_SIZE/BB_VS_LIMP_OPP_SIZE in bot_telemetry/{bot}.jsonl (v146+ only trustworthy); if firing <5%, relax gate to 0.15 (critic mandate) rather than adding call sites. At ≥100g vs sticky+3betters, if firing confirmed but no WR lift, the always-shrink 3bet-heavy branch may be wrong vs polarized 3betters → invert.
- **v145 design cautions**: sticky (fold_to_raise≤0.36→reduce) and limp-heavy (limp_rate≥0.38→increase) can cancel to ~0 net delta for mixed-profile opps — gate mutual-exclusion explicitly (limp-heavy behind not(sticky)). bb_vs_limp delta is theory-driven, NOT H2H-anchored (no <40% matchups cited).
