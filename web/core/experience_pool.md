## OPPONENT_MODELING
- Archetype-suppression gates on EXISTING detectors > AND-gated new detectors ('standard' default = zero downside); respect ≥0.15 confidence floor for VPIP/archetype classification.
- calldown_profile sample trap: foldy opps never reach n≥4 — use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `large_bet_ratio` is RAW (no smooth_rate wrapper); verify the read site before treating the raw-warning as live (routes through value_maximizer_index + fold_to_bet).
- Archetype-gated folds load-bearing ONLY if adversary classifies into that bucket — capture a real `classify_archetype()` snapshot at ≥30 hands first; a fold on a 'standard' opp (e.g. v173) never fires (v184 rock-fold INERT).
- Crossover silently drops recent strategy.py work — prefer highest-rated parent's strategy.py, not arbitrary source_v.

## POSTFLOP_STRATEGY
- **Made-strength table (authoritative):** pair≈0.22, two-pair≈0.40, trips≈0.58. Over-calling leak at 0.20≤made<0.45; 0.45-0.55 sparse — verify band edges before committing.
- **Birth mandate (4+ gen RECURRING defect):** wire offensive primitives AFTER downstream tiers with ≥3 wired dispatch sites (donk/probe/choose_raise) AT BIRTH. v185 RE-OFFENDED at 1 site (strategy.py L1981) — NOT closable by worker-review; enforce at birth.
- **NEW detectors need 6 BIRTH REQUIREMENTS:** new function + new opp-line signal + ≥3 dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs.
- **v186 REFACTOR COMPLETE (the live mandate):** consolidated 16 scattered call_margin additives into ONE unified `_postflop_response_margin()` (opponent.py L704-762, identical constants 0.035/0.04/0.10/0.05/0.50). v187 MUST replace those 16 exhausted gates AT this single substitution point with a principled pot-odds-vs-equity framework (the refactor's PURPOSE) — one edit propagates to all 16 paths. Legitimate strategy.py edits VIA this substitution point are now the mandate, not a last resort.
- **CAP CONSTRAINTS (v186-corrected):** strategy.py 2436→**2370/2500** (~130 headroom reclaimed); opponent.py 879→945 (ample). strategy_helpers.py 2500/2500 EXACT CAP — targeting helpers IMPOSSIBLE. adaptive cap passes quality_gates; "exceeds ~2388" is a v174-class miscalibration false-alarm.
- **FOLD-SIDE RULE:** postflop binary `return -1` folds dead 13+ gens; v183 PARTIALLY reactivated via archetype+EV gating (`_aggro_bluffcatcher_should_fold`, strategy.py L1546), firing only vs confirmed-aggro. Bare binary folds need archetype+EV gating and stay PARTIAL; continuous fold margins are safer. [POSSIBLY EXHAUSTED]
- **Offense value-sizing UP** (value-lead upsizing / turn thin-value / SPR value-ship / SPR dispatch) — 3 of last 7 gens, family saturated, no WR lift (~0.48-0.52 flat); strategy.py full. [POSSIBLY EXHAUSTED]
- **-20k / 0%-fold stack-off leak:** highest-volume loss (18+ gens), OPEN. TWO axes FALSIFIED — preflop-trash WR-lift (gate works but fixes nothing) and river-guard relocation — do NOT re-target either [STALE — no WR-lift]. v184 rock-fold INERT ('standard'); v180 turn-board-danger hypothesis still UNVALIDATED (verify via reachability_test + ≥100g H2H).
- **River call-margin delta-ADDs exhausted** [STALE — no WR-lift]; 0.20-0.45 band narrowing already satisfied by v180's turn primitive. NOTE: v186 revived this axis for framework-REPLACEMENT (unified substitution point), NOT delta-adds — the framework-replacement direction remains live.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense-side bluff axes LIVE — distinguish from the postflop binary-fold axis (PARTIALLY reactivated v183). Add empirical floor fold_to_raise≥0.40 before the confidence≥0.20 gate fires, to avoid bluffing sticky opps (v185 single-site risk vs nemesis v169).

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens, no resolution. EXEMPT: offensive imports adding NEW opponent-signal gating AND river value-sizing structural changes.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44 → use bet_ratio/0.75 direct scaling so gates differentiate bet sizes.
- **Firing verification:** reachability_test (code-reachability proxy) + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (battle.py _PersistentBot stdout-only; v163 blindspot ACTIVE); ALL ≥30g daemon-grep "fired≥5%" steps UNFULFILLABLE — substitute reachability_test, do NOT block on daemon-grep.

## GENERAL
- Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes (e.g. anti-lock bypass guard removal) yield higher EV per line than margin tweaks.
- **Validation thresholds:** <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Workers MUST grep their own function body to confirm every param/signal actually gates an outcome (`fold_to_bet_turn` read only in stderr + unused `my_round_bet` = semi-dead read, v179). [migrated advisory]
- **Plateau states (WR ~0.50 all matchups) have no single DOMINANT exploit** — when H2H shows 45-55% across the board, the correct move is a new structural axis (offense/texture/archetype), not tighter margins.

## RECENT_LESSONS
- **v187**: Critic evidence: H2H weaknesses: v186 weakest matchups are all small-sample noise (v164/v158 @ 0.30 in 10g; v154/v178/v161/v181/v160 @ 0.40 in 10g) — below 30g threshold, not citable. Fix is foundation-level, not matchup-targeted. v186 overall WR=0.504 over 240g.; Experience pool refs: Experience pool tags postflop-response axis [POSSIBLY EXHAUSTED] (v181-v186) and river call-margin deltas [STALE]. v186 refactor created the _postflop_response_margin single-substitution-point mandate for v187 — this gen did NOT fulfill that mandate (different, valid direction chosen: foundation correctness over postflop framework). The preflop-equity primitive axis is UNTAGGED and unexplored, so no local-optima violation.; Diff refs: state.py L5-41: formula rewrite (pairs linear 0.50+0.0292/rank; non-pairs 0.34+0.18*high+0.11*low+suited/gap/wheel bonuses, clamp [0.33,0.67]); state.py L357-380 _verify_preflop_ranking self-test PASSES; strategy.py L310/315/320 raise-sizing delta threshold 0.58/0.60→0.48/0.50 (necessary rescale); strategy.py L1399 postflop-strong 0.72 now correctly isolates TT+ pairs (was AKs/AKo/88+ on old scale); strategy.py L710-711 4bet/jam still gated by hand-class QQ+/AKs not strength — no regression for premiums.
- **v186 (refactor DONE):** `_postflop_response_margin()` (opponent.py L704-762) unifies 16 call_margin lines; strategy.py 2436→2370, opponent.py 879→945. v187 MUST replace those 16 exhausted gates AT the single substitution point with a pot-odds-vs-equity framework — the refactor's explicit PURPOSE (one edit → 16 paths). strategy.py import L13 adds the fn; L1573-1579 replaces 72 lines with a 7-line call; `_turn_board_danger_margin` + `realized_postflop_equity` preserved inline per do_not_touch.
- **v186 (birth-defect persists):** `_board_texture_bluff_raise` still wired at only 1/3 required sites (strategy.py L1981; donk ~L2219 / probe ~L2255 unwired). Next offense-axis gen MUST wire all three AT BIRTH; ~130 LOC headroom now available to accommodate it.
- **v186 (leak):** -20k/0%-fold leak OPEN 18+ gens; two axes FALSIFIED (preflop-trash WR-lift, river-guard relocation). Evidence: WR 0.5353 @ 170g plateau; refactor does NOT target any H2H weakness directly (v167/v155/v176 @ 0.40 spread is sub-30g noise).
- **v185:** single-dispatch-site `_board_texture_bluff_raise` (strategy.py L1981) passed quality_gates but VIOLATES the ≥3-site birth mandate (donk/probe unwired). H2H plateau WR 0.4875/240g, 0.30-0.70 spread, no dominant exploit; EV gate `fold_equity>bet/max(1,pot+bet)` is textbook; reachability_test PASS (A-high river made=0.10). v183→v184 regression (-85 rating, -0.043 h2h_wr) despite passing all gates confirms the plateau is real — offense-novelty NOT lifting ratings.
- **v184:** archetype-axis folds load-bearing only if adversary classifies into the bucket — v173 reads 'standard', so rock axis INERT (THIRD attempt on a tagged-exhausted leak); capture real classify_archetype() @ ≥30h, else lower rock_score / add 'tight_standard' sub-bucket or pivot to deferred offense. conf=0.12 dips below ≥0.15 floor — restore 0.15. NEW `_opp_bluff_prone(om)` guard uses fold_to_bet (NOT fold_to_raise); rock-fold + lag_maniac carve-out wired but only 2 dispatch sites (≥3 required) + no ≥30g gate — incomplete detector birth.

