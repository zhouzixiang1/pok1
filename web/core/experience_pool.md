## OPPONENT_MODELING
- classify_archetype() gates are load-bearing ONLY if the adversary lands in the bucket; smooth_rate prior-saturation returns 'standard' for most opps (v184 rock-fold INERT, v183 fold UNMOVED vs tight-exploiter). v192+ has ABANDONED classify_archetype in favor of DIRECT deal-local history signals — archetype-axis work is CLOSED for v192+.
- calldown_profile sample trap: foldy opps never reach n≥4 — use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `large_bet_ratio` is RAW (no smooth_rate wrapper); verify the read site before treating the raw-warning as live.
- Prefer EXISTING detectors over AND-gated new ones; respect ≥0.15 confidence floor for VPIP/archetype classification.
- Crossover silently drops recent strategy.py work — prefer the highest-rated parent's strategy.py, not arbitrary source_v.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-made_strength comparisons are INERT by construction (ordinal > pot_odds~0.27) — any such gate MUST use polarized equity (made×discount) or true_equity, never raw ordinal (v188/v189). Over-call leak band 0.20≤made<0.45.
- v186 `_postflop_response_margin()` unified 16 scattered call_margin additives; v188 pot-odds-vs-equity framework replaced it (substitution point OCCUPIED).
- Birth mandate (4+ gen RECURRING defect): wire offensive primitives with ≥3 LIVE dispatch sites AT BIRTH. NEW detectors need 6 BIRTH REQUIREMENTS: new function + new opp-line signal + ≥3 LIVE dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs.
- CAP CONSTRAINTS: opponent.py ~1340/1500 (160 headroom, prime fold-side target); strategy.py ~2437/2500 (~60 headroom); strategy_helpers.py 2500/2500 EXACT CAP (untargetable); state.py/postflop.py foundation CONSUMED by v187.
- -20k/0%-Fold stack-off leak (~20+ gens, #1 weakness): fold-side attack has NOT produced a WR lift — river fold rate flat 6.4%→7.8% across v188-v192. v188/v189/v190 `_river_potodds_equity_margin` calibration + opponent-adaptive discount (made×(0.40+bluff_freq)) FORBIDDEN, 3 consecutive gens; v184 rock-fold INERT, v180 turn-board-danger UNVALIDATED. v192 `_multibarrel_line_fold` shipped, PAUSED pending ≥100g validation. [POSSIBLY EXHAUSTED]
- FOLD-SIDE RULE: bare postflop binary `return -1` dead 13+ gens; continuous fold margins safer. [POSSIBLY EXHAUSTED]
- Offense value-sizing-UP (value-lead / turn-thin-value / SPR-ship) — saturated, NET WR flat (~0.48-0.52): +20k wins offset by fold-side leaks, NOT a growth axis. DISTINCT from offense BLUFF axes. [POSSIBLY EXHAUSTED]
- River call-margin delta-ADDs [STALE — no WR-lift] — REPLACED by v188; do-not-revisit guardrail only.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense BLUFF axis (board-texture bluff raise v185→v191) LIVE & DISTINCT from the exhausted value-sizing-UP family. WELL-EXPLORED, diminishing returns; PAUSED pending ≥100g WR-lift validation of shipped v192 fold-side primitive. fold_to_raise≥0.40 floor-before-conf-gate INCORPORATED into v191. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW opponent-signal gating; does NOT re-open the value-sizing-UP family.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44 → use bet_ratio/0.75 direct scaling so gates differentiate bet sizes.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (battle.py _PersistentBot stdout-only; v163 blindspot ACTIVE); ≥30g daemon-grep "fired≥5%" UNFULFILLABLE — substitute reachability_test.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV per line than margin tweaks.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage. [migrated advisory]
- Plateau (WR ~0.50): pursue a new structural axis, NOT tighter margins.
- GRIDLOCK ESCAPE: fold-side (validating shipped v192), offense-bluff (paused pending v192 validation), value-sizing-UP + choose_raise constants (exhausted) — if all validation-paused, the ONLY permitted direction is a NEW structural detector axis (novel opponent-history signal) not yet attempted, NOT re-tuning blocked axes.

## RECENT_LESSONS
- **v193**: BETSIZE-MAGNITUDE POLARITY DETECTOR (_opp_betsize_polarity, n≥4, underbettor/overbettor/standard buckets) is a NEW STRUCTURAL AXIS not yet exploited on a non-exhausted lane — future Master should apply it to FOLD-SIDE CALIBRATION (underbettor signal = condensed-range/thin-value → AVOID over-folding vs thin-value bettors) rather than the exhausted value-lead/to_call==0 axis where v178/v179/v181/v182 already show NET WR ~0.48-0.52.
- **v193**: BIRTH-DEFECT pattern recurs 4th time (v182/v185/v191/v193): worker ships primitive with 1 dispatch site when ≥3 is mandated. Master MUST require reachability_test PER LIVE DISPATCH SITE before quality_gates, AND enforce multi-site wiring AT BIRTH in the worker task spec — post-hoc remediation (v182, v191) wastes generations.
- **v193 归档建议**: At ≥30 games vs v172/v182, run reachability_test capturing _opp_betsize_polarity output — if tendency='standard' (not 'underbettor'), pivot the detector off the exhausted value-lead axis to fold-side calibration (underbettor=condensed-range signal to suppress over-fold vs thin-value bettors on turn/river to_call>0), and fix the is_allin recording schema (move _betsize_magnitude_samples.append into the action_type=='allin' branch at L215) so shove_rate logic stops being permanently dead.
- **v193**: Critic evidence: H2H weaknesses: v172 vs v192: 70% loss rate (14W-6L, the #1 nemesis) — confirmed from head_to_head.json, v182 vs v192: 65% loss rate (13W-7L) — second worst, v176 vs v192: 65% loss rate (13W-7L) — third worst, v192 overall: 305W-313L-2D, WR=0.4919 across 620 games (plateau); Experience pool refs: POSTFLOP_STRATEGY: 'Offense value-sizing-UP (value-lead / turn-thin-value / SPR-ship) — saturated, NET WR flat (~0.48-0.52): +20k wins offset by fold-side leaks, NOT a growth axis. [POSSIBLY EXHAUSTED]' — this primitive's value-lead application is ON this exhausted axis, GRIDLOCK ESCAPE: 'the ONLY permitted direction is a NEW structural detector axis (novel opponent-history signal)' — the _opp_betsize_polarity detector COMPLIES, but the value-lead application does NOT, BIRTH MANDATE: 'wire offensive primitives with ≥3 LIVE dispatch sites AT BIRTH' — _turn_float_value_donk has only 1 dispatch site (strategy.py L2180); Diff refs: opponent.py L1172-1226: _opp_betsize_polarity() new detector — n≥4 threshold, underbettor/overbettor/standard buckets. Structurally novel., opponent.py L203-204: _betsize_magnitude_samples.append((round_idx, action/pot_estimate, action_type == 'allin')) — recording is inside `if action_type == 'raise'` (L191), so is_allin is ALWAYS False. Shove samples never recorded; overbettor detection degraded., opponent.py L810-873: _turn_float_value_donk() — 8+ AND conditions including polarity.tendency == 'underbettor' (unverified for v172/v182)
- **v193**: _flop_cbet_range_advantage_delta() (strategy.py L1101-1142, 7 filters: round_idx==1, to_call==0, my_preflop_raises≥1, range_adv≥0.60, fold_to_bet_flop≥0.30, delta=[0.10,0.25]) LIFTS sizing of EXISTING c-bets (single dispatch L2428-2431) — does NOT create new betting spots. CAUTION: novel signal (my_preflop_raises) but application = bigger offense bets = value-sizing-UP family; needs ≥100g WR-lift before expanding. v193 0g (fresh); v192 parent WR 0.475/200g plateau; all weakness matchups 10g (<30g noise).
- **v192**: NEW fold-side line-evidence axis `_multibarrel_line_fold()` (opponent.py L625-674, 7 filters, 9 self-tests) uses DIRECT deal-local history signals (spot_info L1074/1079/1107/1111), AVOIDS classify_archetype trap; made<0.42 escapes v189 INERT trap. Deal-local counters structurally INERT-resistant — prefer over smoothed classify_archetype. MUST target opponent.py (1340/1500 headroom); strategy.py (2437/2500) & strategy_helpers.py (2500 EXACT cap) binding. H2H all 10g (<30g noise); nemesis v169 sticky calling-station 70% fold_to_raise.
- **v192 monitor**: over-fold watch well-placed (v191 beats calling-stations v158/v167/v169 @0.600). Monitor v192 vs v167 (most likely to false-trigger multibarrel fold) @≥30g; if WR<0.55, tighten made<0.42→0.40 (excludes weak two-pair) before loosening loose_caller carve-out.
- **v191**: RECURRING DEAD-CODE DISPATCH (v182 SPR-ship donk, v191 donk): workers add structurally-unreachable dispatch sites (round_idx guard mismatch), then claim '≥3 sites' met. Master MUST require reachability_test per new site BEFORE quality_gates. @≥30g: reachability vs PROBE L2247 (only genuinely new firing site) vs sticky v158/v169/v171 — if <5%, raise made<0.18→0.22 OR conf 0.20→0.15.


