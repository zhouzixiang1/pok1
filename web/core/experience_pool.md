## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, classify_sizing_tendency) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — they drift across gens), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); gating with it = NEW opponent signal (PARAMETER_TUNING EXEMPT).
- classify_sizing_tendency() (v137) = 4-bucket overbet/underbet classifier, per-street breakdowns + NaN/inf guard; a NEW sizing-aware signal — but shipped with NO firing fixture, so presumed-INERT per POSTFLOP mandate.

## POSTFLOP_STRATEGY
- Defensive fold/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires were on branches absent in HEAD and REGRESSED (0.5706→0.5306@360g); `_spr_commitment_gate`/`_single_reraise_stackoff_guard` still absent. v138 re-added `_river_stackoff_guard` w/ fixture (req #6) but placed AFTER the `to_call>=my_chips` early-return → structurally INERT on true all-ins; fix placement + need ≥100g H2H.
- v129 turn_second_barrel_planner = code-present OFFENSE pivot (since v133), but effectiveness UNCONFIRMED: v134 wired value_maximizer_overbet BEFORE it (dispatch-order shadow), so it may be partially INERT until ≥100g daemon H2H confirms turn+river raise-freq lift ≥40% from ~33.5% baseline. Do NOT treat as proven-effective yet.
- barrel_pressure_profile + check_raise_pressure are code-present (RE-WIRED by v133 after v132 removal), NOT firing-verified — "LIVE" means code-present only. Verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED] (re-grep HEAD before anchoring); structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Mandate: NEW detectors MUST prove firing via persistent fixture/self-test. v134/v135/v136/v137 shipped NEW primitives with NO firing verification = violation (classify_sizing_tendency included). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures); req #6 needs a persistent fixture.
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap, BLUFF_OPPORTUNITY_THRESHOLD) [POSSIBLY EXHAUSTED] — live constant `BLUFF_OPPORTUNITY_THRESHOLD = 0.55` in line_reading.py (v136 reverted to v111's 7000g baseline); require ≥100g H2H vs v93/v117/v114 before further tuning; if it doesn't recover ≥half the v111-vs-v134 gap, declare axis dead.
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v137 critic refs all ≤10-20g; v138 all sub-30g across 27 opponents).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored offense (dropped absent-from-HEAD `_river_stackoff_guard`). Wait ≥100g daemon H2H before cutting either side.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (5th recurrence v127/v129/v130/v135/v137/v138) — cited replays (G{N}H{M} format) are non-existent (actual format is timestamp-based). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add a hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v138**: STACK-OFF GUARD PLACEMENT INVARIANT: any fold-gate targeting catastrophic river stack-offs MUST be placed BEFORE the `if to_call >= my_chips:` early-return at strategy.py:990 (or duplicated inside that branch) — placing it inside the subsequent `if to_call > 0:` block guarantees true all-ins bypass it. v138 is the 2nd attempt at this axis (after v135's 0.88 miscalibration) and repeats a structural-placement miss; future Master plans must cite the line-990/1019 ordering constraint explicitly.
- **v138 归档建议 (mixed)**: Relocate `_river_stackoff_guard` invocation to fire BEFORE the `if to_call >= my_chips:` check at strategy.py:990 (or duplicate inside the all-in branch with adjusted `last_raise_pot_ratio` normalization), then require ≥100g daemon H2H vs v111/v131 — the polarized river barrelers — showing -15k to -20k stack-off frequency drops before claiming the 0%-fold leak is closed.
- **v138**: Ships `_river_stackoff_guard` fixture (✅ first fold-gate meeting birth req #6: 10/10 PASS locally — fires A-high/bottom-pair/weak-one-pair vs allin; NOT mid-two-pair/strong/nut/small-bet/overbettor/cheap-call) — but NO ≥100g H2H yet, and all v137 matchups are sub-30g noise (320 games/27 opponents, max 20g). made_strength 0.88→0.55 floor addresses v135 miscalibration. 🔴 Placement bug: guard sits after the `to_call >= my_chips` early-return → INERT on true all-ins; re-fix placement. ('~76 pairs v14-v137' count = 5th unverified-evidence recurrence.)
- **v137**: classify_sizing_tendency() drives defense relaxation placed FIRST in should_fold_postflop (returns False for non-air vs overbettor large bets; absolute_air exclusion made_strength<0.15 AND not has_draw preserves fold discipline; call_margin ±0.025-0.030). NEW opponent-modeling axis, NOT an exhausted fold-gate. 🔴 req #6 NOT met (stderr SIZING_* = debug-only) → presumed-INERT. To reach critic ≥8: add 3rd wired site (river value-fold / choose_raise vs underbettor) + persistent firing-rate fixture. Placing relaxation BEFORE round-bucket folds/SPR/check_raise_pressure is deliberate precedence change — verify via daemon telemetry that round-bucket folds aren't short-circuited.
- **v136**: DISPATCH-ORDER SHADOW = authoritative root cause of v134 leaks vs disciplined barrel-aggressive bots (all cited matchups sub-30g noise; v111 beats v93 0.5217@230g). Future Master MUST RELOCATE value_maximizer_overbet call-site to AFTER river_value_raise_tier + turn_second_barrel_planner, NOT re-tune the threshold. value_maximizer_overbet (vm_index≥0.75 AND conf≥0.30) + turn_river_blocker_bluff() both 0-3/6 birth reqs (no fixture) = presumed-INERT. BLUFF_OPPORTUNITY_THRESHOLD 0.48→0.55 reverted to v111 7000g baseline.

