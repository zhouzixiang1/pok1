## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic.
- **Firing verification is path-dependent (A1 v146+)**: daemon path (web/core/engine/battle.py _PersistentBot drains stderr → results/bot_telemetry/{bot}.jsonl) IS greppable & authoritative; CLI path (engine/battle.py) is stdout-only & DROPS stderr. Daemon ≥30g → grep `bot_telemetry/` for `delta_adj!=0`/actual param values (token>0 ≠ LIVE — SIZING_MARGIN_ADJ printed only sub-arm B delta → false "99.96% inert"; standard arm A was LIVE v153+, v154 r=1605). Telemetry must print TOTAL return at function scope. reachability_test.py = force-fed CODE-PATH, 0 fires = PENDING-VERIFICATION.
- smooth_rate prior_weight must be reachable: tune it BEFORE adding detectors when symptom = "fires telemetry but doesn't adjust" (4.0 saturated a true 50%-folder below the 0.50 gate; fixed 4.0→2.0 v156).
- calldown_profile sample trap: samples accumulate only via pending_my_pressure + opp NEXT action; each opp FOLD ends hand → foldy opps never reach n≥4, yet detectors gate on n≥3 → every n=3 killed. Fix: populate empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.
- INERT detectors fire telemetry but move sizing <2% pot — require per-hand flags (prior_aggressor_checked_back from history) before approving population-mean opp signals as "new axes".

## POSTFLOP_STRATEGY
- **Telemetry scope invariant (v156)**: block-scoped sys.stderr.write inside a nested if is invisible to daemon grep — hoist to function scope on unconditional path (CALL_MARGIN_FINAL = correct). Gate: daemon ≥30g → require delta!=0 in ≥5% of bot_telemetry lines else INERT → block commit.
- **STACK-OFF GUARD PLACEMENT INVARIANT**: guards inside `to_call>=my_chips` reached ZERO times (any legal non-allin raise leaves ≥1 chip → to_call<my_chips always) — DO NOT place there; relocate to `to_call>0` (~40% decisions) or opponent_allin branch. v148 SPR gate upstream = only validated tail-containment lever (±11692 vs v130).
- **Fold-side is NARROW, not exhausted**: binary `return -1` fold gates EXHAUSTED (v135-v154, 12+ gens) and MUST NOT be added — but the river 0%-fold stack-off leak PERSISTS (fold=0%, -37143/-36712 swings; v149's preflop over-fold ≠ this river under-fold). Only structurally-valid fold-side paths: (a) continuous call_margin EV-integration (v157), (b) RELOCATING an existing guard BEFORE the early-return. Enforce standard-arm/telemetry fix before any fold-side work.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (value_maximizer_overbet, river_value_raise_tier, turn_second_barrel_planner); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.
- v152 caution (resolved v156): vpip≥0.55 OR-gate catches passive limpers → -EV widen vs premium-heavy raises; use pfr≥0.30 / conf<0.20 real steal-opener signals.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) each open a per-axis door but exhaust.
- Preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps → gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; anchors REAPED. Includes check_raise_freq = DEAD directive. Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.
- **Re-verification caveat (v156 unblock)**: all 0-fire / no-WR-lift verdicts above (v144-v151) were measured BEFORE the v156 prior_weight 4.0→2.0 unblock, on detectors that were provably prior-weight-saturated (inert). These exhaustion conclusions are therefore UNVERIFIED — re-grep `bot_telemetry/` post-v156 before treating any [POSSIBLY EXHAUSTED] marker in this section as a hard ban.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing, two-band slope reshape) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).
- Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; widen_threshold must target ≥15pp bands; implied-odds/speculative hands need ≥0.35.

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise (v135 cut v134 after 10g; v136 restored it). Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST) — Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v161**: Critic evidence: H2H weaknesses: v160 rating 1459 (sub-baseline), 48.95% WR over 190g. Weakest sub-40% matchups (v141 30%, v129/v137/v145/v146 40%) all sub-30g noise. Offense direction allowed — fold-side binary gates exhausted per experience pool + dir-audit.; Experience pool refs: BLUFF_CALIBRATION lists 'turn 2nd-barrel' as a per-axis door that opens then exhausts — but refers to v129 VALUE barrel (made_strength >= 0.45); the BLUFF companion is a new axis., POSTFLOP_STRATEGY birth requirement '≥3 wired dispatch sites' FAILS — only 1 site (L1671). 3-gen recurrence flagged., Fold-side binary gates exhausted (v135-v154, 12+ gens) — offense is permitted direction.; Diff refs: strategy_helpers.py L803-889: new turn_bluff_continuation_barrel with 6 gates (street/to_call/flop-aggressor/weak-hand/scare-card/foldable-opp/equity-fallback)., strategy.py L1671-1678: single dispatch site AFTER value barrel (L1659), BEFORE _post_missed_cbet_exploit (L1683) — no shadow., opponent.py L401: fold_to_bet_turn populated — signal is LIVE.
- **v160**: 0%-fold stack-off leak now 10 gens unaddressed (v151-v160 all offense-only): critic+archivist repeatedly identified the structural fix (RELOCATE v138 _river_stackoff_guard or v148 SPR gate BEFORE strategy.py to_call>=my_chips early-return ~L1007) but dir-audit forbade fold-side work. Next dir-audit MUST be lobbied to permit this ONE relocation — highest-EV unaddressed fix in the v137-v160 lineage.
- **v160**: TURN_VALUE_FLOOR is theory-driven (no confirmed calling-station opponent in H2H; v159 51.5% WR overall, weakest matchup vs v94 30% is sub-30g noise) and likely fires rarely (fold_to_bet_turn≤0.42 tight). Daemon ≥30g → verify via get_action() reachability ≥5%; else relax ≤0.45 + add 3rd dispatch site. Only 2 sites (L1394/L1833) = below birth mandate.
- **v160**: Dispatch-site undercount is a 3-gen recurrence (v149/v158/v160 default to 2 sites when mirroring an exploit pattern). Master plans must explicitly mandate 'wire 3 sites incl. donk/probe paths' to satisfy the birth requirement in one pass.
- **v159**: river_call_size_ratio is a NEW opp-signal (added to PARAMETER_TUNING EXEMPT list); v159 replaced v158's made_str=0.55 two-band cliff with continuous license `0.45 + (0.15 + _license*0.25)*t`. Verify RIVER_VALUE_TIER license_milli>0 fires ≥5% at ≥30g; if river_call_size_ratio rarely reaches ≥0.70 in 70-hand HU, the strong 0.45→0.85x slope is dead. Calldown-derived signals need n≥3 samples (not n≥2) before licensing aggressive slopes.
- **v158 (superseded by v159)**: river-value-sizing axis had 4 primitives (v134/v140/v141/v143/v158); if v159's continuous license still shows <5% firing or no WR lift vs v130/v139/v148 at ≥100g, gate the strong band on river_call_size_ratio.

