## OPPONENT_MODELING
- Smoothed archetype axis (classify_archetype via smooth_rate) is CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor. Prefer DIRECT deal-local history signals (v192 `_multibarrel_line_fold`, v193 `_opp_betsize_polarity`) — deal-local signals OPEN, archetype gating CLOSED.
- calldown_profile sample trap: foldy opps never reach n≥4 — use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `large_bet_ratio` is RAW (no smooth_rate wrapper); verify the read site before treating the raw-warning as live.
- Crossover silently drops recent strategy.py work — prefer highest-rated parent's strategy.py, not arbitrary source_v.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-raw-made_strength gates are INERT by construction (ordinal > pot_odds~0.27) — any such gate MUST use polarized equity (made×discount) or true_equity (v188/v189). Over-call leak band 0.20≤made<0.45. Do NOT revisit raw river call-margin delta-ADDs.
- Birth mandate (4+ gen RECURRING defect v182/v185/v191/v193): wire offensive primitives with ≥3 LIVE dispatch sites AT BIRTH. NEW detectors need: new function + new opp-line signal + ≥3 LIVE dispatch sites + reachability/H2H-WR confirmation at ≥30g (NOT a daemon-grep count) + persistent fixture logs.
- CAP CONSTRAINTS (v195 current): opponent.py ~1605/1500 BASE (~105 over base — deepest into adaptive budget, TRUE binding constraint for fold-side work); strategy.py ~2451/2500 (~49 headroom); strategy_helpers.py 2500/2500 EXACT CAP (untargetable); state.py/postflop.py CONSUMED by v187.
- v193 `_opp_betsize_polarity` INFRASTRUCTURE is NOT reliably live: v195 crossover REGRESSED the is_allin fix (hard-coded ratio=2.0 vs v194's actual high_bet/pot) → every all-in misclassifies 'overbet', corrupting the betsize_polarity signal. Restore v194's recording before treating the detector as operational. Only the fold-side PAYOFF stays OPEN; tuning of shipped primitives paused.
- Offense value-sizing-UP (value-lead / turn-thin-value / SPR-ship / c-bet-range-advantage) — saturated, NET WR flat (~0.48-0.52): +20k wins offset by fold-side leaks, NOT a growth axis. Pause expansion. [POSSIBLY EXHAUSTED]
- FOLD-SIDE RULE: bare postflop binary `return -1` dead 13+ gens; continuous fold margins safer. [POSSIBLY EXHAUSTED]
- -20k/0%-Fold stack-off leak (~20+ gens, #1 weakness): shipped-primitive TUNING has NOT produced a WR lift — river fold flat 6.4%→7.8% (v188-v192); v188/v189/v190 calibration + opponent-adaptive discount + v184 rock-fold all INERT. Tuning [POSSIBLY EXHAUSTED] [STALE — no WR-lift]. (Deal-local fold-side detectors + the v194 facing_villain_4bet path stay OPEN.)

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense BLUFF axis (board-texture bluff raise v185→v191): dispatch LIVE but the strategic axis is EXHAUSTED (diminishing returns), PAUSED pending ≥100g WR-lift validation. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating (betsizes, multibarrel counters, facing_villain_4bet path); CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44 → use bet_ratio/0.75 direct scaling so gates differentiate bet sizes.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (battle.py _PersistentBot stdout-only); ≥30g daemon-grep "fired≥5%" UNFULFILLABLE — the ≥30g gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV per line than margin tweaks. Plateau (WR ~0.50): pursue a NEW structural axis, NOT tighter margins.
- Validation thresholds: <30g H2H = noise (v193 parent 310g at WR=0.500; all weakest 20g matchups were <30g noise); ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- If ALL active axes are validation-paused (fold-side tuning pending; offense-bluff paused; value-sizing-UP + choose_raise constants exhausted), the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v196**: Margin-additive pot-odds-vs-equity gates with delta caps become INERT for the upper leak band (made 0.40-0.50) when realized_rate ~0.55-0.65 exceeds the +0.25 cap — switch to a direct binary fold (as in v196) once reachability confirms the band is firing.
- **v196**: Comment-only LOC-condensation tasks must NOT touch behavioral code — even a functionally-inert ratio/sample change violates the worker contract, triggers reviewer scope-drift flags, and (as reviewer caught) shows the worker misunderstood v195 state (shove_rate was already nonzero via v195's elif-allin-branch fix; the L188-190 'so shove_rate is no longer permanently 0' comment is wrong).
- **v196 归档建议**: At >=30g, verify v196 vs v179 (parent) lifts WR against the leak-source nemeses v174/v169/v176 without over-folding weak two-pair (made~0.40, currently inside the [0.20,0.50) leak band) against standard-archetype v167/v178 (WR~0.40 in v195); if WR regresses, either restore the margin-additive path as a secondary check that still lets anti_lock_can_continue/strong_made_continue fire, OR tighten the leak band to made[0.20,0.40) excluding weak two-pair.
- **v196**: Critic evidence: H2H weaknesses: v195 vs v172 WR=0.300 (10g), vs v179 WR=0.300 (10g), vs v190 WR=0.300 (10g) — small-sample weakest matchups; v194/v193 show similar 0.40 clusters vs v174/v178/v185. All <30g noise per experience pool, but directionally consistent with -20k stack-off leak.; Experience pool refs: POSTFLOP_STRATEGY: 'Made-strength table: pot_odds-vs-raw-made_strength gates are INERT by construction (ordinal > pot_odds~0.27) — any such gate MUST use polarized equity (made*discount)'. W2 uses calibrated_equity=made*discount — compliant., POSTFLOP_STRATEGY: 'v188/v189/v190 calibration + opponent-adaptive discount + v184 rock-fold all INERT. Tuning [POSSIBLY EXHAUSTED] [STALE — no WR-lift]'. W2 is a structural dispatch change on the same axis., POSTFLOP_STRATEGY: 'FOLD-SIDE RULE: bare postflop binary return -1 dead 13+ gens; continuous fold margins safer. [POSSIBLY EXHAUSTED]'. W2 introduces exactly this pattern.; Diff refs: strategy.py L13 (import) + L1621-1626 (NEW direct fold dispatch in to_call>0 block, AFTER _multibarrel_line_fold, BEFORE round_idx==0 call_margin block). 13 lines added., strategy.py L1717 (anti_lock_attack return) fires AFTER the new dispatch — theoretical anti-lock regression risk., opponent.py L868-947 (_river_potodds_equity_margin) UNCHANGED — only its dispatch path was rewired. Function gates: river only, bet>=0.4x, made[0.20,0.50), tier!=strong/nut, draw<0.15, overbettor-exempt (samples>=8, confidence>=0.30, tendency=='overbettor').
- **v195**: Allin betsize recording must use the ACTUAL high_bet/pot ratio (v194's approach), NOT a hard-coded ratio=2.0. The v195 crossover REGRESSED this → every short-stack shove inflates the 'overbet' bucket and corrupts the betsize_polarity signal v193/v195 depend on. Restore v194's recording before any further betsize_polarity work.
- **v195**: Reachability verification on LIVE nemeses (not synthetic self-tests) is mandatory before shipping fold-side calibrations keyed to a NEW detector — v195's underbettor floor is cosmetic if v172/v173/v174 classify 'standard'/'overbettor' in actual play. Run reachability_test at ≥30h; if not 'underbettor', widen the detector (n≥3 or small_rate≥0.50) or pivot to calibration against the actually-detected polarity.
- **v194**: Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' makes shove_rate permanently 0. (Fixed in v194, regressed by v195 crossover — restore.)
- **v194**: Wire the dormant `facing_villain_4bet=True` path in `_preflop_spr_commitment_gate` (detect opp_round_raises>=2 preflop) — folding marginal hands (22-66/AQo/AJo/KQo, the MARG_HI=0.62 band) to a 4-bet is the highest-EV part of this gate and a direct attack on the 20+-gen -20k/0%-fold leak. A parameter never passed True at its only call site is dead code even if 'forward-looking'.
- **v193**: `_opp_betsize_polarity` (n≥4, underbettor/overbettor/standard buckets) = NEW STRUCTURAL axis — apply to FOLD-SIDE CALIBRATION (underbettor=condensed/thin-value → AVOID over-fold vs thin-value bettors on turn/river to_call>0), NOT the exhausted value-lead/to_call==0 axis. @≥30g vs v172/v182 run reachability_test; if tendency='standard', pivot to fold-side.


