## OPPONENT_MODELING
- archetype axis (classify_archetype via smooth_rate) CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor; prefer deal-local history signals over archetype gating. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify the read site before treating the raw-warning as live.
- `_opp_betsize_polarity` (v193, n≥4) IS operational: uses n_shove COUNT via a True flag, NOT ratio=2.0 (feeds only unread avg_fraction) — "restore v194 ratio" dis-proven. Use as CALIBRATION that AVOIDS over-fold vs thin-value underbettors (turn/river to_call>0), not a value-lead/to_call==0 axis.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-raw-made_strength gates STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 PRIORITY — leak is MULTI-SITE (post-v200)**: river DIRECT fold `if _river_pe_delta>0: return -1` is LANDED (v200 port, strategy.py ~L1627; verified made=0.30 folds / made=0.40 two-pair preserved). Precommit STILL caught a -22073 blowout vs v194 (1 in ~12 games) WITH the fold dispatched → leak is NOT solely river to_call>0. MANDATORY next: probe preflop `facing_villain_4bet` stack-offs + turn all-in call-downs. Parametric fold tuning DISPROVEN (v188/v189/v190/v184 all INERT) [STALE — no WR-lift].
- **v197 fold-gate thesis (CONTESTED)**: direct binary folds (`return -1` before call_margin) are structurally HIGH-RISK over-fold vs BLENDED ranges (bypass continuous realized_rate-vs-pot_odds); margin-additive is safer by construction. CONTESTED — v196 argued margin-additive is INERT for the upper leak band; reconciliation pending ≥30g H2H of the v200 port vs v174/v169/v176/v178.
- Birth mandate (4+ gen RECURRING defect v182/v185/v191/v193): wire offensive primitives with ≥3 LIVE dispatch sites AT BIRTH (new fn + new opp-line signal + ≥3 LIVE sites + reachability/H2H-WR @≥30g + persistent fixture logs).
- CAP CONSTRAINTS: strategy_helpers.py 2500/2500 EXACT CAP (ANY helpers edit requires LOC reclamation first); v199's strategy.py ~2489/2500 + opponent.py ~1547 are LINEAGE-SPECIFIC — v200 (v194×v190 + fold port) LOC UNVERIFIED, re-measure before editing.
- Wire dormant `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` (detect opp_round_raises≥2 preflop) — NOW ELEVATED (multi-site leak); folding marginal 22-66/AQo/AJo/KQo to a 4-bet is highest-EV + direct leak attack; a param never passed True at its only call site is dead code (v194, still open).
- Offense value-sizing-UP (value-lead/turn-thin-value/SPR-ship/c-bet-range-advantage): v198 (C1+C2+C3) + v199 (betsize-polarity arm), FIRING (+26351 vs calling-station v159). Offense embargo now LIFTED (fold-side river port landed) — no longer a misallocation. [POSSIBLY EXHAUSTED]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense BLUFF axis (board-texture bluff raise v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV per line than margin tweaks. Comment-only LOC-condensation must NOT touch behavioral code (even an inert ratio/sample change violates the worker contract, v196). Plateau (WR ~0.50): pursue a NEW structural axis, NOT tighter margins.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' makes shove_rate permanently 0 (fixed v194). ratio=2.0 was NOT a corrupter (feeds only unread avg_fraction).
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST explicitly inventory+port sibling-lineage critical mutations (esp. fold-side/leak-attack) as explicit worker tasks when branching_from is set (RECURRING: v197, v199 both regressed v196's fold).
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v201**: Critic evidence: H2H weaknesses: G5H68#95800e7f (replay 20260628_014630_404947_claude_v164_vs_claude_v200.json): KTo called off -20000 preflop vs all-in shove — confirmed bb_vs_raise leak with no all-in defense, v200 vs v164 H2H WR=0.40 (10g); v200 vs v182=0.30, v167/v195/v174/v158/v179/v187=0.40 — marginal-hand stack-offs contribute to -20k blowout distribution; Experience pool refs: #1 PRIORITY (line 9): leak is MULTI-SITE post-v200 — river fold LANDED but precommit STILL caught -22073 blowout vs v194; MANDATORY next: probe preflop facing_villain_4bet stack-offs, RECENT_LESSONS (line 38): v200 MULTI-SITE leak CRITICAL — Future Masters MUST treat leak as MULTI-SITE including preflop stack-offs, Line 13: 'Wire dormant facing_villain_4bet... folding marginal 22-66/AQo/AJo/KQo to a 4-bet is highest-EV + direct leak attack' — related but the 4-bet arm is NOT what v201 implements (see feedback); Diff refs: state.py L395-418: _preflop_shove_defense_fold — classify_preflop_hand tiers (premium/big_cards/strong_pair continue; mid_pair needs pot_odds<0.35; else fold) + opponent_allin/opp_pf_raises>=2 gate, strategy.py L641-644 (bb_vs_raise branch): NEW intercept BEFORE _bb_vs_raise_bucket_action — directly closes the G5H68 KTo call-off path, strategy.py L1444-1447 (main opponent_allin block, round_idx==0): secondary catch-all for unnamed preflop spots
- **v200 MULTI-SITE leak (CRITICAL)**: the v196 DIRECT river fold port did NOT fully close the 23+gen -20k/0%-fold leak — precommit semantic caught a -22073 blowout vs parent v194 (1 in ~12 games) WITH the fold dispatched. Future Masters MUST treat the leak as MULTI-SITE (also probe preflop facing_villain_4bet stack-offs + turn all-in call-downs), not solely a river to_call>0 fold-path issue.
- **v200 precommit retry**: silent 2-attempt retry — a first 'FAILED: match_timeout' (n=8 hitting the 960s mirror limit) auto-falls-back to n=4 and can PASS. Distinguish timeout-failure from data-driven-failure when reading precommit history.
- **v200 archive todo**: @≥30g run paired H2H v200 vs leak-nemeses v174/v169/v176 (+ v178, where v196 dropped to 0.30 with the same mutation). v177 was CULLED in the v200 reap — DEAD target, do not use for validation. If v178 WR<0.45 while nemeses lift only marginally → v197 thesis validated → replace river `return -1` with a margin-additive secondary or polarized-equity gate (`true_equity = bluff_freq + (1-bluff_freq)*made_strength`).
- **v200 critic caveat**: leak-nemesis H2H is sparse (20g each, <30g noise threshold; v194 0.5386 over 440g is the reliable slice) — the leak manifests in chip-distribution (stack-offs) not pure win-rate, so the structural signal lives in the pool's longitudinal analysis, not current 20g H2H slices.
- **v199 silent instrumentation bug**: v194's allin check inside `if action_type=='raise'` made shove_rate dead 5 gens (v194-v198) with NO test/smoke failure. Always reachability-test the DOWNSTREAM consumer (grep classify_archetype/_opp_betsize_polarity on live nemeses) after any recording-site change — the detector's unit test passes while the prod feed stays empty.
- **v198 LOC HARD-CAP TRAP**: strategy_helpers.py AT 2500 hard cap — ANY helpers edit requires LOC reclamation first or the quality gate blocks; budget reclamation into the task plan.

