## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — cite SYMBOLS/PATHS, NEVER line numbers (they drift) or commit hashes. rd=350 = Glicko DEFAULT/unrated, not "live top".
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). NEW gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic, not threshold nudges.
- Detector firing: grep symbols in `web/core/results/bot_telemetry/{bot}.jsonl`. ONLY v146+ trustworthy (v137-v145 = pre-Phase-A-fix daemon, 0 markers by construction). Verify via code-reachability proxy, NOT stderr — `battle.py _PersistentBot.call()` reads stdout only (grep=0 ≠ inert).

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW INVARIANT (HARD RULE)**: any fold/commitment guard's dispatch call-site MUST be BEFORE/inside the `to_call >= my_chips` early-return block, NEVER after it (else unreachable for stack-covering all-ins = the -15.5k/-20k cases). Fix = RELOCATE call-site, do NOT re-tune thresholds.
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v135 REGRESSED; v138/v139 structurally INERT (placement shadow); v147/v148 SPR gate reclassified at v149 as the SAME exhausted fold-side axis (v149 dir-audit: bot OVER-FOLDS preflop 99% fold-to-bet, does NOT over-commit; fold-side made the leak WORSE → PIVOTED TO OFFENSE). Do NOT extend/re-calibrate the SPR gate or any fold-side threshold on persistent stack-offs — that is the abandoned counterproductive axis.
- Defensive `_river_*_guard` family [POSSIBLY EXHAUSTED] — v138/139 call-side FOLD gates, v140/143 bet-side own-jam BLOCKERS; ALL DEFENSIVE, NONE offense. EXHAUSTED per v144 dir-audit (HIGH-conf). The ACTUAL bet-side OFFENSE primitives — `_river_value_extraction_amplifier` (v141), `_vulnerable_made_protection_floor` (v142), `_street_fold_exploit_sizing_boost` (v149) — are NOT exhausted and remain open.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v139 FIXTURE≠LIVE lesson). v140+ comply (6/6 birth reqs + grep≥3x). stderr telemetry → bot_telemetry/{bot}.jsonl, ONLY v146+ trustworthy.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost) remain open PER AXIS, but each exhausts. pre-v146 offense detectors (v134/v141/v144/v145) show 0 fires in trustworthy telemetry → PENDING-VERIFICATION, not proven-LIVE. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing logs. Tiers: no consumers=dead; consumers but no firing proof=PENDING-VERIFICATION; firing proven=LIVE.
- preflop opponent-sizing-delta pattern (additive choose_raise BB delta, gated by a single opp signal) [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add a 4th preflop variant; require ≥100g H2H. v145 cautions: open_response_conf≤0.25 risks inertness (relax to 0.15 if firing <5%); sticky (reduce) + limp-heavy (increase) branches cancel for mixed-profile opps — gate mutual-exclusion. NOTE: v149's `_street_fold_exploit_sizing_boost` is a distinct POSTFLOP continuous-boost axis, NOT a 4th preflop variant.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 9+ gens, NO recorded recovery; measurement anchors (v111/v114) REAPED, verdict un-revalidatable. Treat threshold axis as dead; re-anchor to LIVE opponents first.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 10+ gens v122→v146, never scheduled). Revive only with NEW detector + grep-verified LIVE `opp_current_round_check_count` + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed) — don't reflexively fall back to crossover. Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opp-line signal + birth reqs = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v146 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans (master+worker fallback can masquerade as crossover); direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (11th recurrence v127-v149; cited G#H# IDs DON'T EXIST — files are timestamp-based, e.g. 20260620_*.json; v149 docstring repeated unverifiable n=1482 claim). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; hard-gate cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v150**: Critic evidence: H2H weaknesses: v149 parent H2H all sub-30g (10 games/opponent = NOISE per experience pool); weakest sub-30g: v146 (0.40, 10g), v129 (0.40, 10g) — unreliable, not cited as relaxation basis. v149 rating r=1540 rd=39 (very confident).; Experience pool refs: experience_pool.md L39-41 (v149 lessons): critic mandated 'if daemon≥30g<5%, relax confidence 0.20→0.15 OR fold_rate 0.55→0.50, AND wire a 3rd dispatch site on the donk/probe-raise path' — v150 implements ALL THREE. L18: offense primitives (incl. _street_fold_exploit_sizing_boost) remain OPEN PER AXIS, not exhausted. L5: 'Verify via code-reachability proxy, NOT stderr — battle.py _PersistentBot.call() reads stdout only (grep=0 ≠ inert)' — v150's reachability_test.py is the correct proxy. L10: v149 _street_fold_exploit_sizing_boost explicitly listed as NOT exhausted.; Diff refs: strategy_helpers.py L261-268: NEW named constants STREET_FOLD_BOOST_MIN_CONF=0.15, STREET_FOLD_BOOST_MIN_FOLD=0.50 (relaxed from inline 0.20/0.55). L296: raw = 0.08 + (fold - 0.50)*0.30 (was fold-0.55). strategy.py L1594-1605: NEW 3rd dispatch site at donk path (_donk_fold_boost applied to donk ratio). L1614-1623: NEW 4th dispatch site at probe path (_probe_fold_boost applied to probe ratio). reachability_test.py (NEW, 242 lines): end-to-end monkeypatch harness proving boost flows through get_action() — PASSED (boosted=232 vs baseline=205).
- **v149 STRATEGIC PIVOT (HIGH-VALUE)**: dir-audit HIGH OVERRULED match-analysis fold-gate rec — bot OVER-FOLDS PREFLOP (99% fold-to-bet vs v132/v144), does NOT over-commit. Fold-side v138-v148 lineage (incl. SPR gate) is EXHAUSTED and made the leak WORSE → PIVOTED TO OFFENSE. Do NOT re-engage fold/SPR calibration on persistent stack-offs; that is the abandoned counterproductive axis.
- **v149**: `_street_fold_exploit_sizing_boost` CONFIRMED a viable NEW postflop offensive axis (per-street fold_to_bet≥0.55 + conf≥0.20 → +0.08..0.215×conf added to choose_raise ratio, suppressed probe/blocker/induce), distinct from preflop sizing-delta and boolean should_barrel triggers — continuous boost + per-street granularity. 2 dispatch sites only (critic wants ≥3). precommit: boost extracted more from 99%-folding opp AS INTENDED.
- **v149 归档建议**: After daemon≥30g, grep STREET_FOLD_BOOST firing via code-reachability proxy (NOT stderr); if <5%, relax confidence 0.20→0.15 OR fold_rate 0.55→0.50, AND wire a 3rd dispatch site on the donk/probe-raise path. ≥100g WR lift → opponent-aware calibration via classify_sizing_tendency (size up vs over-folding standard, skip vs polarized overbettor).
- **v149**: DISPATCH-REACHABILITY ASSERTION SEMANTICS — fn_refs count (import + N call-sites) ≠ dispatch sites; a 3-REFERENCE assertion passes with only 2 wiring sites. Future fixtures must count call-site DISPATCH POINTS (where return value is consumed), excluding imports. Recurring critic mandate since v137.
- **v148 归档建议**: Critic #3 mandate UNADDRESSED across v147+v148 — use LIVE classify_sizing_tendency (consumed by v141) to make +0.03 fold margin opponent-aware. Given the v149 fold-side-exhaustion pivot, treat this as OFFENSE-side opponent-aware calibration (the v149 follow-up), NOT fold-side SPR threshold tuning. v148 flop/turn draw thresholds (0.12/0.15) are ad-hoc, NOT outs×2-derived (9-out FD ≈0.36 flop).
- **v147**: PLACEMENT SHADOW CLOSED AFTER 7 GENS — `_river_stackoff_guard` (v135/138/143) structurally inert (sat AFTER `to_call>=my_chips` early return); v147 `_spr_commitment_gate()` wired INSIDE early-return BEFORE shove_odds return. RULE: when stderr telemetry never fires, FIRST verify code-path reachability vs early-return chain — do NOT re-tune thresholds.

