## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); NEW gating using it = PARAMETER_TUNING EXEMPT.
- Firing verification is path-dependent: daemon path (web/core/engine/battle.py _PersistentBot drains stderr → results/bot_telemetry/{bot}.jsonl) IS greppable & authoritative; CLI path is stdout-only & DROPS stderr. Daemon ≥30g → grep bot_telemetry/ for `delta_adj!=0`/param values (token>0 ≠ LIVE — standard arm A was LIVE v153+). reachability_test.py = force-fed path, 0 fires = PENDING.
- smooth_rate prior_weight must be reachable BEFORE adding detectors (4.0 saturated a 50%-folder below the 0.50 gate; fixed 4.0→2.0 v156).
- calldown_profile sample trap: foldy opps never reach n≥4 (each fold ends hand) yet detectors gate on n≥3 → use empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2; need n≥3 (not n≥2) before licensing aggressive slopes.
- INERT detectors fire telemetry but move sizing <2% pot — require per-hand flags (prior_aggressor_checked_back from history) before approving population-mean signals as new axes.

## POSTFLOP_STRATEGY
- Telemetry scope invariant: block-scoped sys.stderr.write inside nested-if is invisible to daemon grep — hoist to FUNCTION scope on unconditional path. Gate: daemon ≥30g → require delta!=0 in ≥5% of bot_telemetry lines else INERT → block commit.
- STACK-OFF GUARD PLACEMENT INVARIANT: guards inside `to_call>=my_chips` reached ZERO times (any legal non-allin raise leaves ≥1 chip → to_call<my_chips always) — DO NOT place there; relocate to `to_call>0` (~40% decisions) or opponent_allin branch. v148 SPR gate upstream = only validated tail-containment lever.
- **FOLD-SIDE RULE (river stack-off leak RESOLVED in v162):** binary `return -1` fold gates EXHAUSTED (v135-v154, 12+ gens) — NEVER add a NEW one. The river 0%-fold leak WAS the highest-EV unaddressed fix; v162 CLOSED it by RELOCATING existing _river_stackoff_guard to sit BEFORE strategy.py `to_call>=my_chips` + opponent_allin early-returns (~L1057) — a PLACEMENT FIX of EXISTING code, NOT a new gate / NOT constant tuning, so it does NOT trip the dir-audit fold-side ban. META-LESSON: "relocate existing guard pre-early-return" (ALLOWED) ≠ "add new fold gate" (FORBIDDEN) — v160/v161 Masters misread this, costing 2 gens; future Masters must distinguish. PENDING: verify STACKOFF_GUARD_RELOCATED fires ≥5% at daemon ≥30g vs v139/v149; if <5% relax bet_ratio 0.75→0.60; confirm +0.10 pot-odds buffer doesn't double-fold with _spr_commitment_gate +0.03 win_rate buffer on the same river all-in hands.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (value_maximizer_overbet, river_value_raise_tier, turn_second_barrel_planner); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) each open a per-axis door but exhaust.
- Preflop opponent sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps → gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; most anchors in that range still ACTIVE (only v132/v134/v136 reaped — "anchors REAPED" overstates the cull). Includes check_raise_freq = DEAD directive. Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.
- Re-verification caveat (STILL PENDING, 6 gens later): all 0-fire / no-WR-lift verdicts for v144-v151 were measured BEFORE the v156 prior_weight 4.0→2.0 unblock, on provably prior-weight-saturated (inert) detectors. bot_telemetry/ now holds v157-v162 data but NO re-grep verdict is recorded — do NOT treat any [POSSIBLY EXHAUSTED] marker in this section as a hard ban until re-grepped post-v156.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing, two-band slope reshape, made_str cliffs) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).
- Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; widen_threshold must target ≥15pp bands; implied-odds/speculative hands need ≥0.35.

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise (v135 cut v134 after 10g; v136 restored). Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST) — Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v163**: Critic evidence: H2H weaknesses: v162 weakest H2H: v155/v130 30%, v161/v156/v157/v143 40% — all at 10 games (sub-30g noise, not actionable). v163 has NO H2H data yet (fresh commit). No specific flop c-bet sizing leak confirmed from match data — change is theory-driven from poker principles.; Experience pool refs: choose_raise() sizing [POSSIBLY EXHAUSTED] but EXEMPT clause: 'OFFENSIVE imports adding NEW opponent-signal gating' — v163 adds board_texture (wet/dry) gating + fold_to_bet_flop per-street signal, qualifying for exemption., M5/M6 rules satisfied: continuous delta (no deadzone), function-scope unconditional telemetry., ≥3 dispatch sites birth requirement satisfied (4 sites: 2 choose_raise + donk + probe).; Diff refs: strategy_helpers.py L415-494: NEW _flop_cbet_texture_delta — continuous delta dry_fold_shrink (-0.050 * dry_signal * fold_signal) + wet_value_grow (+0.080 * (1-dry_signal) * made_signal), gated by conf_gate., strategy.py L1410-1437: 1st dispatch (main choose_raise c-bet path, flop round_idx==1 to_call==0)., strategy.py L1767-1770: 2nd dispatch (donk bet path, delta applied to ratio pre-sizing).
- **v162**: Relocation of shadowed _river_stackoff_guard (any call-site AFTER an early-return block = guaranteed dead code) shipped at strategy.py ~L1057, BEFORE opponent_allin + to_call>=my_chips returns; reachability_test confirms weak 3d5c folds (-1), nut AhAd calls (-2). META: placement-fix of EXISTING code ≠ new gate — does NOT trip dir-audit fold-side ban (broke the v160/v161 misread streak).
- **v162 归档建议**: daemon ≥30g vs v139/v149 → grep STACKOFF_GUARD_RELOCATED ≥5%; if <5% relax bet_ratio 0.75→0.60; confirm river fold 0%→>5% + worst-swing -20k→-9k; watch double-fold w/ _spr_commitment_gate +0.03 buffer (fires FIRST upstream = correct).
- **v161**: 5-way AND gate + single dispatch site = high INERTNESS risk (recurring signature since v137); require ≥3 dispatch sites at birth OR pre-relax one AND condition (v155/v151/v149 all needed post-hoc relaxation after daemon <5% firing). turn_bluff_continuation_barrel has only 1 site (strategy.py ~L1671) — must add donk/probe sites.
- **v161**: Bluff-axis (to_call==0 air<0.30) is structurally disjoint from value-axis (made>=0.45) — correct offense pattern avoiding dispatch-order shadow (unlike v134/v141 river-value pile-up). 归档建议: daemon ≥100g vs v144/v143 no WR lift → drop has_equity_fallback gate + add 2nd/3rd dispatch at donk/probe (L1599/L1617).
- **v160**: Dispatch-site undercount is a 3-gen recurrence (v149/v158/v160 default to 2 sites when mirroring an exploit pattern) — Master plans must explicitly mandate 'wire 3 sites incl. donk/probe paths' to satisfy the birth requirement in one pass.
- **v160**: TURN_VALUE_FLOOR is theory-driven (no confirmed calling-station opp in H2H; only 2 sites = below birth mandate; fold_to_bet_turn≤0.42 tight) — daemon ≥30g → verify get_action() reachability ≥5%; else relax ≤0.45 + add 3rd dispatch site.

