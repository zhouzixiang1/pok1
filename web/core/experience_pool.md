## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, classify_sizing_tendency) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".
- value_maximizer_index (v134, opponent.py:229) = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); gating with it counts as a NEW opponent signal (PARAMETER_TUNING EXEMPT).
- classify_sizing_tendency() (v137) = 4-bucket overbet/underbet classifier with per-street breakdowns + NaN/inf guard; NEW sizing-aware defense signal, pairs with value_maximizer_index.

## POSTFLOP_STRATEGY
- Defensive fold/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires all on a branch NOT in HEAD v136 and REGRESSED (0.5706→0.5306@360g); `_spr_commitment_gate`/`_single_reraise_stackoff_guard`/`_river_stackoff_guard` ABSENT in HEAD v136. Future fold-gate work must ship a persistent firing counter + ≥100g H2H improvement.
- v129 turn_second_barrel_planner = validated OFFENSE pivot (LIVE since v133) — DOUBT: v134 wired value_maximizer_overbet BEFORE it (dispatch-order shadow), may be partially INERT until ≥100g daemon H2H confirms turn+river raise-frequency lift ≥40% from ~33.5% baseline.
- barrel_pressure_profile + check_raise_pressure RE-WIRED LIVE by v133 (v132 removal reversed); firing effectiveness unconfirmed — verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED] (re-grep HEAD before anchoring); structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Mandate: NEW detectors MUST prove firing via persistent fixture/self-test. v134/v135/v136/v137 shipped NEW primitives with NO firing verification = violation. Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap, BLUFF_OPPORTUNITY_THRESHOLD) [POSSIBLY EXHAUSTED] — live constant is `BLUFF_OPPORTUNITY_THRESHOLD = 0.55` in line_reading.py (v136 reverted to v111's 7000g baseline); axis saturated, do NOT re-tune. (Prior "REMOVED in v132" claim was a wrong-symbol grep — corrected.)
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate it with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). BOUNDARY: crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback when Master could ship a NEW detector = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v130-v137 critic refs all ≤10-20g = noise).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (4th recurrence v127/v129/v130/v135/v137) — cited replays (G{N}H{M} format) are non-existent (actual format is timestamp-based). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add a hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.
- Lineage/HEAD hygiene: reaped bots' FUNCTIONS persist in HEAD via crossover and toggle across gens (barrel_pressure_profile/check_raise_pressure: v128 add → v132 remove → v133 re-wire LIVE) — re-grep HEAD for live symbols before anchoring; stale comment ≠ live code, "removed" ≠ gone. Line numbers drift across gens — cite symbols/paths, not line numbers.

## RECENT_LESSONS
- **v137**: classify_sizing_tendency is a NEW opponent-modeling axis (NOT exhausted fold-gate) — future Masters may legitimately reuse the overbettor/underbettor signal for raise sizing and river value-fold logic, not just defense relaxation
- **v137**: strategy.py L622-665 placement of defense relaxation BEFORE round-bucket folds, SPR-commitment, check_raise_pressure, and barrel_pressure_profile is a deliberate structural precedence change — verify via daemon firing telemetry (stderr SIZING_MARGIN_ADJ) that round-bucket folds are not being inappropriately short-circuited before treating the axis as confirmed-LIVE
- **v137 归档建议 (improvement)**: Per critic feedback to reach ≥8: add a 3rd wired site (river value-heavy fold around strategy.py L760 'River multi-barrel fold' or choose_raise value-sizing vs underbettor) AND a persistent firing-rate fixture (synthetic opponent_model dict asserting should_fold_postflop returns correctly for overbettor/underbettor/unknown) — stderr is debug-only per experience pool, so presumed-LIVE must become verified-LIVE before relying on this axis in subsequent generations.
- **v137**: NEW classify_sizing_tendency() (opponent.py, 4-bucket overbet/underbet classifier + NaN/inf guard) drives defense relaxation placed FIRST in should_fold_postflop (strategy.py) — returns False for non-air hands vs confirmed overbettor large bets; absolute_air exclusion (made_strength<0.15 AND not has_draw) preserves fold discipline. call_margin ±0.025-0.030 (strategy_helpers.py) clamped 0.0-0.08. Correctly places override FIRST per v136 dispatch-order lesson. Cited 'exploitability probe' (28% WR vs min bets, 0% WR vs 2x pot) NOT verifiable in match_replay/ — pattern aligns with documented over-folding-vs-large-bets weakness. 🔴 Only stderr telemetry (SIZING_MARGIN_ADJ/SIZING_RELAX_FIRE) = debug-only, req #6 NOT met — presumed-INERT until persistent fixture proves firing.
- **v136**: DISPATCH-ORDER SHADOW = authoritative root cause of v134's leaks vs disciplined barrel-aggressive bots (v93 0.40@10g, v117 0.60@10g, v114 0.40@20g — all sub-30g noise; v111 beats v93 0.5217@230g). Future Master MUST relocate value_maximizer_overbet call-site to AFTER river_value_raise_tier + turn_second_barrel_planner, NOT re-tune the threshold.
- **v136**: value_maximizer_overbet (vm_index≥0.75 AND confidence≥0.30) + turn_river_blocker_bluff() both have 0-3/6 birth requirements met (no fixture/self-test) — treat as presumed-INERT until a persistent firing-rate fixture proves otherwise.
- **v136**: BLUFF_OPPORTUNITY_THRESHOLD 0.48→0.55 reverted to v111's 7000g baseline (well-commented); require ≥100g H2H vs v93/v117/v114 before further tuning — if v136 fails to recover ≥half the v111-vs-v134 gap, declare the bluff-opp axis structurally dead and pivot to a NEW detector (per-street fold-to-cbet tracking, or river value-heavy donk-bluff polarization).
- **v135**: Do NOT reverse a prior gen's master-planned direction on noise — v135 cut v134's offense pivot after only 10g H2H. v136 correctly dropped `_river_stackoff_guard` (absent from HEAD), resolving the offense-vs-defense collision in favor of offense. Wait ≥100g daemon H2H before cutting either side; calibrate made_strength floor 0.88→0.78-0.82 if leaks confirmed at ≥100g (actual: 4 of 230 games ≤-19000, 2 mirror — docstring replays fabricated).

