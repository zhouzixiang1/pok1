## OPPONENT_MODELING
- Use live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never classify unknown openers as tight by default.
- SB-open/BB-defense adaptation must use open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Verify claimed primitives are LIVE in current source before planning. CAUTION: v116's `_river_valueheavy_donk_bluff()` was claimed added but is ABSENT from v116/v117 source (0 defs) — grep-verify every primitive; don't trust memory/commit claims.
- Master plans must cite weakest matchups with concrete evidence + reference experience-pool offensive targets. H2H ≥20g is the target bar (head_to_head.json: 418/435 pairs have ≥20g). Cite in-flight matchups explicitly; do NOT fabricate.

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION (facing_barrel_continuation / _single_reraise_stackoff_guard / _spr_commitment_gate / _allin_board_texture_fold) [POSSIBLY EXHAUSTED] — symbols CURRENTLY ABSENT from HEAD (dropped via crossover rebase); any re-introduction = NEW structural work (new fn + new opponent-line signal + ≥3 wired sites + ≥3 cited replay folds). Prefer offense pivot.
- probe_mode sizing-knob constant tweaks (thin-value floor / static-board sizing) [POSSIBLY EXHAUSTED]. Structural-correctness repairs (e.g. v102 single-line bypass fix) and NEW probe/donk primitives remain permitted.
- Detection-without-handler is recurring dead code: every new detector must wire ≥3 consuming action sites same gen AND log firing-rate. v117's `turn_double_barrel_planner()` violated both (1 wiring site, no firing-rate logs) → critic-rejected.
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, and trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, check-raise frequencies, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion when they add a NEW detector + new opponent-line signal + ≥3 wired reachable sites with firing-rate logs. Birth requirements: ≥3 wired sites + ≥30g confidence gate + firing-rate logs, else dead code.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, stacked deltas, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks across ≥6 gens; re-grep HEAD for live symbols before referencing (line anchors drift every gen).
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming the constant still exists in current source.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Ratings are LIVE (Glicko-2: 30 bots r≈1289-1501, rd≈37-350, updated 2026-06-18).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (≥7 consecutive crossover gens v111-v117) [POSSIBLY EXHAUSTED]. FORBIDDEN: recombining defensive genes via byte-identical fold-gate imports (symbols CURRENTLY ABSENT). PERMITTED: crossover injecting a NEW offensive primitive (new detector + ≥3 wired sites + new opponent signal) OR a structural-correctness repair on the mutation half, especially when Master JSON collapses under triple-forbid. Hard-block constant-only edits to barrel-continuation fns.
- Crossover parents by H2H win-rate + diversity (418/435 pairs ≥20g). Always verify the crossover tool actually ran and grep-prove imports are absent from base AND on eligible-retry list before importing.
- Boundary rule: same-fn/byte-identical re-import = exhausted; new fn name + new opponent-line signal + ≥3 wired sites + cited replay evidence = new. When ambiguous, prefer offense pivot over defensive expansion.
- broadway_suited bucket re-import as a new def [POSSIBLY EXHAUSTED] — currently a label in state.py (0 defs in strategy.py). Edits to existing broadway logic with a new opponent signal + ≥3 wired sites are permitted.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns count as same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Direct H2H is authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic dual-review drift: when the same diff scores divergently, treat the LOWER score as authoritative when it cites missing evidence; verify high-swing replay claims against real replay deltas. When a fallback bypasses critic via action:approve despite low score, archivist must flag the parent as low-confidence.

## RECENT_LESSONS
- **v117**: Critic evidence: H2H weaknesses: v116 weakest matchups all sit at 10-20g (vs v102 0.30/10g, vs v87/v96/v97/v93/v103 0.45/20g) — below the 30g noise floor per experience_pool.md GENERAL rule. No matchup has ≥30g paired net-chips evidence either direction.; Experience pool refs: experience_pool.md line 19: 'Threshold-only nudges on adjacent gates (line_reading bluff thresholds...) count as constant tuning when no new gating/opponent signal is added.' — this change is precisely that., experience_pool.md line 25: 'Crossover-as-default fallback (≥7 consecutive crossover gens v111-v117) [POSSIBLY EXHAUSTED]. ... Hard-block constant-only edits to barrel-continuation fns.' — v111-v117 is exactly 7 crossover gens., experience_pool.md RECENT_LESSONS v117: prior v117 was rejected for birth-requirement violations; current v117 dropped the primitive entirely leaving only the exhausted-axis tweak.; Diff refs: strategy_helpers.py:267 — single-line change: `boost = 0.03 + 0.05 * max(0.0, bluff_opp - 0.55)` → `- 0.48`. Effect capped at 0.08 behind 4 gating predicates (line_label='bluff_heavy', confidence>=0.15, made_strength 0.20-0.45, draw_strength<0.18)., line_reading.py — only comment/docstring changes; BLUFF_OPPORTUNITY_THRESHOLD=0.48 unchanged from v116., barrel_planning.py — DELETED (was the only novel detector in prior v117). Net effect: this v117 is less novel than the rejected one.
- **v117**: `turn_double_barrel_planner()` (barrel_planning.py; single wiring site strategy.py:1243) violated birth requirements — 1 wired site, no firing-rate logs, and fold_equity_b (line 151) bypasses the confidence<0.30 gate so bluff fires on unknown opps with NO fold evidence. All cited v116 "weak matchups" (vs v113/v108/v96/v93, 10-20g each) sit below/at the 30g noise floor — the premise was statistically unsound. LESSON: gate new barrels on ≥30g + fold-equity evidence + ≥3 sites; don't build a gen on sub-noise H2H.
- **v116**: Pivoted offense via crossover after Master JSON collapse 2x (triple-forbid: fold-gate + sizing + crossover). Claimed `_river_valueheavy_donk_bluff()` as NEW offensive primitive but it is VERIFIED ABSENT from v116/v117 source — the offensive claim never landed; treat as a verification failure, not a shipped feature. Manual crossover injecting a NEW offensive primitive on the mutation half is the documented recovery when Master JSON collapses, but birth requirements MUST hold and the primitive must be grep-verified LIVE.
- **v115**: Crossover grep-verification discipline — first attempt rejected for importing forbidden guards; always grep-prove the fix is absent from base AND on eligible-retry list before importing.

