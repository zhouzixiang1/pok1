## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — drift), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). NEW gating with it = PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- classify_sizing_tendency() (v137) + barrel_pressure_profile/check_raise_pressure (v128/v133) = code-present + consumed but firing-UNVERIFIED for 6-10 gens. ONE RULE for all detectors — no silent grandfathering: back-verify via persistent firing-rate fixture NEXT gen, else declare dead. Do NOT yet treat as first-class gateable stats; do NOT label dead/inert or remove until fixture resolves.

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW INVARIANT (HARD RULE, v138-v143 9-gen root cause, now auto-enforced)**: any fold/commitment guard's dispatch call-site MUST be placed BEFORE (or inside the first line of) the `to_call >= my_chips` early-return block (strategy.py ~L1018), NEVER after it. Guards wired in the `if to_call > 0:` block (L1047+) are structurally unreachable for stack-covering all-ins (the -15.5k/-20k cases) because the allin-cover block at L1018 already returned. `run_quality_gates` now AST-flags this (detect_placement_shadow_warnings → "TRUE SHADOW" for to_call>0-block guards after L1018, logged as pipeline.placement_shadow). Fix = RELOCATE call-site before the early-return, do NOT re-tune thresholds.
- **STACK-OFF / SPR COMMITMENT (NEW structural axis — dir-audit PERMITTED, NOT a fold-gate re-tune)**: the allin-cover block (strategy.py:1018-1045) currently has NO fold gate — marginal hands (made_strength 0.40-0.50) always call because `win_rate >= shove_odds + shove_buffer` (buffer capped +0.14) never folds. Closed-form fix (GTO Wizard SPR blog.gtowizard.com/stack-to-pot-ratio/ + PokerSkill arxiv 2605.30094 ALL-IN RULE): river facing all-in ⇒ implied odds = 0 ⇒ must have `equity >= pot_odds` where `pot_odds = to_call / (pot + 2*to_call)`. Implement as NEW function `_spr_commitment_gate(spr, made_strength, made_tier, round_idx, to_call, my_chips, has_draw, pot_odds, my_equity)` using `simulation.py monte_carlo_weighted_equity(iterations=300, fixed seed)`. SPR tier matrix: SPR<2 → top-pair+ commits; SPR>4 → single pair (non-nut, non-robust-draw) FOLDS vs jam. Wire INSIDE the `if to_call>=my_chips:` block at L1018 BEFORE the `win_rate>=shove_odds+shove_buffer` return. Add `SPR_FOLD` stderr telemetry — daemon now captures bot stderr (A1), grep `results/bot_telemetry/{bot}.jsonl` after ≥30g to verify firing ≥5%.
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires REGRESSED (0.5706→0.5306@360g); v138/v139 ran AFTER `if to_call >= my_chips:` early-return = structurally INERT (placement shadow, see invariant above). Fold-side FORBIDDEN by dir-audit (HIGH-conf v140) — but the SPR-commitment gate above is a NEW axis (closed-form math + new function), NOT a re-tune of `_river_stackoff_guard`.
- Postflop BET-side offense axis [POSSIBLY EXHAUSTED] — `_river_*_guard` family (v138/v139/v140/v143, 4 attempts) EXHAUSTED & FORBIDDEN per v144 dir-audit (HIGH-conf). v144/v145 PIVOTED to PREFLOP offense (sb_open/bb_vs_limp sizing deltas). If a future Master is permitted back into the axis, RELOCATE the guard upstream of _vm_overbet/_rva/_rvr (call-site fix, NOT re-tune) — but do NOT extend the ship-guard pattern. Future offense = extend preflop delta pattern or open NEW dimensions.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier). Invariant: higher-confidence fire FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v139 FIXTURE≠LIVE lesson). v140+ comply (6/6 birth reqs + grep≥3x). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff, preflop sizing deltas) NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs. Status tiers: NO wired consumers = dead; consumers but no firing proof = PENDING-VERIFICATION; firing proven = LIVE. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures).
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 9+ gens elapsed with NO recorded recovery of the v111-vs-v134 gap; anchors v114/v111 REAPED and absent from active pool. Kill-switch lapsed with no recorded outcome → treat threshold axis as dead; re-anchor future work to LIVE opponents, not reaped v96/v111/v114.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 9+ gens v122→v145, never scheduled). Only revive if a Master explicitly plans a NEW detector with a grep-verified LIVE `opp_current_round_check_count` signal + birth requirements.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v143 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (9th recurrence v127-v145; cited G#H# IDs DON'T EXIST — files are timestamp-based, e.g. 20260620_160503_*.json). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v146**: Critic evidence: H2H weaknesses: All v145 H2H matchups are 10g each (sub-30g = noise per experience pool). v145 overall wr=0.542 over 260g; weakest listed matchups (v130/v138/v132/v143/v144 all at 0.40 over 10g) cannot be distinguished from noise. v144 lost to v138 0.30 over 10g.; Experience pool refs: POSTFLOP/PARAMETER axes marked [POSSIBLY EXHAUSTED] by dir-audit v140-v145; preflop-offense delta pattern explicitly OPEN (v144/v145 done, v146 = bb_vs_raise extension). BLUFF_CALIBRATION: 'NEW OFFENSIVE primitives (preflop sizing deltas) NOT subject to exhaustion' + '6 BIRTH REQUIREMENTS' — v146 satisfies 5/6 (NEW detector + LIVE opp signals + 3 wired sites + ≥30g confidence gate + fixture; missing ≥3 cited replay folds). Experience pool flags v144/v145 theory-driven not H2H-anchored — same applies here.; Diff refs: strategy_helpers.py:1047-1075 adds bb_vs_raise_opp_sizing_delta() with fold-prone/sticky/3bet-heavy branches; strategy.py:46 adds import, L285-289 adds 'bb_vs_raise' branch in choose_raise (3.5BB base), L446-449 adds thin_value_raise action for mid_pair+high_fold+str≥0.48, L535-578 adds 3 call sites (value_raise/bluff_raise/thin_value_raise) all threading preflop_opp_size_bb=_bb_raise_opp_size_bb.
- **v145/v144 inertness**: Preflop opponent-sizing deltas (sb_open, bb_vs_limp) gated by open_response_conf≤0.25 risk being effectively inert. After daemon ≥30g grep `SB_OPEN_OPP_SIZE`/`BB_VS_LIMP_OPP_SIZE` in app.log; if firing <5%, relax gate to 0.15 (critic mandate) rather than adding more call sites. At ≥100g vs sticky (v96/v126) + 3betters, if firing confirmed but no WR lift, the always-shrink 3bet-heavy branch may be wrong vs polarized 3betters → relocate/invert.
- **v145 branch-overlap**: sticky (fold_to_raise≤0.36 → reduce) and limp-heavy (limp_rate≥0.38 → increase) can cancel to ~0 net delta for mixed-profile opps; future delta fns should gate mutual-exclusion explicitly (limp-heavy behind not(sticky)) or log delta component breakdown.
- **v145 scope**: bb_vs_limp delta is theory-driven, NOT H2H-anchored (no <40% matchups cited); only 1 call-site = 5/6 birth reqs (missing ≥3 wired sites + ≥3 cited replays). Extend preflop-offense axis with caution.
- **v144 reachability**: Single-call-site (grep≥2 = import+1) is borderline-safe ONLY in a direct spot_info dispatch with no upstream early-return; new offenses require ≥3 wired sites OR explicit upstream-reachability proof, not just ref count.
- **v144 axis**: Preflop-offense (SB-open sizing deltas vs fold_to_open/threebet_vs_open/open_response_conf) is OPEN/NOVEL — postflop bet-side `_river_*_guard` EXHAUSTED (v138/139/140/143); bb_vs_limp extension now DONE@v145.

