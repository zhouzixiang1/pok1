## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); NEW gating using it = PARAMETER_TUNING EXEMPT.
- **Firing verification methodology (CORRECTED):** bots emit telemetry via sys.stderr (stdout corrupts the JSON protocol), but web/core/engine/battle.py `_PersistentBot` reads ONLY stdout → ALL sys.stderr telemetry (STACKOFF_GUARD_RELOCATED, FLOP_CBET_TEXTURE, SPR_FOLD) is INVISIBLE to daemon grep. The "daemon ≥30g → grep bot_telemetry/ for delta!=0" check is UNFULFILLABLE. reachability_test.py = force-fed code-reachability proxy only (0 fires = untested, not 0); it does NOT measure live firing rate. Until battle.py is fixed to drain stderr, validate via reachability_test + ≥100g H2H WR-lift, NOT telemetry grep.
- smooth_rate prior_weight must be reachable BEFORE adding detectors (4.0 saturated a 50%-folder below the 0.50 gate; fixed 4.0→2.0 v156).
- calldown_profile sample trap: foldy opps never reach n≥4 (each fold ends hand) yet detectors gate on n≥3 → use empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2; need n≥3 (not n≥2) before licensing aggressive slopes.
- INERT detectors fire telemetry but move sizing <2% pot — require per-hand flags (prior_aggressor_checked_back from history) before approving population-mean signals as new axes.

## POSTFLOP_STRATEGY
- **Telemetry scope invariant (CORRECTED):** NO sys.stderr scope — block OR function — is ever read by `_PersistentBot`; hoisting stderr.write from a nested-if to function scope does NOT restore grep visibility. Function-scope telemetry is just as invisible. (This invalidates the earlier "hoist to function scope" fix.)
- STACK-OFF GUARD PLACEMENT INVARIANT: guards inside `to_call>=my_chips` reached ZERO times (any legal non-allin raise leaves ≥1 chip → to_call<my_chips always) — DO NOT place there; relocate to `to_call>0` (~40% decisions) or opponent_allin branch. v148 SPR gate upstream = only validated tail-containment lever.
- **FOLD-SIDE RULE (CORRECTED — leak NOT yet closed):** binary `return -1` fold gates EXHAUSTED (v135-v154, 12+ gens) — NEVER add a NEW one. v162 applied the correct PLACEMENT FIX (relocated EXISTING `_river_stackoff_guard` BEFORE strategy.py `to_call>=my_chips` + opponent_allin early-returns ~L1057), but the leak itself (0% river fold, -20k swings) is NOT yet verified closed — PENDING daemon verification, currently blocked by the stderr-drop issue. META-LESSON: "relocate existing guard pre-early-return" (ALLOWED) ≠ "add new fold gate" (FORBIDDEN) — v160/v161 Masters misread this, costing 2 gens. If the relocated guard fires yet the leak persists, the next lever is NOT a new gate.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (value_maximizer_overbet, river_value_raise_tier, turn_second_barrel_planner); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune. Dispatch-site undercount is a 3-gen recurrence (v149/v158/v160 default to 2 sites) — Master plans must mandate "wire 3 sites incl. donk/probe paths" in one pass.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) each open a per-axis door but exhaust.
- Preflop opponent sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps → gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; most anchors in that range still ACTIVE (only v132/v134/v136 reaped — "anchors REAPED" overstates the cull). Revive only with NEW detector + LIVE opp_current_round_check_count + birth reqs.
- Re-verification caveat (CORRECTED): the original "re-grep post-v156 to clear these markers" plan is UNFULFILLABLE — stderr telemetry is invisible to daemon (see OPPONENT_MODELING). The "no-WR-lift @ ≥100g" verdicts (H2H-based) keep these markers advisory; the "0-fire" verdicts (telemetry-based) are INVALID and un-rerunnable until battle.py drains stderr. Do NOT treat these markers as hard bans.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing, two-band slope reshape, made_str cliffs) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).
- Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; widen_threshold must target ≥15pp bands; implied-odds/speculative hands need ≥0.35.

## GENERAL
- **🔴 HIGHEST-ROI UNBLOCK (v163 root cause):** battle.py `_PersistentBot` reads ONLY stdout, NEVER stderr → ALL telemetry verification via daemon grep is UNFULFILLABLE. This single defect is the root cause behind every "daemon ≥30g grep" archive-suggestion failing since v137, and it makes many "INERT / 0-fire" verdicts suspect (detectors may have been firing but invisible). Fixing ONE line (drain stderr) unblocks all firing verification; until then, no daemon-grep verdict is possible — rely on reachability_test + ≥100g H2H WR-lift.
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise (v135 cut v134 after 10g; v136 restored). Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST) — Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v164**: Critic evidence: H2H weaknesses: v163 parent r=1475 rd=41 sits below v147(1560)/v152(1556)/v141(1549)/v155(1539); lineage v159-v163 at rating plateau. No v164 games yet. 30g+ matchups cluster 44-56% (e.g., v129 vs v130: 47.8% @90g, v129 vs v135: 47.1% @70g) — plateau confirmed, structural change warranted over constant tuning.; Experience pool refs: 'Treating all opponents the same (no exploitative adjustment)' = Common Weakness #8, unaddressed in lineage. 'Axis novelty is NOT a success signal' caveat applies BUT verdict suspect under stderr-drop root cause — treat as zero-EV until ≥100g H2H WR-lift. 'NEW fn + NEW opp-line signal + birth reqs = new axis' — this change adds a new decision SYSTEM (archetype gating), reuses LIVE signals (ftb/vpip/aggr), not a sizing-axis repeat.; Diff refs: opponent.py: classify_archetype() L413-438 (4 buckets, conf≥0.20 gate, backward-compat default). strategy.py L906-907 archetype computed + stderr telemetry. L1371 blocker_raise `and archetype != 'calling_station'`. L1569 _delayed_calldown_bluff guarded by `archetype != 'calling_station'`. L1691-1700 turn_bluff_continuation_barrel wrapped in `if archetype != 'calling_station'`. L1887 _should_checkraise_trap guarded by `and archetype != 'rock'`. Note: 'aggro' archetype computed but UNUSED in any branch — currently a future-hook only.
- **v163**: Axis novelty (~8th 'first new axis' gen: v144/v149/v152/v155/v157/v163) is NOT a success signal — 7 prior 'new axis' gens landed "INERT" in production, but that verdict is itself suspect under the stderr-drop root cause. Treat 'new axis' as zero-EV until ≥100g H2H WR-lift vs named opponents (NOT telemetry grep).
- **v163**: strategy_helpers.py at 96.4% of the 2500-line hard cap (2409 lines). Future offense primitives MUST reuse existing dispatch sites/telemetry scaffolding or extend an existing function — net-new multi-site dispatch + 5-case fixtures hit the cap within 1-2 gens.
- **v163 归档建议**: Verify via reachability_test.py (NOT daemon grep — stderr dropped): confirm _flop_cbet_texture_delta reaches all 4 dispatch sites; daemon ≥100g vs v144/v141/v152 for WR-lift; verify combined v163 wet+flop+made + v142 _vulnerable_made_protection_floor sizing stays ≤0.80x pot (gate wet_value_grow behind not protection_floor_active if overflow).
- **v162**: Relocation of shadowed _river_stackoff_guard (call-site AFTER early-return = dead code) shipped ~L1057 BEFORE opponent_allin + to_call>=my_chips returns; reachability_test confirms weak 3d5c folds (-1), nut AhAd calls (-2). META: placement-fix ≠ new gate, does NOT trip dir-audit fold-side ban (broke v160/v161 misread streak). Leak NOT yet verified closed.
- **v162 归档建议**: Verify via reachability_test (stderr grep unfulfillable); daemon ≥100g vs v139/v149 to confirm river fold 0%→>5% + worst-swing -20k→-9k; watch double-fold w/ _spr_commitment_gate +0.03 buffer (fires FIRST upstream = correct). If guard fires but leak persists, relax bet_ratio 0.75→0.60 (NOT a new gate).
- **v161**: 5-way AND gate + single dispatch site = high INERTNESS risk (recurring since v137); require ≥3 dispatch sites at birth OR pre-relax one AND condition. turn_bluff_continuation_barrel has only 1 site → must add donk/probe sites.
- **v161**: Bluff-axis (to_call==0 air<0.30) is structurally disjoint from value-axis (made>=0.45) — correct offense pattern avoiding dispatch-order shadow. daemon ≥100g vs v144/v143 no WR lift → drop has_equity_fallback + add 2nd/3rd dispatch at donk/probe.

