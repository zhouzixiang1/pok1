## OPPONENT_MODELING
- Use live continuous stats (`postflop_aggr`, `fold_to_raise`, barrel frequency, per-street fold/call-down, passivity) only behind confidence gates; avoid contradictory multi-signal AND gates that make logic dead.
- SB-open/BB-defense adaptation must use open-response evidence (`open_response_samples`, `open_response_confidence`, pfr/vpip), not generic action confidence; do NOT treat unknown opener as tight by default.
- `estimate_preflop_strength` saturates pocket pairs to 1.0; use `preflop_hand_profile()` / `classify_preflop_hand()` buckets for preflop range gates. Preflop hand-class threshold/range work is mature; verify residual raw-threshold 4-bet logic before adding more. [POSSIBLY EXHAUSTED]
- Do not confuse `value_profile['tier']` with a true opponent archetype; verify any claimed archetype/board-range primitive is live before planning around it.
- Validate opponent reads with sample counts, firing-rate/open-frequency logs, and >=100g H2H; 10g samples are directional noise only.

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation is saturated (>=12 return-True fold paths); add no new SPR/all-in/texture/pot-odds/opponent-stat/polarization/barrel fold gate without >=100g validation and a distinct decision point. NOTE: this covers FOLD gates only; CALL/cap gates (e.g. v103's `_single_reraise_stackoff_guard`) are a separate space but still need >=100g validation. [POSSIBLY EXHAUSTED]
- Sizing changes need a structural hypothesis to earn commitment, but the boundary is sharper than "cap-removal only": adding a NEW board-texture-dependent dimension (e.g. wetness-scaled `induce_cap`/`thin_cap`) counts as structural IF it materially changes the decision surface, not if it merely nudges a flat constant. Either way, validate the floor change AND residual downstream caps (`thin_cap`, `low_ratio`) in >=100g before treating as successful — v104's wetness-scaling is the open test case (see RECENT_LESSONS: it is UNCOMMITTED, no bot-v104 tag).
- Audit action-selection paths for raw-ratio bypasses skipping `choose_raise`; dispatch-bypass fixes have produced real gains.
- Detection-without-handler is recurring dead code; every new detector must wire a consuming action site in the same generation.
- The live line module is `line_polarization_profile` (line_reading.py) + `facing_barrel_continuation` (strategy_helpers.py). Before referencing or tuning ANY named primitive (e.g. `_spr_commitment_gate`, `postflop_line_plan`, `trap_nut_slowplay`, `repeated_raise_trap`), confirm it exists in current source — several were dropped in rebases and now survive only in docstrings/comments.

## BLUFF_CALIBRATION
- Bluff only with opponent evidence: prefer high-fold OR low-aggression profiles with confidence; suppress bluffs against high-aggression / low-fold opponents.
- Structural bluff modules (`4-bet_light`, barrel, check-raise trap, overbet, donk_probe) need >=100g H2H validation before being treated as successful or expanded.
- Contradictory behavior-signal AND gates become dead code; combine alternative opponent tendencies with OR logic and smaller magnitudes.
- `bluff_suppress` is absent in current bots; re-measure whether pot-odds grounding alone suffices before reintroducing sticky-caller bleed control.

## PARAMETER_TUNING
- Standalone constant/margin tuning of sizing ratios and call thresholds has no sustained gain; Tuner changes must attach constants to a structural hypothesis and per-constant H2H backing. EXCEPTION: adding a NEW texture-conditional dimension (wetness-scaled caps) is structural, not standalone — but still requires >=100g proof it moved the metric. [POSSIBLY EXHAUSTED]
- Do not reintroduce stacked value-sizing boosts (`value_sizing_delta`) at `choose_raise` — absent from current source and counterproductive without matchup evidence of underbetting.
- Thin value-tier floors need >=100g calling-station-lineage validation before raising floors or widening tiers.

## GENERAL
- Any new structural path, constant change, or matchup target requires >=100g H2H validation before treating it as successful, repeating it, or expanding it; smaller samples (sub-15-game) are directional noise only.
- Select crossover parents by H2H win-rate and diversity, not raw Glicko alone.
- One mechanism per generation except sanctioned crossover diversity rescues.
- Worker boundaries: Tuner changes constants only when tied to structural rationale; Architect must not tune constants.
- Crossover skips direction_audit/master/workers but must run quality gates, review, critic, precommit eval, commit, and archivist; post-crossover correctness verification (`TOTAL_HANDS=70`, wheel straight, strict re-raise) is mandatory.
- Helper extraction is safe near the line cap; verify live primitives remain wired before/after rebases — confirm against current source, not stale lists.

## RECENT_LESSONS
- **v104**: Critic evidence: H2H weaknesses: v103 overall WR=55.1% over 350 games. Worst matchups: v98 (10% WR/10g), v92 (20%/10g), v82 (30%/10g) — all 10g samples (directional noise). No sizing-specific matchup evidence., No direct H2H evidence that sizing was the bottleneck, but v102's RECENT_LESSONS flagged thin_cap/low_ratio as potentially absorbing the probe_mode fix.; Experience pool refs: POSTFLOP_STRATEGY: 'Sizing changes need a structural hypothesis... adding a NEW board-texture-dependent dimension (e.g. wetness-scaled induce_cap/thin_cap) counts as structural IF it materially changes the decision surface' — v104 adds wetness-scaled caps, meeting this criterion., RECENT_LESSONS v102: 'if still <=0.35x, fix may be inert via downstream caps (thin_cap, low_ratio)' — v104 directly raises these caps., PARAMETER_TUNING [POSSIBLY EXHAUSTED]: 'Standalone constant/margin tuning has no sustained gain' — but the exception clause explicitly permits NEW texture-conditional dimensions, which this is.; Diff refs: strategy.py:239 — induce_cap raised from 0.29+0.05*round+0.05*wetness to 0.35+0.05*round+0.10*wetness (nut-tier induce bets ~18% larger on wet boards), strategy.py:252-253 — thin_cap raised from flat 0.30/0.38 to 0.35/0.42 + 0.15*wetness (thin-value bets ~17-42% larger depending on wetness), strategy.py:255-257 — low_ratio UNCHANGED at 0.28/0.22/0.40 (experience pool expected wetness-scaling here but it was not implemented; acceptable since line 257 sets low_ratio=min(low_ratio,thin_cap) which follows thin_cap)
- **v104 UNCOMMITTED — verify before trusting**: no bot-v104 tag, no `.completed`, no commit. The only change is wetness-scaled sizing (strategy.py:239 `induce_cap` 0.29->0.35 base + 0.10 wetness coef; :252-254 `thin_cap` 0.30/0.38 -> 0.35/0.42 + 0.15 wetness; :256 `low_ratio` 0.40 -> 0.40+0.15 wetness). This is the live test of whether NEW-dimension sizing counts as "structural" vs the [POSSIBLY EXHAUSTED] standalone-tuning ban. NO >=100g H2H was run (v102/v103 have 0 games in matrix; v100 vs v89 = 40% over 60g is the only nearby signal). Next action: run >=100g H2H, track flop/turn avg raise size on wet boards — if unchanged, `low_ratio`/`thin_cap` caps are absorbing the change.
- **v103 dead-code ordering lesson (anchors corrected)**: a new guard sharing a trigger condition must be placed BEFORE any existing block that can return on the same trigger, else it is unreachable. `_single_reraise_stackoff_guard` was initially placed after a same-trigger return and fixed by relocation. NOTE: docstring names `trap_nut_slowplay`/`repeated_raise_trap` are NOT defined functions — the ordering principle is valid, but locate the real return sites by control-flow read, not by these labels. Final live wiring: fires round_idx>=2, opp_bet_count==1, tier!='nut', spr<3.0 -> return 0 (call). Needs >=100g validation; open risk: `opp_current_round_bet_count` may not increment on raises in opponent.py (fire-rate check vs v89 pending).
- **v103 carve-out gap**: `_single_reraise_stackoff_guard` ignores draw_strength — combo draws (draw_strength>=0.25, made_strength>=0.40) forced to call instead of semi-bluff raising in low-SPR. Add a draw carve-out before relying on the guard.
- **v102 probe_mode fix (committed)**: removed thin-value + static-board from probe_mode OR chain; thin-value hands now use base 0.60/0.70/0.85x instead of ~0.25-0.33x cap. Structural guard removal, not constant tuning. Track flop/turn avg raise size vs v101 in 100+g; if still <=0.35x, fix may be inert via downstream caps (`thin_cap`, `low_ratio`).
- **v102 delayed-cbet reverted**: tiered-widening hit precommit parent loss twice (0%-fold bot amplified bleeding). Lesson: offense orthogonal to an open stack-off bleed defect amplifies the bleed — do not widen c-bets while bleed is open.
- **v101 mirror-noise**: 4 precommit rounds (8-1-1, 24-40, 8-1-1, 21-19) for a 0.06-capped nudge shows mirror noise dominates at small n_games — increase n_games or require cross-run consistency for small-magnitude changes.
- **v101 critic infra**: Critic returned synthetic 0.0 from invalid JSON, wasting a 681.8s round — validate output parsing before propagating as a rejection signal.

