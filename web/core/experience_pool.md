## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — cite SYMBOLS/PATHS, NEVER line numbers (they drift) or commit hashes. rd=350 = Glicko DEFAULT/unrated, not "live top".
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). NEW gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic, not threshold nudges.
- Detector firing verification: grep symbols in bot_telemetry/{bot}.jsonl (v146+ only trustworthy; v137-v145 pre-Phase-A-fix = 0 markers by construction). reachability_test.py proves CODE-PATH execution only, NOT in-game firing. Verdict: 0 telemetry fires = PENDING-VERIFICATION, NOT verified.

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW INVARIANT (HARD RULE)**: any fold/commitment guard's dispatch call-site MUST be BEFORE/inside the `to_call >= my_chips` early-return block, NEVER after it (else unreachable for stack-covering all-ins = the -15.5k/-20k cases). Fix = RELOCATE call-site, do NOT re-tune thresholds. Closed after 7 gens (v147 SPR gate wired inside early-return); when stderr never fires, FIRST verify code-path reachability vs the early-return chain.
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v135 REGRESSED; v138/v139 structurally INERT; v147/v148 SPR gate = SAME exhausted axis (v149 dir-audit: bot OVER-FOLDS preflop 99% fold-to-bet, does NOT over-commit; fold-side made leak WORSE → PIVOTED TO OFFENSE). Do NOT extend/re-calibrate SPR gate or any fold-side threshold on persistent stack-offs.
- Defensive `_river_*_guard` family [POSSIBLY EXHAUSTED] — v138/139 call-side FOLD gates, v140/143 bet-side own-jam BLOCKERS; ALL DEFENSIVE, exhausted per v144 dir-audit. The OFFENSE pivot (v141 amplifier, v142 protection floor, v149 street-fold-boost, v151 delayed-calldown-bluff) is per-axis open but PENDING-VERIFICATION — see BLUFF_CALIBRATION for per-primitive status.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED]; flop/turn draw thresholds (v148 0.12/0.15) are ad-hoc, NOT outs×2-derived (9-out FD ≈0.36 flop) — re-anchor before tuning.
- ENFORCED STANDARD: NEW detectors MUST prove in-game firing via persistent fixture/self-test at the ACTUAL DISPATCH SITE + telemetry marker in bot_telemetry/{bot}.jsonl (v146+). CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable DISPATCH sites + ≥3 cited on-disk replay folds + ≥30g confidence gate + persistent fixture firing logs. fn_refs count (import + N call-sites) ≠ dispatch sites.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) remain open PER AXIS but each exhausts. pre-v146 offense detectors + v149/v150/v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION. Tiers: no consumers=dead; consumers but no firing proof=PENDING-VERIFICATION; firing proven=LIVE. Do NOT equate code-reachability with firing proof.
- `_street_fold_exploit_sizing_boost` [POSSIBLY EXHAUSTED] — v149/v150 (2 gens) 0 telemetry fires even after conf 0.20→0.15 / fold 0.55→0.50 relaxation (pipeline WORKS: SIZING_MARGIN_ADJ fires 933× in v150). The per-street call-down signal it lacked was DELIVERED in v151 (opponent.py `calldown_profile` + `_delayed_calldown_bluff`), which is itself PENDING-VERIFICATION (3/6 birth reqs). Do NOT re-demand adding the signal — it exists in HEAD; verify `_delayed_calldown_bluff` firing instead.
- preflop opponent-sizing-delta pattern (additive choose_raise BB delta, gated by single opp signal) [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add a 4th preflop variant; require ≥100g H2H. open_response_conf≤0.25 risks inertness; sticky (reduce) + limp-heavy (increase) branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recorded recovery; measurement anchors (v111/v114) REAPED, so the verdict is un-revalidatable. Treat threshold axis as dead; re-anchor to LIVE opponents first.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 14+ gens v122→v151, never scheduled). Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed) — don't reflexively fall back to crossover. Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opp-line signal + birth reqs = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v151 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans (master+worker fallback can masquerade as crossover); direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (recurrence v127-v151; cited G#H# IDs DON'T EXIST — files are timestamp-based, e.g. 20260620_*.json; v149 docstring repeated unverifiable n=1482 claim). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; hard-gate cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v152**: Critic evidence: H2H weaknesses: v151 vs v129: 0.30 wr (10g, sub-30g noise), v151 vs v130: 0.30 wr (10g, sub-30g noise), v151 vs v137: 0.40 wr (10g, sub-30g noise) — v152 docstring claims 152/153 fold-rate but untraceable in match_history.jsonl, v151 vs v14: 0.40 wr (10g, sub-30g noise), v151 overall: 0.50 wr over 260g — plateau, no ≥30g nemesis; Experience pool refs: v149 dir-audit: 'bot OVER-FOLDS PREFLOP (99% fold-to-bet vs v132/v144), does NOT over-commit' — confirms direction, POSTFLOP_STRATEGY: 'Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v148 lineage EXHAUSTED' — pivoted to offense v149-v151, BLUFF_CALIBRATION: 'preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add a 4th preflop variant' — v152 correctly avoids sizing-delta axis (CALL/FOLD not sizing); Diff refs: strategy_helpers.py L1110-1153: NEW _preflop_steal_defense_widen() — pot-odds gated defense widening vs unknown/loose openers and frequent 3-bettors, strategy.py L434-446: Dispatch #1 — BB-vs-raise call/fold widening before tight_opener early-fold (correct placement, no shadow), strategy.py L664-676: Dispatch #2 — SB-vs-3bet implied-odds defense before final fold
- **v151**: `calldown_profile['small_bet_rate']/['large_bet_rate']` are dead data — no consumer reads them; if next-gen needs bet-size-split exploitation, wire a consumer or remove fields to reduce signal bloat.
- **v151**: `_pending_my_bet_ratio` only tracked on 'raise' (not 'allin') — if bluff exploits allin-then-fold opps, the signal misses them.
- **v151 归档建议**: At daemon ≥30g, FIRST grep DELAYED_CALLDOWN_BLUFF stderr + check firing rate; if <5%, relax curr_call_rate 0.45→0.50 and min samples 3→2 (per critic), and wire 2 more dispatch sites on donk_probe.py donk/probe paths — do NOT tune sizing constants until firing is verified.
- **v151**: Critic evidence — H2H weaknesses all sub-30g noise (vs v148/v149 0.30; vs v132/v147/v130/v146/v126/v145 0.40); overall WR 0.50 over 260g, no ≥30g nemesis = plateau. v151 meets 3/6 birth reqs (detector, signal, conf gate); misses ≥3 dispatch sites, fixture, replays. Single dispatch via strategy.py before bad_river_bluff_candidate.
- **v150**: reachability_test.py (full get_action() harness) is authoritative CODE-PATH-reachability proxy (boosted=232 proves flow) but NOT in-game firing — force-fed. Live telemetry: 0 STREET_FOLD_BOOST/VM_OVERBET/SPR_COMMIT/RIVER_STACKOFF fires while SIZING_MARGIN_ADJ fires 933× → gating thresholds genuinely don't trigger. Use reachability_test for path-reachability, telemetry for firing; never conflate.
- **v149 STRATEGIC PIVOT (HIGH-VALUE)**: dir-audit HIGH OVERRULED match-analysis fold-gate rec — bot OVER-FOLDS PREFLOP (99% fold-to-bet vs v132/v144), does NOT over-commit. Fold-side v138-v148 lineage (incl. SPR gate) EXHAUSTED and made the leak WORSE → PIVOTED TO OFFENSE (delivered v149/v151). Do NOT re-engage fold/SPR calibration on persistent stack-offs.

