## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) need ≥30g confidence gates; sub-30g is noise. Never default-classify unknown openers as tight.
- SB-open/BB-defense adaptation uses open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase).
- Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION [POSSIBLY EXHAUSTED] — prefer offense pivot. EXEMPT: a NEW detector + NEW handler counts as a new axis even though defensive. TWO fold-gate detectors now shipped (v127 check_raise_pressure + v128 barrel_pressure_profile); adding MORE parallel detectors is no longer mandated — fix INERT ones via call-site RELOCATION / spot_info-population fix.
- facing_barrel_continuation repeated re-imports across crossover gens [POSSIBLY EXHAUSTED]. Re-grep live HEAD (currently v128) before any anchor; lineage bots were reaped/deleted — don't trust stale version anchors.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]. Structural-correctness single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Detection-without-handler is recurring dead code: every new detector must meet the canonical 6 birth requirements (BLUFF_CALIBRATION).
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical 6): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code. CAVEAT: req #6 "firing-rate logs" is NOT satisfied by FOLD_GATE_FIRE stderr (debug-only, daemon runs mirror_battle without debug_bots → 0 captures ever); a persistent fixture/self-test is required instead.
- bluff/line-reading threshold tuning (BLUFF_OPP_THRESHOLD, VALUE_PRESS_THRESHOLD, bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED].
- check_raise_freq / barrel_pressure FOLD-GATE STATUS (post-v128): TWO detectors shipped — v127 `check_raise_pressure()` (strategy_helpers:319) + v128 `barrel_pressure_profile()` (strategy_helpers:349, bet_count+size bucket, 3 wired sites). Both SHARE architecture, at INERTNESS RISK; v127 SUSPECTED live-but-INERT (trap-only pattern), NOT "proven". NEXT ACTION (precondition: v128 fully rated first — currently rd=235 very_uncertain): add a persistent firing fixture/self-test; if 0 firing on calling-station spots (v96/v108/v123) → next gen RELOCATE call-site / fix spot_info population, NOT add a third detector. Mandate shifted from "deliver with all 6 birth reqs" to "make existing detectors FIRE via a captured mechanism".

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds 0.45/0.50/0.82, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT default to crossover fallback when Master can deliver a mandated NEW detector (success-return bug FIX `061353e` + regression test `web/tests/test_master_success_return.py`; false-positive "malformed JSON" log no longer means the plan failed).
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare a new path/constant/matchup successful. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY RULE: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; a NEW fn + NEW opponent-line signal + birth requirements = new axis (so non-identical mutations like v122/v124 are still permitted). Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge. Diff parents holistically (`diff -rq bots/A bots/B`); direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; ensure reviewer's diff-summary reconciles against the actual diff; watch retry-dilution.

## RECENT_LESSONS
- **v129**: Critic evidence: H2H weaknesses: v118 weakest: v25 0.256/90g, v77 0.289/90g, v14 0.300/90g, v81 0.325/80g, v87 0.338/80g — all ≥30g, statistically meaningful. Function's opponent_model confidence gate (>=0.20) will fire on these. However, docstring cites NO specific H2H matchup; targeting is indirect.; Experience pool refs: POSTFLOP_STRATEGY: 'NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion' — turn second-barrel qualifies structurally, BLUFF_CALIBRATION: 'canonical 6 birth requirements: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs' — v129 meets only 3 of 6, RECENT_LESSONS v127: 'Calibration > new functions — replacing 4 street-specific sub-thresholds with unified 0.38 threshold in empty [0.232, 0.40] band broke exhausted axis' — preferred approach for similar calibration issues; Diff refs: strategy_helpers.py:319-388 NEW turn_second_barrel_planner() — 70 lines, 4 gate stages (round/check, flop aggressor, value hand, non-deteriorated board) + sizing 0.55-0.75x scaled by made_strength + opp fold-to-turn/call-down boosts, strategy.py:1247-1259 SINGLE wired site at turn branch (round_idx==2 AND to_call==0), BEFORE standard value-bet path; risks shadowing existing logic with redundant sizing, Function parameter `value_profile` is passed but UNUSED inside — dead parameter (cleanup needed but not blocking)
- **v129 (UNCOMMITTED WIP — bots/claude_v129/ untracked, HEAD=bot-v128, NOT pipeline-validated)**: Critic cited H2H weaknesses (v110 0.30/10g, v117 0.30/10g, v90/v109/v96/v25/v121/v127 0.40/10g) — ALL sub-30g = noise. Proposed NEW `_nut_commitment_sizing_cap()` at strategy_helpers.py:395-406 (12 lines, NEW combined board-texture signal + hard-cap mechanism, NOT a fold gate; wired strategy.py:239-243 with NUT_COMMITMENT_CAP stderr). Qualifies EXEMPT for POSTFLOP/PARAMETER exhaustion (new detector + new board-texture signal). CAVEAT: NUT_COMMITMENT_CAP stderr shares the SAME debug-only limitation as FOLD_GATE_FIRE — add a persistent fixture before claiming it fires; grep alone will show nothing.
- **v128**: MANDATORY pre-commit verification — before approving any new fold-gate detector reusing the check_raise_pressure/barrel_pressure architecture, assert via a FIXTURE SELF-TEST (NOT FOLD_GATE_FIRE stderr — debug-only, daemon never captures it) that the function returns severity>0 on a spot_info matching the targeted leak. Otherwise commit is likely INERT (2 detectors at risk).
- **v128**: Replay citation integrity — cited replays (G0H28/G4H12/G2H51) must be cross-checked against experience_pool.md + match_replay/ before building a generation; fabricated/untraceable evidence cannot serve as fold-gate rationale (recurred in v127 + v129 critic — assume false until verified).
- **v127**: Calibration > new functions — replacing 4 street-specific sub-thresholds (<0.20/<0.25/<0.35) with a unified 0.38 threshold in the mathematically empty [0.232, 0.40] band between one-pair and two-pair broke the exhausted v90-v126 crossover axis. Future fold-gate work should target similar empty HAND_CLASS_SCORE bands rather than bolting on new gates.
- **v127**: FOLD_GATE_FIRE stderr telemetry is LIVE-IN-SOURCE but DEAD-IN-PIPELINE (daemon runs mirror_battle without debug_bots → 0 captures ever). NOT an authoritative INERT-detection mechanism — a persistent fixture/self-test is required to actually verify firing.

