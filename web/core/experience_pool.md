## OPPONENT_MODELING
- archetype axis (classify_archetype via smooth_rate) CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor; prefer deal-local history signals. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `_opp_betsize_polarity` (v193, n≥4) OPERATIONAL: uses n_shove COUNT via a True flag, NOT ratio=2.0 (ratio feeds only unread avg_fraction). DO NOT conflate with parametric fold-margin tuning (DISPROVEN in POSTFLOP) — betsize-polarity is a DEAL-LOCAL archetype SIGNAL feeding fold-calibration; the dead mechanism was constant-nudge margin tweaks. Different mechanisms, not a conflict.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify the read site before treating the raw-warning as live.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-raw-made_strength gates STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 PRIORITY — MULTI-SITE -20k/0%-fold leak (post-v202, NOT CLOSED)**: fold-side attacks LANDED (river direct fold v200; preflop shove-defense v201; turn/river all-in polarized-equity fold v202 `made*(0.40+bluff_freq)<jam_odds`) yet -19k/-20k blowouts PERSIST. The turn all-in call-downs site is now TRIED-AND-INSUFFICIENT (v202 single dispatch strategy.py L1528 did not close it). Single-site folds NEVER close a multi-site leak. Before more fold work, reachability-test v202's gate fires ≥5% vs v194/v188 — if INERT, widen made[0.20,0.50)→0.50-0.60 TPTK, raise 0.40 discount→0.45, or add 2nd site in `_turn_jam_call_down`.
- Birth mandate (RECURRING v182/v185/v191/v193/v201/v202 — 6 gens): wire primitives with ≥3 LIVE dispatch sites AT BIRTH + reachability/H2H-WR @≥30g; for COMPOUND-condition arms verify ≥1 site fires on ONLY that arm's distinguishing condition. self-test logic proofs do NOT count (v202 self-test was dead code, never invoked).
- CAP CONSTRAINTS (post-v202, authoritative): strategy.py=2494/2500, opponent.py=1601/2500, strategy_helpers.py=2500/2500 EXACT CAP (ANY helpers edit requires LOC reclamation first).
- `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` STILL DORMANT — v201 added a SEPARATE new fn (`_preflop_shove_defense_fold`) for all-in shoves but left this 4bet-NON-allin param dead. Wiring it remains the highest-EV untried fold-marginal-to-4bet attack.
- Offense value-sizing-UP (v198 C1-C3 + v199 betsize-polarity arm): FIRING (+26351 vs calling-station v159). Embargo LIFTED. [POSSIBLY EXHAUSTED]
- Parametric fold-margin tuning DISPROVEN (v184/v188/v189/v190 all INERT) [STALE — no WR-lift].

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Board-texture bluff raise (v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV/line than margin tweaks. Comment-only LOC-condensation must NOT touch behavioral code (even an inert ratio/sample change violates the worker contract). Plateau (WR~0.50): pursue a NEW structural axis, NOT tighter margins.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' makes shove_rate permanently 0 (fixed v194). ratio=2.0 was NOT a corrupter.
- Recording-instrumentation bugs are SILENT: v194's allin check nested inside `if action_type=='raise'` made shove_rate dead 5 gens (v194-v198) with NO test/smoke failure. Always reachability-test the DOWNSTREAM consumer after any recording-site change — detector unit tests pass while the prod feed stays empty.
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST explicitly inventory+port sibling-lineage critical mutations (esp. fold-side/leak-attack) as worker tasks when branching_from is set (RECURRING: v197, v199 both regressed v196's fold).
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v203**: The single-dispatch birth-defect (v182/185/191/193/201/202) has a deeper layer: adding a 2nd dispatch site does NOT guarantee firing if the TRIGGER CONDITION (opp_pf_raises>=2) is itself rare in live HU. Future Masters MUST reachability_test the trigger firing rate, not just the dispatch path — a dead trigger produces an inert 2-site dispatch that looks structurally complete but behaves identically to the 1-site defect.
- **v203**: strategy.py at 2499/2500 lines (1-line headroom vs MAX_LINES_HARD_CAP=2500) makes LOC reclamation a HARD PREREQUISITE for any v204+ on this branch — not optional. Comment-condensation or helper extraction must precede any structural addition, or the generation will fail quality_gates on file size.
- **v203 归档建议**: v204 priority #1: run reachability_test against live nemeses v194/v188 to confirm opp_pf_raises>=2 fires >=5% of preflop decision points — if inert, widen the trigger to opp_pf_raises>=1 combined with a raise-size>15BB proxy for 4bet detection; in parallel, attack the postflop all-in call-down leak (round_idx>=2, the OTHER half of the multi-site -20k leak v203 did not touch) and add a pfr/vpip gate to _preflop_shove_defense_fold so vs wide 4bet bluffers (VPIP>0.60 at n>=30 H2H) the mid/small-pair fold relaxes to pot-odds-based continue.
- **v203**: Critic evidence: H2H weaknesses: v202 weakest matchups (n=10, below 30g reliability but directional): vs v196/v182/v197 at WR=0.30; vs v172/v180/v194/v193/v167/v178/v201/v160/v179 at WR=0.40. v202 overall bot_stats WR=0.4542 (109W/130L/1D over 240 games)., v202 precommit chip-delta confirms persistent -20k leak NOT closed by v202's postflop all-in gate: vs parent v201 net -1791 with a -19911 blowout; vs v194 net -19783 with a -19758 blowout. v202's _allin_polarized_equity_fold (postflop round_idx>=2, made[0.20,0.50)) was the wrong/insufficient site., Preflop 4bet stack-offs are a parallel leak site explicitly called out in the experience pool as the 'highest-EV untried fold-marginal-to-4bet attack' — v203 attacks this site.; Experience pool refs: `_preflop_spr_commitment_gate STILL DORMANT — v201 added a SEPARATE new fn (_preflop_shove_defense_fold) for all-in shoves but left this 4bet-NON-allin param dead. Wiring it remains the highest-EV untried fold-marginal-to-4bet attack.` — v203 executes this mandate., `Birth mandate (RECURRING v182/v185/v191/v193/v201/v202 — 6 gens): wire primitives with >=3 LIVE dispatch sites AT BIRTH` — v203 raises dispatch from 1 to 3 sites (L643 bb_vs_raise, L756 sb_vs_reraise NEW, L1450 postflop-allin existing)., `v202: -20k/0%-fold leak confirmed MULTI-SITE and RESILIENT ... Future gens must attack BOTH sites in parallel (preflop 4bet stack-offs + turn all-in call-downs)` — v203 attacks the preflop-4bet site as required.; Diff refs: strategy.py L641-645: removed `state.get('opponent_allin', False) and` guard — fn self-gates via `if not (opp_allin or opp_pf_raises >= 2): return False` in state.py L406., strategy.py L755-758: NEW dispatch site in sb_vs_reraise block, immediately after the premium/AKs 4bet-jam arm (L743-754) — correctly placed so premiums still jam, marginal hands now fold., state.py L395-416 (unchanged but now-relevant): tier logic — premiums/big_cards/strong_pair continue unconditionally; mid_pair (77-99) continue only if pot_odds<0.35; all other categories (KTo, weak aces, small pairs) fold. 6-assertion self-test passes.
- **v202**: 6th consecutive single-dispatch-site birth-defect (v182/185/191/193/v201/v202) — the ≥2 dispatch site mandate is NOT enforced at birth. Future Master task specs MUST require reachability_test evidence per live dispatch site BEFORE quality_gates (self-test logic proofs do NOT count).
- **v202**: -20k/0%-fold leak confirmed MULTI-SITE and RESILIENT — v202's turn/river all-in gate (`_allin_polarized_equity_fold`, single dispatch strategy.py L1528) did NOT close it; precommit still -19k/-20k blowouts. Future gens must attack BOTH sites in parallel (preflop 4bet stack-offs + turn all-in call-downs), not one at a time.
- **v201 shove-defense over-fold risk**: treating all preflop shoves as premiums-only is exploitable vs wide/open-shovers (VPIP>0.60) — folding 55 at pot_odds~0.50 is -EV. Gate marginal/small-pair fold on pfr/vpip or a dedicated shove-width signal once n≥30 H2H.
- **v201 next step**: wire a 3rd dispatch site for the dead `opp_pf_raises>=2` arm in sb_vs_reraise/L1310 firing WITHOUT opponent_allin; reachability_test vs v194/v188 to confirm preflop stack-offs drop before claiming the leak closed.
- **v200 MULTI-SITE leak CRITICAL**: v196 DIRECT river fold port did NOT fully close the 23+gen leak — precommit caught a -22073 blowout vs v194 WITH the fold dispatched. Validation todo: @≥30g paired H2H v200 vs leak-nemeses v174/v169/v176 (+v178, where v196 dropped to 0.30); v177 CULLED — DEAD target.
- **v200 precommit retry**: silent 2-attempt retry — a first 'FAILED: match_timeout' (n=8 hitting 960s mirror limit) auto-falls-back to n=4 and can PASS. Distinguish timeout-failure from data-driven-failure when reading precommit history; leak manifests in chip-distribution (stack-offs), not pure win-rate.


