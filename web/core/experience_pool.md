## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never default-classify unknown openers as tight.
- SB-open/BB-defense adaptation uses open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — line and version anchors drift each rebase. Re-read HEAD + working bot each gen; do NOT hard-code commit hashes (rot within a gen, often contradicting the outcomes trail).
- Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. Re-read head_to_head.json + glicko_ratings.json each gen — rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION [POSSIBLY EXHAUSTED] — prefer offense pivot. Master-plan collapse is NOT cleanly resolved: the 48b51f2 signature fix addressed the empty-output mode, but a separate malformed-JSON collapse recurred (distinct bug). Treat Master reliability as UNRESOLVED; re-verify audit state before each retry. Neither "fix prompt/schema" nor "it is FIXED" has held up.
- facing_barrel_continuation — re-imported as byte-identical near-duplicate across crossovers (v101 origin → restored v122/v125); exhausted axis. Re-grep live HEAD before any anchor — point-in-time "absent in vX" claims rot.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]. Structural-correctness single-line bypass fixes + NEW probe/donk primitives remain permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED] (critic flagged recreation as v117-recreated-from-v104; "+13.5pp" fabricated, actual +5pp at noise floor).
- Detection-without-handler is recurring dead code: every new detector must meet birth requirements (canonical 6 — see BLUFF_CALIBRATION).
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical, single source of truth — other sections defer here): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code.
- check_raise_freq is MANDATED-BUT-UNDELIVERED 10+ gens — do NOT treat as available permit-unlocker; counts toward the crossover/permit clause ONLY once actually DELIVERED (detector + ≥3 sites + telemetry).

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens (advisory bypass + precommit PASS ≠ strategic validation). Re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming it still exists in HEAD.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (long consecutive streak) [POSSIBLY EXHAUSTED] — do not pin a version to where the streak "ends" (the anchor drifts each gen). BOUNDARY RULE: same-fn/byte-identical re-import = exhausted & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new. The permit clause fired every gen (v122/v123/v124) → its signal is NON-FUNCTIONAL; Master must earn crossover via DELIVERED NEW-offense. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Diff parents holistically (`diff -rq bots/A bots/B`); direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic-loop traps: (a) dual-review drift — treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real deltas. (b) DIFF-SCOPE RECONCILIATION — reviewer's stated summary must reconcile against actual diff (v123: "constants only" vs 14 files). (c) RETRY DILUTION — iterations progressively REMOVED the novel primitive instead of fixing execution gaps; fix gaps, don't restart smaller.

## RECENT_LESSONS
- **v126**: Critic evidence: H2H weaknesses: v118 vs claude_v92: wr=0.467 over 60g (targeted matchup), v118 vs claude_v96: wr=0.457 over 70g (targeted matchup), v118 vs claude_v108: wr=0.457 over 70g (v108 has BLUFF_OPP=0.55, beats v118), Comparative context: v108 (170g, 0.535), v107 (200g, 0.530), v117 (80g, 0.550) all BEAT v92/v96 — supports v118-specific leak hypothesis rather than v92/v96 being inherently strong; Experience pool refs: BLUFF_CALIBRATION: 'low aggression/passivity alone may indicate calling-station behavior' — directly cited in docstring as rationale, PARAMETER_TUNING EXEMPT clause: 'OFFENSIVE value-tier floor imports adding NEW opponent-signal gating count as NEW' — v126 adds NEW opponent-signal gating (if-block on postflop_aggr+barrel_freq), not a constant tweak, BOUNDARY RULE: 'NEW fn + NEW opponent-line signal = new' — v126 modifies a different file/function than v124 with a new guard mechanism (not byte-identical re-import); Diff refs: strategy_helpers.py:277-284 — NEW 8-line calling-station guard block (if post_aggr<=0.28 and barrel_freq<=0.30: return 0.0), Thresholds mirror line_reading.py:58 exactly (post_aggr<=0.28 AND barrel_freq<=0.30 contributes +0.18 to bluff_opportunity) — surgical targeting of the upstream misfire, bluff_heavy_call_widen() is wired LIVE at strategy.py:939 (call_margin += bluff_heavy_call_widen(...)) — confirmed reachable, not dead code
- **v125 — TWO cycles**: (1) Master-plan cycle (v118 base) ABANDONED — JSON-collapsed; empty-signature cause was patched at 48b51f2 but a distinct malformed-JSON collapse recurred (forensic UNRESOLVED, do NOT claim fixed). A valid plan (artifact b51fc9a, `_count_opp_check_raises`→3 fold sites) was emitted once but is NOT pipeline-reusable (execute_workers reads master_plan from CHECKPOINT, not git). (2) Crossover cycle COMMITTED eadbb3d (v118×v113, facing_barrel_continuation restored, UNtagged) — near-duplicate of v122. check_raise_freq NOT delivered (10+ gens); 0%-Fold leak unfixed.
- **v124**: Crossover v118×v108 isolating BLUFF_OPP 0.48→0.55 (retaining river_value=0.45); cleaner attribution than v123 (v118↔v108 diff = 2 functional constants). check_raise_freq STILL undelivered; 0%-fold leak unfixed. Needs ≥100g H2H vs v93/v109/v94.
- **v123**: CONFOUNDED ATTRIBUTION — when Master cites crossover parent H2H edges, diff parents holistically; the v108>v116 edge could stem from the missing v102 probe_mode fix + state.py/edge differences, NOT the imported threshold (≤40g, confounded). Edits were line_reading.py threshold-only nudges = constant tuning, no new gating. check_raise_freq mandate carried, undelivered.

