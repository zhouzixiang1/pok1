## OPPONENT_MODELING
- Archetype suppression gates on EXISTING detectors are structurally safer than AND-gated new detectors — 'standard' default = zero downside; prefer when facing INERTNESS. Tight thresholds (e.g. 0.30) without H2H justification risk inertness; respect pool-mandated ≥0.15.
- `large_bet_ratio` is RAW (no smooth_rate wrapper); apply smooth_rate prior_weight BEFORE using as an offensive signal — raw signals are noisier early and diverge from other model fields.
- calldown_profile sample trap: foldy opps never reach n≥4 — use empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.

## POSTFLOP_STRATEGY
- **FOLD-SIDE RULE:** POSTFLOP binary `return -1` fold gates EXHAUSTED (13+ gens). Continuous EV-integrated postflop fold margins ARE permitted; relocating existing guards (v162/v174 placement-fix) is allowed (placement-fix ≠ new gate).
- **RECONCILED LEAK (audit):** TWO distinct leaks coexist — do NOT conflate. (1) **-20k stack-off from PREFLOP trash commits** (59o/39o at SOURCE) — the preflop offsuit gate targets this but is near-inert (v172/v173 carve-out VPIP≥0.65 rare in 70h mirrors) and has NOT closed it. (2) **POSTFLOP over-calling vs value raises** — v173 WR 0.20 vs v169, 0.30 vs v165, 0.40 vs v152/v154 — THIS is the dominant EXPLOITABLE weakness, and the pool's preflop focus does NOT touch it. The pool's preferred axis (preflop) and the H2H evidence (postflop) point OPPOSITE ways: fix each leak with its own lever.
- **River defensive-margin call_margin axis [POSSIBLY EXHAUSTED] — RETRACTED / RECLASSIFIED LIVE:** Per A1, postflop_call_margin fired 42670× at delta≈0 = INERTNESS (implementation), NOT conceptual exhaustion. The axis never genuinely tightened margins, so flat ~50% is NOT proof of a dead lever. A correctly-calibrated river call-margin TIGHTEN (negative delta, fold more vs value/large bets) is a PERMITTED continuous-EV fold margin AND the most direct lever for the v174 over-calling leak. Treat as LIVE candidate; prior 'exhausted' label is advisory-only (marker retained mechanically, semantically retracted).
- **STACK-OFF GUARD PLACEMENT INVARIANT:** guards nested inside `to_call>=my_chips` reached ZERO — relocate to `to_call>0` or the opponent_allin branch. v174 applied the same principle to `_spr_commitment_gate`.
- **Dispatch-order shadow:** wire offensive primitives AFTER downstream tiers (overbet, amplifier, value-tier); archetype overrides AFTER pure-value functions; mandate ≥3 wired dispatch sites incl. donk/probe at birth.
- NEW detectors require 6 BIRTH REQUIREMENTS: new function + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs.
- **CAP CONSTRAINTS (binding):** strategy.py EXACTLY 2000/2000 (ZERO headroom — next edit trips MAX_LINES_PER_FILE); strategy_helpers.py ~2478/2500 (~22L, SECONDARY). Reuse existing scaffolding; do NOT add net-new ~80L modules; budget refactor/extract-to-helper BEFORE any strategy.py edit.
- *(advisory caution)* v171 DOWN-sizing arm was the WRONG lever: `_opponent_sizing_raise_boost()` DOWN-sizing vs stations did NOT shrink already-committed pots; sizing-DOWN later streets can't fix a preflop-origin leak. Do NOT extend STATION_SIZING or build a third UP-sizing primitive — reuse OR-gate + confidence-scaling scaffolding if revisited. [STALE — no WR-lift]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Binary postflop fold / line-reading threshold tuning [POSSIBLY EXHAUSTED] (13+ gens). Offense-side bluff axes remain live — distinguish from the dead binary-fold axis.

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: offensive imports adding NEW opponent-signal gating AND river value-sizing structural changes.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp virtually never fire in 70-hand HU; widen_threshold must target ≥15pp bands. (The preflop tightness gate is hand-classification based, not pot-odds-tuning, so this does not block it.)
- **Firing verification:** reachability_test (code-reachability proxy) + ≥100g H2H WR-lift is PRIMARY; stderr is now readable (A1 background-drain) but secondary/noisy — supporting signal, not a gate.

## GENERAL
- **✅ RESOLVED (A1):** `battle.py` now drains stderr in a background thread — telemetry verification unblocked. Readable but secondary/noisy; do NOT re-flag stderr as unreadable.
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover (crossover-as-default retired: 9 consecutive gens v163→v171 all master-from-X). BUT reliability ≠ correctness: the preflop axis v172/v173 produced near-inert carve-outs WITHOUT closing the -20k leak — validate axis PAYOFF (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — anti-lock bypass guard removal (v167) is a logic fix with higher EV per line than margin tweaks.
- **Validation thresholds:** <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Do NOT reverse a prior gen's master-planned direction on sub-30g noise.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Critic advisory ≤4.0 with `local_optima_warning=true` on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.

## RECENT_LESSONS
- **v174**: Critic evidence: H2H weaknesses: v173 vs v169 (River Re-Raise Tighten): WR=0.200 (2W-6L-2D, 10g) — weakest matchup, directly a river-re-raise opponent, v173 vs v165 (River Value Raise vs Calling Stations): WR=0.300 (3W-7L, 10g) — river value opponent, v173 vs v152, v154: WR=0.400 each (10g) — postflop-focused opponents, Pattern: all 4 weakest opponents are postflop/river-aggression bots, confirming the postflop over-calling leak (not preflop); Experience pool refs: POSTFLOP_STRATEGY: 'v174 (CRITICAL PIVOT SIGNAL): v173's dominant exploitable leak is POSTFLOP over-calling, NOT preflop' — directly motivates this gate, POSTFLOP_STRATEGY: 'STACK-OFF GUARD PLACEMENT INVARIANT: guards nested inside to_call>=my_chips reached ZERO — relocate to to_call>0' — v174 applies this principle to _spr_commitment_gate, POSTFLOP_STRATEGY: 'FOLD-SIDE RULE: relocating existing guards (v162/v174 placement-fix) is allowed (placement-fix ≠ new gate)' — this is a permitted placement-fix of an existing EV-grounded gate; Diff refs: strategy.py L1235-1241: NEW dispatch `if round_idx == 3 and made_strength < 0.45 and _spr_commitment_gate(...): return -1` inside the `if to_call > 0:` block, placed BEFORE _river_stackoff_guard (L1247), strategy_helpers.py L1362-1405: _spr_commitment_gate body unchanged — internally checks made<0.55, exempts strong/nut tier, protects draws (river: value_heavy+draw≥0.18), fold iff win_rate < pot_odds+0.03, Net change: 12 lines added to strategy.py (now 2012/2000 — OVER cap by 12 lines, see feedback)
- **v174 (CRITICAL PIVOT SIGNAL):** v173's dominant exploitable leak is POSTFLOP over-calling, NOT preflop. H2H: v173 vs v169 ('River Re-Raise Tighten') 0.20 WR (2W-6L-2D), vs v165 ('River Value Raise vs Calling Stations') 0.30 (3W-7L), vs v152/v154 0.40 each — pattern of under-folding to postflop value/aggression. Applied the SPR commitment-gate placement-fix (strategy.py L1230-1239 inside `if to_call>0:`, round_idx≥1, BEFORE `_river_stackoff_guard` L1244) per the v162 invariant. **NEXT:** address postflop over-calling via a PERMITTED continuous river call-margin TIGHTEN (re-open the mis-labeled 'exhausted' axis), NOT more preflop carve-outs.
- **v173 CAP EMERGENCY:** strategy.py EXACTLY 2000/2000 (zero headroom). The next Architect editing strategy.py WILL trip MAX_LINES_PER_FILE. Future Master plans MUST either (a) target only helpers/opponent.py, or (b) budget a refactor/extract-to-helper step BEFORE adding new strategy.py code. BINDING.
- **v173 carve-out tuning:** VPIP≥0.65 is too strict for the mirror pool. Next: tighten to VPIP≥0.70 (cleaner 'ultra-loose') OR add an equity-sufficient sub-range — do NOT just relax VPIP without a behavioral basis.
- **v173 PRIORITY = VERIFY not evolve:** daemon ≥30g grep PREFLOP_OFFSUIT_GATE@bb_vs_raise to confirm v172 base fires ≥5% (regression check), AND grep carve-out=active to quantify A2o-A5o defense vs pool. If the -20k preflop leak (59o/39o trash commit at SOURCE per v163/v171) has NOT shrunk by ≥100g, widen gate thresholds (A2o-A8o band, K9o/Q9o inclusions), not more carve-outs.
- **v172→v173 (DONE):** Built `_is_offsuit_commitment_risk()` PREFLOP gate (offsuit non-pair A2o-A8o/K9o/Q9o/59o/39o/4To → fold) + v173 A2o-A5o carve-out vs ultra-loose. Targets the -20k stack-off leak originating PREFLOP (v163 replay: 59o/39o trash commits) — distinct from the v174 postflop over-calling leak.

