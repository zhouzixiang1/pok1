## OPPONENT_MODELING
- archetype axis (classify_archetype) CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor, prefer deal-local history. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `_opp_betsize_polarity` (v193, n≥4) OPERATIONAL: n_shove COUNT via True flag, NOT ratio=2.0 (ratio feeds only unread avg_fraction). Deal-local SIGNAL feeding fold-calibration.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify read site before treating the raw-warning as live.
- 4bet trigger-inert PRINCIPLE (v203→v206): a path-reachable dispatch site can still NEVER fire if the TRIGGER is rare — always reachability-test the TRIGGER firing rate, not just the path. NOTE: the actionable proxy (opp_pf_raises>=1 / raise-SIZE>15BB) was LANDED in v206 as raise-SIZE>=12BB, closing it; only the principle remains valid.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative, UPDATED v205): pair≈0.22, two-pair≈0.40 [BOUNDARY/foldable post-v205, floor→0.45], trips≈0.58. pot_odds-vs-raw-made_strength STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 PRIORITY — MULTI-SITE -20k/0%-fold leak (v196→v206, ~10 gens RESILIENT)**: prior fold-side attacks were TRIGGER-INERT (blanket 'strong','nut' exemption SHIELDED TPTK). v206 LANDED the FIRST preflop-half attack: raise-SIZE>=12BB trigger on `_preflop_shove_defense_fold` (state.py) bypasses the inert opp_pf_raises>=2 trigger — yet v206 STILL produced -23165 vs v193 from the POSTFLOP sibling site (turn all-in round_idx==2/3), proving the leak is ≥2-site. ⚠ RESOLVED CONTRADICTION: reachability-test @≥30g is the BINDING gate for ANY further threshold movement; v204/v205 SKIPPED it (moved two-pair 0.35→0.45 anyway) = local-optima risk future Masters MUST NOT repeat. IMMEDIATE NEXT: (1) reachability-test v206's 12BB trigger fires ≥5% @≥30g vs v193/v201 before 12BB→15BB or adding the VPIP>0.60 small-pair continue gate; (2) add `_turn_allin_nodraw_fold_gate` @ strategy.py round_idx==2 opponent_allin block to close the postflop half exposed by the -23165. Do NOT inch the made_strength floor. [single-site & per-site-sequential fold approaches: STALE — no WR-lift]
- Birth mandate (RECURRING 8 gens; v204/v206 FIRST COMPLIANT: pair_profile param + multiple dispatch sites): wire primitives with ≥3 LIVE dispatch sites AT BIRTH + reachability/H2H-WR @≥30g. Verify the TRIGGER CONDITION itself fires (a dead trigger makes a 2-site dispatch inert); self-test logic proofs do NOT count (v202 self-test was dead code).
- CAP CONSTRAINTS (NOTE: snapshot pre-v206-revalidation): strategy.py≈2476/2500 (~24-line headroom, NO forced reclamation per edit), opponent.py≈1650/2500, strategy_helpers.py=2500/2500 EXACT CAP (reclamation HARD PREREQ for helpers edits only). v206 edited state.py — RE-MEASURE all LOC before any helper edit relies on these figures. Comment-condensation must NOT touch behavioral code.
- `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` STILL DORMANT — wiring the ORIGINAL 4bet-NON-allin param remains open; verify the graduated gate fires @≥30g first.
- Preflop shove-defense over-fold risk (v201): treating all preflop shoves as premiums-only is exploitable vs wide/open-shovers (VPIP>0.60) — folding 55 at pot_odds~0.50 is -EV. Gate marginal/small-pair fold on pfr/vpip once n≥30 H2H.
- Offense value-sizing-UP (v198/v199): previously claimed FIRING, but proof bot v159 (+26351 lift) was CULLED to graveyard after v203 → figure UNVERIFIABLE. Treat as UNPROVEN, not active-firing; do NOT re-anchor calibrations to culled bots. [POSSIBLY EXHAUSTED]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Board-texture bluff raise (v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — each action_type discriminator (raise/allin) must live in its OWN branch (nesting 'allin' inside 'raise' made shove_rate 0 for 5 gens with NO test failure; fixed v194); always reachability-test the DOWNSTREAM consumer after any recording-site change. Plateau (WR~0.50): pursue a NEW structural axis.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed/no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST inventory+port sibling-lineage critical mutations (esp. fold-side/leak-attack) when branching_from is set (RECURRING: v197, v199 both regressed v196's fold).
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- Precommit silent 2-attempt retry: a first 'FAILED: match_timeout' (n=8 hitting 960s mirror limit) auto-falls-back to n=4 and can PASS — distinguish timeout-failure from data-driven-failure; the leak manifests in chip-distribution (stack-offs), not pure win-rate.
- Evaluate polarized-aggression fixes by net-chips/blowout-frequency, NOT W-L (v204: 5W-3L yet net -25071 vs v193 = razor-thin wins masking one blowout).
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v207**: Critic evidence: H2H weaknesses: v206 vs v186: WR=0.30 n=10 (calling-station archetype — confirmed leak), v206 vs v195: WR=0.30 n=10 (calling-station archetype — confirmed leak), v206 vs v167: WR=0.40 n=10, v206 vs v198: WR=0.40 n=10 — v198 HAD the fix v207 restores; v198 (fixed) beats v206 (unfixed), rating 1663.8 vs 1643.7, v206 vs v174/v190/v196/v198: all WR=0.40 — clustered calling-station losses; Experience pool refs: POSTFLOP_STRATEGY §15: 'Offense value-sizing-UP (v198/v199): previously claimed FIRING, but proof bot v159 (+26351 lift) was CULLED to graveyard after v203 → figure UNVERIFIABLE. Treat as UNPROVEN. [POSSIBLY EXHAUSTED]' — Master correctly distinguishes LOST-MECHANISM (git-confirmed absent from v194/v190 crossover parents) from re-anchoring to a culled calibration; the [POSSIBLY EXHAUSTED] tag was based on unverifiable-FIRING, not on the fix being wrong., DIRECTION_AUDIT: fold-gate axis FORBIDDEN (3 listed exhausted directions); v207's offense/sizing axis is one of the three MANDATED pivots (option a: 'PREFLOP HAND-SELECTION / POT-CONTROL... apply smaller sizing on marginal-made streets so the bot reaches fewer low-SPR river all-in spots' — this change is the inverse-opposite: LARGER sizing on value hands)., GENERAL §28: 'Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.' — v207 has NOT yet passed precommit; the 8 is conditional on ≥30g paired net-chips CI lifting the v186/v195 WR.; Diff refs: strategy.py L1117-1124 NEW _station_delta_with_value_skip wrapper: `if delta < 0.0 and (made_strength >= 0.55 or value_profile.tier in ('strong','nut')): return 0.0` — semantically identical to v198's inline skip clause (verified via git show bot-v198:strategy_helpers.py L1176-1184), strategy.py L2236 (donk), L2289 (probe), L2408 (main choose_raise): 3 call sites replaced — covers ALL postflop value-raise paths so the fix can fire on every street/line, strategy_helpers.py UNCHANGED at 2500/2500 LOC cap — wrapping at call sites is the correct architecture given the LOC constraint
- **v206 (preflop raise-SIZE>=12BB trigger)**: LANDED the FIRST preflop-half attack on the multi-site leak — converted `_preflop_shove_defense_fold` trigger from raise-COUNT (opp_pf_raises>=2, inert) to raise-SIZE (>=12BB), making 2 dispatch sites (bb_vs_raise, sb_vs_reraise) reachable. Beat parent yet STILL produced -23165 vs v193 from a POSTFLOP all-in site → leak is ≥2-site, single-axis fix leaves a blowout path. NEXT: reachability-test 12BB trigger fires ≥5% @≥30g vs v193/v201; add `_turn_allin_nodraw_fold_gate` @ round_idx==2 to close the postflop half; VPIP>0.60 small-pair continue gate unaddressed.
- **v206 (Master-audit validation)**: rejection of try-1 CONFIRMS parametric fold-margin nudges (discount/target/clamp tightening) are DISPROVEN-INERT (v184/v188/v189/v190) — sanctioned fold-side lever is now STRUCTURAL (new dispatch-site / trigger / function), NOT constant-tuning.
- **v205 (two-pair fold-gate graduation 0.35→0.45)**: SKIPPED reachability_test = textbook local-optima trap; two-pair (made=0.40) is now the boundary class (cal_eq∈[0.20,0.32]<<jam_odds≈0.48 justifies fold vs POLARIZED but marginal vs STANDARD). precommit n8 ZERO -20k (worst -14767) = first encouraging signal. strategy.py reclaimed 2499→2476.
- **v204 (graduated tier-exemption, FIRST genuine fold-side test)**: `_allin_polarized_equity_fold` + `_river_potodds_equity_margin` graduated via pair_profile param + 3 dispatch sites. Critic predicted OVER-fold; precommit showed OPPOSITE (continued stack-offs) → weight precommit net-chip over formula theory; do NOT add critic's TPTK floor (re-creates leak).

