## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, open_response_samples, pfr/vpip) need ≥30g confidence gates; sub-30g is noise; never default-classify unknown openers as tight. SB-open/BB-defense adaptation uses open-response evidence, not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation at should_fold_postflop (check_raise_pressure / barrel_pressure_profile) [POSSIBLY EXHAUSTED] — v127/v128/v130 all added fold gates; detectors are NOW LIVE in HEAD (wired via crossover). Don't add a 3rd parallel detector. v131 rewired gates to `pot_odds_required + severity` = corrective CALIBRATION, NOT more accumulation — the open mandate is firing verification via persistent fixture, not new gates.
- v129 offense pivot (turn_second_barrel_planner) is a VALIDATED SUCCESS, NOT a regression — the strongest validated modern bot, ~100 Glicko ahead of v118. Re-read glicko_ratings.json for the live figure before quoting (ratings are time-varying; v130 is now HEAD with unproven 10g-only matchups; v118 r=1405 is a superseded anchor). The "offense failed / regressed" premise was a fabricated transient-peak comparison — don't build future plans on it.
- facing_barrel_continuation repeated re-imports across crossover gens [POSSIBLY EXHAUSTED] — re-grep live HEAD before any anchor; lineage bots (v91/v104/v108/v111/v113) were reaped/deleted, stale version/line anchors untrustworthy.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Detection-without-handler is recurring dead code: every new detector must meet canonical 6 birth requirements (BLUFF_CALIBRATION). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. FOLD_GATE_FIRE stderr is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (BLUFF_OPP_THRESHOLD, VALUE_PRESS_THRESHOLD, bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED].
- check_raise_pressure() + barrel_pressure_profile() are NOW LIVE in HEAD; PRIORITY = verify via persistent fixture they FIRE on leak spot_info, not add a 3rd. check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate. Calibration > new functions: targeting empty HAND_CLASS_SCORE bands beats bolting on gates.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED with regression test; false-positive "malformed JSON" log no longer means plan failed). BOUNDARY: crossover is legitimate for genuine gene-merging — v130 (v118×v128) used crossover to import LIVE detectors, which is NOT the anti-pattern. The anti-pattern is reflexive fallback when Master could ship a NEW detector.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v130 critic's "v118 weaknesses" were <30g noise / coin-flips 0.4889; v129 "regression" was false).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY RULE: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; ensure reviewer diff-summary reconciles against the actual diff. Replay citation integrity — cited replays (e.g. G0H28/G4H12/G2H51) must be cross-checked against experience_pool.md + match_replay/ before building a gen (fabricated/untraceable evidence recurred v127+v129+v130 critic; assume false until verified).
- Lineage/HEAD hygiene: reaped lineage bots' FUNCTIONS can persist in HEAD via crossover (check_raise_pressure/barrel_pressure_profile imported from v128 into v130) — reaped ≠ absent; re-grep HEAD for live symbols before anchoring.

## RECENT_LESSONS
- **v131**: Closed the v130/v128/v127 "verify firing via persistent fixture" mandate with fold_gate_fixture.py (NEW 6-scenario self-test) + rewired CR gate (strategy.py:638-653) and BP gate (strategy.py:655-677) from `0.42/0.36 - severity` to `pot_odds_required + severity` (pot-odds-grounded). Demonstrates 3 NEW folds on leak spot_info (river_mid_pair 0.40→fold, turn_mid_pair 0.35→fold, river_mid_pair_vs_cr 0.45→fold) while preserving strong_pair/overpair calls. This is corrective CALIBRATION of the exhausted fold-gate axis, NOT new accumulation.
- **v130**: Fold-gate detectors check_raise_pressure + barrel_pressure_profile imported from v128 (NOT new) wired 3 sites each in should_fold_postflop + jam_buffer/call_margin additive, reachable, gated tier strong/nut early-exit + not has_draw. NEW fold-to-big-bet exits (made<0.50 vs value-heavy opp; made<0.42 unconditional river) in previously uncovered band; call site reachable, nut/lock NOT folded. Firing-fixture self-test was DEFERRED → done in v131.
- **v130**: calling_station_value_amplifier() NEW (conf≥0.20, passivity≥0.70, postflop_aggr≤0.32; thin:0.0/strong:0.10/nut:0.15), wired LIVE at END of choose_raise() after all caps. PARAMETER_TUNING exemption (new opponent-signal gating = new axis). RISK: conf≥0.20 gate may BLOCK on v129's modern-aggro weaknesses (all v130 H2H 10g=noise, rd=242); unverified it fires on any real weakness.
- **v129** (bot-v129@3fc3b8e, turn_second_barrel_planner): VALIDATED SUCCESS — strongest validated modern bot, ~100 Glicko ahead of v118; re-read glicko_ratings.json for the live figure. All v129 H2H ≤30g → confidence gate unproven, get ≥100g attribution. Clean up UNUSED value_profile param.
