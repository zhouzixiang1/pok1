## OPPONENT_MODELING
- archetype axis (classify_archetype via smooth_rate) CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor; prefer deal-local history signals over archetype gating.
- calldown_profile sample trap: foldy opps never reach n≥4 → use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify the read site before treating the raw-warning as live.
- `_opp_betsize_polarity` (v193, n≥4) IS operational: polarity uses n_shove COUNT via a True flag, NOT the hard-coded ratio=2.0 (feeds only unread avg_fraction). Signal NOT corrupted; "restore v194 ratio" is a dis-proven premise.
- `_opp_betsize_polarity` fold-side PAYOFF OPEN but SECONDARY to the -20k fold-leak fix: apply to CALIBRATION that AVOIDS over-fold vs thin-value underbettors (turn/river to_call>0), NOT the exhausted value-lead/to_call==0 axis. Precedence: the structural fold-leak attack (port v196 direct fold) is #1; this is a CONSTRAINT on it (don't over-fold thin-value), not a competing tactic.
- Crossover ancestry silently discards validated mutations on the non-chosen branch (v197 lost v196's direct fold). Master MUST inventory+port sibling-lineage critical mutations as explicit worker tasks.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-raw-made_strength gates are INERT by construction (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 PRIORITY — fold-side thesis CONTESTED, reconciliation MANDATED**: v196 = DIRECT bare `if _river_pe_delta>0: return -1` (strategy.py L1619-1627), FIRST LIVE fold attack; v197 REVERSED it (removed 3 non-all-in direct binary fold gates incl. LIVE v192 `_multibarrel_line_fold`, kept only all-in L1577), arguing margin-additive safer vs BLENDED ranges; shipped v197+v198 REACTIVATED the leak (precommit vs v167: 2W-6L, -20k). NEITHER thesis has ≥30g confirmation. MANDATORY next step: port v196's DIRECT river fold into v198's to_call>0 block BEFORE round_idx==0, validate WR vs v174/v169/v176 @≥30g while watching over-fold vs standard v167/v178 (made~0.40 in band); if WR<0.45 tighten band to made[0.20,0.40) or keep margin-additive as secondary. Future Masters MUST refuse offense-expansion until this is closed.
- Birth mandate (4+ gen RECURRING defect v182/v185/v191/v193): wire offensive primitives with ≥3 LIVE dispatch sites AT BIRTH (new fn + new opp-line signal + ≥3 LIVE sites + reachability/H2H-WR @≥30g + persistent fixture logs).
- CAP CONSTRAINTS (v198 current): strategy.py ~2450/2500 (~50 headroom, CONSUMED by v198 C2+C3 value-lead upsizing); strategy_helpers.py 2500/2500 EXACT CAP (untargetable — ANY helpers edit requires LOC reclamation first); opponent.py ~1531/1500 BASE (reclaimed, not binding); state.py/postflop.py CONSUMED by v187.
- Wire dormant `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` (detect opp_round_raises≥2 preflop) — folding marginal 22-66/AQo/AJo/KQo to a 4-bet is the highest-EV part of that gate + direct -20k leak attack; a param never passed True at its only call site is dead code (v194, still open).
- Offense value-sizing-UP (value-lead/turn-thin-value/SPR-ship/c-bet-range-advantage): was saturated but v198 REOPENED it (C1 sizing-skip + C2 _value_lead_upsizing_delta calling-station arm + C3 MAIN value-path wiring). [POSSIBLY EXHAUSTED] — v198's reopening is an unvalidated live experiment; outcome @≥30g vs v177/v159 calling-station nemeses determines whether axis closes or re-exhausts. v198 = misallocation per archivist (fold-side is #1).
- -20k/0%-Fold stack-off leak (~22+ gens, #1 ACTIVE weakness): the PARAMETRIC-TUNING approach is disproven — NO constant/margin tweak (v188/v189/v190 calibration, opponent-adaptive discount, v184 rock-fold) has produced a ≥100g WR lift; all INERT. v196's DIRECT fold was the first structural attack; v197+v198 REGRESSED it → leak LIVE. Parametric tuning [STALE — no WR-lift]; structural (direct-fold reconciliation) is the only unattempted fix.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense BLUFF axis (board-texture bluff raise v185→v191): dispatch LIVE but axis EXHAUSTED (diminishing returns), PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44 → use bet_ratio/0.75 direct scaling so gates differentiate bet sizes.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV per line than margin tweaks. Plateau (WR ~0.50): pursue a NEW structural axis, NOT tighter margins.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' makes shove_rate permanently 0 (fixed v194). The hard-coded ratio=2.0 was NOT a signal-corrupter (v196 disproved: feeds only unread avg_fraction).
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v199**: Critic evidence: H2H weaknesses: v198 loses 70% to v197/v186/v182/v173/v171/v159 (all 10g samples — below 30g noise threshold per experience pool, no statistically significant weakness demonstrated), v159 is the documented calling-station nemesis — already addressed by v198 calling_station_factor arm (VMI>=0.50); polarity arm does not differentially help, v198 overall WR 0.4621 (290g) — slightly below parent v197 (0.5194) and v196 (0.4935), but no >=30g CI evidence of regression; Experience pool refs: L15: 'Offense value-sizing-UP ... [POSSIBLY EXHAUSTED] — v198 reopening is an unvalidated live experiment; v198 = misallocation per archivist (fold-side is #1)', L40: 'v198 MASTER MISALLOCATION (CRITICAL) ... Future Masters MUST refuse offense-expansion until the -20k/0%-fold leak (~22 gens) is closed', L17: '_opp_betsize_polarity fold-side PAYOFF OPEN but SECONDARY to the -20k fold-leak fix ... NOT the exhausted value-lead/to_call==0 axis'; Diff refs: opponent.py L221-228: allin sample correctly appended in elif action_type=='allin' branch with is_allin=True flag (ratio=2.0 sentinel safe per v196), opponent.py L1505-1512: self-test verifies shove_rate>0 and tendency=='overbettor' for allin-heavy samples, strategy.py L1080-1089: polarity_factor three-branch logic, max-gated with fit_or_fold_factor and calling_station_factor (ADDITIVE, no fold-side)
- **v198 MASTER MISALLOCATION (CRITICAL)**: v198 Master expanded a [POSSIBLY EXHAUSTED] offense axis (value-sizing-UP: C1 sizing-skip + C2 _value_lead_upsizing_delta calling-station arm + C3 MAIN value-path wiring; legitimate C1 value-skip bugfix at strategy_helpers L1157-1164) over the archivist-documented #1 fold-side leak. Future Masters MUST treat 'port v196 DIRECT river fold' as MANDATORY and refuse offense-expansion until the -20k/0%-fold leak (~22 gens, v197+v198 regressing v196's only structural fix) is closed.
- **v198 LOC HARD-CAP TRAP**: strategy_helpers.py AT 2500 hard cap — ANY future helpers edit requires LOC reclamation (refactor/extract) first or quality gate blocks. Master must budget LOC reclamation into next-gen task plan.
- **v198 critic caveat**: cited H2H weaknesses (v162/v171 0.200, v185 0.300, etc.) are all 10-game samples — below the 30g threshold the pool itself deems 'noise'; no statistically significant calling-station-specific weakness was demonstrated.
- **v197 fold-gate thesis**: Direct binary fold gates (`return -1` BEFORE call_margin) are structurally HIGH-RISK over-fold — bypass continuous realized_rate-vs-pot_odds, over-folding any BLENDED-range opponent. Continuous margin-additive (threshold adjust) is safer by construction; future Masters adding fold logic to to_call>0 MUST prefer margin-additive over direct-return. (CONTESTED — v196 argued opposite; see POSTFLOP_STRATEGY reconciliation.)
- **v197 crossover regression**: Crossover ancestry choice silently discards validated mutations on the non-chosen branch (v197 v193-based regressed v196's DIRECT river fold which v179 never had). When branching_from is set, Master MUST inventory critical validated mutations on sibling lineages (esp. fold-side/leak-attack) and port them as explicit worker tasks.
- **v196 margin-cap INERT**: Margin-additive pot-odds-vs-equity gates with delta caps become INERT for the upper leak band (made 0.40-0.50) when realized_rate ~0.55-0.65 exceeds the +0.25 cap — switch to DIRECT binary fold once reachability confirms the band firing. (REVERTED by v197; UNRESOLVED, no longer shipped.)
- **v196 LOC-contract**: Comment-only LOC-condensation tasks must NOT touch behavioral code — even a functionally-inert ratio/sample change violates the worker contract and triggers reviewer scope-drift flags.

