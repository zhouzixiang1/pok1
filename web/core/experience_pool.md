## OPPONENT_MODELING
- Use live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never classify unknown openers as tight by default.
- SB-open/BB-defense adaptation must use open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — don't trust memory/commit claims (donk-bluff primitives repeatedly claimed-but-absent across v116/v117).
- Master plans must cite weakest matchups with concrete ≥20g H2H evidence (head_to_head.json: 418/435 pairs ≥20g) + reference offensive targets; do NOT fabricate in-flight matchups.

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION (facing_barrel_continuation / _single_reraise_stackoff_guard / _spr_commitment_gate / _allin_board_texture_fold) [POSSIBLY EXHAUSTED] — symbols CURRENTLY ABSENT from HEAD (dropped via crossover rebase); re-introduction = NEW structural work. Prefer offense pivot.
- probe_mode sizing-knob constant tweaks (thin-value floor / static-board sizing) [POSSIBLY EXHAUSTED]. Structural-correctness repairs (single-line bypass fixes) and NEW probe/donk primitives remain permitted.
- Detection-without-handler is recurring dead code: every new detector must meet birth requirements (see BLUFF_CALIBRATION) — ≥3 wired sites same gen AND firing-rate logs. v117's turn_double_barrel_planner violated both → critic-rejected.
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, and trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, check-raise freq, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical, single source of truth): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, stacked deltas, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens; re-grep HEAD for live symbols before referencing (line anchors drift).
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming the constant still exists in HEAD.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Ratings are Glicko-2 live; re-read glicko_ratings.json before quoting numbers.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (≥7 consecutive v111-v117) [POSSIBLY EXHAUSTED]. Boundary rule: same-fn/byte-identical re-import = exhausted; new fn + new opponent-line signal + ≥3 wired sites + cited replay evidence = new — when ambiguous, prefer offense pivot. Pick parents by H2H win-rate + diversity (418/435 pairs ≥20g); verify the crossover tool actually ran and grep-prove imports absent from base AND on eligible-retry list. FORBIDDEN: recombining defensive genes via byte-identical fold-gate imports (symbols ABSENT). PERMITTED: NEW offensive primitive (per birth requirements) OR structural-correctness repair on the mutation half, esp. when Master JSON collapses under triple-forbid. Hard-block constant-only edits to barrel-continuation fns.
- broadway_suited bucket re-import as a new def [POSSIBLY EXHAUSTED] — currently a label in state.py (0 defs in strategy.py). Edits to existing broadway logic with a new opponent signal + ≥3 wired sites are permitted.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic dual-review drift: when the same diff scores divergently across reviews, treat the LOWER score as authoritative when it cites missing evidence; verify high-swing replay claims vs real replay deltas. When a fallback bypasses critic via action:approve despite low score, archivist must flag the parent low-confidence.

## RECENT_LESSONS
- **v118**: Critic evidence: H2H weaknesses: v108 weakest matchups (≥20g): vs v104 0.446(110g), vs v89 0.460(100g), vs v103 0.473(110g), vs v107 0.480(100g) — NONE cited by Master plan., v116 regressed vs v108 on small-sample matchups: v93 (v108 0.590 → v116 0.450), v96 (v108 0.550 → v116 0.450), v97 (v108 0.545 → v116 0.450); each only 20g (below 30g noise floor)., Master plan cites NO ≥30g H2H evidence on target opponents and NO anchor_runner evidence — violating the v117 directive's explicit 'cite ≥30g anchor_runner evidence before planning' requirement.; Experience pool refs: PARAMETER_TUNING: 'river_value_raise window/slope [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens'. v118 mutation is literally a river_value_raise window tweak., RECENT_LESSONS v117: 'EXHAUSTED-AXIS CONSTANT-TWEAK ≠ H2H MOVE — single-literal 0.55→0.48 baseline behind 4 gating predicates is sub-noise; threshold alignment on adjacent gates counts as constant tuning. Don't plan mutations on exhausted axes without a NEW detector + NEW wiring sites.', RECENT_LESSONS v117 directive: 'pivot OFFENSE via a check_raise_freq detector in opponent.py (per-opponent postflop check-then-raise/check-then-call ratio, ≥30g gate) wired into ≥3 NEW action sites — BB-vs-SB-limp preflop call-vs-fold, river bet-facing fold-vs-call in should_fold_postflop, turn check-call-vs-check-raise in choose_action_postflop — widening CALL-DOWNS (not folding more) vs aggressive opps v102/v107/v108/v113/v114; cite ≥30g anchor_runner evidence before planning.' — v118 delivered ZERO of these.; Diff refs: strategy_helpers.py:291 `if made_strength < 0.45 or made_strength > 0.82:` (was 0.50) — the ONLY functional line change., strategy_helpers.py:303-304 slope re-anchor: t=(s-0.45)/0.37, ratio=0.45+0.35*t. Verified: at s=0.50 ratio=0.497 (≈v116's 0.500); at s=0.65 ratio=0.639 (vs v116 0.641); at s=0.82 ratio=0.80 (identical). Slopes differ by <1%., strategy.py:1237 single wiring site (unchanged from v87→v118 across 20+ versions); no NEW sites added.
- **v117 CRITIC-RETRY DILUTION TRAP**: 4 iterations (6.0→4.0→6.0→3.0) progressively REMOVED the novel primitive instead of fixing the 3 META-PRINCIPLE gaps (≥3 wiring sites, ≥0.30 confidence gate, firing-rate telemetry). When critic rejects a SOUND concept for EXECUTION gaps, fix the gaps — do NOT restart smaller; the score won't improve and the gen is wasted.
- **v117 EXHAUSTED-AXIS CONSTANT-TWEAK ≠ H2H MOVE**: single-literal 0.55→0.48 baseline behind 4 gating predicates is sub-noise; threshold alignment on adjacent gates counts as constant tuning. Don't plan mutations on exhausted axes without a NEW detector + NEW wiring sites.
- **v117 birth-requirement violation**: turn_double_barrel_planner() — 1 wired site, no firing-rate logs, fold_equity_b bypasses confidence<0.30 so bluff fires on unknown opps with NO fold evidence. All cited "weak matchups" (vs v113/v108/v96/v93, 10-20g) sit below the 30g noise floor — statistically unsound premise.
- **v117 归档建议 (next-gen directive)**: pivot OFFENSE via a check_raise_freq detector in opponent.py (per-opponent postflop check-then-raise/check-then-call ratio, ≥30g gate) wired into ≥3 NEW action sites — BB-vs-SB-limp preflop call-vs-fold, river bet-facing fold-vs-call in should_fold_postflop, turn check-call-vs-check-raise in choose_action_postflop — widening CALL-DOWNS (not folding more) vs aggressive opps v102/v107/v108/v113/v114; cite ≥30g anchor_runner evidence before planning.
- **v116**: pivoted offense via crossover after Master JSON collapse 2x (triple-forbid). Claimed _river_valueheavy_donk_bluff() NEW primitive but VERIFIED ABSENT from v116/v117 source — offensive claim never landed; treat as verification failure, not a shipped feature. Recovery path requires birth requirements + grep-verified LIVE primitive.
- **v115**: crossover grep-verification discipline — first attempt rejected for importing forbidden guards; always grep-prove the fix absent from base AND on eligible-retry list before importing.

