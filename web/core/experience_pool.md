## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (they drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). Gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic.
- Detector firing verification: grep symbols in bot_telemetry/{bot}.jsonl (A1 unlocked stderr capture v146+). **CRITICAL: grep token>0 ≠ LIVE — SIZING_MARGIN_ADJ fires 42k× but 99.96% delta_adj=+0. MUST grep `delta_adj!=0` / actual param values, not token presence.** reachability_test.py proves CODE-PATH execution only (force-fed inputs), NOT in-game firing; never conflate the two. Verdict: 0 fires = PENDING-VERIFICATION.

## POSTFLOP_STRATEGY
- **`postflop_call_margin` — TELEMETRY ARTIFACT CORRECTED (v154 A1-verified, supersedes prior "99.96% DEADZONE" verdict)**: that verdict described ONLY tendency arm B (`underbettor+small` / `overbettor+large`, compound-AND joint prob ~0.04%, near-dead) because the `SIZING_MARGIN_ADJ` telemetry prints ONLY arm B's `adj_milli`. **Standard arm A is ALREADY LIVE** (v153+ added unconditional deltas `weak_showdown`+0.020 / `air_hand`+0.028 / `facing_postflop_aggression`+0.008-0.032, added to `margin` BEFORE the tendency block, NOT clamped away by `return clamp(margin,0.0,0.08)`, consumed at `strategy.py call_margin=postflop_call_margin(...)`); v154 r=1605 (pool #2) vs v153 r=1413 confirms behavioral liveness. **LESSON: grep `delta_adj!=0` on a sub-arm-only telemetry token = FALSE INERT verdict.** Next = fix TELEMETRY (hoist `sys.stderr.write` to function scope, print TOTAL `margin_milli` + per-arm `reason=` tag, add fixture asserting telemetry==return value — see master_prompt M6), NOT add another standard arm (already done) and NOT freeze the framework in `do_not_touch`. `classify_sizing_tendency` bins 0.30-1.0x pot as 'standard' (96.9% live). **ONLY v152 PREFLOP_DEFEND_WIDEN is LIVE (33×) because it bypasses the framework (OR-gate) — but that LIVE case is MISFIRING** (vpip≥0.55 OR-gate catches passive limpers, makes -EV widen vs premium-heavy raises; fix: remove vpip≥0.55 branch, keep pfr≥0.30/conf<0.20 real steal-opener signal).
- **smooth_rate prior math unreachable**: preflop offense detectors gate on fold_open≥0.50 / ftr≥0.55 / calldown≥0.55, but priors sit at 0.42-0.44 (smooth_rate prior_mean, weight=4.0) — even a TRUE 50%-folder saturates at 0.491 (n=30). Fix: (a) lower gate to prior mean, OR (b) prior_weight 4.0→2.0, OR (c) make delta CONTINUOUS: `delta = sign*0.025*clamp((signal-prior_mean)/0.15, -1, 1)`.
- **calldown_profile sample self-defeating trap**: samples accumulate only via pending_my_pressure (our raise) + opp NEXT action; each opp FOLD ends the hand → foldy opps (the target) never reach n≥4. opponent.py populates empirical rate only at n≥4 else hard-defaults 0.5, while detectors gate on n≥3 → every n=3 case killed by the 0.5 default. v151 `_delayed_calldown_bluff` 0-fire is evidence; v153 CALLDOWN_DONK inherits this → PREDICTED INERT. Fix: populate empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.
- **Telemetry `!=0` gate = INERTNESS blind spot**: `if delta != 0: sys.stderr.write(...)` makes 0-delta silent — 9 gens undetected. MUST print unconditionally with reason tag (`reason=deadzone`/`conf_gate`/`standard_bucket`/`fired`). Validation gate: daemon≥30g → require delta!=0 in ≥5% of detector stderr lines, else INERT → block commit.
- **PLACEMENT SHADOW — CONFIRMED DEAD (v147/v148 SPR gate, A1-verified)**: guards inside `to_call>=my_chips` block are reached ZERO times (equal stacks + any legal non-allin raise leaves ≥1 chip → to_call<my_chips always). v154 relocation into opponent_allin block confirms the fix direction. **DO NOT place guards in to_call>=my_chips block — relocate to `to_call>0` (~40% of decisions) or merge into opponent_allin branch.**
- **NEW postflop fold gates on the river all-in axis MUST NOT be added** — v154 added _river_weak_made_hand_gate (6th river fold gate) against the pool's own HIGHEST-ROI directive. Fold-side axis CONFIRMED EXHAUSTED (v127-v154, 12+ gens); v149 proved bot OVER-FOLDS preflop (99% fold-to-bet). Future Masters MUST enforce the standard-arm fix before any fold-side work.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) open per axis but each exhausts. Pre-v146 + v149-v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION.
- Preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning EXHAUSTED [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; measurement anchors REAPED; revalidation requires NEW anchors. Includes check_raise_freq = DEAD directive (undelivered 14+ gens v122→v151). Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).
- Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; future widen_threshold must target ≥15pp bands minimum.

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v157**: Critic evidence: H2H weaknesses: v156 WR=0.512 (240g, mid-pack). Weakest matchups: v148/v153 at 0.3 (10g noise), v137/v151/v141/v138/v130 at 0.4 (10g noise). All sub-30g per pool threshold = noise. No specific opponent with >=30g evidence supports this axis — change targets the generic 0%-fold leak, not a specific matchup.; Experience pool refs: POSTFLOP_STRATEGY line 13 ('PLACEMENT SHADOW — CONFIRMED DEAD'): 'guards inside to_call>=my_chips block are reached ZERO times'. v157 is placed in to_call>0 block (~40% of decisions), correct placement., POSTFLOP_STRATEGY ('NEW postflop fold gates on the river all-in axis MUST NOT be added — Fold-side axis CONFIRMED EXHAUSTED v127-v154, 12+ gens'): v157 bypasses this directive by using a margin ADDITIVE on the EV path, not a binary gate. Structural distinction is valid., POSTFLOP_STRATEGY ('M5/M6 INERTNESS rules'): v157 implements continuous delta (no thresholded elif) + function-scope unconditional telemetry with reason tag. Compliant.; Diff refs: strategy_helpers.py L164-225: NEW _multi_street_calldown_tax(), 6 gates (round_idx, value tier, made_strength, draw_strength, prior_bets, band), continuous delta formula `street_base * aggression_mult * (0.5 + 0.5 * band_dist)`, capped at 0.075., strategy_helpers.py L1972-2058: Persistent __main__ fixture with 4 asserts (tax>0 standard case, tax==0 strong/nut, tax==0 flop, tax==0 no-prior)., strategy.py L1228-1230: Wire site inside postflop call_margin computation, BEFORE realized_rate check at L1315. Correct LIVE consumer path.
- **v156**: INERTNESS root cause SINGLE point of failure: smooth_rate prior_weight=4.0 saturated empirical signals below their firing thresholds across 9 generations (v144-v152) — reducing to 2.0 unblocks ALL existing detectors without adding new ones. Future Masters facing 'detector fires but doesn't adjust' symptoms should tune prior_weight BEFORE adding new detectors or relaxing per-detector thresholds.
- **v156**: Telemetry scope invariant: block-scoped sys.stderr.write tokens (e.g., prior SIZING_MARGIN_ADJ inside nested if) are functionally invisible to daemon grep — must be hoisted to function scope on the unconditional path. v156's CALL_MARGIN_FINAL at function scope is the correct pattern; the M6 'telemetry != 0 blind spot' loop should now close.
- **v156 归档建议**: Daemon validation gating decision: grep CALL_MARGIN_FINAL margin_milli!=0 reason=standard_arm after daemon>=30g — if <5%, increase the standard-arm deltas themselves (weak_showdown+0.020 / air_hand+0.028 / facing_postflop_aggression+0.008-0.032) rather than adding a 4th preflop opponent-sizing variant (axis [POSSIBLY EXHAUSTED] per pool); separately, v144/v145/V146/v150/v151 detectors still lack per-hand prior_aggressor_checked_back flags — if daemon>=100g vs v130/v139 shows no WR lift, add the per-hand flag, NOT another population-mean detector.
- **v156**: Critic evidence: H2H weaknesses: v155 weakest: v135 0.8 (10g noise), v79 0.2 (10g noise), v152 0.35 (20g), v151 0.35 (20g), v145 0.3 (10g noise). No matchup has >=30g evidence per pool threshold; v152/v151 at 20g are the closest to significance. Change does not target a specific opponent but unlocks the prior offense detectors calibrated against these matchups.; Experience pool refs: OPPONENT_MODELING line 8 (smooth_rate prior math unreachable): 'prior_mean, weight=4.0 means even a TRUE 50%-folder saturates at 0.491'. Pool explicitly lists three fix options: (a) lower gate to prior mean, (b) prior_weight 4.0→2.0, (c) continuous delta. v156 implements (b)., POSTFLOP_STRATEGY line 11 (telemetry != 0 gate = INERTNESS blind spot): 'MUST print unconditionally with reason tag'. v156 implements exactly this with reason=standard_arm/no_margin., POSTFLOP_STRATEGY line 4 (v152 PREFLOP_DEFEND_WIDEN LIVE-but-MISFIRING): 'fix: remove vpip>=0.55 branch, keep pfr>=0.30/conf<0.20 real steal-opener signal'. v156 implements this verbatim.; Diff refs: opponent.py L318-322, L367-369, L380, L385-388: 11 smooth_rate callsites weight 4.0→2.0 (ftr_flop/turn/river, call_down_flop_turn/turn_river, fold_to_raise, fold_to_open_preflop, threebet_vs_open, barrel_freq, turn_to_river_barrel, barrel_abandon_turn/river, sizing_aggr)., opponent.py L572-581: __main__ fixture proving smooth_rate(11,20,0.44,2.0)=0.5382 > smooth_rate(11,20,0.44,4.0)=0.5317 > 0.50 gate., strategy_helpers.py L145-159: SIZING_MARGIN_ADJ telemetry moved from nested tendency-block (fires ~0.04%) to function scope printing CALL_MARGIN_FINAL margin_milli=+N reason=standard_arm/no_margin.
- **v155**: smooth_rate prior unreachability (prior_mean=0.45, weight=4.0) keeps producing INERT detectors that fire telemetry but move sizing <2% of pot — Master must require per-hand gating flags (e.g., prior_aggressor_checked_back boolean computed from history) before approving population-mean-based opp signals as 'new axes', not just population tendencies.
- **v155**: Dispatch-order shadow on river offense is a recurring failure mode: new functions placed BEFORE value_maximizer_overbet (L1575) / river_value_raise_tier (L1609) mute v134/v141 offense paths — new river offense must be relocated AFTER these call-sites or wired as fall-through.
- **v155 归档建议**: Before claiming _post_missed_cbet_exploit targets v130/v139 weak matchups, require ≥30g paired net-chips evidence from web/core/results/match_replay/ (current 10g samples are below pool noise threshold per critic) AND add a per-hand prior_aggressor_checked_back boolean to spot_info so the delta only fires on true missed-cbet dynamics rather than every to_call==0 turn/river — otherwise this is functionally equivalent to a new face on existing postflop_aggr/barrel_freq signals.
- **v155**: Critic evidence: H2H weaknesses: v154 r=1550 rd=149 — mid-pack (v136 r=1632 is top); weakest H2H: v130 3-7 (10g), v139 3-7 (10g), v151/v79/v129/v135/v145/v150 all 4-6 (10g). Worker comment cites 'targets v130/v139 weak matchups (30% WR @ 10g)' but ALL sub-30g = NOISE per experience_pool line 30 ('≥30g paired net-chips before re-adding exhausted features'). No matchup with ≥30g evidence supports this axis.; Experience pool refs: PARAMETER_TUNING line 23: 'choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating.' v155 IS a new opp-signal gate so qualifies for exemption — but the gate fires on population statistics, not per-hand state, weakening the 'new signal' claim., OPPONENT_MODELING line 8: 'smooth_rate prior math unreachable — prior_mean, weight=4.0 means even a TRUE 50%-folder saturates at 0.491'. v155 reuses the exact smooth_rate(...,0.45,4.0) signature for barrel_abandon — same prior-unreachability inherited., POSTFLOP_STRATEGY line 13: 'Dispatch-order shadow: wire offensive primitives AFTER downstream tiers'. v155 Site-1 (river L1564) is wired BEFORE value_maximizer_overbet (L1575) — risks shadowing v134 offense for conf≥0.15 hands with made≥0.45 tier=thin. Worker comments do NOT acknowledge this risk.; Diff refs: strategy_helpers.py L790-794: `has_value = tier in ('thin','strong','nut') or made_strength >= 0.45; has_draw = draw_strength >= 0.10` — broad eligibility with NO check that opp was the prior-street aggressor THIS hand., strategy_helpers.py L801: `raw_delta = 0.04 * (abandon_rate - 0.45) / 0.15` — at default prior abandon=0.55, raw_delta = 0.027, × confidence 0.5 = 0.013. Sizing shift ≈ 1.3% of pot., opponent.py L386-387: `barrel_abandon_turn = 1.0 - smooth_rate(barrel_continue, barrel_hands, 0.45, 4.0)` — this is exactly `1 - barrel_freq` (line 380) recomputed; same axis as existing signal.
- **v154**: Workers added a 6th river fold gate (_river_weak_made_hand_gate) AGAINST the pool's directive. Standard-arm fix (96.9% of hands, delta=+0 since v137) remains HIGHEST-ROI. Future Masters MUST enforce standard-arm fix as non-negotiable until addressed before ANY fold-side work.
- **v154**: SPR gate relocation confirms STACK-OFF GUARD PLACEMENT INVARIANT: fold gates must be upstream of to_call>=my_chips early-return, not downstream (v138-v153 were all shadowed). Relocation into opponent_allin block = correct structural fix.
- **v154**: H2H plateau — v153 WR=0.5083 (240g), mostly 45-55% matchups. No specific opponent where fold-side fixes show promise (v149 proved bot OVER-FOLDS preflop, does NOT over-commit).
- **v153 A1-审计**: v144-v152 offense detectors nearly ALL INERT (SB_OPEN/BB_VS_LIMP/BB_VS_RAISE/SPR_FOLD/STREET_FOLD/DELAYED_BLUFF all 0 fires); only LIVE = v152 PREFLOP_DEFEND_WIDEN 33×, MISFIRING (OR-gate vpip≥0.55 wrong target). v153 Task2 CALLDOWN_DONK inherits calldown_profile sample trap → predicted INERT.
- **v153**: Diff widened 5 preflop pot_odds thresholds (BB-vs-raise mid_pair 0.42→0.45, suited_connector 0.34→0.36; SB-vs-3bet small_pair 0.32→0.36, suited/broadway 0.30→0.35). donk_probe.py NEW calldown_profile consumer in should_donk_bet(); effective_min_strength 0.45→0.38, effective_freq_cap 0.65→0.80 when flop call-down≥0.55 samples≥4.
- **v152**: Preflop pot_odds windows <10pp virtually never fire in 70-hand HU; speculative hand caps (suited_connector/broadway_suited ≤0.30) must account for HU 3-bet pot_odds (0.28-0.35); implied-odds hands need ≥0.35 threshold.
- **v151**: `_pending_my_bet_ratio` only tracked on 'raise' (not 'allin') — misses allin-then-fold opps. daemon≥30g grep DELAYED_CALLDOWN_BLUFF<5% fire → relax curr_call_rate 0.45→0.50, min_samples 3→2, add 2 donk/probe dispatch sites.





