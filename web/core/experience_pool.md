## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never default-classify unknown openers as tight.
- SB-open/BB-defense adaptation uses open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning (donk_probe.py survived gens of false "absent" claims before landing+wiring in v119).
- Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate (v118 Master cited samples that don't exist — all real were 10-50g, below the 30g floor). Re-read head_to_head.json before quoting any number (last verified ~435 pairs / 405 ≥20g / 377 ≥30g; counts drift — re-verify each gen).

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION (facing_barrel_continuation / _single_reraise_stackoff_guard / _spr_commitment_gate / _allin_board_texture_fold) [POSSIBLY EXHAUSTED] — symbols ABSENT from HEAD (dropped via crossover rebase); new def discouraged — prefer offense pivot. Re-import / boundary rule: see GENERAL.
- probe_mode sizing-knob constant tweaks (thin-value floor / static-board sizing) [POSSIBLY EXHAUSTED]. Structural-correctness repairs (single-line bypass fixes) and NEW probe/donk primitives remain permitted.
- Detection-without-handler is recurring dead code: every new detector must meet birth requirements (canonical 6 — see BLUFF_CALIBRATION). v117's turn_double_barrel_planner violated them (1 site, no firing logs, unknown-opp bluff) → critic-rejected.
- broadway_suited bucket is ACTIVELY LIVE at strategy.py:405/439/449/574 (+ state.py:65/92), NOT inert headroom — prior [POSSIBLY EXHAUSTED] tag was a confirmed self-contradiction (grep-verified), removed. New opponent-signal-gated edits + ≥3 wired sites permitted.
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, check_raise_freq, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical, single source of truth — POSTFLOP/PARAMETER/GENERAL defer here): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, stacked deltas, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens (v118 0.50→0.45 lower bound behind 4 gating predicates = sub-noise; advisory bypass + precommit PASS ≠ strategic validation). Re-grep HEAD for live symbols before referencing (line anchors drift). EXEMPT per GENERAL boundary rule: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW, not byte-identical constant tweaks.
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming the constant still exists in HEAD.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Ratings are Glicko-2 live; re-read glicko_ratings.json + head_to_head.json before quoting numbers.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (v111-v120 consecutive, streak active with v121 in-progress) [POSSIBLY EXHAUSTED]. BOUNDARY RULE (single source of truth — POSTFLOP/PARAMETER/BIRTH-REQUIREMENTS defer here): same-fn/byte-identical re-import = exhausted & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements (BLUFF_CALIBRATION) = new. When ambiguous, prefer offense pivot. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base before importing. PERMITTED: NEW offensive primitive OR structural-correctness repair, esp. when Master JSON collapses under triple-forbid.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic dual-review drift: treat the LOWER score as authoritative when it cites missing evidence; verify high-swing replay claims vs real replay deltas. When a fallback bypasses critic via action:approve despite low score, archivist must flag the parent low-confidence.
- CRITIC-RETRY DILUTION TRAP (from v117): 4 iterations (6.0→4.0→6.0→3.0) progressively REMOVED the novel primitive instead of fixing the 3 execution gaps (≥3 wiring sites, ≥0.30 confidence gate, firing-rate telemetry). Fix execution gaps — don't restart smaller.

## RECENT_LESSONS
- **v121**: Critic evidence: H2H weaknesses: v104 vs v107: 0.471 (140g) — confirmed weak, real sample, v104 vs v102: 0.471 (210g) — confirmed weak, real sample, v104 vs v90: 0.471 (210g) — confirmed weak, real sample, v104 vs v113: 0.482 (110g) — confirmed weak, v117 edge claims vs v102/v107/v109/v116 are at 30-40g noise floor (experience_pool: '<30g H2H = noise'); Experience pool refs: POSTFLOP_STRATEGY: 'Defensive fold-gate accumulation as a DIRECTION ... [POSSIBLY EXHAUSTED] — new def discouraged — prefer offense pivot' — the SPR 3.0→3.5 mutation is widening an existing defensive guard, exactly this exhausted axis, RECENT_LESSONS: 'check_raise_freq detector ... is the master-level directive STILL NOT BUILT ... Next gen MUST add the primitive' — v121 marks 5th consecutive gen carrying this undelivered directive, GENERAL: 'Crossover-as-default fallback (v111-v120 consecutive, streak active with v121 in-progress) [POSSIBLY EXHAUSTED]' — v121 extends crossover streak to 11th consecutive gen; Diff refs: strategy.py:411-419 — broadway_suited iso-raise branch with pot_odds<=0.36 gate (real wiring site #2 of 3), strategy.py:711 — `if spr >= 3.5: return None` (mutation: 3.0→3.5 in _single_reraise_stackoff_guard), strategy.py:412-413 — comment claims 'v102 +13.5pp (0.60 vs 0.465)' which is FABRICATED (actual: v117 0.55, v104 0.471 → +5pp at 40g noise floor)
- **v119 (UNDELIVERED since v117, carried v117→v121-in-progress, 5 gens)**: `check_raise_freq` detector (per-opponent postflop check-then-raise vs check-then-call ratio) is the master-level directive STILL NOT BUILT — v119 added passivity_score gating but NOT check_raise_freq. Next gen MUST add the primitive in opponent.py (≥30g or opponent_model confidence≥0.30), wired into ≥3 sites (BB-vs-SB-limp call-vs-fold, river fold-vs-call, turn check-call-vs-raise), WIDENING CALL-DOWNS vs aggressive opps v102/v107/v108/v113/v114. Cite ≥30g anchor_runner evidence + add firing-rate telemetry first.
- **v119 (reaped-donor import)**: importing from a REAPED bot (v112) — the ONLY evidence is source-bot H2H edge gaps (v112 beat v96 52.2% where v116 40%; beats v116 on every common opp ≥30g), but those are ~30g noise-floor samples. Require ≥100g attribution analysis vs same opps post-commit + firing-rate telemetry on _value_floor to detect upstream shadowing. Permitted under crossover rule: value-tier floor is OFFENSIVE + mutation adds NEW passivity_score gating (not byte-identical; PARAMETER exhaustion exempted — see PARAMETER_TUNING/GENERAL).

