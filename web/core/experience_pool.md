## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, classify_sizing_tendency) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — they drift across v138→v139), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); since v134. NEW gating with it = PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- classify_sizing_tendency() (v137) = 4-bucket overbet/underbet classifier, CONSUMED at wired sites but firing-UNVERIFIED (no persistent fixture) — do NOT label dead/inert, do NOT "remove as dead code". ACTION: add persistent fixture/self-test proving firing rate, then declare LIVE.

## POSTFLOP_STRATEGY
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires on absent branches REGRESSED (0.5706→0.5306@360g); v138/v139 `_river_stackoff_guard` sat AFTER the `if to_call >= my_chips:` all-in early-return → structurally INERT on true all-ins. Fold-side now FORBIDDEN by dir-audit (HIGH-conf v140). v140 pivoted to BET-side `_river_value_ship_guard` (placed after choose_raise returns, before `return raise_amount`) — the only sanctioned direction. Need ≥100g H2H to confirm.
- Dispatch-order shadow: v134 wired `value_maximizer_overbet` BEFORE `turn_second_barrel_planner` + `river_value_raise_tier` = root cause of leaks vs disciplined barrel-aggressive bots; primitives (incl `turn_river_blocker_bluff`) hit 0-3/6 birth reqs → presumed-INERT. Fix: RELOCATE overbet call-site AFTER barrel/value tiers, NOT re-tune. Need ≥100g daemon H2H turn+river raise-freq lift ≥40% from ~33.5% baseline.
- `barrel_pressure_profile` + `check_raise_pressure` code-present (re-wired v133), firing-UNVERIFIED — "LIVE" = code-present only. Verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Mandate: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v134-v139 ALL violated; v140 FIRST to comply — 6/6 birth reqs + grep≥3x dispatch-reachability assertion). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures).
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap, BLUFF_OPPORTUNITY_THRESHOLD) [POSSIBLY EXHAUSTED] — live `BLUFF_OPPORTUNITY_THRESHOLD = 0.55`; require ≥100g H2H vs v96/v108/v114 before further tuning (v93+v111 REAPED, unreachable); if it doesn't recover ≥half the v111-vs-v134 gap, declare axis dead.
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). Reliability ≠ firing-fixture compliance — Master reliably produces plans that STILL violate the fixture mandate (POSTFLOP); the fixture gate, not Master output, is the open problem. Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v139 critic refs all sub-30g; v138 = 320 games/27 opponents, max 20g/opp).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (8th recurrence v127-v140) — cited G{N}H{M} replays non-existent (actual = timestamp-based, e.g. 20260620_160503_claude_v77_vs_claude_v128.json). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v141**: Critic evidence: H2H weaknesses: v140 stats: 240g, win_rate=0.4917 (near-flat vs field); weakest H2H all at 0.40 (v131/v135/v97/v130/v128/v90) but ALL ≤10g = sub-30g noise per exp_pool insight #40 — 'No confirmed H2H weakness citable, the weakness (river stack-offs) comes only from master plan + prior critic feedback, not live data.' Cannot cite confirmed H2H weakness; the offensive pivot is rationale-driven, not data-driven.; Experience pool refs: Exp pool #8 (v140): 'classify_sizing_tendency() consumed at wired sites but firing-UNVERIFIED — ACTION: add persistent fixture/self-test proving firing rate.' v141 addresses with 6/6 fixture + dispatch-reachability assertion., Exp pool #13: 'Mandate: NEW detectors MUST prove firing via persistent fixture exercising the ACTUAL DISPATCH SITE (v134-v139 ALL violated; v140 FIRST to comply).' v141 continues the v140-compliant pattern., Exp pool #22 (EXEMPTION clause): 'choose_raise() sizing constants [POSSIBLY EXHAUSTED] — EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.' v141 RVA uses BOTH signals + classify_sizing_tendency → qualifies as NEW, not exhausted-axis.; Diff refs: strategy_helpers.py:357-413 NEW `_river_value_extraction_amplifier()` with 4-path gating (underbettor / standard_sticky / unknown_sticky / none) + 6/6 fixture (lines 955-986) + dispatch-reachability grep assertion., strategy.py:34 import added; strategy.py:1402-1415 wired AFTER value_maximizer_overbet (1395-1401) and BEFORE river_value_raise_tier (1416-1421) — correct dispatch order, no shadow., Sizing curve analysis: RVA lifts baseline by +0.10x to +0.15x pot at made_str 0.50-0.82; underbettor bonus +0.05x.
- **v140**: BET-side `_river_value_ship_guard` placement (after choose_raise returns, before `return raise_amount`) escapes the v138/v139 fold-side shadow that ran AFTER the to_call>=my_chips all-in early-return. The dispatch-reachability assertion in __main__ fixture (guard appears ≥3x in strategy.py + `return raise_amount` follows the call) is the PROVEN pattern to proactively detect INERTNESS — FIRST gen (v138-v140) to meet the fixture birth-req mandate.
- **v140**: 8th recurrence of replay-evidence fabrication: cited G4H45/G6H15 don't exist. Worker MUST `ls web/core/results/match_replay/ | head -5` and cite actual timestamp filenames before any replay-based claim; reviewer should reject G{N}H{M}-style citations.
- **v140 archiving**: After daemon rates v140 ≥100g H2H vs aggressive barrelers (v96/v108), grep river raise-freq vs parent v139; if value-raise drops materially without win-rate gain, relax made_strength floor 0.70→0.65 OR add made_class>=2 (two-pair+) allowance on safe non-paired boards.
- **v140**: No confirmed H2H weakness citable — v139 parent 240g/~30opp ≈ 10g/opp = ALL SUB-30G = NOISE; the "weakness" (river stack-offs) comes only from master plan + prior critic feedback, not live data.
- **v139/v138 (recurring PLACEMENT SHADOW)**: `_river_stackoff_guard()` invocation sat AFTER the `if to_call >= my_chips:` all-in early-return → NEVER fires on true stack-covering river all-ins. v138 band-aided with helper-only fixture; v139 tried turn-sizing mitigation, NOT the structural fix. v140 resolved via BET-side pivot. Fold-side relocation now FORBIDDEN (dir-audit HIGH-conf v140).
- **v139**: FIXTURE ≠ LIVE — `_turn_oop_pot_control` 8-case fixture tests helper in isolation, does NOT exercise the `round_idx==2 and to_call==0` turn-OOP dispatch wire. New helpers need ≥1 test exercising the actual dispatch site, not just the function.

