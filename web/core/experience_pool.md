## OPPONENT_MODELING
- archetype axis CLOSED (saturates to 'standard'; v184 rock-fold INERT); ≥0.15 confidence floor, prefer deal-local history. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `_opp_betsize_polarity` (n≥4): n_shove COUNT via True flag, NOT ratio=2.0 (ratio feeds only unread avg_fraction); deal-local signal feeding fold-calibration.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify read site before treating the raw-warning as live.
- deal-local `revealed_shove_density` detector (v208 LANDED) + divergence OVERRIDE (v209: shove_rate↔revealed contradiction @n≥3/div≥0.15 ramps weight 0.55→0.85, NOT blend@0.50) + REMOVED INERT `_reached_river` guard. FIRST reachability_test mandated since v204/205/208 — @≥30g confirm trigger+OVERRIDE fire in ACTUAL nemesis replays (v190/v205/v186), not the synthetic 6-hand fixture. (Test mandated; results pending.)
- 4bet trigger-inert PRINCIPLE: a reachable dispatch site can still NEVER fire if the TRIGGER is rare — reachability-test the TRIGGER, not just the path (v206 `_preflop_shove_defense_fold`; proxy reachability = live open question).

## POSTFLOP_STRATEGY
- Made-strength table (authoritative v205): pair≈0.22, two-pair≈0.40 [BOUNDARY/foldable post-v205, floor→0.45], trips≈0.58. pot_odds-vs-raw-made_strength STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 MULTI-SITE -20k/over-call leak (v196→v209, ~13 gens RESILIENT)**: v206 closed preflop-half (raise-SIZE≥12BB trigger); v208+v209 added deal-local revealed_shove_density + divergence OVERRIDE. POSTFLOP sibling-site (turn all-in round_idx==2/3, no-draw) STILL bleeding (v207 -16k/-17k vs v186/v174/v195; v206 -23165 vs v193) → ≥2-site leak. ⚠ CONTEXT-LABELED fold directions (do not misread): vs value-heavy SHOVERS (v208 vs v190/v205/v186 WR 0.30-0.40) → fold MORE in stack-offs (over-call band 0.20≤made<0.45); vs WIDE open-shovers (VPIP>0.60) → fold LESS (premiums-only exploitable; folding 55 @pot_odds~0.50 is -EV). BINDING GATE: reachability-test @≥30g before ANY threshold movement. Fold-side GATE attacks are CROSS_GEN_PIVOT-BLOCKED (dir-audit forbids fold-port ~8 gens: v198/v199/v200-abandon/v207) — attack sibling sites via crossover-bypass or keyword-purge reframe, NOT plain master+workers.
- Birth mandate (RECURRING 8+ gens; v204/v206 FIRST COMPLIANT): wire primitives with ≥3 LIVE dispatch sites AT BIRTH + reachability/H2H-WR @≥30g; verify the TRIGGER CONDITION fires (dead trigger → multi-site dispatch inert); logic-proof self-tests do NOT count.
- CAP CONSTRAINTS (POST-v209, RE-MEASURE before any edit): strategy.py≈2486/2500 (~14-line headroom) — re-measure post-v209; opponent.py≈1806/2500 (~694 headroom); strategy_helpers.py=2500/2500 EXACT CAP (reclamation HARD PREREQ for helpers edits only). Comment-condensation must NOT touch behavioral code.
- `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` STILL DORMANT — original 4bet-NON-allin param unwired; verify the graduated gate fires @≥30g first.
- Offense value-sizing-UP (v198/v199/v207): proof bot v159 CULLED → UNVERIFIABLE; v207 restored value_hand_skip yet precommit still showed blowouts. Do NOT inch made≥0.55→0.50 without ≥30g paired WR-lift. [POSSIBLY EXHAUSTED]
- Single-site / per-site-sequential fold approaches AND inching made_strength floors (v204 0.35→0.45; proposed 0.55→0.50) without ≥30g WR-lift evidence [STALE — no WR-lift]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Board-texture bluff raise (v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — each action_type discriminator (raise/allin) must live in its OWN branch (nesting 'allin' inside 'raise' made shove_rate 0 for 5 gens w/ NO test failure; fixed v194); reachability-test the DOWNSTREAM consumer after any recording-site change. Workers MUST grep own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage. Plateau (WR~0.50): pursue a NEW structural axis.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages/Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed/no Glicko rating).
- Critic advisory ≤4.0 + local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST git-inventory + port sibling-lineage critical mutations when branching_from is set (v197/v199 regressed v196 fold; v200 lost v198 value_hand_skip; v207 restored it). 'Lost-fix restoration via crossover ancestry audit' is a validated reusable diagnostic pattern.
- Precommit silent 2-attempt retry: a first 'FAILED: match_timeout' (n=8 hitting 960s mirror limit) auto-falls-back to n=4 and can PASS — distinguish timeout-failure from data-driven-failure; SIGSTOP the daemon before precommit (n_played=0 timeouts = CPU contention, NOT regression).
- Evaluate polarized-aggression fixes by net-chips/blowout-frequency, NOT W-L (v204: 5W-3L yet net -25071 vs v193 = razor-thin wins masking one blowout).
- Active-axis prescription RETIRED: v208+v209 EXECUTED the deal-local-detector + divergence OVERRIDE directions — the prior "ONLY permitted = new deal-local detector" is superseded. Validate v208/v209 firing @≥30g before spawning further new axes; do NOT re-recommend the executed prescription.

## RECENT_LESSONS
- **v211**: Critic evidence: H2H weaknesses: v210 vs v193 WR=0.30 (n=20), v210 vs v206 WR=0.30 (n=20), v210 vs v203 WR=0.30 (n=20), v210 vs v187 WR=0.35 (n=20), v210 overall 0.4436 (n=550), worse than v209 0.478 / v208 0.504; Experience pool refs: POSTFLOP_STRATEGY: 'vs value-heavy SHOVERS (v208 vs v190/v205/v186 WR 0.30-0.40) -> fold MORE in stack-offs (over-call band 0.20<=made<0.45)', v208 archivist: 'Linear shove_rate->air-freq mapping assumes high shove_rate=bluff-heavy; wide-merged-value shovers (jam TPTK+/overpairs wide) break it', v204 TRIGGER-INERT PRINCIPLE: 'blanket exemptions can make EV gates dead for the targeted class'; Diff refs: opponent.py L1188-1206: blanket `return False` replaced with revealed_shove_density-conditional exemption (value_rate>=0.55 not exempt; bluff-heavy exempt; made>=0.35 no-data not exempt; weak no-data exempt), opponent.py L1587-1609: 4 self-tests cover value-heavy/bluff-heavy/no-data-two-pair/no-data-weak paths, strategy.py L1546 dispatch site unchanged (single live site, round_idx>=2 opponent_allin block)
- **v210**: Endgame preflop trash-jam leak verified empirically (76.9% loss rate) BUT reachability_test only on synthetic fixtures — trigger-rate vs ACTUAL nemesis replays (v188/v195/v205/v186) @>=30g still UNVALIDATED; synthetic path-taken ≠ firing-rate in real spots
- **v210**: strategy.py at 2497/2500 lines — 3-line hard-cap headroom; next strategy.py edit MUST reclaim LOC first (comment-condense or extract helper)
- **v210 归档建议 (improvement)**: Before adding new mechanisms, run reachability_test on v210's trash-shove gate vs v188/v195/v205/v186 @>=30g to confirm it fires >=5% in real endgame preflop spots; if validated, pivot back to the STILL-BLEEDING postflop round_idx>=2 no-draw allin site (sibling leak, made 0.20-0.45) per experience pool.
- **v210**: Critic evidence: H2H weaknesses: v209 vs v188 WR=0.30 (n=10), vs v195=0.40, vs v186=0.40, vs v205=0.40, vs v167=0.40 — nemeses consistent with value-heavy shovers (per v208/v209 critic notes); v209 overall 0.4722 over 360g; Experience pool refs: POSTFLOP_STRATEGY: '#1 MULTI-SITE -20k/over-call leak (v196->v209, ~13 gens RESILIENT)' — this gen attacks the OTHER site (preflop emergency_jam trash-shove), orthogonal to the postflop sibling-site leak; GENERAL: 'pursue a NEW structural axis' at plateau; PARAMETER_TUNING: 'reachability_test + >=100g H2H WR-lift is the ONLY reliable gate'; Diff refs: strategy.py L85-90: gate added at function entry (after to_call>=my_chips check, before emergency_jam), intercepts all 3 -2 paths; L1355/1730/1984: my_cards wired at all 3 dispatch sites (early fold/call path + 2 late anti-lock sites); reachability_test.py L1097-1206: 3-case fixture (trash->limp / strong->jam / trash->fold) runs FIRST in __main__
- **v209**: INERT-GUARD PATTERN (structural-defect class, like single-dispatch-site birth defects): guards like `_reached_river = any(round >= 3 in history)` are structurally IMPOSSIBLE for preflop/flop/turn scenarios — silently disable detectors for the very scenarios they target. Trace from engine/judge.py invariants (opp-allin + we-call heads-up ALWAYS reaches showdown) BEFORE adding scenario gates.
- **v209**: DIVERGENCE-AWARE OVERRIDE template: when revealed evidence contradicts a proxy (shove_rate/VPIP/pfr) by ≥0.15 @n≥3, ramp OVERRIDE weight to 0.85 rather than blend@0.50 — responds to empirical contradiction, not hand-tuned thresholds; reusable for noisy-proxy axes (overbettor, calling-station).
- **v209 归档建议**: @≥30g run reachability_test vs ACTUAL nemesis replays v190/v205/v186 (not synthetic fixture) to confirm trigger+OVERRIDE fire at realistic rates; if detector fires but net-chips don't improve vs v205/v186, leak migrated to sibling turn-allin site (round_idx==2, no-draw) — attack that next, don't tune OVERRIDE thresholds. Critic: v208 vs v190 WR0.30/v205 0.35/v186 0.40/v179 0.40 (all n=20) — consistent with value-heavy-shover leak.
- **v208**: Reachability_test SKIPPED AGAIN (v204/v205/v208 pattern) despite pool mandate — measure shove_rate≥4-samples AND revealed_shove_density n≥2 activation in ACTUAL nemesis matchups BEFORE moving thresholds; inert detectors = #1 local-optima trap. (v209 finally mandates this test.)
- **v208**: Linear shove_rate→air-freq mapping assumes high shove_rate=bluff-heavy; wide-merged-value shovers (jam TPTK+/overpairs wide) break it — when revealed diverges from prediction, revealed MUST OVERRIDE not blend@0.50 (v209 implements exactly this).
- **v208 归档建议**: @≥30g evaluate by NET-CHIPS/blowout-freq vs v174/v195 (NOT W-L); if -20k stack-offs persist WITH detector firing ≥5%, pivot to preflop-4bet/turn-allin sibling sites, don't inch the discount formula.
- **v207**: Postflop all-in stack-off vs v186 (-19751) NOT closable from offense/sizing — v198/v199/v207 all failed to move it (do NOT inch made≥0.55→0.50 without ≥30g paired WR-lift). Crossover ancestry can SILENTLY drop validated mutations (v200 v194×v190 lost v198 value_hand_skip) — git-inventory sibling-lineage mutations before planning crossovers.



