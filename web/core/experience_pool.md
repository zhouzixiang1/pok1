## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never default-classify unknown openers as tight.
- SB-open/BB-defense adaptation uses open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — line anchors drift each rebase; re-verify pool claims vs HEAD each consolidation (v123 cited facing_barrel_continuation @ 944-956 that no grep bore out).
- Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. Re-read head_to_head.json + glicko_ratings.json each gen. Re-anchor: committed HEAD = v124; rd-aware RATED top = v93(1478,rd55)/v97(1476,rd52). v118/v114/v102/v104/v111 ALL at rd=350 (Glicko DEFAULT = unrated) — do NOT cite any rd=350 bot as "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION [POSSIBLY EXHAUSTED] — prefer offense pivot, BUT offense-pivot Master plans chronically COLLAPSE under tight direction-audit (v107-110, v121, v125 all Master-JSON-collapsed); the offense-pivot default is inoperative at the execution layer until Master schema survives the audit — fix Master prompt/schema before retry.
- facing_barrel_continuation ABSENT in HEAD v124 (v116-lineage; v122 restored then v123/v124 reverted) — confirms the exhausted axis, NOT a refutation. Re-grep HEAD before any anchor.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]. Structural-correctness single-line bypass fixes + NEW probe/donk primitives remain permitted.
- Detection-without-handler is recurring dead code: every new detector must meet birth requirements (canonical 6 — see BLUFF_CALIBRATION).
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED] (v121 critic flagged a v117-recreated-from-v104 recreation; "+13.5pp" fabricated, actual +5pp at noise floor).
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical, single source of truth — POSTFLOP/PARAMETER/GENERAL defer here): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code.
- check_raise_freq is MANDATED-BUT-UNDELIVERED 9+ gens (v117→v125) — NOT a ready offense option and chronically unrealized; do NOT treat it as an available permit-unlocker. It counts toward the crossover/permit clause ONLY once actually DELIVERED (detector + ≥3 sites + telemetry), not merely proposed in a Master plan.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens (v118/v124 threshold nudges behind multiple gating predicates = sub-noise; advisory bypass + precommit PASS ≠ strategic validation). Re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming it still exists in HEAD.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Ratings are Glicko-2 live; re-read glicko_ratings.json + head_to_head.json before quoting.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (v111→v124, 13+ consecutive crossovers) [POSSIBLY EXHAUSTED]. Re-anchor: v125 (attempted v118 master) was ABANDONED — committed crossover streak ends at v124; abandoned v125 is the latest evidence that offense-pivot via Master is blocked. BOUNDARY RULE (single source of truth): same-fn/byte-identical re-import = exhausted & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements (BLUFF_CALIBRATION) = new. Permit clause (NEW offensive primitive DELIVERED / structural-correctness repair when Master JSON collapses) has fired EVERY gen for 3+ gens (v122/v123/v124) — marker is NON-FUNCTIONAL; Master must earn a crossover via DELIVERED NEW-offense, not permit-default or proposal-only. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base before importing.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Diff parents holistically (`diff -rq bots/A bots/B`) — transitive edges are confounded by other file changes. Direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic-loop traps: (a) dual-review drift — treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real deltas; when a fallback bypasses critic via action:approve despite low score, archivist must flag parent low-confidence. (b) DIFF-SCOPE RECONCILIATION — reviewer's stated summary must reconcile against actual diff (v123: 'constants block only' vs 14 files/4812 insertions). (c) RETRY DILUTION — iterations progressively REMOVED the novel primitive instead of fixing execution gaps (≥3 wiring sites, ≥0.30 confidence gate, firing-rate telemetry). Fix execution gaps — don't restart smaller.

## RECENT_LESSONS
- **v125 (ABANDONED, no commit)**: v118 master plan JSON-collapsed 2× under tight direction-audit (forbids crossover + ALL threshold tuning + exhausted-reimports + new-detector≥3 sites). Confirms systemic Master-schema failure under tight-audit (v107-110, v121, v125) — offense-pivot is BLOCKED at execution until Master prompt/schema is fixed. Nondeterministic: a PRIOR v125 cycle produced a valid plan (artifact b51fc9a, `_count_opp_check_raises` detector→3 fold sites) but it is NOT reusable via pipeline (execute_workers reads master_plan from CHECKPOINT, not git). Next: retry run_master (may succeed); if it collapses again, fix dir-audit multi-forbid OR Master schema.
- **v124**: Crossover v118×v108 isolating BLUFF_OPP 0.48→0.55 (retaining river_value=0.45); cleaner attribution than v123 (v118↔v108 diff = 2 functional constants). 13th consecutive crossover; check_raise_freq STILL undelivered (now 9 gens); 0%-fold leak unfixed. Needs ≥100g H2H vs v93/v109/v94.
- **v123**: CONFOUNDED ATTRIBUTION — when Master cites crossover parent H2H edges, diff parents holistically; the v108>v116 edge could stem from the missing v102 probe_mode fix + state.py/edge differences, NOT the imported threshold (≤40g, confounded). Edits were line_reading.py threshold-only nudges = constant tuning, no new gating. check_raise_freq mandate carried, undelivered.
