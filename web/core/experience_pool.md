## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".
- value_maximizer_index (v134) = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20), LIVE opponent.py:229; gating with it counts as a NEW opponent signal (PARAMETER_TUNING EXEMPT).

## POSTFLOP_STRATEGY
- Defensive fold/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132 rewired pot_odds-grounded but BOTH REGRESSED (0.5706→0.5306@360g); _spr/_single_reraise/_river stack-off guards v103/v135. Future fold-gate attempts MUST ship a persistent firing counter + ≥100g H2H improvement before commit.
- v129 turn_second_barrel_planner = validated OFFENSE pivot (in HEAD since v133) — runtime "PROVEN" now in doubt: v134 wired value_maximizer_overbet BEFORE it (dispatch-order shadow), may be partially INERT until ≥100g daemon H2H confirms turn+river raise-frequency lift ≥40% from ~33.5% baseline.
- barrel_pressure_profile + check_raise_pressure RE-WIRED LIVE by v133 (v132 removal reversed); firing effectiveness unconfirmed — verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED] (unverified if still LIVE in HEAD — v116 may carry it forward, re-grep before anchoring); structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- OFFENSE-vs-DEFENSE COLLISION (unresolved): v134 value_maximizer_overbet ships top-set~0.80-0.85/straight~0.85 while v135 _river_stackoff_guard DENIES ships at made_strength≥0.88 — partially cancel; require ≥100g daemon H2H before cutting either side.
- Mandate: NEW detectors MUST prove firing via persistent fixture/self-test. v134/v135 shipped NEW primitives with NO firing verification = violation. Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel) NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE) is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED]; BLUFF_OPP_THRESHOLD was REMOVED in v132 (0 grep hits) — do not re-add that constant axis.
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate it with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). BOUNDARY: crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback when Master could ship a NEW detector = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v130-v135 critic refs all ≤10-20g = noise).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; reviewer diff-summary must reconcile against the actual diff.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (4th recurrence v127/v129/v130/v135) — cited replays (G{N}H{M} format) are non-existent (actual format is timestamp-based). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add a hard gate rejecting cited replays absent on disk.
- Lineage/HEAD hygiene: reaped bots' FUNCTIONS persist in HEAD via crossover and toggle across gens (barrel_pressure_profile/check_raise_pressure: v128 add → v132 remove → v133 re-wire LIVE) — re-grep HEAD for live symbols before anchoring; stale comment ≠ live code, "removed" ≠ gone. Line numbers drift across gens — cite symbols/paths, not line numbers.

## RECENT_LESSONS
- **v136**: Critic evidence: H2H weaknesses: v134 vs v93: 0.40 (10g) — disciplined barreler, v111 beats 0.5217 (230g), v117 vs v134: 0.60 (10g) → v134 loses 0.40; v111 beats v117 0.5214 (140g), v114 vs v134: 0.40 (20g) — v111 beats v114 0.5048 (210g), v134 vs v95/v96: 0.50 (10-20g) — neutral, consistent with not-tight-foes; Experience pool refs: Insight #5: 'v134 overbet leaks vs tight foes' — directly addressed by confidence floor raise, Insight #3: 'loser-ships-more 10/14 pairs' — supports tighter discipline, BLUFF_CALIBRATION: 'bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED]' — axis flagged; v136 is revert-to-baseline not new tweak; Diff refs: line_reading.py:12-23: BLUFF_OPPORTUNITY_THRESHOLD 0.48→0.55 (revert to v111 baseline, well-commented with H2H evidence), strategy_helpers.py:500-511: confidence floor 0.25→0.30 (+20%) in value_maximizer_overbet() with rationale citing exp-pool #5, strategy.py:1292-1304: dispatch-order UNCHANGED — value_maximizer_overbet still wired BEFORE river_value_raise_tier (shadow risk persists at high-confidence spots)
- **v136**: turn_river_blocker_bluff() (AIR-only gate made<0.20/draw<0.10, fold_to_bet_turn/river in [0.40,0.55], 35% seed freq, 0.55-0.70x pot) = 3/6 birth requirements (NEW detector + NEW signal + confidence gate); MISSING 3 wiring sites (only 2 — river+turn both wired AFTER value_maximizer_overbet/barrel_planner, dispatch-order-shadow risk), replays, fixture. BLOCKER_BLUFF_FIRE stderr alone ≠ req #6. v136 has 0 H2H games; v135 has 0 matchups ≥30g (all 24 entries 10g = noise); docstring cites population-level fold_to_bet, NOT specific H2H weaknesses. Orthogonal to OFFENSE-vs-DEFENSE collision.
- **v135**: Do NOT reverse a prior gen's master-planned direction on noise — v135 cut v134's offense pivot after only 10g H2H (burns a gen on fabricated-premise change). _river_stackoff_guard (made_strength≥0.88) BLOCKS top-set/straight value hands v134 was tuned to ship, 0/6 birth requirements met. Wait ≥100g daemon H2H vs v130/v131/v133; if v134 leaks at ≥100g, calibrate 0.88→0.78-0.82 + add a ship-rate-by-hand-tier fixture (docstring replays fabricated; actual: 4 of 230 games ≤-19000, 2 mirror).
- **v134**: DISPATCH-ORDER SHORT-CIRCUIT RISK — value_maximizer_overbet wired BEFORE river_value_raise_tier AND turn_second_barrel_planner (validated v129 edge); when vm_index≥0.75 fires it shadows them. Future Master plans must specify call-site ordering relative to validated primitives, not just "wire into N sites". PARAMETER_TUNING EXEMPT earned via NEW signal gating; only 2/6 birth requirements met (NEW detector + NEW signal; 2 not 3 sites, no replays, no fixture) — verify firing before next gen.

