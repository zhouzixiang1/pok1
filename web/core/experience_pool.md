## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). Gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic.
- Detector firing: grep symbols in bot_telemetry/{bot}.jsonl (stderr capture A1-unlocked v146+). **CRITICAL: grep token>0 ≠ LIVE — MUST grep `delta_adj!=0` / actual param values.** CAUTIONARY CASE: SIZING_MARGIN_ADJ printed only sub-arm B's delta, producing a FALSE "99.96% inert" verdict — standard arm A was LIVE all along (v153+, v154 r=1605). Confirm telemetry prints TOTAL return at function scope. reachability_test.py = CODE-PATH only (force-fed), NOT in-game firing. 0 fires = PENDING-VERIFICATION.

## POSTFLOP_STRATEGY
- **`postflop_call_margin` telemetry corrected (v154 A1-verified)**: prior "99.96% DEADZONE" verdict was FALSE — it described ONLY tendency arm B (compound-AND ~0.04%, near-dead). **Standard arm A is LIVE** since v153 (unconditional deltas weak_showdown+0.020 / air_hand+0.028 / facing_postflop_aggression+0.008-0.032, added to margin BEFORE tendency block, NOT clamped away, consumed at strategy.py `call_margin=postflop_call_margin(...)`); v154 r=1605 (pool #2) vs v153 r=1413 confirms liveness. LESSON: grep delta_adj!=0 on sub-arm-only telemetry = FALSE INERT verdict. Next = fix TELEMETRY (hoist sys.stderr.write to function scope, print TOTAL margin_milli + per-arm reason= tag), NOT add another standard arm or freeze framework in do_not_touch.
- **smooth_rate prior math unreachable (FIXED v156)**: prior_weight 4.0 meant even a TRUE 50%-folder saturated at 0.491 (n=30) < 0.50 gate. Fix: weight 4.0→2.0 (smooth_rate(11,20,0.44,2.0)=0.5382). Unblocks ALL v144-v152 detectors — tune prior_weight BEFORE adding detectors or relaxing per-detector thresholds.
- **calldown_profile sample self-defeating trap**: samples accumulate only via pending_my_pressure + opp NEXT action; each opp FOLD ends hand → foldy opps never reach n≥4. opponent.py populates empirical rate only at n≥4 else defaults 0.5, while detectors gate on n≥3 → every n=3 case killed. v151 `_delayed_calldown_bluff` 0-fire; v153 CALLDOWN_DONK inherits → INERT. Fix: populate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.
- **v152 PREFLOP_DEFEND_WIDEN LIVE-but-MISFIRING** (33×, bypasses framework via OR-gate): vpip≥0.55 catches passive limpers, makes -EV widen vs premium-heavy raises. Fix (applied v156): remove vpip≥0.55 branch, keep pfr≥0.30/conf<0.20 real steal-opener signal.
- **Telemetry scope invariant (v156)**: block-scoped sys.stderr.write tokens (inside nested if) are functionally invisible to daemon grep — must be hoisted to function scope on unconditional path. CALL_MARGIN_FINAL at function scope = correct pattern. Validation gate: daemon≥30g → require delta!=0 in ≥5% of detector stderr lines else INERT → block commit.
- **PLACEMENT SHADOW CONFIRMED DEAD (v147/v148 SPR gate, v154 relocation)**: guards inside `to_call>=my_chips` block reached ZERO times (equal stacks + any legal non-allin raise leaves ≥1 chip → to_call<my_chips always). DO NOT place guards there — relocate to `to_call>0` (~40% of decisions) or opponent_allin branch.
- **STACK-OFF GUARD PLACEMENT INVARIANT**: fold gates must be UPSTREAM of to_call>=my_chips early-return (v138-v153 all shadowed). v148 SPR gate upstream = only validated tail-containment lever (±11692 vs v130).
- **NEW river all-in fold gates MUST NOT be added** — v154 added 6th (_river_weak_made_hand_gate) against pool's HIGHEST-ROI directive. Fold-side axis CONFIRMED EXHAUSTED (v127-v154, 12+ gens); v149 proved bot OVER-FOLDS preflop. Continuous call_margin EV-integration (v157 pattern) is the ONLY structurally-valid fold-side path that passes the dir-audit binary-gate ban — future fold-side work follows it, NOT more binary gates. Masters MUST enforce standard-arm fix before any fold-side work.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (value_maximizer_overbet, river_value_raise_tier, turn_second_barrel_planner); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) open per axis but each exhausts. Pre-v146 + v149-v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION.
- Preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; measurement anchors REAPED; revalidation requires NEW anchors. Includes check_raise_freq = DEAD directive (undelivered 14+ gens v122→v151). Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).
- Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; future widen_threshold must target ≥15pp bands minimum; implied-odds/speculative hands need ≥0.35.

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v159**: Firing verification must use code-reachability proxy (get_action() harness), NOT stderr grep — battle.py _PersistentBot.call() is stdout-only and drops stderr telemetry entirely (A1 rootcause, reconfirmed v159 critic).
- **v159**: Calldown-derived opponent signals need n≥3 samples before licensing aggressive slopes; n≥2 + conf≥0.20 admits coincidental noise.
- **v159 归档建议**: Verify via code-reachability proxy that RIVER_VALUE_TIER license_milli>0 fires ≥5% of decisions at daemon≥30g, then confirm v159 holds v157's 54.2% WR over ≥100g vs v130/v139/v148 — if river_call_size_ratio rarely reaches ≥0.70 in 70-hand HU, the strong 0.45→0.85x slope is effectively dead and the default 0.45→0.60x conservative path becomes the whole story.
- **v159**: Critic evidence: H2H weaknesses: v158 has only 70g aggregate (34-36, 48.57%) — below the 100g success threshold; no per-opponent river-overcharge leak confirmed (v158 critic noted the same for v157). Pre-emptive calibration per experience pool recommendation.; Experience pool refs: RECENT_LESSONS v158: 'gate the strong band on a NEW per-street opp_call_down_value_ratio signal so the 0.65-0.85x sizing only fires vs confirmed calling-stations rather than folding out worse hands' — EXACTLY what v159 implements., RECENT_LESSONS v158: 'Calibration cliff at made_str=0.55 is ungrounded ... revert to single-slope and smooth with a 3-point piecewise-linear' — v159 removes the cliff via continuous license., PARAMETER_TUNING EXEMPT clause: 'OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile)' — river_call_size_ratio is a new addition to this exempt list.; Diff refs: opponent.py L360-369: NEW river_call_size_ratio computed from _calldown_samples filtered to river (ridx==3) and called==1, n>=2 gate., opponent.py L409: river_call_size_ratio added to opponent_model dict export., strategy_helpers.py L504-515: replaced v158 two-band cliff with continuous license formula `ratio = 0.45 + (0.15 + _license * 0.25) * t` where `_license = call_size_license * conf_license`. Removes made_str=0.55 discontinuity.
- **v158**: PARAMETER_TUNING EXHAUSTED confirmed on the choose_raise river-value-sizing axis: 4 primitives now cover this space (v134/v140/v141/v143/v158). Future Master MUST pivot to a NEW opponent-signal axis — NOT add a 5th river-value primitive. Critic-recommended pivot: per-street opp_call_down_value_ratio (chips-called / pot-size on river calls) to gate the strong band so the bigger sizing only fires vs confirmed calling-stations.
- **v158**: Calibration cliff at made_str=0.55 is ungrounded — if daemon>=30g grep RIVER_VALUE_TIER band=strong shows <5% firing OR no WR lift vs v130/v139/v148 at >=100g, revert to single-slope and smooth with a 3-point piecewise-linear (0.45/0.55/0.85 at made_str 0.45/0.60/0.82) to remove the exploitable 10pp jump.
- **v158 归档建议**: Verify firing via daemon>=30g grep `RIVER_VALUE_TIER band=strong ratio_milli` (battle.py _PersistentBot stdout-only means stderr telemetry is unreliable — use a code-reachability proxy, not grep), and if <5% or no WR lift vs v130/v139/v148 at >=100g, gate the strong band on a NEW per-street opp_call_down_value_ratio signal so the 0.65-0.85x sizing only fires vs confirmed calling-stations rather than folding out worse hands.
- **v158**: Critic evidence: H2H weaknesses: v157 has NO H2H entries (rd=86, recently committed); only aggregate 53.3% WR over 240g is available. No per-opponent river-overcharge leak is confirmed.; Experience pool refs: choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated >=6 gens. EXEMPT only for NEW opponent-signal gating. This change adds no new opp signal., Threshold-only nudges on adjacent gates = constant tuning when no new gating/opp signal added. Two-band slope reshape is threshold-tuning on the value-sizing gate., v118 widened floor 0.50->0.45 in this exact function; v134/v140/v141/v143 all touched adjacent river value/ship logic — same decision point.; Diff refs: strategy_helpers.py L504-516: linear slope `ratio = 0.45 + 0.35*((made_str-0.45)/0.37)` replaced by two-band `if made_str<0.55: 0.45+0.10*t else: 0.65+0.20*t`. At made_str=0.55 ratio jumps from 0.55 to 0.65 (+10pp cliff)., strategy_helpers.py L523-531: RIVER_VALUE_TIER ratio_milli telemetry added at function scope (correct pattern per v156 telemetry-scope invariant)., strategy_helpers.py L2077-2112: 5 new __main__ fixture assertions (thin 0.44-0.56, strong060 0.64-0.72, strong070 0.72-0.82, strong080 0.82-0.86, fold_adj monotonic).
- **v157**: Continuous call_margin integration (additive margin on the LIVE to_call>0 EV path) is the structurally-valid fold-side path — passes the 6-gen dir-audit fold-side ban that EXHAUSTED binary `return -1` gates (v135-v154). Before claiming v157 fires, verify the L1228 wire sits UPSTREAM of any early-return — shadowed placement was the v138/v139 failure mode.
- **v157 归档建议**: Daemon ≥100g grep `CALLDOWN_TAX_FINAL reason=tax_fired` vs v130/v139/v148 (validated containment references + weakest matchups) — <5% firing ⇒ opponent.py may not populate `opp_prior_postflop_raise_count` on the to_call>0 path; firing-but-no-WR-lift ⇒ band peak 0.46 too aggressive, raise floor 0.30→0.35.
- **v156**: smooth_rate prior_weight 4.0→2.0 is the single fix unblocking ALL v144-v152 detectors (11 callsites, fixture proves 0.5382>0.50 gate). Tune prior_weight BEFORE adding new detectors when symptom = "fires telemetry but doesn't adjust".
- **v156 归档建议**: grep `CALL_MARGIN_FINAL margin_milli!=0 reason=standard_arm` daemon≥30g — <5% ⇒ increase standard-arm deltas themselves (NOT a 4th preflop opponent-sizing variant [POSSIBLY EXHAUSTED]); separately, v144/v145/v146/v150/v151 lack per-hand prior_aggressor_checked_back flags — daemon≥100g no WR lift ⇒ add per-hand flag, NOT another population-mean detector.
- **v155**: INERT detectors fire telemetry but move sizing <2% pot (smooth_rate prior) — require per-hand gating flags (prior_aggressor_checked_back computed from history) before approving population-mean opp signals as "new axes".
- **v155 归档建议**: `_post_missed_cbet_exploit` requires ≥30g paired net-chips from match_replay/ (current 10g = noise) AND a per-hand prior_aggressor_checked_back boolean so the delta fires only on true missed-cbet dynamics, not every to_call==0 turn/river.




