## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip) need ≥30g confidence gates; sub-30g is noise. SB-open/BB-defense uses open-response evidence, not generic confidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate axis at should_fold_postflop [POSSIBLY EXHAUSTED] — v127/v128/v130 added gates that proved INERT; v131/v132 rewired to pot_odds-grounded (pot_odds_required + severity) but v131 REGRESSED 0.5706→0.5306@360g. barrel_pressure_profile + check_raise_pressure were removed in v132 then RE-WIRED by v133 (both LIVE again: defs strategy_helpers.py:326/356, 3 call sites each at strategy.py:640/892/995 + 656/895/998) — so the "removed" claim is FALSE for current HEAD; firing effectiveness still unconfirmed. Open mandate = real-world firing verification via persistent fixture, NOT another detector or threshold tune.
- v129 turn_second_barrel_planner is the VALIDATED OFFENSE pivot — now PROVEN AND WIRED INTO HEAD (v133 import strategy.py:35, dispatch 1300-1315): wins 5/6 ≥30g modern-field matchups (v96 0.60@30g, v95 0.55@40g, v93 0.53@30g, v110 0.55@40g, v113 0.575@40g; loses only v97 0.40@20g). Strongest-by-H2H-evidence. NOTE: v129 Glicko r/rd is disputed across sources (1513/79, 1526/92, 1438/73, current file 1438/rd=350 UNRATED) — rely on ≥30g H2H win-rates, not the rating number.
- Dispatch-order inertness: turn barrel primitives MUST sit at strategy.py:1300, before passive_exploit:1372 — verify via daemon H2H raise-frequency lift (target ≥40% from ~33.5% baseline), NOT code-presence grep.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Detection-without-handler is recurring dead code: every new detector must meet the canonical 6 birth requirements (BLUFF_CALIBRATION). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. FOLD_GATE_FIRE stderr is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED]; BLUFF_OPP_THRESHOLD was REMOVED in v132 (0 grep hits) — do not re-add that constant axis.
- Fold-gate axis rewired to pot_odds-grounded in v131/v132 (STRUCTURALLY SUSPECT, v131 REGRESSED @360g); corrective CALIBRATION beats new gate functions but REQUIRES ≥100g H2H improvement + real firing evidence before commit. check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). BOUNDARY: crossover is legitimate for genuine gene-merging (v130 v118×v128); the anti-pattern is reflexive fallback when Master could ship a NEW detector.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v130 critic "v118 weaknesses" = <30g noise; v132 critic H2H refs ≤30g; v133 critic H2H refs all ≤10g = noise).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY RULE: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; ensure reviewer diff-summary reconciles against the actual diff. Cited replays (G0H28/G4H12/G2H51) must be cross-checked against experience_pool.md + match_replay/ before building a gen (fabricated/untraceable evidence recurred v127/v129/v130 critic; assume false until verified).
- Lineage/HEAD hygiene: reaped bots' FUNCTIONS can persist in HEAD via crossover and be toggled across gens (barrel_pressure_profile/check_raise_pressure: v128 add → v132 remove → v133 re-wire LIVE) — re-grep HEAD for live symbols before anchoring; a stale comment ≠ live code, and a "removed" claim ≠ gone (v133 reversed v132's removal).

## RECENT_LESSONS
- **v134**: DISPATCH-ORDER SHORT-CIRCUIT RISK: when wiring a new value-sizing helper into strategy.py, call-site ORDER matters as much as function logic. v134 places value_maximizer_overbet BEFORE river_value_raise_tier (1292 vs 1299) and BEFORE turn_second_barrel_planner (1316 vs 1323) — when vm_index>=0.75 fires, it shadows the validated v129 turn-barrel primitive. Future Master plans must specify call-site ordering relative to validated primitives, not just 'wire into N sites'.
- **v134**: PARAMETER_TUNING EXEMPT can be earned via NEW opponent-signal gating even when underlying sizing tiers (0.75-1.20x pot) are familiar — useful pattern when constant-tuning axes are exhausted but a fresh opponent-model signal is available.
- **v134 归档建议**: After daemon reaches >=100g H2H for v134 vs v130/v131/v133, verify turn+river raise-frequency LIFT (vs the ~33.5% baseline); if v134 regresses specifically vs v129 (whose turn_second_barrel_planner was the validated edge), relocate value_maximizer_overbet to fire AFTER turn_second_barrel_planner at line 1323 so the validated primitive wins on overlapping spots.
- **v134**: Critic evidence: H2H weaknesses: All v133 matchups ≤20 games = NOISE per experience pool (<30g rule). Cannot anchor on confirmed weakness. Strategic principle is sound without H2H anchoring.; Experience pool refs: PARAMETER_TUNING EXEMPT clause: 'OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW' — this change qualifies via NEW value_maximizer_index signal., POSTFLOP_STRATEGY: 'Dispatch-order inertness: turn barrel primitives MUST sit at strategy.py:1300, before passive_exploit:1372' — NEW fn at 1316 is BEFORE turn_second_barrel_planner at 1323 → potential SHADOWING of validated v129 turn-barrel primitive; must verify via daemon raise-frequency telemetry., BLUFF_CALIBRATION: 'CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test' — only 2/6 unambiguously met (NEW detector + NEW signal); 2 wired sites not 3, no cited replays, no fixture.; Diff refs: opponent.py:239-245 adds value_maximizer_index = clamp(call_down_flop_turn_rate*0.25 + call_down_turn_river_rate*0.35 + turn_sticky*0.20 + river_sticky*0.20) — turn/river-weighted, distinct from passivity_score., strategy_helpers.py:480-540 NEW value_maximizer_overbet(): 6 gating conditions (round_idx>=2, to_call==0, confidence>=0.25, vm_index>=0.75, nutted_risk<=0.05, non-dynamic board) + 3 tier sizing branches + ultra-sticky +0.05 boost., strategy.py:1292-1298 NEW river call site (BEFORE river_value_raise_tier at 1299) and 1316-1322 NEW turn call site (BEFORE turn_second_barrel_planner at 1323) — dispatch-order shadowing concern.
- **v133**: Crossover-with-small-principled-mutation works when the donor primitive has ≥30g-confirmed modern-field edge (v129 wins 5/6 ≥30g); re-imported turn_second_barrel_planner AND re-wired barrel_pressure_profile + check_raise_pressure (all LIVE in HEAD now). NEXT: verify ≥100g daemon H2H vs v129/v118/v131 (NOT v111/v116/v121 = 20g noise) before next gen.
- **v133**: Dispatch-order verification pending — turn barrel at strategy.py:1300 before passive_exploit:1372; if turn-barrel raise freq in TPGK/overpair spots stays at ~33.5% baseline, revert the made_strength 0.50→0.45 floor to isolate re-import vs mutation, then wire a 2nd call-site inside choose_raise() for the ≥3-sites requirement.
- **v133** (Master): OFFENSIVE value-tier floor in thin_cap (strategy.py:251-271) — restructured 1 line into a 21-line gate: dry_static_board (wetness≤0.25, not paired) + passive_station (passivity≥0.55, conf≥0.20) → release to 0.52/0.58/0.62. Fits PARAMETER_TUNING EXEMPT clause (NEW passivity_score signal, LIVE opponent.py:229, absent from v132); different axis (sizing) than fold-gate, so the 5th-fold-gate-tune warning does NOT apply.
- **v132**: removed barrel_pressure_profile + check_raise_pressure wiring as dead/inert (⚠️ LATER REVERSED by v133 — see POSTFLOP_STRATEGY/GENERAL). pot_odds-grounded fold-gate applied 2× (v131, v132) and BOTH STRUCTURALLY SUSPECT — future fold-gate attempts MUST ship a persistent firing counter + ≥100g H2H improvement before commit. Recommended next pivot: RE-IMPORT v129 turn_second_barrel_planner (executed in v133); deprioritizes check_raise_freq (9+ gens undelivered).
- **v131**: INERTNESS RESCUE PATTERN — when a fold gate's fixed threshold absorbs its severity signal, rewire to pot_odds-grounded (pot_odds_required + severity) so it couples to bet size; generalizable for future detector gates BUT PROVED STRUCTURALLY SUSPECT after REGRESSION @360g (0.5706→0.5306). Synthetic fixtures verify formula mechanics NOT real-world reachability — a persistent fold-gate firing counter is required.


