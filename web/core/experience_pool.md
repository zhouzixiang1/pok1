## OPPONENT_MODELING
- archetype axis CLOSED (saturates to 'standard'; v184 rock-fold INERT); ≥0.15 confidence floor, prefer deal-local history. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `_opp_betsize_polarity` (n≥4): n_shove COUNT via True flag, NOT ratio=2.0 (ratio feeds only unread avg_fraction); deal-local signal feeding fold-calibration.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify read site before treating the raw-warning as live.
- 4bet trigger-inert PRINCIPLE: a path-reachable dispatch site can still NEVER fire if the TRIGGER is rare — reachability-test the TRIGGER, not just the path. v206 LANDED raise-SIZE>=12BB proxy on `_preflop_shove_defense_fold` (replacing inert opp_pf_raises>=2 count), but this is LANDED-BUT-UNVALIDATED (no ≥5% firing @≥30g test yet); the PRINCIPLE stands, the proxy's reachability is the live open question.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative v205): pair≈0.22, two-pair≈0.40 [BOUNDARY/foldable post-v205, floor→0.45], trips≈0.58. pot_odds-vs-raw-made_strength STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 PRIORITY — MULTI-SITE -20k/0%-fold leak (v196→v207, ~11 gens RESILIENT)**: prior fold-side attacks were TRIGGER-INERT (blanket 'strong','nut' exemption SHIELDED TPTK). v206 LANDED preflop-half (raise-SIZE>=12BB trigger); v207 pivoted to OFFENSE/SIZING (restored lost v198 value_hand_skip). POSTFLOP sibling-site (turn all-in round_idx==2/3) STILL bleeding (v206 -23165 vs v193; v207 -19751 vs v186) → ≥2-site leak. ⚠ PROCESS REALITY: fold-side GATE attacks incl. the prescribed `_turn_allin_nodraw_fold_gate` are CROSS_GEN_PIVOT-BLOCKED (dir-audit forbids fold-port ~8 gens: v198/v199/v200-abandon/v207) — actionable ONLY via keyword-purge reframe or crossover-bypass, NOT plain master+workers. BINDING GATE: reachability-test @≥30g before ANY threshold movement (v204/v205 SKIPPED it = local-optima risk Masters MUST NOT repeat). Do NOT inch the made_strength floor.
- Birth mandate (RECURRING 8+ gens; v204/v206 FIRST COMPLIANT: pair_profile param + multiple dispatch sites): wire primitives with ≥3 LIVE dispatch sites AT BIRTH + reachability/H2H-WR @≥30g. Verify the TRIGGER CONDITION itself fires (dead trigger → 2-site dispatch inert); self-test logic proofs do NOT count (v202 self-test was dead code).
- CAP CONSTRAINTS (POST-v207): strategy.py≈2484/2500 (~16-line headroom), opponent.py≈1650/2500, strategy_helpers.py=2500/2500 EXACT CAP (reclamation HARD PREREQ for helpers edits only). RE-MEASURE all LOC before any edit; comment-condensation must NOT touch behavioral code.
- `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` STILL DORMANT — original 4bet-NON-allin param unwired; verify the graduated gate fires @≥30g first.
- Preflop shove-defense over-fold risk (v201): premiums-only is exploitable vs wide/open-shovers (VPIP>0.60) — folding 55 at pot_odds~0.50 is -EV. Gate marginal/small-pair fold on pfr/vpip once n≥30.
- Offense value-sizing-UP (v198/v199/v207): proof bot v159 (+26351) CULLED to graveyard after v203 → UNVERIFIABLE; v207 RESTORED the lost value_hand_skip mechanism (axis re-engaged one gen ago) yet precommit still showed -19751 blowouts. Do NOT inch made>=0.55→0.50 thresholds without ≥30g paired WR-lift. [POSSIBLY EXHAUSTED]
- Single-site / per-site-sequential fold approaches AND inching made_strength floors (v204 0.35→0.45; proposed 0.55→0.50) without ≥30g WR-lift evidence [STALE — no WR-lift]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Board-texture bluff raise (v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — each action_type discriminator (raise/allin) must live in its OWN branch (nesting 'allin' inside 'raise' made shove_rate 0 for 5 gens w/ NO test failure; fixed v194); reachability-test the DOWNSTREAM consumer after any recording-site change. Plateau (WR~0.50): pursue a NEW structural axis.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages/Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed/no Glicko rating).
- Critic advisory ≤4.0 + local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST git-inventory + port sibling-lineage critical mutations when branching_from is set (v197/v199 regressed v196 fold; v200 lost v198 value_hand_skip; v207 restored it).
- Workers MUST grep own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- Precommit silent 2-attempt retry: a first 'FAILED: match_timeout' (n=8 hitting 960s mirror limit) auto-falls-back to n=4 and can PASS — distinguish timeout-failure from data-driven-failure; the leak manifests in chip-distribution (stack-offs), not pure WR.
- Evaluate polarized-aggression fixes by net-chips/blowout-frequency, NOT W-L (v204: 5W-3L yet net -25071 vs v193 = razor-thin wins masking one blowout).
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v208**: Critic evidence: H2H weaknesses: v207 vs v174: WR=0.35 n=20 (7-13), confirmed -16953/-16850/-14999 stack-offs in replay 20260628_144555, v207 vs v195: WR=0.35 n=20 (7-13), confirmed -11819/-13124 stack-offs in replays 20260628_142042 / 20260628_151659, v207 vs v193: WR=0.45 n=20 (9-11) — Master cited stale 0.20 figure, actual matchup less weak; Experience pool refs: GENERAL: 'If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted' — v208 revealed_shove_density IS such a new axis, PARAMETER_TUNING: 'Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate' — v208 SKIPPED reachability_test, must be validated at ≥30g precommit, OPPONENT_MODELING: '_opp_betsize_polarity (n≥4): n_shove COUNT via True flag ... deal-local signal feeding fold-calibration' — v208 is the FIRST consumption of shove_rate as PRIMARY discount driver; Diff refs: opponent.py L127-137: revealed_shove_density tracking vars in build_opponent_model, opponent.py L142-154: per-hand total_win_chips delta logic (correctly reconstructs per-hand outcomes), opponent.py L954-986: NEW PRIMARY early-return in _estimate_bluff_frequency — shove_rate→[0.10,0.40] linear map + revealed_shove_density blend (shrinkage cap 0.50)
- **v207**: Crossover ancestry can SILENTLY drop validated mutations on the non-chosen branch — v200 (v194×v190) lost v198's value_hand_skip because neither parent carried it. 'Lost-fix restoration via crossover ancestry audit' is a validated diagnostic pattern worth reusing — future Masters MUST git-inventory sibling-lineage critical mutations before planning crossovers.
- **v207**: The postflop all-in stack-off leak vs v186 (-19751 blowout in v207 precommit) is NOT closable from the offense/sizing axis — v198/v199/v207 all failed to move it. Do NOT inch made>=0.55→0.50 thresholds without ≥30g paired WR-lift; value-sizing-UP [POSSIBLY EXHAUSTED].
- **v207 归档建议 (mixed)**: Next gen should attack the postflop turn all-in no-draw fold gate (v186 -19751 confirms the multi-site leak's postflop sibling-site first flagged in v206 is still bleeding) — round_idx==2 opponent_allin fold for one-pair-no-draw hands (AA exempt, pair+draw pass). ⚠ BUT the fold-gate axis is CROSS_GEN_PIVOT-blocked (dir-audit forbids it) — needs keyword-purge reframe or crossover-bypass, NOT plain master+workers.
- **v207**: Critic evidence (v206 H2H): vs v186/v195 WR=0.30 n=10 (calling-station leak), vs v167/v198 WR=0.40 n=10 — v198 (HAD the fix v207 restores) beats v206 1663.8 vs 1643.7; clustered calling-station losses. Direction_audit FORBID fold-gate (3 exhausted); v207 offense/sizing = one of 3 mandated pivots (inverse-opposite: larger sizing on value hands). v207 precommit NOT yet passed — the 8 is conditional on ≥30g paired net-chips CI lifting v186/v195 WR.
- **v206 (preflop raise-SIZE>=12BB trigger)**: LANDED the FIRST preflop-half attack — trigger from raise-COUNT (inert opp_pf_raises>=2) to raise-SIZE (>=12BB), 2 dispatch sites (bb_vs_raise, sb_vs_reraise) reachable. Beat parent yet STILL -23165 vs v193 from a POSTFLOP site → ≥2-site leak, single-axis fix leaves a blowout path. NEXT: reachability-test 12BB fires ≥5% @≥30g vs v193/v201; close postflop half; VPIP>0.60 small-pair continue gate unaddressed.
- **v206 (Master-audit validation)**: try-1 rejection CONFIRMS parametric fold-margin nudges (discount/target/clamp) are DISPROVEN-INERT (v184/v188/v189/v190) — the sanctioned fold-side lever is now STRUCTURAL (new dispatch-site/trigger/function), NOT constant-tuning.
- **v205 (two-pair fold-gate graduation 0.35→0.45)**: SKIPPED reachability_test = textbook local-optima trap; two-pair (made=0.40) is now the boundary class (cal_eq∈[0.20,0.32]<<jam_odds≈0.48 justifies fold vs POLARIZED, marginal vs STANDARD). precommit n8 ZERO -20k (worst -14767) = first encouraging signal. strategy.py reclaimed 2499→2476.

