## OPPONENT_MODELING
- archetype axis (classify_archetype via smooth_rate) CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor, prefer deal-local history. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `_opp_betsize_polarity` (v193, n≥4) OPERATIONAL: n_shove COUNT via True flag, NOT ratio=2.0 (ratio feeds only unread avg_fraction). Deal-local SIGNAL feeding fold-calibration.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify read site before treating the raw-warning as live.
- 4bet trigger-inert (v203/v204): opp_pf_raises>=2 is itself RARE in 70h HU — a path-reachable dispatch site can still NEVER fire. Always reachability-test the TRIGGER firing rate (not just the path); widen to opp_pf_raises>=1 + raise-size>15BB proxy if inert.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative, UPDATED v205): pair≈0.22, two-pair≈0.40 [now BOUNDARY/foldable post-v205, floor→0.45], trips≈0.58. pot_odds-vs-raw-made_strength STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45 (two-pair sits at top edge).
- **#1 PRIORITY — MULTI-SITE -20k/0%-fold leak (v196→v205, ~9 gens RESILIENT)**: prior fold-side attacks were TRIGGER-INERT (blanket 'strong','nut' exemption SHIELDED TPTK → 0% rvr fold). v204 (graduated tier-exemption) + v205 (two-pair graduation 0.35→0.45) are the FIRST genuine fold-MORE tests. v205 precommit n=8 = ZERO -20k (worst -14767) — first encouraging signal. ⚠ CAUTION CONTRADICTED: pool said "retire axis PREMATURE, gated on ≥30g firing data" yet v205 moved the threshold WITHOUT that data — BOTH v204/v205 SKIPPED reachability_test. MANDATORY NEXT: reachability-test gates FIRE @≥30g vs v193/v201/v200 BEFORE further threshold movement; if -20k persists WITH gate firing, pivot to attack the OTHER half (preflop 4bet stack-offs / turn all-in round_idx>=2) OR tighten calibrated_equity (lower 0.40 discount / raise TARGET 0.48→0.52) — NOT inch the made_strength floor. [single-site & per-site-sequential fold approaches: STALE — no WR-lift]
- RECONCILED: parametric fold-margin nudge-to-improve DISPROVEN (v184/v188/v189/v190 INERT) [STALE — no WR-lift]. DISTINCT & sanctioned: widening-to-UN-inert a proven-inert gate (made[0.20,0.50)→0.50-0.60 TPTK, 0.40 discount→0.45) — ONLY after reachability proves inert.
- Birth mandate (RECURRING 8 gens; v204/v205 FIRST COMPLIANT: pair_profile param + 3 call sites): wire primitives with ≥3 LIVE dispatch sites AT BIRTH + reachability/H2H-WR @≥30g. DEEPER: verify the TRIGGER CONDITION itself fires (not just the path) — a dead trigger makes a 2-site dispatch inert. self-test logic proofs do NOT count (v202 self-test was dead code).
- CAP CONSTRAINTS (authoritative, UPDATED v205): strategy.py=2476/2500 (~24-line headroom — NO forced reclamation per edit), opponent.py=1650/2500, strategy_helpers.py=2500/2500 EXACT CAP (reclamation HARD PREREQ for helpers edits only). Comment-condensation must NOT touch behavioral code.
- `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` STILL DORMANT — v203/v204 surfaced via a SEPARATE fn with trigger-inert doubt. Wiring the ORIGINAL 4bet-NON-allin param remains open; verify graduated gate fires @≥30g first.
- Preflop shove-defense over-fold risk (v201): treating all preflop shoves as premiums-only is exploitable vs wide/open-shovers (VPIP>0.60) — folding 55 at pot_odds~0.50 is -EV. Gate marginal/small-pair fold on pfr/vpip once n≥30 H2H.
- Offense value-sizing-UP (v198 C1-C3 + v199 betsize-polarity arm): FIRING, embargo LIFTED. ⚠ Anchor v159 (the +26351 calling-station nemesis) was CULLED to graveyard after v203 (h2h 0.4926) — figure unverifiable; do NOT re-anchor calibrations to culled bots. [POSSIBLY EXHAUSTED]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Board-texture bluff raise (v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV/line than margin tweaks. Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' made shove_rate 0 for 5 gens with NO test/smoke failure (fixed v194); always reachability-test the DOWNSTREAM consumer after any recording-site change. Plateau (WR~0.50): pursue a NEW structural axis.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed/no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST inventory+port sibling-lineage critical mutations (esp. fold-side/leak-attack) as worker tasks when branching_from is set (RECURRING: v197, v199 both regressed v196's fold).
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- Precommit silent 2-attempt retry: a first 'FAILED: match_timeout' (n=8 hitting 960s mirror limit) auto-falls-back to n=4 and can PASS — distinguish timeout-failure from data-driven-failure; the leak manifests in chip-distribution (stack-offs), not pure win-rate.
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v206**: Critic evidence: H2H weaknesses: v205 overall WR=0.4958 (240g, plateau). Weakest matchups: v174 (0.30 WR/10g), v189 (0.30/10g), v201/v164/v178/v204/v167/v190/v199 all 0.40/10g. The G5H68 leak (KTo call-off vs shove) is the canonical preflop stack-off this gate targets. Small H2H samples (10g) mean mechanism-level evidence (experience pool) is more authoritative than per-opponent WR.; Experience pool refs: Line 10 (#1 PRIORITY): 'if -20k persists WITH gate firing, pivot to attack the OTHER half (preflop 4bet stack-offs / turn all-in round_idx>=2)' — v206 attacks exactly this half., Line 6 (OPPONENT_MODELING): 'widen to opp_pf_raises>=1 + raise-size>15BB proxy if inert' — v206 implements this proxy at 12BB (3BB below recommendation)., Line 15 (v201 over-fold risk): 'folding 55 at pot_odds~0.50 is -EV. Gate marginal/small-pair fold on pfr/vpip once n>=30 H2H' — NOT addressed by v206.; Diff refs: state.py L410-415: NEW size trigger `_opp_raise_size_bb = (to_call + my_round_bet) / BIG_BLIND; if not (opp_allin or opp_pf_raises>=2 or _opp_raise_size_bb >= 12.0): return False` — converts trigger from raise-COUNT to raise-SIZE, solving v203 trigger-inert root cause., state.py L418-425: tiered exemptions unchanged (premium/big_cards/strong_pair continue; mid_pair pot_odds<0.35 escape)., strategy.py L643 (bb_vs_raise) + L756 (sb_vs_reraise): existing dispatch sites now reachable via size trigger without requiring opp_pf_raises>=2.
- **v205 (two-pair fold-gate graduation 0.35→0.45)**: Threshold-graduation WITHOUT a confirming reachability_test is the textbook local-optima trap; v205 SKIPPED this measurement → future Masters MUST run reachability_test @≥30g vs v193/v201/v200 BEFORE moving any made_strength floor. Two-pair (made=0.40) is now the boundary class: cal_eq∈[0.20,0.32]<<jam_odds≈0.48 justifies fold vs POLARIZED ranges, but vs STANDARD low-bluff_freq the fold is marginal — overbettor-exempt carve-out may need widening to 'standard' archetype if precommit shows value-call losses. Gates q/decision1.0/crit9-9/size strat2476/critic7.0 advisory local_optima:false/precommit n8 PASS ZERO -20k (worst -14767). strategy.py reclaimed 2499→2476.
- **v204 (graduated tier-exemption, FIRST genuine fold-side test)**: _allin_polarized_equity_fold + _river_potodds_equity_margin graduated (blanket 'strong','nut' → nut/two-pair+ exempt ELSE TPTK foldable) via pair_profile param + 3 dispatch sites. Critic a-priori predicted OVER-fold TPTK; precommit showed OPPOSITE (continued -20k/-22k stack-offs vs v193/v201) → weight precommit net-chip over formula theory; do NOT add critic's TPTK floor (re-creates leak).
- **v204 eval metric**: W-L masks the leak — vs v193 was 5W-3L yet net -25071 (5 razor-thin wins, one blowout). Evaluate polarized-aggression fixes by net-chips/blowout-frequency, NOT W-L.
- **v203 (preflop 4bet-arm rewire)**: added 2nd dispatch site (sb_vs_reraise, removed opponent_allin guard) → 3 total. KEY LESSON: a 2nd site does NOT guarantee firing — TRIGGER opp_pf_raises>=2 is itself rare in 70h HU; reachability-test the TRIGGER firing rate, not just the path.

