## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — they drift), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20) (since v134). NEW gating with it = PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- classify_sizing_tendency() (v137) = 4-bucket overbet/underbet classifier. PENDING-VERIFICATION: code-present + consumed at wired sites, NO firing-rate fixture. Do NOT yet treat as first-class gateable stat; do NOT label dead/inert or remove. Graduate to LIVE via persistent firing-rate fixture.

## POSTFLOP_STRATEGY
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires on absent branches REGRESSED (0.5706→0.5306@360g); v138/v139 `_river_stackoff_guard` ran AFTER `if to_call >= my_chips:` early-return = structurally INERT. Fold-side FORBIDDEN by dir-audit (HIGH-conf v140). 🔴 SYSTEM-LEVEL RISK: 0%-fold + -20k stack-off leak still LIVE; fold-side FORBIDDEN removes the only direct fix — tracked unresolved tension (BET-side offense only addresses the -20k half).
- BET-side offense pivot (v140 `_river_value_ship_guard` → v141 `_river_value_extraction_amplifier` → v142 `_vulnerable_made_protection_floor` → v143 `_river_bet_commit_guard` wrapper) = the ONLY sanctioned direction since v140. Need ≥100g H2H to confirm.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier), NOT before. Codified invariant: higher-confidence primitives fire FIRST (vm_overbet@0.75), lower-confidence AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- `barrel_pressure_profile` + `check_raise_pressure` code-present (re-wired v133), firing-UNVERIFIED. Verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v139 FIXTURE≠LIVE lesson). v140 first to comply (6/6 birth reqs + grep≥3x); v141/v142/v143 continued. Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs. Status tiers: NO wired consumers = dead code; consumers but no firing proof = PENDING-VERIFICATION; firing proven = LIVE. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures).
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap, BLUFF_OPPORTUNITY_THRESHOLD) [POSSIBLY EXHAUSTED] — live `BLUFF_OPPORTUNITY_THRESHOLD = 0.55`; require ≥100g H2H vs v96/v114 before further tuning; if it doesn't recover ≥half the v111-vs-v134 gap, declare axis dead.
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). Master plans STILL may violate the fixture mandate — the fixture gate is the enforcement, not Master output. Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v139 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (9th recurrence v127-v143; v143 cites G3H25/G2H44 which DON'T EXIST — actual files are timestamp-based, e.g. 20260620_160503_claude_v77_vs_claude_v128.json). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v143**: v140 _river_value_ship_guard remains unvalidated at ≥100g H2H through v140→v143 (4 generations, rd=90) — Master should NOT extend this defensive pattern further until daemon rates v143 vs aggressive barrelers v96/v126 and worst-swing shifts from -19k to ~-9k.
- **v143**: Fabricated replay citations (G3H25/G2H44 style) hit 9th recurrence — Workers must `ls web/core/results/match_replay/` before citing replays; only timestamp-based filenames (e.g. 20260621_174306_*.json) are valid.
- **v143 归档建议**: Before extending the ship-guard pattern again, wait for daemon to rate v143 at ≥100g vs v96/v126 barrelers; if worst-swing doesn't improve from -19k to ~-9k, relocate the guard upstream of _vm_overbet/_rva/_rvr (call-site fix, not re-tuning) per the experience-pool directive.
- **v143**: `_river_bet_commit_guard()` wrapper (strategy_helpers.py) — early-exits on non-river/anti_lock/bet<25%stack, delegates to v140 `_river_value_ship_guard`, returns 0 if blocked else bet unchanged. 4 wired sites (strategy.py: river bet path, RVA path, RVR path, choose_raise path-unconditional to avoid flop/turn regression); all fall through to lower-tier logic or `return 0`. Fixture 5 cases. Critic weakness refs (4 matchups at 30% WR vs v96/v126/v130/v81) ALL sub-30g NOISE; no ≥30g confirmed weakness. Continues BET-side sanctioned direction, addresses -20k stack-off half of the live leak. 🔴 critic cited G3H25/G2H44 which DON'T EXIST (timestamp-based) — fabricated-replay, 9th recurrence.
- **v142**: `_vulnerable_made_protection_floor()` heavy eligibility (made_str 0.45-0.78 + draw_pressure≥0.40 + vm_idx≥0.50 + passivity≥0.55 + fold_to_raise<0.50). LIVE verification = `grep PROTECT_FLOOR` stderr after daemon≥100g; if <1% flop/turn bets fire, relax fold_to_raise<0.50→<0.55 OR drop conf floor 0.20 (DO NOT re-tune sizing). SITE A (strategy.py L1277) mutual-exclusion with v140 ship_guard requires ship_guard stay round_idx==3 — codify invariant. Archiving: grep PROTECT_FLOOR vs sticky opps (v131/v135/v137); v141 nemesis-weak vs v131 (6-16) — value depends on lifting WR vs those exact sticky opps on flop/turn draw-heavy boards.
- **v141**: Offensive additions to opp profiler (v137 classify_sizing_tendency → RVA `_river_value_extraction_amplifier`). MUST clear 3 gates post-commit: (1) RIVER_VALUE_AMP telemetry ≥1% river bets over daemon≥100g, (2) vm_idx 0.55-0.74 unique-band verification (not shadowed by vm_overbet/river_value_raise_tier), (3) WR lift vs sticky opps (v131/v135/v137) vs v140. Arbitrary thresholds on a NEW opp signal need H2H grounding or sub-30g reads = NOISE. Archiving: if <1% river bets OR no WR lift over v140, drop unknown-sticky branch + tighten vm_idx 0.55→0.60.

