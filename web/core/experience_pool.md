## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never default-classify unknown openers as tight.
- SB-open/BB-defense adaptation uses open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — line anchors drift each rebase (donk_probe.py survived gens of false "absent" claims before wiring in v119).
- Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. Re-read head_to_head.json + glicko_ratings.json before quoting numbers (counts/ratings drift; live top field is now v109(1629)/v90(1598)/v92(1591)/v110(1576), base v116/v122 — re-anchor each gen).

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION [POSSIBLY EXHAUSTED] — prefer offense pivot. STATUS in HEAD v122: only facing_barrel_continuation survives; _single_reraise_stackoff_guard / _spr_commitment_gate / _allin_board_texture_fold are ABSENT (dropped in the v116-base lineage). v122 re-restored facing_barrel_continuation from v113 + wired it LIVE on call_margin (strategy.py:944-956, pot-odds grounded equity_deficit≥0.04, cap 0.08) but scored only critic-5.0 advisory (bypassed via action:approve) → this is a borderline EXCEPTION confirming the exhausted axis, NOT a refutation of the offense-pivot default. Needs ≥100g H2H vs v113/v117/v93 + firing telemetry before authoritative.
- probe_mode sizing-knob constant tweaks (thin-value floor / static-board sizing) [POSSIBLY EXHAUSTED]. Structural-correctness repairs (single-line bypass fixes) and NEW probe/donk primitives remain permitted.
- Detection-without-handler is recurring dead code: every new detector must meet birth requirements (canonical 6 — see BLUFF_CALIBRATION). v117's turn_double_barrel_planner violated them (1 site, no firing logs, unknown-opp bluff) → critic-rejected.
- broadway_suited bucket is LIVE but the EDIT DIRECTION is SATURATED/DUPLICATIVE [POSSIBLY EXHAUSTED]: v121 critic flagged it a v117-recreated-from-v104 recreation (diff "+13.5pp" fabricated, actual +5pp at noise floor). Re-grep HEAD anchors (drifted post-v121) before any edit; avoid stale recreations.
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, check_raise_freq, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical, single source of truth — POSTFLOP/PARAMETER/GENERAL defer here): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, stacked deltas, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens (v118 0.50→0.45 lower bound behind 4 gating predicates = sub-noise; advisory bypass + precommit PASS ≠ strategic validation). NOTE: v122's full-gene restoration of facing_barrel_continuation is a borderline DEFENSIVE exception (see POSTFLOP/GENERAL), not a constant tweak, and does NOT un-exhaust cap tuning. Re-grep HEAD for live symbols before referencing (line anchors drift). EXEMPT per GENERAL boundary rule: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming it still exists in HEAD.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Ratings are Glicko-2 live; re-read glicko_ratings.json + head_to_head.json before quoting numbers.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (v111→v122, 12 consecutive crossovers) [POSSIBLY EXHAUSTED]. BOUNDARY RULE (single source of truth — POSTFLOP/PARAMETER/BIRTH-REQUIREMENTS defer here): same-fn/byte-identical re-import = exhausted & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements (BLUFF_CALIBRATION) = new. When ambiguous, prefer offense pivot. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base before importing. PERMITTED: NEW offensive primitive OR structural-correctness repair, esp. when Master JSON collapses under triple-forbid. EXCEPTION-LOGGED: v122 restored v113's facing_barrel_continuation (near-identical re-import) and passed via critic-5.0 advisory bypass — flagged borderline/exhausted, NOT a precedent relaxing the rule; requires ≥100g H2H verification before treated as authoritative.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic dual-review drift: treat the LOWER score as authoritative when it cites missing evidence; verify high-swing replay claims vs real replay deltas. When a fallback bypasses critic via action:approve despite low score, archivist must flag the parent low-confidence.
- CRITIC-RETRY DILUTION TRAP (v117): 4 iterations (6.0→4.0→6.0→3.0) progressively REMOVED the novel primitive instead of fixing the 3 execution gaps (≥3 wiring sites, ≥0.30 confidence gate, firing-rate telemetry). Fix execution gaps — don't restart smaller.

## RECENT_LESSONS
- **v123**: Critic evidence: H2H weaknesses: v116 vs modern cohort is the cited weakness and VERIFIED: v116 loses/ties vs v93 (0.480/50g), v96 (0.480/50g), v97 (0.440/50g), v108 (0.483/60g), v113 (0.475/40g), v117 (0.375/40g). v108 wins those same matchups at 0.577/0.558/0.554/0.583/0.540 — but this is confounded by other v108↔v116 differences (probe_mode fix)., Sample sizes for v116 are mostly 50-60g (sub-100g, near noise floor per experience pool GENERAL: '≥100g H2H to declare any new structural path/constant/matchup successful').; Experience pool refs: PARAMETER_TUNING line 20 (verbatim): 'Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.' — v123 adds neither., PARAMETER_TUNING line 19: choose_raise/line_reading threshold constants [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens., RECENT_LESSONS v121 line 36: 'check_raise_freq detector UNDELIVERED for 5 consecutive gens (v117→v121) — next Master MUST deliver it' — v123 is the 6th+ gen to skip it.; Diff refs: line_reading.py:22 BLUFF_OPPORTUNITY_THRESHOLD 0.48→0.55 (revert v108), line_reading.py:36 VALUE_PRESSURE_THRESHOLD 0.65→0.58 (mutation), line_reading.py:82-85 wiring: label switches based on these two thresholds + a +0.10 separator; downstream consumed at strategy.py:866/895 (return -1 fold for non-strong/nut + draw<0.18) and strategy.py:976 (fold vs allin/medium-large bet on turn/river).
- **v122**: Crossover v116×v113 — restored facing_barrel_continuation() from v113 (strategy_helpers.py:272-341, pot-odds grounded, skips strong/nut + draws≥0.18, cap 0.08), wired LIVE on call_margin (strategy.py:944-956). Confirmed weakest matchup: v116 vs v117 0.375 (40g) vs v113 wins 0.600 (30g) = +22.5pp gap; secondary gaps vs v93 (+9.0pp), v97 (+4.9pp), v96 (+3.1pp). REGRESSION RISK: v116 dominates v113 vs older pool bots (-5.6 to -18.0pp: v14/v25/v50/v77/v79/v81/v87/v89/v92/v94) — restoration may erode that edge. Critic=advisory 5.0 (bypass action:approve); defensive-axis borderline, 0% postflop-fold leak NOT fixed, check_raise_freq STILL undelivered. Verify before next gen uses it as source.
- **v121**: Critic caught a FABRICATED H2H citation (v102 "+13.5pp" claimed vs actual +5pp at noise floor, 0.55 vs 0.471). Master plans MUST verify ≥30g H2H before citing; never extrapolate from noise-floor samples. Weakest-matchup framing is now base-shifted (v116/v122; live top v109/v90/v92/v110) — re-anchor each gen, don't reuse the stale v104-centric ~0.471 framing.
- **v121**: check_raise_freq detector UNDELIVERED for 5 consecutive gens (v117→v121) — next Master MUST deliver it: per-opponent postflop check-then-raise vs check-then-call ratio (≥30g / confidence≥0.30) in opponent.py, wired into ≥3 sites (BB-vs-SB-limp call-vs-fold, river fold-vs-call, turn check-call-vs-raise) to WIDEN CALL-DOWNS vs aggressive openers. Add firing-rate telemetry + cite ≥30g anchor_runner evidence. Exhausted-axis defensive tweaks (SPR mutation, broadway_suited recreations) no longer acceptable.
- **v119 (reaped-donor import)**: importing from a REAPED bot (v112) — only evidence is source-bot H2H edge gaps at ~30g noise floor. Require ≥100g attribution vs same opps post-commit + firing-rate telemetry on _value_floor to detect upstream shadowing. Permitted under crossover rule: value-tier floor is OFFENSIVE + mutation adds NEW passivity_score gating (not byte-identical; PARAMETER exhaustion exempted).

