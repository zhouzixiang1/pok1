## OPPONENT_MODELING
- Use live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) only behind confidence gates (≥30g); sub-30g is noise. Never classify unknown openers as tight by default.
- SB-open/BB-defense adaptation must use open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — don't trust memory/commit claims (donk-bluff primitives repeatedly claimed-but-absent across v116/v117).
- Master plans must cite weakest matchups with concrete ≥30g H2H evidence (head_to_head.json: 418/435 pairs ≥20g) + reference offensive targets; do NOT fabricate in-flight matchups.

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION (facing_barrel_continuation / _single_reraise_stackoff_guard / _spr_commitment_gate / _allin_board_texture_fold) [POSSIBLY EXHAUSTED] — symbols CURRENTLY ABSENT from HEAD (dropped via crossover rebase). Byte-identical re-import FORBIDDEN, genuinely-new def discouraged — prefer offense pivot.
- probe_mode sizing-knob constant tweaks (thin-value floor / static-board sizing) [POSSIBLY EXHAUSTED]. Structural-correctness repairs (single-line bypass fixes) and NEW probe/donk primitives remain permitted.
- Detection-without-handler is recurring dead code: every new detector must meet birth requirements (canonical 6 — see BLUFF_CALIBRATION). v117's turn_double_barrel_planner violated them → critic-rejected.
- Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, overlapping handler order, and trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may indicate calling-station behavior.
- NEW OFFENSIVE primitives (opponent-line river raise-bluff, check-raise freq, line-polarisation-gated barrels, value-heavy donk-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. BIRTH REQUIREMENTS (canonical, single source of truth — POSTFLOP/GENERAL reference this): NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + firing-rate logs — else dead code.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise window/slope, wetness caps, induce_cap/thin_cap/low_ratio, stacked deltas, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated for pure constant tweaks ≥6 gens; re-grep HEAD for live symbols before referencing (line anchors drift).
- Threshold-only nudges on adjacent gates (line_reading bluff thresholds, pot_odds bounds, value_pressure firing threshold) count as constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound — after confirming the constant still exists in HEAD.

## GENERAL
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare any new structural path/constant/matchup successful. Ratings are Glicko-2 live; re-read glicko_ratings.json before quoting numbers.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback (v111-v118 consecutive) [POSSIBLY EXHAUSTED]. Boundary rule: same-fn/byte-identical re-import = exhausted; new fn + new opponent-line signal + birth requirements (see BLUFF_CALIBRATION) = new — when ambiguous, prefer offense pivot. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base AND on eligible-retry list before importing. FORBIDDEN: recombining defensive genes via byte-identical fold-gate imports. PERMITTED: NEW offensive primitive OR structural-correctness repair, esp. when Master JSON collapses under triple-forbid. Hard-block constant-only edits to barrel-continuation fns.
- broadway_suited bucket re-import as a new def [POSSIBLY EXHAUSTED] — ACTIVELY BRANCHED live at 4 sites in strategy.py (lines 368/402/412/537, e.g. `if hand_cat=='broadway_suited' and not trash_hand`); NOT inert headroom. New opponent-signal-gated edits + ≥3 wired sites permitted; treat existing branches as live logic.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants within it; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge source. Direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but the def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic dual-review drift: when the same diff scores divergently, treat the LOWER score as authoritative when it cites missing evidence; verify high-swing replay claims vs real replay deltas. When a fallback bypasses critic via action:approve despite low score, archivist must flag the parent low-confidence.

## RECENT_LESSONS
- **v119**: Critic evidence: H2H weaknesses: v116 weakest matchups (all 30g, small sample): vs v108 0.400, vs v96 0.400, vs v112 0.450, vs v104 0.450, vs v113 0.467, v112 beats v116 on EVERY common opponent with ≥30g: v96 +0.122 (90g vs 30g), v104 +0.083 (120g vs 40g), v113 +0.083 (80g vs 30g), v106 +0.050 (100g vs 30g), v108 +0.070 (100g vs 30g), Direct v112 vs v116: v112 wins 0.55 (40g) — v112 is genuinely better; v116 lacking the value-tier floor is the most plausible structural difference (per memory: 'ONLY structural diff v112 has v116 lacks'); Experience pool refs: PARAMETER_TUNING flags choose_raise() value-tier floors as [POSSIBLY EXHAUSTED] for pure constant tweaks — but the rule explicitly states: 'Threshold-only nudges count as constant tuning when no new gating/opponent signal is added'. v119 ADDS new passivity_score gating → not exhausted., Crossover-as-default rule FORBIDS byte-identical re-imports of defensive fold-gates; value-tier sizing floor is OFFENSIVE (raises bet sizing for nut hands) so FORBIDDEN clause does not apply. Import is not byte-identical to v112 (mutation adds +0.06 floor boost gated by NEW signal)., v118/v117 carry-forward directive (check_raise_freq detector, NEW primitive in opponent.py with ≥3 wired sites) REMAINS UNDELIVERED for 3rd consecutive gen (v117, v118, v119). v119 adds passivity_score gating but does NOT add check_raise_freq.; Diff refs: strategy.py:256-292 — single 37-line insertion, no edits to existing code; placement AFTER low_ratio computed, BEFORE clamp(ratio, low_ratio, 1.45) — correctly reaches the binding point, Exemptions preserve existing behavior: semi_bluff, blocker_bluff, probe_mode, inducing_value, thin_cap=None — all paths where small sizing is intentional, Preflop round_idx==0 falls through to `else: _value_floor = low_ratio` → no-op, preserves v116 preflop desired_total logic
- **v118/v117 CARRY-FORWARD DIRECTIVE (offense pivot, UNDELIVERED across 2 gens)**: Build NEW `check_raise_freq` detector in opponent.py (per-opponent postflop check-then-raise vs check-then-call ratio), gate on ≥30g H2H confidence OR opponent_model['confidence']≥0.30, wire into ≥3 NEW sites (BB-vs-SB-limp preflop call-vs-fold, river bet-facing fold-vs-call in should_fold_postflop, turn check-call-vs-check-raise in choose_action_postflop), WIDENING CALL-DOWNS (not folding more) vs aggressive opps v102/v107/v108/v113/v114. Cite ≥30g anchor_runner evidence + add firing-rate telemetry before planning. v118 delivered ZERO of these.
- **v118**: exhausted-axis mutation — river_value_raise_tier lower bound 0.50→0.45 (strategy_helpers.py:291, slope re-anchor ±<1%) behind 4 gating predicates = sub-noise constant tweak. Critic advisory bypass (raw_approved=false, score 4.0) + precommit PASS ≠ strategic validation; treat as process leak requiring structural pivot.
- **v118**: Master plan cited NO ≥30g H2H evidence on target opponents. v108 weakest matchups (≥20g): vs v104 0.446(110g), vs v89 0.460(100g), vs v103 0.473(110g), vs v107 0.480(100g) — NONE cited.
- **v117**: CRITIC-RETRY DILUTION TRAP — 4 iterations (6.0→4.0→6.0→3.0) progressively REMOVED the novel primitive instead of fixing the 3 execution gaps (≥3 wiring sites, ≥0.30 confidence gate, firing-rate telemetry). Fix execution gaps — don't restart smaller.
- **v117**: turn_double_barrel_planner birth-requirement violation — 1 wired site, no firing logs, bluff fired on unknown opps with NO fold evidence; all cited "weak matchups" (10-20g) below 30g noise floor.
- **v116**: offense-via-crossover pivot after Master JSON collapse 2x; claimed _river_valueheavy_donk_bluff() NEW primitive but VERIFIED ABSENT from v116/v117 source — offensive claim never landed; recovery needs birth requirements + grep-verified LIVE primitive.

