## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — drift), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20) (since v134). NEW gating with it = PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- classify_sizing_tendency() (v137) + barrel_pressure_profile/check_raise_pressure (v128/v133) = code-present + consumed but firing-UNVERIFIED for 6-10 gens. ONE RULE for all detectors — no silent grandfathering: back-verify via persistent firing-rate fixture NEXT gen, else declare dead. Do NOT yet treat as first-class gateable stats; do NOT label dead/inert or remove until fixture resolves.

## POSTFLOP_STRATEGY
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires REGRESSED (0.5706→0.5306@360g); v138/v139 ran AFTER `if to_call >= my_chips:` early-return = structurally INERT. Fold-side FORBIDDEN by dir-audit (HIGH-conf v140). 🔴 SYSTEM-LEVEL: 0%-fold + -20k stack-off leak still LIVE; only BET-side offense sanctioned (covers the -20k half).
- BET-side offense pivot (v140 `_river_value_ship_guard` → v141 `_river_value_extraction_amplifier` → v142 `_vulnerable_made_protection_floor` → v143 `_river_bet_commit_guard`) = ONLY sanctioned direction since v140. Need ≥100g H2H to confirm.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier). Invariant: higher-confidence fire FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE (v139 FIXTURE≠LIVE lesson). v140+ comply (6/6 birth reqs + grep≥3x). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs. Status tiers: NO wired consumers = dead; consumers but no firing proof = PENDING-VERIFICATION; firing proven = LIVE. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures).
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 9+ gens elapsed with NO recorded recovery of the v111-vs-v134 gap; anchors v114/v111 now REAPED and absent from the active pool. Kill-switch lapsed with no recorded outcome → treat threshold axis as dead; re-anchor any future work to LIVE opponents, not reaped v96/v111/v114.
- check_raise_freq = SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v139 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (9th recurrence v127-v143; v143 cites G3H25/G2H44 which DON'T EXIST — actual files are timestamp-based, e.g. 20260620_160503_*.json). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v144**: Critic evidence: H2H weaknesses: No ≥30g weak matchup available — v143's worst matchups (vs v14, vs v90 at 0.30 WR) are all n=10 noise; v144 has 0 games. Plateau-rule applies; structural novelty scored under plateau allowance.; Experience pool refs: POSTFLOP_STRATEGY: 'BET-side offense pivot (v140-v143)' — v144 BREAKS the postflop-only streak with preflop sizing, a new dimension., PARAMETER_TUNING: 'EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW' — v144 adds NEW signals (fold_to_open_preflop, threebet_vs_open), qualifying for exemption., OPPONENT_MODELING: 'Live opponent stats ... need ≥30g confidence gates; sub-30g = noise' — v144's `open_response_confidence < 0.25` gate correctly defers firing until ≥4 samples.; Diff refs: strategy_helpers.py: sb_open_opp_sizing_delta() NEW (961-1001) + persistent fixture 6/6 PASS + dispatch-reachability grep PASS, strategy.py:44 import; strategy.py:191 new kwarg preflop_opp_size_bb; strategy.py:274-276 modified desired_total with base floor 2.0BB; strategy.py:459-465 stderr telemetry; strategy.py:479 call-site passes delta to choose_raise
- **v143**: `_river_bet_commit_guard()` wrapper (strategy_helpers.py) extends v140 `_river_value_ship_guard` to the river open-bet path (to_call==0); early-exits non-river/anti_lock/bet<25%stack, delegates to v140, returns 0 if blocked else bet unchanged. 4 wired sites (river bet / RVA / RVR / choose_raise-unconditional) all fall through. Fixture 5 cases PASS, all prior fixtures PASS. quality9/review9/critic6.0 advisory. 🔴 critic weakness refs (4 matchups vs v96/v126/v130/v81 at 30% WR) ALL sub-30g NOISE; 🔴 critic cited G3H25/G2H44 = fabricated (timestamp-based only).
- **v143**: v140 `_river_value_ship_guard` STILL unvalidated ≥100g through v140→v143 (4 gens, rd=90) — do NOT extend ship-guard pattern further until daemon rates v143 vs aggressive barrelers v96/v126 AND worst-swing shifts -19k→~-9k; otherwise relocate guard upstream of _vm_overbet/_rva/_rvr (call-site fix, NOT re-tune).
- **v142**: `_vulnerable_made_protection_floor()` eligibility (made_str 0.45-0.78 + draw_pressure≥0.40 + vm_idx≥0.50 + passivity≥0.55 + fold_to_raise<0.50) — FIRST flop/turn offense in v138-142. LIVE verify = grep PROTECT_FLOOR after daemon≥100g; if <1% flop/turn bets fire, relax fold_to_raise<0.50→<0.55 OR drop conf 0.20 (DO NOT re-tune sizing). SITE-A (strategy.py L1277) mutual-exclusion with v140 ship_guard requires ship_guard stay round_idx==3 — codify invariant.
- **v141**: Offensive `_river_value_extraction_amplifier` (0.65-0.90x pot) on v137 classify_sizing_tendency, 3rd classify_sizing site. 3 post-commit gates: (1) RIVER_VALUE_AMP telemetry ≥1% river bets over daemon≥100g, (2) vm_idx 0.55-0.74 unique-band (not shadowed by vm_overbet/river_value_raise_tier), (3) WR lift vs sticky opps v131/v135/v137 vs v140. Archiving: if <1% river bets OR no WR lift over v140, drop unknown-sticky branch + tighten vm_idx 0.55→0.60.

