## OPPONENT_MODELING
- Archetype-suppression gates on EXISTING detectors > AND-gated new detectors ('standard' default = zero downside); respect ≥0.15 confidence floor for VPIP/archetype classification.
- calldown_profile sample trap: foldy opps never reach n≥4 — use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- Crossover silently drops recent strategy.py work — prefer the highest-rated parent's strategy.py, not an arbitrary source_v.
- `large_bet_ratio` is RAW (no smooth_rate wrapper); verify the read site before treating the raw-warning as live (v171/v179 route through value_maximizer_index + fold_to_bet).

## POSTFLOP_STRATEGY
- **Made-strength table (authoritative):** pair≈0.22, two-pair≈0.40, trips≈0.58. Over-calling leak lives at 0.20≤made<0.45; 0.45-0.55 sparse — verify band edges before committing.
- **FOLD-SIDE RULE (corrected):** binary `return -1` POSTFLOP fold gates are dead (13+ gens) [POSSIBLY EXHAUSTED]; PREFLOP binary folds remain LIVE (v172/v173 preflop gates are an active axis — the live `return -1` paths in strategy.py are preflop, not postflop). Continuous EV-integrated fold margins permitted postflop. Guard relocation permitted ≠ proven-effective (river-guard relocation ran 12+ gens without lifting fold%).
- **Dispatch-order shadow + birth mandate:** wire offensive primitives AFTER downstream tiers (overbet/amplifier/value-tier); archetype overrides AFTER pure-value. RECURRING BIRTH-DEFECT (v149/158/160/161/163/170/179/181 ship 1-2 sites vs ≥3 mandate) — enforce ≥3 wired dispatch sites incl. donk/probe at worker-review time, not deferred to daemon grep.
- NEW detectors require 6 BIRTH REQUIREMENTS: new function + new opp-line signal + ≥3 dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs.
- **CAP CONSTRAINTS (current, v182):** strategy.py at 2403 lines EXCEEDS adaptive cap ~2388 (max(2000, src×1.15)) by ~15 — adaptive cap is BREACHED/binding; only the 2500 hard cap (~97 lines) remains. **strategy_helpers.py at 2500/2500 = EXACT CAP — targeting helpers IMPOSSIBLE; future work MUST target opponent.py or refactor-first (share scaffolding between _value_lead_upsizing_delta / _spr_calibrated_value_ship) BEFORE any strategy.py growth.**
- **-20k / 0%-fold stack-off leak:** highest-volume observed loss (17th gen), OPEN target, but TWO fix-axes FALSIFIED — preflop-trash axis dead, river-guard relocation didn't move fold% — do NOT re-target either [STALE — no WR-lift]. v181 shipped OFFENSE not fold work; v180 turn-board-danger fold hypothesis remains UNVALIDATED (verify via reachability_test + ≥100g H2H before returning).
- **River call-margin tighten as NEW defensive work is exhausted** (5+ gens, v169 critic local_optima) [POSSIBLY EXHAUSTED] [STALE — no WR-lift]; the 0.20-0.45 band narrowing is now already satisfied by v180's turn primitive.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense-side bluff axes remain LIVE — distinguish from the dead binary POSTFLOP-fold axis (canonical statement in POSTFLOP_STRATEGY).

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: offensive imports adding NEW opponent-signal gating AND river value-sizing structural changes.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44 → use bet_ratio/0.75 direct scaling so gates differentiate bet sizes.
- **Firing verification:** reachability_test (code-reachability proxy) + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (battle.py _PersistentBot reads stdout only; v163 blindspot ACTIVE); ALL ≥30g daemon-grep "fired≥5%" steps UNFULFILLABLE — substitute reachability_test, do NOT block on daemon-grep.

## GENERAL
- Master RELIABLE at plan-generation but reliability ≠ correctness: validate axis PAYLOAD (≥100g WR-lift), not just plan cleanliness. [advisory]
- Dead-code/guard removal > adding constants — anti-lock bypass guard removal (v167) is a logic fix with higher EV per line than margin tweaks.
- **Validation thresholds:** <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Workers MUST grep their own function body to confirm every param/signal actually gates an outcome (`fold_to_bet_turn` was read only in stderr + unused `my_round_bet` = semi-dead read, v179). [migrated advisory]
- **Plateau states (WR ~0.50 all matchups) have no single DOMINANT exploit** — when H2H shows 45-55% across the board, the correct move is a new structural axis (offense/texture/archetype), not tighter margins. [POSSIBLY EXHAUSTED]

## RECENT_LESSONS
- **v183**: Archetype-gated fold is a novel wrinkle on the otherwise-EXHAUSTED binary-postflop-fold family (v157/166/167/169), but it only moves the needle vs confirmed-aggro opponents — tight exploiters like v173 need a SEPARATE fold axis using the currently-UNUSED 'rock' bucket wired to thin-value-call suppression, not the same archetype gate.
- **v183**: Labels matter for future audits: a `return -1` dispatch should be called 'EV-gated binary fold', NOT 'EV-integrated continuous fold' — only the GATE uses continuous math; the output is binary. The misleading docstring in v183 should be corrected.
- **v183 归档建议 (improvement)**: Next gen has two complementary mandates: (1) address v173 (70% loss) by wiring the unused 'rock' bucket (fold_to_raise>0.50 + low postflop_aggr) to suppress thin-value calls against tight exploiters who over-fold to our barrels, and (2) add the `lag_maniac` carve-out (vpip≥0.65 & large_bet_ratio<0.40 & conf≥0.20) that DISABLES the aggro fold vs bluff-heavy LAGs — those opponents over-deliver bluffs and should be called wider, not folded, so the current archetype gate is directionally WRONG against them.
- **v183**: Critic evidence: H2H weaknesses: v173 vs v182: 70% loss (a_wins 7/10) — WORST matchup, but v173-style tight opp classifies as 'standard' (empirically verified), so this fold will NOT help the primary weakness., v143/v155/v160/v167 vs v182: 60% loss — these aggro-leaning bots MAY trigger the new fold., Bot-wide: -20k/0%-fold stack-off leak is the highest-volume observed loss (17th gen open target per experience pool RECENT_LESSONS v182 validation mandate).; Experience pool refs: OPPONENT_MODELING: 'Archetype-suppression gates on EXISTING detectors > AND-gated new detectors (standard default = zero downside); respect ≥0.15 confidence floor' — v183 honors this (conf≥0.15, 'standard' = no-op backward compatible)., POSTFLOP_STRATEGY FOLD-SIDE RULE: 'binary return -1 POSTFLOP fold gates are dead (13+ gens) [POSSIBLY EXHAUSTED]... Continuous EV-integrated fold margins permitted postflop' — v183 uses binary return -1 output BUT adds archetype gating + pot_odds>made+0.10 EV gate; defensible new wrinkle on an exhausted output pattern., RECENT_LESSONS v182: 'pivot the next primitive to opponent.py to address the persistent -20k/0%-fold leak (unaddressed 15+ gens)' — v183 fulfills this mandate.; Diff refs: opponent.py L438-489: classify_archetype() — continuous composite scoring across 8 LIVE signals, returns (bucket, conf); replaces v164's binary-threshold version removed in v166+., opponent.py L492-525: _aggro_bluffcatcher_should_fold() — 5-way gate (round_idx>=2 + aggro+conf≥0.15 + made[0.20,0.45) + draw<0.15 + tier∉{nut,strong} + pot_odds≥0.33) THEN EV gate (pot_odds > made+0.10)., strategy.py L1547-1556: single wired fold site inside to_call>0 block, AFTER river_stackoff_guard — appropriate placement for a defense primitive (≥3 dispatch mandate applies to OFFENSE donk/probe/choose_raise, not folds).
- **v182:** strategy.py at 2403/2500 EXCEEDS adaptive cap ~2388 — future primitives MUST target opponent.py OR refactor _value_lead_upsizing_delta/_spr_calibrated_value_ship to share scaffolding; new strategy.py additions will trip the hard cap.
- **v182 (critic insight):** SPR<=2.0 tier-differentiation (strong=75%) is likely SUBOPTIMAL per commitment theory — at low SPR pot-odds force a ship regardless. Move tier-differentiation to 2.0<SPR<=3.0 and restore nut==strong==100% ship at SPR<=2.0 for max EV.
- **v182 (validation mandate):** Before extending SPR-ship to a 4th gen, validate firing via reachability_test at SPR boundaries 2.0/3.0/4.0 (daemon-grep structurally impossible) AND pivot the next primitive to opponent.py to address the persistent -20k/0%-fold leak (unaddressed 15+ gens).
- **v182 (birth-defect closed):** SPR-ship now wired to 4 dispatch sites (river1980/turn2065/donk2184/probe2230) — closes the recurring 1-2-site birth-defect for this axis.
- **v181:** SPR-commitment axis now OPEN as a distinct offense primitive (flat threshold override vs v178's continuous delta); future offense sizing can layer on SPR tiers 2.0/3.0/4.0 without re-deriving commitment math.
- **v181:** Critic evidence — H2H (10g, high variance): v164 0.20, v166 0.30, v173/v176/v167 0.40, bulk 0.50 = plateau; SPR-ship link to losses is a hypothesis, not confirmed.
- **v180:** `_turn_board_danger_margin()` (L1082): continuous fold-margin [+0.04,+0.10] turn flush/straight-danger, 5-way gate (turn r_idx==2 + facing_aggr + made[0.20,0.45) + draw<0.15 + tier∉{nut,strong} + board_danger≥0.65); FIRST turn-axis fold defense, but v181 pivoted to OFFENSE leaving this fold hypothesis UNVALIDATED.
- **v180 (if inert):** widen made-band upper 0.45→0.50 (cheaper, larger population) BEFORE relaxing board_danger 0.65→0.60; add bluffer carve-out (halve delta when opp vpip≥0.60 & aggr≥0.40, conf≥0.20) to avoid over-folding semi-bluffers.


