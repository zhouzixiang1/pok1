## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (they drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). Gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic.
- Detector firing verification: grep symbols in bot_telemetry/{bot}.jsonl (A1 unlocked stderr capture v146+). **CRITICAL: grep token>0 ≠ LIVE — SIZING_MARGIN_ADJ fires 42k× but 99.96% delta_adj=+0. MUST grep `delta_adj!=0` / actual param values, not token presence.** reachability_test.py proves CODE-PATH execution only (force-fed inputs), NOT in-game firing; never conflate the two. Verdict: 0 fires = PENDING-VERIFICATION.

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW — CONFIRMED DEAD (v147/v148 SPR gate, A1-verified)**: `_spr_commitment_gate` sits INSIDE the `if to_call>=my_chips:` block, reached ONLY when opponent_allin==False — equal stacks + any legal non-allin raise leaves raiser≥1 chip → to_call<my_chips always → block entered ZERO times across 153,484 v147/v152 replay decisions. The 7-gen `_river_stackoff_guard` shadow was NEVER fixed, only relocated INTO the dead block. **DO NOT place guards in the to_call>=my_chips block — relocate to `to_call>0` (ALIVE, ~40% of decisions) or merge into the opponent_allin branch.**
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v152 lineage; v131/v135 REGRESSED; v138/v139 structurally INERT; v147/v148 SPR gate same exhausted axis. **CONFIRMED (not merely possible): v149 proved the bot OVER-FOLDS preflop, does NOT over-commit; fold-side made the leak WORSE.** Fix = PIVOT TO OFFENSE, do NOT re-tune thresholds.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket EDIT DIRECTION EXHAUSTED [POSSIBLY EXHAUSTED]; flop/turn draw thresholds ad-hoc, NOT outs×2-derived — re-anchor before tuning.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.
- **INERTNESS HARD RULE — `postflop_call_margin` framework is a 99.96% DEADZONE (verified v137-v152, 9 gens)**: it decouples FIRING from ADJUSTING — stderr telemetry sits OUTSIDE the two real arms (underbettor+small / overbettor+large), so 42,670+ "fires" are 99.96% delta=+0 no-ops. The arms use compound AND of (opp-tendency-bucket AND this-hand-size-bucket), joint prob ~0.04%. `classify_sizing_tendency` bins 0.30-1.0x pot as 'standard' (96.9% live). v144-v151 preflop offense all route the same signals into bucket-specific AND arms → 0 fires. **ONLY v152 PREFLOP_DEFEND_WIDEN is LIVE (33×) because it bypasses the framework (OR-gate) — and that one LIVE case is MISFIRING.**
- **smooth_rate prior math unreachable**: preflop offense detectors gate on fold_open≥0.50 / ftr≥0.55 / calldown≥0.55, but priors sit at 0.42-0.44 (opponent.py smooth_rate prior_mean, weight=4.0) — even a TRUE 50%-folder saturates at 0.491 (n=30). Threshold above the smoothed asymptote of any realistic opp → structurally unreachable. Fix: (a) lower gate to prior mean, OR (b) prior_weight 4.0→2.0, OR (c) make delta CONTINUOUS: `delta = sign*0.025*clamp((signal-prior_mean)/0.15, -1, 1)`.
- **Telemetry `!=0` gate = INERTNESS blind spot**: `if delta != 0: sys.stderr.write(...)` makes 0-delta silent — 9 gens undetected. MUST print unconditionally with reason tag (`reason=deadzone`/`conf_gate`/`standard_bucket`/`fired`). Validation gate (add to quality_gates): daemon≥30g → require delta!=0 in ≥5% of detector stderr lines, else INERT → block commit. Replaces the "grep token>0" proxy that passes for dead detectors.
- **calldown_profile sample self-defeating trap**: samples accumulate only via pending_my_pressure (our raise) + opp NEXT action; each opp FOLD ends the hand → foldy opps (the target) never reach n≥4. opponent.py populates empirical rate only at n≥4 else hard-defaults 0.5, while detectors gate on n≥3 → every n=3 case killed by the 0.5 default. v151 `_delayed_calldown_bluff` 0-fire is the evidence; v153 CALLDOWN_DONK inherits this → PREDICTED INERT. Fix: populate empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) open per axis but each exhausts. Pre-v146 + v149-v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION.
- `_street_fold_exploit_sizing_boost` [POSSIBLY EXHAUSTED] — v149/v150 zero telemetry fires after conf relaxation; v151 delivered per-street calldown signal, so root cause was signal-starvation, not axis exhaustion. Do NOT retune boost — verify `_delayed_calldown_bluff` firing instead.
- Preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning EXHAUSTED [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; measurement anchors (v111/v114) REAPED; revalidation requires NEW anchors against LIVE opponents.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 14+ gens v122→v151). Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v154**: Critic evidence: H2H weaknesses: v153 overall WR=0.5083 (240g) — plateau with no decisive edges. H2H data shows mostly 45-55% matchups (flat plateau). No specific opponent where fold-side fixes show promise — v149 proved the bot OVER-FOLDS preflop (99% fold-to-bet vs v132/v144).; Experience pool refs: POSTFLOP_STRATEGY: 'Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v152 lineage; v131/v135 REGRESSED; v138/v139 structurally INERT; v147/v148 SPR gate same exhausted axis. CONFIRMED: v149 proved the bot OVER-FOLDS preflop, does NOT over-commit. Fix = PIVOT TO OFFENSE, do NOT re-tune thresholds.', POSTFLOP_STRATEGY: 'PLACEMENT SHADOW — CONFIRMED DEAD (v147/v148 SPR gate, A1-verified): _spr_commitment_gate sits INSIDE the if to_call>=my_chips: block... ZERO fires across 153,484 decisions.', RECENT_LESSONS v153: '下代 = fix-margin-framework代 (加 tendency=='standard' arm 到 postflop_call_margin), 非 add-detector-#10'; Diff refs: strategy.py L1089-1099: SPR gate relocated into opponent_allin block (was L1115 in dead to_call>=my_chips block), strategy.py L1103-1110: NEW _river_weak_made_hand_gate call (after SPR gate, before jam_buffer final decision), strategy_helpers.py L1006-1031: NEW _river_weak_made_hand_gate() — river-only, made_strength<0.60, pot>=65% stack, no strong/nut tier, draw<0.18, no pot-odds math
- **v153 A1-解锁审计 (21-agent 对抗验证)**: v144-v152 offense detectors 几乎全 INERT (SB_OPEN/BB_VS_LIMP/BB_VS_RAISE/SPR_FOLD/STREET_FOLD/DELAYED_BLUFF 全 0 fires; 唯一 LIVE = v152 PREFLOP_DEFEND_WIDEN 33×, 且 misfiring — OR-gate 的 vpip≥0.55 抓 passive limpers, 对 premium-heavy raise 做 -EV widen). **v153 Task1 前提错** (PREFLOP_DEFEND_WIDEN 非 INERT 早已 fire): 应改"修 misfiring" (去掉 OR-gate vpip≥0.55 分支, 保留 pfr≥0.30/conf<0.20 真实 steal-opener 信号); 0.30→0.35 放宽可留. **v153 Task2 CALLDOWN_DONK 继承 calldown_profile sample 陷阱 → 预测 INERT**; daemon≥30g grep CALLDOWN_DONK delta≠0<5% 则 drop 或改 n≥3+pool-wide fallback. **下代 = "fix margin framework" 代** (加 tendency=='standard' arm 到 postflop_call_margin, 让 96.9% standard 池输出非零 delta + telemetry 无条件打印带 reason tag), 非 "add detector #10". trigger A5 literature_probe=True.
- **v153 Critic evidence**: v152 H2H: vs v131 wr=0.30 n=10, vs v135/v139/v147 0.40 each (all sub-30g noise, consistent with fold-leak pattern); overall v152 WR=0.5032/310g = plateau, no decisive edges. Diff: strategy_helpers widened 5 pot_odds thresholds (BB-vs-raise mid_pair 0.42→0.45, suited_connector 0.34→0.36; SB-vs-3bet small_pair 0.32→0.36, suited/broadway 0.30→0.35, mid_pair 0.30→0.35); donk_probe.py NEW calldown_profile consumer in should_donk_bet(), _effective_min_strength 0.45→0.38, _effective_freq_cap 0.65→0.80 when flop call-down≥0.55 samples≥4; fixtures added.
- **v152**: Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; future widen_threshold must target ≥15pp bands minimum. Speculative hand caps (suited_connector/broadway_suited ≤0.30) must account for HU 3-bet pot_odds (0.28-0.35); implied-odds hands need ≥0.35 threshold.
- **v151**: calldown_profile['small_bet_rate']/['large_bet_rate'] are dead data — no consumer reads them; wire a consumer or remove fields. `_pending_my_bet_ratio` only tracked on 'raise' (not 'allin') — misses allin-then-fold opps. daemon≥30g grep DELAYED_CALLDOWN_BLUFF<5% fire → relax curr_call_rate 0.45→0.50, min_samples 3→2, add 2 donk/probe dispatch sites.

