## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, classify_sizing_tendency) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — they drift, already did across v138→v139), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); established since v134. Gating with it counts as PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- classify_sizing_tendency() (v137) = 4-bucket overbet/underbet classifier, CONSUMED at 3 live sites (strategy.py + strategy_helpers.py) in v138/v139 but firing-UNVERIFIED (no persistent fixture) — do NOT label it dead/inert and do NOT "remove as dead code". ACTION: add a persistent fixture/self-test proving firing rate, then declare LIVE.

## POSTFLOP_STRATEGY
- Defensive fold/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires were on branches absent in HEAD and REGRESSED (0.5706→0.5306@360g); `_spr_commitment_gate`/`_single_reraise_stackoff_guard` still absent. v138 re-added `_river_stackoff_guard` WITH fixture but its invocation runs AFTER the `if to_call >= my_chips:` all-in early-return in choose_action → structurally INERT on true all-ins. Axis exhaustion CONFIRMED by v138+v139 repeating the placement miss. Future gates MUST cite that all-in early-return ordering constraint BY NAME and place BEFORE it. Need ≥100g H2H after placement fix.
- Dispatch-order shadow: v134 wired `value_maximizer_overbet` BEFORE `turn_second_barrel_planner` + `river_value_raise_tier` = authoritative root cause of leaks vs disciplined barrel-aggressive bots; both primitives (and `turn_river_blocker_bluff`) are 0-3/6 birth reqs, no fixture → presumed-INERT. Fix: RELOCATE the overbet call-site AFTER the barrel/value tiers, NOT re-tune thresholds. Effectiveness UNCONFIRMED; need ≥100g daemon H2H turn+river raise-freq lift ≥40% from ~33.5% baseline.
- `barrel_pressure_profile` + `check_raise_pressure` are code-present (re-wired by v133 after v132 removal), NOT firing-verified — "LIVE" means code-present only. Verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED] (re-grep HEAD before anchoring); structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Mandate: NEW detectors MUST prove firing via a persistent fixture/self-test that exercises the ACTUAL DISPATCH SITE, not just the helper in isolation. v134-v139 ALL shipped NEW primitives with NO such dispatch-exercising verification (v138 met req #6 on the helper only; v139's `_turn_oop_pot_control` fixture likewise skipped the `round_idx==2 and to_call==0` turn-OOP dispatch wire) = recurring violation. Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures).
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap, BLUFF_OPPORTUNITY_THRESHOLD) [POSSIBLY EXHAUSTED] — live `BLUFF_OPPORTUNITY_THRESHOLD = 0.55` (v136 reverted to v111's 7000g baseline); require ≥100g H2H vs v93/v117/v114 before further tuning; if it doesn't recover ≥half the v111-vs-v134 gap, declare axis dead.
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE at PLAN-GENERATION — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). Reliability ≠ firing-fixture compliance — Master reliably produces plans that STILL violate the fixture mandate (see POSTFLOP); the fixture gate, not Master output, is the open problem. Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v139 critic refs all sub-30g; v138 = 320 games/27 opponents, max 20g/opp).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (7th recurrence v127/v129/v130/v135/v137/v138/v139) — cited G{N}H{M} replays are non-existent (actual format is timestamp-based). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add a hard gate rejecting cited replays absent on disk. Treat the LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v140**: BET-side placement (after choose_raise returns, before 'return raise_amount') avoids the v138 fold-side placement shadow that ran AFTER to_call>=my_chips all-in early-return; the dispatch-reachability assertion in __main__ fixture is the proven pattern to proactively detect INERTNESS for all future guards.
- **v140**: 8th recurrence of systemic replay-evidence fabrication: cited G4H45/G6H15 do not exist — replay files are timestamp-named (e.g., 20260620_160503_claude_v77_vs_claude_v128.json). Worker MUST run `ls web/core/results/match_replay/ | head -5` and cite actual filenames before any replay-based evidence claim; reviewer should reject diffs with G{N}H{M}-style citations.
- **v140 归档建议**: After daemon rates v140 at >=100g H2H vs aggressive barrelers (v96/v108), grep river raise frequency vs parent v139; if value-raise frequency drops materially without win-rate gain, relax made_strength floor 0.70→0.65 OR add explicit allowance for made_class>=2 (two-pair+) on safe non-paired boards to preserve legitimate thin-value ships.
- **v140**: Critic evidence: H2H weaknesses: v140 not yet evaluated by daemon (0 H2H games). v139 parent has 240g across ~30 opponents = ~10g/opp — ALL SUB-30G = NOISE per experience pool rule. Cannot cite any confirmed H2H weakness that this change addresses. The 'weakness' (river stack-offs) is named only in master plan + prior critic feedback, not in live data.; Experience pool refs: POSTFLOP_STRATEGY: 'v138 re-added _river_stackoff_guard WITH fixture but its invocation runs AFTER the if to_call >= my_chips: all-in early-return in choose_action -> structurally INERT on true all-ins. Axis exhaustion CONFIRMED by v138+v139 repeating the placement miss.' — v140 pivots to BET-side to escape this exhaustion., POSTFLOP_STRATEGY (mandate): 'NEW detectors MUST prove firing via a persistent fixture/self-test that exercises the ACTUAL DISPATCH SITE, not just the helper in isolation. v134-v139 ALL shipped NEW primitives with NO such dispatch-exercising verification.' — v140 is the FIRST to meet this: fixture tests BOTH helper AND strategy.py dispatch wire., GENERAL: 'FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (7th recurrence v127/v129/v130/v135/v137/v138/v139) — cited G{N}H{M} replays are non-existent (actual format is timestamp-based).' — v140 makes it the 8th recurrence (cites G4H45/G6H15; verified via ls that no such replay files exist).; Diff refs: strategy_helpers.py:738-776 NEW _river_value_ship_guard(round_idx, my_chips, made_strength, value_profile, board_texture, pair_profile, raise_amount) — 39-line helper returning True to BLOCK raise when ALL of: round_idx==3 AND raise_amount>=my_chips*0.25 AND NOT (made_strength>=0.70 AND tier strong/nut AND board_safe)., strategy.py:1265-1274 NEW 10-line wire INSIDE the choose_raise return branch (after `if raise_amount is not None and raise_amount > to_call:`), BEFORE `return raise_amount`. Avoids the v138/v139 placement shadow because this branch is only entered AFTER choose_raise() chose to raise — no to_call>=my_chips early-return precedes it., strategy_helpers.py __main__: NEW 30-line fixture (6/6 PASS, verified locally) covering thin/strong/flush/2pair-paired/trips-paired/small-raise cases PLUS a dispatch-reachability assertion (verifies '_river_value_ship_guard' appears >=3 times in strategy.py and that 'return raise_amount' follows the guard call). First fixture in v138-v140 chain to exercise the dispatch site.
- **v139 / v138 (recurring)**: PLACEMENT SHADOW — the `_river_stackoff_guard()` invocation sits AFTER the `if to_call >= my_chips:` all-in early-return in choose_action, so it NEVER fires on true stack-covering river all-ins. v138 band-aided with a helper-only fixture; v139 tried upstream turn-sizing mitigation instead of the structural fix and did NOT cite the all-in early-return ordering constraint despite the v138 lesson. NEXT Master MUST relocate the invocation BEFORE that early-return (or duplicate inside the all-in branch), then re-verify vs v111/v131 (polarized river barrelers) at ≥100g H2H that the -15k/-20k stack-off frequency drops before claiming the 0%-fold leak closed.
- **v139**: FIXTURE ≠ LIVE — v139's 8-case `_turn_oop_pot_control` fixture tests the helper in isolation but does NOT exercise the `round_idx==2 and to_call==0` turn-OOP dispatch wire (reviewer flagged INERTNESS). New helpers need ≥1 test exercising the actual dispatch site, not just the function directly.
- **v138**: Ships `_river_stackoff_guard` fixture (first fold-gate meeting birth req #6: 10/10 local PASS — fires A-high/bottom-pair/weak-one-pair vs allin; skips mid-two-pair/strong/nut/small-bet/overbettor/cheap-call); made_strength 0.88→0.55 floor fixes v135 miscalibration. NO ≥100g H2H yet; "~76 pairs v14-v137" count = unverified-evidence recurrence.
- **v137**: `classify_sizing_tendency()` defense relaxation placed FIRST in should_fold_postflop (overbettor-large-bet → return False; absolute_air exclusion made_strength<0.15 AND has_draw preserves fold discipline; call_margin ±0.025-0.030). NEW opponent-modeling axis, NOT an exhausted fold-gate. req #6 NOT met (stderr SIZING_* debug-only) → firing-UNVERIFIED (signal IS consumed at 3 sites but no fixture). To reach critic ≥8: add 3rd wired site + persistent firing-rate fixture; verify round-bucket folds aren't short-circuited.


