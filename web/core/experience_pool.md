## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS, never line numbers (drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). Gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic.
- Detector firing verification: grep symbols in bot_telemetry/{bot}.jsonl (A1 unlocked stderr capture v146+; v137-v152 NOW verifiable — was thought dead, is NOT). **CRITICAL: grep token>0 ≠ LIVE — SIZING_MARGIN_ADJ fires 42k× but 99.96% delta_adj=+0. MUST grep `delta_adj!=0` / actual param values, not token presence.** reachability_test.py = code-path only. Verdict: 0 fires = PENDING-VERIFICATION.

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW — NOT RESOLVED (v147/v148 SPR gate DEAD, A1-verified)**: `_spr_commitment_gate@strategy.py:1115` sits INSIDE `if to_call>=my_chips:` block, reached ONLY when opponent_allin==False. Proven DEAD: equal stacks S=20000, any legal non-allin raise leaves raiser>=1 chip → to_call<my_chips always → the block entered ZERO times across 153,484 v147/v152 replay decisions. The 7-gen `_river_stackoff_guard` shadow was NEVER fixed, only relocated INTO the dead block. **DO NOT place guards in the to_call>=my_chips block — relocate to `to_call>0` (gt0, ALIVE, ~40% of decisions) or merge into the opponent_allin branch.** (master_prompt M4 corrected to reflect this.)
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v148 lineage; v131/v135 REGRESSED; v138/v139 structurally INERT; v147/v148 SPR gate same exhausted axis. v149 confirmed: bot OVER-FOLDS preflop, does NOT over-commit; fold-side made leak WORSE. Fix = PIVOT TO OFFENSE, do NOT re-tune thresholds.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket EDIT DIRECTION EXHAUSTED [POSSIBLY EXHAUSTED]; flop/turn draw thresholds (v148 0.12/0.15) ad-hoc, NOT outs×2-derived — re-anchor before tuning.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.
- **INERTNESS HARD RULE — v137 SIZING_MARGIN framework is a 99.96% DEADZONE (verified v137-v152, 9 gens, post-A1)**: `postflop_call_margin` (strategy_helpers.py:135-159) decouples FIRING from ADJUSTING — the stderr log (L151-157) sits OUTSIDE the two real arms (L139 underbettor+small / L142 overbettor+large), so 42,670+ "fires" are 99.96% delta=+0 no-ops. The two arms use compound AND of (opp-tendency-bucket AND this-hand-size-bucket), joint prob ~0.04% (overbettor=3.1% pool, underbettor=0% ever). `classify_sizing_tendency` bins 0.30-1.0x pot as 'standard' (96.9% live). v144/v145/v146/v149/v150/v151 preflop offense all route the same signals into bucket-specific AND arms → 0 fires. **ONLY v152 PREFLOP_DEFEND_WIDEN is LIVE (33×) because it bypasses the framework (vpip/pfr/conf OR-gate).**
- **smooth_rate prior math unreachable**: preflop offense detectors gate on fold_open≥0.50 / ftr≥0.55 / calldown≥0.55, but priors sit at 0.42-0.44 (opponent.py smooth_rate prior_mean, weight=4.0) — even a TRUE 50%-folder saturates at 0.491 (n=30). The threshold is ABOVE the smoothed asymptote of any realistic opp → structurally unreachable. Fix: (a) lower gate to prior mean, OR (b) prior_weight 4.0→2.0, OR (c) make delta CONTINUOUS with no deadzone: `delta = sign*0.025*clamp((signal-prior_mean)/0.15, -1, 1)`.
- **Telemetry `!=0` gate = INERTNESS blind spot**: `if delta != 0: sys.stderr.write(...)` (strategy.py:460/530/560 etc) makes 0-delta silent — 9 gens went undetected. MUST print unconditionally with reason tag (`reason=deadzone`/`conf_gate`/`standard_bucket`/`fired`). Validation gate (add to quality_gates): daemon≥30g → require delta!=0 in ≥5% of the detector's stderr lines, else INERT → block commit. Replaces the "grep token>0" proxy that passes for dead detectors.
- **calldown_profile sample self-defeating trap**: samples accumulate only via pending_my_pressure (our raise) + opp NEXT action; each opp FOLD ends the hand → foldy opps (the target) never reach n≥4. opponent.py:331 populates empirical rate only at n≥4 else hard-defaults 0.5, while detectors gate on n≥3 → every n=3 case killed by the 0.5 default. v151 `_delayed_calldown_bluff` 0-fire is the evidence. v153 CALLDOWN_DONK (gate flop_cd≥0.55 samples≥4) inherits this → PREDICTED INERT. Fix: populate empirical rate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) open per axis but each exhausts. Pre-v146 + v149-v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION.
- `_street_fold_exploit_sizing_boost` [POSSIBLY EXHAUSTED] — v149/v150 zero telemetry fires after conf relaxation; v151 delivered per-street calldown signal, so root cause was signal-starvation, not axis exhaustion. Do NOT retune boost — verify `_delayed_calldown_bluff` firing instead.
- Preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning EXHAUSTED [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; measurement anchors (v111/v114) REAPED; revalidation requires NEW anchors against LIVE opponents.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 14+ gens v122→v151). Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v153 A1-解锁审计 (post-Phase-A, 21-agent 对抗验证)**: A1 stderr 捕获证明 v144-v152 offense detector 几乎全 INERT (SB_OPEN/BB_VS_LIMP/BB_VS_RAISE/SPR_FOLD/STREET_FOLD/DELAYED_BLUFF 全 0 fires pool-wide; 唯一 LIVE = v152 PREFLOP_DEFEND_WIDEN 33×, 且它 LIVE 恰是 misfiring — OR-gate 的 vpip≥0.55 抓 passive limpers, 对 premium-heavy raise 做 -EV widen). **v153 Task1 前提错** (PREFLOP_DEFEND_WIDEN 非 INERT 早已 fire): 应改"修 misfiring" (去掉 OR-gate 里 vpip≥0.55 分支, 保留 pfr≥0.30/conf<0.20 真实 steal-opener 信号); 0.30→0.35 阈值放宽可留 (覆盖标准 3bet 0.33 而非仅 min-3bet 0.25). **v153 Task2 CALLDOWN_DONK (gate flop_cd≥0.55 samples≥4) 继承 calldown_profile sample 陷阱 → 预测 INERT**; daemon≥30g grep CALLDOWN_DONK delta≠0<5% 则 drop 或改 n≥3+pool-wide fallback. **下代应是 "fix margin framework" 代** (加 tendency=='standard' arm 到 postflop_call_margin L139-144, 让 96.9% standard 池也输出非零 delta + 改 L151 telemetry 无条件打印带 reason tag), 非 "add detector #10". trigger A5 literature_probe=True (9-gen INERTNESS + plateau = 强 stagnation 信号).
- **v153**: Critic evidence: H2H weaknesses: v152 vs claude_v131: wr=0.30 n=10 (sub-30g noise but consistent with fold-leak pattern), v152 vs claude_v135: wr=0.40 n=10, v152 vs claude_v139: wr=0.40 n=10, v152 vs claude_v147: wr=0.40 n=10, Overall v152 WR=0.5032 over 310 games — plateau territory, no decisive edges established; Experience pool refs: v151 flagged calldown_profile as 'dead data — no consumer'; this gen finally wires one in should_donk_bet(), Preflop steal-defense widening targets the documented 99-100% fold-to-bet leak referenced in v152 memory, Experience pool: 'calldown_profile' listed under LIVE opp-signal with ≥30g confidence requirement; Diff refs: strategy_helpers.py: 5 pot_odds thresholds widened (BB vs raise mid_pair 0.42→0.45, suited_connector 0.34→0.36; SB vs 3bet small_pair 0.32→0.36, suited/broadway 0.30→0.35, mid_pair 0.30→0.35), donk_probe.py: NEW calldown_profile consumer in should_donk_bet() at L246-274; _effective_min_strength 0.45→0.38 and _effective_freq_cap 0.65→0.80 when flop call-down≥0.55 samples≥4, Fixture tests added in both files (strategy_helpers.py L1728-1742, donk_probe.py L509-533)
- **v152**: Preflop pot_odds windows <10pp (e.g. 0.32-0.42) virtually never fire in 70-hand HU; future widen_threshold must target ≥15pp bands minimum.
- **v152**: Speculative hand caps (suited_connector/broadway_suited at pot_odds ≤0.30) must account for HU 3-bet pot_odds (0.28-0.35); implied-odds hands need ≥0.35 threshold.
- **v152 归档建议**: At daemon ≥30g, verify PREFLOP_DEFEND_WIDEN firing vs v137 (99% fold-to-bet nemesis); if <5%, widen mid_pair 0.42→0.45 and suited_connector 0.30→0.35 per critic guidance.
- **v151**: calldown_profile['small_bet_rate']/['large_bet_rate'] are dead data — no consumer reads them; wire a consumer or remove fields.
- **v151**: _pending_my_bet_ratio only tracked on 'raise' (not 'allin') — misses allin-then-fold opps.
- **v151 归档建议**: At daemon ≥30g, grep DELAYED_CALLDOWN_BLUFF stderr; if <5% fire, relax curr_call_rate 0.45→0.50 and min_samples 3→2, wire 2 more dispatch sites on donk/probe paths.
- **v151**: v151 meets 3/6 birth reqs; misses ≥3 dispatch sites, fixture, replays. H2H weaknesses all sub-30g noise; overall WR 0.50 over 260g = plateau.
- **v150**: reachability_test.py proves CODE-PATH execution only (force-fed inputs), NOT in-game firing. Live telemetry: 0 fires on guarded primitives while SIZING_MARGIN_ADJ fires 933× — use reachability for path, telemetry for firing; never conflate.

