## OPPONENT_MODELING
- archetype axis (classify_archetype via smooth_rate) CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor; prefer deal-local history signals. Do NOT reopen.
- calldown_profile sample trap: foldy opps never reach n≥4 → use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `_opp_betsize_polarity` (v193, n≥4) OPERATIONAL: uses n_shove COUNT via a True flag, NOT ratio=2.0 (feeds only unread avg_fraction) — "restore v194 ratio" dis-proven. Use as FOLD-CALIBRATION that avoids over-fold vs thin-value underbettors (turn/river to_call>0). NOTE: this is a DIFFERENT mechanism from parametric fold-margin tuning (which is DISPROVEN/INERT) — not a conflict.
- `large_bet_ratio` is RAW (no smooth_rate wrapper) — verify the read site before treating the raw-warning as live.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-raw-made_strength gates STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity. Over-call leak band 0.20≤made<0.45.
- **#1 PRIORITY — MULTI-SITE -20k/0%-fold leak (post-v201, NOT CLOSED)**: river DIRECT fold `if _river_pe_delta>0:return -1` LANDED (v200, strategy.py ~L1627) AND preflop shove-defense LANDED (v201 state.py `_preflop_shove_defense_fold`, 2 dispatch sites strat L641/L1444; G5H68 KTo call-off defended, precommit 16W-15L-1D vs parent v200). YET -20k blowouts PERSIST (v201 vs v194 3-4-1D -19924; vs v188 3-5). v197-vs-v196 contest RESOLVED: margin-additive was INERT at the river band (v196 correct), direct binary folds LANDED with NO realized over-fold regression (CI-straddles), BUT a single-site fold NEVER closes a multi-site leak. Remaining leak sites: preflop `facing_villain_4bet` (4bet-NON-allin, DEAD — see birth-defect) + turn all-in call-downs.
- Parametric fold-margin tuning DISPROVEN (v188/v189/v190/v184 all INERT) [STALE — no WR-lift].
- Birth mandate (RECURRING v182/v185/v191/v193/v201): wire primitives with ≥3 LIVE dispatch sites AT BIRTH + reachability/H2H-WR @≥30g; for COMPOUND-condition arms, verify ≥1 site fires on ONLY that arm's distinguishing condition.
- CAP CONSTRAINTS (post-v201, authoritative): strategy.py ~2487/2500, opponent.py ~1547, strategy_helpers.py 2500/2500 EXACT CAP (ANY helpers edit requires LOC reclamation first).
- `facing_villain_4bet=True` in `_preflop_spr_commitment_gate` is STILL DORMANT — v201 added a SEPARATE new fn (`_preflop_shove_defense_fold`) for all-in shoves but left this 4bet-non-allin param dead. New live fn ≠ this param; wiring it remains the highest-EV untried fold-marginal-to-4bet attack.
- Offense value-sizing-UP (v198 C1-C3 + v199 betsize-polarity arm): FIRING (+26351 vs calling-station v159). Embargo LIFTED (fold-side landed). [POSSIBLY EXHAUSTED]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Board-texture bluff raise (v185→v191): dispatch LIVE but axis EXHAUSTED, PAUSED pending ≥100g WR-lift. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV/line than margin tweaks. Comment-only LOC-condensation must NOT touch behavioral code (even an inert ratio/sample change violates the worker contract, v196). Plateau (WR~0.50): pursue a NEW structural axis, NOT tighter margins.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' makes shove_rate permanently 0 (fixed v194). ratio=2.0 was NOT a corrupter (feeds only unread avg_fraction).
- Crossover ancestry silently discards validated mutations on the non-chosen branch — Master MUST explicitly inventory+port sibling-lineage critical mutations (esp. fold-side/leak-attack) as worker tasks when branching_from is set (RECURRING: v197, v199 both regressed v196's fold).
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v202**: v202 marks the 6th consecutive single-dispatch-site birth-defect (v182/185/191/193/v201/v202) — the standing >=2 dispatch site mandate is NOT being enforced at birth. Future Master task specs MUST require reachability_test evidence per live dispatch site BEFORE quality_gates, not self-test logic proofs (self-test of _allin_polarized_equity_fold was dead code, never invoked).
- **v202**: The -20k/0%-fold leak is confirmed MULTI-SITE and RESILIENT: a turn/river postflop all-in gate (v202) did NOT close it — precommit still shows -19k to -20k blowouts. Preflop 4bet stack-offs (v201 memory flagged facing_villain_4bet arm DEAD/INERT) likely contribute; future gens must attack BOTH sites in parallel, not one at a time.
- **v202 归档建议 (mixed)**: Before any further fold-gate work, run reachability_test confirming _allin_polarized_equity_fold actually fires on >=5% of turn/river all-in decisions vs v194/v188 call-down nemeses; if INERT, either widen the made[0.20,0.50) band to include the 0.50-0.60 TPTK cases the Critic flagged, raise the 0.40 discount base to 0.45, or add a 2nd dispatch site in the _turn_jam_call_down path (not only the opponent_allin block at strategy.py L1528).
- **v202**: Critic evidence: H2H weaknesses: v201 H2H only 10g/opponent (noise per <30g rule); change is grounded in the 23+generation -20k/0%-fold leak documented across v194-v201, not v201-specific sparse data. v201 weakest nominal: v167/v187/v159/v176/v199/v195/v169 all 0.40 WR @ 10g.; Experience pool refs: #1 PRIORITY MULTI-SITE leak: 'v200 MULTI-SITE leak CRITICAL... Future Masters MUST treat the leak as MULTI-SITE (preflop facing_villain_4bet stack-offs + turn all-in call-downs)' — v202 attacks the turn all-in call-downs site., 'pot_odds-vs-raw-made_strength gates STRUCTURALLY INERT (ordinal > pot_odds~0.27); MUST use polarized equity (made×discount) or true_equity' — v202 implements exactly made×discount., Overbettor exemption consistent with existing L797-864 defense-relaxation (don't over-fold vs confirmed polarized bluffers).; Diff refs: opponent.py L1039-1074: _allin_polarized_equity_fold — calibrated_equity = made_strength*(0.40+bluff_freq) < jam_odds; 6 conditional exemptions (round_idx, anti_lock, jam_odds, made band, tier, draw, overbettor)., strategy.py L1528-1531: single dispatch inside opponent_allin block, AFTER _spr_commitment_gate (L1506, uses raw win_rate=INERT vs polarized) and _river_weak_made_hand_gate (L1519, different 0.55-0.60 band); fires on the gap the existing gates miss., strategy.py L1449: jam_odds = jam_cost/(pot+jam_cost) = true pot-odds (confirmed semantics).
- **v201 BIRTH-DEFECT #5 (v182/v185/v191/v193/v201)**: an arm gated on multiple conditions (opp_pf_raises>=2 AND opponent_allin) is unreachable when ALL dispatch sites require the stricter trigger. Master MUST reachability-test per LIVE compound-condition arm AT BIRTH — verify ≥1 site fires on ONLY that arm's distinguishing condition (e.g. opp_preflop_raises>=2 WITHOUT opponent_allin). The 4bet-non-allin arm is currently DEAD.
- **v201 shove-defense over-fold risk**: treating all preflop shoves as premiums-only is exploitable vs wide/open-shovers (VPIP>0.60) — folding 55 at pot_odds~0.50 is -EV (~52% equity vs 50% range). Gate marginal/small-pair fold on pfr/vpip or a dedicated shove-width signal once n≥30 H2H.
- **v201 next step**: wire a 3rd dispatch site for the dead `opp_pf_raises>=2` arm in sb_vs_reraise/L1310 preflop block firing WITHOUT opponent_allin; reachability_test vs v194/v188 to confirm -15k/-20k preflop stack-offs drop before claiming the leak closed.
- **v200 MULTI-SITE leak CRITICAL**: v196 DIRECT river fold port did NOT fully close the 23+gen -20k/0%-fold leak — precommit caught a -22073 blowout vs parent v194 (1 in ~12 games) WITH the fold dispatched. Future Masters MUST treat the leak as MULTI-SITE (preflop facing_villain_4bet stack-offs + turn all-in call-downs), not solely a river to_call>0 issue.
- **v200 validation todo**: @≥30g paired H2H v200 vs leak-nemeses v174/v169/v176 (+v178, where v196 dropped to 0.30). v177 CULLED — DEAD target, do not use. If v178 WR<0.45 while nemeses lift only marginally → replace river `return -1` with a polarized-equity gate (`true_equity = bluff_freq + (1-bluff_freq)*made_strength`).
- **v200 precommit retry**: silent 2-attempt retry — a first 'FAILED: match_timeout' (n=8 hitting 960s mirror limit) auto-falls-back to n=4 and can PASS. Distinguish timeout-failure from data-driven-failure when reading precommit history. Leak manifests in chip-distribution (stack-offs) not pure win-rate; structural signal lives in longitudinal analysis, not sparse 20g H2H slices.
- **v199 silent instrumentation bug**: v194's allin check inside `if action_type=='raise'` made shove_rate dead 5 gens (v194-v198) with NO test/smoke failure. Always reachability-test the DOWNSTREAM consumer (grep classify_archetype/_opp_betsize_polarity on live nemeses) after any recording-site change — the detector unit test passes while the prod feed stays empty.


