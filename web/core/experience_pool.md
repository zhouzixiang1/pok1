## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio) need ≥30g confidence gates; sub-30g = noise.
- Grep-verify every claimed primitive is LIVE in HEAD — cite SYMBOLS/PATHS only, never line numbers (drift) or commit hashes. value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). Gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic.
- Detector firing verification: grep symbols in bot_telemetry/{bot}.jsonl (stderr capture A1-unlocked v146+). **CRITICAL: token>0 ≠ LIVE — MUST grep `delta_adj!=0`/actual param values.** SIZING_MARGIN_ADJ printed only sub-arm B's delta → FALSE "99.96% inert" verdict; standard arm A was LIVE all along (v153+, v154 r=1605). Confirm telemetry prints TOTAL return at function scope. reachability_test.py = CODE-PATH only (force-fed), NOT in-game firing; 0 fires = PENDING-VERIFICATION.
- smooth_rate prior_weight must be reachable: tune prior_weight BEFORE adding new detectors when symptom = "fires telemetry but doesn't adjust" (prior_weight 4.0 saturated even a true 50%-folder below the 0.50 gate; fixed 4.0→2.0 v156, unblocking ALL v144-v152 detectors).
- calldown_profile sample self-defeating trap: samples accumulate only via pending_my_pressure + opp NEXT action; each opp FOLD ends hand → foldy opps never reach n≥4. opponent.py populates empirical rate only at n≥4 else defaults 0.5, while detectors gate on n≥3 → every n=3 case killed (v151 0-fire; v153 CALLDOWN_DONK inherits). Fix: populate at n≥3 + fallback to pool-wide fold_to_raise when per-street samples<2.
- INERT detectors fire telemetry but move sizing <2% pot — require per-hand gating flags (prior_aggressor_checked_back from history) before approving population-mean opp signals as "new axes".

## POSTFLOP_STRATEGY
- **Telemetry scope invariant (v156)**: block-scoped sys.stderr.write tokens (inside nested if) are functionally invisible to daemon grep — must be hoisted to function scope on unconditional path. CALL_MARGIN_FINAL at function scope = correct pattern. Validation gate: daemon≥30g → require delta!=0 in ≥5% of detector stderr lines else INERT → block commit.
- **PLACEMENT SHADOW CONFIRMED DEAD**: guards inside `to_call>=my_chips` block reached ZERO times (equal stacks + any legal non-allin raise leaves ≥1 chip → to_call<my_chips always). DO NOT place guards there — relocate to `to_call>0` (~40% of decisions) or opponent_allin branch.
- **STACK-OFF GUARD PLACEMENT INVARIANT**: fold/value gates must be UPSTREAM of to_call>=my_chips early-return (v138-v153 all shadowed). v148 SPR gate upstream = only validated tail-containment lever (±11692 vs v130).
- **Fold-side work is NARROW, not exhausted**: binary `return -1` fold gates are EXHAUSTED (v135-v154, 12+ gens) and MUST NOT be added — but the river 0%-fold stack-off leak still PERSISTS (fold=0%, -37143/-36712 swings = bot UNDER-folds in stack-offs; v149's PREFLOP over-fold finding ≠ this river under-fold leak). The ONLY structurally-valid fold-side paths: (a) continuous call_margin EV-integration (v157), and (b) RELOCATING an existing guard BEFORE the to_call>=my_chips early-return — NOT new binary gates. Masters MUST enforce standard-arm/telemetry fix before any fold-side work.
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (value_maximizer_overbet, river_value_raise_tier, turn_second_barrel_planner); high-conf fires FIRST. Fix: RELOCATE call-site, don't re-tune.
- NEW detectors require 6 BIRTH REQUIREMENTS: new detector + new opp-line signal + ≥3 wired dispatch sites + ≥3 replay folds + ≥30g confidence gate + persistent fixture logs. fn_refs (import + N call-sites) ≠ dispatch sites.
- v152 caution (resolved v156): vpip≥0.55 OR-gate catches passive limpers → makes -EV widen vs premium-heavy raises. Use pfr≥0.30 / conf<0.20 real steal-opener signals, not vpip.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost, delayed-calldown-bluff) open per axis but each exhausts. Pre-v146 + v149-v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION.
- Preflop opponent-sizing-delta pattern [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add 4th preflop variant; require ≥100g H2H. Sticky/limp-heavy branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v151), NO recovery; measurement anchors REAPED; revalidation requires NEW anchors. Includes check_raise_freq = DEAD directive (undelivered 14+ gens v122→v151). Revive only with NEW detector + grep-verified LIVE opp_current_round_check_count + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open, calldown_profile, river_call_size_ratio).
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing, two-band slope reshape) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).
- Preflop pot_odds windows <10pp (0.32-0.42) virtually never fire in 70-hand HU; future widen_threshold must target ≥15pp bands minimum; implied-odds/speculative hands need ≥0.35.

## GENERAL
- Master RELIABLE at PLAN-GENERATION — don't reflexively fall back to crossover. Crossover-as-default [POSSIBLY EXHAUSTED] — same-fn re-import = exhausted axis; NEW fn + NEW opp-line signal + birth reqs = new axis.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting.
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard fns = same-axis work.
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (v127-v151; cited G#H# IDs DON'T EXIST). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification.

## RECENT_LESSONS
- **v160**: Critic evidence: H2H weaknesses: v159 overall 51.5% WR (134-125-1, 260g). Weakest confirmed matchup: vs v94 at 30% (3-7, sub-30g noise). No specific calling-station opponent identified — the change is theory-driven, not data-driven. No analysis showing turn value underbetting as a confirmed leak pattern.; Experience pool refs: 'Offensive primitives ... each exhausts. Pre-v146 + v149-v151 = 0 fires in trustworthy telemetry → PENDING-VERIFICATION' — v160 inherits this risk; TURN_VALUE_FLOOR firing is UNVERIFIED., 'choose_raise() sizing/constants [POSSIBLY EXHAUSTED] — EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating' — v160 qualifies for exemption (new LOW-direction gating on fold_to_bet_turn)., 'NEW detectors require 6 BIRTH REQUIREMENTS: ... ≥3 wired dispatch sites' — v160 has only 2 dispatch sites (L1394, L1833), below the mandate.; Diff refs: strategy_helpers.py L371-414: new _turn_value_extraction_floor() — continuous delta 0.15*call_stickiness*confidence + 0.04 dynamic bonus, gated round_idx==2 + tier in (thin,strong) + made≥0.45 + conf≥0.15 + fold_turn≤0.42., strategy.py L290-291: delta added to ratio AFTER thin_cap clamp (same pattern as v149 street_fold_boost at L284-285) — non-overlapping inverse condition ensures v149 and v160 never co-fire., strategy.py L1394+L1833: exactly 2 dispatch sites (to_call>0 and to_call==0 paths).
- **v159**: Firing verification must use code-reachability proxy (get_action() harness), NOT stderr grep — battle.py _PersistentBot.call() is stdout-only and drops stderr telemetry entirely (A1 rootcause, reconfirmed v159 critic). Calldown-derived signals need n≥3 samples before licensing aggressive slopes; n≥2 + conf≥0.20 admits coincidental noise.
- **v159**: river_call_size_ratio is a NEW opp-signal (added to PARAMETER_TUNING EXEMPT list); v159 implements v158's recommended pivot — replaced the two-band made_str=0.55 cliff with a continuous license formula `0.45 + (0.15 + _license*0.25)*t`, removing the exploitable 10pp jump.
- **v159 归档建议**: Verify via code-reachability that RIVER_VALUE_TIER license_milli>0 fires ≥5% at daemon≥30g, then confirm v159 holds v157's 54.2% WR over ≥100g vs v130/v139/v148 — if river_call_size_ratio rarely reaches ≥0.70 in 70-hand HU, the strong 0.45→0.85x slope is effectively dead and the 0.45→0.60x conservative path becomes the whole story.
- **v158 (direction superseded by v159)**: PARAMETER_TUNING EXHAUSTED on the choose_raise river-value-sizing axis (v134/v140/v141/v143/v158 = 4 primitives). If v159's continuous license still shows <5% firing OR no WR lift vs v130/v139/v148 at ≥100g, gate the strong band on the river_call_size_ratio signal so the 0.65-0.85x sizing only fires vs confirmed calling-stations.
- **v157**: Continuous call_margin integration (additive margin on the LIVE to_call>0 EV path) is the structurally-valid fold-side path — passes the dir-audit fold-side ban that EXHAUSTED binary `return -1` gates. Before claiming it fires, verify the wire sits UPSTREAM of any early-return (shadowed placement was the v138/v139 failure mode).
- **v157 归档建议**: Daemon ≥100g grep `CALLDOWN_TAX_FINAL reason=tax_fired` vs v130/v139/v148 — <5% firing ⇒ opponent.py may not populate `opp_prior_postflop_raise_count` on the to_call>0 path; firing-but-no-WR-lift ⇒ band peak 0.46 too aggressive, raise floor 0.30→0.35.

