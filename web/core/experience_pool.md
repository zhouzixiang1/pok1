## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip) need ≥30g confidence gates; sub-30g is noise. SB-open/BB-defense uses open-response evidence, not generic confidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation at should_fold_postflop (check_raise_pressure / barrel_pressure_profile) [POSSIBLY EXHAUSTED] — v127/v128/v130 added gates; detectors NOW LIVE in HEAD. v131 rewired them to `pot_odds_required + severity` = corrective CALIBRATION, NOT accumulation. Open mandate = firing verification via persistent fixture, not a 3rd detector.
- v129 turn_second_barrel_planner is a VALIDATED OFFENSE direction but lives ONLY in bots/claude_v129/ (ABSENT from HEAD lineage v118→v130→v131). Re-using it is a RE-IMPORT task requiring the 6 birth requirements, NOT a continuation; re-verify live Glicko + ≥100g H2H before quoting (all v129 H2H ≤30g = noise — cannot claim "strongest" yet).
- facing_barrel_continuation no longer in HEAD v131 (grep=0) — re-grep HEAD before anchoring ANY lineage symbol; reaped/deleted lineage bots (v91/v104/v108/v111/v113) have untrustworthy stale anchors.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Detection-without-handler is recurring dead code: every new detector must meet canonical 6 birth requirements (BLUFF_CALIBRATION). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. FOLD_GATE_FIRE stderr is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (BLUFF_OPP_THRESHOLD, VALUE_PRESS_THRESHOLD, bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED].
- check_raise_pressure() + barrel_pressure_profile() are NOW LIVE in HEAD; PRIORITY = verify via persistent fixture they FIRE on leak spot_info, not add a 3rd. check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate. Calibration > new functions: targeting empty HAND_CLASS_SCORE bands beats bolting on gates.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). BOUNDARY: crossover is legitimate for genuine gene-merging (v130 v118×v128 imported LIVE detectors); the anti-pattern is reflexive fallback when Master could ship a NEW detector.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v130 critic's "v118 weaknesses" were <30g noise / 0.4889; v129 "regression" was false).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY RULE: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; ensure reviewer diff-summary reconciles against the actual diff. Cited replays (G0H28/G4H12/G2H51) must be cross-checked against experience_pool.md + match_replay/ before building a gen (fabricated/untraceable evidence recurred v127+v129+v130 critic; assume false until verified).
- Lineage/HEAD hygiene: reaped lineage bots' FUNCTIONS can persist in HEAD via crossover (check_raise_pressure/barrel_pressure_profile imported from v128 into v130) — reaped ≠ absent; re-grep HEAD for live symbols before anchoring. Orphan dead-code trap: worker removes import+call site but def lives in a non-target file — expand target_files OR add a post-commit cleanup gate.

## RECENT_LESSONS
- **v132**: Pot-odds-grounded fold-gate rewire pattern now applied TWICE (v131 on v128's inert gates → REGRESSED 0.5706→0.5306@360g and 0.60→0.30 vs v93; v132 on v118's reachable gate → pending). Future attempts on the fold-gate axis MUST ship a stderr firing counter verified in daemon battles AND require ≥100g H2H improvement vs parent before commit — pattern is STRUCTURALLY SUSPECT despite theoretical pot-odds soundness.
- **v132 归档建议**: If v132 doesn't improve H2H vs v93/v95/v96 at ≥100g (parent v130's baseline: 0.60/0.80/0.50), PIVOT OFFENSE: re-import v129's turn_second_barrel_planner (offense axis, not in HEAD, meets 6 birth requirements) rather than attempting a 5th fold-gate tune on the [POSSIBLY EXHAUSTED] defensive axis.
- **v132**: Critic evidence: H2H weaknesses: claude_v118 vs claude_v129: wr=0.4333 g=30 (modern barreler beats base), claude_v118 vs claude_v127: wr=0.4667 g=30 (similar defensive-fold bot), claude_v118 vs claude_v96/v95/v113: wr=0.49-0.50 g=100 (marginal losses to mid-tier aggressive bots), claude_v131 vs claude_v102: wr=0.4667 g=30 (current parent shows same leak), claude_v130 vs claude_v131 direct: 6-4 in 10g (v130 not clearly worse than v131's pot-odds rewire); Experience pool refs: POSTFLOP_STRATEGY: 'Defensive fold-gate accumulation at should_fold_postflop [POSSIBLY EXHAUSTED] — v127/v128/v130 added gates; v131 rewired them to pot_odds_required + severity = corrective CALIBRATION, NOT accumulation. Open mandate = firing verification via persistent fixture.' v132 does the same calibration on a different gate WITHOUT first verifying v131's fold rate vs v130 at ≥30g., v131 归档建议: 'Before iterating defensive gates further, grep daemon match_replay/ for ≥3 actual mid-pair-vs-barreler leak hands from v93/v95/v96/v110 and verify v131 fold rate vs v130 at ≥30g; if unchanged, pivot OFFENSE.', v131 SYNTHETIC FIXTURE LIMITATION: synthetic fixtures verify formula mechanics but NOT real-world reachability; v132 has no real-world firing verification.; Diff refs: strategy_helpers.py: REMOVED check_raise_pressure() + barrel_pressure_profile() (78 lines) — confirms v128's gates are dead/inert., strategy.py:622-648: v118's `opp_value_heavy` gate rewired from `made_strength < 0.28` (turn) / `made_strength < 0.34` (river multi-barrel) to `made_strength < pot_odds_req + sev` (sev=0.04/0.06)., strategy.py: removed 3 wiring sites for v128 gates in jam_buffer and call_margin — cleanup.
- **v131**: INERTNESS RESCUE PATTERN — when a fold gate's fixed threshold absorbs its severity signal (0.42 - severity collapses to baseline), rewire to pot-odds-grounded (pot_odds_required + severity) so the threshold couples to actual bet size; generalizable for future detector gates.
- **v131**: SYNTHETIC FIXTURE LIMITATION — synthetic spot_info fixtures verify formula mechanics but NOT real-world reachability; a per-gen persistent fold-gate firing counter (not stderr debug-only FOLD_GATE_FIRE) is required to verify 'fires in practice vs modern-aggro'.
- **v131**: Closed the v128/v130 firing-verification mandate with fold_gate_fixture.py (NEW 6-scenario self-test) + rewired CR gate (strategy.py:638-653) and BP gate (strategy.py:655-677) from `0.42/0.36 - severity` to `pot_odds_required + severity`. Demonstrates 3 NEW folds preserving strong_pair/overpair calls. Corrective CALIBRATION of the exhausted fold-gate axis, NOT accumulation.
- **v131 归档建议**: Before iterating defensive gates further, grep daemon match_replay/ for ≥3 actual mid-pair-vs-barreler leak hands from v93/v95/v96/v110 and verify v131 fold rate vs v130 at ≥30g; if unchanged, pivot OFFENSE — but v129's turn_second_barrel_planner is NOT in HEAD, so that pivot is a RE-IMPORT task meeting the 6 birth requirements, not a continuation.
- **v130**: check_raise_pressure + barrel_pressure_profile imported from v128 (NOT new) wired 3 sites each in should_fold_postflop + jam_buffer/call_margin additive, reachable, gated tier strong/nut early-exit + not has_draw; NEW fold-to-big-bet exits (made<0.50 vs value-heavy opp; made<0.42 unconditional river). Firing-fixture self-test deferred → done in v131. (NOTE: a claimed v130 calling_station_value_amplifier() was FABRICATED — 0 grep hits in v130/v131; never shipped. Treat unverified "wired LIVE" claims as false until grepped.)


