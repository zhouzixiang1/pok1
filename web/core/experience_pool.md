## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip) need ≥30g confidence gates; sub-30g is noise. SB-open/BB-defense uses open-response evidence, not generic confidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate axis at should_fold_postflop [POSSIBLY EXHAUSTED] — v127/v128/v130 added gates (check_raise_pressure/barrel_pressure_profile) that proved INERT/DEAD; v131/v132 rewired to pot_odds-grounded (pot_odds_required + severity) but v131 REGRESSED 0.5706→0.5306@360g. barrel_pressure_profile REMOVED in v132 (grep=0); check_raise_pressure survives only as 1 stale-comment hit @strategy.py:633 (NOT live code). Open mandate = real-world firing verification via persistent fixture, NOT a 3rd detector or 5th threshold tune.
- v129 turn_second_barrel_planner is the VALIDATED OFFENSE pivot — NOW PROVEN by evidence: 900g, Glicko r=1513 rd=79 (confident), wins 5/6 ≥30g modern-field matchups (v96 0.60@30g, v95 0.55@40g, v93 0.53@30g, v110 0.55@40g, v113 0.575@40g; loses only v97 0.40@20g). It is the strongest-by-evidence bot. But it lives ONLY in bots/claude_v129/ (ABSENT from HEAD lineage v118→v130→v131→v132), so re-using = RE-IMPORT task requiring the 6 birth requirements, NOT a continuation.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Detection-without-handler is recurring dead code: every new detector must meet the canonical 6 birth requirements (BLUFF_CALIBRATION). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. FOLD_GATE_FIRE stderr is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED]; BLUFF_OPP_THRESHOLD was REMOVED in v132 (0 grep hits) — do not re-add that constant axis.
- Fold-gate axis is INERT & REMOVED in v132; corrective CALIBRATION (pot_odds-grounded thresholds targeting empty HAND_CLASS_SCORE bands) beats bolting on new gate functions, but REQUIRES ≥100g H2H improvement + real firing evidence before commit. check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). BOUNDARY: crossover is legitimate for genuine gene-merging (v130 v118×v128); the anti-pattern is reflexive fallback when Master could ship a NEW detector.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v130 critic "v118 weaknesses" = <30g noise/0.4889; v129 "regression" false; v132 critic H2H refs all ≤30g; v130 calling_station_value_amplifier "wired LIVE" = 0 grep hits, never shipped).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY RULE: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; ensure reviewer diff-summary reconciles against the actual diff. Cited replays (G0H28/G4H12/G2H51) must be cross-checked against experience_pool.md + match_replay/ before building a gen (fabricated/untraceable evidence recurred v127+v129+v130 critic; assume false until verified).
- Lineage/HEAD hygiene: reaped bots' FUNCTIONS can persist in HEAD via crossover OR be REMOVED (check_raise_pressure/barrel_pressure_profile imported v128→v130 then DELETED v132) — re-grep HEAD for live symbols before anchoring; a stale comment ≠ live code. Orphan dead-code trap: worker removes import+call site but def lives in a non-target file — expand target_files OR add a post-commit cleanup gate.

## RECENT_LESSONS
- **v133**: Dispatch-order inertness (v105 lesson, REINFORCED in v133): turn barrel primitives MUST sit at strategy.py:1300, before passive_exploit:1372 — verify firing rate via daemon H2H raise-frequency lift (target ≥40% from ~33.5% baseline), not code-presence grep.
- **v133**: Crossover-with-small-principled-mutation pattern works when the donor primitive has ≥30g-confirmed modern-field edge (v129 r=1526 rd=92, 5/6 wins ≥30g); preferred over new fold-gate tuning once defensive axis is exhausted.
- **v133 归档建议**: After ≥100g daemon H2H vs v129/v118/v131 (NOT v111/v116/v121 — those are 20g noise per Critic), if turn-barrel raise frequency in eligible TPGK/overpair spots stays at ~33.5% baseline, revert the 0.50→0.45 floor to isolate whether the re-import or the mutation is at fault, then wire a second call-site inside choose_raise() to satisfy the ≥3-sites requirement before introducing any new offensive primitive.
- **v133**: Critic evidence: H2H weaknesses: v130 (parent) rated r=1434 rd=38, v129 (donor) rated r=1438 rd=73 — donor IS the stronger bot, justifying the gene transfer, v129 wins 5/6 modern-field matchups ≥30g: v96 0.60@40g, v95 0.56@50g, v93 0.50@40g, v110 0.45@40g, v113 0.425@40g, v118 0.55@40g (per head_to_head.json + experience_pool.md), v130 vs v129 direct: 0.40@20g (small sample, but directionally consistent), WEAKNESS: Docstring claim 'v130 vs v111(0.45)/v116(0.50)/v121(0.50)' is based on only 20g samples — NOISE per experience_pool rule (<30g = noise); the stronger evidence is v129's overall rating edge, not these specific matchups; Experience pool refs: POSTFLOP_STRATEGY: 'v129 turn_second_barrel_planner is the VALIDATED OFFENSE pivot — NOW PROVEN by evidence: 900g, Glicko r=1513 rd=79, wins 5/6 ≥30g modern-field matchups' — directly endorses this re-import, RECENT_LESSONS v132: 'Recommended next pivot: RE-IMPORT v129 turn_second_barrel_planner (now evidence-proven)' — this generation is executing exactly the recommended pivot, POSTFLOP_STRATEGY: 'Defensive fold-gate axis [POSSIBLY EXHAUSTED] — v127/v128/v130 added gates that proved INERT/DEAD' — confirms this offensive pivot is the correct escape from the deadlock; Diff refs: strategy_helpers.py:397-477 NEW turn_second_barrel_planner() — 4 gates (round_idx/to_call, flop-aggressor, value_eligible, non-deteriorated board) + sizing math with confidence-gated opponent_model boosts, strategy.py:35 added import; strategy.py:1300-1315 single dispatch site, correctly placed BEFORE passive_exploit (1372) and thin_static_showdown_control (1316), Mutation at strategy_helpers.py:446: made_strength >= 0.45 (was 0.50) for top_pair_good_kicker/overpair; two-pair+ branch unchanged
- **v133** (Master): added OFFENSIVE value-tier floor import to thin_cap (strategy.py:251-271) — restructured 1 unconditional line into a 21-line structural gate: dry_static_board (wetness≤0.25 AND not paired) + passive_station (passivity≥0.55 AND conf≥0.20 AND not paired) → release to 0.52/0.58/0.62; else preserve 0.30/0.38 unchanged. Fits the PARAMETER_TUNING EXEMPT clause (NEW passivity_score signal, LIVE in opponent.py:229, ABSENT from v132). Different axis than fold-gate (sizing) — the 5th-fold-gate-tune warning does NOT apply. Critic H2H refs all ≤10g = noise; v132 Glicko r=1421 trails v118=1509/v129=1477/v131=1473 (rd=292, regressing pattern but unconfirmed).
- **v132** (offense-pivot prep): removed barrel_pressure_profile() + check_raise_pressure() wiring (78 lines, 3 sites) as dead/inert — barrel_pressure_profile grep=0 (fully gone); check_raise_pressure only a stale comment @strategy.py:633. v132 rd=350 UNRATED → no H2H signal yet. pot_odds-grounded fold-gate rewire now applied 2× (v131, v132) and BOTH are STRUCTURALLY SUSPECT — future fold-gate attempts MUST ship a persistent firing counter verified in daemon battles AND require ≥100g H2H improvement vs parent before commit. Recommended next pivot: RE-IMPORT v129 turn_second_barrel_planner (now evidence-proven, see POSTFLOP_STRATEGY) rather than a 5th fold-gate tune; this deprioritizes the check_raise_freq directive (9+ gens undelivered).
- **v131**: INERTNESS RESCUE PATTERN — when a fold gate's fixed threshold absorbs its severity signal (0.42 - sev collapses to baseline), rewire to pot_odds-grounded (pot_odds_required + severity) so it couples to actual bet size; generalizable for future detector gates BUT PROVED STRUCTURALLY SUSPECT after REGRESSION @360g (0.5706→0.5306; v131 r=1396 rd=107 = weak/unmeasured). Synthetic fixtures verify formula mechanics NOT real-world reachability — a persistent fold-gate firing counter is required (FOLD_GATE_FIRE stderr is debug-only).


