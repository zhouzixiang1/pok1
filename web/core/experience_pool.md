## OPPONENT_MODELING
- Opponent signals (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning — cite SYMBOLS/PATHS, NEVER line numbers (they drift) or commit hashes. rd=350 = Glicko DEFAULT/unrated, not "live top".
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20). NEW gating with it = PARAMETER_TUNING EXEMPT only for genuinely new gating logic, not threshold nudges.
- Detector firing verification: grep symbols in `web/core/results/bot_telemetry/{bot}.jsonl` (ONLY v146+ trustworthy; v137-v145 = pre-Phase-A-fix daemon, 0 markers by construction). reachability_test.py (full get_action() harness) proves CODE-PATH execution only, NOT in-game firing — a working pipeline with 0 marker fires means thresholds don't bind. Verdict: 0 telemetry fires = PENDING-VERIFICATION, NOT verified.

## POSTFLOP_STRATEGY
- **PLACEMENT SHADOW INVARIANT (HARD RULE)**: any fold/commitment guard's dispatch call-site MUST be BEFORE/inside the `to_call >= my_chips` early-return block, NEVER after it (else unreachable for stack-covering all-ins = the -15.5k/-20k cases). Fix = RELOCATE call-site, do NOT re-tune thresholds. Closed after 7 gens (v147 SPR gate wired inside early-return); when stderr never fires, FIRST verify code-path reachability vs the early-return chain.
- Defensive fold-side/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v135 REGRESSED; v138/v139 structurally INERT; v147/v148 SPR gate = SAME exhausted axis (v149 dir-audit: bot OVER-FOLDS preflop 99% fold-to-bet, does NOT over-commit; fold-side made the leak WORSE → PIVOTED TO OFFENSE). Do NOT extend/re-calibrate SPR gate or any fold-side threshold on persistent stack-offs.
- Defensive `_river_*_guard` family [POSSIBLY EXHAUSTED] — v138/139 call-side FOLD gates, v140/143 bet-side own-jam BLOCKERS; ALL DEFENSIVE, exhausted per v144 dir-audit. The bet-side OFFENSE primitives (`_river_value_extraction_amplifier` v141, `_vulnerable_made_protection_floor` v142, `_street_fold_exploit_sizing_boost` v149) are per-axis open BUT currently 0 fires (PENDING-VERIFICATION).
- Dispatch-order shadow: wire offensive primitives AFTER downstream tiers (turn_second_barrel_planner, river_value_raise_tier); high-conf fires FIRST (vm_overbet@0.75), lower AFTER (RVA sticky@0.55), baseline LAST. Fix: RELOCATE call-site, don't re-tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- ENFORCED STANDARD: NEW detectors MUST prove in-game firing via persistent fixture/self-test exercising the ACTUAL DISPATCH SITE + a telemetry marker in bot_telemetry/{bot}.jsonl (v146+ only). CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable DISPATCH sites + ≥3 cited on-disk replay folds + ≥30g confidence gate + persistent fixture firing logs. fn_refs count (import + N call-sites) ≠ dispatch sites — count call-site DISPATCH POINTS where the return value is consumed.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- Offensive primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn 2nd-barrel, blocker-bluff, street-fold-exploit boost) remain open PER AXIS but each exhausts. pre-v146 offense detectors (v134/v141/v144/v145) + v149/v150 street-fold-exploit = 0 fires in trustworthy telemetry → PENDING-VERIFICATION, NOT proven-LIVE. Tiers: no consumers=dead; consumers but no firing proof=PENDING-VERIFICATION; firing proven=LIVE. Do NOT equate code-reachability with firing proof.
- `_street_fold_exploit_sizing_boost` [POSSIBLY EXHAUSTED] — v149/v150 (2 gens) 0 telemetry fires even after conf 0.20→0.15 / fold 0.55→0.50 relaxation (pipeline WORKS: SIZING_MARGIN_ADJ fires 933× in v150). Per the ≥100g-no-WR-lift pivot rule the mandate is NOW DUE: pivot to a NEW opponent signal (per-street call-down frequencies via classify_sizing_tendency on donk/probe paths), not further threshold tuning.
- preflop opponent-sizing-delta pattern (additive choose_raise BB delta, gated by single opp signal) [POSSIBLY EXHAUSTED] — v144/v145/v146 (3 gens) no WR lift; do NOT add a 4th preflop variant; require ≥100g H2H. open_response_conf≤0.25 risks inertness; sticky (reduce) + limp-heavy (increase) branches cancel for mixed-profile opps — gate mutual-exclusion.
- bluff/line-reading threshold tuning [POSSIBLY EXHAUSTED] — 13+ gens (v138→v150), NO recorded recovery; measurement anchors (v111/v114) REAPED, verdict un-revalidatable. Treat threshold axis as dead; re-anchor to LIVE opponents first.
- check_raise_freq = DEAD directive [POSSIBLY EXHAUSTED] (undelivered 14+ gens v122→v150, never scheduled). Revive only with NEW detector + grep-verified LIVE `opp_current_round_check_count` + birth reqs.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index, fold_to_open_preflop, threebet_vs_open) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opp signal added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master RELIABLE at PLAN-GENERATION (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed) — don't reflexively fall back to crossover. Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opp-line signal + birth reqs = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v150 critic refs all sub-30g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it. Wait ≥100g daemon H2H before cutting either side.
- Trust git diff over commit messages and Master plans (master+worker fallback can masquerade as crossover); direct H2H authoritative over transitive chains.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE systemic (11th recurrence v127-v149; cited G#H# IDs DON'T EXIST — files are timestamp-based, e.g. 20260620_*.json; v149 docstring repeated unverifiable n=1482 claim). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; hard-gate cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v151**: Critic evidence: H2H weaknesses: v150 weakest (sub-30g noise per experience-pool rule): vs v148 0.30, vs v149 0.30, vs v132/v147/v130/v146/v126/v145 all 0.40; overall WR 0.50 over 260g. No single nemesis with ≥30g evidence — plateau pattern.; Experience pool refs: v150 AXIS PIVOT DUE: 'next Master MUST pivot to a NEW opponent signal (per-street call-down frequencies from classify_sizing_tendency) on the donk/probe paths — the only structurally novel lever left on postflop offense.' v151 implements exactly this signal., CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable DISPATCH sites + ≥3 cited on-disk replay folds + ≥30g confidence gate + persistent fixture firing logs. v151 meets 3/6 (detector, signal, conf gate) but misses ≥3 dispatch sites, fixture, replays., BLUFF_CALIBRATION: 'Bluff only with explicit fold-equity evidence + confidence' — v151's fold-rate-grounded sizing satisfies this.; Diff refs: strategy_helpers.py:309-361 NEW _delayed_calldown_bluff(): 6 gates (to_call==0, round∈{2,3}, made<0.28, draw<0.20, conf≥0.20, samples≥4/3) + fold-equity-grounded sizing 0.50+0.10×clamp((fold-0.50)/0.30)., opponent.py:178-274 NEW _pending_my_bet_ratio + _calldown_samples tracking; 324-342 builds per-street calldown_profile., strategy.py:1468-1480 single dispatch wire BEFORE bad_river_bluff_candidate (avoids shadow).
- **v150**: reachability_test.py (full get_action() harness) is the authoritative CODE-PATH-reachability proxy (boosted=232 proves the boost flows through), BUT it does NOT prove in-game firing — it is force-fed. Live telemetry: 0 STREET_FOLD_BOOST/VM_OVERBET/SPR_COMMIT/RIVER_STACKOFF fires while SIZING_MARGIN_ADJ fires 933× (pipeline works) → gating thresholds genuinely don't trigger. Use reachability_test for path-reachability, telemetry for firing; never conflate the two.
- **v150**: AXIS PIVOT DUE — `_street_fold_exploit_sizing_boost` is now 2 gens old (v149→v150) with 0 telemetry fires even after conf 0.20→0.15 / fold 0.55→0.50 relaxation. Per the no-≥100g-WR-lift pivot rule, the next Master MUST pivot to a NEW opponent signal (per-street call-down frequencies from classify_sizing_tendency) on the donk/probe paths — the only structurally novel lever left on postflop offense.
- **v149 STRATEGIC PIVOT (HIGH-VALUE)**: dir-audit HIGH OVERRULED match-analysis fold-gate rec — bot OVER-FOLDS PREFLOP (99% fold-to-bet vs v132/v144), does NOT over-commit. Fold-side v138-v148 lineage (incl. SPR gate) is EXHAUSTED and made the leak WORSE → PIVOTED TO OFFENSE. Do NOT re-engage fold/SPR calibration on persistent stack-offs.
- **v149**: `_street_fold_exploit_sizing_boost` = PENDING-VERIFICATION (0 telemetry fires), a distinct POSTFLOP continuous-boost axis (per-street fold_to_bet + conf → additive choose_raise ratio, suppressed probe/blocker/induce), NOT a 4th preflop variant. v150 added 3rd+4th dispatch sites (donk/probe) + relaxed thresholds = 1 nudge spent; 0 fires confirms thresholds don't bind in-game.
- **v148 归档建议**: classify_sizing_tendency opp-aware +0.03 fold margin (Critic #3 mandate) UNADDRESSED across v147+v148 — given the v149 fold-side-exhaustion pivot, treat as OFFENSE-side opp-aware calibration, NOT fold-side SPR tuning. v148 flop/turn draw thresholds (0.12/0.15) are ad-hoc, NOT outs×2-derived (9-out FD ≈0.36 flop).

