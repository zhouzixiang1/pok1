## OPPONENT_MODELING
- Smoothed archetype axis (classify_archetype via smooth_rate) is CLOSED — saturates to 'standard' (v184 rock-fold INERT); respect ≥0.15 confidence floor. Prefer DIRECT deal-local history signals (v192 `_multibarrel_line_fold`, v193 `_opp_betsize_polarity`) — deal-local OPEN, archetype gating CLOSED.
- calldown_profile sample trap: foldy opps never reach n≥4 — use empirical rate at n≥3, fall back to pool-wide fold_to_raise when per-street samples<2.
- `large_bet_ratio` is RAW (no smooth_rate wrapper); verify the read site before treating the raw-warning as live.
- `_opp_betsize_polarity` (v193, n≥4) IS operational — v196 proved the hard-coded ratio=2.0 feeds ONLY the unread avg_fraction field; polarity uses the n_shove COUNT via a True flag, so the signal is NOT corrupted. "Restore v194 ratio" is NOT a blocker (disproven premise); the fold-side PAYOFF stays OPEN.
- Crossover silently drops recent strategy.py work — prefer highest-rated parent's strategy.py, not arbitrary source_v.

## POSTFLOP_STRATEGY
- Made-strength table (authoritative): pair≈0.22, two-pair≈0.40, trips≈0.58. pot_odds-vs-raw-made_strength gates are INERT by construction (ordinal > pot_odds~0.27) — any such gate MUST use polarized equity (made×discount) or true_equity (v188/v189). Over-call leak band 0.20≤made<0.45.
- **Margin-additive pot-odds-vs-equity gates are INERT for the UPPER leak band (made 0.40-0.50)**: the delta cap ~0.25 cannot overcome monte_carlo realized_rate ~0.50-0.65. v196 unblocked the #1 -20k/0%-fold leak by switching to a DIRECT bare `if _river_pe_delta>0: return -1` (strategy.py L1619-1627) — the FIRST LIVE fold attack. This SUPERSEDES the old "bare `return -1` dead 13+ gens; continuous margins safer" rule: direct binary folds ARE correct once reachability confirms the band fires.
- Birth mandate (4+ gen RECURRING defect v182/v185/v191/v193): wire offensive primitives with ≥3 LIVE dispatch sites AT BIRTH. NEW detectors need: new function + new opp-line signal + ≥3 LIVE dispatch sites + reachability/H2H-WR confirmation at ≥30g + persistent fixture logs.
- CAP CONSTRAINTS (v196 current): opponent.py ~1449/1500 BASE (headroom reclaimed, NO longer binding); strategy.py ~2464/2500 (~36 headroom); strategy_helpers.py 2500/2500 EXACT CAP (untargetable); state.py/postflop.py CONSUMED by v187.
- `_opp_betsize_polarity` fold-side PAYOFF OPEN: apply to fold-side calibration (underbettor=thin-value → AVOID over-fold turn/river to_call>0), NOT the exhausted value-lead/to_call==0 axis. @≥30g run reachability_test on v172/v173/v174; if 'standard', pivot to fold-side calibration.
- Offense value-sizing-UP (value-lead/turn-thin-value/SPR-ship/c-bet-range-advantage) — saturated, NET WR flat (~0.48-0.52): +20k wins offset by fold-side leaks. Pause expansion. [POSSIBLY EXHAUSTED]
- -20k/0%-Fold stack-off leak (~20+ gens, #1 weakness): shipped-primitive TUNING produced NO WR lift until v196's direct-fold rewire (FIRST structural attack, pending ≥30g validation vs v174/v169/v176). v188/v189/v190 calibration + opponent-adaptive discount + v184 rock-fold all INERT. [STALE — no WR-lift]

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence; low aggression/passivity alone may signal a calling-station.
- Offense BLUFF axis (board-texture bluff raise v185→v191): dispatch LIVE but the strategic axis is EXHAUSTED (diminishing returns), PAUSED pending ≥100g WR-lift validation. [POSSIBLY EXHAUSTED]

## PARAMETER_TUNING
- choose_raise() constant-only nudges [POSSIBLY EXHAUSTED] — saturated ≥6 gens. EXEMPT only for structural rewrites adding NEW DEAL-LOCAL opponent-signal gating; CLOSED archetype axis does NOT reopen.
- Don't carry kept-but-inert constants: RAISE to bind or REMOVE the dead bound.
- Preflop pot_odds windows <10pp rarely fire in 70-hand HU; widen_threshold must target ≥15pp bands.
- pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44 → use bet_ratio/0.75 direct scaling so gates differentiate bet sizes.
- Firing verification: reachability_test + ≥100g H2H WR-lift is the ONLY reliable gate. stderr NOT readable (_PersistentBot stdout-only); daemon-grep "fired≥5%" UNFULFILLABLE — the gate must be reachability/H2H-WR, NOT a grep count.

## GENERAL
- Master is RELIABLE at plan-generation but reliability ≠ correctness: validate the axis PAYLOAD (≥100g WR-lift), not just plan cleanliness.
- Dead-code/guard removal > adding constants — logic fixes yield higher EV per line than margin tweaks. Plateau (WR ~0.50): pursue a NEW structural axis, NOT tighter margins.
- Validation thresholds: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g to declare success.
- Trust git diff over commit messages and Master plans; direct H2H authoritative over transitive chains. Do NOT base work on unvalidated bots (no .completed / no Glicko rating).
- Critic advisory ≤4.0 with local_optima_warning=true on an exhausted axis mandates a direction_audit pivot — advisory doesn't gate commit (precommit authoritative) but mandates pivot enforcement.
- Workers MUST grep their own dispatch sites + run reachability_test to confirm every param/signal gates an outcome before claiming coverage.
- If ALL active axes are validation-paused, the ONLY permitted direction is a NEW deal-local opponent-history detector axis not yet attempted — do NOT re-tune blocked axes.

## RECENT_LESSONS
- **v197**: Critic evidence: H2H weaknesses: v177 vs v193: 0.700 (v193 loses at 0.300, 10g — NOISE per <30g rule), v159 vs v193: 0.600 (v193 loses at 0.400, 10g — NOISE per <30g rule), v177 vs v179: 0.425 (v179 wins at 0.575, 40g — SOLID but transitive/different baseline), v159 vs v179: 0.433 (v179 wins at 0.567, 30g — SOLID but transitive/different baseline), v179 vs v193: 0.600 (v193 beats v179, 10g — contradicts 'v179 superior' transitive chain); Experience pool refs: POSTFLOP_STRATEGY: 'Margin-additive pot-odds-vs-equity gates are INERT for the UPPER leak band (made 0.40-0.50) — delta cap ~0.25 cannot overcome realized_rate ~0.50-0.65. v196 unblocked the #1 -20k/0%-fold leak by switching to a DIRECT bare if _river_pe_delta>0: return -1 — the FIRST LIVE fold attack. SUPERSEDES the old continuous-margins-safer rule.' → v197 reverts this., POSTFLOP_STRATEGY: '_multibarrel_line_fold (v192) DIRECT deal-local history signals — OPEN axis' → v197 removes the non-all-in dispatch of a LIVE OPEN-axis signal without reachability proof it over-fires., PARAMETER_TUNING: 'choose_raise() constant-only nudges [POSSIBLY EXHAUSTED]'; 'pot_odds-scaled deltas with low caps (≤0.06) saturate when made≤0.44' → 0.48→0.42 threshold tweak is a margin-tweak on an inert gate.; Diff refs: strategy.py L13: import pruned — _aggro_bluffcatcher_should_fold / _rock_value_bet_fold removed, _multibarrel_line_fold retained (still dispatched at L1577 all-in block)., strategy.py L1585-1595: the 3 non-all-in binary fold dispatches in to_call>0 block replaced with a prose comment. CRITICALLY: no DIRECT _river_potodds_equity_margin return here (v196's L1619-1627 direct fold is ABSENT — v193 never had it)., strategy.py L1577-1583: all-in _multibarrel_line_fold RETAINED for stack-covering -20k protection (correct).
- **v196**: Margin-additive pot-odds-vs-equity gates with delta caps become INERT for the upper leak band (made 0.40-0.50) when realized_rate ~0.55-0.65 exceeds the +0.25 cap — switch to a DIRECT binary fold (strategy.py L1619-1627) once reachability confirms the band is firing.
- **v196**: Comment-only LOC-condensation tasks must NOT touch behavioral code — even a functionally-inert ratio/sample change violates the worker contract and triggers reviewer scope-drift flags (reviewer also caught a wrong "shove_rate permanently 0" comment — it was already nonzero via v195's elif-allin-branch fix).
- **v196 归档建议**: At ≥30g verify v196 vs v179 (parent) lifts WR vs leak-source nemeses v174/v169/v176 without over-folding weak two-pair (made~0.40, inside [0.20,0.50)) vs standard v167/v178 (WR~0.40 in v195); if regresses, restore margin-additive as a SECONDARY check that still lets anti_lock_can_continue/strong_made_continue fire, OR tighten leak band to made[0.20,0.40).
- **v195**: Reachability verification on LIVE nemeses (not synthetic self-tests) is mandatory before shipping fold-side calibrations keyed to a NEW detector — v195's underbettor floor was cosmetic if v172/v173/v174 classify 'standard'/'overbettor' in actual play; @≥30g run reachability_test, widen detector (n≥3 or small_rate≥0.50) or pivot if not 'underbettor'.
- **v194**: Wire the dormant `facing_villain_4bet=True` path in `_preflop_spr_commitment_gate` (detect opp_round_raises>=2 preflop) — folding marginal hands (22-66/AQo/AJo/KQo, MARG_HI=0.62 band) to a 4-bet is the highest-EV part of this gate and a direct attack on the -20k/0%-fold leak. A param never passed True at its only call site is dead code.
- **v194/v195**: Each action_type discriminator (raise/allin) must live in its OWN branch — nesting 'allin' inside 'raise' makes shove_rate permanently 0 (fixed v194, kept by v195's elif-allin branch). The separate hard-coded ratio=2.0 was NOT a signal-corrupter (v196 disproved: ratio feeds only unread avg_fraction).

