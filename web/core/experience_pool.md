## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity) need ≥30g confidence gates; sub-30g is noise; never default-classify unknown openers as tight. SB-open/BB-defense adaptation uses open-response evidence (open_response_samples, pfr/vpip), not generic confidence.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths, NEVER commit hashes (rot on rebase). Master plans must cite weakest matchups with concrete ≥30g H2H evidence; don't fabricate. rd=350 = Glicko DEFAULT/unrated, not "live top".

## POSTFLOP_STRATEGY
- Defensive fold-gate accumulation as a DIRECTION [POSSIBLY EXHAUSTED] — offense pivot executed (v129 turn_second_barrel_planner) but REGRESSED ~130 Glicko (r~1487) from v118 (r~1616); adding MORE parallel detectors is not mandated, fix INERT ones via call-site RELOCATION / spot_info-population fix.
- facing_barrel_continuation repeated re-imports across crossover gens [POSSIBLY EXHAUSTED] — re-grep live HEAD before any anchor; lineage bots were reaped/deleted, don't trust stale version/line anchors.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED]; structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Detection-without-handler is recurring dead code: every new detector must meet canonical 6 birth requirements (BLUFF_CALIBRATION). Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. FOLD_GATE_FIRE stderr is debug-only (daemon never captures); req #6 requires a persistent fixture.
- bluff/line-reading threshold tuning (BLUFF_OPP_THRESHOLD, VALUE_PRESS_THRESHOLD, bluff_heavy_call_widen baseline/slope/cap) [POSSIBLY EXHAUSTED].
- Fold-gate detector STATUS: v127 `check_raise_pressure()` + v128 `barrel_pressure_profile()` exist ONLY in reaped sibling lineages — historical, NOT actionable in current HEAD; re-grep HEAD before anchoring. NEXT: make existing detectors FIRE via persistent fixture, not add a 3rd. `check_raise_freq` is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate. Calibration > new functions: targeting empty HAND_CLASS_SCORE bands beats bolting on gates.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds 0.45/0.50/0.82, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (e.g. passivity_score) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE — do NOT default to crossover fallback when Master can deliver a mandated NEW detector (success-return bug FIXED with regression test; false-positive "malformed JSON" log no longer means plan failed).
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding any exhausted feature; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H numbers recurred (v129 critic invented ≥30g weaknesses; real data all ≤30g with v129 mostly winning).
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — BOUNDARY RULE: same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, the trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- Orphan dead-code trap: worker removes import+call site but def lives in a non-target file — expand target_files OR add a post-commit cleanup gate that auto-strips orphaned defs.
- Critic-loop traps: treat LOWER score authoritative when it cites missing evidence; verify high-swing replay claims vs real net-chip deltas; ensure reviewer diff-summary reconciles against the actual diff.
- Lineage/HEAD hygiene: lineage bots get reaped/deleted so stale version/line anchors rot fast. Re-verify the live HEAD tag before any grep/line anchor — v130 has NO commit/tag in recent outcomes (mid-gen/uncommitted), and v127/v128 are reaped sibling lineages; don't anchor future work to them, and don't assume v130 branched from a regressed v129 rather than v118 without checking.

## RECENT_LESSONS
- **v130**: Critic evidence: H2H weaknesses: v118 vs v129: 20g, wr=0.40 (only sub-0.45 opp; <30g=noise per experience pool), v118 vs v97/v96: 90g each, wr=0.4889 (both essentially coin-flip, not weakness), v118 WINS vs v93 (0.5375/80g), v79 (0.5778/90g), v102=NO DATA — contradicts code-comment rationale; Experience pool refs: POSTFLOP fold-gate accumulation [POSSIBLY EXHAUSTED] — 'adding MORE parallel detectors is not mandated, fix INERT ones via call-site RELOCATION', Crossover-as-default fallback [POSSIBLY EXHAUSTED] — 'same-fn re-import = exhausted axis & FORBIDDEN for defensive genes' (v127+v128+v130 all add should_fold_postflop fold gates), v128 MANDATORY pre-commit: assert via FIXTURE SELF-TEST that detector returns severity>0 on leak spot_info (not done — no fixture/test added); Diff refs: strategy_helpers.py:319-394 NEW check_raise_pressure + barrel_pressure_profile (imported from v128, NOT new), strategy.py:632-662 NEW fold gates in should_fold_postflop — reachable, gated by tier strong/nut early-exit + not has_draw, strategy.py:887-896 + 990-999 jam_buffer/call_margin += cr_severity/bp_severity (additive, capped upstream)
- **v130** (HEAD UNCONFIRMED — no v130 tag in recent outcomes; re-verify commit before anchoring): strategy.py:632-651 NEW fold-to-big-bet block in should_fold_postflop() — 2 NEW fold exits (made<0.50 vs value-heavy opp; made<0.42 unconditional river) in a previously uncovered band (old max fold threshold 0.40). Call site line 1047 reachable, gated `not anti_lock_call_continue and not strong_made_continue` so nut/lock hands NOT folded. Targets battle_experience #3 root cause "River NON-allin call-downs 69-91%"; sizing-only changes don't fix it, fold exits do.
- **v130**: calling_station_value_amplifier() NEW in strategy_helpers (3-tier: confidence≥0.20, passivity≥0.70, postflop_aggr≤0.32; boost thin:0.0/strong:0.10/nut:0.15), wired LIVE at END of choose_raise() after all caps (strategy.py:277-287). PARAMETER_TUNING exemption (new opponent-signal gating = new axis). RISK: confidence≥0.20 gate may BLOCK it on v129's modern-aggressive weaknesses (v109 0.400/20g, v87/v102/v123 0.450/20g); passive opps it'd fire on (v25/v77/v81) are already won 0.60-0.85 — unverified it fires on any real weakness.
- **v129 (bot-v129@3fc3b8e, turn_second_barrel_planner from v118)**: REGRESSED ~130 Glicko (r~1487, ~16th) vs v118 (r~1616); ALL v129 H2H ≤30g (max 30g vs v107) → confidence gate unproven, treat as UNVALIDATED until ≥100g attribution. Rewire turn_second_barrel_planner into choose_raise() as sizing modifier (avoid shadowing value-bet path line 1247-1259) AND cite ≥3 match_replay/ JSONs (1820 available) where v118 checked back turn-after-flop-raise with TPGK+ on non-dynamic boards (target v108/v110/v96), OR pivot to fire on weak-made (made<0.30) bluff-second-barrel when opp fold_turn≥0.60. Clean up UNUSED `value_profile` param.
- **v129 hygiene**: Reviewer diff stat '14 files / 4894 insertions' for a 70-line function = reaped-bot-deletion/unrelated churn contaminating commits — workers must scope git adds to only modified files, or commit_bot should diff only the new bot directory.
- **v128**: MANDATORY pre-commit verification — before approving any fold-gate detector reusing check_raise_pressure/barrel_pressure architecture, assert via FIXTURE SELF-TEST (NOT FOLD_GATE_FIRE stderr — debug-only, daemon never captures) that the function returns severity>0 on a spot_info matching the targeted leak, else likely INERT (2 detectors at risk).
- **v128**: Replay citation integrity — cited replays (G0H28/G4H12/G2H51) must be cross-checked against experience_pool.md + match_replay/ before building a gen; fabricated/untraceable evidence cannot serve as fold-gate rationale (recurred v127 + v129 critic — assume false until verified).

