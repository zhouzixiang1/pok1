## OPPONENT_MODELING
- Live opponent stats (postflop_aggr, fold_to_raise, per-street fold/call-down, passivity, pfr/vpip, value_maximizer_index, classify_sizing_tendency) need ≥30g confidence gates; sub-30g = noise. SB-open/BB-defense uses open-response evidence; never default-classify unknown openers as tight.
- Grep-verify every claimed primitive is LIVE in HEAD before planning; cite symbols/paths (NOT line numbers — they drift), NEVER commit hashes (rot on rebase). rd=350 = Glicko DEFAULT/unrated, not "live top". Re-grep HEAD before anchoring — reaped bots' functions persist/toggle across gens.
- value_maximizer_index = clamp(call_down_flop_turn*0.25 + call_down_turn_river*0.35 + turn_sticky*0.20 + river_sticky*0.20); ESTABLISHED primitive code-present since v134 (no longer novel). Gating with it counts as PARAMETER_TUNING EXEMPT only when adding genuinely new gating logic, not threshold nudges.
- classify_sizing_tendency() (v137) = 4-bucket overbet/underbet classifier, a sizing-aware signal, but shipped with NO firing fixture → presumed-INERT (unresolved through v138). ACTION: add persistent fixture/self-test to prove firing, or remove as dead code; do not record as a tombstone.

## POSTFLOP_STRATEGY
- Defensive fold/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires were on branches absent in HEAD and REGRESSED (0.5706→0.5306@360g); `_spr_commitment_gate`/`_single_reraise_stackoff_guard` still absent. v138 re-added `_river_stackoff_guard` WITH fixture (req #6 first met locally) but placed AFTER the `to_call>=my_chips` early-return → structurally INERT on true all-ins. Axis exhaustion CONFIRMED by v138 repeating a placement miss; future gates MUST cite the strategy.py:990 early-return ordering constraint and place BEFORE it. Need ≥100g H2H after placement fix.
- v129 turn_second_barrel_planner = code-present OFFENSE pivot (since v133), effectiveness UNCONFIRMED: v134 wired value_maximizer_overbet BEFORE it (dispatch-order shadow), so it may be partially INERT until ≥100g daemon H2H confirms turn+river raise-freq lift ≥40% from ~33.5% baseline. Do NOT treat as proven-effective.
- barrel_pressure_profile + check_raise_pressure are code-present (RE-WIRED by v133 after v132 removal), NOT firing-verified — "LIVE" means code-present only. Verify via persistent fixture, NOT another detector/threshold tune.
- probe_mode sizing-knob constant tweaks [POSSIBLY EXHAUSTED] (re-grep HEAD before anchoring); structural single-line bypass fixes + NEW probe/donk primitives still permitted.
- broadway_suited bucket is LIVE but EDIT DIRECTION SATURATED [POSSIBLY EXHAUSTED].
- Mandate: NEW detectors MUST prove firing via persistent fixture/self-test. v134/v135/v136/v137/v138 shipped NEW primitives with NO firing verification (v138 first to meet req #6 locally only) = recurring violation. Before modifying behavior, audit action-selection for raw-ratio bypasses, skipped choose_raise, downstream caps, dispatch-order shadowing, trap-guard exclusion lists.

## BLUFF_CALIBRATION
- Bluff only with explicit fold-equity evidence + confidence (PRIMARY); low aggression/passivity alone may signal a calling-station.
- NEW OFFENSIVE primitives (river raise-bluff, line-polarisation barrels, value-heavy donk-bluff, turn second-barrel, blocker-bluff) are NOT subject to POSTFLOP/PARAMETER exhaustion. CANONICAL 6 BIRTH REQUIREMENTS: NEW detector + NEW opponent-line signal + ≥3 wired reachable sites + ≥3 cited replay folds + ≥30g confidence gate + persistent fixture/self-test firing-rate logs — else dead code. stderr telemetry (FOLD_GATE_FIRE/BLOCKER_BLUFF_FIRE/SIZING_*) is debug-only (daemon never captures).
- bluff/line-reading threshold tuning (bluff_heavy_call_widen baseline/slope/cap, BLUFF_OPPORTUNITY_THRESHOLD) [POSSIBLY EXHAUSTED] — live `BLUFF_OPPORTUNITY_THRESHOLD = 0.55` (v136 reverted to v111's 7000g baseline); require ≥100g H2H vs v93/v117/v114 before further tuning; if it doesn't recover ≥half the v111-vs-v134 gap, declare axis dead.
- check_raise_freq is a SEPARATE UNDELIVERED directive (9+ gens) — don't conflate with the fold-gate axis.

## PARAMETER_TUNING
- choose_raise() sizing/constants (value-tier floors, river_value_raise_tier bounds, wetness caps, induce_cap/thin_cap/low_ratio, barrel-continuation caps) [POSSIBLY EXHAUSTED] — saturated ≥6 gens; re-grep HEAD for live symbols. EXEMPT: OFFENSIVE value-tier floor imports adding NEW opponent-signal gating (passivity_score, value_maximizer_index) count as NEW.
- Threshold-only nudges on adjacent gates (pot_odds bounds, value_pressure firing) = constant tuning when no new gating/opponent signal is added.
- Don't carry kept-but-inert constants: if a floor sits below existing base ratios, RAISE it to bind or REMOVE the dead bound (after confirming it exists in HEAD).

## GENERAL
- Master is RELIABLE at PLAN-GENERATION — do NOT reflexively fall back to crossover when Master can deliver a mandated NEW detector (success-return bug FIXED; false-positive "malformed JSON" log ≠ plan failed). NOTE: reliability ≠ firing-fixture compliance — Master reliably produces plans that STILL violate the persistent-fixture mandate (see POSTFLOP); the fixture gate, not Master output, remains the open problem. Crossover legitimate for genuine gene-merging (v130 v118×v128); reflexive fallback = anti-pattern.
- Validation: <30g H2H = noise; ≥30g paired net-chips before re-adding exhausted features; ≥100g H2H to declare success. Re-read glicko_ratings.json + head_to_head.json before quoting — FABRICATED H2H recurred (v130-v138 critic refs all sub-30g; v138 320 games/27 opponents max 20g).
- Do NOT reverse a prior gen's master-planned direction on sub-30g noise — v135 cut v134's offense after 10g; v136 restored it (dropped absent-from-HEAD `_river_stackoff_guard`). Wait ≥100g daemon H2H before cutting either side.
- Trust the git diff over commit messages and Master plans; master+worker fallback can masquerade as crossover.
- Crossover-as-default fallback [POSSIBLY EXHAUSTED] — same-fn/byte-identical re-import = exhausted axis & FORBIDDEN for defensive genes; NEW fn + NEW opponent-line signal + birth requirements = new axis. Pick parents by H2H win-rate + diversity; grep-prove imports absent from base.
- Worker boundaries: Architect defines structural logic, Tuner adjusts constants; constant-only edits to defensive-guard/barrel-continuation fns = same-axis work. Re-verify strategy.py line count each gen (core limit 2000).
- Attribution test: pair candidate H2H vs opponents where donor>base by ≥3pp; if candidate doesn't recover ≥half the gap, trait wasn't the edge. Diff parents holistically; direct H2H authoritative over transitive chains.
- FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (6th recurrence v127/v129/v130/v135/v137/v138) — cited G{N}H{M} replays are non-existent (actual format is timestamp-based). Master/Worker prompts MUST require `ls web/core/results/match_replay/` verification; add a hard gate rejecting cited replays absent on disk. Treat LOWER critic score authoritative when it cites missing evidence.

## RECENT_LESSONS
- **v139**: RECURRING PLACEMENT SHADOW (v138→v139): River stack-off guard at strategy.py:1019 runs AFTER strategy.py:990 `to_call>=my_chips` early-return → never fires on true stack-covering all-ins. Two generations have now band-aided around this (v138 fixture-only, v139 upstream turn sizing) without fixing it. Next Master MUST relocate the guard BEFORE L990 or duplicate inside the all-in branch — upstream mitigations do NOT replace the structural fix.
- **v139**: FIXTURE ≠ LIVE: v139's 8-case _turn_oop_pot_control fixture tests the helper in isolation but does NOT exercise the strategy.py:1407 dispatch wire (reviewer flagged INERTNESS risk). New helpers need at least one test that exercises the actual dispatch site, not just the function directly — mirrors v137's 'presumed-LIVE ≠ verified-LIVE' failure mode.
- **v139 归档建议**: Before adding more defensive mechanisms, FIX v138's placement shadow by relocating _river_stackoff_guard() invocation BEFORE strategy.py:990 early-return (or duplicating inside the all-in branch) so it actually fires on stack-covering river all-ins, then re-verify against v111/v131 H2H where the -15k to -20k stack-off cases persist in precommit.
- **v139**: Critic evidence: H2H weaknesses: All v138 H2H matchups sub-30g noise (mostly 10-20g). v138 weakest: v132 30%(10g), v131/v118/v128/v109/v135/v134 40% (10-30g). Experience pool labels sub-30g as noise; no ≥100g evidence cited to justify targeting turn sizing.; Experience pool refs: POSTFLOP_STRATEGY: 'Defensive fold/commitment-gate axis [POSSIBLY EXHAUSTED] — v127-v130 INERT; v131/v132/v135 rewires REGRESSED'. v139 is the 3rd attempt at river stack-off leak from yet another angle., v138 lesson: 'future gates MUST cite the strategy.py:990 early-return ordering constraint and place BEFORE it' — v139 does NOT cite this and does NOT fix the bug., RECENT_LESSONS: 'FABRICATED/UNTRACEABLE REPLAY EVIDENCE is systemic (6th recurrence)' — v139 fixture is good but no daemon H2H evidence yet.; Diff refs: strategy_helpers.py:592-684 _turn_oop_pot_control() (93 lines NEW). Severity weights 0.40/0.30/0.20/0.25/0.20 arbitrary; sizing 0.30-0.40× pot vs standard 0.50-0.75×., strategy.py:1374-1382 (compute once) + L1407-1408 (dispatch in `round_idx==2 and to_call==0` block, BEFORE _vm_overbet and _turn_barrel)., strategy_helpers.py:738-825 — replaces v138's 10/10 _river_stackoff_guard fixture with 8/8 _turn_oop_pot_control + 2/2 regression. Firing coverage maintained.
- **v138**: STACK-OFF GUARD PLACEMENT INVARIANT — any fold-gate targeting catastrophic river stack-offs MUST fire BEFORE the `if to_call >= my_chips:` early-return at strategy.py:990 (or duplicate inside that branch), else true all-ins bypass it. v138 is the 2nd attempt (after v135's 0.88 miscalibration) and repeats a structural-placement miss; future Master plans must cite the :990 ordering constraint explicitly. 🔴 Fix: relocate invocation before :990 or duplicate in all-in branch; require ≥100g daemon H2H vs v111/v131 (polarized river barrelers) showing -15k/-20k stack-off frequency drops before claiming the 0%-fold leak closed.
- **v138**: Ships `_river_stackoff_guard` fixture (✅ first fold-gate meeting birth req #6: 10/10 local PASS — fires A-high/bottom-pair/weak-one-pair vs allin; correctly skips mid-two-pair/strong/nut/small-bet/overbettor/cheap-call). made_strength 0.88→0.55 floor addresses v135 miscalibration. NO ≥100g H2H yet; all v137 matchups sub-30g noise. '~76 pairs v14-v137' count = 6th unverified-evidence recurrence.
- **v137**: classify_sizing_tendency() defense relaxation placed FIRST in should_fold_postflop (returns False for non-air vs overbettor large bets; absolute_air exclusion made_strength<0.15 AND not has_draw preserves fold discipline; call_margin ±0.025-0.030). NEW opponent-modeling axis, NOT an exhausted fold-gate. 🔴 req #6 NOT met (stderr SIZING_* debug-only) → presumed-INERT. To reach critic ≥8: add 3rd wired site (river value-fold / choose_raise vs underbettor) + persistent firing-rate fixture; verify round-bucket folds aren't short-circuited.
- **v136**: DISPATCH-ORDER SHADOW = authoritative root cause of v134 leaks vs disciplined barrel-aggressive bots (cited matchups sub-30g noise; v111 beats v93 0.5217@230g). Future Master MUST RELOCATE value_maximizer_overbet call-site to AFTER river_value_raise_tier + turn_second_barrel_planner, NOT re-tune the threshold. value_maximizer_overbet (vm_index≥0.75 AND conf≥0.30) + turn_river_blocker_bluff() both 0-3/6 birth reqs (no fixture) = presumed-INERT. BLUFF_OPPORTUNITY_THRESHOLD 0.48→0.55 reverted to v111 7000g baseline.


